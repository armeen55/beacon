# Beacon v1 Core Consolidation — Legacy Map (2026-06-26)

> Phase A deliverable. Ruthless classification of every recommendation/scoring/loader/Profound/DataForSEO/SEMrush/diagnostics path against the unified ActionPack brain (Rank-&-Revenue + Profound coverage + DataForSEO). Generated by a 6-agent parallel code audit; verify before deleting.

**237 modules — KEEP 139 · ADAPT 39 · QUARANTINE 44 · DELETE 15.**

Unified surface: `/diagnostics/action-packs` (ONE ranked worklist; all sources adapt into `domains/action-pack/`). Phase E (kill legacy) is gated on that surface matching/beating the old ones.


## DELETE (15) — dead / obsolete / unsafe / replaced — remove (verify callers first)

- `migrations/2026-06-12_profound_agent_analytics.sql` — DEAD TABLES. sync-nightly.ts writes bot_rows (lines 288) + referral_rows (line 337) but NO consumer reads them in src/. No signal loaders, no triggers
- `src/lib/connectors/semrush/client.ts` — Operator stated SEMrush GONE (replaced by DataForSEO). This is the raw API fetch layer; DataForSEO will provide equivalent domain_organic, keyword_gap
- `src/lib/connectors/semrush/domain-reports.ts` — Parser for raw Semrush reports. Functionally replaceable by DataForSEO parsers. No downstream consumers outside the SEMrush connector stack.
- `src/lib/connectors/semrush/domain-organic.ts` — SEMrush-specific report (domain_organic). DataForSEO provides equivalent SERP/rank data without this intermediate layer.
- `src/lib/connectors/semrush/domain-gap.ts` — SEMrush keyword-gap report. DataForSEO SERP provides competitor rank data; gap detection can be rebuild pure in the trigger layer.
- `src/lib/connectors/semrush/sync-organic-keywords.ts` — SEMrush nightly sync. Replaced by DataForSEO sync. Migration: spin up new DataForSEO sync cron, drop semrush_organic_keywords table after cutover.
- `src/lib/connectors/semrush/persist-domain-metrics.ts` — SEMrush diagnostics-only (operator /diagnostics/semrush page). No production rec logic depends on it. DELETE the operator page + this module once remo
- `src/domains/product/visibility-score-delta.test.ts` — Test for legacy visibility-score. DELETE when visibility-score module is removed.
- `src/domains/product/visibility-score-filter.test.ts` — Test for legacy visibility-score. DELETE when visibility-score module is removed.
- `src/domains/today/visibility-read-model-platform-filter.test.ts` — Test for legacy visibility read-model. DELETE after visibility surfaces deprecated.
- `src/domains/today/visibility-read-model-equivalence.test.ts` — Test validating parity between two legacy compute paths. DELETE once both deprecated.
- `src/domains/today/visibility-read-model-freshness.test.ts` — Test for legacy visibility model. DELETE after deprecation.
- `src/domains/today/visibility-read-model-cache-split.test.ts` — Test for legacy visibility loader. DELETE after visibility model deprecated.
- `src/app/(shell)/diagnostics/semrush/page.tsx` — Operator-only diagnostic. SEMrush GONE (replaced by DataForSEO). DELETE this page + all SEMrush-specific diagnostic UI.
- `src/app/(shell)/diagnostics/semrush/actions.ts` — Semrush connector actions. DELETE once SEMrush connector removed.

## QUARANTINE (44) — operator-only legacy — keep but isolate; delete after the unified surface proves out

- `src/domains/recommendation-intelligence/safety-audit.ts` — Operator-facing audit infrastructure. Not part of the unified scoring/delivery pipeline. Kept for safety analysis but not fed to the new brain.
- `src/domains/recommendations/prioritize.ts` — Live customer-facing scorer feeding recommendations/page.tsx. COMPETING scorer (simpler rubric, competitor-primary focus vs first-party demand). Must 
- `src/domains/recommendations/generate.ts` — Live customer pipeline. Produces candidates for prioritize.ts. Different taxonomy (create_cluster_page, strengthen_page_copy, watch_winning_cluster) v
- `src/domains/recommendations/load-queue.ts` — Live customer queue loader. Routes to /recommendations page. Calls generate + prioritize (legacy scorers). Must remain unchanged until new brain flip.
- `src/domains/guided-execution/priority-scorer.ts` — Domain-specific (guided-execution, not recommendations). Scores detected gaps for Workbench move selection. Orthogonal to the recommendation-intellige
- `src/app/(shell)/today-data.ts` — Marked for deletion per #25, #67, #72. Kept for now because buildTodayLifecycleSummary is still called by load-queue.ts. Being replaced by narrow v2 l
- `src/app/(shell)/pages/page.tsx` — Deliberately disabled per operator feedback (#100); documented follow-up in IDEAS_PARKING_LOT.md to rebuild per-URL citation history from Supabase whe
- `src/app/(shell)/pages/loading.tsx` — Dead code; page.tsx renders static placeholder. Kept for eventual rebuild; do not reference.
- `src/app/(shell)/pages/scan-action.ts` — Dead code; preserved for rebuild follow-up. Do not reference.
- `src/app/(shell)/pages/verify-action.ts` — Dead code; preserved for rebuild follow-up. Do not reference.
- `src/app/(shell)/pages/wave-actions.ts` — Dead code; preserved for rebuild follow-up. Do not reference.
- `src/app/(builder)/moves/[id]/page.tsx` — Intentional stub; routable but non-functional. Guided execution lives on /recommendations. No loaders to map. Keep for URL routing safety.
- `src/adapters/profound/entity-seed.ts` — OPERATOR-ONLY UTILITY. Builds TrackedEntity[] from hardcoded domain maps (COMPETITOR_DOMAINS, DIRECTORY_DOMAINS, SOCIAL_DOMAINS). Runs during CSV impo
- `src/adapters/profound/prompt-adapter.ts` — IMPORT-ONLY UTILITY. Parses Profound CSV rows → TrackedPrompt[]. Used in CSV orchestration. No live Profound API input. Dead code if CSV import is nev
- `src/adapters/profound/execution-adapter.ts` — IMPORT-ONLY UTILITY. Parses CSV execution rows → PromptAnswerObservation[]. Feeds snapshot derivation. Dead without CSV input. Keep with import orches
- `src/adapters/profound/citation-adapter.ts` — IMPORT-ONLY UTILITY. Parses CSV citation rows → CitationObservation[]. Feeds citation-index build. Dead without CSV. Keep with import orchestrator.
- `src/adapters/profound/benchmark-adapter.ts` — IMPORT-ONLY UTILITY. Parses CSV benchmark snapshots → EntityCandidate[], DailyMetricSnapshot[]. Seeds entity tracking. Dead without CSV. Keep with imp
- `src/adapters/profound/csv-discovery.ts` — IMPORT-ONLY UTILITY. Discovers .data/*.csv files, classifies by header signature. Feeds import orchestrator. Dead without CSV import. Keep with import
- `src/adapters/profound/merge-ingest.ts` — IMPORT-ONLY UTILITY. Idempotent merge logic for CSV ingest (stable natural keys, newest-wins on collision). Feeds import orchestrator. Dead without CS
- `src/adapters/profound/actions.ts` — OPERATOR-ONLY ACTION. Single export: importProfoundData() → runProfoundImport() with hardcoded 'ritz-builders' tenant. UI-facing server action (likely
- `src/domains/recommendations/entity-pollution-filter.ts` — LEGACY FILTER. Comment (line 30) references adapters/profound/entity-seed.ts:DIRECTORY_DOMAINS. Filters directory/spam entities from recs. CSV import 
- `src/domains/link-authority/backlink-provider.ts` — STUBBED: getProfile() returns null (no implementation). Skeleton + interface only. computeLinkGaps() is pure logic (works). Live DataForSEO call defer
- `src/domains/today/visibility-read-model.ts` — LEGACY brand-visibility metric (operator wants PER-PROMPT Profound coverage, not a global daily visibility-score). Reads daily_metric_snapshots (pre-c
- `src/domains/product/visibility-score.ts` — LEGACY metric computation (mention + citation averaging). Feeds visibility-read-model. Used by visibility-score chart + today v2 leaderboard. QUARANTI
- `src/domains/today/visibility-read-model-constants.ts` — Shared metadata for legacy visibility-score surfaces. DELETE once visibility-read-model is retired.
- `src/domains/today/observation-rollup.ts` — Legacy observation aggregation for visibility-score. Feeds visibility-read-model. QUARANTINE: delete once visibility model deprecated. Profound-based 
- `src/domains/daily-metric-snapshots/build-from-observations.ts` — Builds the snapshot table that feeds visibility-read-model. LEGACY: operator wants per-prompt coverage, not daily aggregates. QUARANTINE: keep while v
- `src/domains/daily-metric-snapshots/types.ts` — Schema for legacy visibility aggregation. Delete after visibility model deprecated.
- `src/domains/daily-metric-snapshots/today-kpis.ts` — Feeds today v2 KPI cards. QUARANTINE: delete when visibility-read-model is removed.
- `src/domains/daily-metric-snapshots/today-primary-share.ts` — Today v2 primary-share card. QUARANTINE: delete after visibility model deprecated.
- `src/components/today/ai-visibility-hero.tsx` — Today v2 hero surface for visibility-score. QUARANTINE: remove once per-prompt coverage integration ships (operator wants Profound topic coverage, not
- `src/components/today/visibility-leaderboard.tsx` — Today v2 visibility leaderboard. QUARANTINE: delete post-migration to per-prompt topic coverage.
- `src/components/today/visibility-score-chart.tsx` — Today v2 visibility timeseries chart. QUARANTINE: remove once per-prompt coverage integration replaces it.
- `src/domains/observation-intelligence/types.ts` — Legacy observation model (visibility-score driver). Once per-prompt coverage integration ships, observations can be rearchitected. For now, QUARANTINE
- `src/domains/observations/visibility-read.ts` — Legacy observation reader. QUARANTINE: delete once observation model migrated to Supabase (per-prompt Profound coverage replaces this).
- `src/domains/observations/visibility-persist.ts` — Legacy observation writer. QUARANTINE: delete once observation model migrated.
- `src/domains/observations/visibility-types.ts` — Legacy observation type system. QUARANTINE: delete after visibility model deprecated.
- `src/domains/prompt-answer-observations/enrichment-rollup.ts` — Legacy enrichment layer (feeds visibility-score computation). QUARANTINE: delete after visibility model deprecated.
- `src/app/(shell)/diagnostics/dev-notes/page.tsx` — Operator-only (2026-06-10). Internal dev-notes surface. No clear current usage or tie to unified brain. Candidates for consolidation into /diagnostics
- `src/app/(shell)/today-client.tsx:485` — STALE COPY. Lines 485-486: 'Connect Google Search Console (plus GA4, SEMrush, or Clarity) and refresh to see your visibility scoreboard and ranked act
- `src/app/(shell)/page.tsx:166` — STALE COPY. Duplicate of today-client.tsx:485. Same 'visibility scoreboard and ranked actions' claim. Must update to remove scoreboard/ranking languag
- `src/components/today/visibility-leaderboard.tsx` — OLD paradigm component. Renders top entities by a deprecated 'visibility score' metric. NEW brain does not use entity-ranking scorecards. Component sh
- `src/components/today/visibility-score-chart.tsx` — OLD paradigm visualization. Charts deprecated 'visibility score' metric over time. NEW brain replaces this with Rank-&-Revenue (no score) + Profound (
- `src/components/today/ai-visibility-hero.tsx:260` — Comment (line 260+) references OLD 'ranked' framing issue. Component itself shows entity rankings. Likely part of deprecated visibility-score feature.

## ADAPT (39) — useful data, wrong shape — wrap with an adapter into ActionPack/evidence

- `src/domains/recommendation-intelligence/edit-feedback.ts` — Utility for draft enrichment. Consumed by promotion-writer. Useful but ancillary. Could be unified with learni
- `src/domains/attribution/profound-proof-observations.ts` — Partial Profound coverage integration; reads proof_observations. Evidence enrichment layer consumed by specifi
- `src/domains/recommendation-intelligence/profound-topic-signals.ts` — Partial Profound integration; provides topic-level evidence. Evidence enrichment only; not core queue signal.
- `src/domains/recommendation-intelligence/triggers/profound-aeo-gap.ts` — Recommendation generation trigger using Profound; part of trigger candidate funnel, not core queue.
- `src/adapters/profound/import-orchestrator.ts` — LEGACY path (CSV batch import for onboarding/backfill, not nightly sync). Parses Profound CSVs (.data/*.csv), 
- `src/adapters/profound/bridge.ts` — LEGACY BRIDGE. Adapts CSV-imported DailyMetricSnapshot[] to Result[] (old shape) for event-detection + candida
- `src/domains/citation-lifecycle/compute-repeat-citation.ts` — LEGACY BRIDGE. Accepts profoundImportRuns parameter (from CSV import runs) to compute repeat-citation lifecycl
- `src/domains/citation-lifecycle/load-lifecycle.ts` — LEGACY BRIDGE. Reads profoundImportRuns, handles 'profound_default' → 'per_tenant' threshold flip (sample size
- `src/domains/citation-lifecycle/render-copy.ts` — LEGACY BRIDGE. Renders citation copy with source='profound_default' fallback (empty-state placeholder). Commen
- `src/domains/citation-lifecycle/load-repeat-citation.ts` — LEGACY BRIDGE. Calls getProfoundImportRuns(), passes to compute-repeat-citation for legacy sourcing. NEW brain
- `src/domains/visibility-events/windowing.ts` — LEGACY CLASSIFIER. Classifies visibility signals into window types; includes 'profound' (line 62). Historical 
- `src/domains/prompts/prompt-library.ts` — LEGACY ANNOTATION. Marks prompts source='profound' during CSV import (line 135). Historical metadata. NEW brai
- `src/domains/prompts/types.ts` — LEGACY TYPE. PromptSource union includes 'profound' (line 10). Historical metadata type. No NEW brain change; 
- `src/domains/pages/classify.ts` — LEGACY CLASSIFIER. 'profound' is a junk word (line 194) to filter from page types. Historical noise filter. Ha
- `src/domains/observation-runs/types.ts` — LEGACY TYPE. Comment reference (line 18) to adapters/profound/. Historical context. Type definitions only; no 
- `src/domains/lifecycle-eligibility/types.ts` — LEGACY TYPE. threshold_source union (line 205) includes 'profound_default' for historical decision tracking. N
- `src/domains/lifecycle-eligibility/aggregate-counters.ts` — LEGACY LOGIC. Line 172: returns 'profound_default' source when gate closed. Historical fallback. NEW brain doe
- `src/domains/answer-snapshots/types.ts` — LEGACY TYPE. SourceSystem union (line 16) includes 'profound_import' for historical metadata. CSV import marks
- `src/lib/querying/types.ts` — LEGACY TYPE. SourceSystem union (line 21) includes 'profound_import'. Historical metadata. No NEW brain change
- `src/lib/import/parsers.ts` — LEGACY PARSER. Line 205: assetMap.set('profound', 'infrastructure'). Historical metadata mapper for CSV ingest
- `src/domains/attribution/change-taxonomy.ts` — LEGACY TAXONOMY. Line 283: 'infra.tracking.profound-integration-update' (measurement plumbing). Historical eve
- `src/domains/attribution/memory.ts` — LEGACY MEMORY. Line 632 includes 'profound' signal type in memory union. Historical tracking. No NEW brain cha
- `src/domains/recommendations/cross-tenant-brain/compute-tenant-thresholds.ts` — LEGACY LOGIC. Comment (line 45-46): profound_default → source fallback + T2C_THRESHOLDS. Historical threshold 
- `src/domains/recommendation-intelligence/semrush-page-signals.ts` — Pure aggregator over synced rows; logic is domain-agnostic (striking-distance band logic, tokenization). KEEP 
- `src/domains/recommendation-intelligence/load-trigger-candidates-for-tenant.ts` — CORE orchestrator; feeds unified brain directly. Lines 329–458: semrush signal loads (striking, gap, cannibali
- `src/lib/connector-store.ts` — Core connector registry. KEEP the pattern (token storage, status, sync timing). ADAPT: (1) add DataForSEO toke
- `src/components/connectors/connector-capability-copy.ts` — Connector UI labels. ADAPT: replace SEMrush capability text with DataForSEO capability text (SERP data, keywor
- `src/app/(shell)/settings/connectors/page.tsx` — Connector UI. ADAPT: remove SEMrush card; add DataForSEO card (same pattern as Profound: API key input, last-s
- `src/app/(shell)/settings/connectors/connectors-client.tsx` — Connector UI components. ADAPT: remove SEMrush component; add DataForSEO component (reuse Profound pattern).
- `src/app/(shell)/settings/connectors/actions.ts` — Connector server actions. ADAPT: replace Semrush actions with DataForSEO actions (same Supabase token + status
- `src/domains/recommendation-intelligence/customer-copy-templates.ts` — CORE. Includes copy for SEMrush triggers (strikingDistanceCopy, cannibalizationCopy, keywordGapCopy/keywordGap
- `src/domains/recommendation-intelligence/evidence-summary.ts` — CORE evidence synthesis. Lines 27–34: reads semrushSignal + buildSemrushEvidenceLines. ADAPT: (1) rename build
- `src/domains/recommendation-intelligence/evidence-summary.test.ts` — Test suite for evidence synthesis. ADAPT: (1) rename test cases from 'semrush' to 'rank'; (2) update assertion
- `src/domains/recommendations/load-queue.ts` — CORE persistent rec loader. Decorates queue with signals. Lines ~287: loads semrushSignals from loadSemrushPag
- `src/domains/recommendation-intelligence/promotion-eligibility.ts` — CORE queue-promotion logic. Reads confidence + trigger_signal. ADAPT: ensure 'semrush_*' trigger_signal values
- `src/domains/recommendation-intelligence/priority-score.ts` — CORE priority computation. Check if any priority logic explicitly checks for 'semrush_*' trigger_signal names.
- `src/domains/recommendation-intelligence/promotion-writer.ts` — Generates promotion copy. If any promotion copy references 'SEMrush' explicitly, ADAPT: rename to 'Rank Data' 
- `src/components/today/data-freshness-heartbeat.tsx:223,225,232,234,265` — Currently says 'Beacon refreshed AI visibility' + 'Update AI visibility when you refresh connected data.' Tech
- `src/app/(shell)/prompts/page.tsx:158` — Says 'Beacon updates your AI visibility each time you refresh your connected data.' Same old 'visibility' term

## Phase E — recommended kill order (after `/diagnostics/action-packs` is proven)

1. **SEMrush connector** (6 files under `lib/connectors/semrush/*`) + `/diagnostics/semrush` page+actions — operator: SEMrush GONE (DataForSEO replaces it). Remove `semrush-page-signals` callers (triggers) first, then the connector.
2. **Brand-visibility-score machinery** (the whole `today/visibility-read-model*` + `product/visibility-score*` + `daily-metric-snapshots/*` + `observations/*` + the `components/today/ai-visibility-hero|visibility-leaderboard|visibility-score-chart` cluster + their 6 tests) — replaced by per-prompt Profound coverage. Quarantine first (route off the cockpit), then delete.
3. **Dead agent-analytics migration + sync** — `migrations/2026-06-12_profound_agent_analytics.sql` + the `sync-nightly.ts` bots/referrals writers (403 on this key, write to a missing `profound_fanout_rows`).
4. **Competing customer scorer** — `recommendations/prioritize.ts` + `generate.ts` + `load-queue.ts` (the legacy `/recommendations` queue) — reconcile with the unified worklist, then retire.
5. **Stale customer copy** — `today-client.tsx:485` / `page.tsx:166` (SEMrush + 'visibility scoreboard' framing) → unified 'AI is asked X, cites these' language.
