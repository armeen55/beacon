# Beacon Execution Plan

> Sections older than 2026-07-01 (completed April sprints, the original launch plan, and reference
> matrices) are archived verbatim in `docs/archive/EXECUTION_PLAN_HISTORY_2026H1.md`.
> That archive holds first-half-2026 history; this file holds the live plan plus July onward.

## Live priority order (operator, 2026-07-11)

This is the operator's live priority order. I work it strictly top to bottom. The dated
chronology of what already landed lives in VERIFICATION_LOG.md; this list is only what is next.

> 🟢 **2026-07-21 continuous-work window, SECOND AND FINAL BATCH DEPLOYED AND EXACT-SHA VERIFIED
> (commits `099b158e`, `f5ab307c`, `55d52b88`; the window is now closed).** Closed-window
> contamination verdicts precompute into the surface snapshot at rebuild and serve from it at
> render, skipping the heavy permutation reads for frozen rows while open windows stay live;
> immutability is anchored on the completed 28 day basis window plus ledger-mutation
> invalidation, verified end to end in production (contaminationByClosedRow carries 2 frozen
> rows, honest since only 2 of 25 changes have closed windows). A fresh reachability graph over
> the post-phase-4 tree harvested 262 second-order orphan files (60,697 lines) plus csv-parse;
> constitutional pins pruned only where their subjects died, each justified; full suite green.
> Sixty dead exports pruned from surviving files (1,053 lines) in a capped batch, remainder
> documented. Accepted-risk decision: the moderate @hono/node-server advisory sits in a
> transitive dependency of the shadcn package whose globals.css genuinely imports; the vector is
> Windows-only path traversal in static serving Beacon never uses on Linux Vercel; removal broke
> the CSS build and was reverted; the risk is accepted and documented. Operator blockers
> recorded: CallRail parked cluster and off-site authority parked trio (delete or revive are
> product decisions), 55 unused DB indexes (schema drops), DataForSEO unfreeze and Perplexity key
> (paid). Window metrics: production TypeScript 276,796 to 245,412 lines; test code 234,399 to
> 205,630; test cases 15,194 to 13,513; server build 50M. Final gate: typecheck clean; 13,513
> passed / 0 failed; lint clean; `git diff --check` clean; production build passed; one
> accepted-risk advisory documented. **Next is unchanged:** the operator ships wave 1 from the
> Ready tab.

> 🟢 **2026-07-21 continuous-work window, FIRST BATCH DEPLOYED AND EXACT-SHA VERIFIED (commit
> `f06f31d7`, Vercel `dpl_Hez9JmjuDThdjFCjwfasKJosFD9P`; the window is still open and a further
> entry may follow).** Reliability root fix: background enrichment runs LAST in the visit cycle
> behind a 5 second settle guard so it can never starve the readiness reads again (root fix for
> the Ready-zeroing class; the 30 second read bound stays as defense in depth; three regression
> tests pin the order). Directed fold: the monthly spend total renders as one honest self-hiding
> line on /settings ("I spent $X.XX this month on AI and data calls. Every call is capped and
> logged before it runs."), fail-soft reader, five state tests. Defects fixed: the /competitors
> bookmark stub no longer chains into the deleted /prompts 404 (now /changes); four no-op
> revalidations of the deleted recommendations route removed; two stub-hop links point directly
> at /changes?status=ready. master_execution_plan.md compacted 3,370 to 373 lines with full
> verbatim history in docs/archive/MASTER_EXECUTION_PLAN_HISTORY.md, counts reconciling.
> Verifications: Wix credential proven LIVE via read-only API check (the Discover-collections
> unblock will work); help, onboarding, public routes, and the redirect-stub class audited clean;
> performance advisors zero slow-query findings (55 unused-index INFO notices recorded, index
> drops operator-gated); server build 112M to 50M after phase 4. Process incident recorded
> honestly: a lane agent's git stash/checkout cycles on the shared tree wiped uncommitted work
> including user-owned untracked files; everything was recovered (stash -u parents plus fsck) and
> re-applied with verification; new standing rules are that agents never run git state commands
> on the shared tree and work is banked immediately after each landing. **Next is unchanged:**
> the operator ships wave 1 from the Ready tab.

> 🟢 **2026-07-21 phase 4, THE HIDDEN-SECOND-PRODUCT DELETION, FOUR BANKED DEPLOYS, DEPLOYED AND
> EXACT-SHA VERIFIED (final fix `50991401`, Vercel `dpl_7F5WVgAUGmUHcwaQs98vANyzXJAg`).** The
> operator's scope decision is the product boundary: Beacon is Today, Changes, Results,
> Connections, minimal Settings, and minimal onboarding. 4A (`8bcb2ac3`): Profound CSV import
> island deleted (2,881 lines), removing the one unguarded Vercel file write by deleting its only
> caller; token expiry email machinery and backup verify producer deleted (1,139 lines);
> connection health untouched. 4B (`cf7ae6df`): the hidden second product deleted (ask, activity,
> keyword research, prompts, recommendations surfaces plus competitor, opportunity, observation,
> and local detail pages, net minus 24,938 lines); intelligence survives as backend evidence;
> activity receipts fold into Connections as Recent activity; the indexing directive hold now
> guards the LIVE accept paths on Changes and Today with a constitutional pin. 4C plus 4D
> (`c7c1834f`): Settings collapsed from nine sections to Connections and Business info (5,210
> lines); Today reduced from ten blocks and a drawer to a six block spine (net minus 6,379 lines)
> with the investigation finding as one honest self-hiding alert line; nav, palette, shortcuts,
> and titles reference only live routes. Regression caught and fixed (`50991401`): concurrent
> enrichment pushed the readiness critical drafts read past its 4 second timeout and silently
> zeroed the Ready queue; the read now carries the same 30 second bound as its siblings, verified
> live with Ready rendering 1 (tehran). Final checklist all passed: signed-in Today, Changes,
> Results, Connections real data zero error states; Ready above zero; Iranopedia the only active
> tenant, zero Ritz strings; Results answer band 0.48 seconds; zero deleted-surface references;
> production exact SHA `50991401`. Metrics after phase 4: production TypeScript 276,796 lines
> (campaign start 344,970); test code 234,399 lines (from 302,804); test cases 15,194 (from
> 20,533); test files 1,066. Deferred: decouple on-visit enrichment from the surface-build lambda
> so slow reads degrade non-catastrophically; remaining numeric diet targets need further
> operator scope decisions. **Next is unchanged:** the operator ships wave 1 from the Ready tab.

> 🟢 **2026-07-21 repository amputation campaign, THREE BANKED PHASES DEPLOYED AND EXACT-SHA
> VERIFIED (operator-directed; Phase 3 at `cc743bf5`, Vercel
> `dpl_Hb69586NRjKGRi53Z92KLT2ERxNS`).** Honest scoreboard: production TypeScript 344,970 to
> 304,375 lines (target under 200,000, not reached); test code 302,804 to 247,475 lines (target
> under 100,000, not reached); test cases 20,533 to 15,930 (target under 5,000, not reached).
> Three independent discovery agents proved the remaining suite is distinct-behavior trust
> coverage and the remaining production code is reachable from the live product; hitting the
> numeric targets would require deleting live product or trust coverage, which the campaign rules
> forbid, so those targets now wait on operator decisions. Phase 1: diagnostics web product
> deleted (29 routes replaced by one 158 line operator health page, Wix collection mapping moved
> to the Connections card); architecture suite replaced by a 27 file constitution (176 files /
> 29,877 lines to 27 files / 5,057 lines, 13 clauses, 400 tests). Phase 2 (`d72f3deb`,
> `a630dc3e`, deployed at `a3e1a71b`): 85 executed scripts, all 8 never-scheduled cron routes plus
> the autopilot cluster, the unlinked review/reports/builder route trees, 130 unreachable
> production files and their tests, and the outreach send-pitch feature deleted; constitutional
> pins acted as tripwires and saved 64 load-bearing files the graph had called dead. Phase 3
> (`cc743bf5`): the orphaned nightly orchestrator cron-sync.ts deleted with its zero-cost
> per-tenant producers re-homed as one bounded daily enrichment step in the on-use cycle, so the
> Today sections they feed refresh again; language-gap deleted under the English first rule; the
> Results answer band renders in about 0.64 seconds (was about 6.3); deadman speaks on-use truth
> and can never claim a deleted job stalled; persistence is confirmed single-model in prod with
> one latent bug flagged for the operator (writeCitationShard unguarded on Vercel, needs a
> durability mirror before guarding). Final gate: typecheck clean; suite 15,930 passed / 23
> skipped / 0 failed (1,140 files); lint clean; npm audit 0 vulnerabilities; `git diff --check`
> clean; production build passed; signed-in smoke of Today, Changes, and Results rendered real
> data with zero error states after every phase. Deferred: the numeric diet targets (operator
> decision on live surfaces), the citation-shard durability fix, token-expiry-notify and
> backup-verify re-homing. **Next is unchanged:** the operator ships wave 1 from the Ready tab.

> 🟢 **2026-07-20/21 three-phase campaign (verification, canonicalization, reliability), DEPLOYED
> AND EXACT-SHA VERIFIED (operator-authorized, about 20 agents; Phase B commits `a9a22514`,
> `c7de4db3`, `7e802cd3`, `d9a9352c`; Phase C commit `002e79d8` deployed as
> `dpl_7hCWQ7mFJqN8Jq21ooGEtdyk3KKM`, six routes 307).** Phase A certified the signed-in journey
> read-only against real production data with paid keys stripped: Today reconciled 14 of 14
> headline numbers exactly to SQL; Results reconciled all data-integrity checks (25 rows, caveats,
> 10 contradiction lines matching canonical-null rows exactly); the native-capability map shows
> Profound's core wedge native and running same-day (ChatGPT engine) and SEMrush cleanly retired
> with GSC-native striking distance live, with remaining gaps being delivery (single engine,
> DataForSEO dry-run pause), not honesty; the destructive-action audit found zero destructive
> queue items and page creation triple-guarded. Phase B: Ready is above zero for the first time,
> verified live (verification-based staleness with copyBasisHash to 45 days, no shadowing by
> regressed packs, orphaned ready drafts surfaced honestly, /tehran renders ready with an approved
> draft); the epoch-zero date bomb is guarded; ready means one thing; plain-language ranking
> rationale; true 90 day windows; measuring points to Results without a tab; notes sanitized at
> render; chart early counts labeled; stale engines drop off the checked claim; Activity honors
> page links. One canonical connector registry (headline 4 of 4 connected, 2 need attention). GA4
> root cause closed: the single 27,000 row upsert that lost the statement-timeout race is now
> 1,000 row batches plus 3-strike escalation without a cron. Indexing directives held for review;
> consolidate rows can never carry paste-ready instructions; new-page actions on existing pages
> decide as edits. Phase C: the stage-receipt table verified honest across the on-visit cycle with
> the stalled-backfill gap fixed; Today's click-loss claim states its comparison and dated window;
> /reports applies the same clean-attribution dollar rule as Today and Results; per-request read
> dedupe and the measurement/LLM graph off the Results render path; pino removed (13 packages);
> body-parser advisory patched to 0 vulnerabilities. Final gate: typecheck clean; full suite
> 20,533 passed / 23 skipped / 0 failed; lint 0 errors; npm audit 0 vulnerabilities; `git diff
> --check` clean; production build passed. Deferred on record: DataForSEO unfreeze and Perplexity
> key (paid, operator decision), /results side-read latency, weekly-cadence copy nuance, shadcn
> devDependency reclassification. **Next:** the operator begins wave 1 of Iranopedia edits from
> the Ready tab, which now has real prepared work.

> 🟢 **2026-07-20 unattended truth-and-convergence phase, DEPLOYED AND EXACT-SHA VERIFIED
> (operator-authorized, strictly ordered tasks 1 to 6; commits `ed4d7d78`, `1bd4dd5e`,
> `cebb210c`, `95b8c715`, `75e6aab3`; Vercel `dpl_52jeQ4KG84C8F9F9FphGrsma7zAQ`; six routes
> 307).** The signed-in journey audit ran on a local operator-bypass server but was blocked from
> live numbers because the local .env.local still points at the dead pre-cutover Supabase project
> (operator-fix item); the outage exposed five database-unreachable honesty defects, all fixed.
> The production surface-blob truth audit came back tenant identity CLEAN, canonical 25
> reconciling across Today, Changes, and Results, Ritz pause holding. Forensic QA of the
> Iranopedia queue top items found and fixed the headline defects: the 0 Ready mystery (about 24
> genuine ready_to_review drafts orphaned by a packet URL-join miss and re-invalidated by an
> all-inputs evidence hash; drafts now attach by canonical URL, staleness keys only on
> copy-invalidating fields plus a 14 day age bound, Ready should light up on the next on-use
> rebuild); two destructive approved redirects that passed the old same-host-only gate (a new
> deterministic gate requires thin, demand-subset, or near-duplicate sources, hubs never redirect
> to a leaf, unsafe plans demote to an internal-link suggestion, both live items demote on next
> rebuild); card demand claims now state their true 90 day window; five database-unreachable lies
> replaced with honest states; the unrendered TodayView counts, measuring list, attention fields,
> and phantom movesReady stat deleted so stale divergent numbers can never persist again (the 7/0
> blob predated the count-unification deploy by 10 hours and self-heals). The architecture-test
> diet folded 45 duplicated source-scanning tests into 7 parameterized guard files (214 files /
> 33,725 lines to 176 files / 29,881 lines, every trust invariant at exactly one pin), and the
> canonical docs are compacted (VERIFICATION_LOG 36,722 to 7,428 lines, this plan 1,751 to 949
> lines, halves archived verbatim). Final gate: typecheck clean; full suite 20,497 passed / 23
> skipped / 0 failed in about 64 seconds; lint 0 errors / 63 warnings; npm audit 0
> vulnerabilities; `git diff --check` clean; production build passed. Deferred: briefs slice 2,
> GSC query-window reader consolidation, ts-prune long tail, master_execution_plan.md compaction,
> and the operator-only .env.local fix. **Next is unchanged:** the operator begins wave 1 of
> Iranopedia edits; the Ready queue should now populate on their first visit.

> 🟢 **2026-07-18 autonomous simplification campaign, BOTH PHASES DEPLOYED AND EXACT-SHA VERIFIED
> (operator-authorized, 5 audit agents plus 12 fix lanes).** Phase 1 (`349b63f0`, `e19db90b`, `68f7b075`) is net minus
> 39,666 lines: 173 zero-importer files deleted (retired domains, legacy Today v2 components,
> superseded cost modules, the executed customer-one backfill migration and its 2,710 line test,
> the 1,787 line morning-brief engine); the legacy 2,041 line recommendation engine retired to an
> 82 line extraction; Today's measuring/decided counts unified onto the same ledger classifier as
> Changes and Results (25 whole-tenant is the honest number, the 25 vs 7 contradiction is
> resolved); the header refresh chip bounded by pipeline progress instead of Refreshing 0/8
> forever; the Results staleness banner reading the finalized data watermark instead of the
> connector sync stamp (18 days claimed vs real 3 days fixed); 26 silent catch blocks feeding
> rendered numbers now log, and a failed shock-window read suppresses the lifetime earnings number
> instead of overstating it. Phase 1 is deployed and exact-SHA verified at `68f7b075`
> (Vercel `dpl_HAsgMH9d5zFFPQcKexCGHiyVJ3Le`, all 7 routes 307). Phase 2 (HEAD `99b8c1fc`) is
> deployed and exact-SHA verified as `dpl_6Hpmgce1XojNqGyEUnpWf4QJHcfJ`; production
> `/api/version` returned `99b8c1fce827624d02dd2b2092ba418e500a7827` and `/`, `/today`,
> `/changes`, `/results`, `/briefs`, `/worklist`, and `/settings/connectors` all returned their
> 307 login continuations, confirming the briefs redirect behaves exactly like worklist. It is
> net minus 11,792 lines: changes/[id] collapsed to its
> redirect (477 to 116 lines) with the orphaned v2 client/loader chain deleted; /briefs,
> /briefs/proposed, and /briefs/[id] redirect to /changes per the worklist precedent with six
> orphaned components deleted; on-use connector refresh extracted to
> src/lib/connectors/on-use-refresh.ts so the hot path stops importing the 1,544 line nightly
> orchestrator; the auto-measure due loop unified to one core with two thin entry points;
> freshness thresholds consolidated beside SOURCE_SLA; stale scheduled-cron docstrings corrected.
> Final full gate: 21,810 passed / 23 skipped / 0 failed, about 1,300 dead tests removed and
> about 15 seconds faster; lint 0 errors / 63 warnings (down from 74); npm audit 0
> vulnerabilities; `git diff --check` clean; production build passed. Deferred with reasons on
> record in
> HANDOFF_VERIFIED_STATE.md: the offline study harness (kept), GSC query-window reader
> consolidation, briefs slice 2 (about 45 files, cascade map written), and the ts-prune long tail
> of 335 dead exports. **Next is unchanged:** the operator begins wave 1 of Iranopedia edits.

> 🟢 **2026-07-18 tenant-fallback incident, CLOSED, DEPLOYED AND EXACT-SHA VERIFIED at
> `4cd7169a` (Vercel `dpl_47ABTKPKwBG5xkJ8fceUf7dfqcfN`).** The operator's live Iranopedia
> session flipped to Ritz Builders mid-session on /results, including one mixed render (Iranopedia
> header over Ritz staleness data). Root cause: the middleware tenant_members lookup timed out
> under Supabase load and the app silently fell back to BEACON_TENANT_ID (tenant-ritz-founder),
> amplified by the operator account holding two memberships. Data fix, applied directly to
> production Supabase: the operator's tenant-ritz-founder membership row is deleted (recorded for
> future restore: user 465480f5-6419-4bae-a4bd-42f59305ec40, created 2026-06-19 18:20:38 UTC) and
> tenant-ritz-founder is paused, so Beacon is now single tenant (Iranopedia) with all Ritz data
> preserved for a future separate account. Code fix: authenticated requests can never reach the
> env fallback; lookup failure honors the membership-validated beacon_tenant cookie or redirects
> to /login?error=tenant_unavailable; stale non-member cookies are scrubbed; the operator bypass
> validates against active tenants via an edge-safe REST check; listActiveTenants() feeds all
> background fan-outs so the paused tenant consumes zero work; tenant switching rejects non-active
> targets; and the resolution layer logs loudly if the env fallback is ever reached inside a
> request. Leak-pinning tests were rewritten to pin the safe contract and five stale fixtures
> updated. Gate: strict typecheck clean; full suite about 1,546 files / 23,113 passed / 23 skipped
> / 0 failed; production build passed; production `/api/version` returned the exact SHA. **Next is
> unchanged:** the operator begins the first wave of real Iranopedia edits.

> 🟢 **2026-07-18 trust hardening, DEPLOYED AND EXACT-SHA VERIFIED at `a19ba958`
> (Vercel `dpl_4eGz8bcz5dZntAJJQhn9GQMwthtR`).** Results now
> warns when a latched live-verified edit's latest crawl came back not_found or crawl_failed with
> no newer re-confirmation, firing on 12 of the 25 shipped changes today, and a directional-read
> caveat now marks all 25 pre-protocol rows measured before the predeclared-protocol lock-in. Every
> live ship already funnels through recordShippedChange, so every NEW ship is measured by the
> predeclared protocol going forward. On-use autonomy is now real: the page-level 60-second timeout
> override that was killing the roughly 210-second post-response background cycle on the landing
> page is removed from every shell route, a stuck "running" receipt older than 15 minutes now says
> so instead of hanging forever, a new autonomous_run_claims table (migration applied to production
> Supabase, 0 rows) gives cross-instance mutual exclusion on the paid research pipeline, and GSC
> deep-history backfill now continues one chunk per owned background cycle during normal use
> instead of only through the disabled cron path. Tenant isolation: answer-texts is tenant-scoped,
> the prompt-library global singleton is deleted, result-mode's global visibility rules are gone
> (Bay Area patterns are founder-tenant-only), and unknown store classification now fails loudly
> instead of silently minting a tenant-blind cache key. Demo data now serves only under one shared
> shouldServeDemoData predicate (founder tenant, zero imports, no real connector), closing the bug
> where a connected founder tenant saw fabricated numbers with no banner. Copy honesty: every
> remaining false "tonight / overnight / nightly" claim is removed with a guard test per surface,
> and five verified-zero-mount files are deleted outright. Verification is green at 1,544 files /
> 23,100 passed / 23 skipped / 0 failed plus typecheck, lint (0 errors / 74 pre-existing warnings),
> and production build; three drifted architecture test pins (gsc-no-hardcoded-site-url fixture,
> perf-shell-layout demo predicate pin, autonomous-execution-loop extraction marker) were fixed for
> the right reasons and pass. The autonomous_run_claims migration was applied to production
> Supabase and verified before push. Six commits reached `origin/main` (`183f4317`, `7b6f5afd`,
> `7ffe0e77`, `c8471edd`, `d559fe6e`, `a19ba958`); production `/api/version` returned the exact
> release SHA and unauthenticated `/`, `/today`, `/changes`, `/results`, `/activity`, and
> `/settings/connectors` returned their expected 307 login continuations. **Next, in strict
> order:** (1) the operator begins the first wave of real Iranopedia edits; every accepted change
> now gets a predeclared measurement plan automatically; (2) the external gates remain unchanged:
> five genuinely unseen blind cases, one authenticated hosted visit receipt/timing pass, real unit
> economics, and Ritz reconnection; (3) verify the first approved Iranopedia publish live and wait
> through its predeclared measurement windows.

> 🟢 **2026-07-17 on-use customer-language convergence — DEPLOYED AND EXACT-SHA VERIFIED at
> `9174b0f1` (Vercel `dpl_FGsSQhMVrfzCQfx2SwJDSzN4mTjc`).** The dormant
> overnight Autopilot card is no longer mounted on Connections, and audited customer surfaces no
> longer promise overnight, nightly, or next-scheduled work that does not exist. Execution copy is
> consistently Today; autonomous recovery says it advances while Beacon is used or when new
> evidence arrives. A cataloged architecture invariant pins the operating-model boundary. Full
> verification is green at 1,537 files / 23,085 passed / 23 conditional skips / 0 failed plus
> typecheck, lint, build, audit, and diff checks. Production returned the exact full SHA and core
> signed-out route continuations remained correct. The strict external acceptance order below is
> unchanged.

> 🟢 **2026-07-17 acceptance hardening — DEPLOYED AND EXACT-SHA VERIFIED at `cacb0ad3`
> (Vercel `dpl_3BA1TVcUjYa6brJGJBpqZyxTyCwW`).** Beacon now has one honest autonomous
> operating model: Vercel schedules are intentionally empty, while normal authenticated use starts
> the bounded post-response visit runner and persists visit/source receipts. Connections reports
> those receipts instead of inferring a nightly sync. Blind validation is now a server-sealed,
> append-only preregister → predict → reveal protocol bound to the live release SHA; reused IDs,
> revealed-before-predicted cases, and cases whose build changed are refused or spent. Verification
> is green at 1,536 files / 23,080 passed / 23 conditional skips / 0 failed plus typecheck, lint,
> build, audit, and diff checks. Production returned the exact full SHA and core signed-out route
> continuations remained correct. **Strict order now:** (1) execute five genuinely unseen
> hosted cases through all three phases; (2) make one authenticated hosted visit and confirm the
> on-use visit/source receipt plus Today/Changes/Results timings; (3) enter real unit economics;
> (4) reconnect Ritz; (5) approve and publish one Iranopedia move; (6) verify it live and wait for
> the predeclared measurement windows. No extra product surface belongs ahead of those gates.

> 🟢 **2026-07-17 code-verifiable trust backlog — CLOSED, deployed, exact-SHA verified.** Durable blind-run
> receipts, push fail-closed behavior, mark-shipped authorization, schema revert restoration,
> accept idempotency, live-slug duplicate protection, fan-out parsing, GA4 pagination, stable page
> identity, scan-shell/error preservation, FAQ direction, content-schema parity, dense GSC series,
> citation-regime abstention, AI-claim firewall reach, review content dedupe, safe unknown tenant
> segmentation, tenant-keyed site identity, concurrent atomic-write temp isolation, exact
> observation-level citation rates, prose-safe schema descriptions, hierarchical breadcrumbs,
> bounded queue hygiene, page-specific Profound gaps, schema-less publisher inference, and crawl-
> age confidence labels are green. The old audit-wave-2 through wave-7 code defects are therefore
> superseded by this closure; dead State-of-the-
> Union and batch-planner callouts require no resurrection. Release
> `9350d0dce45b041ee0d363a3f6da0c3924be1d92` is live in Vercel deployment
> `dpl_5eBWaXqFgrX6CdUDFQuAtQNVmmrF`, and production returned the exact SHA. **Next, in strict
> order:** (1) run five genuinely unseen blind cases against the hosted release through the
> server-sealed phased protocol; (2) observe one authenticated on-use visit/source receipt in
> production; (3) enter real unit
> economics before certifying revenue; (4) reconnect Ritz as the second tenant; (5) approve and
> publish one Iranopedia move; (6) verify it live and wait for the
> predeclared measurement windows before claiming learning. No additional feature surface belongs
> ahead of those acceptance gates.

> 🟢 **2026-07-17 exhaustive audit execution loop — ALL EIGHT SLICES DEPLOYED AND
> EXACT-SHA VERIFIED.**
> Competitor teardown evidence is tenant-explicit from orchestration through cache persistence and
> deployed at `5a869835`; production returned the exact SHA. `/changes/[id]` now resolves the
> tenant's fresh changelog row and hands every outcome question to the canonical proof-gsc Results
> card, including shared compound-package outcomes. An untracked historical row lands on Results
> without a legacy verdict. Slice 2 is deployed at `19d292c1` and exact-SHA verified. The proof
> runner now appends immutable versioned reads for all predeclared windows, applies day 56 as
> demote-only, keeps day 84 context-only, and reuses the first-written decision on later passes.
> Slice 3 is deployed at `ec0d8f1f` and exact-SHA verified. The live move path now merges cached
> DataForSEO demand, related keywords, difficulty, GSC, and AI questions into the canonical
> page-element plan, using one cache read and zero render-time spend. Slice 4 is deployed at
> `49d56191` and exact-SHA verified. Citation phrasing, answer drift, and second-order citations now
> converge into the same dossier during the autonomous knowledge stage, with real aggregate counts
> and explicit tenant cache reads. Slice 5 is deployed at `5a892dd0` and exact-SHA verified.
> Page-matched Clarity friction, citation-backed competitor forensics, and fanout relevance/
> corroboration now converge into the canonical dossier without a new surface or paid call.
> Slice 6 is deployed at `46fe9517` and exact-SHA verified. The unreachable 2,255-line Diagnostics
> index body, dead provider barrel, and unreachable Anthropic failure stub are preserved outside
> compilation as archive text; live route/config contracts now pin the reduced surface. Its full
> suite is green at 1,514 files / 22,997 passed / 23 conditional skips / 0 failed. Slice 7 is
> deployed at `2f3821cb` and exact-SHA verified. The highest-risk customer route, Results, now
> delegates its proof-card presentation to a synchronous no-I/O module while retaining every
> measurement and compound-package contract; the full suite is green at 1,515 files / 23,001
> passed / 23 conditional skips / 0 failed. The final slice is deployed at `3648fdd2` and
> exact-SHA verified. **Next:** use the normal Today → Changes → Results loop on a real prepared
> Iranopedia change; open a new engineering slice only for a defect that real use exposes.
>
> The tenant-boundary slice removed all ambient read/write seams in the production graph and is
> pinned by read-and-write isolation tests.

> 🟢 **2026-07-17 signed-in journey correction — DEPLOYED IN `5a869835`.** The real
> Iranopedia journey exposed the remaining trust gap: Changes promised five copy-ready items while
> Ready was zero, and Results made 25 measuring rows feel like 25 decisions. Exact redirect maps
> now appear before action, Ready copy is fail-closed, the eight-stage background brain has factual
> progress, and Results is answer-first with five nearest reads plus collapsed history. Intentional
> same-page same-date edits are measured as one named package and may never train an individual
> lever; accidental later overlaps remain quarantined. Edited
> operator copy now remains identical through live verification and proof recording. **Next:** use
> the first real Ready item; do not act on a merely directional To do card.

> 🟢 **2026-07-15 continuous execution loop — DEPLOYED AND SHA-VERIFIED (`ef41bac6`).** The
> operator selected items 2 and 3 and explicitly skipped item 1. Beacon now restores a five-item
> copy-ready queue during ordinary same-day navigation and after every handled row without a cron,
> manual prepare control, Wix dependency, or render-time paid call. It scans the full final Changes
> order under a hard maintenance budget and atomically republishes Today + Changes. The visible
> product spine is Today → Changes → Results; research and audit support remain searchable but do
> not compete with the working loop. **Next:** use one real Changes session and evaluate only the
> resulting queue continuity and copy quality; do not add another surface or control unless that
> real use exposes a specific defect.

> 🟢 **2026-07-15 autonomous-ready extension — DEPLOYED AND SHA-VERIFIED (`17255d5b`).** The
> operator-approved top three are complete: automatic evidence-gated preparation of the first five
> ranked changes with no Wix dependency; an eight-stage durable cursor that resumes from the first
> failed or timed-out dependency; and a single versioned tenant customer snapshot consumed by
> Today, Changes, and New Pages. The prior complete snapshot stays visible until the replacement is
> fully assembled. No button, cron, publish right, or render-time paid call was added. **Next:** use
> the normal Today → Changes journey and evaluate the first real prepared edit; engineering should
> react only to an actual route/list/tenant/draft defect, not ask the operator to wait and refresh.

1. **Verdict quarantine.** DONE. Every stored won or lost verdict now reads as
   uncalibrated through the single choke point src/domains/proof-gsc/verdict-calibration.ts, and
   the protective brakes stay on the raw verdicts. Pooled verdicts have a separate empty registry.
2. **Blind-defect correction wave.** DEPLOYED AND SHA-VERIFIED at eacd0049. Owned-page
   coverage blocks duplicate new pages, and zero-click demand blocks unsupported work without
   hiding independently evidenced AEO opportunities.
3. **Evaluation-claim boundary.** DEPLOYED AND SHA-VERIFIED at 02f8ca31. The known gold library is
   explicitly regression evidence, and a separate fail-closed contract is the only path to a
   fresh blind pass. Spent cases, label leakage, missing archetypes, failures, and fewer than five
   countable cases all deny eligibility.
4. **Truthful sync-connectors HTTP health.** DEPLOYED AND SHA-VERIFIED at 7967d729. Healthy,
   degraded, broken, zero-tenant, zero-connected-source, and non-durable-receipt
   outcomes now have distinct status/body contracts; the route can no longer return fake green
   merely because the function completed.
5. **Results initial-paint streaming.** DEPLOYED AND SHA-VERIFIED at 86ecd1f1. Only the ledger
   blocks the shell; four nonessential data reads plus operator context
   share one promise behind Suspense with truthful fallbacks and no duplicate I/O.
6. **GA4 property-calendar reconciliation.** DEPLOYED AND SHA-VERIFIED at d2ed7193. Direct report
   windows and partial-month classification use the stored GA4 property
   timezone; missing, invalid, conflicting, or changed timezones withhold the claim.
7. **Today post-snapshot parallel context.** DEPLOYED AND SHA-VERIFIED at 1a02d8a4. Eleven
   independent reads run in one bounded batch rather than additive sequential waits; command
   ranking, source values, and fail-soft behavior are unchanged.
8. **Legacy Profound tenant boundary.** DEPLOYED AND SHA-VERIFIED at ac2f8d60. The active Settings
   action no longer hardcodes Ritz; request tenant, business identity,
   owned domain, and benchmark identity are explicit. Hosted or cross-process-tenant CSV imports
   fail closed because their underlying cold files are not multi-tenant stores.
9. **Today compulsory-gate parallelization.** DEPLOYED AND SHA-VERIFIED at a0722e1f. Import/activity,
   connected-source, and tenant resolution start together before the
   gate's narrow observation/prompt checks; demo and first-reading outcomes are unchanged.
10. **Tenant-neutral inventory matching.** DEPLOYED AND SHA-VERIFIED at 19409888. Global
   builder/construction synonyms are removed from the active edit-vs-new decision;
   universal morphology remains and domain vocabularies require explicit injection.
11. **Explicit owned-URL identity.** DEPLOYED AND SHA-VERIFIED at 000d1c99.
   Canonicalization and citation-index construction receive the tenant domain explicitly; founder
   aliases are no longer global and A→B→A calls remain isolated.
12. **Evidence-tier site identity.** DEPLOYED AND SHA-VERIFIED at 51f4eb63.
   Relative URLs resolve only against an explicitly supplied tenant domain; absent context lowers
   confidence and can never borrow another tenant's process-global domain.
13. **Attribution site identity.** DEPLOYED AND SHA-VERIFIED at c06e4b56.
   URL matching, evidence tiering, and citation support share an explicit optional tenant domain;
   absent context is conservative, never process-global. Architecture and A→B→A guards pin it.
14. **Explicit attribution registry selection.** DEPLOYED AND SHA-VERIFIED at f31ecd14. The shared
   last-warmed pointer is removed; registries and citation-topic
   indexes are keyed and selected by explicit tenant ID, and the architecture allowlist is empty.
15. **Tenant-aware Proposed Brief archetypes.** DEPLOYED BY ANCESTRY AND SHA-VERIFIED at 83fa59b5. City
   presence alone cannot produce LocalBusiness/NAP/service-area plans; only explicit
   `local_service` tenants receive coverage expansion.
16. **Tenant-explicit recommendation URLs.** DEPLOYED AND SHA-VERIFIED at 83fa59b5. Relative strengthen/investigate targets resolve from explicit tenant origin or withhold
   the absolute URL; the active engine no longer imports process-global site config.
17. **Tenant-explicit website scan.** DEPLOYED AND SHA-VERIFIED at 199acb0e. Resolve the
   current tenant's business domain once and thread it through crawl, robots, homepage identity,
   and findings. Never inherit `BEACON_SITE_DOMAIN` from process state.
18. **Tenant-explicit post-import milestones.** DEPLOYED AND SHA-VERIFIED at 96a475b0.
   Owned citation attribution and competitor classification use one current-tenant business config,
   while independent import evidence reads run concurrently.
19. **Connect the existing research-to-draft loop.** DEPLOYED AND SHA-VERIFIED at 004e2694.
   The one-click plan action now runs competitor winner teardown before keyword/SERP enrichment,
   move preparation, and competitor-grounded draft regeneration. Cached analyses and fresh fetches
   are reported separately; failure of external research does not block the rest of the plan.
20. **Test and dead-code reduction audit.** Measure suite ownership, duplication, runtime, and
   production reachability before proposing deletions. Remove obsolete protection and unreachable
   code in bounded reviewed batches, never by raw test-count target. FIRST PERF FIX DEPLOYED AND
   SHA-VERIFIED at b66ae0c1:
   the onboarding launch suite now injects its first-scan dependency instead of crawling a real host;
   33/33 assertions remain and the full suite fell from 308.95s to 234.91s. Audit baseline: about
   322k test lines, 360 source-reading files / 4,273 statically collected cases, 62 skipped tests,
   257 unused declarations, 44 static-graph orphans, and 103 test-only production files. Deletion
   order: skipped legacy blocks and unused imports; proven orphans; test-only feature islands plus
   their tests; then consolidate source contracts and split parallel-safe from serial route tests.
   DEPLOYED AND SHA-VERIFIED at `d6dcb0c7`: after explicit operator deletion approval, the first bounded wave removes 60
   proven unreachable/test-only files and about 5,550 lines. No surviving production import points to
   them; strict typecheck and the complete 1,488-file / 22,849-pass / 23-conditional-skip suite are
   green. A four-worker full-suite experiment in an
   isolated CI-clean `/tmp` copy passed every file in 77.10s; the real checkout then passed all
   1,505 files / 23,132 assertions / 40 conditional skips in 74.47s, versus 239.14s serial.
   SECOND BOUNDED CLEANUP DEPLOYED AND SHA-VERIFIED at `fc7c980a`: removed the retired pre-tenant
   robots flat-file implementation and stale planner imports/locals, leaving tenant repositories as
   the sole robots-state boundary without changing planner output. Focused 65/65 and the complete
   1,504-file / 22,971-pass suite are green. THIRD BOUNDED CLEANUP DEPLOYED AND SHA-VERIFIED at
   `b23a4cfc`: a strict `noUnusedLocals` / `noUnusedParameters` audit established 231 diagnostic
   lines across 113 source-tree files; the first import/local-only batch removed 30 diagnostics
   across 21 modules, leaving 201 lines and 97 affected source-tree files. Parameters and public
   shapes remain untouched until each caller contract is reviewed. FOURTH BOUNDED CLEANUP DEPLOYED
   AND SHA-VERIFIED at `e7719d1e`: reviewed algorithm-adjacent residue across 14 modules, removed 49
   dead lines without changing exported behavior, and reduced the strict output to 185 lines across
   85 affected source-tree files. Continue with caller-by-caller review; do not mass-delete unused
   parameters or public shapes.
20a. **Autonomous research-before-ranking MVP.** DEPLOYED AND SHA-VERIFIED at `d6dcb0c7` (initial
   scheduler `ce4d4345`, suite cleanup `96e137ab`, AI-poll hardening `433614ac`). Every
   signed-in shell visit can trigger one bounded tenant/day post-response cycle: connector freshness,
   winner teardown, DataForSEO competitor keyword gaps and clone briefs, keyword/SERP enrichment,
   multi-engine AI answers, AI citations, question/factual/internal-link graphs, loss and steal
   reflexes, final fusion/ranking, capped draft preparation, then Today. It uses existing guarded
   producers and never publishes. Local hardening overlaps native AI calls through a bounded pool,
   preserves sequential paid-ledger calls, reports same-day no-op honestly, and includes engine cost.
20b. **Dream-state convergence seam.** SECOND SLICE DEPLOYED AND SHA-VERIFIED at `4f7fa734`. Real top GSC queries
   and impression weights now survive graph compilation into EvidencePacket, intent routing, prepared
   packs, and drafting; minor count drift is bucketed before evidence-hash invalidation. The global
   shell header shows the existing durable autonomous receipt without adding a control, and the unified
   allocator uses the tenant-explicit keyword-library reader. The second slice adds one tenant-explicit
   ResearchDossier per graph move from cached keyword volume/difficulty, live SERP patterns, AI
   prompts/citations/fanouts, clone briefs, and question coverage; it becomes part of EvidencePacket,
   draft inputs, staleness hashing, source references, and the persisted PreparedMove receipt. Next:
   prepare allocator-only winners through this same path without creating a second ranker. FOLLOW-UP
   DEPLOYED AND SHA-VERIFIED at `acca40f3`: the warm path now rebuilds the separate fused Changes snapshot after the worklist, so
   autonomous evidence/ranking changes paint before Today and do not wait on Changes' cache TTL.
   THIRD SLICE DEPLOYED AND SHA-VERIFIED at `4191a90d`: the final actionable Changes order now persists
   a compact server-only ranked handoff. Worklist, AEO, SERP-steal, and keyword-library winners enter
   the same EvidencePacket/PreparedMove path with exact query/instruction, grounded seeds, and honest
   measured-demand provenance. Autonomous, prepare-ahead, auto-advance, and manual preparation use
   that order without a second ranker. FOURTH SLICE DEPLOYED at `06644b31`: native AEO cited pages,
   exact questions, fanouts, citation counts, and multi-winner consensus now survive into the same
   dossier and prepared move. The exact final top-ten order also receives cache-first live Google
   research; up to two on-topic winners per query are politely torn down before packets/drafts are
   built, and new-page moves are held before LLM spend when Google rejects them. No second ranker,
   button, cron, or publish path was added. FIFTH SLICE DEPLOYED AND SHA-VERIFIED at `287d7c3a`:
   immediately before final fusion, the visit runner completes missing exact keyword demand for up
   to 25 AEO-gap, SERP-steal, and graph candidates through one existing guarded batch. Cached volume
   attaches across every lane; it only breaks otherwise-equal unsized ties and never fabricates a
   forecast or changes sized-opportunity ranking. The autonomous receipt reports checked terms. No
   render-time paid call, second ranker, button, cron, or publish path was added. P0 TENANT-ISOLATION
   CORRECTION DEPLOYED AND SHA-VERIFIED at `dfa8ff8a`: authenticated evidence caught an Iranopedia
   Changes snapshot populated from Ritz through ambient dependencies inside an explicit background
   rebuild. Changes, worklist, Today, new-page, and competitor-audit reads are now tenant-explicit;
   stored surface snapshots carry and validate tenant identity; and an owned-domain guard rejects
   cross-domain edit targets. CUSTOMER-PATH CORRECTION CODE-COMPLETE at `940a2e40`: the legacy
   diagnostics index, filesystem-backed brain report, empty spike forensics, and allocator dump now
   return signed-in users to Today, Results, or the canonical Changes list. No customer-facing flow
   asks the operator to run scripts, configure environment values, or choose a diagnostic ranker.
   SIXTH SLICE DEPLOYED AND SHA-VERIFIED at `4779a1bb`: the on-visit cycle now repairs abandoned
   weekly page-factory work without a button or new cron, and Today keeps trustworthy Google data
   separate from background-work health. Exact winning pages receive up to 500 ranked-keyword rows;
   final topic seeds independently receive up to 1,000 related-keyword rows per call. Both corpora
   are 30-day cached behind the existing cost controls, and related rows feed the unified library.
   Prepared moves carry compact observed content blueprints and candidate sources through the
   existing verification gate. Topic sibling clustering, AEO fanout relevance, and redirect safety
   are tightened before display. Next: run one authenticated Iranopedia visit cycle, refresh Today,
   then inspect the top Changes and one PreparedMove. This is operational proof, not another
   architecture slice. AUTHENTICATED-PRODUCTION CORRECTION DEPLOYED AND SHA-VERIFIED at
   `198299e3`: `/today` is a compatibility redirect; an autonomous continuation writes a terminal
   receipt by 210 seconds and may continue from caches after a one-minute cooldown; the narrow
   page-factory repair runs after the primary brain; duplicate same-request GSC aggregate reads
   collapse; tenant source configuration hydrates before preparation; and up to five failing New
   Pages briefs automatically re-enter the exact-SERP source verification path. The internal
   page-factory receipt no longer renders as a red customer emergency. Next: one authenticated
   Iranopedia navigation, one refresh after four minutes, and inspection of one regenerated brief.
20c. **Instant autonomous surfaces.** DEPLOYED AND SHA-VERIFIED at `d685cd15`. Today and Changes
   retain their last complete snapshot across mutations, mark it stale instead of deleting it, and
   replace it atomically through the existing SWR path. The visit runner publishes a usable cached-
   evidence surface before expensive research, while the global status retains the last completed
   summary during refresh. Navigation never waits for the deep research pipeline; no cron, button,
   external integration, or publish permission was added. DELAY-RECOVERY FOLLOW-UP DEPLOYED AND
   SHA-VERIFIED at `90c3f843`: shared deadline fallbacks and Changes' cold/error states now retry
   once automatically, with a per-page 30-second session cooldown that prevents refresh loops. The
   user is no longer instructed to come back or manually refresh these states. RESULTS-MAINTENANCE
   FOLLOW-UP DEPLOYED AND SHA-VERIFIED at `c6b872d9`: authenticated Results visits schedule the
   existing bounded $0 measurement/reverification pass without an operator-mode dependency;
   failed `after()` registration cannot poison its throttle, and remaining normal-path recovery
   copy no longer asks for a manual refresh.
21. **Fresh blind unseen-topic benchmark.** Run 5 preregistered cases not used to build these
   corrections. A case that changes code is spent and must be replaced.
22. **On-use refresh receipt.** No Vercel schedule is a product dependency. The first authenticated
   visit after this deploy must leave its visit-runner receipt and any due connected-source receipt;
   those durable records, not an inferred nightly schedule, prove that autonomous upkeep is alive.
23. **Proof-model replacement with independent validation.** INFRASTRUCTURE LANDED, CERTIFICATION
   HONESTLY BLOCKED ON UNITS, NOT ON METHOD. The verdict classifier now runs under the independent
   statistical validation protocol (predeclaration contract plus a frozen-artifact holdout
   harness), but the release gate failed because the tenant cannot yet supply enough judged units
   per cell. Certification waits on more calendar or a pooled-verdict design.
24. **56 to 84 day confirmation ladder.** STORAGE AND WINDOW CONTRACT LANDED, 56 DAY READS
   PROVISIONAL UNTIL HISTORY SUPPORTS THEM. The demote-only confirmation tier above the 28 day
   primary read is in the contract and the store; a 56 day demotion is flagged provisional until
   this tenant's placebo history depth supports the window.
25. **June GSC UI comparison (operator).** The operator compares the stored June total of 3,460
   clicks and 251,274 impressions against the Search Console UI.
26. **Hosted UX and performance closure. PARTIAL, artifact boundary DEPLOYED AND SHA-VERIFIED at
    `1f3bb082`.** Server file tracing no longer packages local data/backups, source, tests, docs,
    scripts, migrations, or temporary files. Representative Vercel function size fell from 14.02 MB
    to 2.18 MB and repeated hosted route continuations stayed correct. Remaining closure still needs
    authenticated paint/interaction timing against the signed-in customer surfaces.
27. **Fix product gaps from holdouts.** Address whatever the fresh blind benchmark exposes.
28. **Second tenant.** Bring a second tenant onto the same workflow; Ritz stays cold-start until
    the operator reconnects it.
29. **Publish one operator-approved move.** Ship a single move the operator has approved, end to
    end.
30. **Verify live and measure under the corrected model.** Confirm the published move is live and
    measure it under the corrected proof model, never the quarantined one.
31. **Claim the learning loop only then.** Only after 1 through 30 hold may we claim the learning
    loop works.

## Recently landed (see VERIFICATION_LOG.md for the dated chronology)

The verdict quarantine (f298bd52 plus review batch b71cd1af), the cron invocation receipts with
/api/version and the honest Ritz initial-silence (b7b8a523, 3543e0d9), and before them the E-39 D4
wiring, the G3, G8, and G9 Wave 4 renders, the drafter cycle, and the hygiene batch are all on
main. Their per-commit receipts and gate numbers live in VERIFICATION_LOG.md. This plan no longer
stacks one section per landed commit; it carries only the live priority order above plus the
standing reference below.

## Wave 4 pilot - CLOSED 2026-07-11: /famous-iranian-singers

Wave 4 demonstrated that Beacon's components can support one known singers opportunity after
targeted engineering. It did not yet prove that Beacon independently discovers and executes
Armeen's workflow on unseen topics.

The singers case is retired to a regression fixture and may not be reused as acceptance evidence.
The real acceptance test is the fresh blind unseen-topic benchmark (priority 18 above).

## Parity-matrix ranked fixes - queued 2026-07-10

Ranked from the parity-matrix audit, highest leverage first:

1. **Cron reliability repair.** Per `cron_runs`, `sync-connectors` succeeded 3 of its last 8 runs
   and `precompute` 2 of its last 10; Profound has been stale since 2026-06-29 and SEMrush since
   2026-06-18. This, not a dead connector, is the real cause of stale data across the product
   (GA4 tokens are healthy; see the HANDOFF head-state correction). Highest-leverage fix in this
   list.
2. **Stale-crawl age trigger. DONE, DEPLOYED, SHA-VERIFIED at `0cbbac4d`.** The existing on-visit
   post-response cycle continues an unfinished owned-site frontier and force re-discovers a
   completed frontier once its last update is seven days old. It remains tenant-explicit, bounded,
   free, and independent of cron timing.
3. **Dismissal-reason capture. DONE, DEPLOYED, SHA-VERIFIED at `0cbbac4d`.** Changes' existing Not
   now menu passes a fixed structured reason through the canonical recommendation-response store;
   exact dismissals already feed the existing suppression/track-record path. The same menu retains
   one truthful one-week deferral and removes the false tomorrow/month labels.
4. **Consolidate/prune emitters.** Reduce duplicate or redundant recommendation emitters so the
   same underlying issue does not surface as multiple unrelated-looking cards.
5. **Insertion anchor + winners strip.** Give new recommendations a stable insertion point and add
   a winners strip so a shipped, proven win stays visible instead of scrolling out of view.

**I-59 final operating decision (2026-07-17):** Vercel schedules are intentionally empty. Normal
authenticated use is the primary trigger: the bounded post-response visit runner re-checks source
staleness, advances research/crawl/preparation, republishes cached surfaces, and records durable
visit/source receipts. Guarded `/api/cron/*` routes remain only as explicit maintenance endpoints;
customer health, readiness, and copy may not depend on or infer executions from them. Publishing
keeps its separate approval and safety path. If Beacon is not used, it makes no promise of a hidden
nightly run; when it is used, the work starts without a button and the prior complete snapshot stays
available while the replacement is built.

## E-39 adaptive control pools (LANDED in worktree e39-impl, operator-approved 2026-07-10)

Admit-with-caution replaces the active-control hard lock: a page serving as a comparison
page (or mid-measurement, or with a thin comparison pool) stays EDITABLE and carries an
attribution caution instead of `eligible:false`. Hard blocks stay ONLY for genuine hazards
(recent_no_lift, high_risk_page, ownership_uncertain, stale_research, and last_clean_donor
when releasing would leave a measurement with zero comparables). Verdict-lag repair
(resolveVerdictLag) settles at 28d + grace when GSC data is available and otherwise releases
the page + preserves the measurement + marks it blocked_data + retries within a bounded fair
window, never manufacturing a verdict from wall-clock age. Promotion writer annotates rather
than deletes. Adaptive pool: 2 comparison pages is the minimum defensible fallback (reduced
confidence), 3 is required for high confidence, fewer than 2 routes to a low-confidence
caution rather than a freeze; recording is capped at 3.

### FUTURE SLICE (E-39 D6 ledger) - the 56 to 84 day CONFIRMATION window

**CORRECTED 2026-07-10:** the claim below ("NOTHING calls a 28-day result final") was inaccurate
when written. An adversarial review found three rendered surfaces still doing exactly that
(cumulative-outcome.ts, proof-summary-section.tsx, changes-list-client.tsx), and a follow-up
sweep found two more (decision-thresholds.ts, results-header-strip.tsx). All five are fixed in
the review-fix commit (see VERIFICATION_LOG.md's 2026-07-10 "E-39 adversarial review P1 fixes"
entry) and pinned with tests so this class fails the gate next time.

The proof contract is 7-day early signal / 28-day PROVISIONAL verdict / 56 to 84 day
confidence tier. The 56 to 84 day tier is NOT built (the maturity ladder tops at 28d). E-39
did NOT lower the requirement: it audited measurement-maturity.ts and the surface copy so
NOTHING calls a 28-day result "final" or "highest-confidence" while the long tier is missing
(the mature explanation now reads "my 28-day read... the strongest confirmation window I
measure today"; the early/interim checkpoints say "The 28-day checkpoint opens..." not "Final
checkpoint"; the results summary says "my strongest read so far"). Building the full 56 to 84
day window is OUT of scope for this wave.

Acceptance criteria for the future slice:
- A fourth maturity tier (`confirmed_result` or equivalent) that only opens once a 56 to 84
  day window has closed with sufficient data, sitting ABOVE mature_result (28d) in the ladder.
- Only at that tier may operator copy use "final" / "highest confidence" language; the 28-day
  read stays "provisional / strongest so far" until then.
- PROOF_WINDOW_DAYS (measure.ts) extended to include the long window, with its own check-date
  math, learning-eligibility gate, and confidence mapping (a 28-day high can be revised by the
  56 to 84 day read; a revision is surfaced honestly, never silently overwritten).
- Tests: a 28-day result never renders "final"; a 56 to 84 day result may; a 28-day verdict
  that flips at the long window is reported as a revision with both reads visible.

### E-39 review P2 follow-ups (non-blocking, ledgered 2026-07-10)

Small, honest items the adversarial review raised that do not block this wave landing:

- `resolveVerdictLag`'s `markState`/`retryEligible` fields are computed but not yet wired to any
  caller that acts on them - a future slice should either consume them (persist blocked_data,
  drive the bounded retry) or fold them back into the function until a caller exists.
- A record older than 35 days that never ran any window reads as "stale" -> `inconclusive` under
  `outcomeStateOf`, which can trip the same-family `recent_no_lift` hard block on a page that was
  never actually measured (no data, not a proven loss). Needs reconciling with the `blocked_data`
  state in a future slice so "never measured" and "measured and lost" stay distinguishable.
- The LWW (last-write-wins) upsert path can race an operator's manual override under concurrent
  writes; pre-existing behavior, not introduced by E-39, but still open.
- The retry queue's FIFO `slice(0, 15)` can starve pages that keep landing past the cutoff;
  fairness (e.g. round-robin or oldest-first across retries) is a future consideration.
- The stale `insufficient_controls` comment at `build-daily-candidates.ts:384-385` (said
  "excludes from selection", but D1's admit-with-caution model downgrades confidence instead of
  excluding) is fixed as part of the review-fix commit - one line, no behavior change.

> 🟢 **2026-07-10 latest - CURRENT HEAD STATE: TASK #230 TRUST-CORRECTION WAVE INTEGRATED, GATE
> GREEN, SAFE TO FAST-FORWARD.** A Codex adversarial audit of the W5/W9/spec-debt release reopened
> 3 P1 (source-to-draft single-token coverage too weak, SSRF protection incomplete - a
> DNS-check-then-connect gap rather than an actual pin, and stale release-ledger doc entries
> claiming pushes that had not happened) plus 3 P2 (an ambient-tenant file fallback and three Ask
> providers that could read without an explicit tenant; a per-request deadline that did not bound
> a multi-hop redirect chain; a non-ASCII numeral coverage gap in the per-sentence check, closed
> below). Three lanes, built in parallel worktrees, merged conflict-free: Lane 1 source-coverage
> (480c70c0, 1742dee0) - per-sentence factual coverage with a negation guard. Lane 2 SSRF-pinning
> (6b139e98) - a socket-pinned fetch (a per-hop undici Agent binding the connection to the exact
> approved IP) with a full parsed-CIDR policy and a whole-draft verify deadline. Lane 3
> tenant-isolation (36dbea5c) - explicit-tenant fail-closed reads for the file fallback and the
> three remaining Ask providers. Integrated tip aa8dac5e; first hermetic gate there was red
> (fullgate7.log: 2 files / 3 tests failed) because Lane 1's new per-sentence factual coverage rule
> is stricter, and two pre-existing caller suites (src/lib/business-config.test.ts tenant-isolation
> pin; two governance/fail-soft cases in src/domains/page-factory/production-line.test.ts) had not
> been updated to match. **eb056890 fixed both fixtures** (Lane 1's rule itself was correct and is
> unchanged); full hermetic gate GREEN at eb056890: typecheck_exit=0, test_exit=0 (1425 files /
> 22188 tests passed, 62 skipped), build_exit=0 (fullgate8.log). An adversarial re-audit then
> cleared all 3 P1 + 3 P2 above with no P0, and surfaced + closed one more P2 in this same pass: a
> Persian/Arabic-Indic numeral claim was invisible to the per-sentence protected-number check
> (ASCII-only), so it fell into the weaker zero-protected branch instead of being matched against a
> source's excerpt - fixed locally in source-authority.ts, gate GREEN again (fullgate9.log). See
> HANDOFF_VERIFIED_STATE.md and VERIFICATION_LOG.md for full detail. **Current order:
> (1) architect/operator: fast-forward origin/main to this tip - no code changes are gating it;
> (2) W9 Ask Slice 2 - the multi-provider planner routing the other 8 wrapped providers live
> (unblocked once this lands); (3) operator: run the OAuth acceptance checklist
> (docs/OAUTH_ROOT_CAUSE_2026-07-09.md) and confirm the Vercel production build for ee89c14b in
> the dashboard; (4) W10/W11/W12 explicitly not started per operator instruction.**
>
> 🚢 **2026-07-09 - W5 draft safety release SHIPPED to main (1de8ea67; origin/main has since
> advanced to ee89c14b, folding in W9 Slice 1 + B-15/E-36 + the Task #230 wave above), hosted
> smoke operator-blocked.** W5 (J-69/70/71/73 + C-25) closed the six-finding operator stop-ship
> audit (F1-F6: P0 SSRF in the source fetcher, P0 a weak 50 percent token-overlap check, four
> P1s - ambient tenant read in after(), re-verify gated on due measurements, verifyState
> downgrade, retry starvation) with a redesign implemented in the isolated worktree, committed
> (a01a5fc3, 81d2c500, 1de8ea67) and pushed (fast-forward d43ff7e3..1de8ea67; hermetic full gate
> 1418 files / 22087 passed / 0 failed / 62 skipped; adversarial review no P0, one DNS-rebinding
> TOCTOU residual documented and accepted at the time - closed for real by the Task #230 wave
> above). Deployed = Vercel build confirmation unavailable to the agent (no token); production is
> reachable-checked and serving fresh (HTTP 307/200/307). Authenticated both-tenant smoke is
> operator-blocked (no smoke credentials). The per-tenant OAuth reliability wave + operator-spec
> waves (W1c/W4/W6) from the prior release remain on main underneath this one. Service accounts
> were REJECTED (parked at 31af9539, reverted by 90cebafb); per-tenant OAuth is canonical.
>
> 🚢 **2026-07-09 later - W9 Ask Slice 1 DONE, PUSHED (commit 6c808c09, folded into origin/main
> ee89c14b).** Fact-provider registry over the 9 existing fact-assembly.ts loaders (all
> prodLive: true, every source Supabase-backed) + permanent grep-based denylist guard (no
> connector-token/budget-ledger/raw-payload imports under src/domains/ask, ever) + deterministic
> zero-LLM count/rank/list answers (page_ranking + site_trend always bypass the LLM; any class on
> "how many"/"list" phrasing) + site_trend wired end to end with freshness and provenance in the
> rendered sentence ("Data through <date>, from my Search demand read."). Gate at the rebased
> tip: ask suites 8 files / 119 tests green; full hermetic gate receipts in fullgate5.log.
> **Next: Slice 2 (multi-provider planner routing the other 8 wrapped providers live).**
>
> 🚢 **2026-07-09 later - SPEC-DEBT B-15 + E-36 DONE, PUSHED (commit 118d5252, rebased onto the
> W9 tip 6c808c09, folded into origin/main ee89c14b).** B-15: Today's "What to do next" cards
> deep-link /changes?focus=<changeId>; the Changes list opens + scrolls that exact row's own
> detail panel (verified on live Iranopedia data). E-36: the LIVE auto_revert path is deleted -
> decideRevert is propose-only, the nightly pass counts but never pushes, the operator-gated
> one-click restore is the ONLY execution path, with first-person ask-first copy and
> confirmation-required pin tests. Commit 118d5252; 262 targeted tests + typecheck exit 0.
> **Next: resume the queue per the head state above.**

> ❓ **2026-07-03 - MASTER PLAN R17a DONE (worktree, NOT committed): P2 GSC depth pack slice 1 of 3 - brand split, ingestion-gap classification, striking-distance portfolio, anonymized-query share.** ONE brand classifier (domains/gsc/brand-split.ts, R9's tokens moved + re-exported) powers the Today scoreboard's non-brand growth sub-line and the three biggest growth claims now name their lens. Missing GSC days classify as final_lag/gap/pre_history; the connections GSC card names the missing dates and the nightly sync re-pulls up to 10 a night through the same per-day body (zero-totals marker stops quiet days re-flagging). The striking-distance portfolio ("N searches rank just below the top results... worth L to H extra clicks a month" via the tenant CTR curve) renders as the keywords hero second line + a Today demand-band line from ONE loader. Page dossiers note when over 30 percent of a page's traffic comes from searches Google keeps private. Typecheck clean; 68 new unit tests; ~1,060 targeted tests green; ratchet held at 1342. Details in `docs/HANDOFF_VERIFIED_STATE.md` + `docs/VERIFICATION_LOG.md`. **Next action: commit + push R17a when the operator merges the wave; after deploy, open Today + /research/keywords + /settings/connectors on real Iranopedia data and quote the rendered lens/gap/portfolio lines; queue resumes at R17b (P2 slice 2: v1 136-138, 195, 264, 268, 428, 491, 493).**

> ❓ **2026-07-03 - MASTER PLAN R14b DONE (worktree, NOT committed): P1 trust receipts slice 2 of 2 - receipts everywhere, see-the-math completion, named controls, spend joins, the how-i-decide registry, CSV export. R14 pack CLOSED.** ONE shared receipt convention ("From your Search Console data through Jul 2, checked just now.") now sits under the 10 most trust-critical assertions (Today scoreboard x2, cumulative strip on Today + Results, counts tiles, plan panel, /changes list, /results band, /keywords hero, /activity header, data-sources collapse) - loader field additions only, zero new reads. The strip's dollar figure opens into its per-win rows + THE ONE DOLLAR RULE sentence (breakdown pinned to sum to the figure); sized change rows open their opportunity-math inputs. /results card charts draw each change's first 2 comparison pages as dashed lines with "Compared against /a and /b, chosen before shipping." /activity now shows spend rows ("I spent $0.03 checking live Google results for 12 keywords.") from llm_budget_ledger, and cards show their move's prep spend when > $0. /settings/how-i-decide lists every live threshold from ONE registry that imports the enforcing constants (pin test holds it to the source values). "Download as spreadsheet" on /results + /activity (GET routes over the same loaders). 60 new tests; typecheck + architecture (ratchet 1342 held) + domains + app suites green. Details in `docs/HANDOFF_VERIFIED_STATE.md` + `docs/VERIFICATION_LOG.md`. **Next action: commit + push R14b when the operator merges the wave; after deploy, open /results and /settings/how-i-decide on real Iranopedia data and quote the rendered receipts; queue resumes at R15 (N4 behavior-verdict lane).**
>
> ❓ **2026-07-03 - MASTER PLAN R12 DONE (worktree, NOT committed): T0e new-site golden path - "grow any website I want" made real.** A stranger (or the operator with a new site) types ONE address on the new `/onboard` root: Beacon probes it honestly, saves domain + derived name to the pending tenant, persists the config, runs a bounded first look, and lands on `/onboard/done` with a real scorecard (pages read + honest "about N total", missing titles/descriptions, thin pages, questions answered, ONE deterministic first win with numbers, skippable Search Console connect with www-safe property auto-detect, keep-scanning, the R3 finish-setup card; unreachable = clear sentence + retry). Underneath: a RESUMABLE Vercel-safe crawl queue (new `crawl-frontier` store, Supabase-mirrored; 15 pages / 45s per batch, 150-page cap, idempotent page ids shared with in-process-scan) continued nightly (cron phase 1d-2b, pending tenants included) and on demand; day-0 baselines seed the question library from the crawl (new `loadCrawl` source) + queue 5 Google checks through the EXISTING capped gauntlet (dry-run default, $0). Stalled signups (quiet > 24h) surface on /diagnostics with one-click resume. 47 new tests + contract updates; typecheck + architecture (4526) + all touched suites green. Details in `docs/HANDOFF_VERIFIED_STATE.md` + `docs/VERIFICATION_LOG.md`. **Next action: commit + push R12 when the operator merges the wave; after deploy, walk the real journey once (fresh domain on /onboard) and quote the rendered scorecard; queue resumes at R14b.**
>
> ❓ **2026-07-03 - MASTER PLAN R14a DONE (worktree, NOT committed): P1 trust receipts slice 1 - /activity audit log + append-only verdict revisions + "We got this wrong" recap + "Why not the others?" inspector.** /activity (new nav route, system group) composes ONE reverse-chronological stream from existing stores only: shipped changes with who shipped them (autopilot vs you), plan approvals/clears, cron receipts under plain names, failures above the spike floor, connection events; paged 50, deadline-bounded, honest empty state, every row deep-links. Verdicts that flip on a re-measurement now append a tiny {at, from, to, reason} revision at the measureRecord seam (never rewritten; `verdict_revisions jsonb` applied to beacon-main) and the /results card says "I first called this a win; the 28-day read revised it to no clear effect." A self-hiding recap below the /results bands owns retracted wins + proven-did-nothing changes (max 5, each linking its card). Tonight's plan record freezes the planner's own exclusions (cap 8) and the daily card renders them behind a quiet "Why not the others?" expander. 58 new tests; typecheck + results/proof-gsc/experiments/ops/architecture suites green (design ratchet holds at 1342). Details in `docs/HANDOFF_VERIFIED_STATE.md` + `docs/VERIFICATION_LOG.md`. **Next action: R14b, the receipts-everywhere + see-the-math sweep over the remaining P1 items (91, 173-177, 204, 214, 334-339, 390, 405, 463-464, 523-525).**
>
> ❓ **2026-07-03 - MASTER PLAN R10b DONE (worktree, NOT committed): P4 measurement rigor slice 2 - distinct-query growth + equivalence + FDR + clean-window salvage, closing R10.** Four computed-only proof-gsc attachments (R10a's pattern: recomputed per measure, recordToRow untouched, verdicts/clocks never touched): the win card now names reach vs depth ("18 more searches; the win is reach, not just rank"), a mature non-win with tight bounds is a PROVEN "did nothing" that N10 grades solid-for-learning (distinct from inconclusive), too-close wins among many simultaneous measurements get the champagne-hold caution and demote solid to decent (pool-wide adjustment at the loadProofLedger choke point, no lab word on any surface), and a shock-muddied window salvages its >= 10 clean days right under the weather caveat. Typecheck clean; proof-gsc 767 + ledger consumers 106 green. Details in `docs/HANDOFF_VERIFIED_STATE.md` + `docs/VERIFICATION_LOG.md`. **Next action: commit + push R10b when the operator merges the wave; queue resumes at R12.**
>
> ❓ **2026-07-03 - MASTER PLAN R11 DONE (worktree, NOT committed): N30 question universe + N20 SERP consensus + N29 snippet capture.** One demand-ranked question universe per tenant (GSC question queries + AI fanouts + tracked questions + People also ask; ownership from the N2 registry; coverage checked against the owner page's stored extracts; nightly cron phase 2a-q persists the `question-universe` store) now feeds the FAQ/answer-block drafters, a self-hiding /prompts "Questions people ask that no one answers well" section, and top-3 questions on New Pages cards. Teardown briefs gained a 3-of-5 `consensusSpec` with named single-winner outliers pinned never to reach a brief. A format-matched featured-snippet capture trigger (rank 2-10, capped 5, deduped against steal-lane cards) rides the existing candidate pipeline. Typecheck + ~8,600 targeted tests green; live ground truth blocked by a Supabase 522 outage this session (all consumers verified honest-empty). Details in `docs/HANDOFF_VERIFIED_STATE.md` + `docs/VERIFICATION_LOG.md`. **Next action: after the next successful nightly sync, quote the rendered /prompts universe rows on real Iranopedia data; queue resumes at R10/R12.**
>
> 🧭 **2026-07-03 - FINISHED PRODUCT FP4 DONE (worktree, NOT committed): route names finally match the product.** /worklist -> /changes and /proof -> /results (permanent 308 redirects preserving query strings; the old /changes redirect stub died; every internal link/revalidatePath/palette/chord retargeted, G C = Changes, G E = Results). Titles + breadcrumbs now derive from ONE route registry (`routeCrumbFor` in `src/lib/navigation.ts`); detail pages push their real subject via `<HeaderTitle/>` (prompt text, change title, page path) so no surface renders a raw slug or bare "Detail". Legacy sweep: /expansion + dead moves-worklist-client deleted; shims retargeted; /review /briefs /local /observations /settings/history /topics/opportunity /competitors/[id] verified live-linked and kept. Settings: ONE `settings-sections.ts` registry feeds both the tab strip and the /settings index (Connections leads, sidebar group heading dedup). Ask chips dedupe + render human surface names; `plainChangeKind()` ends edit_meta/add_answer_block leaks. Verified: typecheck clean, ~7,600 tests green across routes/architecture/shell/domains suites, live dev renders quoted in `docs/VERIFICATION_LOG.md`. **Next action: FP waves continue per the master plan (freshness/S5 was queued next before FP4; re-rank at the next planning pass).**
>
> 🎨 **2026-07-02 - FINISHED PRODUCT wave 2 item FP6b DONE (worktree, NOT committed): worst-3 raw-styled files migrated to the FP6a primitives.** `today-moves-card.tsx` (597 raw-palette classes -> 69), `changes-list-client.tsx` (340 -> 34), `daily-experiments-section.tsx` (280 -> 0) all migrated onto `Card`/`Pill`/`SectionHeader`/`EmptyState` + tokens + the five-size type scale; `RAW_PALETTE_BASELINE` lowered 2884 -> 1773. Zero logic changes (D6 session loop, UX3 split panel/multi-select/batch-collapse, Measuring-tab canonical badge all byte-identical); 176/176 targeted tests green before and after; one honest class-assertion pin fix. Kept deliberately: MoveCard's 4 action-tone identity colors + Research/Prepared boxes, ChangesListClient's 10 FAMILY_CHIP lever colors (all content-type identities, not verdicts). Full detail in `docs/HANDOFF_VERIFIED_STATE.md` and `docs/VERIFICATION_LOG.md`'s 2026-07-02 FP6b entries. **Next action: migrate `war-room-sections.tsx` (187, now the worst file) and `today-newpages-card.tsx` (131), the next FP6 wave.**
>
> 🧹 **2026-07-02 - MASTER PLAN v2 item UX5 DONE (worktree, NOT committed): the legacy deletion sweep.** Two independent research agents plus direct verification confirmed and deleted the `/pages`, `/topics` (index only), and `/workbench` routes outright - zero inbound links from anywhere reachable, confirmed twice - keeping `/topics/opportunity/[id]` (live via Settings -> Imported history -> a result -> Related Opportunities) and the whole `src/domains/pages/` + `src/domains/recommendations/` domain layers (both massively shared foundations, never route-exclusive - the operator's own caution in the task brief). The route audit surfaced a much bigger second wave: 4 Today components dead since the 2026-06-16 legacy-TodayClient deletion, and 12 more dead since the 2026-06-28 `today-v2-sections.tsx` deletion (Command Center, off-site tile, edit-lifecycle tile, enrichment v2/badges, implementation queue, lifecycle strip, morning brief, poll-health calm banner, and their support files) - each host deletion had left dependents and their architecture contract tests stale for days to weeks, an accumulating pattern this sweep closes out. 9 architecture tests trimmed to their still-live pins, 9 more deleted outright, `docs/ARCHITECTURE_INVARIANTS_CATALOG.md` synced per its own documented option-(a) precedent (delete the row when the file is deleted outright, don't mark retired). Also swept: a dead `/recommendations?v2=1` working-rail component, the fully-dead `domains/insight` State-of-the-Union chain, 4 zero-importer legacy `/today` data files, one dead competitor-intel loader, the never-wired `BEACON_AUTO_PROMOTE_SCHEMA` flag, and 67 orphaned one-off scratch scripts (58 independently `git grep`-verified zero-reference + 9 more from doc/audit call-outs). One live dead-end link repointed (`today-newpages-card.tsx` -> `/worklist#new-pages`, was `/pages`). Net: 161 files deleted, 48 edited, ~28,300 lines removed. `npm run typecheck` clean; full `npm run test` 18988 passed / 62 pre-existing skips / 1 pre-existing unrelated failure (confirmed zero diff from this sweep, inside the concurrent N14 agent's in-flight `war-room-sections.tsx` rewrite). Dev server curl: `/`, `/worklist`, `/proof` 200; `/pages` `/topics` `/workbench` now correctly 404; `/recommendations` still 200 (redirect intact); `/topics/opportunity/[id]` still 200. Full ledger in `docs/HANDOFF_VERIFIED_STATE.md`'s top entry. **Next action:** the next item in the locked UX/N sequence per the operator's live priority call - UX5 was the last named UX item in the current wave.
>
> 🕸️ **2026-07-03 - MASTER PLAN v2 item N14 SHIPPED (worktree, NOT committed): the general interference graph.** New `src/domains/proof-gsc/interference-graph.ts` (pure) generalizes N12 (not yet built) and N13 (shipped) into one composed model: for any target ship, which OTHER in-flight ships could bleed into its measurement, via five edge kinds - `linked_page_treated` (crawled internal-link direct adjacency both directions, PLUS a control-dependency source that reuses control-contamination.ts's own ship-date-in-window test rather than a live-measuring check, catching a control that already settled its own 7-day verdict but whose ship date still falls inside a dependent's window), `same_template_family` (first-path-segment grouping, requires the other ship measuring or a real window overlap), `sitewide_event` (algorithm-weather.ts's `overlappingShock` reused verbatim), `redirect_related` (honest permanent no-op - no redirect store exists in the codebase), `query_overlap` (intent-clusters.ts conflicts plus a ledger-native `targetQueries` Jaccard overlap needing zero SERP spend). Two additive consumers: `daily-experiment-planner.ts` gained an optional `interference` lookup + a new `"interference_hold"` exclusion reason with a plain-English `plainReason`; `verdict-reliability.ts` gained an optional `interferenceFlagged` input that demotes an otherwise-clean mature grade to "shaky". Both byte-identical when omitted. 52 new tests (43 interference-graph + 5 planner + 4 verdict-reliability), full proof-gsc+experiments suites (1071) green, `npm run typecheck` clean project-wide, full `npm run test` 20097 passed / 1 unrelated pre-existing failure inside the concurrent UX4 agent's in-flight `war-room-sections.tsx` rewrite. Ground truth on the real 25-ship Iranopedia ledger via `scripts/ground-truth-interference-graph.ts`: 25/25 ships carry a significant edge, 302 total (181 linked/control-dependency, 112 same-family, 9 sitewide); the documented 7-ship `/persian-male-names` contamination cluster from N13 reproduces exactly as 7 control-dependency edges. Rendered hold reason: "This page links to /finglish, which I shipped a change to on 2026-07-01 and am still measuring. 4 more interference signals also apply." Rendered N10 grade effect (same ship, isolating the new input): "solid" without it, "shaky: a linked or same-family page still measuring could bleed into this result" with it. Full detail in `docs/HANDOFF_VERIFIED_STATE.md` and `docs/VERIFICATION_LOG.md`'s 2026-07-03 N14 entry. **Next action:** wire `computeInterferenceGraphForLedger` into `build-today-preview.ts`'s live nightly planner call and the `/proof` card's `gradeFromPresentation` call - deliberately NOT done yet in this pass since today's real-data signal hits 100% of ships, too aggressive to flip live without an operator review of how many real candidates the honest floors would hold first.
>
> 🗞️ **2026-07-02 - MASTER PLAN v2 item UX4 DONE (worktree, NOT committed): Today as a concise briefing.** Teammate pills, the alert-first pipeline placement, applied-batch collapse, the measuring one-line strip, friction severity bars, and the AI funnel with clickable stalled stages were all already shipped by earlier waves - verified unchanged, not rebuilt. Shipped this pass: a deterministic **lead story** card (`src/domains/changes/lead-story.ts`, pure, 9 tests) picking the single highest-signal item right after any pipe alert - a landed verdict beats a fired alert, beats tonight's top pick, beats the day's biggest click-mover, all from data the page already loaded; **chart tabs** (`scoreboard-chart-tabs.tsx`, client) adding Google/AI visibility/Visitors/Value tabs over series the scoreboard already fetches, self-hiding a tab under 2 points; a **health four-state split** (`data-sources-strip.tsx`'s `summarizeDataSourceHealth`/`dataSourceHealthLine`) replacing "All data sources connected" with "N connected, M healthy[, K needs attention]" so it can never contradict an alert above it; a **demand section split** into three labeled mini-headers (Searches moving this week / Research gaps / Language gaps); the **top-3 new pages** footer link reworded to "See all N in Changes"; and the **refresh control moved into the page header** (`PageHeader`'s new children slot), reading one shared connected-source count with the strip below. One broken pin fixed (`ops-pipeline-section.test.ts`'s literal self-closing-tag match, updated for the new `PageHeader` children usage). Full targeted sweep 734 passed, 1 pre-existing skip, 68 files; `npm run typecheck` clean. Ground-truthed live on tenant-iranopedia via `curl -m 200`: lead story renders "The change on /best-persian-restaurants did not work (shipped Jun 21). Here is what I learned."; health line renders "5 connected, 5 healthy."; new-pages footer renders "See all 10 in Changes ->"; all three demand mini-headers render. Full detail in `docs/HANDOFF_VERIFIED_STATE.md`'s top entry. **Next action:** UX5 (legacy deletion sweep) per the locked UX0 -> UX1 -> UX2 -> UX3 -> UX4 -> UX5 order.
>
> 📥 **2026-07-03 - MASTER PLAN v2 item UX3 DONE (worktree, NOT committed): Changes as a dense inbox.** `/worklist`'s compact rows gained a split detail panel (`selectedId` lifted to `ChangesListClient`, same `RowDetailContent` renders in a sticky `lg+` side panel without reflowing the list; narrow screens keep the prior inline expansion), an applied-batch collapse ("Tonight's batch: N applied, all verified" for `selectedForToday` changes that reached verify/measuring/result, expandable to receipts, scoped to the flat status views only), a single "Prepare tonight's plan (takes a minute)" command (`prepareTonightsPlanAction` composes the existing enrich/prepare/improve pipelines; the three granular buttons moved into a `<details>` "More options" overflow), buyer-language strategy labels (Best opportunities / Fastest growth / Safest bets, display-only), and multi-select (checkbox per row + a floating Mark done/Skip bar running the same per-row actions sequentially with honest progress text). D6 (next-best advance, shipped counter, j/k/enter/d) preserved exactly, all pre-existing session-loop tests pass unmodified. 33 new tests; full targeted sweep (`(shell)/` + `domains/changes/` + `domains/allocator/`) 586 passed, 1 pre-existing skip; `npm run typecheck` clean. Ground-truthed live on tenant-iranopedia via `curl -m 200`: strategy picker shows all three buyer-language labels, a real compact row (persian female first names, Title chip, "clicks dropped 51%", Review + Skip, checkbox) renders across 196 rows, "Prepare tonight's plan" + "More options" both render; batch summary correctly stays silent today (0 rows currently selected-for-today in verify/measuring/result, confirming the collapse is scoped to tonight's plan, not any measuring row). Full detail in `docs/HANDOFF_VERIFIED_STATE.md`'s top entry. **Next action:** UX4 (Today as a concise briefing) per the locked UX0 -> UX1 -> UX2 -> UX3 -> UX4 -> UX5 order.
>
> 🏆 **2026-07-03 - MASTER PLAN v2 item N10 SHIPPED (worktree, NOT committed): one verdict-reliability grade.** New `src/domains/proof-gsc/verdict-reliability.ts` (pure) combines every N10 feeder already living on `MeasurementPresentation` (recrawl N11, contamination N13, weak comparison, weather/seasonal overlap, shared attribution, sample sufficiency, optional permutation-null) into one grade: "too early" / "shaky" / "decent" / "solid", each with plain reasons and a first-person sentence. `gradeAllowsLearning()` is a verified superset-safe proxy for `learningEligibility`. Wired computed-only into `/proof`'s card (chip next to the badge + sentence inside "See the math") and a one-line addition to `fact-assembly.ts`'s measurement facts. 32 new tests, 148 tests across every importer confirm zero pins broke, `npm run typecheck` clean. Ground truth on the real 25-ship Iranopedia ledger: 9 shaky + 16 too early, 0 solid/decent today (honest given N13's contamination backlog and N11's empty recrawl sweep). Full detail in `docs/HANDOFF_VERIFIED_STATE.md`. **Next action:** N14 (full interference graph) is the next Quality Constitution law-4 item, or apply the pending N13 `control_donor_pool` migration so new ships can eventually earn a live "solid" grade.
>
> ⏸ **2026-07-02 — MASTER PLAN v2 item N9 DONE (worktree, NOT committed): pause recommendations when data sources contradict (law 1).** See `docs/HANDOFF_VERIFIED_STATE.md` top entry for the full build. Short version: `src/domains/evidence/source-contradiction.ts` (new, pure, 3 rules with documented floors, never fires on absence) wired into the one choke point every rec passes through (`reviewRecommendation` in `recommendation-quality.ts`) as a new `paused_source_contradiction` decision that `passesDailyGate` already excludes from the nightly plan/Prepare; a self-hiding "paused until the data agrees" line was added to the daily card. Ground-truthed live on tenant-iranopedia: 4 real contradictions found (GA4 shows real visitors, GSC shows 0 clicks, on 4 pages). **Next 3:** (1) thread GA4 + live-SERP evidence into more call sites so rules (a)/(b) fire wherever that data is already loaded, not just build-today-preview.ts's rule-c path; (2) N10 (one verdict-reliability grade) is the next Top-50 item in operator order; (3) a spawned follow-up task flagged a pre-existing GA4/GSC page-signal key-collision bug (relative-path vs absolute-URL keys holding different numbers for the same page) worth a dedicated fix.
>
> 🟢 **2026-07-01 — ASSISTANT-FIRST PROGRESS (branch `claude/daily-experiments-native`, merging to main each turn).** Against the north-star sequence below: **S1 ✅**, **S2 ✅** (intent-aware LLM writer, slices C/D-1/D-2/D-3), **S3 ✅** (full "how we know" battlefield on the daily card: keyword volume+competition E-1 `2b3c675d` + live SERP reaction + competitor "steal this" E-2 `247ef279`), and **Phase 1d ✅** (competitor "steal this" now also on the `/worklist` MoveCard `05298d8a`; the map showed it already had keyword+SERP so only the steal-line was added; card made dash-clean). **DataForSEO is LIVE** — real Iranopedia data populated (keywords 214 terms; daily card 9/12 SERP + 4/12 teardown; MoveCard 5/12 steal + 3/12 SERP; ~$0.17 of the $50 cap). **NEXT (S5 + freshness):** (1) **continuous live refresh** — an operator button (or light cron) that re-runs the keyword+SERP+competitor populate for the tenant's top pages so the brief never goes stale (today it was seeded by a one-off script; `enrichTopResearchPacksAction` already covers keyword+SERP on /worklist, competitor audits need a trigger); (2) **S5/Phase 3** — verifier Wix hero-placement false-negative, wrong-field after-the-fact popup, GSC quota-aware friendly queue. Vercel env flags for full prod-live listed in HANDOFF. App-wide visible-dash sweep remains a separate spawned task.
>
> 🎯 **2026-06-30 — THE REASONING ENGINE (operator north star, supersedes prior next-steps).** After the first real Daily Experiment batch the operator's verdict: LOVES the new nav; HATES that Beacon became a "clinical experiment console" instead of "a friendly assistant that tells you what to do and learns in the background." Core intelligence flaw: on `/chaharshanbe-suri` Beacon proposed surfacing a DEFINITION when the page's #1 query is "chaharshanbe suri 2026" (194 impr / 0 clicks = DATE demand). Directive, verbatim: **"every touchpoint should literally be maximum reasoning"** — per page: up to ~500 batched keywords (cheap, cached forever) → query fanouts → live SERP checks → steal the best from every competitor page → an LLM-reasoned, STRUCTURED "here's why this is the smartest move" brief that CONVINCES ("we researched your keywords → these are the best to attack → we live-scraped the pages stealing your citations → here's what they did better → therefore this exact change wins"). LLM **batched + tiered** (cheap model default, escalate only when hard); cost is NOT a constraint if we're genuinely far smarter. Feel = modern Claude/ChatGPT tool. Keep the science (controls/proof/measurement) but **run it in the background**. Operator applies manually for now (keep it dead-simple); he's converting DYNAMIC pages to Wix SEO variables → armed auto-push for dynamic pages later (static stays manual). Memories: [[project_convince_me_reasoning_card]], [[feedback_assistant_not_experiment_console]], [[project_reasoning_gap_answer_intent]].
>
> **BUILD SEQUENCE:**
> - **S1 ✅ SHIPPED — Intent reasoning brain** (`src/domains/experiments/answer-intent.ts`, 11 tests). Deterministic $0: classify what a page is ACTUALLY asked (impression-weighted: when/what/how/cost/where/who/list/compare) → score each candidate answer against that intent → pick the RIGHT answer + emit a structured `AnswerReasoningBrief` (chosen / intent / cited evidence / rationale / alternatives / confidence / honest GAP) + a commerce/CTA firewall. LIVE PROOF on chaharshanbe: intent=WHEN (55%, 194 impr) → GAP "write the date answer, do not move a definition" (inverse of today's failure). Not yet wired into the batch — S2.
> - **S2 — Wire S1 + tiered LLM adjudicator into the daily plan** (`build-today-preview.ts`): after the deterministic pick, one batched cheap-model-first LLM pass that (a) confirms/repairs the answer vs real intent, (b) WRITES the answer when missing (the chaharshanbe date), (c) authors the "why this is the smartest move" brief. Reuse dormant `llm-draft-gateway` / `draftAnswerBlockWithLLM`.
> - **S3 — Maximum-evidence brief:** ~500 batched keywords (cached forever) → fanouts → LIVE SERP + competitor teardown ("what the pages beating you did better") → one structured, persisted PreparedMovePack that survives handoffs; wire dormant SpecialistOpinion/MoveRouter.
> - **S4 — Re-skin to the assistant:** friendly card ("Do this → exact text → why it's smartest → I'll watch it"); hide controls/reservations/proof/verification jargon behind a "how we know" expander.
> - **S5 — Kill robot work + friction:** fix the live-verifier Wix visual-vs-DOM false-negative ("buried #3" on a correct hero placement); pre-flight wrong-Wix-field catch (pahlavi title-vs-description); GSC-quota-aware batching; then armed push for dynamic pages.
> - **S6 — "Beacon learned…"** plain-English outcome surface when proof settles.

> ✅ **2026-06-30 — REASONING ENGINE S1 WIRED + ground-truth correction (branch `claude/profound-iranopedia-intel`).** Two things landed tonight (operator away; "as much analyzed/figured out"):
> **(1) S1 is now WIRED — the chaharshanbe class of bug is fixed at the source.** `proposeSafeAnswerBlock` (`safe-answer-block.ts`) took `topQuery` but never used it — it blindly promoted the first DEFINITIONAL sentence, so "chaharshanbe suri 2026" (a WHEN/date query) surfaced the festival's *definition*. It now classifies the top query's intent (`classifyOneQuery`) and picks the firewall-clean candidate that best ANSWERS it (`scoreAnswerForIntent`): a date for "when", money for "cost", a definition for "what". For a non-definitional intent, if nothing on the page answers the real question it emits an **honest GAP (null)** instead of promoting a definition — the LLM writer / operator fills that gap. Deterministic, $0, no LLM, no publish; only future batch GENERATION changes (proposals the operator reviews), never the accepted/measuring plan. tsc 0 · 28/28 tests (answer-intent 11 + safe-answer-block 17, incl. 3 new: WHAT→definition, WHEN→date, WHEN-with-no-date→gap). The bug-enshrining test (WHEN query asserting the definition wins) was CORRECTED, not weakened.
> **(2) GROUND-TRUTH CORRECTION — the structured-LLM layer S2 wanted to "build" is ALREADY BUILT + wired.** A 6-agent design pass proposed building a new adjudicator/schema/drafter; verification against the code shows: `llm/schemas.ts` (9 zod draft schemas — AnswerBlockDraft/AtomicEditDraft/CreatePageBrief/AeoPromptBrief/… each carrying `evidenceRefs`≥1 + confidence + risks + operatorSteps + proofPlan) and `llm/structured-drafter.ts` (gate→budget→call→robust-parse→zod→content-firewalls[numeric-fidelity/placeholder/em-dash/superlative]→retry-once→fail-closed) already exist and are the PROD drafting path, **called from `demand-graph/prepare-today-moves.ts`** (draftAnswerBlock/AtomicEdit/CreatePage/CROFix) + `serp/prepare-create-page-verdicts.ts` + `prepared-move-pack.ts`. So the real remaining work is **WIRING, not rebuilding** (do NOT duplicate these — CLAUDE.md reuse rule).
> **REVISED gaps (what's actually left):** (S2') route S1's GAP → the existing `draftAnswerBlockStructured` so the LLM WRITES the missing answer (e.g. the chaharshanbe date) grounded in page+scrape, landing as an operator-approve-before-live proposed change — wired into the DAILY batch (`build-today-preview.ts`/`build-daily-candidates.ts`), which today does NOT call the structured drafters (only the demand-graph "Prepare" flow does); (S4) the friendly re-skin (hide the lab-console jargon behind "how we know"); (S5) the live-verifier Wix DOM-vs-visual false-negative + wrong-field pre-flight + GSC-quota batching; (S6) the "Beacon learned…" surface. Full build-ready specs from the design pass persisted at `tasks/wawc5907i.output` (98KB). **NEXT (operator, when back):** flip on S2' in the daily batch (approve-before-live) after eyeballing one written chaharshanbe-date proposal.

> 🟢 **2026-06-29 — DATAFORSEO PRODUCER SHIPPED (main `fbb0075e`).** Operator-triggered, dry-run-default producer fills the research caches off the render path: `extractSerpPattern` + `planResearchEnrichment` (pure) + `enrichResearchPacks` (gauntlet-backed) + `enrichTopResearchPacksAction` + consumer (`serpPattern` on the card: "SERP rewards: …") + "✦ Enrich research packs" button on `/worklist`. Top-5 dry-run ≈ $0.10 (1 volume + ≤5 SERP). tsc 0 · 23 tests · build PASS · $0. PageResearchPack v1 is now functionally complete (audit→brain→live cards→ownership→element plan→volume consumer→SERP producer). **NEXT 3:** (1) operator clicks "Enrich research packs" for the live dry-run numbers, then sets `DATAFORSEO_DRY_RUN=false` to populate real volume + SERP patterns onto the cards; (2) **attack the ~100s cold `/worklist` render** (operator-flagged as too slow for a real product — cache the heavy demand-graph/loaders read or persist a V2 surface; the producer was built off-render precisely so this is the clean next target); (3) deferred PageResearchPack tail: keyword-expansion Labs endpoints, cannibalization intent classifier, proof-maturity 7/14/28d labels, card progressive-disclosure.

> 🟢 **2026-06-29 — PAGERESEARCHPACK v1 LIVE-WIRED (main `0d90afa1`).** The research brain is now on the live top cards: `today-moves-data` assembles `PageResearchInput` per top existing-page move + attaches `researchPack`; the shared `MoveCard` renders "Research — what this page should own" (Own / Cross-link don't merge / Do first / Skip-proof) + reconciles the old cannibalization "fold" copy to "keep separate + cross-link" (boy↔girl bug gone); `planResearchSpend` gives the dry-run estimate (top-5 ≈ sub-$0.20). tsc 0 · 26 tests · build PASS · no console errors. Live browser render NOT captured (dev cockpit ~100s+ hydration; verify on prod). **NEXT 3:** (1) **operator: eyeball `/worklist` on Vercel prod** to confirm the module renders on the top-5 (the one open success-criterion); (2) **operator: flip `DATAFORSEO_DRY_RUN=false`** to populate live keyword VOLUME + SERP winner-title study into the packs (≤$0.15/run, $50 cap), then wire those numbers onto the keywords + element plan; (3) optional: harden the `/worklist` heavy-load render (the ~100s cold load is the pre-existing cockpit fragility). **DEFERRED (unchanged):** keyword-expansion Labs endpoints, cannibalization intent classifier (full), proof-maturity labels, card progressive-disclosure.

> 🟢 **2026-06-29 — PAGERESEARCHPACK v1, PHASE 1 SHIPPED (main `a7e62c69`); scope = v1-on-existing-surface (operator).** Pure research brain `page-research-pack.ts` (intent clustering own/sibling/new_page/internal_link/noise + keyword→element map + proof-aware levers; tenant-agnostic, $0, 10 tests). **NEXT (the live increment, $0 in dry-run):** (1) assemble `PageResearchInput` from the live loaders for the top-5 existing-page moves (GSC ranking/losing/fanout/competitor/page-facts/proof already loaded in `today-moves-data`); (2) apply the EXISTING DataForSEO keyword-VOLUME (`dataforseo-keywords.ts`) + SERP winner-title/format STUDY (`dataforseo-serp.ts`) through the dry-run-default gauntlet so the pack carries real volume + SERP patterns; (3) surface a compact "Why this exact move" research module on the card (primary intent, owned keywords + elements, blocked levers, SERP pattern). Then operator flips `DATAFORSEO_DRY_RUN=false` to populate live (est ≤$0.15/run, $50 cap). **DO NOT (this slice):** build keyword-expansion Labs endpoints, the cannibalization intent classifier, proof-maturity labels, or the card progressive-disclosure UI — all deferred.

> ✅ **2026-06-29 — SENIOR RECOMMENDATIONS, SUB-SLICE 1 SHIPPED (main `7b93ed61`).** Logic-first trust fix from a 7-agent Phase-0 audit (the brain is real; the OUTPUT layer was templated). Shipped: evidence-based intent-aware titles with a reason per option (universal "Complete Guide" killed); lost-lever gate (no_lift title move → drop title options + "Try a different lever"); Clarity %-clamp (no "400%"); self-competition display fix (no "X vs your X"). Live-verified on Iranopedia `/worklist`: Complete-Guide-BEST 0, Explained-BEST 0, no_lift gate fired, max dead 100%, 0 collisions. tsc 0 · 14+15 tests · build PASS · $0. **NEXT (operator-directed, Max): PageResearchPack — keyword-universe → intent map → SERP study → page-element plan for the top-5 pages.** Use DataForSEO as a research analyst (not a validation badge): seed from the page (current title/H1/queries/losing-queries/AI-fanout/competitors) → expand (related/questions/long-tail/SERP) under a DRY-RUN + explicit spend cap → cluster intent (same page / sibling / new page / internal link / noise) → map keywords to page elements (title/H1/H2/FAQ/answer-block/link/schema) → study the SERP (winner title + format patterns) → emit evidence-cited recommendations. Hard rules: no Wix, no proof-row mutation, paid only after dry-run + cap (stop if over), cache all results, no generic titles without supporting evidence. **DEFERRED to later sub-slices (do NOT do in PageResearchPack):** compounding-edit warning, cannibalization intent classifier (same/related/different phrasing), proof-maturity 7/14/28d labels, card progressive-disclosure UI.

> ✅ **2026-06-29 — PROOF LOSS → ACTIONABLE DIFFERENT-LEVER FOLLOW-UP (main `0231d478`).** Verify-first: the lost-outcome chip + lost-row visibility already existed (`/worklist` shares `MoveCard` with `/`; `/proof` shows "Did not help" + rose) — no rebuild. NEW: deterministic family-keyed `suggestNextLever()` → `OutcomeCaution.nextLever`, so a no-lift loss now nudges a DIFFERENT lever (rendered as `↳` on the shared card). Live: `/funny-farsi-phrases` → "try an answer block, internal links, or a UX fix"; no measuring boosts. tsc 0 · 15 caution tests (2 new) · build PASS · 5 routes 200 · $0. **NEXT 3:** (1) accumulate ≥3 decided per family → broad pattern prior engages (usage + time, no code); (2) operator-merge `claude/profound-iranopedia-intel`; (3) broad-topic brief pass-rate (≤$0.05 regen).

> ✅ **2026-06-29 — PROOF LOOP CLOSED (migration applied, settlement persists, learning ACTIVE) (main `fb840a80`).** Applied the missing additive `operator_verdict_override` column to beacon-main via the Supabase MCP (verified single project = beacon-main) → proof writes now persist (no more PGRST204 file-split). Ran the real measurement once: 2 due rows settled `measuring→lost`, durably confirmed via fresh Supabase read (`{measuring:7, lost:2}`), 0 collateral. Learning activates honestly — page-specific `no_lift ×0.9` caution live; broad prior withheld at 2<MIN_DECIDED=3; no measuring boosts. tsc 0 · 79 tests · build PASS · 4 routes 200 · $0. The Move→Ship→Prove→Learn loop is now closed + self-feeding (settles as the operator opens /proof). **NEXT 3:** (1) accumulate ≥3 decided outcomes per family → the broad pattern prior engages (no code; just usage + time); (2) operator-merge `claude/profound-iranopedia-intel`; (3) broad-topic brief pass-rate (≤$0.05 regen).

> 🟩 **2026-06-29 — PASSIVE AUTO-MEASURE on /proof + CRITICAL persistence bug (RESOLVED above) (main `6cf9061a`).** Built the passive /proof auto-measure (fire-and-forget `next/after`, operator-gated, throttled, +3 tests) + banner + a loud-warn hardening on `upsertShippedChange`. WHILE verifying end-to-end, found the real blocker: **proof settlement has never durably persisted on beacon-main** — `recordToRow` emits `operator_verdict_override` but that column was never added (migration `2026-06-23_…_operator_verdict_override.sql` UNAPPLIED) → every verdict upsert PGRST204 → silently routed to the ephemeral `.data` file. That's why all 9 rows are stuck `measuring`. **#1 ACTION (operator-gated, I lack the mgmt token): apply that additive migration to beacon-main** (`scripts/apply-migrations-mgmt-api.mjs`); then the passive trigger + manual button settle due rows and the wired prior/caution activate. tsc 0 · build PASS · 4 routes 200; the new trigger is necessary-not-sufficient until the column exists. **NEXT 3:** (1) **apply the proof migration** (above) → re-run measure → confirm 2 rows settle + learning activates; (2) operator-merge `claude/profound-iranopedia-intel`; (3) broad-topic brief pass-rate (≤$0.05 regen).

> ✅ **2026-06-29 — PROOF→RANKING ACTIVATION AUDIT → loop fully wired; "outcome-prior" SATISFIED (main `dc7ed140`).** Verification-first: the outcome-prior loop is NOT a frontier — it's wired end-to-end + tenant-correct + covered by 39 tests (settled-only priors ±15%, page caution ±10%, measuring never boosts, card `🧠` chip). Live: 9 proof rows all `measuring`/0 settled (→ 0 priors, correct); GSC fresh to 06-26; 2 rows due now but un-measured because `autoMeasureDuePass` runs only from the operator action, not render fire-and-forget (crons off = on-demand by design). **No code shipped — nothing was missing.** The prior "auto-measure loop / outcome-prior into ranking" next-step is hereby marked DONE (was already built in Sprint 3). **NEXT 3:** (1) OPTIONAL operator-gated — fire-and-forget `autoMeasureDuePass` on `/proof` render so due rows settle passively (or just click "Measure now"); (2) operator-merge `claude/profound-iranopedia-intel`; (3) broad-topic brief pass-rate (≤$0.05 regen).

> ✅ **2026-06-29 — NEAR-DUPE AUDIT, remaining ActionPack types → NO EXTENSION (main `3832600f`).** Evidence-first: audited edit_existing_page/add_answer_block/add_internal_links for the create-page/hub near-dupe pattern. ALL apparent clusters are DIFF-URL (distinct pages sharing a topic token, e.g. 12 dynasty-flag pages); same-URL near-dupes = 0. Existing-page actions are URL-keyed + already exact-deduped, so collapsing would destroy real per-page work → **no extension**, only a guard comment on `CONTENT_DEDUP_TYPES`. The ActionPack near-dupe surface is now fully + correctly handled (create-page 140→101, create-hub 42→31, existing-page intentionally untouched). **NEXT 3:** (1) operator-merge `claude/profound-iranopedia-intel`; (2) broad-topic brief pass-rate (optional ≤$0.05 regen); (3) auto-measure loop — feed proof verdicts into ranking (the open back-half).

> ✅ **2026-06-29 — CREATE-HUB PACK DEDUP (main `37a98202`).** Extended the canonical collapse to `create_hub` packs. `collapseCreatePagePacks` → `collapseCreateContentPacks` groups each create-content actionType (create_new_page + create_hub) in its own bucket (hub↔page never merge), reusing the conservative grouper + evidence merge. Live Iranopedia: create_hub 42→31 (11 absorbed), total packs 315→305; create_new_page unchanged (101). tsc 0 · 165 tests (4 new) · build PASS · 5 routes 200 · $0 spend.

> ✅ **2026-06-29 — CREATE-PAGE DEDUP MOVED UPSTREAM (graph + ActionPack, main `21b43a60`).** Landed the board-side canonicalization to main (FF `a9f2435d..ef2d3a67`), then removed the root cause: the product no longer EMITS sibling create-page opportunities. `collapse-create-page-siblings.ts` (pure load-graph post-pass) dedups create_page MOVES (→ `MoveCandidate.canonicalGroup`); `collapseCreatePagePacks` (action-pack) dedups create_new_page PACKS across the demand-graph + Profound-coverage sources (→ `ActionPack.canonicalGroup`). Both reuse the one conservative grouper; the New Pages board reads the metadata (its own grouper removed). Live Iranopedia: graph create_page **40→31**, ActionPack create_new_page **140→101**. tsc 0 · 161 tests (17 new) · build PASS · 5 routes 200 · $0 spend. **NEXT 3:** (1) extend the same collapse to `create_hub` packs (the coverage compiler emits ~42 hubs with their own near-dupes); (2) operator-merge `claude/profound-iranopedia-intel`; (3) broad-topic brief pass-rate (optional ≤$0.05 regen).

> ✅ **2026-06-29 — NEW PAGES CANONICALIZATION (board-side, main `5f5de85c`) — superseded by the upstream dedup above.** Closes the New Pages chain's last gap (canonicalization + brief pass-rate, NOT more API). Pure conservative grouper `canonical-create-page.ts` collapses near-duplicate create-page candidates into ONE canonical card (merge only on same matched keyword OR identical distinguishing-token set; weak-keyword merges collapse but don't inherit; etiquette/literature stay separate). `today-newpages-data.ts` drops absorbed siblings, shows "Also covers: …", inherits a high-confidence sibling's passing brief (so the 14.8k nowruz card stays brief-ready). `CREATE_PAGE_SYSTEM` tuned to forbid invented counts on broad topics. 33→24 groups; tsc 0 · 122 tests · build PASS · 4 routes 200; **$0 spend, no migration/Wix/SEMrush/flags. NEXT 3:** (1) lift broad-topic brief pass-rate further (gate/prompt, optional regen ≤$0.05); (2) canonicalize near-duplicate move labels at the GRAPH level so siblings never emit upstream; (3) operator: merge `claude/profound-iranopedia-intel` → main + deploy.

> ✅ **2026-06-26 — PROFOUND AEO PRODUCT SLICE SHIPPED TO PROD (main `fd58a316`, Vercel build success).** The coherent slice the operator sequenced is live: (1) durable Profound storage + on-demand sync + fast cached reader (migration applied); (2) Prompt-to-Page Coverage Compiler + operator diagnostic (`/diagnostics/profound-coverage`); (3) AEO **evidence fusion** into demand-graph Moves (`MoveCandidate.aeoEvidence`, attached in load-graph from the cached store — no live API on render, evidence-only, no score/action change); (4) New-Pages **"✦ AI-validated" badge**; (5) **"Draft AEO brief"** button on `/moves` (reuses the capped/gated structured drafter). GA4 dollar-value test red proven PRE-EXISTING on origin/main before the FF-merge. **NEXT (operator-stated):** harden the cockpit `/` New-Pages **8s-timeout fragility** so the badge/brief render reliably on `/` too (the section self-hides under heavy concurrent load + the legacy `sync-nightly.ts` bots/referrals 403s; `/moves` is reliable). Optional perf: per-prompt rollup table → sub-second light read. Deferred relevance follow-up (spawned task): compiler `internal_link_fix` generic-token over-match ("famous Iranian films → athletes") needs an LLM/curated relevance gate — held operator-diagnostic-only, never a customer action.

> 🏗️ **2026-06-25 — TEAM-ARCHITECTURE EXECUTION IN FLIGHT (master plan: `~/.claude/plans/…twinkly-balloon.md`).** Beacon → a superhuman SEO/AEO/growth execution team where each connector is a specialist (see HANDOFF "PRODUCT UNDERSTANDING" + B74). **Sprint 1 (foundation) — ✅ DONE:** P1 `SpecialistOpinion` + 8 specialists, P2 `MoveRouter` (debate→decision, vetoes/boost/downgrades, never mutates the pure scorer), P3 `PreparedMovePack` (state machine + projected persist via `move_drafts`, no migration), read-only wired into Today Moves; `tsc` clean, 38/38 tests, ground-truthed on real Iranopedia. **Sprint 2 — Prepared Today Moves — ✅ DONE (B75 + B76):** P4 structured LLM schemas (zod added; AnswerBlockDraft/CreatePageBrief/AtomicEditDraft/ToolAssetSpec/CommerceAssetSpec/CROFixSpec/ExperimentPlan, each carrying evidenceRefs; validate→retry-once→fail-closed; first prod caller of the gated drafter) → P5 `prepareTodayMovesForTenant` + "✦ Prepare my top 10" (operator-gated, cache-first, capped) → **10/10 Iranopedia Moves reach `ready_to_review`** with structured drafts + experiments + proof plans, surfaced as a prepared-readiness strip on each card (~$0.026 LLM, no SERP, no migration, no main merge). **Sprint 3 — Learning loop — ✅ DONE (B77, branch `claude/sprint-3-learning-loop`, NOT merged):** auto-measure engine (`autoMeasureDuePass`, reuses measureRecord, $0) + multi-dim bounded outcome-prior (`learning/experiment-prior.ts`) wired into the R&R score (post-`buildDemandGraph`, never mutating the pure scorer) + mark-applied/measure operator actions + "Beacon learned" UI. No migration, no publish, no paid calls; +22 tests; live-verified neutral (no settled outcomes yet → zero-risk). **Sprint 4 (later):** P6 DataForSEO Keywords/Labs demand engine, P7 Trend Radar, P8 Store/Product — the growth-expansion wave. **Hard rails (all sprints):** Iranopedia first, tenant-agnostic/no-hardcoding, never auto-publish (Ritz hard-blocked), no destructive migration / no broad uncapped paid runs / no spend-cap increase without operator approval, structured-JSON LLM only (no loose product blobs), never mutate the pure graph/scorer contracts. **NOT yet:** trend radar (P7), store/product engine (P8), Ritz local (P16), backlinks (P17), CMS adapter (P14).

> ✅ **2026-06-24 — ENGINE LIVE FOR IRANOPEDIA + PREMIUM COCKPIT SHIPPED (see HANDOFF B42/B43).** Steps 1–6 + the activation are DONE: `BEACON_DEMAND_GRAPH_RECS=tenant-iranopedia` is set in Vercel prod; 10 high-confidence answer-block Moves flow into the live queue; the cockpit `/` leads with the premium "Today's Moves" ritual (hero + one-tap Ship + paste-ready Draft + CTR Title Lab) and a "New Pages to Build" board (create_page wedge + teardown). Adversarially verified (no isolation/correctness/crash bugs; the one perf finding — double graph compute — fixed via `loadDemandGraphForTenantCached`). Ritz/Finglish provably unaffected (sections self-hide with no engine data). **NEXT 3 (verified levers):** (1) **LLM-draft integration for demand-graph Moves** — the LLM path (`draftProposedTextForCandidate` + openai provider) currently serves the AEO *specific-edit* recs, NOT the demand-graph EvidencePacket; a new bridge (demand-graph packet → LLM gateway) is needed before flipping `BEACON_LLM_PROVIDER` gives the Moves real paste-ready copy (cost bounded by the fail-closed $200/mo cap). (2) **/recommendations queue premium parity** — make the Drafts queue render the same rich Move cards the hero does (the hero's "See all" lands there). (3) **operator: connect Ritz GSC** (+ DataForSEO key) to light up the engine for tenant #2. ⚠️ Local dev can't render heavy-Supabase sections (Node22/undici gzip `transformAlgorithm` bug) — ground-truth loaders headless (`BEACON_TENANT_ID=… DATA_SOURCE=supabase tsx`), not the dev server.

> 🚀 **ACTIVE (historical): Beacon v1.0 Rank-&-Revenue Engine** (spec `.claude/plans/…twinkly-balloon.md`, Iranopedia-only for now). Core loop = Graph → cited competitors → teardown → Change Pack → precompute → ship/proof. **DONE:** Step 1 (real graph + `/diagnostics/rank-revenue` Top-25) `c553086`/`43ee5f3`; Step 2 (competitor cited-page edges + create_page) `38747ad`; Step 3 (deterministic competitor teardown) `b353c7e`; Step 4 (gap-compiler → EvidencePacket: `evidence-packet.ts` + `gap-compiler.ts` + per-row packet expander; deterministic skeleton, NO LLM) `5655258`. **NEXT:** Step 5 new-page + ASSET engine — lift `push/cluster-factory.ts` to demand-driven `create_page` + add `create_tool`/`create_calculator`/`create_offer`/`create_lead_magnet` action types + `tool_gap` signal + a factory mode that briefs the asset spec (the `missing_tool` gap in the EvidencePacket already detects these). Then Step 6 precompute, L7 DataForSEO (operator credentials), L8 CRO, L9 link-authority. **Operator-gated before turning on:** LLM drafting (Step 4+ stays deterministic skeletons until OK'd) + DataForSEO. Follow-ups: apply `competitor_page_audit` migration for Vercel; surface create_page packets separately (rank below GSC-demand top-25); Profound visibility positional-decode. Follow-ups: apply `2026-06-24_competitor_page_audit.sql` (operator) for Vercel persistence; Profound visibility positional-decode bug. Lever 0j (Profound max-extraction) is now largely realized by Steps 2+; the visibility-report positional decode (`"Mystery Of"` in date col) is an open follow-up.

> 🔭 **NEXT LEVERS (from the 2026-06-23 autonomous big-moves loop — 15+ ships incl. audit-6 cold-start 5-bug batch; see VERIFICATION_LOG/HANDOFF).** Ranked by impact toward the autonomous-brain dream, with the exact blocker for the deferred ones:
> 0. **Cold-start generic-tenant catch-22 — CLOSED (2026-07-17).** Launch inference accepts two substantial semantic article pages and post-crawl inference accepts five substantial structured non-local pages even when Article JSON-LD is absent. Address or phone always vetoes publisher classification; one incidental post, four pages, and pages with service/location signals remain generic. This lets schema-less publishers receive the schema/content help they need without broadly flipping local businesses.
> 0b. **Experiment-planner proof-integrity — RETIRED/NO ACTION (2026-07-17).** The referenced batch-planner loader was removed with its dead State-of-the-Union execution chain. Do not resurrect dead code to satisfy a historical audit note; the live proof ledger's measuring-only and compound-package exclusions are independently pinned.
> 0c. **getSiteConfig tenant-ification — CLOSED (2026-07-17).** Site config is tenant-keyed, resolves the explicit tenant's business domain/brand before global fallback, and accepts tenant identity through URL helpers. Live async entity/competitor readers and registry/scan CLIs pass explicit tenant identity; A→B isolation is tested.
> 0d. **Audit-wave-2 — CLOSED (2026-07-17).** New page IDs hash tenant+normalized URL while preserving existing IDs by URL; scan error/shell/partial persistence carries forward last trustworthy canonical snapshots and element rows; FAQ diffs carry prior counts; pre-native cold citation history abstains; content tenants use Article/Person/store schema expectations; GSC daily series fill internal zero days; unsupported AI citation claims are caught across the full bounded sentence while generic readability language remains allowed.
> 0j. **Profound MAX extraction — CLOSED/RETIRED (2026-07-17).** Durable prompt/answer/fanout tables, topic-scoped on-demand sync, cached reads, prompt-to-page compiler, evidence fusion, and page-specific recommendation wiring are live. The older standalone `analyzeProfoundAnswers` path is superseded by the richer coverage compiler. Automatic prompt creation into a borrowed provider workspace is intentionally not part of autonomous customer behavior; operator-managed provider scope remains the trust boundary.
> 0i. **Audit-wave-7 — CLOSED (2026-07-17).** Push caps fail closed on configured Supabase failure; Wix SEO schema reverts restore the captured tag snapshot; both mark-shipped actions require publish authority; fan-out queries use the canonical parser; accept fan-out is deterministic and retry-idempotent; stale URL maps trigger a live slug check; GA4 proof reads are ordered/paginated and reject partial data. The historical `issue-actions.ts` and url-organic adapter callouts no longer exist in the live tree and require no replacement.
> 0h. **Audit-wave-6 — CLOSED/RETIRED (2026-07-17).** Review merge identity now hashes normalized author+date+text across manual/connector IDs while exact provider IDs remain authoritative. Missing/invalid tenant segments coerce to `unknown`, which disables local engines and isolates pattern learning. The old State-of-the-Union chain was previously deleted as zero-import dead code, so its 3,000-row note is retired rather than rebuilt.
> 0g. **Audit-wave-5 Profound page mapping — CLOSED (2026-07-17).** The cached coverage compiler maps prompts to existing pages; the queue now emits the strongest unique page-specific answer-block candidates, one per URL and at most five. The old single homepage card remains only as a visibility-only fallback when richer assignments do not exist.
> 0f. **Audit-wave-4 — CLOSED (2026-07-17).** Owned entities require exact normalized alias equality; Profound “owned” categories require suffix-safe owned-host proof; spike explanations use the impact breakdown's single change count.
> 0e. **Audit-wave-3 — CLOSED/DECIDED (2026-07-17).** New native snapshots persist an exact cited-observation numerator in existing metadata and Today uses it; historical rows retain a bounded compatibility fallback. Citation decay abstains across the pre-native regime; Article/WebPage descriptions reuse the prose gate; Page Surgeon breadcrumbs walk root/intermediate/leaf and omit homepages; the existing nightly path runs resolved-card demotion then the 30-day/50-row machine-queue sweeper. URL watcher remains tenant-scoped by design because URL-change evidence is customer data, not a global pattern artifact.
> 1. **Crawl-staleness label + confidence downgrade — CLOSED (2026-07-17).** Matching crawl evidence older than 45 days adds the exact age to customer/operator copy and lowers high to medium. Staleness alone never hides a row; medium remains visible and fresh/unmatched rows are byte-identical.
> 2. **First Iranopedia measured verdict — CLOSED historically.** The additive override column was applied and real due rows settled; current Results/learning contracts are independently verified.
> 3. **Native nightly AI visibility — CLOSED historically.** `/api/cron/ai-engines` and `runEnginePollForTenant` provide the bounded multi-engine poll with tenant question isolation and cost gates.
> 4. **Today read caching — CLOSED historically.** Tenant-scoped canonical reads are windowed and request/SWR snapshots serve last-known-good data while background refresh rebuilds.
> 5. **SEMrush expansions freshness gate — RETIRED.** The table does carry `fetched_at`, but the SEMrush caller was removed caller-first and no live runtime consumes `semrush_keyword_expansions`; do not add TTL machinery to an unreferenced provider cache.

> 🟢 **STATUS 2026-06-16 — MAX_SEO_AEO AUDIT → 6-PHASE EXECUTION (P0 sequence).** Operator directive: turn `docs/MAX_SEO_AEO_EXPERT_AUDIT_2026-06-16.md` (500 findings) into phases; Iranopedia is the reference tenant but NOTHING Iranopedia-specific is hardcoded in domain logic (tenant config / Supabase rows only); Wix-first; review-gated publishing is non-negotiable. **Phases:**
> 1. **✅ CODE-COMPLETE (migration apply pending operator OK) — Wix url-map + collection-config → durable, tenant-scoped Supabase** (audit P0 #1). New `wix_collection_config` + `wix_url_map` tables (RLS, content_field_roles jsonb) + `src/lib/connectors/wix/mappings-store.ts` (Supabase-direct, ambient-tenant, durable upsert+delete-stale, file fallback) + `url-map.ts` delegation. Migration `2026-06-16_wix_mappings.sql` written but **NOT applied to prod** (awaiting approval; code falls back to file until then). Commit `412d033`. 11 new tests; 235 files/5,258 pass; adversarial-review-hardened.
> 2. **Wix collection-mapper UI** — discover collections/fields, suggest field roles, persist per-tenant via the Phase-1 store (replaces hand-edited JSON on /diagnostics/wix).
> 3. **AEO-honesty close** — recs never imply "0 AI answers" unless Profound evidence exists (largely done: BATCH B + PM-12; verify residual).
> 4. **GSC readiness** — property picker, selected-property state, backfill + freshness status.
> 5. **Push receipts** — before/after value, URL, source evidence, live verification, rollback path (first-class artifact on Changes + Recommendations).
> 6. **Daily Golden Path UI** — Refresh → Review → Approve → Verify → Learn.
>
> **NEXT 3 ACTIONS:** (1) operator approves applying `migrations/2026-06-16_wix_mappings.sql` to prod Supabase (via MCP) → Phase 1 DoD "durable in Supabase" fully met; (2) start Phase 2 (mapper UI) on the Phase-1 store; (3) (operator) reconnect GSC + add OpenAI billing (429 `insufficient_quota` blocks all LLM features — see VERIFICATION_LOG). Work is on `claude/iranopedia-blockers` (53+ commits ahead of origin/main; merge to main is an operator call).

> 🟢 **STATUS 2026-06-15 — GOLDEN PATH, CRONS OFF, ON-DEMAND.** Decision: GitHub Actions cron schedules are removed (no minutes); the nightly job's work is now operator-triggered from the `/diagnostics/connectors` cockpit — **Refresh all** (connectors) · **Run today's AI reading** (native poll) · **Recompute causal proof** — plus the existing **Promote** (`/diagnostics/recommendation-triggers`) and **Approve & Push** (Wix). The full operator runbook + exact Vercel env vars is `docs/GOLDEN_PATH_SETUP.md`. **Crons-off reality:** the DEFAULT (legacy) `/today`+`/recommendations` surfaces regenerate recs LIVE on render, so the zero-extra-setup path is connect → Refresh-all → Run-AI-reading → open the page; V2 (instant loads) adds a Promote click + 2 env flags. **Verified (ground-truth):** Iranopedia renders real recs ("Create a Persian Food page"); it has 70 shippable persisted edits (24 edit_title, 10 add_schema, 10 fix_schema, …) → Wix EDIT/SEO route. Fixed a `/today` statement-timeout (lean observation projection, ratcheted). **Next (operator, 5pm):** (1) set/confirm Vercel env per the runbook (`BEACON_LLM_PROVIDER=openai`+`OPENAI_API_KEY` to draft the 11 meta + 2 intro recs that lack text — deterministic works without); (2) connect Clarity/GA4/Profound/Wix on `/settings/connectors` (GSC already connected), then Refresh-all + Run-AI-reading; (3) review Iranopedia's queue → map Wix collection fields on `/diagnostics/wix` → Approve & Push. **Deferred (tasks #73/#74, non-blocking):** data-rich-tenant legacy-surface perf (V2 or aggregate RPC); in-process Vercel scan (GSC supplies inventory). Branch `claude/iranopedia-blockers`, NOT pushed (conserve-CI). Banners below predate this and are history.

> 🌙 **STATUS 2026-06-12 (midnight shift, PRs #80–#88): EVERYTHING OUTSIDE OPERATOR CONTROL IS BUILT.** The killer-feature trilogy below is live (Proof Engine firing + placebo-gated; forecast surfaces self-gating; calls feed flipped) and the connector layer is complete UI→fetch→tables→cron for all six sources (GSC, GA4, SEMrush, Profound, Clarity, Wix) — every sync step dormant-until-key with an honest skip reason, prod-verified by cron dispatch. The 35-check audit passed all ten P0s; every actionable finding is fixed. **The remaining steps are the operator's by design:** (1) paste keys / reconnect OAuth on `/settings/connectors` (GSC, GA4, SEMrush, Profound [Enterprise API access on request], Clarity, Wix); (2) decide the LLM-nightly policy (working implementation parked on `claude/llm-flip-on`; decision framed in PR #74); (3) tap Accept on the iranopedia queue (store-product schema cards push live; the rest paste-ready); (4) tenant #2 + `BEACON_CROSS_TENANT_BRAIN=1` to light the peer forecast; (5) top up OpenAI billing for full multi-engine polling. Sections below this line predate the cutover and are kept as history.

> 🧭 **DIRECTION — NEXT KILLER FEATURE: "Proof Engine + Move Forecast" (2026-06-09, from a 61-segment multi-agent codebase analysis).** The one system no AEO competitor has, buildable on the existing (test-green) foundation by surfacing engines that already exist: **forecast an edit's AI-citation lift from real peer outcomes BEFORE you ship, then prove causally what each shipped edit drove — down to phone calls.** (A) **Move Forecast** — feed `cross-tenant-brain` `CrossTenantPattern` (helpingRate + median_days) into `product/whatif-engine.ts simulateAction` behind the existing gate → "N builders like you shipped this; X% saw citations rise in ~D days" on the top Move. (B) **Proof Engine** — surface the already-hardened `attribution/natural-controls.ts` diff-in-diff as a "Proof" tab on Changes detail (adjusted-lift vs control pages; Helping/Hurting/Nothing-Yet; computed-vs-weak_estimate discipline). (C) **Close to revenue** — CallRail-shaped connector (clone `connectors/ga4/persist-url-traffic.ts`) → flip the two `qualifiedCallCount: 0` feeds in `outcome-attribution/load-outcomes-summary-for-tenant.ts` → Today/Changes show "received 14 sessions + 3 calls since you shipped this." **Build order: SHIP IT → turn on real recs (BEACON_LLM_PROVIDER + live-write) → Proof Engine slice (pure surfacing, no new math) → calls → Move Forecast once tenant #2 exists.** Full detail + the collapsed accomplishment log: `~/.claude/plans/enter-maximum-depth-planning-mode-twinkly-balloon.md` (collapsed 2026-06-09; original archived alongside). **Top remaining gaps to be worth $499/mo:** (0) ✅ DONE 2026-06-10 — stack deployed (PR #1 @ 871eff5, hosted smoke green); now run the live week; (1) turn on real recommendations; (2) prove ROI on one real edit; (3) de-hardcode Bay-Area/builder assumptions for vertical #2; (4) tenant #2 to activate the brain; (5) repeatable provisioning + connector auto-refresh; (6) fail-loud hosted alerting.

> 🟢 **COMPETITOR INTEL — "STEAL THIS MOVE" + "WHY THEM, NOT YOU" SHIPPED + PREVIEW-VERIFIED (NOT PUSHED, 2026-06-09).** Parking-lot ideas #4+#6 built as one domain (`src/domains/competitor-intel/`): rival page changes (sitemap + structural diff) joined with that URL's AI-citation aftermath → tiered moves (proven/early/watching/quiet, associative copy only) with a deterministic equivalent-action + the What-If evidence line + the gated peer-forecast seam; plus per-rival forensics (their page vs your closest match, jealousy-direction gaps, AI descriptor contrast, them-cited/you-not loss rows). Customer sections live on `/competitors`; operator `/diagnostics/competitor-intel` refresh (robots-respecting, bounded, no cron). 64 tests; suite 13944/0; **rendered live in dev preview** (proven-move card + full forensic report, zero console errors). **OPERATOR:** one click on `/diagnostics/competitor-intel` → "Refresh competitor intel" populates it with real rival data. **Follow-up flagged:** `answer-texts.json` exists (recon error earlier) → thread real answer quotes into why-them.

> 🟢 **MOVE FORECAST COMPUTE — BUILT + GATED (NOT PUSHED, 2026-06-09).** Killer-feature **half A** ("before you ship") — completes the trilogy (A forecast / B proof-already-surfaced / C calls-done). Pure composer `src/domains/product/move-forecast.ts` `buildPeerMoveForecast()` fills the gap where `simulateAction` returns `insufficient_data` (thin this-tenant history) using the cross-tenant brain's `CrossTenantPattern[]`: matches by action type, picks the strongest pattern, applies honest floors (suppress <3 peer attempts; seeded "early signal" caveat <10), emits one plain-English associative (non-causal) line. 11 tests; full suite green. **Inert until tenant #2 + `BEACON_CROSS_TENANT_BRAIN=1` + a customer Move surface** (producer returns [] at n=1; `simulateAction` is operator-diagnostic-only today) — same built-but-gated posture as the brain. Render activation = deferred runtime wiring.

> 🟢 **CONNECTOR REFRESH-ALL ORCHESTRATOR — SHIPPED (NOT PUSHED, 2026-06-09).** #5's single-tenant half ("connector auto-refresh"). Operator-only `/diagnostics/connectors` lists every cache-backed source (GA4, CallRail, Semrush) with status + last-refreshed + a single "Refresh all connected sources" button. `refreshAllDataSources()` composes the three already-gated per-connector refresh actions, runs them independently (one failing never blocks the rest), and classifies each refreshed/skipped/failed. **No cron added** (operator-triggered + poll-callable seam — the scheduled half is the constraint-gated/operator piece). 10 tests; full suite green. Keeps the whole outcome-attribution substrate current in one click instead of visiting each connector page.

> 🟢 **CALLRAIL CONNECTOR — REVENUE LOOP CLOSED (NOT PUSHED, 2026-06-09).** Killer-feature **half C** ("close it to revenue"). connect → fetch → attribute-by-landing-URL → persist → **feed the Mode A engine** → view, all local + green (46 tests, full suite 13824/0). Backend: `connector-store` callrail provider/token + `src/lib/connectors/callrail/*` (key-auth `/v3/a/{account_id}/calls.json` client, fail-soft, qualified classifier good_lead-OR-answered-≥60s, group-by-canonical-URL/UTC-day, refresh+cache + `loadQualifiedCallCountForUrl`/`loadRecentCallAttribution`) + migration `2026-06-09_call_url_attribution.sql`. **Feed-flip:** both Mode A loaders (`load-mode-a-for-changes-detail.ts` + `load-outcomes-summary-for-tenant.ts`) now read real qualified-call counts instead of hardcoded `0` — soft-fails to 0 (byte-identical) when CallRail is unconnected, so Today/Changes light up "received N sessions + M calls since you shipped this" the moment a key is connected. Surface: operator-only `/diagnostics/callrail` (connect form: key + account id → refresh → attribution-row view + soft-disconnect). **OPERATOR TO-DO to make it live:** (1) on `/diagnostics/callrail` (operator mode) paste your CallRail API key + numeric account id + click **Refresh calls** — I can't (no key). (2) apply the migration on deploy. (3) tune the qualified rule (answered ≥60s OR good_lead) per business if needed.

> 🟢 **SEMRUSH CONNECTOR — FULL LOOP SHIPPED (NOT PUSHED, 2026-06-09).** connect → fetch → persist → view, all local + green (32 tests, build 0, full suite 13791/0). Backend: `connector-store` semrush provider/token + `src/lib/connectors/semrush/*` (key-auth client, CSV parse, fail-soft, domain_ranks overview + domain_organic_organic competitors, refresh+cache) + migration `2026-06-09_semrush_domain_metrics.sql`. Surface: operator-only `/diagnostics/semrush` (connect form → refresh → snapshot view + soft-disconnect). **OPERATOR TO-DO to make it live:** (1) on `/diagnostics/semrush` (operator mode) paste your Semrush API key + click **Refresh metrics** — I can't (no key; avoids burning your week-limited units). (2) apply the migration on deploy. (3) connect + pull NOW while the key is active; soft-disconnect preserves the cached snapshot after the week. **Follow-ups (flagged):** a `/settings/connectors` SEMrush card + auto-merging SEMrush organic competitors into the tracked set / recommendation inputs (the connector + cross-check note are done; deeper auto-consumption is a deliberate product slice).


> 🟢 **SLICE — MVP AUDIT SWEEP + STRAGGLER TRUTH-UP (READY_TO_COMMIT — NOT PUSHED, 2026-05-26).** Audited 4 subsystems vs the max-depth plan: **§8 GSC + §9 Outcome-Attribution = VERIFIED COMPLETE** (no gaps); **§3 Brain = partial-by-design**; **§12 = N1+N4 done, N2/N3 open.** Caught+fixed a `dd4c52f` straggler (stale `process-global` mock fixture in `off-site-authority-page.test.tsx`) + 2 stale comments (`types.ts`, discipline-test). Repo-wide old-string sweep now clean. 3 code/test + 3 docs. Comment/fixture-only. Gates LOCAL ONLY: typecheck ✅ · targeted off-site ✅ · **full suite 13671 passed / 0 failures ✅**. Proposed commit: `chore(off-site): finish process-global truth-up + fix stale test fixture`. **READY_TO_COMMIT — not pushed. Local stack → 10 ahead.**
>
> **REMAINING SLICES (S1–S5) — verdict: product is feature-complete + green for n=1 dogfood; none of these block a one-tenant dogfood. Full prompts in `docs/FABLE_CONTEXT_CAPSULE.md` §5.**
> - **S1 — §3 per-tenant threshold wiring (Phase A.2 Step 3). ✅ DONE + VERIFIED GREEN (2026-05-26 correction).** Already shipped in `load-lifecycle.ts` (resolves per-tenant thresholds via `computeTenantThresholds`, threads into `deriveLifecycleStage`'s optional 2nd arg, source in cache key) + `render-copy.ts` (per-source copy: borrowed vs per-tenant "Computed from {N} cited shipped edits on this site"). Consumed by Today tile/strip + Changes `[id]`. Pinned by `threshold-decision-loader-wiring.test.ts`; 442 lifecycle/threshold/component tests green. `thresholds.ts` correctly stays borrowed-constants. **No further work** — earlier "pending" was an audit error (the §3 trace looked only at `thresholds.ts`, missed the load-lifecycle/render-copy wiring).
> - **S2 — §12 N2 ledger isolation pin + `cap_kind`.** ✅ **DONE 2026-05-26 (isolation-pin half):** extended `cost-controls.test.ts` with "only adjudicator-budget writes the `llm-budget` store" (bare-key scan + writer smoke; allowlist {adjudicator-budget, store-classification}). Confirmed only `adjudicator-budget.ts` writes the bare `"llm-budget"` json-store key. **STILL OPEN:** `cap_kind` annotation + the unified `recordSpend({ledger})` router (cost-path refactor — riskier; dedicated slice). **Balanced.**
> - **S3 — §12 N3 tenant-isolation. ✅ CLOSED via leak audit (2026-05-26).** Attempted the scoped version: audited the 39 highest-risk data-access functions (no-arg persisters/writers + loaders) by reading each body → **ALL properly tenant-scoped; ZERO leaks.** No comprehensive test shipped (the 5 valid scoping mechanisms can't be captured by one signature assertion → brittle; isolation already guaranteed by repo `forTenant` + RLS + 13+ targeted invariants). Comprehensive-scan sizing that proved brittleness: 995 exports / 31 tenant-bearing. Audit detail in VERIFICATION_LOG.
> - **S4 — §3 brain. SCRUBBER ✅ + AGGREGATOR ✅ + PRODUCER ✅ + SYNC-CHAIN SEAM ✅ + OPERATOR SURFACE ✅ + CUSTOMER TILE ✅ (2026-05-26).** Entire brain compute + surface layer built + green: `privacy.ts` (23) + `aggregate.ts` (14) + `producer.ts` (`computeCrossTenantPatterns`, gate-first, 7) + optional `crossTenantPatterns?` threaded through `buildSpecificEditEvidencePacket` + `buildPacketForRec` (102 packet tests unchanged) + `/diagnostics/cross-tenant-brain` (9) + `beacon-learned-tile` (8). **ONLY the runtime activation remains, and it's HARD-GATED:** the async ancestor that computes the producer + passes it into `buildPacketForRec` is the LLM-evidence runtime path — `buildPacketForRec` has no non-test runtime caller (the specific-edit packet build is `BEACON_LLM_PROVIDER`-gated, i.e., the no-LLM constraint), so wiring it is entangled with LLM-provider activation + needs the prod deps (per-tenant outcomes loader + `buildBlocklist`). Plus it's gated by `BEACON_CROSS_TENANT_BRAIN`(off) + returns `[]` at n=1. Contained activation slice; all deps built + proven.
> - **S5 — MT-5 deprecated no-arg overload removal. APPROVAL-GATED** (~23-call test migration + 5 invariant flips).
> - **S6 — §10 Phase B regenerate completion.** Gateway infra shipped + safe (operator-only, env-gated-OFF, budget/validator/provider-reuse/tenant-iso enforced) but non-persisting by design. Complete per §10 L1–L5: (a) brandAssertions ≥3 gate, (b) 5-min per-rec cooldown, (c) persist regenerated draft to `recommended_edits.metadata.regenerated_draft` NEVER overwriting original `proposed_text` + revert UI, (d) show-original-on-fail UI. Touches a live LLM-cost path → deliberate, operator-aware slice; (a)/(b)/(c-shape) local-feasible, but NO LLM execution in current no-LLM mode. **Max tier.**
>
> **AUDIT COVERAGE COMPLETE (2026-05-26):** §3/§8/§9/§10/§12/§4.5 deep-verified by agents; §2/§4/§5/§6/§7 confirmed-built. **§4.5 = production-ready (customer-queue flip live + 9 safety props pinned); §8/§9 = complete.** Product is feature-complete + green (13671) for n=1 dogfood.
>
> **NEED FROM OPERATOR (unchanged + restated):**
> 1. **Disable `daily-native-poll` + `poll-canary` in GitHub Actions UI now** (stop live minute burn; committed cron-thinning only bites after merge).
> 2. **At Actions-reset: approve a push** → land the now-**10-commit** stack → PR → merge → watch maiden CI Build gate → verify Vercel by SHA → hosted smoke → re-enable schedules → dogfood.
> 3. Approve S5/MT-5 if/when you want the deprecated overload gone. (S1 = shipped+green; S3 = closed via leak audit, 0 leaks.) **No open safe local slices remain — all attempted/dispositioned 2026-05-26:** S2-remainder = **preflighted execute-ready** (router reconciles `budget.ts`+`adjudicator-budget.ts` `recordSpend` across 4 call sites; NOT rushed — no marginal safety over the shipped isolation pin, `cap_kind` blocked on S6, dispatch-bug mis-accounting risk); S4 = constraint-blocked (≥2 tenants + LLM); S6 = Phase B persist (no-LLM-gated); S5/MT-5 = explicit-approval-gated. All further progress needs your push/deploy or an explicit greenlight on a deliberate slice.

> 🟢 **SLICE — OPS-SAFETY: THIN POLL SCHEDULES + CI BUILD GATE (READY_TO_COMMIT — NOT PUSHED, Actions minutes exhausted, 2026-05-26).** Pre-dogfood hardening: `daily-native-poll` + `poll-canary` thinned 3/day → 1/day (backups commented, `workflow_dispatch` kept; `daily-scan` untouched) → net scheduled fires **7/day → 3/day**; `ci.yml` gains a `npm run build` gate (tenant env) so CI catches Vercel-breaking build/route issues. Local-only verified: 4 workflows parse (js-yaml) ✅, build green at `dd4c52f` ✅. Proposed commit: `ci(actions): conserve minutes — thin poll schedules + add build gate`. **READY_TO_COMMIT — not pushed.**
>
> **LANDING RUNBOOK (operator-gated — needs explicit push approval + the Actions reset window):**
> 1. **Stop the live burn NOW (no push):** disable `daily-native-poll` + `poll-canary` in the GitHub UI (Actions tab → "Disable workflow"). Instant, reversible. This is the ONLY pre-push way to stop the burn — the committed cron thinning takes effect only once landed on `origin/main`.
> 2. **At the reset window, land the stack** (now **9 commits** ahead of origin/main: C7d · C7e · MT-3A · MT-3B · MT-3C · MT-3C.2 · MT-4 · footer-copy · ops-safety). Local gate already green at `dd4c52f` (typecheck ✅ · test 13671 ✅ · build exit 0 ✅) — only `npm run build` was the prior gap and it's now closed.
> 3. **Push `claude/objective-davinci-c81e70` → open PR → merge to `main`** (or fast-forward per your preference). The merge CI run is the **maiden execution of the new build gate** — watch it; if it needs env beyond `BEACON_TENANT_ID`/`BEACON_TENANT_SLUG`, add the secret(s) or mark `/rank` dynamic.
> 4. **Verify Vercel by SHA:** confirm prod/preview is serving the merged successor of `dd4c52f`, build passed, then hosted smoke the core read-only pages (`/`, `/recommendations`, `/changes`, `/prompts`, `/diagnostics/off-site-authority`).
> 5. **Re-enable thinned schedules** (they're already in the merged file) — confirm one clean `daily-native-poll` + one `poll-canary` cycle with no duplicate/no-op burn.
> 6. **Start dogfood** after the reset + one clean scheduled ops cycle + no hosted-smoke regressions.
>
> **FOLLOW-UP flagged (NOT done):** deeper Actions optimization — make the poll/canary preflight decide-to-skip BEFORE checkout/install (the review's "avoid checkout/install on duplicate/preflight-noop paths"). Bigger refactor; out of this tiny ops-safety slice. Also still open: `off-site-authority/types.ts:7` doc-comment truth-up + MT-5 (deprecated no-arg overload removal).

> 🟢 **SLICE — OFF-SITE `data_sources_note` COPY REFRESH (READY_TO_COMMIT — NOT PUSHED, Actions minutes exhausted, 2026-05-23).** The MT-4 follow-up — refreshed the stale RENDERED operator-footer copy (`ALWAYS_INCLUDED_NOTES` in `compute-snapshot.ts`) that still said "process-global today": business-config + connector-tokens → "tenant-scoped" framing; industry/local-press → "operator-configured profile URLs only; no automated detection yet"; + the module-header "Tenant-scope reality" comment + the invariant-#6 note (C7d/C7e "deferred" → "now shipped"). Copy/string-only; zero logic change. Mid-slice catch: a draft put the literal `getBusinessConfig` identifier in copy → tripped `off-site-authority-pure-module-purity` → reworded to identifier-free phrasing. 2 test files updated to match (compute-snapshot data_sources_note assertions + recommendation-rules `STANDARD_NOTES` fixture). Files: 1 src + 2 tests. Line +20/−20 (net 0, pure swaps). Gates LOCAL ONLY: typecheck ✅ · off-site batch 14 files / 758 cases ✅ · full vitest **683 files / 13671 passed / 0 failures ✅**. Proposed commit: `chore(config): refresh off-site data-source copy`. **READY_TO_COMMIT — not pushed.**
>
> **Local stack now 8 ahead of origin/main (unpushed; Actions exhausted):** C7d + C7e + MT-3A + MT-3B + MT-3C + MT-3C.2 + MT-4 + footer-copy-refresh. Batch-push all when billing restored.
>
> **FLAGGED FOLLOW-UP (out of this slice's footer-only scope):** `src/domains/off-site-authority/types.ts:7` module-header comment still says "process-global today," claims C7d/C7e "defer," and cites the **retired** `off-site-authority-multi-tenant-prerequisite` catalog row. A tiny doc-comment truth-up — NOT folded in here (honoring the "footer refresh only" scope lock). Fold into the next off-site doc slice OR the MT-5 slice.
>
> **MT-5 still DEFERRED** (see below) — the multi-tenant business-config workstream is otherwise complete + truth-aligned; only the deprecated no-arg overload remains.

> 🟢 **SLICE MT-4 — MULTI-TENANT DOC TRUTH-UP (READY_TO_COMMIT — NOT PUSHED, Actions minutes exhausted, 2026-05-23).** Comment/doc-only: corrected the stale "PROCESS-GLOBAL" claims in the off-site loader (import annotations + prerequisite note) + the `BusinessConfig` interface note, and marked the `off-site-authority-multi-tenant-prerequisite` catalog row **retired** (resolved by MT-1/2/3*). Zero behavior change. FOLLOW-UP: the runtime `data_sources_note` strings in `compute-snapshot.ts` are still stale (rendered footer copy = behavior; deferred). Files: 2 src comments + 1 catalog row. Gates LOCAL ONLY: typecheck ✅ · 121 invariant/catalog-sync ✅ · tenant-env build exit 0 ✅ · full suite NOT run (comment-only, zero executable change). Proposed commit: `docs(config): correct stale process-global notes`. **READY_TO_COMMIT — not pushed.**
>
> **Local stack now 7 ahead of origin/main (unpushed; Actions exhausted):** C7d + C7e + MT-3A + MT-3B + MT-3C + MT-3C.2 + MT-4. Batch-push all when billing restored.
>
> **MT-5 PREFLIGHT — DEFERRED (report-first, per the operator default).** Runtime is fully clean: 0 no-arg `getBusinessConfig()` runtime callers, 0 `?? getBusinessConfig()` fallbacks, 0 no-tenant `saveBusinessConfig` callers. So removal is *mechanically* possible. BUT it is NOT "zero test blockers / obviously safe" — removing the deprecated no-arg overload requires a coordinated test+invariant migration:
> - `src/lib/business-config.test.ts` — **23 no-arg `getBusinessConfig()` calls** (the D1 suite + the MT-1 deprecated-path tests) → migrate each to `getBusinessConfig(tenantId)` or rewrite the deprecated-path tests.
> - 3 other test files call/mock no-arg: `load-trigger-candidates-for-tenant.test.ts`, `off-site-authority/load-snapshot.test.ts`, `changes-id-v2-switcher.test.ts` → audit + migrate.
> - **5 architecture invariants** sanity-pin that `export function getBusinessConfig(): BusinessConfig;` EXISTS (tenant-keyed-cache, customer-surface, operator-surface, deep-runtime, pure-helpers) → flip each to "overload removed."
> - `business-config-deep-runtime-tenant-aware` → convert from "src/** except business-config.ts" to a fully GLOBAL no-arg ban (include business-config.ts).
> - Remove the no-tenant `saveBusinessConfig(patch)` overload too (0 callers — trivial).
> 
> **Recommendation: MT-5 is a dedicated slice needing explicit operator approval** (a ~25-call test migration + 5 invariant flips). Defer until approved.

> 🟢 **SLICE MT-3C.2 — PAGE-EXTRACTOR CONFIG INJECTION (READY_TO_COMMIT — NOT PUSHED, Actions minutes exhausted, 2026-05-23).** Tightened the last 2 pure helpers (`getLocationRegex`, `getServiceRegex`) to require config; the page extractor now injects `getBusinessConfig(tenantId)` via its existing required `tenantId` param (1 runtime caller, no ripple). **ZERO `?? getBusinessConfig()` fallbacks remain.** Production identical (verify-action passes the real tenant); no extractor test asserts location/service terms. Updated 2 invariants (5-of-5 require config; 0 fallbacks). Files: 2 src + 2 invariants + docs. Line +79/−45. Gates LOCAL ONLY: typecheck ✅ · 129 targeted ✅ · catalog-sync ✅ · tenant-env build exit 0 ✅ · full vitest 13671 passed / 0 failures ✅. Proposed commit: `refactor(config): require config for page-extractor helpers`. **READY_TO_COMMIT — not pushed.**
>
> **Local stack now 6 ahead of origin/main (unpushed; Actions exhausted):** C7d + C7e + MT-3A + MT-3B + MT-3C + MT-3C.2. Batch-push all when billing restored.
>
> **NEXT:** **MT-4** (doc truth-up: the off-site loader's stale connector-store "PROCESS-GLOBAL" comment + the `off-site-authority-multi-tenant-prerequisite` catalog row — both now largely RESOLVED by MT-1/MT-2/MT-3*; zero behavior change). Then **MT-5 preflight** (remove the deprecated no-arg `getBusinessConfig()` overload + flip `business-config-deep-runtime-tenant-aware` to a fully global no-arg ban — now that 0 fallbacks remain, the only blocker is the test-only no-arg deprecated-path test in `business-config.test.ts` + the invariants that scan for the literal string).

> 🟢 **SLICE MT-3C — PURE-HELPER CONFIG INJECTION (READY_TO_COMMIT — NOT PUSHED, Actions minutes exhausted, 2026-05-23).** Tightened 3 of 5 business-config pure helpers to require injected `config` (no no-arg fallback): `getSectionAnalyzerConfig`, `getFaqTemplates`, `isDirectoryDomain`. Callers updated: today-data (businessConfig in scope), changes/[id] (resolve-once before line 265). DEFERRED to MT-3C.2: `getLocationRegex` + `getServiceRegex` (only caller is the pure page extractor with no config in scope — ripples into the scan pipeline). NEW `business-config-pure-helpers-require-config` invariant (3 required-config sigs + exactly-2-fallbacks-remain). changes test mock updated. Files: 1 core + 2 callers + 1 test-mock + 1 invariant + docs. Line +137/−17. Gates LOCAL ONLY: typecheck ✅ · 72 targeted ✅ · catalog-sync ✅ · tenant-env build exit 0 ✅ · full vitest 13668 passed / 0 failures ✅. Proposed commit: `refactor(config): require injected config for pure helpers`. **READY_TO_COMMIT — not pushed.**
>
> **Local stack now 5 ahead of origin/main (unpushed; Actions exhausted):** C7d + C7e + MT-3A + MT-3B + MT-3C. When billing restored → batch-push all.
>
> **NEXT:** **MT-3C.2** (thread tenant config into `extractPageSnapshot` so `getLocationRegex`/`getServiceRegex` can require config — drops the last 2 `?? getBusinessConfig()` fallbacks). Then **MT-4** (doc truth-up: stale connector-store "process-global" comment in the off-site loader + catalog row). Then **MT-5** (remove the deprecated no-arg `getBusinessConfig()` overload; flip `business-config-deep-runtime-tenant-aware` to a fully global no-arg ban — only safe once MT-3C.2 removes the last fallbacks). Or **C7f / C7g v2** (off-site HTTP verification).

> 🟢 **SLICE MT-3B — DEEP-RUNTIME BUSINESS-CONFIG HELPER MIGRATION (READY_TO_COMMIT — NOT PUSHED, Actions minutes exhausted, 2026-05-23).** Migrated the remaining deep domain/lib runtime callers off no-arg `getBusinessConfig()`: 7 → `getBusinessConfig(tenantId)` (recommendation-intelligence promotion+triggers, indexability loaders, pages/verify-action, profound import, ga4 persist), 1 → `await getBusinessConfigForCurrentTenant()` (yelp sync), and `classifyCompetitorType(domain, directoryDomains)` with config injected by callers (discover opts + competitors/page + milestones closures). **ZERO active no-arg `getBusinessConfig()` calls now remain in `src/` runtime code** (only the 5 pure-helper fallbacks + the deprecated overload in business-config.ts). NEW `business-config-deep-runtime-tenant-aware` invariant (src-wide no-arg scan). yelp test mock updated for the wrapper. Files: 12 src + 1 test-mock + 1 invariant + docs. Line +145/−22. Gates LOCAL ONLY: typecheck ✅ · 1112 targeted ✅ · catalog-sync ✅ · tenant-env build exit 0 ✅ · full vitest 13660 passed / 0 failures ✅. Proposed commit: `refactor(config): inject business config into deep helpers`. **READY_TO_COMMIT — not pushed.**
>
> **Local stack now 4 ahead of origin/main (unpushed; Actions exhausted):** C7d + C7e + MT-3A + MT-3B. When billing restored → batch-push all.
>
> **NEXT — MT-3C (pure-helper fallback tightening):** make `getLocationRegex`/`getServiceRegex`/`isDirectoryDomain`/`getSectionAnalyzerConfig`/`getFaqTemplates` require an injected `config` (drop the `?? getBusinessConfig()` fallback) — now safe since MT-3B threaded config into all runtime callers. Then **MT-4** (doc truth-up: stale connector-store "process-global" comment) and **MT-5** (remove the deprecated no-arg overload + flip the deep-runtime invariant to a fully global no-arg ban).

> 🟢 **SLICE MT-3A — OPERATOR-SURFACE BUSINESS-CONFIG MIGRATION (READY_TO_COMMIT — NOT PUSHED, Actions minutes exhausted, 2026-05-22).** Migrated the 9 operator-facing business-config entries (settings + diagnostics pages/actions) off no-arg `getBusinessConfig()`: 6 → `getBusinessConfig(tenantId)` (tenantId in scope), 3 → `await getBusinessConfigForCurrentTenant()` (settings/config/page.tsx converted sync→async; no test renders it). `settings/config/actions.ts` save migrated to `saveBusinessConfig(tenantId, …)`. NEW `business-config-operator-surface-tenant-aware` invariant (9-file allowlist). Deep helpers (MT-3B), pure-helper fallbacks (MT-3C), CLI paths (MT-5) deferred. Files: 9 src + 1 invariant + docs. Line +131/−17. Gates LOCAL ONLY: typecheck ✅ · 128 + 241 targeted ✅ (diagnostics + connectors re-verified) · catalog-sync ✅ · tenant-env build exit 0 ✅ · full vitest 13657 passed / 0 failures ✅. Proposed commit: `refactor(config): resolve operator surfaces by tenant`. **READY_TO_COMMIT — not pushed.**
>
> **Local stack now 3 ahead of origin/main (unpushed; Actions exhausted):** C7d + C7e + MT-3A. When billing restored → batch-push all together (one CI run).
>
> **NEXT — operator's choice:**
> - **MT-3B (deep domain/lib helper injection):** thread injected config into `promotion-writer`, `load-trigger-candidates-for-tenant`, indexability loaders, `classify-type`, ga4/yelp sync, `pages/verify-action`, profound import — each receives config from its caller instead of no-arg.
> - **MT-3C (pure-helper fallback tightening):** make `getLocationRegex`/`getServiceRegex`/`isDirectoryDomain`/`getSectionAnalyzerConfig`/`getFaqTemplates` require an injected config (drop the `?? getBusinessConfig()` fallback) once MT-3B threads config through.
> - **MT-4** = doc truth-up (stale connector-store "process-global" comment); **MT-5** = remove the deprecated no-arg overload + global no-arg ban.
> - Or **C7f / C7g v2** (off-site HTTP verification depth).

> 🟢 **SLICE C7e — RECOMMENDATIONS OFF-SITE OPPORTUNITIES SECTION (READY_TO_COMMIT — NOT PUSHED, Actions minutes exhausted, 2026-05-22).** Read-only "Off-site opportunities" region at the bottom of `/recommendations`, separate from the website-edit queue, REUSING the C7d `OffSiteAuthorityTile` (C7d untouched). Async server section (loads off-site snapshot + pure candidates, soft-fails to hidden; is_local_service + renderability gate); rendered at the bottom of both v2 + legacy branches of `RecommendationsAsyncContent`. NO queue promotion (off-site stays diagnostic_only), no Accept/Defer/Dismiss, no raw URLs, no external calls. 2 NEW invariants (section safe-copy + no-queue; no-queue scans only the section file since page.tsx legitimately imports queue loaders). Files: 1 section + page.tsx + section test + 2 invariants + docs. Line +507 (under ≤850 target). Gates LOCAL ONLY (no remote CI/Vercel): typecheck ✅ · 632 + 244 targeted ✅ · catalog-sync ✅ · tenant-env build exit 0 ✅ · full vitest 13637 passed / 0 failures ✅ (first run caught 10 failures — async section needed a `<Suspense fallback={null}>` wrap for the `renderToStaticMarkup` route tests; fixed + re-run green). Proposed commit: `feat(recommendations): show off-site opportunities section`. **READY_TO_COMMIT — not pushed.**
>
> **NEXT — operator's choice (when Actions billing restored, batch-push C7d + C7e together):**
> - **MT-3 (deep business-config migration):** operator/diagnostic/settings + deep domain/lib helpers → tenant-aware; tighten pure helpers; EXPAND the `business-config-customer-surface-tenant-aware` allowlist. (MT-4 = doc truth-up incl. stale connector-store "process-global" comment; MT-5 = remove deprecated no-arg overload + global no-arg ban.)
> - **C7f / C7g v2 (off-site depth):** HTTP HEAD verification of operator-vouched profile URLs (promote "Configured" → "Confirmed"); off-site freshness/decay re-fire.
> - **Section 5/6/9 follow-ups** per the master plan.

> 🟢 **SLICE C7d — TODAY OFF-SITE AUTHORITY TILE (READY_TO_COMMIT — NOT PUSHED, no CI minutes used, 2026-05-22).** First customer-facing off-site surface (unblocked by MT-2's tenant-correct off-site data path). Read-only Today card: "Connected profiles" chips (claimed===true; Connected/Configured; review count+rating only for high-confidence GBP/Yelp) + "Worth a manual review" (only the 3 non-policy-risk candidates; request_gbp_reviews/pursue_local_pr excluded; no raw URLs; no "missing"). Option A (page.tsx in scope, dedicated `<Suspense fallback={null}>` section). 2 NEW invariants (safe-copy + no-queue); the 2 existing isolation invariants LEFT UNCHANGED (they pass + auto-cover the tile; weakening avoided). Files: 1 new component + sections.tsx + page.tsx + 1 tile test + 2 invariants + docs. Line +719 (over ≤700 target, within ≤850 soft; no trim). Gates LOCAL ONLY: typecheck ✅ · 646 targeted ✅ · catalog-sync ✅ · full vitest + tenant-env build (running). Proposed commit: `feat(today): show off-site authority summary`. **READY_TO_COMMIT — not pushed.**
>
> **NEXT — operator's choice:**
> - **C7e (Recommendations off-site section):** read-only "Off-site opportunities (manual follow-up)" section on `/recommendations`, structurally SEPARATE from the website-edit queue — off-site rows stay `diagnostic_only` (NOT queue promotion). Needs its own preflight + paired safe-copy/no-queue invariant extension.
> - **MT-3 (deep business-config migration):** operator/diagnostic/settings + deep domain/lib helpers (`classify-type`, `recommendation-engine`, `section-analyzer`, indexability loaders, `recommendation-intelligence/*`, ga4/yelp sync, verify-action, profound import) → tenant-aware; tighten the pure helpers (drop the no-arg fallback); EXPAND the `business-config-customer-surface-tenant-aware` allowlist. (MT-4 = doc truth-up incl. stale connector-store "process-global" comment; MT-5 = remove deprecated no-arg overload + global no-arg ban.)

> 🟢 **SLICE MT-2 — CUSTOMER-FACING BUSINESS-CONFIG ENTRY-POINT MIGRATION (READY_TO_COMMIT — NOT PUSHED, no CI minutes used, 2026-05-22).** Migrated the 8 customer-facing entry files off the deprecated no-arg `getBusinessConfig()`: 5 use `getBusinessConfig(tenantId)` (off-site loader, visibility-read-model, changes/[id], today-data, today-v2 visibility+descriptors), 3 use `await getBusinessConfigForCurrentTenant()` (competitors, local/page, local-presence). The off-site one-liner makes the **C7d/C7e off-site data path tenant-correct end-to-end**. NEW invariant `business-config-customer-surface-tenant-aware` (8-file allowlist; no-arg forbidden + tenant-aware-form required; comment-strip is line-before-block to survive `// .data/*.json` phantom openers). No test-mock updates needed (mocks arg-agnostic). `RECOMMENDATION_INTELLIGENCE_AUDIT.md` not impacted. Line +204/−20 (net ~184, under ≤700). Gates LOCAL ONLY: typecheck ✅ · 23 targeted/799 ✅ · full vitest 13504 passed/0 failures ✅ · tenant-env build exit 0 ✅. Proposed commit: `refactor(config): resolve customer surfaces by tenant`. **READY_TO_COMMIT — not pushed.**
>
> **NEXT — operator's choice (both unblocked):**
> - **MT-3 (deep migration):** expand to operator/diagnostic/settings (`diagnostics/*`, `settings/*`) + deep domain/lib helpers (`classify-type`, `recommendation-engine`, `section-analyzer`, indexability loaders, `recommendation-intelligence/*`, `ga4`/`yelp` sync, `pages/verify-action`, profound import) + tighten the pure helpers (`getLocationRegex`/`getServiceRegex`/`isDirectoryDomain`/`getSectionAnalyzerConfig`/`getFaqTemplates` → required-config, drop the no-arg fallback). EXPANDS the MT-2 invariant allowlist. (MT-4 = doc truth-up incl. the stale connector-store "process-global" comment; MT-5 = remove the deprecated no-arg overload + global no-arg ban.)
> - **Section 7 C7d/C7e (customer build):** Today off-site authority tile + Recommendations off-site cards — now buildable because the off-site data path is fully tenant-correct after MT-2. Requires its own preflight (customer-surface scope-discipline invariant: operator-vouched vs Beacon-verified disambiguation).

> 🟢 **SLICE MT-1 — TENANT-KEYED BUSINESS-CONFIG CORE (READY_TO_COMMIT — NOT PUSHED, no CI minutes used, 2026-05-22).** First slice of the multi-tenant-store prerequisite. Preflight finding: the prerequisite was ~80% already shipped — middleware injects `x-beacon-tenant` per request (7.4), `connector-store` is already tenant-scoped, OAuth writes are tenant-scoped. The ONLY genuine process-global blocker was `src/lib/business-config.ts`'s `_cached` singleton. MT-1 replaces it with a tenant-keyed `Map` cache + `getBusinessConfig(tenantId)` (resolution a→b→c→d) + `getBusinessConfigForCurrentTenant()` async wrapper + a deprecated no-arg overload (keeps all consumers green) + `saveBusinessConfig(tenantId, patch)`. **Ritz byte-identical via back-compat (b); zero consumer edits; zero Section 7 surfaces.** Files: 1 src + 1 test extended (+11 cases) + 1 NEW invariant `business-config-tenant-keyed-cache.test.ts` (7 cases) + docs. Line budget +606/−55 (net ~551, under ≤700). Gates LOCAL ONLY: typecheck ✅ · 41 targeted ✅ · 471 tenant-isolation/off-site ✅ · full vitest 13485 passed/0 failures ✅ · tenant-env build exit 0 ✅. Proposed commit: `refactor(config): key business config cache by tenant`. **READY_TO_COMMIT — not pushed.**
>
> **NEXT — MT-2 (customer-facing entry-point migration):** migrate the customer-facing loaders to `await getBusinessConfigForCurrentTenant()` and thread config down — `today-data.ts` (5 sites), `today-v2-data.ts` (3 sites), `off-site-authority/load-snapshot.ts`, `changes/[id]/page.tsx`, `competitors/page.tsx`, `local/page.tsx`, `local-presence.ts`, `today/visibility-read-model.ts`. Land the customer-surface "no no-arg `getBusinessConfig`" invariant alongside. **MT-2 is what unblocks Section 7 C7d (Today off-site tile) + C7e (Recommendations off-site cards).** MT-3 = operator/diagnostics/settings + remaining domain/lib + pure-helper tightening; MT-4 = doc truth-up (correct the stale connector-store "process-global" claim in the off-site loader comment + catalog row); MT-5 = remove the deprecated no-arg overload + global no-global invariant.

> 🟡 **SLICE — BRAND ASSERTION KEY ALIGNMENT (READY_TO_COMMIT — NOT PUSHED, no CI minutes used, 2026-05-22).** Generic tenant-identity fix surfaced by B.4a (Brand-supported = 0 despite Ritz's `architect-led design-build` assertion). Root cause: `TENANT_ASSERTIONS` + `TENANT_NAME_STYLES` keyed by slug (`ritz-builders`); all callers pass tenantId (`tenant-ritz-founder`) → `[]`/`null` in production → dormant LLM brand-assertion guidance + dormant validator brand-name-style gate; slug-based unit test masked it. Fix (Option 4): re-key both registries by canonical tenantId — no Ritz `if`, no async resolver, no caller changes, sync signatures preserved. **Re-activates intended behavior** (LLM allowed-phrase guidance + validator brand-name-style gate) — verified via the full validator/LLM suite. **Files (1 src + 3 tests + 1 NEW invariant + 5 docs)**: (a) `brand-assertions.ts` — re-key + `__brandRegistryKeys` test-only export + doc comments. (b) `brand-assertions.test.ts` — re-key lookups + 2 regression tests (slug → `[]`/`null`, no alias). (c) `specific-edit-validator-brand-claims.test.ts` (operator-approved fixture expansion) — 11 packet fixtures slug→id; the 2 bare-Ritz-rejection tests now fire. (d) `specific-edit-validator-faq-pairing.test.ts` (operator-approved) — 1 fixture slug→id. `static-bundle.test.ts` untouched (unaffected, 22 green). (e) NEW `tests/architecture/brand-assertions-tenant-key-contract.test.ts` — every registry key is an `ops/active-tenants.json` tenantId, no key is a slug, + behavioral regression. **Locked decisions honored**: Option 4; generic; sync; no slug alias; fixtures corrected not weakened. **Auto-pass**: B.1 + B.2 + B.4a invariants (scanner unchanged); recommendation-intelligence + tenant-isolation invariants; catalog-sync (1 new row). **Hard contracts honored**: no mutation · no migration · no LLM · no polls/scans · no env · no validator-logic change · no customer-route change · no scanner change · no Ritz-specific logic. **Line budget**: src+tests delta TBD (expected ≤500 target). **Quality gates (LOCAL ONLY)**: typecheck ✅ · brand-assertions 55 + invariant 7 ✅ · validator brand-claims+faq 43 ✅ · static-bundle 22 ✅ · catalog-sync 8 ✅ · full architecture + full vitest + tenant-env build (TO RUN). **STOPPED AT READY_TO_COMMIT — not pushed.** Proposed commit message: `fix(recommendations): key brand assertions by tenant id`. **Recommended next**: operator review → local commit → push → re-inspect `/diagnostics/recommendation-safety-audit` to confirm Brand-supported > 0 + architect-led supported → resume B.4b/B.4c scope decision against correctly-resolved data.

> 🟢 **SLICE 4.5.G-B.4a — GENERIC CLAIM-RISK CLASSIFICATION (TAG, DON'T SUPPRESS) (READY_TO_COMMIT — NOT PUSHED, no CI minutes used, 2026-05-22).** First slice of Section 4.5.G-B.4. Operator-locked architecture: GLOBAL risk-pattern detection (pure scanner) + TENANT-PROVIDED brand-assertion support (via context) + TAG-DON'T-SUPPRESS. The safety audit scanner now CLASSIFIES each `architect_overclaim` / `unsupported_claim` violation as `brand_supported: true | false` against tenant-supplied assertions (phrase-contains OR category-unlock) — never removing a violation — and the operator diagnostic surfaces a per-field × per-term × brand-support debt breakdown. Tenant-agnostic: scanner owns a GLOBAL claim-risk registry (token → 6 categories: professional_credential/process/ranking/award/guarantee_outcome/superiority) + GLOBAL unlock map; NEVER imports brand-assertions / getBrandAssertions / business-config, NEVER references getRepository, NEVER branches on a tenant name. Diagnostic page resolves `getBrandAssertions(tenantId)` at the boundary + passes a local structural shape via `context.brandAssertions`. No assertions → brand_supported false (safe default). **Operator-diagnostic + classification ONLY. NO customer surface · NO migration · NO backfill · NO mutation · NO LLM · NO scans · NO B.1/B.2/validator change · NO credentials category (B.4d).** **Files**: (a) MODIFIED `safety-audit.ts` — `ClaimRiskCategory` type + 4 optional violation fields (architect/unsupported only) + `SafetyAuditBrandAssertion` structural shape + `context.brandAssertions?` + `CLAIM_RISK_BY_TOKEN` (18 tokens mapped) + `CLAIM_RISK_UNLOCK` + `classifyClaim`/`classificationForToken`; `pushViolationsForLiteral` gained optional classification spread; existing kinds UNCHANGED. (b) MODIFIED `recommendation-safety-audit/page.tsx` — boundary `getBrandAssertions` import + structural map + context pass; page-layer `FIELD_VISIBILITY` (proposed_text+why=customer_visible, customer_copy=unknown, operator_evidence=operator_internal); brand-support counters; `DebtBreakdownTable` grouped section; flat-table brand-support data-attrs + column. (c) MODIFIED `safety-audit.test.ts` (+20 → 51, synthetic tenants). (d) MODIFIED `recommendation-safety-audit-page.test.tsx` (+9 → 19). (e) MODIFIED `recommendation-safety-audit-read-only.test.ts` (+5 scanner pins; page brand-assertions pin flipped negative→positive; 32 cases). (f) MODIFIED `recommendation-safety-audit-coverage.test.ts` (+15 → 30, claim-risk coverage + synthetic-tenant pin). **Locked decisions honored**: tag-don't-suppress; generic-not-Ritz; scanner import-free; page-boundary resolution; no credentials category; field-visibility unknown-where-uncertain. **Auto-pass**: no-queue-write (scanner write-axis unchanged); B.1 + B.2 tests/invariants UNCHANGED; all recommendation-intelligence invariants; catalog-sync (rows edited in place). **Quality gates (LOCAL ONLY)**: typecheck ✅ · targeted 9 files / 233 cases ✅ · recommendation-intelligence invariants 192 cases ✅ · full architecture + full vitest + tenant-env build (TO RUN). **Line budget**: src+tests delta TBD (expected ≤700 target). **STOPPED AT READY_TO_COMMIT — not pushed.** Proposed commit message: `feat(diagnostics): classify brand-supported safety findings`. **Recommended next**: operator review → local commit only → optional push + inspect the diagnostic debt breakdown in production → decide whether B.4b (validator: premier/proven) + B.4c (render guard for why/measurement_plan, brand-assertion-aware) are still needed against REAL per-field/per-term data. B.4d (credentials category) only if operator verifies + wants to claim licensing.

> 🟢 **SLICE 5.B.2 — TODAY EDIT-LIFECYCLE TILE REPEAT-CITATION BAND COUNTER (READY_TO_COMMIT — NOT PUSHED, no CI minutes used, 2026-05-21).** Closes Section 5 end-to-end. Sections 5.A (compute + loader + diagnostic) + 5.B.1 (Changes detail Act 3 sub-line) already deployed pre-slice. 5.B.2 ships the last Section-5 customer surface: a compact "Citation stability (past 30 days)" counter section inside the existing Today `EditLifecycleTile` (not a new tile). **Pure additive render-time extension. NO production data mutations · NO LLM · NO Supabase writes · NO migrations · NO new top-level routes · NO new dashboards · NO env changes · NO compute-repeat-citation.ts / load-repeat-citation.ts / diagnostic-page / Changes detail mutations · NO band-threshold or denominator-logic changes.** **3 src files modified + 2 test files extended + 1 NEW architecture invariant + 5 doc syncs**. **Files**: (a) MODIFIED `src/domains/citation-lifecycle/load-lifecycle.ts` — adds `repeat_citation_30d` field to `LifecycleSummary`; loader body invokes existing `loadRepeatCitationForEdit({ tenantId, recommendedEdit, windowDays: 30, now })` in `Promise.all` across the same `candidates` set the per-stage loop iterates; aggregates per-band counts; per-edit `.catch()` + outer try/catch for soft-fail. (b) MODIFIED `src/components/today/edit-lifecycle-tile.tsx` — adds optional `repeatCitation30d?` prop + new `CitationStabilitySection` internal component with locked customer labels (lower-case sentence-case forms vs 5.B.1's title-case; both reference the same band concept). Zero-buckets hidden; section suppressed when prop is null / total === 0 / total_with_band === 0. Type-only import of `RepeatCitationBand`. (c) MODIFIED `src/app/(shell)/today-v2-sections.tsx` — single-line: pass `repeatCitation30d={summary.repeat_citation_30d}` to `<EditLifecycleTile />`. (d) MODIFIED `tests/components/today/edit-lifecycle-tile.test.tsx` — extended from 8 to 20 cases (+12 new 5.B.2 cases): section renders when populated · locked labels rendered · zero-buckets hidden · section absent when prop absent / null / total_with_band=0 / total=0 · no raw band names · no percentages · no Section-6 vocab · no causal/revenue language · per-stage rollup coexists. (e) MODIFIED `tests/domains/citation-lifecycle/load-lifecycle.test.ts` — extended with 7 new 5.B.2 cases via new mock for `loadRepeatCitationForEdit`. All 31 cases passing. (f) MODIFIED `tests/architecture/repeat-citation-no-customer-surface.test.ts` — extended ALLOWED_FILES with 2 new files; introduced `ADDITIONAL_SCAN_FILES` list to include `today-v2-sections.tsx` (sits at sibling depth, not picked up by existing walk). 585 cases passing. (g) NEW `tests/architecture/repeat-citation-today-tile-band-counter.test.ts` (~280 lines, 31 cases) — 1 file-exists + 7 locked customer-label pins + 8 module-purity pins + 10 forbidden-vocab pins + 5 render-surface pins. **Locked decisions honored**: G1 (30d customer / 60+90d operator already covered by existing diagnostic; tile shows 30d only); G2 (successful poll-day denominator UNCHANGED — loader inherits via existing `loadRepeatCitationForEdit`); G3 (per-platform divergence on Changes detail only; tile shows single aggregate row); G4 (no half-life — invariant pins absence); G5 (band thresholds UNCHANGED). 5.B.1 customer labels exactly preserved (lower-case forms in tile counter; title-case forms in Changes detail badge). **Auto-pass invariants**: `repeat-citation-pure-purity` · `repeat-citation-forbidden-vocab` · `repeat-citation-customer-copy-vocab` (5.B.1 Changes detail UNCHANGED) · `repeat-citation-no-section-6-bridge` · `repeat-citation-loader-tenant-scope` (5.A loader UNCHANGED) · `repeat-citation-changes-detail-band-mapping` · `repeat-citation-operator-page-discipline` + `repeat-citation-operator-page-vocab` · `citation-lifecycle-tenant-isolation`. **Hard contracts honored**: pure additive · NO mutations · NO LLM · NO Supabase writes · NO migrations · NO env changes · NO 5.A / 5.A.2 / 5.B.1 mutations · NO Section 6 / 7 / 9 / 10 / 4.5.G-B work. **Line budget**: src+tests delta TBD (expected ≤800 target). **Quality gates (LOCAL ONLY)**: typecheck ✅ · targeted suites ✅ · full architecture + full vitest + tenant-env build (TO RUN). **STOPPED AT READY_TO_COMMIT — not pushed.** Proposed commit message: `feat(today): show repeat citation stability counters`. **Section 5 is now CLOSED end-to-end.** **Recommended next**: operator review → local commit only → optional push + Today-tile visual smoke (one of the 4 G-B layers exception — visual smoke IS the natural verification for a new visible counter), OR pivot to Section 6 (primary-recommendation) or Section 7 (off-site authority) or slice 4.5.G-B.4 (architect/unsupported brand-assertion carve-out).

> 🟢 **SLICE 4.5.G-B.2 — WRITE-TIME SANITIZER FORWARD-PREVENTION (READY_TO_COMMIT — NOT PUSHED, no CI minutes used, 2026-05-21).** Second half of Section 4.5.G-B safety hardening. **Forward-only complement to B.1's render-time guard.** B.1 (deployed at `origin/main = 25cb285`) blocks the 4 high-severity leak kinds at customer render; B.2 prevents NEW leaks from being persisted by extending the existing write-time sanitizer (`sanitizeOperatorEvidenceText` at `src/domains/recommendations/copy-sanitize.ts`) which already scrubs canonical UUIDs at row creation per the M2 (2026-05-05) operator audit. B.2 adds two new pattern families: (a) long 32+ hex/hash strings (SHA-1/SHA-256/MD5/similar) replaced with `"prompt evidence"`; (b) the 12 locked internal taxonomy tokens (mirrors B.1's blocklist EXACTLY) replaced with operator-approved customer-safe wording. **Pure string transformation. NO production data mutations · NO LLM calls · NO Supabase writes · NO env changes · NO new top-level routes · NO migrations · NO validator changes · NO brand-assertions changes · NO registry/action-types changes · NO B.1 render-guard changes.** Inherits B.1 scope discipline: `unsupported_claim` + `architect_overclaim` deferred to slice 4.5.G-B.4; competitor-name scrubbing stays on the separate `validateCompetitorPublicCopy` validator path. **Files**: (a) MODIFIED `src/domains/recommendations/copy-sanitize.ts` — added `LONG_HEX_HASH_PATTERN_GLOBAL` regex, `INTERNAL_TOKEN_REPLACEMENTS` table (10 pattern→replacement pairs; the 3 Mode labels share one `\bMode\s+[ABC]\b` regex), exported helper `scrubInternalLeakagePatterns(text)` that runs long-hex THEN walks the token table, and a 2-line evolution of `sanitizeOperatorEvidenceText` to call the helper AFTER the UUID-replacement phase (existing M2 early-return restructured so B.2 runs unconditionally). Replacement style locked: `aiSearchSignal` → `search-intent signals`, `actualSearchQueries` → `observed search-intent signals`, `action_type` → `action type`, `trigger_signal` → `signal`, `evidence_tier` → `evidence`, `Mode A/B/C` → `Beacon's evaluation mode`, `source_rec_id` → `source recommendation`, `rec_id` → `recommendation`, `diagnostic_only` → `diagnostic`, `customer-queue-ready` → `customer queue`, long-hex → `prompt evidence`. Idempotent. (b) MODIFIED `src/domains/recommendations/copy-sanitize.test.ts` — extended from 16 to 49 cases (+33 B.2 cases). (c) NEW `tests/architecture/recommendation-copy-sanitize-purity.test.ts` (30 cases) — 1 file-exists + 12 module-purity pins (no fetch/Supabase/LLM/persistence/brand-assertions/validator/why-display-guard/suggested-copy-display-guard/action-types/Supabase write shape; exports `sanitizeOperatorEvidenceText` + `scrubInternalLeakagePatterns`) + 4 coverage pins (long-hex pattern + `"prompt evidence"` fallback + 9 per-token literal pins + 1 shared `Mode\s+[ABC]` regex + 1 replacement-style spot-check) + 4 deferral pins (no `architect-led`/`architect-designed`/`\bbest\b` replacement keys; no competitor scrubbing). **Locked decisions honored**: extend `sanitizeOperatorEvidenceText` (correct central point — already wired at `recommended-edits-persistence.ts:246-259` for `why`/`expectedImpact`/`measurementPlan`/`displayLabel`); pure transformation; idempotent; preserves M2 UUID-with-prompt-mapping behavior; B.1 render-guard UNCHANGED. **Auto-pass invariants**: `recommendation-why-render-guard` (B.1 unchanged); `recommendation-safety-audit-coverage` (A.1 unchanged); `recommendation-safety-audit-read-only` (A.1/A.2 unchanged); `recommendation-intelligence-no-queue-write` (sanitizer lives under `src/domains/recommendations/`, not under `recommendation-intelligence/`); `llm-safety-invariants` (no provider imports); `catalog-sync` (1 new row). **Hard contracts honored**: pure transformation · NO mutations · NO LLM · NO Supabase writes · NO migrations · NO env changes · NO B.1 changes · forward-only (existing rows unchanged). **Line budget**: src+tests (TO measure) — expected ~430-500 lines. **Quality gates (LOCAL ONLY — no CI minutes used)**: typecheck ✅ · targeted B.2 suite (sanitizer 49 + invariant 30 = 79 cases) ✅ · regression on B.1 (57 cases) ✅ · regression on A.1 (46 cases) ✅ · regression on A.2 (27 cases) ✅ · catalog-sync (TO RE-RUN) · full architecture + full vitest + tenant-env build (TO RUN). **STOPPED AT READY_TO_COMMIT — not pushed.** Proposed commit message: `fix(recommendations): scrub internal tokens in reasoning copy`. **Section 4.5.G-B safety floor is now complete for the 4 B.1-locked reason kinds end-to-end** — A.1 detects historical violations at audit time, B.1 catches them at customer render, B.2 prevents new ones at row persistence. **Recommended next**: operator review → local commit only → optional push for CI/Vercel + production deploy verification (test-only sufficient; B.2 is write-time and historical rows are unchanged), OR pivot to slice 4.5.G-B.4 (architect/unsupported with brand-assertion carve-out) OR pivot to Section 5/6/7.

> 🟢 **SLICE 4.5.G-B.1 — RECOMMENDATION `WHY` RENDER-TIME GUARD (READY_TO_COMMIT — NOT PUSHED, no CI minutes used, 2026-05-21).** First half of Section 4.5.G-B (defense-in-depth follow-up to the production audit findings at `/diagnostics/recommendation-safety-audit` deployed against `origin/main = 450a3f2`). Production scan surfaced 73 violations across 25 of 28 rows: 10 uuid_leak (high), 5 internal_token (medium, e.g., `aiSearchSignal`), 4 competitor_name (high), 40 architect_overclaim (medium, many legitimate Ritz brand-assert phrasing), 14 unsupported_claim, 0 placeholder/causal/em_dash/leading. Operator-locked decision: render-guard + future prevention, NO production data mutation. **B.1 scope: render-time guard for customer-visible `why` field only**, blocking the 4 high-confidence non-overclaim kinds (uuid_leak / long_hex_hash / internal_token / competitor_name). **unsupported_claim + architect_overclaim INTENTIONALLY DEFERRED to slice 4.5.G-B.4** to preserve Ritz's brand-asserted "architect-led design-build" positioning until the brand-assertion carve-out lands. **NO production mutations · NO LLM · NO polls/scans · NO env changes · NO validator changes · NO brand-assertions changes · NO registry changes · NO suggested-copy-display-guard changes · NO Section 6/9/10 changes.** **1 NEW src file** + **2 NEW test files** + **5 doc syncs** + **6 customer-facing render path modifications** + **2 server page modifications**. **Files**: (a) NEW `src/domains/recommendations/why-display-guard.ts` (~200 lines) — pure `checkWhyDisplaySafe(why, context)` returning discriminated `{ ok: true, text } | { ok: false, fallback, reasons }`. 4 reasons (uuid_leak / long_hex_hash / internal_token / competitor_name); 12 locked internal tokens; competitor_name via passed-in `context.competitorNames` whole-word case-insensitive match. Locked fallback copy verbatim: `"Beacon has additional context for this recommendation, but it needs review before showing here."` ZERO dependency on validator / brand-assertions / suggested-copy-display-guard / action-types / persistence / Supabase / LLM provider. (b) NEW `tests/domains/recommendations/why-display-guard.test.ts` (~250 lines, 33 cases) — clean + null + 4 violation kinds + multi-reason + 5 intentional non-blocking (architect-led / architect-designed / best / em-dash / leading Best) + fallback shape. (c) NEW `tests/architecture/recommendation-why-render-guard.test.ts` (~200 lines, 24 cases) — guard purity (11 source-text pins + 1 file-exists + 2 positive pins) + 12 site-level pins across 4 render sites (file-exists + import + ≥2 references each). Locked render sites: `recommendation-detail-client.tsx`, `suggested-copy-act.tsx`, `recommendations-client.tsx` (legacy drawer), `recommendation-v2-card.tsx` (v2 card). **Files modified (8)**: `src/app/(shell)/recommendations/page.tsx` + `src/app/(shell)/recommendations/[id]/page.tsx` (load `tracked_entities` via `getRepository().forTenant(tenantId).getTrackedEntities()`, filter to active competitors, thread `competitorNames` down) · `src/app/(shell)/recommendations/recommendations-v2-client.tsx` · `src/app/(shell)/recommendations/recommendations-client.tsx` (legacy; threads through 3 hops: client → table → row → drawer; guard wraps existing `sanitizeOperatorEvidenceText`) · `src/components/recommendations/v2/recommendation-v2-card.tsx` · `src/app/(shell)/recommendations/[id]/recommendation-detail-client.tsx` (header + Act 2 `why`) · `src/app/(shell)/recommendations/[id]/suggested-copy-act.tsx` (Act 4 `firstSentence(...)`). **Locked decisions honored**: 4-reason scope (uuid_leak / long_hex_hash / internal_token / competitor_name); unsupported_claim + architect_overclaim deferred to B.4; competitor source = `tracked_entities` filtered to `entity_type === "competitor"` AND `is_active === true` (mirrors A.2 page pattern); fallback copy locked verbatim; guard module pure (zero I/O / zero LLM / zero validator+brand-assertions+registry imports). **Auto-pass invariants**: `recommendation-safety-audit-coverage` (A.1 unchanged); `recommendation-safety-audit-read-only` (A.1/A.2 unchanged); `recommendation-intelligence-no-queue-write` (no new persistence imports — guard lives under `src/domains/recommendations/` not `recommendation-intelligence/`); `llm-draft-gateway-render-isolation` (unchanged); `llm-safety-invariants` (no provider imports / no `runProviderAndPersist`); `catalog-sync` (1 new row added). **Hard contracts honored**: pure render-time guard · NO production data mutations · NO LLM call on render · NO paid API · NO Supabase writes · NO migrations · NO env changes · NO validator changes · NO brand-assertions changes · NO `generatorActive` flips · NO new top-level routes · NO `revalidatePath` · NO server actions · NO Today/Changes/Prompts/Settings/Section 6/9 changes · NO `recommendation-intelligence` tree changes · original render text preserved when guard returns `{ ok: true }`. **Line budget**: src+tests (TO measure precisely) — expected ~700-900 lines; under +1,000 hard stop. **Quality gates (LOCAL ONLY — no CI minutes used)**: typecheck ✅ · targeted B.1 suite (guard 33 + invariant 24 = 57 cases) ✅ · regression on A.1 scanner (31 cases) ✅ · regression on A.1 coverage (15 cases) ✅ · regression on A.2 read-only (27 cases) ✅ · catalog-sync (TO RE-RUN with new row) · full architecture suite + full vitest + tenant-env build (TO RUN). **STOPPED AT READY_TO_COMMIT — not pushed.** Proposed commit message: `fix(recommendations): guard customer-facing why copy`. **Customer-visible `why` field now end-to-end guarded across all 4 render paths** — uuid_leak / long_hex_hash / internal_token / competitor_name leaks now resolve to locked fallback. **Recommended next**: operator review → local commit only → optional push for production deploy + post-deploy verification on customer-facing recommendation routes (`/recommendations`, `/recommendations/[id]`, legacy drawer if `?legacy=1`), OR hold for slice 4.5.G-B.2 (write-time sanitizer extension to prevent future internal_token / uuid leaks at row creation, complementing the render-time guard).

> 🟢 **SLICE 4.5.G-A.2 — RECOMMENDATION SAFETY AUDIT DIAGNOSTIC PAGE + READ-ONLY INVARIANT (READY_TO_COMMIT — NOT PUSHED, no CI minutes used, 2026-05-21).** Second half of Section 4.5.G-A. **Completes the operator-facing safety-audit vertical** — operator can visit `/diagnostics/recommendation-safety-audit` and inspect historical safety violations on every `recommended_edits` row. Consumes the A.1 scanner (`auditRecommendedEditRow`, locally committed at `506c6ae`). **NO A.1 scanner modifications · NO writes · NO mutations · NO server action · NO `revalidatePath` · NO LLM · NO validator changes · NO brand-assertions changes.** **1 NEW src file** + **2 NEW test files** + **5 doc syncs**. **Files**: (a) NEW `src/app/(shell)/diagnostics/recommendation-safety-audit/page.tsx` (~287 lines) — operator-only diagnostic page. Reads `recommended_edits` + `tracked_entities` via existing repository pattern. Builds competitor-name list from active competitor entities (`entity_type === "competitor"` AND `is_active === true`). Renders flat one-row-per-violation table with `data-safety-rec-id` / `data-safety-violation-kind` / `data-safety-violation-severity` / `data-safety-violation-field` attrs. Counter strip + empty-state success banner. **NO action buttons. NO forms. NO mutations.** (b) NEW `tests/app/diagnostics/recommendation-safety-audit-page.test.tsx` (~269 lines, 10 cases) — operator gate · empty state · clean rows · violation table · multi-violation DOM rows · counter totals · active/inactive competitor flow · no action buttons / forms. (c) NEW `tests/architecture/recommendation-safety-audit-read-only.test.ts` (~217 lines, 27 cases) — 2 file-exists pins + 8 scanner source-text negatives + 17 page contracts (14 negative + 3 positive). **Locked decisions (K1-K7) honored**: K1 split (A.1 scanner + A.2 page = operator-reachable end-to-end) · K2 NO `credentials` BrandAssertionCategory · K3 architect/licensing audit-only patterns · K4 3-band severity · K5 standard operator gate · K6 audit ALL rows · K7 include all lifecycle statuses. **Auto-pass invariants**: `no-queue-write` (auto-covers scanner) · `llm-draft-gateway-render-isolation` (unchanged) · `promotion-live-write-guards` (unchanged) · `offsite-contract` (unchanged) · `specific-edit-target-constraint` (no heavy-packet-builder invocation) · `recommendation-safety-audit-coverage` (A.1 invariant; scanner unchanged) · `llm-safety-invariants` (page does not import provider) · `catalog-sync` (1 new row added in lockstep). **Hard contracts honored**: page is pure read-only · NO writes · NO mutations · NO server action · NO `revalidatePath` · NO queue writes · NO LLM calls · NO OpenAI provider import · NO llm-draft-gateway import · NO `recommended-edits-persistence` import · NO `persistRecommendedEditsLocal` / `syncRecommendedEdits` · NO `runProviderAndPersist` · NO Supabase write shape · NO brand-assertions.ts changes · NO `BrandAssertionCategory` changes · NO validator changes · NO display-guard changes · NO registry / `generatorActive` / trigger / customer-facing-route changes · NO Today/Changes/Prompts/Settings/Recommendations/Section 9 changes · NO migrations · NO cron/workflows · NO env mutation in Vercel. **Line budget**: src+tests ~X (TO measure) — expected ~773; under +1,000 hard stop. **Quality gates (LOCAL ONLY — no CI minutes used)**: typecheck ✅ · targeted A.2 suite (page 10 + read-only 27 = 37 cases) ✅ · regression on A.1 (31 + 15 = 46 cases) ✅ · catalog-sync (8 cases) ✅ · full vitest + tenant-env build TO RUN. **STOPPED AT READY_TO_COMMIT — not pushed.** Proposed commit message: `feat(recommendations): add safety audit diagnostic`. **Section 4.5.G-A safety-audit vertical complete end-to-end.** **Recommended next**: operator review → local commit only (2 commits ahead of `origin/main` after commit) → batch-push A.1 + A.2 as the Section 4.5.G-A checkpoint OR hold for 4.5.G-B (validator/category extensions, scope still TBD).

> 🟡 **SLICE 4.5.G-A.1 — RECOMMENDATION SAFETY AUDIT SCANNER + COVERAGE INVARIANT (LOCAL COMMIT `506c6ae`, NOT PUSHED, no CI minutes used, 2026-05-21).** First half of Section 4.5.G-A (safety cleanup / defense-in-depth) after operator-approved split. Original 4.5.G-A first pass was 1,724 lines = +724 over the +1,000 hard stop; operator approved Option 1 split. **A.1 = scanner + scanner tests + coverage invariant only.** A.2 = diagnostic page + page tests + read-only file-boundary invariant (deferred). **NO diagnostic page surface yet · NO mutations · NO writes · NO LLM · NO server action · NO new env flag · NO validator changes · NO brand-assertions changes.** Section 4.5.E is production-verified / closed at `origin/main = a97affb`. **Files (A.1 scope)**: (a) NEW `src/domains/recommendation-intelligence/safety-audit.ts` (413 lines) — pure `auditRecommendedEditRow(row, ctx)` scanner with 9 locked violation kinds: placeholder · competitor_name · unsupported_claim · architect_overclaim · causal_language · em_dash · leading_superlative · internal_token · uuid_leak. Severity bands per K4 (high / medium / low). Local `AuditableEditRow` type — ZERO `recommended-edits-persistence` dependency. Smart `\b` boundary handling for non-word tokens (`#1` / `architect-led`). (b) NEW `tests/domains/recommendation-intelligence/safety-audit.test.ts` (401 lines, 31 cases) — every kind exercised + multi-violation row + context excerpt + field coverage + null/empty safety. (c) NEW `tests/architecture/recommendation-safety-audit-coverage.test.ts` (167 lines, 15 cases) — all 9 `SafetyViolationKind` exercised + locked 10-token architect-overclaim + locked 9-token causal-language + scanner field coverage on `proposed_text` + `why`. **Locked decisions (K1-K7) honored**: K1 split (G-A only; G-B deferred); A.1 now further split per operator + hard-stop rule · K2 NO `credentials` BrandAssertionCategory · K3 architect/licensing audit-only patterns (10 locked terms) · K4 3-band severity · K5 standard operator gate (applied in A.2's page) · K6 audit ALL rows · K7 include all lifecycle statuses. **Auto-pass invariants**: `no-queue-write` (auto-covers scanner via `walk(INTEL_DIR)`) · `llm-draft-gateway-render-isolation` (unchanged) · `promotion-live-write-guards` (unchanged) · `offsite-contract` (unchanged) · `specific-edit-target-constraint` (no heavy-packet-builder invocation) · `catalog-sync` (1 new row added in lockstep). **Hard contracts honored**: scanner is pure / deterministic / no I/O / no LLM / no fetch / no Supabase / no persistence imports · NO `recommended-edits-persistence` import · NO `persistRecommendedEditsLocal` / `syncRecommendedEdits` · NO `runProviderAndPersist` · NO Supabase write shape · NO brand-assertions.ts changes · NO `BrandAssertionCategory` changes · NO validator changes · NO display-guard changes · NO registry changes · NO `generatorActive` flips · NO trigger predicate changes · NO customer-facing route changes · NO migrations · NO cron/workflows · NO env mutation in Vercel. **Line budget**: src+tests 981 lines — **under +1,000 hard stop by 19 lines** ✅. **Quality gates (LOCAL ONLY — no CI minutes used)**: typecheck ✅ · targeted A.1 suite (scanner 31 + coverage 15 = 46 cases ✅) · catalog-sync (8 cases ✅) · full architecture suite (TO RE-RUN after A.2 files removed) · full vitest + tenant-env build (TO RE-RUN). **STOPPED AT READY_TO_COMMIT — not pushed.** Proposed commit message: `feat(recommendations): add safety audit scanner`. **The pure scanner substrate is in place.** Any future operator surface (A.2 page, future cron, ad-hoc script) can call `auditRecommendedEditRow(...)` to detect violations across the 9 locked kinds. **Recommended next**: operator review → local commit only (1 commit ahead of `origin/main` after commit) → optional push of A.1 alone (safe — no surface; tests + invariant only) OR hold for A.2 (page + page tests + read-only file-boundary invariant; ~743 lines, well under hard stop). After A.2 lands, Section 4.5.G-A becomes operator-reachable end-to-end.

> 🟡 **SLICE 4.5.E.α₁b₂-B — LLM-DRAFT PREVIEW UI + 8-STATE RESULT BANNER (READY_TO_COMMIT — NOT PUSHED, no CI minutes used, 2026-05-21).** Fifth slice of Section 4.5.E. **Completes the operator-facing LLM-draft preview vertical** — operator can now click "Generate draft" on a weak-H2 diagnostic row from the browser. Pure UI consumer of α₁b₂-A's `generateLlmDraftAction`. **NO action / gateway / builder / trigger / registry / domain / invariant changes.** **1 MODIFIED src file (page.tsx)** + **1 NEW test file** + **5 doc syncs**. **Files**: (a) MODIFIED `src/app/(shell)/diagnostics/recommendation-triggers/page.tsx` (+372 insertions) — imports `generateLlmDraftAction` (function reference; NOT gateway) + `isLlmDraftGatewayEnabled`; threads `llmDraftResult` + `llmDraftEnabled` + `weakH2DiagnosticRows` into the page render path; adds `LlmDraftResult` 8-variant discriminated union + `parseLlmDraftResult(searchParams)` + `LlmDraftResultBanner` (8 states + unknown→null) + `LlmDraftPreviewSection` (filtered weak_h2 + rewrite_h2 diagnostic_only rows; calm empty state; env-off disabled button + caption; env-on per-row `<form action={generateLlmDraftAction}>` with hidden `candidate_dedupe_key` + `Generate draft` submit). (b) NEW `tests/app/diagnostics/recommendation-triggers-page-llm-draft-section.test.tsx` (~415 lines, 18 cases) — section render + 8 banner states + sanity carry-over (gateway mock never invoked from page render). **Render-isolation invariant UNCHANGED**: page.tsx satisfies all 6 source-text negatives (no gateway import / no `draftProposedTextForCandidate` reference / no `openaiProvider` import or symbol / no `openaiProvider.generate` / no `fetch(`); global allowlist still finds EXACTLY ONE caller = actions.ts. **Locked decisions (1-16) honored**: single UI-only slice · MODIFY page.tsx only · ADD page test file · NO action / gateway / build-thin-packet / trigger / registry / queue-write / persistence / runProviderAndPersist / Supabase-write / OpenAI-provider changes · NO LLM call on page render · NO bulk LLM calls · page imports ONLY `generateLlmDraftAction` from `./actions` · page does NOT import gateway · page does NOT reference `draftProposedTextForCandidate` · page does NOT import `openaiProvider`. **Auto-pass invariants**: render-isolation, no-queue-write, llm-draft-gateway-contract, promotion-live-write-guards, offsite-contract, specific-edit-target-constraint, catalog-sync. **Hard contracts honored**: NO action changes · NO gateway changes · NO build-thin-packet changes · NO trigger changes · NO registry changes · NO `generatorActive` flips · NO queue writes · NO persistence imports · NO `recommended-edits-persistence` import · NO `runProviderAndPersist` · NO direct Supabase `recommended_edits` write shape · NO OpenAI provider import in page · NO gateway import in page · NO `draftProposedTextForCandidate` reference in page · NO automatic LLM calls on page render · NO bulk LLM calls · NO customer-facing route changes · NO Today/Changes/Prompts/Settings/Recommendations/Section 9 changes · NO migrations · NO cron/workflows · NO env mutation in Vercel. **Line budget**: src+tests +X (TO measure) — expected ~787; under +1,000 hard stop. **Quality gates (LOCAL ONLY — no CI minutes used)**: typecheck ✅ · targeted α₁b₂-B page suite (18 cases) ✅ · regression (39 + 21 + 22 + 19 = 101 cases) ✅ · full architecture suite (5241 tests) ✅ · full vitest + tenant-env build TO RUN. **STOPPED AT READY_TO_COMMIT — not pushed.** Proposed commit message: `feat(recommendations): add llm draft preview UI`. **The LLM-draft preview vertical is now complete end-to-end behind `BEACON_LLM_DRAFT_GATEWAY_ENABLED=true`**: operator visits `/diagnostics/recommendation-triggers`, sees weak-H2 diagnostic candidates in the new LLM-Draft Preview section, clicks "Generate draft" on a row, and sees the result in one of 8 banners after the redirect. **Recommended next**: operator review → local commit only (5 commits ahead of `origin/main` after commit). Then operator decides whether to **batch-push the Section 4.5.E checkpoint** (all 5 commits together) or wait for additional 4.5.E work first.

> 🟡 **SLICE 4.5.E.α₁b₂-A — OPERATOR-ONLY SERVER ACTION + EVOLVED RENDER-ISOLATION INVARIANT (READY_TO_COMMIT — NOT PUSHED, no CI minutes used, 2026-05-21).** Fourth slice of Section 4.5.E. **First slice that activates the LLM call path end-to-end** (server-action side; page UI consumer deferred to α₁b₂-B). Split from oversized α₁b₂ first pass (1,725 lines = +725 over hard stop). **NO page UI change. NO LLM call from page render. NO bulk LLM call. NO queue write. NO confirmation phrase.** **1 MODIFIED src file** + **1 EVOLVED invariant** + **1 NEW test file** + **5 doc syncs**. **Files**: (a) MODIFIED `src/app/(shell)/diagnostics/recommendation-triggers/actions.ts` (+251 lines) — adds `generateLlmDraftAction(formData)` with 11-step gate ladder. (b) EVOLVED `tests/architecture/recommendation-intelligence-llm-draft-gateway-render-isolation.test.ts` — 7 cases → 19 cases (zero-callers → single-allowlist). (c) NEW `tests/app/diagnostics/recommendation-triggers-generate-llm-draft-action.test.ts` (22 cases). **Locked decisions honored (1-10)**: single slice (after operator split decision) · FormData input · fresh re-read · diagnostic_only only · URL params transport · proposed_text in URL capped 500 chars + truncated flag (operator OVERRIDE) · action-contract pins folded into render-isolation invariant · no confirmation phrase · no per-tenant rate limit · snapshot exact URL match. **Auto-pass invariants**: `no-queue-write` (auto-covers actions.ts already in scan set); `llm-draft-gateway-contract` (α₀ unchanged); `promotion-live-write-guards` (α₁c unchanged); `offsite-contract` (4.5.F unchanged); `specific-edit-target-constraint` (no heavy-packet-builder invocation); `catalog-sync` (existing row updated in-place). **Hard contracts honored**: NO page.tsx change · NO UI section · NO LLM-Draft Preview visual surface · NO bulk LLM calls · NO automatic LLM calls on page render · NO queue write · NO live promotion · NO persistence imports · NO `recommended-edits-persistence` import · NO `runProviderAndPersist` · NO direct Supabase `recommended_edits` write shape · NO OpenAI provider import in action (provider reached ONLY through gateway) · NO registry changes · NO `generatorActive` flips · NO trigger predicate changes · NO weak-h2 changes · NO llm-draft-gateway changes · NO build-thin-packet changes · NO `rewrite_faq` activation · NO `refresh_stale_page` addition · NO customer-facing route changes · NO Today/Changes/Prompts/Settings/Recommendations/Section 9 changes · NO migrations · NO cron/workflows · NO env mutation in Vercel. **Line budget**: src+tests +X (TO measure) — expected ~938; under +1,000 hard stop. **Quality gates (LOCAL ONLY — no CI minutes used)**: typecheck (clean ✅) · targeted α₁b₂-A suite (action 22 + render-isolation 19 = 41 cases ✅) · full vitest + tenant-env build TO RUN. **STOPPED AT READY_TO_COMMIT — not pushed.** Proposed commit message: `feat(recommendations): add llm draft action`. **The LLM call path is operator-reachable end-to-end via hand-crafted POST** behind `BEACON_LLM_DRAFT_GATEWAY_ENABLED=true`. **Recommended next**: operator review → local commit only (4 commits ahead of `origin/main` after commit: `2923f25` α₀ + `ee90111` α₁a + `5666639` α₁b₁ + new α₁b₂-A). Then **Slice 4.5.E.α₁b₂-B preflight** — page UI section + result banner + page render tests; consumes the action shipped here. No invariant changes expected (page.tsx adds a button that POSTs to the existing action; gateway is still imported by exactly one file).

> 🟡 **SLICE 4.5.E.α₁b₁ — ENV FLAG + THIN PACKET BUILDER + RENDER-ISOLATION INVARIANT (READY_TO_COMMIT — NOT PUSHED, no CI minutes used, 2026-05-21).** Third slice of Section 4.5.E LLM-assisted sub-chain. **Caller-side infrastructure ONLY.** Adds (1) the env-flag helper that will gate future α₁b₂ server-action invocation of the gateway, (2) the pure thin packet builder that bridges α₁a weak-H2 candidates to α₀ gateway packets, and (3) the render-isolation architecture invariant that pins the "no caller yet" boundary. **NO server action. NO LLM call. NO gateway invocation. NO UI change. NO action-type registry change. NO eligibility-table change. NO loader change.** The α₀ gateway stays uncalled; α₁b₂ will wire the env-gated operator-only action. **2 NEW src modules** + **1 NEW unit-test suite (21 cases)** + **1 NEW architecture invariant (7 cases)** + **5 doc syncs**. **Files added**: (1) `src/lib/llm-draft-gateway-flag.ts` — `isLlmDraftGatewayEnabled()` reading `BEACON_LLM_DRAFT_GATEWAY_ENABLED === "true"` (strict casing; default unset ⇒ `false`). (2) `src/domains/recommendation-intelligence/build-thin-packet.ts` — `buildThinPacketForCandidate(...)` pure builder; 8 fail-loud preconditions (target_url presence/sentinel + action_type==="rewrite_h2" + tenant_id non-empty + URL match + h2_list non-empty + `h2[<n>]=` token + index in range + non-blank text); locked field assignments per α₁b₁ design lock (resolution: null, affectedPrompts: [], single-entry allowedTargetUrls/ActionTypes, targetPageElements synthesized w/ `h2[<index>]:<sha1.slice(0,12)>` elementKey, recId `"preview-" + dedupe_key.slice(0,16)`, evidenceHash sha1 of canonical-JSON sans hash); empty brandAssertions does NOT throw (validator handles via abstention contract Rule B). (3) `tests/domains/recommendation-intelligence/build-thin-packet.test.ts` — 21 cases (10 fail-loud + 10 happy-path/locked-field + 2 lowest-index + 3 determinism). (4) `tests/architecture/recommendation-intelligence-llm-draft-gateway-render-isolation.test.ts` — 7 cases (4 source-text pins on diagnostic page.tsx + 2 global negative scans across `src/app/**`). **Locked α₁b₁ decisions (1-15) honored**: single slice (no split) · strict-cased env flag · pure builder · 8 fail-loud preconditions · empty brandAssertions allowed · render-isolation invariant scope (page.tsx + global app sweep) · element_key shape · recId/evidenceHash shapes · 21+7 test cases · stop at READY_TO_COMMIT (no commit / no push / no CI / no Vercel). **Auto-pass invariants**: `recommendation-intelligence-no-queue-write` (auto-covers builder via `walk(INTEL_DIR)`); `recommendation-intelligence-llm-draft-gateway-contract` (α₀; gateway unchanged); `recommendation-intelligence-promotion-live-write-guards` (α₁c; unchanged); `recommendation-intelligence-offsite-contract` (4.5.F; unchanged); `specific-edit-target-constraint` (builder does not invoke the heavy packet builder); `recommendation-intelligence-no-llm-decides` (no new generatorActive flips). **Hard contracts honored**: NO push · NO CI · NO Vercel · NO LLM · NO server action · NO gateway invocation · NO customer-facing route changes · NO α₀ / α₀a-d / α₀b / α₁a-b / α₁c / 4.5.F / 4.5.E.α₀ / 4.5.E.α₁a module modifications · NO OpenAI provider / validator / budget ledger changes · NO action-type registry changes · NO eligibility-table changes · NO customer-copy template changes · NO loader change · NO queue writes · NO persistence imports · NO migrations / cron / workflows. **Line budget**: src+tests +X lines (TO measure) — expected ~535; well under ≤700 target. **Quality gates (LOCAL ONLY — no CI minutes used)**: typecheck (clean ✅) · targeted α₁b₁ suite (2 files / 32 cases ✅) · α₀ + α₁a regression (3 files / 58 cases ✅) · catalog-sync (8 cases ✅) · full architecture suite (TO RE-RUN) · full vitest + tenant-env build (TO RUN). **STOPPED AT READY_TO_COMMIT — not pushed.** Proposed commit message: `feat(recommendations): add llm draft thin packet builder`. **Recommended next**: operator review → local commit only (3 commits ahead of `origin/main` after commit: `2923f25` α₀ + `ee90111` α₁a + new α₁b₁). Decide whether to solo-push α₁b₁ or batch with α₁b₂. Then **Slice 4.5.E.α₁b₂ preflight** — env-gated operator-only server action under `src/app/(shell)/diagnostics/recommendation-triggers/actions.ts` that consults `isLlmDraftGatewayEnabled()` + reads a single weak-H2 candidate + invokes `buildThinPacketForCandidate(...)` + hands the packet to `draftProposedTextForCandidate(...)` + renders the result in the operator preview surface; will revise the render-isolation invariant to allow that single allowlisted action file.

> 🟡 **SLICE 4.5.E.α₁a — WEAK-H2 TRIGGER PREDICATE + `rewrite_h2` ACTIVATION (READY_TO_COMMIT — NOT PUSHED, no CI minutes used, 2026-05-21).** Second slice of Section 4.5.E LLM-assisted sub-chain. Pure detection layer only — adds `weak-h2` predicate emitting `rewrite_h2` candidates at `confidence: "low"` → `diagnostic_only` routing. **NO env flag. NO server action. NO LLM call. NO gateway invocation. NO queue write. NO UI.** α₀ gateway (locally committed at `2923f25`) stays dormant. **1 NEW src predicate** + **1 NEW unit suite (29 cases)** + **6 modified files** + **4 architecture invariants extended** + **5 doc syncs**. **Locked α₁a decisions honored**: activate `rewrite_h2` only · do NOT activate `rewrite_faq` or `refresh_stale_page` · `weak_h2::rewrite_h2 = "diagnostic-only"` eligibility tier · `confidence: "low"` LITERAL · `generator_kind: "llm_assisted"` LITERAL · per-page emission (NOT per-H2) · operator-locked `rewriteH2Copy()` phrasing · no UI / env flag / gateway / queue write in this slice. **Auto-pass invariants**: no-queue-write (auto-covers new predicate file); trigger-predicates-purity; no-llm-decides (auto-discovers `action_type: "rewrite_h2"` literal); llm-draft-gateway-contract (α₀ unchanged); promotion-live-write-guards (α₁c unchanged); offsite-contract (4.5.F unchanged). **Hard contracts honored**: NO push · NO CI · NO Vercel · NO LLM · NO gateway invocation · NO customer-facing route changes · NO α₀ / α₀a-d / α₀b / α₁a-b / α₁c / 4.5.F / 4.5.E.α₀ module modifications · NO OpenAI provider / validator / budget ledger changes · NO `rewrite_faq` activation · NO `refresh_stale_page` registry add · NO queue writes · NO persistence imports · NO migrations / cron / workflows. **Line budget**: src+tests +X lines (TO measure) — expected ~435; well under ≤700 target. **Quality gates (LOCAL ONLY — no CI minutes used)**: typecheck (clean ✅) · targeted Section 4.5.E.α₁a suite (11 files / 204 cases green ✅) · full suite + tenant-env build (TO RUN). **STOPPED AT READY_TO_COMMIT — not pushed.** Proposed commit message: `feat(recommendations): activate weak h2 llm diagnostic trigger`. **First LLM-assisted detection predicate is in place.** **Recommended next**: operator review → local commit only (2 commits ahead of `origin/main` after commit: `2923f25` α₀ + new α₁a). Decide whether to solo-push the α₁a (or batch with α₀ as the operator's preferred batch-with-α₁ pattern) or hold for α₁b₁ batch. Then **Slice 4.5.E.α₁b₁ preflight** — `BEACON_LLM_DRAFT_GATEWAY_ENABLED` env helper + thin packet builder + render-isolation invariant. NO UI; UI work lands in α₁b₂.

> 🟢 **SLICE 4.5.E.α₀ — LLM-ASSISTED DRAFTING GATEWAY (LOCAL COMMIT `2923f25` — NOT PUSHED, no CI minutes used, 2026-05-21).** First slice of Section 4.5.E LLM-assisted sub-chain. **Pure infrastructure only.** Gateway module `src/domains/recommendation-intelligence/llm-draft-gateway.ts` wires `openaiProvider.generate()` through existing budget + validator gates and returns a discriminated-union draft result (4 states: `drafted` / `abstained` / `validation_failed` / `blocked_budget`). **NO production caller yet** — callable from tests + future α₁+ slices only. **No action-type activations, no trigger predicates, no preview surface, no queue write, no persistence path, no env flag.** **1 NEW src module** + **1 NEW unit suite (17 cases)** + **1 NEW invariant (12 cases)** + **1 catalog row added**. **Locked α₀ decisions honored**: gateway accepts a built `SpecificEditEvidencePacket` as input · reuses `openaiProvider.generate(packet)` / `checkBudget()` / `recordSpend()` / `validateSpecificEdit()` · NEVER calls `runProviderAndPersist` · NO `recommended-edits-persistence` import · tests mock `openaiProvider` at the import boundary (NO real LLM calls in tests). **Auto-pass (25 existing α-family + α₀a + α₀b + α₁a + α₁b + α₁c + 4.5.F invariants)**, including `recommendation-intelligence-no-queue-write` which auto-covers the new gateway file via `walk(INTEL_DIR)`. **Hard contracts honored**: NO push · NO CI · NO Vercel · NO action-types.ts changes · NO `generatorActive` flips · NO trigger predicates · NO diagnostic page changes · NO customer-facing route changes · NO OpenAI provider / validator / packet-builder changes · NO α₀a / α₀b / α₁a / α₁b / α₁c module modifications · NO Section 7 / 4.5.F adapter or queue-rule changes · NO persistence imports · NO Supabase write shapes · NO migrations / cron / workflows · NO new env flag · gateway is type-agnostic (no branching on action_type). **Line budget**: src+tests +750 lines (gateway 153 · gateway test 450 · invariant 147) — 50 over the ≤700 target; **100-line cushion to ≤850 soft threshold** ✅ (no mandatory trim); 250-line cushion to +1,000 hard stop. The 5 spend-recording cases were kept as per-state explicit pins (justification: spend semantics are the most cost-safety-critical contract). **Quality gates (LOCAL ONLY — no CI minutes used)**: typecheck (clean ✅) · targeted Section 4.5.E.α₀ suite (3 files / 36 cases green ✅) · full suite + tenant-env build (TO RUN). **STOPPED AT READY_TO_COMMIT — not pushed.** Proposed commit message: `feat(recommendations): add llm draft gateway`. **LLM-drafting infrastructure is in place; no production behavior change.** **Recommended next**: operator review → local commit only; decide whether to solo-push 4.5.E.α₀ alone (zero blast radius — no caller exists) or batch with α₁. Then **Slice 4.5.E.α₁ preflight** — first trigger predicate (e.g., `rewrite-h2-needed`) + `rewrite_h2` activation + env-flag-gated operator-only diagnostic preview section (`BEACON_LLM_DRAFT_GATEWAY_ENABLED`). NO queue write in α₁ — preview-only.

> 🟢 **SLICE 4.5.F — OFF-SITE SHARED QUEUE CONTRACT (LOCAL COMMIT `c357136` → origin/main, production-verified, 2026-05-21).** Pure type-level + plumbing slice. Defines the adapter `offSiteCandidateToCandidateRow` (Section 7's `OffSiteCandidateAction` → Section 4.5's `RecommendationCandidateRow`) + extends `applyQueueRules` with an off-site carve-out that ALWAYS routes off-site rows to `diagnostic_only`. **NO Section 7 wire-up. NO customer surface change. NO persistence path. NO `generatorActive` flips.** Off-site stays detection-only + permanently blocked from customer-queue promotion (locked at α₀a.1 + α₀a.3a Gate 1 + the new routing carve-out — triple defense in depth). **1 NEW src adapter (107 lines)** + **1 MODIFIED emitter (+32 net)** + **1 NEW unit suite (17 cases)** + **1 EXTENDED emitter test (+14 cases)** + **1 NEW invariant (27 cases)** + **1 catalog row added**. **Operator decisions honored**: F1 (off-site→diagnostic_only routing only, NEVER candidates) · F2 (adapter in off-site-authority/) · F3 (target_url: null) · F4 (off_site:${channel} namespace) · F5 (impact_estimate mapping) · F6 (reuse unsupported_claim_risk) · F7 (reuse business_config evidence kind) · F8 (no loader wire-up) · F9/F10 (full invariant pin set). **Locked safer correction honored**: off-site detection inside `applyQueueRules` uses `ACTION_TYPE_REGISTRY[actionType].signalType === "off_page_seo"` (derived from existing registry); **did NOT modify `promotion-eligibility.ts`** or any α₀a module. **Auto-pass (24 existing α-family + α₀a + α₀b + α₁a + α₁b + α₁c invariants).** **Hard contracts honored**: NO push · NO CI · NO Vercel · NO LLM · NO external API · NO fetch · NO Supabase migration · NO cron · NO customer-facing route changes · NO Section 7 row-builder modification · NO `/diagnostics/off-site-authority` page modification · NO `/diagnostics/recommendation-triggers` page or action modification · NO `generatorActive` flip · NO persistence path · NO promotion-writer change · NO live-write guard change · NO α₀a / α₀b / α₁a / α₁b / α₁c module modifications · NO new evidence-kind value · NO new safety-flag value. **Line budget**: src+tests **+677 insertions / +673 net** — **under ≤700 target by 23–27 lines** ✅; 327-line cushion to +1,000 hard stop. **Quality gates (LOCAL ONLY — no CI minutes used)**: typecheck (clean ✅) · targeted suite (4 files / 73 cases green ✅) · full suite + tenant-env build (TO RUN). **STOPPED AT READY_TO_COMMIT — not pushed.** Proposed commit message: `feat(recommendations): add off-site shared queue contract`. **Shared queue language is now formalized**: deterministic-promotion + off-site + (future) LLM-assisted rows all use the `RecommendationCandidateRow` carrier; routing differences live in `applyQueueRules` + downstream safety gates; the customer-queue boundary remains protected by the triple-gate defense. **Recommended next**: operator review → local commit only; decide whether to batch-push at the Section 4.5.E/G readiness checkpoint or push 4.5.F alone. Then **4.5.E preflight** (LLM-assisted gateway, independent of Section 10 per O11) OR **4.5.G preflight** (safety cleanup) — operator picks ordering.

> 🟢 **SLICE 4.5.D.α₁c — OPERATOR-ONLY LIVE-WRITE GESTURE + ENV-FLAG GUARD (LOCAL COMMIT `14eebc3` → origin/main, production-verified, 2026-05-20).** First slice that wires a UI-reachable code path to `promoteEligibleCandidates({ dryRun: false })`. **2 NEW src modules** (env helper + server action) + **1 MODIFIED page** + **1 NEW action test** + **1 EXTENDED page test** + **1 NEW invariant** + **1 EVOLVED invariant in-place** + **1 catalog row added + 1 catalog row refreshed**. **Adds**: (1) `src/lib/promotion-live-write.ts` — pure helper `isPromotionLiveWriteEnabled()` reading `process.env.BEACON_PROMOTION_LIVE_WRITE_ENABLED === "true"` (strict case; mirrors `isOperatorModeServer` convention). (2) `src/app/(shell)/diagnostics/recommendation-triggers/actions.ts` — `"use server"` file exporting `promoteEligibleCandidatesAction(formData)`. Three-gate ladder: operator mode → env flag → `formData.get("confirmation") === "PROMOTE"` (strict). After all 3 pass: resolve tenant → call `promoteEligibleCandidates({ tenantId, dryRun: false })` → on throw catch + log.warn + redirect with `action_result=error&msg=...` (200-char truncate) → on success `revalidatePath` + redirect with `action_result=promoted&promoted_count=N&skipped_count=M&mapped_row_count=K[&sync_warning=...]`. NO direct `recommended-edits-persistence` import; NO `runProviderAndPersist`; NO direct Supabase write shape — writer is the SOLE persistence path. (3) `page.tsx` modifications — `parseActionResult` typed parser + `<ActionResultBanner>` (5 known kinds + unknown → no banner) + `<PromoteForm>` (disabled/enabled/suppressed-at-0 states). (4) Action test (12 cases): operator OFF + NODE_ENV!=test → notFound · env flag OFF/True/1 all blocked · missing/lowercase/title-case confirmation blocked · tenant resolve throws → blocked_no_tenant · happy-path writer called with `dryRun:false` + `revalidatePath` called + correct redirect URL · sync_warning truncated to 200 chars · writer throws → error+msg → `revalidatePath` NOT called · writer throws long message → msg truncated. Uses `vi.hoisted()` mockState + `Mock<TFunc>` parameterization + typed redirect/notFound error capture + `vi.stubEnv`/`vi.unstubAllEnvs` for NODE_ENV exercise. (5) Page-test extension (8 new cases): enabled form · disabled form · suppressed at eligible=0 · promoted banner with counts · promoted banner with sync_warning · blocked banners (parametric × 3) · error banner · unknown action_result renders no banner. (6) NEW invariant `recommendation-intelligence-promotion-live-write-guards` (10 cases): 8 source-text pins on `actions.ts` + 1 global negative scan asserting `actions.ts` is the ONLY file under `src/app/**` pairing `promoteEligibleCandidates` + `dryRun: false`. (7) EVOLVED `recommendation-intelligence-no-queue-write` in-place: file scan set extends to include `actions.ts`; 2 new pins (set non-empty includes both surfaces; actions.ts NOT in persistence-import allowlist regression guard). (8) Catalog: refreshed no-queue-write entry; added new live-write-guards row. **Operator decisions honored**: Y1—Y8 from approval. **Auto-pass (19 existing α-family + α₀a + α₀b + α₁a + α₁b invariants).** **Hard contracts honored**: NO `runProviderAndPersist` · NO LLM · NO external API · NO `fetch(` · NO Supabase migration · NO cron / workflow changes · NO customer-facing route changes · NO Today / Changes / Prompts / Settings / Recommendations / Section 9 changes · α₀a / α₀b / α₁a / α₁b src modules UNCHANGED · `SuppressionReason` / `SpecificEditEvidenceRef` / `SpecificEditSource` UNCHANGED. **Line budget**: src+tests ~735 lines net — **under ≤850 soft threshold by 115 lines** ✅; 265-line cushion to +1,000 hard stop; 35 over the ≤700 target but within the "above 700 / under 850" approval envelope. **Quality gates (LOCAL ONLY — no CI minutes used)**: typecheck (clean ✅) · targeted suite (25 invariant cases + 12 action cases + 51 page cases all green ✅) · full suite + tenant-env build (TO RUN). **STOPPED AT READY_TO_COMMIT — not pushed.** Proposed commit message: `feat(recommendations): add promotion live-write guard`. **Customer-queue writer pathway is now FULLY WIRED locally (α₀a + α₀b + α₁a + α₁b + α₁c).** Live writes require: operator mode ON + `BEACON_PROMOTION_LIVE_WRITE_ENABLED=true` + operator visits `/diagnostics/recommendation-triggers` + types `PROMOTE` + clicks submit. **Recommended next**: operator review → local commit only (5 commits ahead of origin/main); decide whether to batch-push at this Section 4.5.D closeout checkpoint. After that: Section 4.5 closeout report. Then 4.5.E LLM-assisted gateway (independent of Section 10) OR 4.5.F off-site contract OR 4.5.G safety cleanup — operator picks ordering per O11/O12 locks.

> 🟢 **SLICE 4.5.D.α₁b — PROMOTION WRITER + IDEMPOTENT PERSISTENCE (LOCAL COMMIT `8ae1603` — NOT PUSHED, no CI minutes used, 2026-05-20).** First slice that actually crosses the customer-queue write boundary. The α₀a pure decision engine + α₀b operator preview + α₁a mapper all stayed write-free. **α₁b is the SINGLE allowlisted importer of `recommended-edits-persistence`** in the recommendation-intelligence tree — pinned via the EVOLVED `recommendation-intelligence-no-queue-write` invariant. **1 NEW src module (193 lines)** + **1 NEW unit suite (13 cases)** + **1 INVARIANT EVOLUTION in-place** + **1 catalog refresh**. **Adds**: (1) `promotion-writer.ts` exporting `promoteEligibleCandidates(input): Promise<PromoteEligibleCandidatesResult>`. **Default-safe (Y2)**: `dryRun` defaults to `true`; live-write requires explicit `dryRun: false`. **Recompute-don't-trust (Y3)**: writer ALWAYS recomputes `selectPromotableCandidates` at write time from fresh `loadTriggerCandidatesForTenant` + repository `getRecommendedEdits` + `getRecommendationResponses` reads (3 parallel `Promise.all`); does NOT consume α₀b's render-time preview cache. **Idempotency (3 layers)**: α₁a `rec_id = "promotion-${cooldown_key.slice(0, 16)}"` + `persistRecommendedEditsLocal` `Map<row.id, row>` dedupe + Supabase `(tenant_id, rec_id, action_type, target_element_key)` unique index `NULLS NOT DISTINCT`. **Error handling (Y4 — mirrors `markRecommendedEditsAccepted` lines 360–368)**: local write throws → propagate (source-of-truth failure, fail-loud); Supabase sync throws → `log.warn` + return `sync_warning` in result; operator can retry; next call re-syncs from local. **Result shape (Y8)**: `{ dryRun, candidate_count, eligible_count, promoted_count, skipped_count, mapped_rows, sync_warning }` — ALWAYS returns `mapped_rows` so the caller can inspect what WOULD or DID get written across BOTH modes. **Skipped count**: `eligible_count - promoted_count` (rows the α₁a mapper rejected via its 5 defensive null-returns within the eligible set). **No new env flag (Y6 deferred to α₁c)**; **no UI gesture (Y7 deferred to α₁c)**. (2) `promotion-writer.test.ts` (13 cases, all passing): empty input · dryRun default true · dryRun true explicit · dryRun false happy path · `mapped_rows` returned with dryRun false · diagnostic-only suppression · low-confidence suppression · safety-flag suppression · 6-same-URL cap (5+1 max_rows_per_page) · idempotency × 2 calls → identical `rec_id` set · persist throw propagates fail-loud · sync throw populates `sync_warning` without breaking call · mixed batch counter math. Uses `vi.hoisted()` mockState pattern to dodge `vi.mock` factory hoisting init errors. (3) **Architecture invariant EVOLVED in-place (Y5)** — `recommendation-intelligence-no-queue-write.test.ts` refactored: NEW `ALLOWED_PERSISTENCE_IMPORT_FILES` Set (single entry: `promotion-writer.ts` absolute path). Split parametric `it.each(FORBIDDEN_IMPORTS)` into 2 separate tests: (a) parametric persistence-import scan WITH allowlist exception; (c) `runProviderAndPersist` GLOBAL scan (no allowlist — even the writer uses persistence helpers directly, NOT the LLM orchestrator). NEW positive importer pin test (b): `promotion-writer.ts` MUST exist AND import `@/domains/recommendations/recommended-edits-persistence` AND call both `persistRecommendedEditsLocal` + `syncRecommendedEdits` — defines the customer-queue writer contract. Existing Supabase write-shape scan UNCHANGED. **Catalog**: `docs/ARCHITECTURE_INVARIANTS_CATALOG.md` no-queue-write entry refreshed with α₁b evolution narrative + 4-test contract + last-verified date 2026-05-20. **Operator decisions honored**: Y1 (ship α₁b as one slice) · Y2 (`dryRun: true` default) · Y3 (always recompute) · Y4 (local-fail-loud + sync-best-effort) · Y5 (evolve `no-queue-write` invariant in-place with single-file allowlist) · Y6 (no env flag — α₁c) · Y7 (no UI gesture — α₁c) · Y8 (always return `mapped_rows`). **Auto-pass (17 existing α-family invariants)** including all α₀a / α₀b / α₁a pins. **Hard contracts honored**: NO `runProviderAndPersist` import or call (LLM orchestrator path STAYS forbidden — α₁b uses persistence helpers directly) · NO LLM call · NO external API · NO `fetch(` · NO Supabase migration · NO cron / workflow / env-flag changes · NO UI gesture (α₁c) · NO customer-facing route changes · NO `/diagnostics/recommendation-triggers/page.tsx` edits · NO Today / Changes / Prompts / Settings / Recommendations / Section 9 changes · α₀a / α₀b / α₁a modules UNCHANGED · `SuppressionReason` union UNCHANGED · `SpecificEditEvidenceRef` UNCHANGED · `SpecificEditSource` UNCHANGED at α₁a's 5+1 values. **Line budget**: src+tests +786 lines total (writer src 193 · writer test 541 · invariant in-place evolution +52 net) — **under ≤850 soft threshold by 64 lines** ✅; 214-line cushion to +1,000 hard stop; 86 over the ≤700 target but within the operator's "above 700 / under 850" approval envelope. **Quality gates (LOCAL ONLY — no CI minutes used)**: typecheck (clean ✅) · targeted suite (2 files / 19 cases green ✅) · catalog-sync ✅ · full suite · `BEACON_TENANT_ID=tenant-ritz-founder npm run build`. **STOPPED AT READY_TO_COMMIT — not pushed.** Proposed commit message: `feat(recommendations): add promotion writer with idempotent persistence`. **Customer-queue writer pathway is now COMPLETE locally (α₀a + α₀b + α₁a + α₁b).** **Recommended next**: operator review → optional commit → **Slice 4.5.D.α₁c preflight** — operator-only "Promote eligible candidates" form action on `/diagnostics/recommendation-triggers` (the UI gesture that calls `promoteEligibleCandidates({ dryRun: false })`) + optional `BEACON_PROMOTION_LIVE_WRITE` env-flag gate.

> 🟢 **SLICE 4.5.D.α₁a — PROMOTION ROW MAPPER (LOCAL COMMIT `77f3b00` — NOT PUSHED, no CI minutes used, 2026-05-20).** First slice on the customer-queue boundary chain. Pure mapper from `PromotionResult` → `recommended_edits`-shaped row. **NO writes. NO UI. NO observable customer behavior yet.** **1 NEW src module** + **1 enum extension** + **1 NEW unit suite** + **2 NEW invariants** + **2 catalog rows**. **Adds**: (1) `promotion-result-to-edit-row.ts` — pure `promotionResultToRecommendedEditRow(result, now)`. Returns local minimal `DeterministicPromotionEditRow` (no `recommended-edits-persistence` import). 5 defensive null-returns (belt-and-suspenders for α₀a.3a safety gates). `rec_id = promotion-${promotion_cooldown_key.slice(0, 16)}` leverages existing unique index for idempotency. `source = "deterministic_promotion"` distinct from legacy `"deterministic"`. `evidence_hash = sha1(${dedupe_key}::${cooldown_key}::${priority_score})` for full back-trace. `evidence: [{ type: "owned_page", url: target_url }]` — operator-locked: ONLY `SpecificEditSource` extended; `SpecificEditEvidenceRef` discriminated union UNCHANGED. (2) `SpecificEditSource` extended with exactly one new value `"deterministic_promotion"`. (3) 2 architecture invariants pinning enum + behavioral mapping. **Operator decisions honored**: X1 (3-way split α₁a + α₁b + α₁c) · X2 (mapper-only) · X3 (new source value) · X4 (rec_id format) · X5 (traceability via existing fields, NOT new variants) · X6 (local minimal type) · X7 (5 defensive rejections) · X8 (`difficulty: "low"` v1). **Auto-pass (17 existing invariants)** including `recommendation-intelligence-no-queue-write` UNCHANGED. **Hard contracts honored**: NO writes · NO persistence imports · NO `runProviderAndPersist` · NO LLM · NO Supabase mutation · NO connector/external API · NO migrations · NO cron · NO UI · NO diagnostic page edits · NO env flag · α₀a / α₀b modules UNCHANGED · `SuppressionReason` UNCHANGED · `SpecificEditEvidenceRef` UNCHANGED. **Line budget**: 722 src+tests — **under ≤850 soft threshold by 128 lines**; 278-line cushion to hard stop. **Quality gates (LOCAL ONLY — no CI minutes used)**: typecheck (clean ✅) · targeted suite (30 cases green ✅) · catalog-sync ✅ · full suite · `BEACON_TENANT_ID=tenant-ritz-founder npm run build`. **STOPPED AT READY_TO_COMMIT — not pushed.** Proposed commit message: `feat(recommendations): add promotion row mapper`. **Recommended next**: operator review → optional commit → **Slice 4.5.D.α₁b preflight** (writer module + idempotent persistence). α₁c operator UI gesture follows α₁b.

> 🟢 **SLICE 4.5.D.α₀b — OPERATOR-ONLY PROMOTION PREVIEW UI (LOCAL COMMIT `0d7414d` — NOT PUSHED, no CI minutes used, 2026-05-20).** First post-α₀a slice. Adds a DRY-RUN render-only visualization of the pure decision engine on the existing operator-only `/diagnostics/recommendation-triggers` page. **1 MODIFIED page** + **1 EXTENDED unit suite** + **1 NEW invariant** + **1 catalog row**. **No writes. No customer surface. No new top-level route.** **Adds**: (1) MODIFIED `page.tsx` — 4 new imports (repository + business-config + α₀a.3b orchestrator + page-classifier + response-store) + 3 read-only data loads + inline `pageTypeByUrl` builder + `<PromotionPreviewSection>` component with 3 subsections (Eligible · Capped · Safety-suppressed) + 9-column row table. Always renders (even at 0/0) so operator confirms engine is active. (2) EXTENDED test file — 8 new α₀b render cases. Extended repository mock to expose `getRecommendedEdits`; new mock for `recommendation-response-store`. (3) NEW invariant `recommendation-triggers-promotion-preview-no-writes` — 3 negative + 5 positive source-text assertions pinning the section's existence + no-write contract. **Operator decisions honored**: W1 (1-slice) · W2 (repository read path; no persistence imports) · W3 (response-store read acceptable) · W4 (inline `pageTypeByUrl`) · W5 (3 subsections) · W6 (always-render with 0/0; no empty tables) · W7 (new invariant + existing `no-queue-write` unchanged) · W8 (local-only). **Auto-pass (14 existing α-family invariants).** **Hard contracts honored**: NO writes to `recommended_edits` · NO imports from `recommended-edits-persistence` · NO `runProviderAndPersist` · NO LLM · NO Supabase mutation · NO connector/external API call · NO migrations · NO cron / workflow changes · NO customer-facing route changes · NO Today / Changes / Prompts / Settings / Recommendations / Section 9 changes · NO env flag · α₀a.1 / α₀a.2 / α₀a.3a / α₀a.3b modules UNCHANGED · `SuppressionReason` union UNCHANGED at 12 values. **Line budget**: 481 src+tests — **219 UNDER the ≤700 target.** Cleanest landing of the α₀a chain. **Quality gates (LOCAL ONLY — no CI minutes used)**: typecheck (clean ✅) · targeted suite (51 cases green ✅) · catalog-sync ✅ · full suite · `BEACON_TENANT_ID=tenant-ritz-founder npm run build`. **STOPPED AT READY_TO_COMMIT — not pushed.** Proposed commit message: `feat(recommendations): add operator-only promotion preview UI`. **Pure decision engine (α₀a.1 + α₀a.2 + α₀a.3a + α₀a.3b) + operator preview (α₀b) both complete locally.** **Recommended next**: operator review → optional commit + push at a meaningful checkpoint (e.g., batched with α₁ writer; the α₀b preview gives the operator visual validation BEFORE the writer pathway ever flips a customer queue). Then **Slice 4.5.D.α₁ preflight** — customer-queue writer pathway (first slice that crosses the customer-queue boundary; explicit operator authorization required).

> 🟢 **SLICE 4.5.D.α₀a.3b — PROMOTION ORCHESTRATOR + CAPS (LOCAL COMMIT `77ae6a4` — NOT PUSHED, no CI minutes used, 2026-05-20).** Fourth sub-slice of the de-scoped 4.5.D.α₀a chain. Closes the pure promotion decision engine. **1 NEW src module** + **1 NEW unit suite** + **2 NEW cap invariants** + **2 catalog rows**. **No UI. No writes. No customer surface.** **Adds**: (1) `promote-to-queue.ts` — three-stage pure orchestrator `selectPromotableCandidates(...)`. Stage 1 per-candidate gate + score + key build. Stage 2 partition + sort eligibles desc by priority with `promotion_dedupe_key.localeCompare()` ascending tiebreaker. Stage 3 caps in priority order: per-page cap (key=`target_url ?? "no_url"`) runs first, then per-family cap (key=`action_type`); surplus rows flip to `eligible: false` with `max_rows_per_page` / `max_rows_per_family` suppression — surplus rows ARE kept in output, not deleted. Exports: `MAX_ROWS_PER_PAGE = 5`, `MAX_ROWS_PER_FAMILY = 10`, `PromotionResultSuppressionReason` (extends locked α₀a.3a `SuppressionReason` with the 2 cap values; the underlying `SuppressionReason` is UNCHANGED), `PromotionResult`, `SelectPromotableCandidatesInput`, `selectPromotableCandidates(...)`. (2) Two architecture invariants pinning both caps + behavior. **Operator decisions honored**: V1 (1-slice ship) · V2 (5/10 caps locked) · V3 (cap reasons OUTSIDE locked `SuppressionReason`) · V4 (caps apply only to safety-passed rows) · V5 (deterministic `localeCompare()` tiebreaker) · V6 (local-only workflow). **Auto-pass (13 existing α-family invariants)**: registry-active-set, customer-copy-vocab, no-queue-write, no-llm-decides, promotion-eligibility-pin, priority-score-contract, dedupe-key-formula, cooldown-windows, no-diagnostic-only-promotion, confidence-low-stays-diagnostic, promotion-respects-already-accepted, page-classifier-applied-at-promotion, no-promotion-without-evidence. **Hard contracts honored**: NO writes to `recommended_edits` · NO imports from persistence stores · NO `runProviderAndPersist` · NO LLM · NO Supabase mutation · NO connector/external API call · NO migrations · NO cron / workflow changes · NO customer-facing route changes · NO `/diagnostics/recommendation-triggers` UI edits · NO Today / Changes / Prompts / Settings / Recommendations / Section 9 changes · NO env flag · α₀a.3a `SuppressionReason` union UNCHANGED. **Line budget**: 836 src+tests — **under the ≤850 soft threshold by 14 lines**; 164-line cushion to +1,000 hard stop; within operator-approved ~720 estimate (+16% margin). No trim pass triggered. **Quality gates (LOCAL ONLY — no CI minutes used)**: typecheck (clean ✅) · targeted suite (19 cases green ✅) · catalog-sync ✅ · full suite · `BEACON_TENANT_ID=tenant-ritz-founder npm run build`. **STOPPED AT READY_TO_COMMIT — not pushed.** Proposed commit message: `feat(recommendations): add promotion orchestrator and caps`. **Pure promotion decision engine is now COMPLETE** (α₀a.1 + α₀a.2 + α₀a.3a + α₀a.3b together). **Recommended next**: operator review → optional commit + push at meaningful checkpoint (e.g., before α₀b UI or before operator manual production smoke). Then **Slice 4.5.D.α₀b preflight** — operator-only Promotion Preview UI section on `/diagnostics/recommendation-triggers` (DRY-RUN; no writes; consumes `selectPromotableCandidates` for read-only display). Customer-queue writer remains deferred to α₁.

> 🟢 **SLICE 4.5.D.α₀a.3a — PROMOTION SAFETY-GATES MODULE (SHIPPED, commit `ebc06f6`, 2026-05-20).** Third sub-slice of the de-scoped 4.5.D.α₀a chain. Ships the 13-step per-row safety-gate ladder + 5 paired gate-level invariants. **1 NEW src module** + **1 NEW unit suite** + **5 NEW invariants** + **5 catalog rows**. **No orchestrator. No caps. No UI. No writes.** **Adds**: (1) `safety-gates.ts` exporting `applyPromotionSafetyGates(...)` + `SuppressionReason` (12-value union: blocked_tier · diagnostic_only_tier · low_confidence · safety_flags_set · no_evidence · missing_target_url · missing_target_page_type · skip_page_type · in_cooldown · accepted_ancestor_exists · signal_stale · prerequisite_unresolved) + `SafetyGateContext` + `SafetyGateOutcome`. 13-step ordered ladder; first failing gate wins. fix_* families bypass content-edit page-type gates. Cooldown gate consumes α₀a.2's `isInCooldown` and carries `cooldown_expires_at`. Accepted-ancestor gate computes `buildPromotionDedupeKey` per ancestor row and matches against the candidate. NO imports from `recommended-edits-persistence` or `recommendation-response-store`. (2) Five architecture invariants pinning: diagnostic-only never promotes · low-confidence never promotes · accepted ancestor suppresses · page-classifier-applied at promotion (content-edit skip + fix_* bypass) · no promotion without evidence. **Operator decisions honored**: U1 (2-way split α₀a.3a/α₀a.3b approved) · U2 (~915 estimate; landed at 961) · U3 (trim/split fallback in place but unused — first-pass under hard stop) · U4 (operator-review-only auto-suppressed as `diagnostic_only_tier`). **Auto-pass (8 existing α-family invariants)**: registry-active-set, customer-copy-vocab, no-queue-write, no-llm-decides, promotion-eligibility-pin, priority-score-contract, dedupe-key-formula, cooldown-windows. **Hard contracts honored**: NO writes to `recommended_edits` · NO imports from persistence stores · NO `runProviderAndPersist` · NO LLM · NO Supabase mutation · NO connector/external API call · NO migrations · NO cron / workflow changes · NO customer-facing route changes · NO `/diagnostics/recommendation-triggers` UI edits · NO Today / Changes / Prompts / Settings / Recommendations / Section 9 changes · NO env flag · NO `selectPromotableCandidates` · NO `MAX_ROWS_PER_PAGE` / `MAX_ROWS_PER_FAMILY` constants · NO `max_rows_per_*` suppression reasons (those belong to α₀a.3b's orchestrator). **Quality gates** (TO RUN): typecheck (clean ✅) · targeted suite (57 cases green ✅) · catalog-sync ✅ · full suite · `BEACON_TENANT_ID=tenant-ritz-founder npm run build`. **STOPPED AT READY_TO_COMMIT.** Proposed commit message: `feat(recommendations): add promotion safety gates`. **Recommended next**: operator review → commit + push (FF-safe to `origin/main`) + verify CI + Vercel. Then **Slice 4.5.D.α₀a.3b preflight** — `promote-to-queue.ts` orchestrator (`selectPromotableCandidates(...)` + caps at `MAX_ROWS_PER_PAGE = 5` + `MAX_ROWS_PER_FAMILY = 10`) + 2 cap invariants (`max-rows-per-page` + `max-rows-per-family`). Customer-queue writer remains deferred to α₁; operator-only Promotion Preview UI remains α₀b.

> 🟢 **SLICE 4.5.D.α₀a.2 — DEDUPE + COOLDOWN FOUNDATION (SHIPPED, commit `16a81f1`, 2026-05-20).** Second sub-slice of the de-scoped 4.5.D.α₀a chain. Ships the dedupe + cooldown primitives ONLY — no safety gates, no orchestrator, no UI, no writer. **1 NEW src module** + **1 NEW unit suite** + **2 NEW invariants** + **2 catalog rows**. **Adds**: (1) `dedupe-cooldown.ts` — `buildPromotionDedupeKey` (5-part sha1) · `buildPromotionCooldownKey` (3-part sha1) · `COOLDOWN_WINDOW_DAYS` (10-entry locked table) · `getCooldownWindowForStatus` · `promotionEditStatus` (legacy fallback) · `isInCooldown` (LATEST-EXPIRING binding across `recommendedEdits` + `dismissed` `recommendationResponses` with matching `targetPageUrl`). 4 local minimal anchor types — NO `recommended-edits-persistence` / `recommendation-response-store` imports. (2) Two architecture invariants pinning both sha1 formulas + the window table + `not_found_after_7d=0` + `verified_live_decay_refire` absent. **Operator-locked Q1–Q4 decisions honored**: Q1 = `not_found_after_7d` cooldown 0 days · Q2 = `verified_live_decay_refire` excluded (not a real status) · Q3 = only dismissed `recommendationResponses` honored (deferred/accepted ignored) · Q4 = approved ship at +928 lines (above ≤850 soft threshold, under +1,000 hard stop; explanation accepted: `isInCooldown` is the load-bearing primitive). **Auto-pass (6 existing α-family invariants)**: registry-active-set, customer-copy-vocab, no-queue-write, no-llm-decides, promotion-eligibility-pin, priority-score-contract. **Hard contracts honored**: NO writes to `recommended_edits` · NO imports from persistence stores · NO `runProviderAndPersist` · NO LLM · NO Supabase mutation · NO connector/external API call · NO migrations · NO cron / workflow changes · NO customer-facing route changes · NO `/diagnostics/recommendation-triggers` UI edits · NO Today / Changes / Prompts / Settings / Recommendations / Section 9 changes. **Quality gates** (TO RUN): typecheck (clean ✅) · targeted suite (47 cases green ✅) · catalog-sync ✅ · full suite · `BEACON_TENANT_ID=tenant-ritz-founder npm run build`. **STOPPED AT READY_TO_COMMIT.** Proposed commit message: `feat(recommendations): add promotion dedupe cooldown`. **Recommended next**: operator review → commit + push (FF-safe to `origin/main`) + verify CI + Vercel. Then **Slice 4.5.D.α₀a.3 preflight** — safety gates + `selectPromotableCandidates` orchestrator (11 sequenced gates + caps at MAX_ROWS_PER_PAGE=5 + MAX_ROWS_PER_FAMILY=10 + 7 paired invariants). Customer-queue writer remains deferred to α₁.

> 🟢 **SLICE 4.5.D.α₀a.1 — PROMOTION ELIGIBILITY + PRIORITY SCORING FOUNDATION (SHIPPED, commit `52d1ce6`, 2026-05-20).** **De-scoped from the rejected 4.5.D.α₀a attempt (which landed at +3,530 src+tests vs the +1,000 hard stop).** This slice ships ONLY the eligibility table + priority-score formula foundation — the smallest piece downstream slices can build against. **2 NEW pure source modules** + **2 NEW unit-test suites** + **2 NEW architecture invariants** + **2 catalog rows**. **Adds**: (1) `promotion-eligibility.ts` — locked 16-entry (trigger_signal, action_type) → tier table. `robots_blocks_ai_bots → fix_robots` operator correction honored. Off-site action types permanently blocked. Exposes `eligibilityForTrigger(...)` + `listCustomerQueueReadyPairs()`. (2) `priority-score.ts` — pure ranking `round((SEVERITY + INDEX_BLOCKER + PAGE_IMPORTANCE) * CONFIDENCE * PREREQ * SAFETY / EFFORT)`. SEVERITY (0–30), PAGE_IMPORTANCE (3–15), EFFORT (1.0–1.5) tables exported readonly. INDEX_BLOCKER +25 bonus iff `fix_*` AND confidence ≠ "low". **Architecture invariants (2 NEW)**: `recommendation-intelligence-promotion-eligibility-pin` (16-entry table pin including the α₃b correction) · `recommendation-intelligence-priority-score-contract` (3 rules: bonus + indexability-outranks-content + low-confidence-skips-bonus). Catalog sync: 2 new rows. **Auto-pass (4 existing α-family invariants)**: registry-active-set (no flips), customer-copy-vocab (no new templates), no-queue-write (no `recommended-edits-persistence` import), no-llm-decides (no `generatorActive` flips). **Deferred** (NOT in this slice): dedupe-cooldown.ts (→ α₀a.2) · safety-gates.ts (→ α₀a.3) · promote-to-queue.ts (→ α₀a.3) · the 9 remaining promotion invariants (→ α₀a.2 + α₀a.3) · operator-only Promotion Preview UI (→ α₀b) · customer-queue writer (→ α₁). **Hard contracts honored**: NO writes to `recommended_edits` · NO imports from `recommended-edits-persistence` · NO `runProviderAndPersist` · NO LLM · NO Supabase mutation · NO connector/external API call · NO migrations · NO cron / workflow changes · NO customer-facing route changes · NO `/diagnostics/recommendation-triggers` UI edits · NO Today / Changes / Prompts / Settings / Recommendations / Section 9 changes. **Net src+tests delta**: **+725 lines** (within +700 target with 25-line cushion; under +1,000 hard stop). **Quality gates** (TO RUN): typecheck (clean ✅) · targeted suite (74 cases green ✅) · catalog-sync ✅ · full suite · `BEACON_TENANT_ID=tenant-ritz-founder npm run build`. **STOPPED AT READY_TO_COMMIT.** Proposed commit message: `feat(recommendations): add promotion eligibility foundation`. **Recommended next**: operator review → commit + push (FF-safe to origin/main) + verify CI + Vercel. Then **Slice 4.5.D.α₀a.2 preflight** — dedupe + cooldown foundation (5-part promotion dedupe key + 3-part cooldown key + locked window table + `isInCooldown` predicate + 2 paired architecture invariants: `dedupe-key-formula` + `cooldown-windows`).

> 🔴 **SLICE 4.5.D.α₀a — DE-SCOPED AND REJECTED (2026-05-20).** First-attempt promotion decision engine landed at +3,530 src+tests vs the +1,000 hard stop. Operator rejected the as-is commit; green tests do not override the ceiling because 4.5.D bridges diagnostic intelligence toward customer-visible recommendations. The work is REPLACED by the four-sub-slice plan above (α₀a.1 → α₀a.2 → α₀a.3 → α₀b → α₁). No code from the rejected attempt was committed or pushed.

> 🟢 **SLICE 4.5.C.α₃b — MISSING-SCHEMA TRIGGER PREDICATE + `add_schema` FLIP (SHIPPED, commit `968f8f5`, 2026-05-20).** Second sub-slice of α₃ — closes Section 4.5.C with the structured-data family. **1 new pure per-snapshot predicate** (`missing-schema`) + **1 `generatorActive` flip** (`add_schema`) + **1 new customer-copy template** (`addSchemaCopy()`). **Customer queue UNCHANGED.** **Adds**: (1) `missing-schema` predicate reuses the existing pure `diffSchemaCoverage()` + `EXPECTED_SCHEMA_BY_ASSET_TYPE` substrate. Inline PageType → AssetType mapping (does NOT use Ritz-tuned `classifyAssetType()`). Three safety guards: page-type allowlist (homepage/city/service/project/hub; skip utility/other/technical_asset); skip `extraction_certainty="uncertain"`; fire only when `!coverage.satisfies_all_required` (recommended-only gaps are evidence-only). Emits `add_schema` at `confidence: "low"` → diagnostic_only routing (Tier-2 sensitive: existing expectation map is local-service-tuned). One candidate per page (matches existing findings-pipeline shape). (2) `addSchemaCopy()` operator-locked template. (3) Registry: 1 flip (`add_schema` → active). (4) Loader: PREDICATE_COUNT 14→15; missing-schema invoked in per-snapshot loop. **Architecture invariants (2 updated, 0 new)**: `recommendation-registry-active-set` `LOCKED_ACTIVE_SET` 11→12; `recommendation-intelligence-customer-copy-vocab` `PROBE_SETS` extended. 5 other α-family invariants auto-pass. **Tests added (~18 new + mechanical updates)**: predicate test (~14 cases incl. oneOf semantics, extraction_certainty guard, recommended-only no-fire); loader test +3; customer-copy-templates +2; diagnostic page test +4; ONE pre-existing diagnostic-page test fixture updated to populate `schema_types` so the empty-bucket-suppression assertion survives missing_schema becoming active; pinning tests bumped. **Hard contracts honored**: NO LLM · NO paid API · NO GSC · NO Supabase mutation · NO migration · NO cron · NO customer queue write · NO Today / Changes / Prompts / Settings / Section 9 changes · NO new BusinessConfig fields · NO per-industry schema map · NO classifyAssetType usage · NO per-missing-type emission · NO main-candidates promotion. **Quality gates** (TO RUN): typecheck · targeted suite · full suite · build (`BEACON_TENANT_ID=tenant-ritz-founder`). **STOPPED AT READY_TO_COMMIT.** Net src/ delta: +209 lines (well under +300 target; far under +700 hard stop). **Recommended next**: operator review → commit + push + verify CI + Vercel + manual smoke (`/diagnostics/recommendation-triggers` with `BEACON_OPERATOR_MODE=true`; confirm `Predicates run: 15`; confirm any `missing_schema` rows appear ONLY in the diagnostic-only section, only on homepage/city/service/project/hub URLs, only with extraction_certainty=confirmed). Then **4.5.C closeout** (declare the indexability + internal-linking + schema family complete; all 5 indexability remediation types + `add_internal_link` + `add_schema` are now active). Then **Slice 4.5.D preflight** — customer-queue promotion (the diagnostic-only validation surface is complete; operator decides which low/medium-confidence rows promote per per-tenant calibration). Full detail: [`docs/VERIFICATION_LOG.md`](VERIFICATION_LOG.md#2026-05-20--slice-45c%CE%B1%E2%82%83b-missing-schema-trigger-predicate).

> 🟢 **SLICE 4.5.C.α₃a — ORPHAN-PAGE CROSS-SNAPSHOT TRIGGER PREDICATE (SHIPPED, commit `13a8c4e`, 2026-05-20).** First sub-slice of α₃ — activates the internal-linking family. **1 new pure cross-snapshot predicate** (`orphan-page`) + **1 `generatorActive` flip** (`add_internal_link`) + **1 new customer-copy template** (`addInternalLinkCopy()`). **Customer queue UNCHANGED.** **Adds**: (1) `orphan-page` predicate — runs ONCE before the per-snapshot loop (same pattern as α₂ duplicate-title/meta). Aggregates each snapshot's `internal_links[]` array; resolves relative hrefs via `new URL(href, snap.url)`; canonicalizes via `canonicalizeCitationUrl`; builds `Map<canonicalUrl, Set<sourceCanonical>>` (self-links excluded). Fires on snapshots with 0 inbound owned-page links AND page-type ∈ {homepage, city, service, project, hub}. Skips utility/other/technical_asset. **Global emptiness guard**: when `totalResolvedInbound === 0`, suppresses ALL emissions (data unavailable). Emits `add_internal_link` at `confidence: "medium"` → main candidates section. (2) `addInternalLinkCopy()` operator-locked template. (3) Registry: 1 flip (`add_internal_link` → active). `add_schema` stays inactive (α₃b). (4) Loader: PREDICATE_COUNT 13→14; `orphanPage` invoked alongside existing cross-snapshot duplicate predicates. **Architecture invariants (2 updated, 0 new)**: `recommendation-registry-active-set` `LOCKED_ACTIVE_SET` 10→11; `recommendation-intelligence-customer-copy-vocab` `PROBE_SETS` extended. 5 other α-family invariants auto-pass. **Tests added (~14 new + mechanical updates)**: predicate test (~12 cases including global emptiness guard, canonicalization, cross-snapshot aggregation, self-link exclusion); loader test +3 (orphan wiring + cross-snapshot-once + global-emptiness suppression); customer-copy-templates +2; pinning tests bumped. **Hard contracts honored**: NO LLM · NO paid API · NO GSC · NO Supabase mutation · NO migration · NO cron · NO customer queue write · NO Today / Changes / Prompts / Settings / Section 9 changes · NO `add_schema` activation · NO missing_schema predicate · NO tenant-specific slugs. **Quality gates** (TO RUN): typecheck · targeted suite · full suite · build (`BEACON_TENANT_ID=tenant-ritz-founder`). **STOPPED AT READY_TO_COMMIT.** Net src/ delta: +216 lines (well under +400 target; far under +700 hard stop). **Recommended next**: operator review → commit + push + verify CI + Vercel + manual smoke (`/diagnostics/recommendation-triggers` with `BEACON_OPERATOR_MODE=true`; confirm `Predicates run: 14`; confirm any `orphan_page` rows appear in main candidates section, only on homepage/city/service/project/hub URLs). Then **Slice 4.5.C.α₃b preflight** — missing-schema predicate + `add_schema` flip. Full detail: [`docs/VERIFICATION_LOG.md`](VERIFICATION_LOG.md#2026-05-20--slice-45c%CE%B1%E2%82%83a-orphan-page-cross-snapshot-predicate).

> 🟢 **SLICE 4.5.C.α₂ — TIER-2 SENSITIVE INDEXABILITY PREDICATES + DIAGNOSTIC-ONLY BUCKET RENDER (SHIPPED, commit `a7d8c44`, 2026-05-20).** Second α₂ slice — activates the sensitive indexability remediation family. **2 new pure Tier-2 trigger predicates** emitting at `confidence: "low"` (route to `result.diagnostic_only` via `applyQueueRules`); **1 `generatorActive` flip** (`fix_noindex`); **NEW diagnostic page section** rendering `result.diagnostic_only` so the low-confidence rows are operator-visible. **Customer queue UNCHANGED.** **Adds**: (1) `noindex-on-indexable-page` predicate — fires on `noindex_meta` verdict with 3 safety guards (page-type allowlist homepage/city/service/project; skip `extraction_certainty="uncertain"`; skip `has_canonical_mismatch=true`). Emits `fix_noindex`. (2) `robots-blocks-ai-bots` predicate — fires on `blocked_by_robots_for_ai` verdict; per-bot blocked-count + boolean evidence; allowlist homepage/city/service/project/hub. Emits the already-active `fix_robots` (no new flip). (3) Registry: 1 flip (`fix_noindex` → active). (4) Loader extension: `PREDICATE_COUNT` 11→13; per-snapshot block invokes both new predicates with the same batch-map verdict; existing `indexability_unavailable` soft-fail carries over. (5) Diagnostic page: new `<DiagnosticOnlySection>` component + `data-counter="diagnostic_only_count"` line; rendered only when `result.diagnostic_only.length > 0`. **Architecture invariants (2 updated, 0 new)**: `recommendation-registry-active-set` `LOCKED_ACTIVE_SET` 9 → 10; `LOCKED_REGISTRY_COUNT` unchanged at 37. `recommendation-triggers-diagnostic-source-and-copy` — extended with 2 positive assertions pinning the new diagnostic-only render contract (data attributes + header copy + counter). 5 other α-family invariants auto-pass. **Tests added (~45 new + mechanical updates)**: 2 predicate test files; loader test +4; diagnostic page test +6 (incl. safety-guard regression guards); pinning tests bumped (action-types, specific-edit-evidence, predicates_run counters 11→13). **Hard contracts honored**: NO LLM · NO paid API · NO GSC fetch · NO Supabase mutation · NO migration · NO cron · NO customer queue write · NO `recommended_edits` mutation · NO Today / Changes / Prompts / Settings / Section 9 changes · NO new copy templates · NO main-candidates promotion · NO `add_internal_link`/`add_schema` activation. **Quality gates** (TO RUN): typecheck · targeted suite · full suite · build (`BEACON_TENANT_ID=tenant-ritz-founder`). **STOPPED AT READY_TO_COMMIT.** Net src/ delta: +401 lines (well under +500 target; far under +700 hard stop). **Recommended next**: operator review → commit + push + verify CI + Vercel + manual smoke (`/diagnostics/recommendation-triggers` with `BEACON_OPERATOR_MODE=true`; confirm `Predicates run: 13`; confirm the new "Diagnostic-only signals (low-confidence)" section appears when any Tier-2 rows fire). Then **Slice 4.5.C.α₃ preflight** — internal linking + schema family (`add_internal_link` + `add_schema` activation). Full detail: [`docs/VERIFICATION_LOG.md`](VERIFICATION_LOG.md#2026-05-20--slice-45c%CE%B1%E2%82%82-tier-2-sensitive-indexability-predicates).

> 🟢 **SLICE 4.5.C.α₁ — TIER-1 INDEXABILITY TRIGGER PREDICATES (SHIPPED, commit `aa85c7d`, 2026-05-20).** First α₁ slice of Section 4.5.C — activates the indexability remediation family. **4 new pure deterministic trigger predicates** over Section 4's `owned_url_indexability` verdict substrate, **4 `generatorActive` flips** (active set 5 → 9), **new `loadIndexabilityBatchForTenant` helper** (ONE substrate pass per render). **Adds**: (1) `loadIndexabilityBatchForTenant` (new exported helper in indexability domain) — loads sitemap reconciliation + robots state + page snapshots + business config once, synthesizes per-URL verdicts via pure `computeIndexability`, returns `Map<canonicalUrl, OwnedUrlIndexability>`. (2) 4 new pure trigger predicates: `sitemap-missing` / `robots-blocks-googlebot` / `bad-http-status` / `canonical-mismatch`. Each consults the page-classifier for applicability, emits a candidate keyed on a single locked indexability verdict, returns the row with the operator-locked customer copy from 4.5.C.α₀. (3) Registry: 4 flips (`fix_sitemap`, `fix_robots`, `fix_status_code`, `fix_canonical` → active); `fix_noindex` stays inactive (α₂). (4) Loader extension: `PREDICATE_COUNT` 7→11; new `indexability_unavailable` soft-fail status; per-snapshot Tier-1 predicate invocations with canonicalizer-keyed batch-map lookup. (5) `load-indexability.ts` exports 5 private helpers (`normalizeHost`, `nullRobotsFlags`, `computeRobotsAgeDays`, `buildSitemapSignal`, `buildRobotsSignal`) for batch-helper reuse — no behavior change. **Architecture invariants (1 updated, 0 new)**: `recommendation-registry-active-set` `LOCKED_ACTIVE_SET` 5 → 9; `LOCKED_REGISTRY_COUNT` unchanged at 37. 5 other α-family invariants auto-pass (purity / no-queue-write / no-llm-decides / diagnostic-source / page-classifier-applied). **Tests added (~50 new + multiple mechanical updates)**: 4 predicate test files; loader test +6 (4 wiring + soft-fail + missing-batch-entry); diagnostic page test +5 (4 render cases + partial-result render); pinning tests bumped (action-types, specific-edit-evidence, predicates_run counters 7→11). **Hard contracts honored**: NO LLM · NO paid API · NO GSC fetch · NO Supabase mutation · NO migration · NO cron · NO customer queue write · NO `recommended_edits` mutation · NO Today / Changes / Prompts / Settings / Section 9 changes · NO `fix_noindex` activation · NO `add_internal_link`/`add_schema` activation · NO new action types. **Quality gates** (TO RUN): typecheck · targeted suite · full suite · build (`BEACON_TENANT_ID=tenant-ritz-founder`). **STOPPED AT READY_TO_COMMIT.** Net src/ delta: +688 lines (under +700 hard stop with 12-line cushion). **Recommended next**: operator review → commit + push + verify CI + Vercel + manual smoke (`/diagnostics/recommendation-triggers` with `BEACON_OPERATOR_MODE=true`; confirm `Predicates run: 11`; confirm any of the 4 new trigger signals if Ritz has any indexability findings). Then **Slice 4.5.C.α₂ preflight** — Tier-2 sensitive predicates (`noindex_on_indexable_page` + AI-bot robots variant) + activate `fix_noindex`. Full detail: [`docs/VERIFICATION_LOG.md`](VERIFICATION_LOG.md#2026-05-20--slice-45c%CE%B1%E2%82%81-tier-1-indexability-trigger-predicates).

> 🟢 **SLICE 4.5.C.α₀ — INDEXABILITY REMEDIATION REGISTRY FOUNDATION (SHIPPED, commit `bcba874`, 2026-05-19).** First slice of Section 4.5.C — opens the indexability remediation family with registry + customer-copy templates only. **Scope-locked**: NO predicates, NO `generatorActive` flips, NO loader changes, NO diagnostic page changes. **Adds**: (1) Registry expanded 32→37 with 5 new inactive indexability-remediation types (`fix_sitemap` / `fix_robots` / `fix_noindex` / `fix_status_code` / `fix_canonical`). All carry `signalType: "technical"` per O4 + `elementTypeDomain: []` (configuration-surface, not on-page element) + `requiresProposedText: false` (operator-task rows; no LLM drafts a robots.txt rule). (2) `recommendation-action-rows.ts` exhaustiveness arms for both `actionRowTypeForEdit()` (routes all 5 to `review_decision` to keep Act 4 Suggested Copy suppressed) and `composeEditRowTitle()` (dead code; `generatorActive: false` guarantees no runtime path reaches them). (3) 5 new no-arg customer-copy templates with operator-locked phrasing: `fixSitemapCopy()` / `fixRobotsCopy()` / `fixNoindexCopy()` / `fixStatusCodeCopy()` / `fixCanonicalCopy()`. **Architecture invariants (2 updated, 0 new)**: `recommendation-registry-active-set` (`LOCKED_REGISTRY_COUNT` 32→37; `LOCKED_ACTIVE_SET` unchanged at 5); `recommendation-intelligence-customer-copy-vocab` (`PROBE_SETS` extended with 5 new no-arg probes). The 5 other α-family invariants auto-pass — no new predicates, no diagnostic-page edits. **Tests updated (~10 new + 4 fixture updates)**: customer-copy-templates.test.ts +5 phrasing + purity; action-types.test.ts (registry count 32→37; spelled-out list adds 5; inactive count 27→32; `ZERO_ELEMENT_DOMAIN_ALLOWLIST` extended). **Hard contracts honored**: NO predicates · NO `generatorActive` flips · NO loader changes · NO diagnostic page changes · PREDICATE_COUNT stays 7 · all 7 trigger signals unchanged · NO customer queue write · NO `recommended_edits` mutation · NO LLM · NO paid API · NO Supabase mutation · NO migration · NO cron · NO Today / Changes / Prompts / Settings / Section 9 code changes · NO GSC / CallRail / GBP insights · NO new BusinessConfig fields. **Quality gates** (TO RUN): typecheck · targeted suite · full suite · build (`BEACON_TENANT_ID=tenant-ritz-founder`). **STOPPED AT READY_TO_COMMIT.** Net src/ delta: under +200 target (well under +400 hard stop). **Recommended next**: operator review → commit + push + verify CI + Vercel. **Next slice**: Slice 4.5.C.α₁ — Tier-1 indexability fix predicates (`sitemap_missing`, `robots_blocks_googlebot`, `bad_http_status`, `canonical_mismatch`) + activate 4 of the 5 new types. Full detail: [`docs/VERIFICATION_LOG.md`](VERIFICATION_LOG.md#2026-05-19--slice-45c%CE%B1%E2%82%80-indexability-remediation-registry-foundation).

> 🟢 **SLICE 4.5.B.α₂.2 — PAGE-INTELLIGENCE + TRIGGER APPLICABILITY LAYER (SHIPPED, commit `8f17e6e`, 2026-05-19).** Trigger-quality fix surfaced by operator manual production smoke after α₂.1. Production showed `Owned snapshots: 36` (α₂.1 reliability fix worked) + 26 candidates, mostly noisy: `/llms.txt` fired missing_*; utility/hub/project pages fired title_h1_mismatch; `/locations` hub fired weak_h1. **Operator-locked principle**: pattern-based + config-driven, NOT tenant-specific slug-hardcoded. **Adds**: (1) NEW `src/domains/recommendation-intelligence/page-classifier.ts` — universal page-intelligence layer with 8 page types (`homepage` / `city` / `service` / `project` / `hub` / `utility` / `technical_asset` / `other`); segment-bounded prefix matching fixes the α₁ `/locations`-hub false-positive; 50+ generic English utility-segment patterns; 10 universal hub names; 22 non-HTML extensions. (2) ALL 7 predicates apply the classifier: missing_* + duplicate_* skip technical_asset; weak_h1 city+service only; title_h1_mismatch homepage+city+service only. (3) `weak-h1.ts` retires the inline `classifyForGate` (regression-guarded). (4) Loader passes `businessConfig` to titleH1Mismatch (new signature). (5) NEW architecture invariant `recommendation-triggers-page-classifier-applied` (17 tests, 5 rules) pinning the philosophy. Catalog updated. **Tests added (~95 new + many updated)**. **Hard contracts honored**: NO registry changes · NO new predicates · NO `generatorActive` flips · NO customer queue write · NO LLM · NO paid API · NO Supabase mutation · NO migration · NO cron · NO new BusinessConfig fields · NO hardcoded tenant slugs · PREDICATE_COUNT stays 7 · all 7 trigger signals unchanged. **Quality gates**: typecheck CLEAN · targeted suite green (304/304) · full suite green · build green. **STOPPED AT READY_TO_COMMIT.** Net src/ delta: +200 (under +250 target). **Expected production cleanup**: 26 noisy candidates → 0–7 genuine ones (3× `/llms.txt` + 7× utility title_h1_mismatch + 6× project title_h1_mismatch + 2× hub title_h1_mismatch + 1× `/locations` weak_h1 all eliminated). **Recommended next**: operator review → commit + push + verify CI + Vercel + manual smoke on `/diagnostics/recommendation-triggers` to confirm dramatic cleanup. **THEN Slice 4.5.C preflight only if the remaining candidates look genuinely useful.** Full detail: [`docs/VERIFICATION_LOG.md`](VERIFICATION_LOG.md#2026-05-19--slice-45b%CE%B1%E2%82%82-2-page-intelligence--trigger-applicability-layer).

> 🟢 **SLICE 4.5.B.α₂.1 — DIAGNOSTIC RELIABILITY FIX (SHIPPED, commit `b8731c7`, 2026-05-19).** Tightly-scoped fix surfaced by operator production smoke after α₂. **Root cause**: α₀ loader used the file-only `@/domains/pages/snapshot-store::getPageSnapshots()` boundary which reads `.data/page-snapshots.json` — never deployed to Vercel. Production showed misleading "Owned snapshots: 0 / No candidate rows produced" + stale "Slice 4.5.B.α₀ / 2 metadata" copy. **Adds**: (1) Loader swapped to repository pattern (`getRepository().forTenant(tenantId).getPageSnapshots()`, routes to Supabase under `DATA_SOURCE=supabase`, matching customer Recommendations pipeline). (2) Page description slice-agnostic; interpolates `result.meta.predicates_run`. (3) Empty-state branches distinguish `snapshot_count=0` (new `empty-snapshots` warning banner) from `candidate_count=0` (existing empty-candidates copy). (4) NEW architecture invariant `recommendation-triggers-diagnostic-source-and-copy` (6 tests) pinning loader source + page copy against regression. Catalog updated. **Tests updated (~10 new + 2 fixture rewrites)**. **Hard contracts honored**: NO registry changes · NO new predicates · NO `generatorActive` flips · NO customer queue write · NO LLM · NO paid API · NO Supabase mutation · NO migration · NO cron · NO Today / Changes / Prompts / Settings / Recommendations / Section 9 code changes. **Quality gates**: typecheck CLEAN · targeted suite green · full suite green · build green. **STOPPED AT READY_TO_COMMIT.** Net src/ delta: +35 (well under +200 target). **Recommended next**: operator review → commit + push + verify CI + Vercel + manual smoke (visit `/diagnostics/recommendation-triggers` with `BEACON_OPERATOR_MODE=true`; confirm description says "7 active"; confirm `Owned snapshots > 0` in production; confirm empty-snapshots banner only when env genuinely lacks snapshots). Then **Slice 4.5.C preflight** (indexability + GSC family). Full detail: [`docs/VERIFICATION_LOG.md`](VERIFICATION_LOG.md#2026-05-19--slice-45b%CE%B1%E2%82%821-diagnostic-reliability-fix).

> 🟢 **SLICE 4.5.B.α₂ — CROSS-SNAPSHOT DUPLICATE-TITLE + DUPLICATE-META PREDICATES (SHIPPED, commit `ec4e1ca`, 2026-05-19).** Third activation; operator-validation surface only — customer queue UNCHANGED. **Adds**: (1) NEW `duplicate-title.ts` — cross-snapshot aggregation predicate (runs ONCE over the full tenant list before per-snapshot loop). Normalizes via trim + lowercase + collapse-whitespace; groups by normalized title; emits `edit_title` candidates per URL past alphabetical canonical anchor (group size N → N-1 candidates). (2) NEW `duplicate-meta.ts` — symmetric over `meta_description`. (3) Extended `customer-copy-templates.ts` with 2 count-aware operator-locked phrasings (using `String()` concatenation, NOT `${expression}` literals). (4) Extended loader: `PREDICATE_COUNT` 5 → 7; cross-snapshot predicates invoked before per-snapshot loop. **NO registry flips. NO new action types. NO new architecture invariants.** 1 invariant probe-set extension (`recommendation-intelligence-customer-copy-vocab` gains integer-arg probes for the new templates). **Tests added (~42 new + 3 fixture updates to pre-existing assertions)**. **Hard contracts honored**: NO registry changes · NO `generatorActive` flips · NO customer queue write · NO `recommended_edits` mutation · NO `runProviderAndPersist` · NO LLM · NO paid API · NO Supabase mutation · NO migration · NO cron · NO Today / Changes / Prompts / Settings / Section 9 code changes · NO activation of `add_answer_block` / `add_internal_link` / `add_schema`. **Quality gates**: typecheck CLEAN · targeted suite green · full suite green · build green. **STOPPED AT READY_TO_COMMIT.** Net src/ delta: +185 (well under +400 target). **Recommended next**: operator review → commit + push + verify CI + Vercel + manual smoke (`/diagnostics/recommendation-triggers` with `BEACON_OPERATOR_MODE=true`; confirm `duplicate_title` / `duplicate_meta` trigger_signal values appear in the candidates table when Ritz has any title/meta duplicates; confirm `Predicates run: 7` in counters strip). Then **Slice 4.5.C** — indexability + GSC family. **CallRail STAYS DEFERRED.** Full detail: [`docs/VERIFICATION_LOG.md`](VERIFICATION_LOG.md#2026-05-19--slice-45b%CE%B1%E2%82%82-cross-snapshot-duplicate-title--duplicate-meta-predicates).

> 🟢 **SLICE 4.5.B.α₁ — H1 PREDICATE FAMILY + `change_h1` ACTIVATION (SHIPPED, commit `5c653aa`, 2026-05-19).** Second activation; operator-validation surface only — customer queue UNCHANGED. **Adds**: (1) Flipped `change_h1` `generatorActive: false → true` (paired with 3 new H1 predicates). (2) NEW `missing-h1.ts` — pure predicate; fires on null/empty/whitespace H1. (3) NEW `weak-h1.ts` — **page-type-gated** pure predicate (city + service only, skips homepage / project / other). Inlines a minimal `classifyForGate` mirroring section-analyzer's `inferPageType`. Lowercase substring scan against `businessConfig.locationTerms` / `serviceTerms` (with `.locations` / `.services` fallback). (4) NEW `title-h1-mismatch.ts` — pure predicate; stopword-aware Jaccard ≥ 0.3 threshold; below threshold emits PAIRED `edit_title` + `change_h1` candidates with distinct dedupe_keys. (5) Extended `customer-copy-templates.ts` with 3 operator-locked templates. (6) Extended loader: `PREDICATE_COUNT` 2 → 5; new `getBusinessConfig` resolution; new `status: "config_unavailable"` soft-fail. **Architecture invariants (2 updated, 0 new)**: `recommendation-registry-active-set` (active set 4 → 5: adds `change_h1`); `recommendation-intelligence-customer-copy-vocab` (PROBE_SETS extended with 3 new templates). The 4 other α₀ invariants auto-extend coverage to the new predicate files. **Tests added (~50 across 6 files)**. **Hard contracts honored**: NO customer queue write · NO `recommended_edits` mutation · NO `runProviderAndPersist` · NO LLM · NO paid API · NO Supabase mutation · NO migration · NO cron · NO Today / Changes / Prompts / Settings / Section 9 code changes · NO activation of `add_answer_block` / `add_internal_link` / `add_schema` · NO α₂ duplicate predicates. **Quality gates**: typecheck CLEAN · targeted suite green (132/132) · full suite green · build green. **STOPPED AT READY_TO_COMMIT.** Net src/ delta: +397 (under +500 target). **Recommended next**: operator review → commit + push + verify CI + Vercel + manual smoke (`/diagnostics/recommendation-triggers` with `BEACON_OPERATOR_MODE=true`; confirm `missing_h1` / `weak_h1` / `title_h1_mismatch` trigger_signal values appear in the candidates table on populated Ritz snapshots). Then **Slice 4.5.B.α₂** (cross-snapshot duplicate-title + duplicate-meta predicates; no new flips). Then **Slice 4.5.C** (indexability + GSC family). **CallRail STAYS DEFERRED.** Full detail: [`docs/VERIFICATION_LOG.md`](VERIFICATION_LOG.md#2026-05-19--slice-45b%CE%B1%E2%82%81-h1-predicate-family--change_h1-activation).

> 🟢 **SLICE 4.5.B.α₀ — FIRST ACTIVATION: METADATA TRIGGER DIAGNOSTICS FOUNDATION (SHIPPED, commit `454bd93`, 2026-05-19).** Operator-validation surface only — customer queue UNCHANGED. **Adds**: (1) Registry: 3 inactive types (`update_intro`, `add_h3_section`, `add_image_alt_text`) + 1 flip (`edit_meta`); active set becomes 4: `edit_title` / `edit_meta` / `add_h2_section` / `add_faq`. (2) `action-rows.ts` exhaustiveness arms (dead code; `generatorActive: false` guarantees no runtime path reaches them). (3) Emitter scaffold: `candidate-row.ts` type + `dedupe-key.ts` + `cooldown-key.ts` + minimal `apply-queue-rules.ts` (4 of 10 rules). (4) Customer-copy templates: `missingTitleCopy()` + `missingMetaCopy()`. (5) Trigger predicates: `missing-title` + `missing-meta` (pure functions over `PageSnapshot`, no I/O). (6) Loader: `load-trigger-candidates-for-tenant` (reads file-backed snapshots, tenant-scoped, soft-fails). (7) Operator-only `/diagnostics/recommendation-triggers` page (gated, no write paths). **Architecture invariants (4 new + 1 updated)**: `recommendation-registry-active-set` (UPDATED count 29→32, active 3→4); `recommendation-trigger-predicates-purity` (NEW); `recommendation-intelligence-no-queue-write` (NEW); `recommendation-intelligence-no-llm-decides` (NEW); `recommendation-intelligence-customer-copy-vocab` (NEW). Catalog updated with 4 new rows + 1 row update. **Tests added (~85 across 12 files)**: emitter (7+5+9=21) + predicates (9+6=15) + customer-copy templates (3) + loader (10) + diagnostic page (8) + architecture invariants (28). **Hard contracts honored**: NO customer queue write · NO `recommended_edits` mutation · NO LLM · NO paid API · NO Supabase mutation · NO migration · NO cron · NO customer surface changes outside the new operator-only diagnostic page · NO Section 9 changes. **Quality gates**: typecheck CLEAN · targeted (85/85) + full suite + build all green. **STOPPED AT READY_TO_COMMIT.** Net src/ delta: +678 (under +700 hard stop). **Recommended next**: operator review → commit + push + verify CI + Vercel + manual smoke (visit `/diagnostics/recommendation-triggers` with `BEACON_OPERATOR_MODE=true`; confirm operator gate + per-row data attributes + customer-vocab cleanliness). Then **Slice 4.5.B.α₁** (H1 predicate family: `missing-h1` + `weak-h1` + `title-h1-mismatch`; flips `change_h1`). Then **4.5.B.α₂** (cross-snapshot duplicate predicates). Then **4.5.C** (indexability + GSC family). **CallRail STAYS DEFERRED.** Full detail: [`docs/VERIFICATION_LOG.md`](VERIFICATION_LOG.md#2026-05-19--slice-45b%CE%B1%E2%82%80-first-activation-metadata-trigger-diagnostics-foundation).

> 🟢 **SLICE 4.5.A — RECOMMENDATION INTELLIGENCE EXPANSION: REGISTRY AUDIT + ACTIVATION PLAN (SHIPPED, commit `5238de6`, 2026-05-19).** First slice of Section 4.5 (Recommendation Intelligence Expansion). **Docs + tests only — zero source changes.** Sets the canonical sequencing record for the 4.5.A → 4.5.G slice progression that converts Beacon from "3 active deterministic generators" → "ranked deterministic + LLM-assisted recommendation engine across 18 families." O1–O12 operator decisions (locked 2026-05-18) gate the entire sequence. **Adds**: (1) NEW `docs/RECOMMENDATION_INTELLIGENCE_AUDIT.md` (~470 lines, sections A–M) — canonical audit pinning current 29-type registry + 3-active generator set + 19 LLM-reachable inactive + 7 off-site locked-inactive + 23 proposed new action types mapped to slices 4.5.B–4.5.G + 18-family mapping table + deterministic/LLM-assisted/human-task split + trigger signal sources + queue-vs-diagnostics rules + priority formula + 21 locked architecture invariants + recommended next slice + stop conditions. (2) NEW `tests/architecture/recommendation-registry-active-set.test.ts` — pins (a) `ACTION_TYPES.length === 29`; (b) exact 3-active generator set `{edit_title, add_h2_section, add_faq}` + every other type binary-false; (c) audit doc presence + canonical references to locked active set + count + slice sequence + off-site types. (3) MODIFIED `docs/ARCHITECTURE_INVARIANTS_CATALOG.md` — new "Section 4.5 / Slice 4.5.A" section with one catalog row; retirement-condition documents per-slice update cadence. **Hard contracts honored**: NO source changes (registry byte-unchanged) · NO `generatorActive` flips · NO new trigger predicates · NO recommendation generator modules · NO customer surface changes · NO Section 9 code changes · NO LLM · NO migrations · NO cron · stale top-of-module docstring on `action-types.ts` documented in audit doc but NOT fixed this slice (deferred to 4.5.B). **Decisions referenced (locked 2026-05-18)**: O1–O12. **Quality gates**: typecheck CLEAN · targeted invariant green · catalog-sync green · full suite green · build green. **STOPPED AT READY_TO_COMMIT.** Proposed commit message: `docs(recommendations): audit intelligence expansion plan`. **Recommended next**: operator review → commit + push + verify CI + Vercel green (no runtime behavior changes; smoke is "render the audit doc"). Then **Slice 4.5.B** — metadata + H1 + new-page deterministic triggers (5 existing types flip generatorActive: `edit_meta` / `change_h1` / `add_answer_block` / `add_internal_link` / `add_schema`; 3 new types added: `update_intro` / `add_h3_section` / `add_image_alt_text`; new operator-only `/diagnostics/recommendation-triggers` page surfaces candidate rows BEFORE customer-queue flip per O12 contract; first 9 deterministic trigger predicates ship; customer queue still untouched until Slice 4.5.D). Full detail: [`docs/VERIFICATION_LOG.md`](VERIFICATION_LOG.md#2026-05-19--slice-45a-recommendation-intelligence-expansion-registry-audit--activation-plan).

> 🟡 **SECTION 9 TODAY TILE — "EDIT OUTCOMES (PAST 30 DAYS)" (READY_TO_COMMIT, 2026-05-19).** Second + final Section-9 customer-facing surface. Closes the surface set per Section 9.5 locked design. **Adds**: (1) NEW `src/domains/outcome-attribution/load-outcomes-summary-for-tenant.ts` — tenant-scoped aggregator. Verified-live edits within last 30 days; URL-scoped Supabase SELECT (mirrors 9.A2β.1); per-edit Mode A iteration with `qualifiedCallCount=0`; aggregates to `OutcomesSummary` discriminator (`status` + counts + sessions). 6h `unstable_cache` with today-UTC-date in key + `recommended_edits:${tenantId}` tag. Soft-fails to `data_unavailable` on every documented failure path. (2) NEW `src/components/today/edit-outcomes-tile.tsx` — pure presentational React; type-only summary import; state machine (empty / still_gathering / healthy); plural-aware; CallRail K2-deferred calls clause source-present but runtime-suppressed; locked Section 9.5 customer copy. (3) MODIFIED `src/app/(shell)/today-v2-sections.tsx` — new `TodayV2EditOutcomesSection`. (4) MODIFIED `src/app/(shell)/today-v2-skeleton.tsx` — new `TodayV2EditOutcomesSkeleton`. (5) MODIFIED `src/app/(shell)/page.tsx` — new Suspense between Edit lifecycle and Descriptors. **NEW architecture invariants (3)**: `edit-outcomes-tile-vocab` (15 forbidden tokens incl. connector names) · `edit-outcomes-tile-suppression` (state machine + data attribute pins) · `edit-outcomes-loader-no-ga4-api` (per-file no-GA4-API contract across loader + sections). Catalog updated. **Tests added (~110 across 5 files)**: 28 tile + 23 loader + 24 vocab + 8 suppression + 23 no-ga4-api. **Hard contracts honored**: NO change to Mode A compute · NO change to canonicalize-url · NO change to GA4 substrate · NO change to refresh action or operator diagnostic · NO change to Changes detail wiring · NO change to today-v2-data.ts · NO migration · NO cron · NO LLM · NO CallRail enablement (calls clause source-present but runtime-suppressed) · NO legacy-row cleanup · NO causal/revenue/connector-name vocab leaks. **Quality gates**: typecheck CLEAN · targeted + full suite green · build green. **STOPPED AT READY_TO_COMMIT.** Net src/ delta within +500 ceiling (see report). **Recommended next**: operator review → commit + push + verify CI + Vercel + manual smoke (hard reload `/?v2=1`; confirm "Edit outcomes (past 30 days)" tile appears between Edit lifecycle and Descriptors with locked Section 9.5 copy). Then **Section 9 customer-facing wedge fully closed**. Next operator-discretion: GA4 zombie-row cleanup (maintenance) / operator-diagnostic 1,000-row cap fix (operator polish) / Refresh UX reload polish (operator polish) / OR a non-Section-9 next slice from the master plan. **CallRail STAYS DEFERRED** (operator-locked; do not recommend). Full detail: [`docs/VERIFICATION_LOG.md`](VERIFICATION_LOG.md#2026-05-19--section-9-today-tile-edit-outcomes-past-30-days).

> 🟢 **SLICE 9.A2β.1 — MODE A LOADER URL-SCOPED SELECT FIX (SHIPPED + VERIFIED, 2026-05-19).** Smallest follow-up to 9.A2β. Fixes the manual-verification failure where the Outcomes section did not render on the whole-home-remodel Changes detail page despite substrate + wiring being correct. Root cause: loader's tenant-scoped SELECT hit the Supabase JS client's default 1,000-row cap (tenant has 5,502 rows in ga4_url_traffic — 2,751 path-only legacy + 2,751 full-URL parallel rows from 9.A2γ.1). Matching rows truncated non-deterministically → Mode A returned ineligible → component suppressed. **Adds**: (1) MODIFIED `load-mode-a-for-changes-detail.ts` — runtime import of `canonicalizeCitationUrl`; canonical-first compute; null early-out (no Supabase round-trip on sentinel/null/path-only/malformed target_url); `.in("url", [canonical, canonical + "/"])` chained after `.eq("tenant_id", ...)`; cache key bumped v1→v2. (2) UPDATED loader tests with 8 new cases pinning the URL-scoped SELECT contract + early-out paths + cache key version. **NO new architecture invariants** — existing 9.A2β invariants continue to cover. **Hard contracts honored**: NO component change · NO Mode A compute change · NO canonicalize-url change · NO GA4 substrate change · NO refresh action change · NO page/client wiring change · NO migration · NO cron · NO DELETE of legacy zombie rows · NO customer copy change. **Quality gates**: typecheck CLEAN · targeted + full suite green · build green. **STOPPED AT READY_TO_COMMIT.** Net src/ delta within +500 ceiling (see report). **Recommended next**: operator review → commit + push + verify CI + Vercel + manual smoke (reload `/changes/cl-mogzw78nv8pu54?v2=1`; Outcomes section should render with eligible copy `"This page received 18 sessions in the 21 days since going live."` per the full SQL-truth aggregate). Then **9.A2β manual-verification closeout**. Then operator-discretion next: 9.B CallRail / Section 9 Today tile / non-Section-9 next. Full detail: [`docs/VERIFICATION_LOG.md`](VERIFICATION_LOG.md#2026-05-19--slice-9a2%CE%B21-mode-a-loader-url-scoped-select-fix).

> 🟢 **SLICE 9.A2β — CHANGES DETAIL MODE A CUSTOMER COPY (SHIPPED, 2026-05-19).** First customer-facing Section-9 surface. Mode A outcome sub-line inside Changes detail Act 3, alongside the existing lifecycle + repeat-citation sub-lines. Consumes 9.A2α.2 Mode A read model + 9.A2γ-refreshed + 9.A2γ.1-normalized cached `ga4_url_traffic` rows. **Adds**: (1) NEW `src/components/changes/outcome-attribution-act3.tsx` (pure presentational; type-only `ModeAResult`; locked K5 customer copy with plural-aware formatting; suppression on every ineligible discriminator + null). (2) NEW `src/domains/outcome-attribution/load-mode-a-for-changes-detail.ts` (tenant-scoped Supabase admin read of `ga4_url_traffic`; `unstable_cache` 6h TTL; soft-fail to null on admin/42P01/read-error; calls Mode A compute with `qualifiedCallCount=0` for K2-deferred CallRail). (3) MODIFIED Changes detail page.tsx (loader call alongside repeat-citation; same try/catch fail-soft posture). (4) MODIFIED Changes detail client (new `modeAResult` prop; renders `<OutcomeAttributionAct3 />` after `<RepeatCitationAct3 />`). **NEW architecture invariants (3)**: `outcome-attribution-changes-detail-vocab` (K5 forbidden tokens + pure-component posture) · `outcome-attribution-changes-detail-suppression` (behavioral + source-text suppression pins) · `outcome-attribution-changes-detail-no-ga4-api` (per-file no-Data-API across component/loader/page/client). Catalog updated. **Tests added (~80 across 5 files)**: 26 component + 11 loader + 20 vocab + 10 suppression + 33 no-ga4-api. **Locked copy** (P1–P5): eligible (calls=0) drops "and 0 calls" clause · insufficient_days names 7-day threshold explicitly · insufficient_volume stays generic (no 5-session leak) · no sample-window date range · ineligible silent. **Hard contracts honored**: NO Mode A compute change · NO canonicalize-url.ts change · NO GA4 substrate change · NO refresh-action change · NO migration · NO cron · NO LLM · NO Today tile · NO Recommendations/Prompts/Local/Competitors/Settings touch · NO Mode B/C · NO percentages · NO causal language. **Quality gates**: typecheck CLEAN · targeted + full suite green · build green. **STOPPED AT READY_TO_COMMIT.** Net src/ delta within +500 ceiling (see report). **Recommended next**: operator review → commit + push + verify CI + Vercel + manual smoke (load a Ritz Changes detail; confirm whole-home-remodel verified-live edit renders the still-learning copy in Act 3 with `data-outcome-attribution-kind="still_learning_insufficient_volume"`). Then **9.B CallRail connector** OR **Section 9 Today tile** OR **non-Section-9 next** — operator discretion at the next checkpoint. Full detail: [`docs/VERIFICATION_LOG.md`](VERIFICATION_LOG.md#2026-05-19--slice-9a2%CE%B2-changes-detail-mode-a-customer-copy).

> 🟢 **SLICE 9.A2γ.1 — GA4 pagePath → FULL URL PERSIST-TIME NORMALIZATION (SHIPPED + VERIFIED, 2026-05-19).** Smallest follow-up to 9.A2γ. Fixes the manual-verification finding that Mode A returned `ineligible: no_traffic_data` despite 1,000+ rows in `ga4_url_traffic` — root cause was GA4's path-only `pagePath` shape mismatching the canonicalizer's locked "full URLs only" contract. **Adds**: (1) NEW `src/lib/connectors/ga4/normalize-page-path.ts` (pure server-only helper resolving path-only inputs to `https://{businessConfig.domain}{pagePath}`; full-URL pass-through; empty-domain soft-fail; domain hygiene). (2) MODIFIED `src/lib/connectors/ga4/persist-url-traffic.ts` (reads `getBusinessConfig().domain`, normalizes each row before upsert, logs single warn on empty domain when rows > 0). **NEW architecture invariant**: `ga4-url-traffic-stored-as-full-url` (7 assertions pinning the normalizer import + upsert-via-normalizer + no-customer-surface scan + Mode A separation-of-concerns + canonicalizer untouched). **Tests added (~40 across 2 files)**: normalize-page-path.test.ts (33) + persist-url-traffic.test.ts extended (5 new). Catalog updated. **Hard contracts honored**: NO change to Mode A · NO change to canonicalize-url.ts · NO change to diagnostic page render · NO migration · NO cron · NO direct DB backfill · NO LLM · NO customer surface change · NO daily-scan/poll/canary touch · existing 2,751 rows handled in-place by composite-PK upsert on next Refresh click. **Quality gates**: typecheck CLEAN · targeted + full suite green · build green. **STOPPED AT READY_TO_COMMIT.** Net src/ delta within +500 ceiling (see report). **Recommended next**: operator review → commit + push + verify CI + Vercel + manual smoke (click Refresh; confirm `ga4_url_traffic.url` rows store full URLs; confirm whole-home-remodel edit flips from `ineligible: no_traffic_data` to `eligible` or `still_learning_outcome`). Then **Slice 9.A2β preflight re-run** with proven Mode A matchability. Full detail: [`docs/VERIFICATION_LOG.md`](VERIFICATION_LOG.md#2026-05-19--slice-9a2%CE%B31-ga4-pagepath--full-url-persist-time-normalization).

> 🟢 **SLICE 9.A2γ — OPERATOR-ONLY GA4 TRAFFIC REFRESH ACTION (SHIPPED, 2026-05-19).** Smallest unblocker for 9.A2β customer-copy work. Preflight found `ga4_url_traffic` at 0 production rows because no writer code existed. 9.A2γ adds the operator-triggered fill path. **Adds**: (1) NEW `src/lib/connectors/ga4/persist-url-traffic.ts` (server-only helper exporting `persistGa4UrlTraffic` + pure `computeRefreshDateRange` — 90-day default, expandable to `min(live_at)` within 180-day cap; wraps `runGa4UrlTrafficReport`; upserts on conflict `(tenant_id, url, date)` with `last_synced_at` + verbatim `raw` JSONB; discriminated fail-soft union with passthrough Data API reasons + `admin_unavailable` / `persist_failed` / `invalid_args`). (2) NEW `src/app/(shell)/diagnostics/outcome-attribution/actions.ts` (`"use server"` exporting `refreshTenantGa4Traffic()`; gated by `isOperatorModeServer()` BEFORE any token/DB/HTTP work; reads GA4 token + property + edits; calls persist; `revalidatePath` on success only). (3) MODIFIED `src/app/(shell)/diagnostics/outcome-attribution/page.tsx` (adds operator-only `<form>` refresh button + "Last refresh" freshness line derived from `MAX(last_synced_at)` over cached rows; Supabase select extended to include `last_synced_at`). **NEW architecture invariants (2)**: `outcome-attribution-refresh-operator-only` (7 assertions) · `outcome-attribution-refresh-no-customer-surface` (3 assertions). Catalog updated. **Tests added (~45 across 3 files)**: persist-url-traffic.test.ts (23) · outcome-attribution-actions.test.ts (12) · outcome-attribution-page.test.tsx (5 new). **Hard contracts honored**: no customer surface change · no cron · no automatic refresh · no CallRail · no GBP insights · no LLM · no revenue/causal copy in customer-visible markup (framework `<script>` stripped before vocab scan) · no Mode A customer copy · no new migration · no daily-scan/poll/canary changes · 9.A2α.1 substrate + 9.A2α.2 Mode A + applied migration all byte-unchanged. **Quality gates**: typecheck CLEAN · targeted suite all green · full suite green · build green. **STOPPED AT READY_TO_COMMIT.** Net src/ delta within +500 ceiling (see report). **Recommended next**: operator review → commit + push + verify CI + Vercel + manual smoke (click Refresh on `/diagnostics/outcome-attribution` and confirm rows populate from production GA4). Then **Slice 9.A2β preflight re-run** with non-empty `ga4_url_traffic` to verify Mode A renders non-silent for at least one Ritz edit before customer-copy implementation. Full detail: [`docs/VERIFICATION_LOG.md`](VERIFICATION_LOG.md#2026-05-19--slice-9a2%CE%B3-operator-only-ga4-traffic-refresh-action).

> 🟢 **SLICE 9.A2α.3 — OPERATOR-ONLY OUTCOME-ATTRIBUTION DIAGNOSTIC (SHIPPED, 2026-05-19).** Split-slice 3 of 3 for Mode A outcome attribution. Closes the operator-substrate loop on top of the shipped 9.A2α.1 + 9.A2α.2. **Adds**: `src/app/(shell)/diagnostics/outcome-attribution/page.tsx` (~+286 src/) — operator-only Server Component gated by `isOperatorModeServer() || NODE_ENV === "test"` → `notFound()`. Reads verified-live `recommended_edits` via existing repository pattern + reads `ga4_url_traffic` via Supabase admin. Computes Mode A per row using `computeModeATrafficAttribution`. **Renders**: counter strip + per-edit table with `data-row-mode-a-kind` + `data-row-mode-a-reason` + `data-diagnostics-row-count` data attributes. **Fail-soft Supabase read**: 4-state `TrafficLoadStatus` discriminator surfaces operator-side banner for `table_missing` (42P01) / `read_error` / `admin_unavailable`; never crashes. **NEVER calls GA4 Data API on render** — 3 source-text test contracts pin this (no `data-api` import, no `fetch(`, no `runGa4UrlTrafficReport` identifier). **No new architecture invariants** — diagnostic-page contract fully pinned by 10 unit-test cases. **Quality gates**: typecheck CLEAN · targeted 9.A2α.3 + 9.A2α.2 + 9.A2α.1 suite 110/110 · existing connector / GA4 / GSC regression sweep still green · full suite + build green. **Hard contracts honored**: no customer surface change · no cron · no CallRail · no GBP insights · no LLM · no revenue/causal copy · no GA4 call on page load · no `business-config` field · no GSC/GBP/Yelp regression · no scan/poll/canary changes · no migration · 9.A2α.1 substrate + 9.A2α.2 Mode A + migration byte-unchanged. **STOPPED AT READY_TO_COMMIT.** Net src/ delta: **+353** (within +500 ceiling; +147 headroom). **Recommended next**: operator review of the 9.A2α.3 READY_TO_COMMIT report; on approval, commit + push + Vercel deploy. After deploy + operator-side hosted smoke (`/diagnostics/outcome-attribution` with operator-mode env enabled), Section 9.A2α is fully closed at the operator-substrate level. Then **Slice 9.A2β** (Changes detail Act 3 Mode A customer copy on `/changes/[id]?v2=1` + K5 forbidden-vocab catalog extension + customer-render tests). Full detail: [`docs/VERIFICATION_LOG.md`](VERIFICATION_LOG.md#2026-05-19--slice-9a2%CE%B13-operator-only-outcome-attribution-diagnostic).

> 🟢 **SLICE 9.A2α.2 — MODE A PURE-COMPUTE READ MODEL (SHIPPED, 2026-05-19).** Split-slice 2 of 3 for Mode A outcome attribution. Pure compute over caller-supplied `Ga4UrlTrafficRow[]` consumed from the 9.A2α.1 substrate. **Adds**: `src/domains/outcome-attribution/mode-a-cited-here-traffic-here.ts` (~+284 src/) exporting `computeModeATrafficAttribution` + locked Section 9 K4 thresholds (`MODE_A_MIN_DAYS_POST_LIVE = 7` · `MODE_A_MIN_SESSIONS = 5` · `MODE_A_MIN_QUALIFIED_CALLS = 1`) + discriminated `ModeAResult` union. **NEW architecture invariant** `outcome-attribution-mode-a-sample-size-guard` (15 assertions) — three K4 constants + OR-branch + purity (no Supabase / Data API / connector runtime imports; type-only imports OK) + K5 defense-in-depth. **27 unit tests** covering eligible / still_learning / ineligible / pre-live + future-row filtering / multi-row aggregation / canonical URL matching / no-mutation purity guarantees. **Quality gates**: typecheck CLEAN · targeted 9.A2α.2 suite 27+15 green · existing connector + GA4 substrate + GSC regression sweep still green · catalog-sync green · full suite + build green. **Hard contracts honored**: no customer surface change · no cron · no CallRail · no GBP insights · no LLM · no revenue/causal copy anywhere · no GA4 call on page load · no `business-config` field · no GSC/GBP/Yelp regression · 9.A2α.1 substrate byte-unchanged · migration byte-unchanged · operator diagnostic page absent (9.A2α.3 deferred) · no customer Mode A copy (9.A2β deferred). **STOPPED AT READY_TO_COMMIT.** Net src/ delta: **+284** (within +500 ceiling; +216 headroom). **Recommended next**: operator review of the 9.A2α.2 READY_TO_COMMIT report; on approval, commit + push + Vercel deploy. Then **Slice 9.A2α.3** (operator-only `/diagnostics/outcome-attribution` page that reads `ga4_url_traffic` + Ritz verified-live edits + renders per-edit Mode A result via this read model; estimated ~+287 src/, well within +500 ceiling). Then **Slice 9.A2β** (Changes detail Act 3 Mode A customer copy + K5 forbidden-vocab catalog extension + customer-render tests). Full detail: [`docs/VERIFICATION_LOG.md`](VERIFICATION_LOG.md#2026-05-19--slice-9a2%CE%B12-mode-a-pure-compute-read-model).

> 🟢 **SLICE 9.A2α.1 — GA4 DATA API SUBSTRATE + FIRST SECTION-9 MIGRATION (SHIPPED + MIGRATION APPLIED, 2026-05-19).** Split slice 1 of 3. The full 9.A2α bundle hit +992 src/ (over the locked +500 ceiling); operator approved Option α — three-way split. This slice ships substrate + migration ONLY. External prerequisite verified: Google Analytics Data API enabled in production Cloud project (operator confirmed). **Substrate**: new `src/lib/connectors/ga4/data-api.ts` (server-only, tenant-scoped, fail-soft, refresh-on-401, `[ga4-data-api]` non-2xx logging) + additive `Ga4UrlTrafficRow` / `Ga4RunReportArgs` / `Ga4UrlTrafficReportResult` types + one-line symmetric `[ga4-client]` non-2xx log on substrate `client.ts` (folds in the 9.A1β-deferred logging fix). **First Section-9 migration**: `ga4_url_traffic` (composite PK `(tenant_id, url, date)` + RLS deny-all + 24h TTL via `last_synced_at`). **9.A2α.2 (Mode A read model) DEFERRED** — pure-compute consumer of this substrate; ships next. **9.A2α.3 (operator-only diagnostic) DEFERRED** — ships after Mode A lands. **2 new architecture invariants** (Mode A guard invariant deferred with its source to 9.A2α.2): `ga4-data-api-tenant-isolation` + `ga4-data-api-non-2xx-logging`. **Quality gates**: typecheck CLEAN · targeted 9.A2α.1 suite green · regression sweep still green · catalog-sync green · full suite + build green. **Hard contracts honored**: no customer surface change · no cron · no CallRail · no GBP insights · no LLM · no revenue/causal copy anywhere · no GA4 call on page load · no `business-config` field · no GSC/GBP/Yelp regression · substrate 9.A1α public surface byte-identical except the one symmetric `log.warn` extension. **STOPPED AT READY_TO_COMMIT.** Net src/ delta: **+421** (within +500 ceiling). **Recommended next**: operator review of the 9.A2α.1 READY_TO_COMMIT report; on approval, commit + push + Vercel deploy + apply migration on production Supabase. Then **Slice 9.A2α.2** (`src/domains/outcome-attribution/mode-a-cited-here-traffic-here.ts` pure-compute Mode A read model + `outcome-attribution-mode-a-sample-size-guard` architecture invariant + Mode A unit tests). Then **Slice 9.A2α.3** (operator-only `/diagnostics/outcome-attribution` page). Then **Slice 9.A2β** (Changes detail Act 3 Mode A customer copy + K5 forbidden-vocab catalog extension). Full detail: [`docs/VERIFICATION_LOG.md`](VERIFICATION_LOG.md#2026-05-19--slice-9a2%CE%B11-ga4-data-api-substrate--first-section-9-migration).

> 🟢 **SLICE 9.A1β — GA4 SETTINGS UI + PROPERTY PICKER (SHIPPED, 2026-05-18).** Consumes the 9.A1α GA4 connector substrate (commit `e88a060`). Adds the customer-visible Google Analytics card to `/settings/connectors` with a 4-state UI machine + property picker, 3 GA4 server actions, and tightens the `ga4-no-page-load-call` architecture invariant by re-adding the positive sanity check on the actions seam. **Substrate locked** — no file under `src/lib/connectors/ga4/` modified. **page.tsx**: reads `getConnectorInfo("google_ga4")`; inline `formatGa4StaleCopy` helper. **connectors-client.tsx**: new "Google Analytics" card (NOT CONNECTED · CONNECTED + NO PROPERTY · CONNECTED + WITH PROPERTY · SOFT-DISCONNECTED) + property picker (loads on user click only). **actions.ts**: 4 new server actions (`getGoogleGa4ConnectorStatus`, `listGa4Properties`, `selectGa4Property` with defense-in-depth re-list verification, `disconnectGoogleGa4` soft-disconnect mirroring GSC J5). `disconnectGoogle()` intentionally leaves GA4 alone. **Tests**: 14 actions + 5 card render tests + connectors-smoke extension for the GA4 card data-attribute + tightened `ga4-no-page-load-call` invariant with positive actions-seam sanity check. **Quality gates**: typecheck CLEAN · targeted suite 69/69 · existing GSC/connector regression sweep 93/93 still green · full suite green · build green. **Hard contracts honored**: no GA4 Data API · no sessions/events/conversions · no migrations · no Today tile · no Changes detail outcome copy · no `/diagnostics/outcome-attribution` · no CallRail · no GBP insights · no customer outcome claims · no revenue/causal copy · no LLM · no paid APIs beyond the substrate's Admin API path · no `business-config` GA4 property field · no GSC/GBP/Yelp regression · no file under `src/lib/connectors/ga4/` modified. **STOPPED AT READY_TO_COMMIT.** Net src/ delta: **+498** (within the +500 ceiling). **Recommended next**: operator review of the 9.A1β READY_TO_COMMIT report; on approval, commit + push + Vercel deploy + hosted Google OAuth manual smoke (real OAuth flow + property pick). Then Slice 9.A2 (GA4 Data API + Mode A outcome attribution read model + first migration). Full detail: [`docs/VERIFICATION_LOG.md`](VERIFICATION_LOG.md#2026-05-18--slice-9a1%CE%B2-ga4-settings-ui--property-picker).

> 🟢 **SLICE 9.A1α — GA4 CONNECTOR SUBSTRATE (SHIPPED, 2026-05-18).** Split slice. The first 9.A1 bundle exceeded its explicit +500 src/-line stop condition (actual +985), so the operator split it into **9.A1α — substrate only** and **9.A1β — Settings UI + property picker** (next slice). 9.A1α ships the backend that makes GA4 a valid Google connector kind WITHOUT exposing any UI or callers. **Substrate scope**: `GoogleConnectorKind = "gsc" | "gbp" | "ga4"`; `GA4_SCOPE = "analytics.readonly"`; `ConnectorProvider` widened with `"google_ga4"`; payload extended with optional `ga4_property_id` + `ga4_property_display_name` + `ga4_account_display_name` via JSONB extension (no schema migration). 3 new server-only modules: `src/lib/connectors/ga4/{types.ts,client.ts,property-selection.ts}`. OAuth callback extended with single discriminator branch for `kind: "ga4" → provider: "google_ga4"`. **Settings UI INTENTIONALLY UNCHANGED in 9.A1α** — `page.tsx` + `connectors-client.tsx` + `actions.ts` byte-identical to HEAD; no Google Analytics card; no GA4 server actions. 4 new architecture invariants cataloged: `ga4-connector-server-only` · `ga4-connector-tenant-isolation` · `ga4-no-page-load-call` (substrate posture — tightens in 9.A1β with a positive sanity check on the actions seam) · `ga4-scope-split`. **Quality gates**: typecheck CLEAN · targeted GA4 suite 40/40 · existing connector suite 93/93 still green · catalog-sync green with 4 new rows. **Shipped commit**: `e88a060` on `origin/main`. CI ✅ Vercel ✅ (deployment `beacon-o3tsljlyk-armeen-5267s-projects.vercel.app` Ready, Production). Full detail: [`docs/VERIFICATION_LOG.md`](VERIFICATION_LOG.md#2026-05-18--slice-9a1%CE%B1-ga4-connector-substrate).

> 🟢 **SECTION 8 J-BLOCK REMAINING LANDED (2026-05-18).** J2 (token expiry soft-fail 7-day cache) + J3 (4-hour hash-bucketed quota stagger by tenant_id) + J4 (`mobile_usability` completion via `raw` JSONB extraction) + J5 (soft disconnect — preserves cached `gsc_url_inspections` rows, sets `disconnected_at` on the token payload instead of deleting the row). NEW modules: `src/lib/connectors/gsc/expiry-handler.ts`, `quota-stagger.ts`, `disconnect-flow.ts`. EXTENDED: `src/lib/connectors/gsc/{client.ts,types.ts}`, `src/domains/indexability/{types.ts,load-gsc-signal.ts}`, `src/lib/connector-store.ts` (added optional `disconnected_at` field on the `GoogleConnectorToken` payload — JSONB, no schema migration), `src/app/(shell)/settings/connectors/{actions.ts,connectors-client.tsx,page.tsx}` (Disconnect now soft-disconnects GSC; new "Last refreshed at X days ago" tooltip rendered server-side via `formatLastRefreshedCopy` — the helper stays `server-only`). NEW architecture invariant `gsc-quota-stagger-tenant-isolation` pins the stagger module as pure + explicit-tenant + locked constants. **Quality gates**: typecheck CLEAN · 91/91 targeted GSC tests · full suite green · build green. **Hard contracts honored**: no LLM · no paid APIs · no Supabase migrations · no destructive token writes · no GSC calls on page load · J1 multi-property remains DEFERRED. **Recommended next**: Section 9 (Outcome Attribution — GA4 + CallRail + GBP insights) per master plan strict order; Section 4.5 (Recommendation Intelligence Expansion) remains parked. Full detail: [`docs/VERIFICATION_LOG.md`](VERIFICATION_LOG.md#2026-05-18--section-8-j-block-remaining).

> 🟢 **PHASE A.2 STEP 3c — ATOMIC CUSTOMER-VISIBLE THRESHOLD FLIP LANDED (2026-05-14).** Closes the customer-side of Phase A.2 (Sections 3.7 + 3.10). `render-copy.ts` gains `buildTileStrings(decision)` for the Today tile's per-source strings; loader threads the resolved `ThresholdDecision` into both `deriveLifecycleStage` (re-bucketing on the user-visible call sites) and `renderLifecycleCopy` (per-source copy variants). Summary loader re-classifies records against `decision.thresholds` atomically with the tile-label swap — counts and labels move together by construction. `edit-lifecycle-tile.tsx` boundary tightened: no direct import of `T2C_THRESHOLDS` or `BORROWED_BENCHMARK_TOOLTIP`; tile reads pre-rendered strings via props. `buildTenantLifecycleRecords` refactored to use a pure structural `classifyForBrainSeed(result)` (the brain only checks cited-vs-uncited set membership). Architecture invariant `threshold-decision-loader-wiring.test.ts` inverted to A.2.3c contract (MUST-pass + tile import-boundary pin). **Quality gates**: typecheck CLEAN · 8719/8744 tests passed · build green. **Hard contracts honored**: no LLM · no paid APIs · no migrations · no mutations · no new routes · legacy + Phase B Regenerate untouched. **Recommended next**: operator manual UI verification on production (`/today?v2=1` + `/changes/[id]?v2=1` for Ritz) to confirm whether the per-tenant gate has been crossed and the Beacon-owned numbers + "for this site" honesty suffix are visible; then begin Phase A.3 (Section 4 — indexability + GSC) pre-flight planning. Full detail: [`docs/VERIFICATION_LOG.md`](VERIFICATION_LOG.md#2026-05-14--phase-a2-step-3c-atomic-customer-visible-threshold-flip).

> 🟢 **PHASE A.1 SURFACE BUNDLE LANDED (2026-05-14).** Last bundle of Phase A.1. The pure-foundation modules (thresholds + eligibility + canonicalize-url + compute-time-to-citation + lifecycle-stage) shipped over Steps 1–5; this bundle adds the customer-visible surfaces and the architecture invariants that protect them. **Code added**: `src/domains/citation-lifecycle/render-copy.ts` (per-stage copy variants + per-platform divergence + Section 2.16 stuck-stage bridge phrase + D15 locked tooltip), `src/domains/citation-lifecycle/load-lifecycle.ts` (tenant-scoped `unstable_cache`-wrapped loader with two entry points: single-edit + per-tenant 90-day rollup), `src/components/today/edit-lifecycle-tile.tsx` (client tile with empty + populated states + "Why these benchmarks?" tooltip). **Code wired**: Changes detail v2 client renders the new lifecycle block inside Act 3 (gated by linked verified-live edit; null for the rest); Today v2 mounts a new `TodayV2EditLifecycleSection` between the Action Cards and the Descriptors with its own `<Suspense>` boundary. **Architecture invariants added**: `citation-lifecycle-tenant-isolation.test.ts` (pure-compute modules never import the persistence layer; only the lone loader file is allowed to hit the repository; pair-import of `getRepository` must include `.forTenant`) + `citation-lifecycle-stuck-bridge-phrase.test.ts` (literal "next bundle will add automated sitemap + robots checks" + Phase A.3 REPLACE note pinned in the source). **Tests**: 40 new behavioral (20 lifecycle copy renderer + 13 Changes detail render + 7 Today tile render) + 7 new architecture-invariant assertions (4 tenant-isolation + 3 stuck-stage bridge-phrase). Quality gates: typecheck CLEAN · **8564/8589 tests passed** (+47 since pre-bundle baseline 8517) · build pending operator green-light · browser preview confirmed lifecycle line renders inside Act 3 against a seeded verified-live fixture (screenshot captured). **Hard contracts honored**: no LLM · no paid APIs · no Supabase migrations · no mutations · no new top-level routes · legacy route untouched · Phase B Regenerate untouched. **Recommended next**: Phase A.2 (Section 3) — cross-tenant brain scaffolding (`BEACON_CROSS_TENANT_BRAIN`) + per-tenant Beacon-owned threshold replacement (Section 3.7) + customer-facing "Beacon learned" tile (`BEACON_BRAIN_LEARNED_TILE`) per E-block locks. Full detail: [`docs/VERIFICATION_LOG.md`](VERIFICATION_LOG.md#2026-05-14--phase-a1-surface-bundle-changes-detail-lifecycle-line--today-tile).

> 🟢 **PHASE A.1 — TIME-TO-CITATION READ MODEL — STEP 1 LANDED (2026-05-13).** Maximum-depth plan locked at `/Users/armeen/.claude/plans/enter-maximum-depth-planning-mode-twinkly-balloon.md` (Sections 1–12 fully decomposed; Section 13 PARKED; 76 decisions locked across D–N blocks; 10 coding prompts ready). Executing in atomic per-step commits per the plan. **This step** is the foundational constants module + its architecture invariant pin: `src/domains/citation-lifecycle/thresholds.ts` (BORROWED DEFAULTS — Profound study, 6 / 18 / 37 days, integer rounding per D8) + `tests/architecture/thresholds-provenance.test.ts` (7 pinning assertions: file path · Profound attribution · 6/18/37 rounding · Phase A.2 replacement promise · canonical `T2C_THRESHOLDS` export · `as const` lock · self-referential test path). Pure constants, no UI, no migration, no external calls. Quality gates green: typecheck CLEAN · **8409/8434 tests passed** (+7) · build green. **Recommended next**: continue Phase A.1 step-by-step (per Section 2.20): (1) `eligibility.ts` predicate with ~14-case truth table; (2) `canonicalize-url.ts` (verify reuse from `src/domains/pages/*` first); (3) `compute-time-to-citation.ts` with per-platform breakdown; (4) `lifecycle-stage.ts` 6-stage derivation; (5) Changes detail Act 3 lifecycle copy variants; (6) Today edit-lifecycle tile + tooltip per D15 locked copy. Section 12 catalog build can interleave at operator discretion. Full detail: [`docs/VERIFICATION_LOG.md`](VERIFICATION_LOG.md#2026-05-13--phase-a1-step-1-citation-lifecycle-thresholds).

> 🟢 **RECOMMENDATION EXECUTION LAYER v1 PHASE A LANDED → Suggested Copy renders on /recommendations/[id] (2026-05-13).** Acts re-ordered to 1 Recommendation · 2 Why · 3 Evidence · **4 Suggested copy** · 5 Measurement · 6 Next step. Render-only over existing `recommended_edits.proposed_text` — zero LLM calls, zero paid APIs, zero DB mutations, zero migrations. 11 supported action-row types (edit_h1/h2/title/meta, add_faq/section/schema/comparison_table/internal_links, improve_copy, technical_fix); 3 suppressed (create_page, review_decision, regenerate_edit). Render-time display-safety guard hard-blocks 35+ internal taxonomy tokens (aiSearchSignal, actualSearchQueries, topSearchQueries, rec_id, source_rec_id, evidence_tier, etc.), UUIDs, multi-segment snake_case, suspicious snake_case suffixes, multi-transition camelCase. Unsafe rows render the calm fallback "Beacon has a draft for this recommendation, but it needs review before showing here." 3 new files (display-guard + adapters + act component) + 1 modified (detail-client renumbering); 4 new test files, 159 new tests. Quality gates: typecheck CLEAN, 8,347/8,372 tests PASS, build green, `/recommendations/[id]` in route table as dynamic. `?legacy=1` untouched. Full detail: [`docs/VERIFICATION_LOG.md`](VERIFICATION_LOG.md#2026-05-13--recommendation-execution-layer-v1-phase-a-render-only-suggested-copy-act). **Recommended next:** (1) operator visually verifies Phase A on production after Vercel deploy — confirm at least one tenant rec renders the Act 4 tile and at least one historical row triggers the fallback; (2) decide on Phase B (Regenerate button + per-rec cooldown wrapping the existing `runProviderAndPersist` + `llm-budget.json` $10/mo cap); prerequisite for Phase B is tracing `packet.brandAssertions` provenance so self-serve tenants don't hit Rule 16.A hard-abstention. None blocking.

> 🟢 **UX.6.3 LANDED → AI Visibility hero promotion on /today (2026-05-08).** Operator-approved hero promotion after this morning's twin cron fixes. NEW `src/components/today/ai-visibility-hero.tsx` (pure presentation) renders executive header + dynamic brand copy + optional Full/Partial sample badge + lead sentence (`{brandName} is #N across tracked AI answers.`) + 4-card metric strip (Visibility score · Rank · Closest challenger · Sample) + per-platform footer (ChatGPT 42% primary · Perplexity 18% primary) + `Why this number?` link. Wired in today-client.tsx via `aiVisibilityHeroProps` useMemo over already-loaded `visibilityData` + `enrichmentV2` (no new I/O, no math change). Leaderboard heading renamed `Visibility Score Rank → AI visibility leaderboard`. AreaChart hover bug fixed (reserved `min-h-[14px]` row prevents checkbox-jump on hover). 22 new behavioral tests + 19 new architecture invariants. UX.5B.1 invariants reconciled. Quality gates: 6,565/6,565 tests, build green, integrity scripts green (Ritz UNCHANGED 9896/16521/28), brain-health YELLOW (queue-idle WARN only), LLM budget SHA `d36eed8c…` byte-identical. Zero OpenAI / paid polling / production data mutations / math changes. **/today is now friend-demo ready** — pending operator visual verification. Recommended next: operator visually verifies UX.6.3 on production; UX.6.4 (next pass) candidates from the touchpoint audit can resume on a clean baseline once operator gives the green light. Full detail: [`docs/HANDOFF_VERIFIED_STATE.md`](HANDOFF_VERIFIED_STATE.md) + [`docs/VERIFICATION_LOG.md`](VERIFICATION_LOG.md#2026-05-08--ux63-ai-visibility-hero-promotion-on-today).

> 🟢 **CRON GREEN → poll-health classifier recognizes direct-CLI shape (2026-05-08, 2nd patch).** After the morning UTC-day-guard fix (`c9d0ae9`), manual workflow_dispatch ran successfully — 200 obs + 100 snapshots landed for May 8, ~$3.07 spent — but verify-persistence failed RED because the poll-health classifier regex `/chunk offset=/i` over-matched the new direct-CLI scope_label shape (`chunk offset=0 limit=all`), forcing a 1/4-partial verdict on what was actually 1/1-ok whole-mode. Fix: tightened to `/chunk offset=\d+ limit=\d+/i` so `limit=all` falls to whole-mode. 3 new behavioral tests + 3 new architecture invariants. Quality gates: 6,503/6,503 tests, build green, integrity scripts green (Ritz UNCHANGED 9896/16521/28), brain-health YELLOW (queue-idle WARN only), LLM budget SHA `d36eed8c…` byte-identical. Manual canary verification confirms `✓ All platforms ok for 2026-05-08`. **Recommended next**: UX.6.3 — AI Visibility hero promotion. Cron + persistence pipeline is fully green; no rerun needed for May 8; 2026-05-09 07:00 UTC scheduled cron will be the natural full-green-path test. Full detail: [`docs/HANDOFF_VERIFIED_STATE.md`](HANDOFF_VERIFIED_STATE.md) + [`docs/VERIFICATION_LOG.md`](VERIFICATION_LOG.md#2026-05-08--verify-persistence-false-fail-fix-poll-health-partitioner-recognizes-direct-cli-shape).

> 🟡 **CRON FIX LANDED → UTC-day-anchored budget guard (2026-05-08).** P0 cron-skip diagnostic + patch. May-8 07:00 UTC scheduled poll silently skipped because the 20h rolling budget guard fired on May-7's late 16:11 UTC recovery completion (14h49m diff < 20h window). Zero paid API calls; LLM budget SHA byte-identical. Fix: removed `BUDGET_GUARD_WINDOW_MS`; added exported pure `utcDayStartIso(now?: Date)` helper; rewrote `defaultHasRecentRun(source, tenantId?)` to query `.gte("completed_at", utcDayStartIso())` + conditional `.eq("tenant_id", tenantId)`; tenant-scoped (Customer-2 multi-tenant safe); skip-path note updated to "today (UTC)". 1 reconciled test + 6 new behavioral tests + 8 new architecture invariants. Quality gates: 6,497/6,497 tests, build green, integrity scripts green (Ritz UNCHANGED 9896/16521/28), brain-health YELLOW (queue idle WARN only), LLM budget SHA `d36eed8c…` byte-identical. **Recommended next**: operator clicks Actions → Daily native AI-visibility poll → Run workflow → Run; expected cost ~$3.07. After rerun lands May-8 observations, UX.6.3 hero promotion can resume on a green system. Full detail: [`docs/HANDOFF_VERIFIED_STATE.md`](HANDOFF_VERIFIED_STATE.md) + [`docs/VERIFICATION_LOG.md`](VERIFICATION_LOG.md#2026-05-08--cron-skip-bug-fix-utc-day-anchored-budget-guard-replaces-rolling-20h).

> 🟢 **UX.6.2 LANDED → vocabulary + hierarchy cleanup on /today, 5 fixes (2026-05-07).** Operator brief: make /today feel less chaotic. **Fix 1 — One vocabulary**: same scan_findings layer was three competing names ("133 important scan diffs" + "775 page issues" + "Scan diffs to review (7)"). NEW `src/lib/site-findings-labels.ts` is the single source of truth (`SITE_FINDING_NOUN`, `compactStripLabel`, `doNextHeadline`, `doNextSubtitle`, `recentSiteChangesHeading`, `RECENT_SITE_CHANGES_SUBTITLE`). Three consumers route through it: TodayDoNextCard, TodayActionQueue strip, ChangeReview accordion. Old "scan diff" / "page issue" / "raw website differences" wording absent from JSX (comments allowed as migration history). **Fix 2 — Calmer styling**: DoNext review_site_findings branch keys tone off `criticalCount > 0` only; important-only renders neutral muted instead of warning amber. **Fix 3 — Rec duplication cleanup**: dropped DoNext `decide_recommendation` discriminant + JSX (was a third copy of the rec already shown in Command Center NextBestActionCard + Action Queue ActionCard). NextBestActionCard refactored to true SUMMARY: NEW `summarizeRationale` extracts leading sentence + 110-char hard cap; `line-clamp-1` instead of `line-clamp-3`; explicit "Full evidence in the action queue below" pointer; data-attributes for telemetry. **Fix 4 — Empty lifecycle compression**: TodayImplementationQueue empty branch returns null (drops the "Nothing waiting on you" styled card); TodayLifecycleStrip computes `allActionableCountsZero` and appends "· nothing waiting" muted suffix when true; stamps `data-lifecycle-empty="true"`. **Fix 5 — /today hierarchy preserved**: source-order pin asserts CC → Visibility → DoNext → Strip → ImplQueue → ActionQueue → wins → Metrics → ChangeReview. NEW `tests/architecture/ux6-2-vocabulary-hierarchy-contract.test.ts` (43 invariants) + NEW `src/lib/site-findings-labels.test.ts` (21 behavioral). 4 prior tests reconciled. Quality gates: typecheck CLEAN, **6,481/6,481 tests**, build green, all 3 integrity scripts green (Ritz counts UNCHANGED 9896/16521/28), brain-health YELLOW (queue idle WARN only), LLM budget SHA `d36eed8c…` byte-identical. Zero OpenAI / paid polling / production data mutations / new Supabase reads / onboarding / cron / RLS / auth / billing touches. **Recommended next**: UX.6.3 hero promotion (operator-deferred candidate from the touchpoint audit) once operator visually verifies UX.6.2 in production. None blocking. Full UX.6.2 detail: [`docs/HANDOFF_VERIFIED_STATE.md`](HANDOFF_VERIFIED_STATE.md) + [`docs/VERIFICATION_LOG.md`](VERIFICATION_LOG.md#2026-05-07--ux62-vocabulary--hierarchy-cleanup-on-today-5-fixes).

> 🟢 **UX.6.1 LANDED → trust restoration on /today, 3 fixes (2026-05-07).** Audit's three highest trust-breaking issues fixed. **Fix 1 — Brain readiness card lying in production**: Command Center's brain card reads `.data/_reports/brain-health-*.json`; `.data/` is gitignored → never bundled to Vercel → "Waiting for next reading" rendered for every tenant on every deploy since UX.2. NEW pure helper `deriveBrainSummaryFromCounts(input)` derives a grade-letter brain summary from already-loaded /today inputs (obs 7d, recent snaps, owned URLs cited, rec queue size, platform coverage). Wired via refactored `resolveCommandCenterFailSoft(args)` + sibling `deriveBrainFromTodayInputs(args)` projector. Returns null when totalObs <= 0 so first-reading tenants still hit Gap F.1. Zero new Supabase reads. **Fix 2 — Poll health false alarm pre-cron**: NEW `<PollHealthCalmBanner />` ("Next reading scheduled" muted styling) + pure `isPreCronPending({platforms, date, now?})` (true when ALL pending AND now < 08:00 UTC = 07:00 UTC scheduled cron + 1h grace). Tier 0 alerts now ternary calm-vs-warning. Partial / failed / post-cutoff still warn. **Fix 3 — Wins copy too caveated by default**: Old "Citations rose. URL-level signal detected; not proof of causation." → New "This page gained citations after the change. Beacon is tracking the pattern so you can repeat what worked." (positive) / "This page lost citations after the change. Beacon is tracking to see if it recovers." (negative). Methodology caveat preserved in lineageBullets (drawer). 24 new architecture invariants + 21 new behavioral tests. 2 prior tests reconciled. Quality gates: 6,414/6,414 tests, build green, integrity scripts green (Ritz counts UNCHANGED 9896/16521/28), brain-health YELLOW (queue idle WARN only), LLM budget SHA `d36eed8c…` byte-identical. Zero OpenAI / paid polling / production data mutations / new Supabase reads. **Recommended next**: UX.6.2 — pick the next highest items from the touchpoint audit (F2 scan-diff naming chaos / F4 recommendation duplication / F5+) once operator visually verifies UX.6.1 in production. None blocking. Full UX.6.1 detail: [`docs/HANDOFF_VERIFIED_STATE.md`](HANDOFF_VERIFIED_STATE.md) + [`docs/VERIFICATION_LOG.md`](VERIFICATION_LOG.md#2026-05-07--ux61-trust-restoration-on-today-3-fixes).

> 🚨 **EGRESS-P0 LANDED → emergency Supabase egress mitigations (2026-05-07).** P0 incident: Supabase Free Plan 15.52 GB / 5 GB (3.1× over) + statement-timeouts on `page_snapshots` + `prompt_answer_observations`. Recent UX work was NOT the cause. Five fixes shipped: windowed reads on /recommendations + /settings/prompts + /prompts/[id]; tenant-scoped getPageSnapshots cap 5000→500 + column projection; /today page_snapshots memoization; kill switches for Command Center + Recommendations Strip. Estimated 10–15× egress reduction. 18 new invariants + 5 prior tests reconciled. Quality gates green. **Operator decisions**: (a) optionally set kill-switch env vars in Vercel; (b) **Supabase Pro upgrade ($25/mo) recommended as durable fix** before customer-2.

> 🟢 **OPERATOR EXPERIENCE SPRINT UX.5B LANDED → premium hierarchy on /today + /recommendations (2026-05-07).** Continuation of UX.1–4. UX.5A audit confirmed git state at `1f1b95b` and production `/signup` rendering Gap B branding; could not directly verify Command Center via WebFetch (auth-gated). UX.5B.1 added "AI Visibility" headline + dynamic sub-copy above /today's Tier 1.5 chart. UX.5B.2 added top-pick row emphasis on /recommendations rank #1 (3px accent border + "Top pick" chip + faint background tint; selector mirrors strip's Top opportunity card). UX.5B.3 added needs-more-evidence microcopy ("Beacon is watching for stronger support before recommending this.") on rows where derived === needs_review. UX.5B.4 unified Command Center empty states ("Waiting for next reading" / "Watching for movement" / "No action queued yet"). 22 new invariants pin: source order, conditional rendering, exact copy, no scary jargon, Ritz preservation. Quality gates: 6336/6336 tests, build green, integrity scripts green (Ritz counts UNCHANGED 9896/16521/28), LLM budget SHA byte-identical. **Recommended next**: (a) operator visually verifies the deploy in a logged-in browser; (b) if a friend test reveals further rough edges, address those first; (c) optional next UX upgrades from sprint report — visibility chart hero treatment (now partially landed via the hero header), Tier 1 Do-Next polish, /changes table layout polish. None blocking.

> 🟢 **OPERATOR EXPERIENCE SPRINT (UX.1–4) LANDED → /today + /recommendations FEEL UPGRADED (2026-05-07).** Direction change from operator: stop docs/onboarding work; main app needed visible upgrade for Ritz. Sprint shipped on top of `e37d358`. UX.1 audit confirmed "nothing changed visually" feeling was correct (9 prior commits were all backend/onboarding). UX.2 added Beacon Command Center to /today (4 cards above existing 7-tier layout: Brain readiness B + Latest reading per-platform pills + Top movement delta + Next best action with CTA + tiny operator-only /diagnostics/brain link). UX.3 added Recommendations Executive Strip (4 cards above the Toolbar + "Why this order?" disclosure) and reframed the derived `needs_review` pill from "Needs review" → "Needs more evidence". UX.4 softened verdict labels: "Measured win" → "Measured lift", "Early signs of lift" → "Early signal", "Too early" → "Still watching", "No signal" → "No movement yet". 70 new tests pin: Ritz mature-render preserved (Command Center renders AFTER demo + first-reading early returns; render guard requires data; integrity scripts confirm zero data mutations); customer-safe copy enforced; top-pick logic prefers strong/moderate over needs_review; pill reframe + tooltip semantics pinned. Sprint report: [`docs/BEACON_OPERATOR_EXPERIENCE_SPRINT_REPORT_2026_05_07.md`](BEACON_OPERATOR_EXPERIENCE_SPRINT_REPORT_2026_05_07.md). Quality gates: 6314/6314 tests, build green, integrity scripts green (Ritz counts UNCHANGED), LLM budget SHA byte-identical, brain-health YELLOW (queue idle WARN only). **Recommended next 3 UX upgrades**: (1) visibility chart hero treatment / promotion into Command Center; (2) top-pick row emphasis in /recommendations table itself; (3) /today empty-state composability for the in-between tenant state. None blocking.

> 🟢 **FRIEND-TEST READY → SEND THE LINK (2026-05-07). Operator playbook + guarded reset script in place.** Eight self-serve gaps shipped (A → B → C.1 → C.2 → C.3 → E.1 → C.4 → F.1). Friend-test QA shipped: `docs/BEACON_FRIEND_TEST_CHECKLIST_2026_05_07.md` (full 12-section walkthrough — pre-flight, customer-safe DM template, expected screens, DB verification queries, reset playbook, failure modes, 8-box send-the-link checklist) + `scripts/reset-test-tenant.ts` (guarded reset for cleaning up a test tenant after the test — refuses Ritz patterns, defaults to dry-run, only touches 3 enumerated tables, won't reset paused/cancelled/many-observation tenants). 51 new tests pin the safety contract. **Decision gate**: open the checklist, confirm sections 1 + 10 are all green, send the DM from section 3 to one trusted friend. **Recommended next post-friend-test work** (sequenced by what the friend test reveals): (a) **fix any rough edges captured during the test** — bounded UI/copy work; (b) **Gap F.2 (immediate-poll trigger)** if friend signals impatience between Launch and next morning's first reading — adds paid-API code path, needs cost cap + per-tenant budget guard, ship behind a feature flag; (c) **Gap G (operator alerts on new tenant activation)** — email/Slack notification when a self-serve tenant flips to active; (d) **Gap D (auto-detect)** — pre-fill cities/services/competitors during onboarding from a homepage crawl. None blocking. Full QA detail: [`docs/HANDOFF_VERIFIED_STATE.md`](HANDOFF_VERIFIED_STATE.md) + [`docs/VERIFICATION_LOG.md`](VERIFICATION_LOG.md#2026-05-07--friend-test-readiness-qa-checklist-doc--guarded-reset-script). Friend-test playbook: [`docs/BEACON_FRIEND_TEST_CHECKLIST_2026_05_07.md`](BEACON_FRIEND_TEST_CHECKLIST_2026_05_07.md).

> 🟢 **GAP F.1 LANDED → BEACON IS FRIEND-TEST READY (2026-05-07). Self-serve onboarding is end-to-end functional + the post-Launch UX is now non-broken.** Pipeline: Gap A (DB-driven cron) ✅ → Gap B (signup + provisioning) ✅ → Gap C.1 (business profile) ✅ → Gap C.2 (cities + services) ✅ → Gap C.3 (competitors) ✅ → Gap E.1 (deterministic prompt generator + preview) ✅ → Gap C.4 (review + Launch) ✅ → **Gap F.1 (first-reading waiting state on /today) ✅**. End-to-end customer journey: signup at /signup → magic-link → auth-callback provisions pending tenant → wizard collects business + cities + services + competitors → /onboard/review shows preview + TOS + Launch button → click Launch → starter prompts inserted (is_active=true) + tenants flips to status='active' + tos_accepted_at set → redirect to /today → freshly launched tenant sees "Beacon is preparing your first AI visibility reading. Your first dashboard will appear after the next daily reading." card → next 07:00 UTC cron polls the tenant → next morning /today fills in with the first real reading. All Ritz behavior preserved (verified by integrity scripts + architecture invariants — Ritz has observations → flag is false → renders unchanged). **Optional next mini-phases (NOT blocking customer-2)**: **Gap F.2 (immediate-poll trigger)** — have launchTenant() synchronously kick off `runNativePoll(tenantId, platform)` so the first reading lands within ~3-5 minutes instead of next morning. Adds a paid-API code path; needs cost cap + per-tenant budget guard; ship only if customer impatience signals appear after F.1 friend test. **Gap G (operator alerts)** — email/Slack notification when a self-serve tenant flips to active. **Gap D (auto-detect)** — pre-fill cities/services/competitors during onboarding from a homepage crawl. **Recommended posture**: friend-test Gap F.1 before deciding F.2/G/D order. The flow works end-to-end without any of them. Full Gap F.1 detail: [`docs/HANDOFF_VERIFIED_STATE.md`](HANDOFF_VERIFIED_STATE.md) + [`docs/VERIFICATION_LOG.md`](VERIFICATION_LOG.md#2026-05-07--self-serve-onboarding-gap-f1-first-reading-waiting-state-on-today).

> 🟢 **GAP C.4 LANDED → GAP F NEXT (2026-05-07). Self-serve onboarding is end-to-end functional.** Pipeline: Gap A (DB-driven cron) ✅ → Gap B (signup + provisioning) ✅ → Gap C.1 (business profile) ✅ → Gap C.2 (cities + services) ✅ → Gap C.3 (competitors) ✅ → Gap E.1 (deterministic prompt generator + preview) ✅ → **Gap C.4 (review + Launch) ✅** → **Gap F (day-0 first-poll trigger / waiting state) NEXT**. Today: a real customer can sign up at /signup → magic-link → auth-callback provisions pending tenant → /onboard/business → /onboard/scope → /onboard/competitors → /onboard/review (preview + TOS + Launch button) → click Launch → starter prompts inserted into tracked_prompts (account_id=tenant.slug, is_active=true) + tenants flips to status='active' with tos_accepted_at set → redirect to /today. The next 07:00 UTC cron picks them up automatically (Gap A's lister now sees them). **Gap F scope** (the "first reading" UX): when a customer activates mid-day, they wait up to 24h for the next 07:00 UTC cron. Two options to consider: (a) **immediate trigger** — launchTenant kicks off `runNativePoll(tenantId, platform)` synchronously (or queues it) so the customer's first reading lands within ~3-5 minutes after Launch; (b) **waiting state UX** — /today renders a "Your first reading lands tomorrow morning" interstitial when no observations exist yet. Recommend (b) first (zero new code paths to paid APIs), then (a) as a follow-up if customer impatience signals. **Out of scope until Gap D/G + later**: auto-detect competitors/cities from homepage crawl (Gap D), operator alerts on new signups (Gap G), billing (separate concern), per-tier prompt cap (Pro tier = 100 prompts, Agency = 250). Functionally, Beacon is now sellable to a 2nd tenant. Full Gap C.4 detail: [`docs/HANDOFF_VERIFIED_STATE.md`](HANDOFF_VERIFIED_STATE.md) + [`docs/VERIFICATION_LOG.md`](VERIFICATION_LOG.md#2026-05-07--self-serve-onboarding-gap-c4-launch-step-the-first-statusactive-code-path).

> 🟢 **GAP E.1 LANDED → GAP C.4 NEXT (2026-05-07).** Self-serve onboarding pipeline progress: Gap A (DB-driven cron) ✅ → Gap B (signup + provisioning) ✅ → Gap C.1 (business profile) ✅ → Gap C.2 (cities + services) ✅ → Gap C.3 (competitors) ✅ → **Gap E.1 (deterministic prompt generator v0 + /onboard/review preview) ✅** → **Gap C.4 (Launch step) NEXT**. Today: customers complete steps 1-3 → /onboard/review shows the saved profile + a preview of up to 25 starter prompts (brand discovery + competitor comparison + service-in-city + cost queries) generated deterministically from the saved inputs. NO Launch button yet, NO DB write of prompts yet — preview-only. The "active tenant with zero prompts" failure mode is now solved at design time. **Gap C.4 scope** (the FIRST and ONLY step that flips `tenants.status` from `pending_onboarding` → `active`): add a TOS checkbox + Launch button to /onboard/review. On click, server action `launchTenant()` runs a single transaction: (1) INSERT each `PromptDraft` from `generateStarterPrompts(...)` into `tracked_prompts` with `account_id = tenant.slug`, `is_active = true`; (2) UPDATE tenants SET `tos_accepted_at = now()`, `status = 'active'`, `updated_at = now()` WHERE id = ? AND status = 'pending_onboarding' AND tos_accepted_at IS NULL — double-click safe; (3) on success, redirect to `/today` (or to a Gap F waiting-state interstitial if F ships first). **Out of scope until Gap D/F/G**: auto-detect (Gap D), day-0 first-poll trigger (Gap F), operator alerts (Gap G). Full Gap E.1 detail: [`docs/HANDOFF_VERIFIED_STATE.md`](HANDOFF_VERIFIED_STATE.md) + [`docs/VERIFICATION_LOG.md`](VERIFICATION_LOG.md#2026-05-07--self-serve-onboarding-gap-e1-deterministic-prompt-generator-v0-preview-only).

> 🟢 **GAP C.3 LANDED → GAP C.4 NEXT (2026-05-07).** Self-serve onboarding pipeline progress: Gap A (DB-driven cron) ✅ → Gap B (signup + provisioning) ✅ → Gap C.1 (business profile) ✅ → Gap C.2 (scope: cities + services) ✅ → **Gap C.3 (competitors: 1-5 builders) ✅** → **Gap C.4 (review + Launch) NEXT**. Today: customers sign up → magic-link → auth-callback provisions pending tenant → /onboard/business (Gap C.1) → /onboard/scope (Gap C.2) → /onboard/competitors (Gap C.3, NEW) → /onboard/review placeholder (awaiting Gap C.4). All 100% of inputs needed before activation are now collected: `business_name`, `domain`, `cities_served`, `project_mix`, `discovered_competitors`. **Gap C.4 scope** (the FIRST step that activates a tenant — high care): replace /onboard/review placeholder with a real Launch step. Show full saved profile + a TOS checkbox (sets `tos_accepted_at`) + a Launch button. On click, single transactional UPDATE flips `tenants.status` from `pending_onboarding` → `active` AND sets `tos_accepted_at` AND sets `updated_at` — gated by `WHERE id = ? AND status = 'pending_onboarding' AND tos_accepted_at IS NULL` so double-clicks / replays are no-ops. After UPDATE, redirect to `/today` (or a "first reading lands tomorrow morning" interstitial — Gap F). **Out of scope for Gap C.4**: auto-prompt generation (Gap E — without prompts, the first cron poll runs over zero questions, so think about whether C.4 should block on E or activate-without-prompts as a known gap), day-0 immediate first poll (Gap F), operator alerts (Gap G), billing/Stripe (separate concern). **Critical pre-Gap-C.4 design decision for the operator**: should activation also require Gap E to have generated prompts, or is "tenant active but empty prompt list, first poll a no-op" an acceptable interim state? Recommend revisiting before Gap C.4 implementation. Full Gap C.3 detail: [`docs/HANDOFF_VERIFIED_STATE.md`](HANDOFF_VERIFIED_STATE.md) + [`docs/VERIFICATION_LOG.md`](VERIFICATION_LOG.md#2026-05-07--self-serve-onboarding-gap-c3-competitors-step-1-5-builders-to-track).

> 🟢 **GAP C.2 LANDED → GAP C.3 NEXT (2026-05-07).** Self-serve onboarding pipeline progress: Gap A (DB-driven cron) ✅ → Gap B (signup + provisioning) ✅ → Gap C.1 (business profile step) ✅ → **Gap C.2 (scope step — cities + services) ✅** → **Gap C.3 (competitors step) NEXT**. Today: customers sign up at /signup → magic-link → auth-callback provisions pending tenant → /onboard/business collects business name + website (Gap C.1) → /onboard/scope collects cities + project_mix (Gap C.2, NEW) → /onboard/competitors placeholder (awaiting Gap C.3). **Gap C.3 scope**: replace /onboard/competitors placeholder with a real form for `discovered_competitors` (free-text builder names with optional domain hint, min 1, ≤5). Same access-guard + status-gated UPDATE pattern; same architecture invariants (no paid APIs, no activation, no prompts, no second active tenant). On save, redirect to /onboard/review (placeholder). **Gap C.4 scope (later)**: replace /onboard/review placeholder with the Launch step — show a summary, accept TOS, and on confirm flip `tenants.status` from `pending_onboarding` → `active`. That's the only step that activates a tenant. Auto-prompt generation, first-poll trigger, and operator alerts (Gaps D/E/F/G) follow. Full Gap C.2 detail: [`docs/HANDOFF_VERIFIED_STATE.md`](HANDOFF_VERIFIED_STATE.md) + [`docs/VERIFICATION_LOG.md`](VERIFICATION_LOG.md#2026-05-07--self-serve-onboarding-gap-c2-scope-step-cities--services).

> 🟢 **GAP C.1 LANDED → GAP C.2 NEXT (2026-05-07).** Self-serve onboarding pipeline progress: Gap A (DB-driven cron) ✅ → Gap B (signup + auth-callback provisioning) ✅ → **Gap C.1 (business profile step) ✅** → **Gap C.2 (scope step — cities + services) NEXT**. Today: customers sign up at /signup → magic-link → auth-callback provisions pending tenant → /onboard/business collects business name + website (Gap C.1, NEW) → /onboard/scope placeholder. **Gap C.2 scope**: replace /onboard/scope placeholder with a real form for cities served (free-text chips, min 1) + services / project mix (multi-select against `ProjectMixTag` enum, min 1). Same access-guard + status-gated UPDATE pattern; same architecture invariants (no paid APIs, no activation, no prompts, no second active tenant). On save, redirect to /onboard/competitors (placeholder). **Out of scope until Gap C.4**: status flip pending_onboarding → active, prompt creation, first poll trigger, billing. Full Gap C.1 detail: [`docs/HANDOFF_VERIFIED_STATE.md`](HANDOFF_VERIFIED_STATE.md) + [`docs/VERIFICATION_LOG.md`](VERIFICATION_LOG.md#2026-05-07--self-serve-onboarding-gap-c1-business-profile-step-name--website).

> 🟢 **GAP B LANDED → GAP C NEXT (2026-05-07).** Self-serve onboarding pipeline progress: Gap A (DB-driven cron) ✅ → Gap B (public /signup + auth-callback provisioning) ✅ → **Gap C (onboarding wizard) NEXT**. Today: customers can sign themselves up at /signup, get a `pending_onboarding` tenant + `tenant_members` row provisioned automatically, and land on a placeholder /onboard/business welcome page. Cron pipeline ignores them (Gap A's lister filters `status='active'`); operator must explicitly flip `tenants.status` to `active` to start daily polling. **Gap C scope**: replace the /onboard/business placeholder with a 4-step wizard (business name + website → cities/services → competitors → prompt review); persist drafts on the `tenants` row or in a new `tenant_drafts` table; on Launch, flip status `pending_onboarding` → `active` so the next 07:00 UTC cron picks the new tenant up. **Out of scope for Gap C**: auto-detect from homepage crawl (Gap D), auto-prompt generation (Gap E), waiting state (Gap F), operator alerts (Gap G). Full Gap B detail: [`docs/HANDOFF_VERIFIED_STATE.md`](HANDOFF_VERIFIED_STATE.md) + [`docs/VERIFICATION_LOG.md`](VERIFICATION_LOG.md#2026-05-07--self-serve-onboarding-gap-b-public-signup--auth-callback-tenant-provisioning).

> 🟡 **MULTI-TENANT CRON SCAFFOLD LANDED — AWAITING 2026-05-07 07:00 UTC PROOF-RUN (2026-05-06).** `daily-native-poll.yml` + `daily-scan.yml` + `poll-canary.yml` now read tenant identity from **NEW `ops/active-tenants.json`** (single Ritz row, `enabled: true`) via a `compute-matrix` job that **fails loud if Ritz is missing or disabled** — no silent-skip path possible. Poll + scan are matrix-driven; rebuild-citation-evidence-index + verify-persistence + canary are singletons by design (endpoints / scripts are tenant-agnostic today; per-tenant flag is a future bundle when a 2nd tenant is enabled). **Adding a 2nd tenant is a one-line JSON edit** in `ops/active-tenants.json` — no workflow YAML change required. NEW invariant `multi-tenant-cron-scaffold.test.ts` (21/21) pins the new shape; 2 pre-existing invariants updated to match. Profound runtime isolation 114/114 still PASS. Full suite 4731/4731 (was 4710; +21 new); typecheck clean; build green; ledger byte-identical (SHA `d36eed8c…`). Total architecture invariants: **267** (246 prior + 21 new). **GATING VERIFICATION:** tomorrow (2026-05-07) after 07:00 UTC, confirm matrix cron runs Ritz cleanly (4/4 Perplexity + 4/4 ChatGPT) or reports honestly. Customer-2 infrastructure work pauses until this proof-run lands. Commit `f9bd483`. Full audit: [`docs/VERIFICATION_LOG.md`](VERIFICATION_LOG.md#2026-05-06--multi-tenant-cron-scaffold-architecturally-complete-awaiting-2026-05-07-0700-utc-proof-run).
>
> 🟢 **RLS DENY-ALL APPLIED + LEAKED-PASSWORD PROTECTION DEFERRED (2026-05-06).** Customer-2-onboarding's top DB-side blocker is closed. Migration `rls_deny_all_with_tenant_members_self_read` enabled RLS on 36 `public` tables and added 72 explicit policies (36 `deny_anon` + 35 `deny_authenticated` + 1 `members_self_read` carve-out for the auth middleware's `tenant_members` query). `service_role` bypasses RLS — every Beacon `getSupabaseAdmin()` data path keeps working unchanged. Anon REST reads return `[]`; anon REST writes return HTTP 401 + Postgres `42501` ("new row violates row-level security policy"). Hosted sign-in works without `/login?error=no_tenant` loop. Security Advisor: 0 ERRORs (was 36); 1 WARN remains (`auth_leaked_password_protection`) — **acknowledged and intentionally deferred** because Beacon currently uses magic-link / OTP auth only (`signInWithOtp` is the sole auth call; no `signInWithPassword`, no `signUp`, no `updateUser({password})`). The toggle also requires Supabase **Pro Plan** (Beacon org's plan: `free`). Full audit + verification in [`docs/VERIFICATION_LOG.md`](VERIFICATION_LOG.md#2026-05-06--rls-deny-all-migration--leaked-password-protection-decision). **Customer-2 onboarding precondition added** in §7.9 below.
>
> 🟢 **PROFOUND MAY 10 READINESS GUARDRAILS LANDED (2026-05-06).** 7-block architecture invariant `tests/architecture/profound-runtime-isolation.test.ts` (114/114 PASS) pins: no daily-cron workflow references Profound; no `/api/*` route imports from `src/adapters/profound`; no daily-routine script imports it; no shell route outside `/settings/import` references it; `/settings/import/import-page.tsx` is the ONLY caller of `importProfoundData()`; zero `PROFOUND_*` env vars in workflows / package scripts / env examples; `daily-native-poll.yml` invokes only native endpoints. Operator-readable readiness checklist at [`docs/PROFOUND_MAY_10_READINESS.md`](PROFOUND_MAY_10_READINESS.md) — includes May 10 + May 11 morning verification + 13-item post-cutoff cleanup queue + explicit "DO NOT delete before 2026-05-11" rule. Total architecture invariants: **246** (132 prior + 114 new).
>
> 🟢 **MORNING VERIFICATION GREEN + ROUND 2 PAPER-CUTS LANDED (2026-05-06).** Daily-poll post-mode GREEN (199 obs, /today FRESH). Round 2 shipped 3 fixes: `prettifySlug()` for /prompts/[id] (UUIDs render nothing, slugs become "Cupertino, CA"), /recommendations empty-state copy ("raw decision signals" → "today's prompt-by-prompt observations"), /recommendations status pill differentiation (needs_fresh_edit now dashed border + status-info blue, distinct from needs_review warning-amber). 12 new architecture invariants. Ledger byte-identical (SHA `d36eed8c…`) after another full `npm run test`. **Total architecture invariants: 132.**
>
> **Top of stack — when ready, in any order:**
> 1. **Customer-Readiness Round 2 (queued, optional).** From the audit: prompt drilldown friendly slugs (issue #8), Tier 1A comment sweep (issue #9), status pill color differentiation (issue #10), `/recommendations` empty-state copy ("raw decision signals"), `enrichment-v2.tsx` empty-state copy parity. All copy/small-render. Defer until operator wants to round 2.
> 2. **Wait for queue regrowth before LR-3.** Current queue exhausted by LR-1 + LR-2. New medium-conf observation-tier candidates emerge organically as the daily 07:00 UTC cron runs and entity registry / search signals refresh. The 120 architecture invariants will block any drift when LR-3 is built.
> 3. **Other operator priorities.** Engine + UX safety nets verified. Move to: Profound May 10 expiry, customer-2 onboarding scaffold, `/today` findings polish, etc.
> 4. Independent of decisions: all 120 architecture invariants run on every PR. UUID leaks, removed abstention rule wording, removed packet `resolution` block, harness drift to persistence API, regression of cutover ceiling at 9, regression of single-prompt-only-as-hard-trigger rule, test writes to real `.data/global/llm-budget.json`, future LR-N harness drift away from any of the 13 safety properties, OR re-introduction of any Round 1 forbidden phrase (`live_at = now`, `Pre-pivot CSV / PDF rebuild`, `Supabase schema`, `GitHub Actions logs`, `proof run`, etc.) — all fail the build before reaching production.

> ✅ **Phase v4 Commits 1–7 LANDED (2026-04-30).** "Replace Profound in 2 weeks
> while compounding the moat" is complete. Profound's 2026-05-10 expiry is now
> a non-event for daily operation.
>
> - **Source plan:** `/Users/armeen/.claude/plans/you-are-taking-over-floofy-giraffe.md` (Commits 1–4) + `/Users/armeen/.claude/plans/you-are-working-on-purring-shell.md` (finishing pass for Commits 5–7).
> - **Latest verification:** `docs/VERIFICATION_LOG.md` 2026-04-30 entry — KPI flip + schema v2 verified live; citation_evidence_index rebuilt from native (Supabase + on-disk both at `built_at=2026-04-30T21:13:52.951Z`); mixed-source partial-overlap math replacing the Commit 2 abstain in `url-verdict.ts`; copy audit on 6 surfaces.
> - **Remaining cutover work:** none. Profound import code (`src/adapters/profound/bridge.ts` + `src/app/(shell)/settings/import/import-page.tsx`) deletion deferred until ≥2026-05-10 in case operator pulls one final historical CSV.
>
> **What's next (per `HANDOFF_VERIFIED_STATE.md`):**
>
> 1. Watch tomorrow's 07:00 UTC `daily-native-poll.yml` run — verify new `rebuild-citation-evidence-index` GH Actions job fires post-poll and `built_at` updates < 60 min later.
> 2. Vercel preview spot-check on /pages, /competitors, /topics — top-3 should reflect native rankings, not last week's Profound-era frozen data.
> 3. After H2's 7-day bake window (earliest 2026-05-05), decide on flipping `BEACON_LIFECYCLE_VERDICT_ENABLED=1`.
> 4. After 2026-05-10: delete Profound adapter + import UI. Sprint 7.9 multi-tenant onboarding waits on a confirmed second tenant. Phase 6B one-click triage UX waits on a real ambiguous match in production.
