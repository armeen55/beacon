# Architecture invariants catalog

> **Purpose.** Canonical map from every test file under
> `tests/architecture/*.test.ts` to the architecture invariant it
> pins, the plan section that introduced it, its purpose in one
> sentence, and (when known) the retirement condition that closes
> its lifetime. Built per Section 12.1 / Decision Lock N1 of
> `~/.claude/plans/enter-maximum-depth-planning-mode-twinkly-balloon.md`.
>
> **Contract.**
> 1. Every architecture test file MUST be referenced exactly once in
>    the table below.
> 2. Every entry below MUST resolve to an existing test file.
> 3. Mismatches trip `tests/architecture/catalog-sync.test.ts`.
> 4. The sync test enforces structural coverage (file existence +
>    uniqueness) only. It does NOT check the prose in the *purpose*
>    or *retirement-condition* columns — those stay human-reviewed.
>
> **How to add a new entry.**
> When a new `tests/architecture/<name>.test.ts` lands, append a row
> to the matching section (or create a new section heading if the
> plan introduced one). Use today's date as `last-verified`. When you
> retire a test, set `status: retired` rather than deleting the row —
> the historical trail is the audit value.
>
> **How to retire an invariant.**
> When the conditions in `retirement-condition` are met, the operator
> approves a focused PR that either (a) deletes the test file and the
> catalog row in one commit, or (b) edits the test file to a new
> invariant and updates the row's `status` / `retirement-condition`
> / `last-verified` accordingly. The sync test catches half-finished
> retirements (e.g., test file deleted but catalog row left).
>
> **Status values.**
> - `active` — the invariant currently fails the build if violated.
> - `retired` — the test file is gone; the row is kept for history.
>   No `retired` rows in the seed; the field exists so future
>   retirement edits have a clean target.
>
> **Date semantics.**
> `last-verified` = the most recent date this entry was reviewed end-
> to-end by a human (test file read, contract still applicable). Not
> the date the test was first written; not the date of the last test
> run. Update only on intentional review, never automatically.

---

## Phase A.1 — Section 2 (Time-to-Citation Read Model)

| name | source test file | section | purpose (one sentence) | status | retirement-condition | last-verified |
|---|---|---|---|---|---|---|
| thresholds-provenance | `tests/architecture/thresholds-provenance.test.ts` | Section 2.8 / D8 / A.2 Step 3c (2026-05-14) | Pins the BORROWED Profound 6/18/37 defaults in `src/domains/citation-lifecycle/thresholds.ts` as the fallback consulted when `computeTenantThresholds` returns `profound_default`. Per-tenant decisions override at runtime via the loader's `thresholdDecision`; the constants themselves remain the locked Profound shim. | active | Evolves when Phase A.2 (Section 3) further refines the decision tree or when a future phase deprecates the Profound shim entirely. | 2026-05-14 |
| citation-lifecycle-stuck-bridge-phrase | `tests/architecture/citation-lifecycle-stuck-bridge-phrase.test.ts` | Section 2.16 / Section 4.8 / Phase A.3 §4 (2026-05-14) | Pins the stuck-stage render contract AFTER the A.3.4 customer-visible flip: (1) the literal "next bundle will add automated sitemap + robots checks" bridge phrase remains in `render-copy.ts` as the FALLBACK path (for `target_url === null`, `needs_new_page` sentinel, and indexability-loader-throw cases); (2) attached to `STUCK_BRIDGE_PHRASE` constant; (3) source comment documents the A.3.4 fallback role; (4) `LifecycleCopy` declares `diagnostic: string | null` AND `renderLifecycleCopy` calls `renderStuckDiagnostic` to populate it; (5) Changes detail v2 client renders the diagnostic sub-line PREFERRED OVER the bridge sub-line (mutually exclusive at the visible-sub-line level — `data-change-detail-act3-lifecycle-diagnostic="true"` precedes `data-change-detail-act3-lifecycle-bridge="true"` in the LifecycleLine ternary). | active | Retire fully when production telemetry confirms zero stuck rows hit the bridge fallback path for ≥ 30 sustained days, OR when a future step removes the `needs_new_page` sentinel + the loader-throw fallback path entirely (at which point `STUCK_BRIDGE_PHRASE` + `bridgeLine` can be deleted alongside this invariant). | 2026-05-14 |
| citation-lifecycle-tenant-isolation | `tests/architecture/citation-lifecycle-tenant-isolation.test.ts` | Section 2.18 (+ Path A cold-store branch, 2026-05-14) | Pins tenant-scoped repo gate in `src/domains/citation-lifecycle/`, the cold-store allowlist, and the regime-gate cross-reference. | active | Expand `ALLOWED_CROSS_TENANT_FILES` when Section 3 brain producer lands; update `ALLOWED_COLD_STORE_FILES` if Section 4 GSC adds another consumer. | 2026-05-14 |

## Cross-cutting — Tenant isolation

| name | source test file | section | purpose (one sentence) | status | retirement-condition | last-verified |
|---|---|---|---|---|---|---|
| canonical-store-tenant-isolation | `tests/architecture/canonical-store-tenant-isolation.test.ts` | Pre-plan (customer-2 onboarding audit, 2026-05-06) | `loadFreshCanonicalData()` must use tenant-scoped `getTrackedPrompts()` / `getTrackedEntities()` not the unscoped base methods. | active | Permanent — multi-tenant safety floor. | 2026-05-14 |
| factory-functions-require-tenant | `tests/architecture/factory-functions-require-tenant.test.ts` | Pre-plan (Stage D2) | Every Stage D2 factory function (or its CLI wrapper) accepts a `tenant_id` argument. | active | Permanent — multi-tenant safety floor. | 2026-05-14 |
| no-tenant-id-empty-string-literals | `tests/architecture/no-tenant-id-empty-string-literals.test.ts` | Pre-plan | Forbid `tenant_id: ""` empty-string literals in `src/`. | active | Permanent — multi-tenant safety floor. | 2026-05-14 |
| no-unscoped-tier-a-reads | `tests/architecture/no-unscoped-tier-a-reads.test.ts` | Sprint 7 Phase 7.5b Commit 5 / 7.5d/3 | Tier-A tenant-scoped tables can only be read through `.forTenant()`; extended to scripts/** and adapters/poll. | active | Permanent — multi-tenant safety floor. | 2026-05-14 |
| profound-runtime-isolation | `tests/architecture/profound-runtime-isolation.test.ts` | Pre-plan (2026-05-10) | The Profound import pipeline cannot reach native polling state at runtime. | active | Permanent — keeps two regimes isolated at runtime. | 2026-05-14 |
| script-cli-tenant-required | `tests/architecture/script-cli-tenant-required.test.ts` | Pre-plan | Every CLI script writing tenant-scoped data requires an explicit tenant argument. | active | Permanent — multi-tenant safety floor. | 2026-05-14 |
| sync-wrappers-stamp-tenant-id | `tests/architecture/sync-wrappers-stamp-tenant-id.test.ts` | Pre-plan | Every Supabase `sync*` writer stamps `tenant_id` before the upsert. | active | Permanent — multi-tenant safety floor. | 2026-05-14 |
| list-active-tenants-contract | `tests/architecture/list-active-tenants-contract.test.ts` | Gap A (2026-05-07) | Pins the fail-loud `ops/active-tenants.json` lister used by the daily-poll matrix. | active | Permanent — cron-scaffold safety. | 2026-05-14 |
| reset-test-tenant-contract | `tests/architecture/reset-test-tenant-contract.test.ts` | Gap F.1 QA reset-test-tenant safety | Pins the test-tenant reset script's safety gates. | active | Permanent — test-tenant data hygiene. | 2026-05-14 |
| multi-tenant-cron-scaffold | `tests/architecture/multi-tenant-cron-scaffold.test.ts` | Pre-plan (2026-05-06) | Pins the matrix-from-JSON daily-poll cron scaffold (no hardcoded tenant IDs). | active | Permanent — cron-scaffold safety. | 2026-05-14 |
| business-config-tenant-keyed-cache | `tests/architecture/business-config-tenant-keyed-cache.test.ts` | MT-1 Tenant-keyed business-config core (2026-05-22) | Pins the multi-tenant-store prerequisite's first slice: `src/lib/business-config.ts` caches by canonical tenantId, not a process-global singleton. The preflight found business-config was the ONLY genuine process-global blocker for Section 7 customer surfaces + Customer 2 (middleware already injects `x-beacon-tenant` per request; connector-store is already tenant-scoped). **Contract (7 cases):** (1) NO `let _cached: BusinessConfig \| null` singleton remains (comment-stripped — JSDoc may mention it historically); (2) a tenant-keyed `_cacheByTenant = new Map<...>()` exists; (3) the deprecated no-arg overload is `@deprecated` (raw source) + its overload signature `export function getBusinessConfig(): BusinessConfig;` is present; (4) the tenant-aware overload `getBusinessConfig(tenantId: string)` is present; (5) `getBusinessConfig` + `getBusinessConfigForCurrentTenant` are exported functions; (6) behavioral — a `BEACON_BUSINESS_CONFIG_JSON_BY_TENANT` entry keyed by tenantId resolves via the tenantId and MISSES via the bare slug (cache key is tenantId, not slug — the slug-vs-id lesson from the brand-assertions bug); (7) MT-1 wires the new multi-tenant entry into NO rendered customer-surface component (`src/components/{today,recommendations,changes,prompts,local}/**` reference neither `getBusinessConfigForCurrentTenant` nor `@/lib/business-config`). | active | Refines at MT-2 when loaders adopt `getBusinessConfigForCurrentTenant` and at MT-5 when the deprecated no-arg overload is removed — at which point cases (3) flips to "no no-arg overload remains" and the source pins tighten. | 2026-05-22 |
| business-config-deep-runtime-tenant-aware | `tests/architecture/business-config-deep-runtime-tenant-aware.test.ts` | MT-3B Deep-runtime helper migration (2026-05-22) | Pins that, after MT-1/MT-2/MT-3A/MT-3B, ZERO active no-arg `getBusinessConfig()` CALLS remain anywhere in `src/` runtime code. Walks every `.ts`/`.tsx` under `src/` EXCEPT `src/lib/business-config.ts` (the one file that legitimately defines the deprecated overload + the 5 pure-helper `?? getBusinessConfig()` fallbacks until MT-3C/MT-5) and asserts no comment-stripped source matches `getBusinessConfig(\s*)` (`typeof getBusinessConfig` type usage is NOT a call, allowed). **MT-3B migrated:** recommendation-intelligence promotion-writer + load-trigger-candidates (`getBusinessConfig(tenantId)`), indexability batch-load + load (`getBusinessConfig(opts.tenantId)`), pages/verify-action + profound import-orchestrator + ga4/persist-url-traffic (`getBusinessConfig(tenantId)`), yelp-reviews-sync (`await getBusinessConfigForCurrentTenant()`), and the competitor classifier `classifyCompetitorType(domain, directoryDomains)` — config injected by callers through `discover.ts` (opts) + `competitors/page.tsx` + `milestones/post-import-sync.ts` (closures). Sanity pins: business-config.ts still has the deprecated overload signature + the `?? getBusinessConfig()` fallbacks (NOT globally removed). | active | MT-3C removes the pure-helper fallbacks (then the ALLOWED_FILES exception narrows); MT-5 removes the deprecated overload + this converts to a fully global no-arg ban. | 2026-05-22 |
| business-config-operator-surface-tenant-aware | `tests/architecture/business-config-operator-surface-tenant-aware.test.ts` | MT-3A Operator-surface entry-point migration (2026-05-22) | Pins that the 9 OPERATOR-facing business-config entry files (settings + diagnostics pages/actions) resolve config tenant-aware (`getBusinessConfig(tenantId)` when tenantId is in scope, or `await getBusinessConfigForCurrentTenant()` otherwise) — extending the MT-2 customer-surface migration to operator surfaces. **Explicit allowlist (exactly 9 files):** `settings/connectors/{actions,page}`, `settings/config/{actions,page}`, `diagnostics/page`, `diagnostics/recommendation-triggers/{page,actions}`, `diagnostics/repeat-citation/page`, `diagnostics/indexability/page`. **Contract:** per file — (negative) NO no-arg `getBusinessConfig()` call (comment-stripped, line-comments-first; `typeof getBusinessConfig` type usage allowed since it's not a no-arg call); (positive) references `getBusinessConfigForCurrentTenant` OR `getBusinessConfig(<identifier>)`. Plus a sanity pin that the deprecated no-arg overload still EXISTS in core (NOT globally banned). **MT-3A covers operator entries ONLY** — deep domain/lib helpers (MT-3B), pure-helper fallbacks (MT-3C), and CLI/script paths keep their no-arg fallback until later slices. | active | MT-3B/MT-3C migrate the remaining deep helpers + tighten the pure helpers; MT-5 converts to a GLOBAL no-arg ban once the deprecated overload is removed. | 2026-05-22 |
| business-config-customer-surface-tenant-aware | `tests/architecture/business-config-customer-surface-tenant-aware.test.ts` | MT-2 Customer-facing entry-point migration (2026-05-22) | Pins that the 8 customer-facing business-config ENTRY files resolve config tenant-aware (`getBusinessConfig(tenantId)` when tenantId is in scope, or `await getBusinessConfigForCurrentTenant()` otherwise) — closing the process-global/no-arg read on the customer data paths and making the off-site C7d/C7e path tenant-correct end-to-end. **Explicit allowlist (exactly 8 files):** `domains/off-site-authority/load-snapshot.ts`, `domains/today/visibility-read-model.ts`, `app/(shell)/changes/[id]/page.tsx`, `app/(shell)/today-data.ts`, `app/(shell)/today-v2-data.ts`, `app/(shell)/competitors/page.tsx`, `app/(shell)/local/page.tsx`, `lib/local-presence.ts`. **Contract:** per file — (negative) NO no-arg `getBusinessConfig()` call (comment-stripped; line-comments stripped BEFORE block-comments to survive `// .data/*.json`-style phantom openers); (positive) references `getBusinessConfigForCurrentTenant` OR `getBusinessConfig(<identifier>)`. Plus carry-forward of the MT-1 component guard (`src/components/{today,recommendations,changes,prompts,local}/**` reference neither the resolver nor `@/lib/business-config`) and a sanity pin that the deprecated no-arg overload still EXISTS in core (NOT globally banned). **MT-2 covers customer entries ONLY** — deeper domain/lib helpers + operator/diagnostic/settings keep their no-arg fallback until MT-3. | active | MT-3 EXPANDS the allowlist to operator/diagnostic/settings + deep domain/lib helpers (and tightens the pure helpers); MT-5 converts this to a GLOBAL no-arg ban once the deprecated overload is removed. | 2026-05-22 |

## Cross-cutting — LLM safety + cost controls

| name | source test file | section | purpose (one sentence) | status | retirement-condition | last-verified |
|---|---|---|---|---|---|---|
| cost-controls | `tests/architecture/cost-controls.test.ts` | Sprint 6A.3e (2026-04-26) | Pins the native-polling budget gate, kill switch, and cost-ledger write isolation. | active | Section 12.2 (budget-ledger routing helper) refines but does NOT delete this invariant. | 2026-05-14 |
| llm-budget-test-isolation | `tests/architecture/llm-budget-test-isolation.test.ts` | Pre-plan | Tests writing to global persistence stores cannot leak into the LLM budget ledger. | active | Permanent — test infra safety. | 2026-05-14 |
| llm-dryrun-harness-no-persistence | `tests/architecture/llm-dryrun-harness-no-persistence.test.ts` | Pre-plan | The LLM specific-edit dry-run harness MUST NOT persist outputs. | active | Permanent — dry-run safety. | 2026-05-14 |
| llm-live-regen-allowlist | `tests/architecture/llm-live-regen-allowlist.test.ts` | Pre-plan | LLM live-regeneration harness call sites are allowlisted. | active | Refines with Section 10 (Phase B Regenerate) but does not delete. | 2026-05-14 |
| llm-safety-invariants | `tests/architecture/llm-safety-invariants.test.ts` | Sprint 6A.2d (2026-04-26) | LLM safety architectural invariants (no paid calls on page load, budget gates, etc.). | active | Permanent — LLM safety floor. | 2026-05-14 |
| abstention-contract-trust | `tests/architecture/abstention-contract-trust.test.ts` | Trust Sprint Mini-Phase T4.1 (2026-05-06) | Pins the abstention-contract validator wiring + the 4 reject reasons at the source-text level. | active | Permanent — LLM safety floor. | 2026-05-14 |
| openai-system-prompt-dryrun2 | `tests/architecture/openai-system-prompt-dryrun2.test.ts` | LLM-DryRun-2 (2026-05-05) | Pins SYSTEM_PROMPT rules added after DryRun-1 found UUID leaks + fabricated numbers + failure-to-abstain. | active | Refines when Phase B Regenerate / Section 10 ships; do not delete without an explicit replacement contract. | 2026-05-14 |
| openai-system-prompt-dryrun3 | `tests/architecture/openai-system-prompt-dryrun3.test.ts` | LLM-DryRun-3 (2026-05-05) | Pins strict-abstention SYSTEM_PROMPT invariants (Rule 16.A triggers). | active | Same as dryrun2. | 2026-05-14 |
| openai-system-prompt-dryrun3.5 | `tests/architecture/openai-system-prompt-dryrun3.5.test.ts` | LLM-DryRun-3.5 (2026-05-05) | Refines Rule 16.A trigger 1 scope. | active | Same as dryrun2. | 2026-05-14 |
| packet-carries-resolver-confidence | `tests/architecture/packet-carries-resolver-confidence.test.ts` | LLM-DryRun-3 (2026-05-05) | The recommendation evidence packet surfaces the resolver's confidence to the LLM (used by Rule 16.A trigger 2). | active | Permanent — packet contract. | 2026-05-14 |
| specific-edit-target-constraint | `tests/architecture/specific-edit-target-constraint.test.ts` | Sprint 6A.2g.A (2026-04-26) | Pins the strict target-URL alignment for `buildSpecificEditEvidencePacket`. | active | Permanent — LLM packet safety. | 2026-05-14 |
| recommendations-step-3.7-brand-grounding | `tests/architecture/recommendations-step-3.7-brand-grounding.test.ts` | W3 Step 3.7 (2026-05-03) | Brand-claim grounding architecture invariants. | active | Permanent — LLM packet safety. | 2026-05-14 |
| brand-assertions-tenant-key-contract | `tests/architecture/brand-assertions-tenant-key-contract.test.ts` | Brand Assertion Key Alignment (2026-05-22) | Pins that the brand-assertion registries (`TENANT_ASSERTIONS` + `TENANT_NAME_STYLES` in `src/domains/recommendations/brand-assertions.ts`) are keyed by `BeaconTenant.id` (the value `currentTenantId()` returns + every caller passes), NEVER by a tenant slug. Reads `ops/active-tenants.json` for the canonical id/slug sets + the exact registry keys via the test-only `__brandRegistryKeys` export (`Object.keys`, authoritative — no fragile source-text parse). **Contract (7+ cases):** (1) active-tenants has ≥1 tenant with distinct id≠slug (Ritz: id `tenant-ritz-founder`, slug `ritz-builders` — the exact shape that caused the bug); (2) both registries non-empty; (3) every `TENANT_ASSERTIONS` key is an active-tenants tenantId; (4) every `TENANT_NAME_STYLES` key is an active-tenants tenantId; (5) NO registry key equals an active-tenants slug (the slug-vs-id guard); (6) behavioral — for every registered tenant, `getBrandAssertions(tenantId)` is non-empty AND `getBrandAssertions(slug)` is `[]` (no alias); (7) same for `getBrandNameStyle(tenantId)` non-null / `getBrandNameStyle(slug)` null. **Root cause this would have caught (2026-05-22):** registries were slug-keyed (`"ritz-builders"`) while all production callers (`specific-edit-evidence.ts`, the recommendation-triggers action, the B.4a safety-audit diagnostic) + the validator's `getBrandNameStyle(packet.tenantId)` pass the tenant id — so lookups silently returned `[]`/`null` in production, disabling LLM brand-assertion guidance + the validator brand-name-style gate, while the slug-based unit test stayed green. Protects Customer 2 from the same trap. | active | Permanent — multi-tenant brand-grounding key-correctness floor. Refines if the canonical tenant-identity source moves off `ops/active-tenants.json`. | 2026-05-22 |

## Cross-cutting — Cron + poll integrity

| name | source test file | section | purpose (one sentence) | status | retirement-condition | last-verified |
|---|---|---|---|---|---|---|
| cron-cli-architecture | `tests/architecture/cron-cli-architecture.test.ts` | Bundle 2 (2026-05-07) | Pins the GitHub-runner CLI cron architecture (not 4-chunk curl-to-Vercel). | active | Refines if cron infra changes; do not delete without operator approval. | 2026-05-14 |
| cron-redundant-schedules | `tests/architecture/cron-redundant-schedules.test.ts` | Bundle (2026-05-10) | Pins the multiple-times-per-UTC-day workflow schedule shape for reliability. | active | Permanent — cron reliability ratchet. | 2026-05-14 |
| cron-utc-day-guard-contract | `tests/architecture/cron-utc-day-guard-contract.test.ts` | Bundle (2026-05-08 incident fix) | Pins the UTC-day-anchored budget guard in `defaultHasRecentRun`. | active | Permanent — cron-skip-bug regression guard. | 2026-05-14 |
| poll-health-copy | `tests/architecture/poll-health-copy.test.ts` | Pre-plan | The poll-health-block subline copy contract. | active | Permanent — customer-vocabulary contract. | 2026-05-14 |
| poll-health-direct-cli-classifier-contract | `tests/architecture/poll-health-direct-cli-classifier-contract.test.ts` | Bundle (2026-05-08 2nd patch) | Pins the poll-health partitioner regex for the direct-CLI scope shape. | active | Permanent — poll-health regression guard. | 2026-05-14 |
| poll-integrity-contract | `tests/architecture/poll-integrity-contract.test.ts` | Post May 2-4 incident | Poll integrity hardening invariants. | active | Permanent — poll-pipeline safety. | 2026-05-14 |
| scheduled-scan-cron | `tests/architecture/scheduled-scan-cron.test.ts` | Pre-plan | Architecture invariants for the scheduled-scan + cron infrastructure. | active | Permanent — cron infra safety. | 2026-05-14 |
| sampling-guard-observability-log | `tests/architecture/sampling-guard-observability-log.test.ts` | D4 (operator audit, 2026-05-05) | Sampling-status guard observability log shape invariant. | active | Permanent — observability safety. | 2026-05-14 |

## Cross-cutting — Persistence routing

| name | source test file | section | purpose (one sentence) | status | retirement-condition | last-verified |
|---|---|---|---|---|---|---|
| dual-write-loud-fail | `tests/architecture/dual-write-loud-fail.test.ts` | Pre-plan | Dual-write must throw on persistent Supabase failure (not silent fall-back to file). | active | Permanent — persistence safety. | 2026-05-14 |
| dual-write-onconflict | `tests/architecture/dual-write-onconflict.test.ts` | Pre-plan | Dual-write onConflict targets must match the unique constraint of the underlying table. | active | Permanent — persistence safety. | 2026-05-14 |
| json-store-routing-invariants | `tests/architecture/json-store-routing-invariants.test.ts` | Sprint 7 Phase 7.8b-2-e (2026-04-26) | Pins the JSON-store routing dispatch for `readStore`/`writeStore`. | active | Permanent — persistence safety. | 2026-05-14 |
| last-scan-result-routing | `tests/architecture/last-scan-result-routing.test.ts` | Phase 5 hosted-cron fix (2026-04-28) | Pins `last_scan_result` writes route through the canonical persistence layer (not direct file). | active | Permanent — persistence safety. | 2026-05-14 |
| no-page-store-allpages | `tests/architecture/no-page-store-allpages.test.ts` | Sprint 7 Phase 7.5c/3 (2026-04-25) | Forbid unscoped `allPages` reads of the page store. | active | Permanent — multi-tenant safety floor. | 2026-05-14 |

## Cross-cutting — Egress + perf

| name | source test file | section | purpose (one sentence) | status | retirement-condition | last-verified |
|---|---|---|---|---|---|---|
| egress-bounded-reads-p0 | `tests/architecture/egress-bounded-reads-p0.test.ts` | EGRESS-P0 (2026-05-07) | Pins the bounded-read contracts that drop steady-state Supabase egress. | active | Permanent — egress safety. | 2026-05-14 |
| supabase-egress-windowing | `tests/architecture/supabase-egress-windowing.test.ts` | E1-E5 (operator audit, 2026-05-05) | Pins the windowed read contract for `loadFreshCanonicalData`-shaped paths. | active | Permanent — egress safety. | 2026-05-14 |
| deploy-settings-prompts-dynamic | `tests/architecture/deploy-settings-prompts-dynamic.test.ts` | Deploy hardening (2026-05-12) | Source-level pin for `/settings/prompts` dynamic rendering (prevents prerender timeout). | active | Permanent — deploy safety. | 2026-05-14 |
| perf-load-queue-trace | `tests/architecture/perf-load-queue-trace.test.ts` | Emergency P0 (2026-05-12) | Pins granular trace labels inside `loadLiveRecommendationQueue`. | active | Permanent — perf-trace coverage. | 2026-05-14 |
| perf-projected-page-reads | `tests/architecture/perf-projected-page-reads.test.ts` | Perf+egress bundle 2 (2026-05-12) | Pins the additive `getPageSummaries` column projection. | active | Permanent — egress safety. | 2026-05-14 |
| perf-prompts-scoped-reads | `tests/architecture/perf-prompts-scoped-reads.test.ts` | Emergency P0 (2026-05-12) | Source-level pin for `/prompts` + `/prompts/[id]` scoped repo reads. | active | Permanent — egress safety. | 2026-05-14 |
| perf-recs-detail-fast-not-found | `tests/architecture/perf-recs-detail-fast-not-found.test.ts` | Emergency P0 v2 (2026-05-12) | Source-level pin for the `/recommendations/[id]` fast path. | active | Permanent — perf regression guard. | 2026-05-14 |
| perf-recs-persisted-fast-loader | `tests/architecture/perf-recs-persisted-fast-loader.test.ts` | Emergency P0 v5 (2026-05-12) | Source-level pin for the persisted-row fast loader on `/recommendations`. | active | Permanent — perf regression guard. | 2026-05-14 |
| perf-recs-queue-cached | `tests/architecture/perf-recs-queue-cached.test.ts` | Emergency P0 v4 (2026-05-12) | Source-level pin for the cached `loadLiveRecommendationQueueForPage` wrapper + tag invalidation. | active | Permanent — perf regression guard. | 2026-05-14 |
| perf-shell-layout-parallel-awaits | `tests/architecture/perf-shell-layout-parallel-awaits.test.ts` | Perf bundle 6 (2026-05-12) | Pins shell layout parallelization (no sequential awaits). | active | Permanent — perf regression guard. | 2026-05-14 |
| perf-shell-nav-prefetch-disabled | `tests/architecture/perf-shell-nav-prefetch-disabled.test.ts` | Emergency P0 v3 (2026-05-12) | Source-level pin for `prefetch={false}` on every shell-nav Link. | active | Permanent — perf regression guard. | 2026-05-14 |
| perf-streaming-bundle | `tests/architecture/perf-streaming-bundle.test.ts` | Streaming bundle (2026-05-12) | Source-level pins for the perceived-speed refactor of `/today` and `/recommendations`. | active | Permanent — perf regression guard. | 2026-05-14 |
| perf-today-discrepancy-gated | `tests/architecture/perf-today-discrepancy-gated.test.ts` | Perf bundle 5 (2026-05-12) | Source-level pin for the discrepancy-detection gate on `/today`. | active | Permanent — perf regression guard. | 2026-05-14 |
| perf-today-rollup-wired | `tests/architecture/perf-today-rollup-wired.test.ts` | Perf bundle 3 (2026-05-12) | Pins the today rollup fan-out wiring. | active | Permanent — perf regression guard. | 2026-05-14 |
| perf-today-section-streaming | `tests/architecture/perf-today-section-streaming.test.ts` | Today section-streaming bundle (2026-05-12) | Source-level pin for the per-section streaming refactor of `/page.tsx`. | active | Permanent — perf regression guard. | 2026-05-14 |
| perf-trace-wired | `tests/architecture/perf-trace-wired.test.ts` | Perf bundle 7 (2026-05-12) | Source-level pin for the production perf trace wiring (gated by `BEACON_PERF_TRACE`). | active | Permanent — observability coverage. | 2026-05-14 |
| perf-windowed-seed-and-cache | `tests/architecture/perf-windowed-seed-and-cache.test.ts` | Perf+egress bundle (2026-05-12) | Source-level pins for the windowed seed + cache fixes across v2 routes. | active | Permanent — egress + perf regression guard. | 2026-05-14 |

## Cross-cutting — Customer vocabulary + safety

| name | source test file | section | purpose (one sentence) | status | retirement-condition | last-verified |
|---|---|---|---|---|---|---|
| forbidden-customer-vocabulary-contract | `tests/architecture/forbidden-customer-vocabulary-contract.test.ts` | Bundle 3 (2026-05-10, maximum-depth UI audit) | Strips operator/internal vocabulary from customer-facing routes. | active | Expands with Section 12.4 forbidden-vocab additions (Mode A/B leakage + revenue/causal); does not delete. | 2026-05-14 |
| no-operator-jargon | `tests/architecture/no-operator-jargon.test.ts` | Step 1.2 (master plan) | Guards against operator-facing jargon sneaking back into `src/app` and `src/components`. | active | Permanent — customer-vocabulary floor. | 2026-05-14 |
| no-placeholder-in-active-recs | `tests/architecture/no-placeholder-in-active-recs.test.ts` | Pre-plan | No placeholder phrase can reach an active recommendation surface. | active | Permanent — customer-vocabulary floor. | 2026-05-14 |
| no-placeholder-recommended-edits | `tests/architecture/no-placeholder-recommended-edits.test.ts` | W3 Step 3.1 (2026-05-01) | Placeholder + structural-quality regression guard for `recommended_edits`. | active | Permanent — customer-vocabulary floor. | 2026-05-14 |
| no-uuid-in-active-recs | `tests/architecture/no-uuid-in-active-recs.test.ts` | Pre-plan | No raw prompt UUID may reach an operator-visible / customer-visible surface in active recs. | active | Permanent — customer-vocabulary floor. | 2026-05-14 |
| score-provenance-trust-labels | `tests/architecture/score-provenance-trust-labels.test.ts` | Trust Sprint Mini-Phase T3.1 (2026-05-06) | Pins the customer-facing score-provenance trust label set. | active | Permanent — trust label contract. | 2026-05-14 |
| verdict-provenance-trust-labels | `tests/architecture/verdict-provenance-trust-labels.test.ts` | Trust Sprint Mini-Phase T3.2 (2026-05-06) | Pins the customer-facing verdict-provenance trust label set. | active | Permanent — trust label contract. | 2026-05-14 |
| ux4-customer-safe-labels-contract | `tests/architecture/ux4-customer-safe-labels-contract.test.ts` | UX.4 (2026-05-07) | Customer-safe labels contract. | active | Permanent — customer-vocabulary floor. | 2026-05-14 |
| ux5b-premium-hierarchy-contract | `tests/architecture/ux5b-premium-hierarchy-contract.test.ts` | UX.5B (2026-05-07) | Premium hierarchy contract for /today. | active | Permanent — UX contract. | 2026-05-14 |
| ux6-1-trust-restoration-contract | `tests/architecture/ux6-1-trust-restoration-contract.test.ts` | UX.6.1 (2026-05-07) | Trust restoration on /today (3 fixes). | active | Permanent — UX contract. | 2026-05-14 |
| ux6-2-vocabulary-hierarchy-contract | `tests/architecture/ux6-2-vocabulary-hierarchy-contract.test.ts` | UX.6.2 (2026-05-07) | Vocabulary + hierarchy cleanup on /today (5 fixes). | active | Permanent — UX contract. | 2026-05-14 |
| ux6-3-ai-visibility-hero-contract | `tests/architecture/ux6-3-ai-visibility-hero-contract.test.ts` | UX.6.3 (2026-05-08) | AI Visibility hero promotion on /today. | active | Permanent — UX contract. | 2026-05-14 |

## Cross-cutting — v2 customer routes + surfaces

| name | source test file | section | purpose (one sentence) | status | retirement-condition | last-verified |
|---|---|---|---|---|---|---|
| changes-stale-pending-affordance | `tests/architecture/changes-stale-pending-affordance.test.ts` | D5 (operator audit, 2026-05-05) | `/changes` stale-pending tooltip + button affordance. | active | Permanent — customer-vocabulary floor. | 2026-05-14 |
| competitor-page-blueprints-contract | `tests/architecture/competitor-page-blueprints-contract.test.ts` | T-CompPageBlueprints (2026-05-08) | `competitorPageBlueprints` populated-fields contract. | active | Permanent — packet contract. | 2026-05-14 |
| customer-nav-exposure | `tests/architecture/customer-nav-exposure.test.ts` | T-CustomerNav (2026-05-08) | Customer nav exposure contract: sidebar + command palette + settings tabs expose only the customer-safe set. | active | Permanent — customer-nav floor. | 2026-05-14 |
| customer-readiness-round-1 | `tests/architecture/customer-readiness-round-1.test.ts` | Round 1 (2026-05-05) | 6 customer-readiness paper-cut fixes. | active | Permanent — customer-vocabulary floor. | 2026-05-14 |
| customer-readiness-round-2 | `tests/architecture/customer-readiness-round-2.test.ts` | Round 2 (2026-05-06) | Round 2 customer-readiness paper-cuts. | active | Permanent — customer-vocabulary floor. | 2026-05-14 |
| demo-path-fixes-2026-05-06 | `tests/architecture/demo-path-fixes-2026-05-06.test.ts` | War-room bundle (2026-05-06) | 5 customer-facing copy/UX fixes. | active | Permanent — customer-vocabulary floor. | 2026-05-14 |
| demo-path-fixes-2026-05-06-bis | `tests/architecture/demo-path-fixes-2026-05-06-bis.test.ts` | War-room bundle 2-bis (2026-05-06) | 8 customer-facing copy/UX fixes. | active | Permanent — customer-vocabulary floor. | 2026-05-14 |
| exec-confidence-default-surfaces | `tests/architecture/exec-confidence-default-surfaces.test.ts` | Trust Sprint T6.4 (2026-05-06) | Executive confidence default surfaces contract. | active | Permanent — trust contract. | 2026-05-14 |
| mark-shipped-accepted-only | `tests/architecture/mark-shipped-accepted-only.test.ts` | M4 (operator audit, 2026-05-05) | Mark Shipped button restricted to `accepted` rows only. | active | Permanent — lifecycle correctness. | 2026-05-14 |
| recommendations-executive-strip-contract | `tests/architecture/recommendations-executive-strip-contract.test.ts` | UX.3 | ExecutiveStrip + needs_review reframe contract. | active | Permanent — UX contract. | 2026-05-14 |
| recommendations-step-3.15-faq-grouping | `tests/architecture/recommendations-step-3.15-faq-grouping.test.ts` | W3 Step 3.15 (2026-05-04) | FAQ Q+A grouping + title polish source-scan invariants. | active | Permanent — recommendations surface contract. | 2026-05-14 |
| recommendations-step-3.5e-action-table | `tests/architecture/recommendations-step-3.5e-action-table.test.ts` | W3 Step 3.5e (2026-05-03) | Recommendations action-table invariants. | active | Permanent — recommendations surface contract. | 2026-05-14 |
| recommendations-step-3.5f-row-polish | `tests/architecture/recommendations-step-3.5f-row-polish.test.ts` | W3 Step 3.5f (2026-05-03) | Ranked-action-table row polish invariants. | active | Permanent — recommendations surface contract. | 2026-05-14 |
| recommendations-step-3.5g-polish | `tests/architecture/recommendations-step-3.5g-polish.test.ts` | W3 Step 3.5g (2026-05-03) | Ranked-action-table polish invariants. | active | Permanent — recommendations surface contract. | 2026-05-14 |
| recommendations-ui-cleanup | `tests/architecture/recommendations-ui-cleanup.test.ts` | Post-Step 3.5e (2026-05-03) | Source-scan contracts that survive across UI redesigns. | active | Permanent — recommendations surface contract. | 2026-05-14 |
| recs-resolver-debug-panel | `tests/architecture/recs-resolver-debug-panel.test.ts` | P0 (2026-05-13) | Inline debug panel at `/recommendations?debugResolver=1` is render-only. | active | Permanent — debug-panel safety. | 2026-05-14 |
| settings-import-customer-safe-default | `tests/architecture/settings-import-customer-safe-default.test.ts` | D2 (operator audit, 2026-05-05) | `/settings/import` default surface is customer-safe. | active | Permanent — customer-vocabulary floor. | 2026-05-14 |
| suggested-copy-architecture | `tests/architecture/suggested-copy-architecture.test.ts` | Recommendation Execution Layer v1 Phase A (2026-05-13) | Architecture invariants for the Suggested Copy act (no paid call on load, display-safety guard, etc.). | active | Refines when Phase B Regenerate / Section 10 ships. | 2026-05-14 |
| today-action-card-copy | `tests/architecture/today-action-card-copy.test.ts` | T2 + T3 (operator audit, 2026-05-05) | `/today` action-card + win-card copy hygiene. | active | Permanent — customer-vocabulary floor. | 2026-05-14 |
| today-command-center-contract | `tests/architecture/today-command-center-contract.test.ts` | UX.2 Command Center (2026-05-07) | Command Center contract on /today. | active | Permanent — UX contract. | 2026-05-14 |
| today-empty-state-copy | `tests/architecture/today-empty-state-copy.test.ts` | D3 (operator audit, 2026-05-05) | KPI empty-state guidance copy. | active | Permanent — customer-vocabulary floor. | 2026-05-14 |
| today-first-reading-short-circuit | `tests/architecture/today-first-reading-short-circuit.test.ts` | Today perf cleanup (2026-05-12) | `resolveFirstReadingState` short-circuit + silent fail-soft. | active | Permanent — first-reading correctness. | 2026-05-14 |
| today-headline-sampling | `tests/architecture/today-headline-sampling.test.ts` | Pre-plan | `/today` headline KPI tile honors `samplingStatus`. | active | Permanent — sampling correctness. | 2026-05-14 |
| today-v2-chart-hierarchy | `tests/architecture/today-v2-chart-hierarchy.test.ts` | Today v2 (2026-05-12, two passes) | Visibility chart hierarchy + rank copy + legacy-cleanup guardrails. | active | Permanent — UX contract. | 2026-05-14 |
| v2-qa-polish-bundle | `tests/architecture/v2-qa-polish-bundle.test.ts` | v2 QA polish bundle (2026-05-11) | Source-level pins for fixes without a natural render-test home. | active | Permanent — v2 regression guard. | 2026-05-14 |
| v2-recommendations-no-customer-legacy-hops | `tests/architecture/v2-recommendations-no-customer-legacy-hops.test.ts` | UX follow-up (2026-05-13) | `/recommendations` v2 surface must NOT push customers into legacy. | active | Retire only when legacy routes are removed entirely. | 2026-05-14 |

## Cross-cutting — Onboarding

| name | source test file | section | purpose (one sentence) | status | retirement-condition | last-verified |
|---|---|---|---|---|---|---|
| onboard-business-step-contract | `tests/architecture/onboard-business-step-contract.test.ts` | Gap C.1 (2026-05-07) | Business step contract. | active | Permanent — onboarding surface contract. | 2026-05-14 |
| onboard-competitors-step-contract | `tests/architecture/onboard-competitors-step-contract.test.ts` | Gap C.3 (2026-05-07) | Competitors step contract. | active | Permanent — onboarding surface contract. | 2026-05-14 |
| onboard-first-reading-contract | `tests/architecture/onboard-first-reading-contract.test.ts` | Gap F.1 (2026-05-07) | First-reading step contract. | active | Permanent — onboarding surface contract. | 2026-05-14 |
| onboard-launch-step-contract | `tests/architecture/onboard-launch-step-contract.test.ts` | Gap C.4 (2026-05-07) | Launch step contract. | active | Permanent — onboarding surface contract. | 2026-05-14 |
| onboard-prompt-generator-contract | `tests/architecture/onboard-prompt-generator-contract.test.ts` | Gap E.1 (2026-05-07) | Prompt generator contract (services × cities × intent templates). | active | Permanent — onboarding surface contract. | 2026-05-14 |
| onboard-scope-step-contract | `tests/architecture/onboard-scope-step-contract.test.ts` | Gap C.2 (2026-05-07) | Scope step contract. | active | Permanent — onboarding surface contract. | 2026-05-14 |
| signup-onboarding-routes-contract | `tests/architecture/signup-onboarding-routes-contract.test.ts` | Gap B (2026-05-07) | Public signup route + auth-callback → onboarding routing. | active | Permanent — auth flow safety. | 2026-05-14 |

## Cross-cutting — Brain / operator / measurement

| name | source test file | section | purpose (one sentence) | status | retirement-condition | last-verified |
|---|---|---|---|---|---|---|
| brain-health-watchdog-contract | `tests/architecture/brain-health-watchdog-contract.test.ts` | Trust Sprint T7.3 (2026-05-07) | Brain-health watchdog runs the 3 verify scripts + parses grade + checks freshness. | active | Refines when Section 3 cross-tenant brain lands. | 2026-05-14 |
| operator-brain-route-contract | `tests/architecture/operator-brain-route-contract.test.ts` | Trust Sprint T7.6 (2026-05-07) | Operator brain route (`/diagnostics/brain`) contract — tenant-local Brain Readiness Grade. | active | Distinct from Section 3's `/diagnostics/cross-tenant-brain`; retains contract when Section 3 ships. | 2026-05-14 |
| operator-mode-helpers | `tests/architecture/operator-mode-helpers.test.ts` | C1 (2026-05-09) | Pins the contract for `src/lib/operator-mode.ts` helpers. | active | Permanent — operator-mode safety. | 2026-05-14 |
| main-product-final-confidence-sweep | `tests/architecture/main-product-final-confidence-sweep.test.ts` | Trust Sprint T7.7 (2026-05-07) | Final confidence sweep across main product surfaces. | active | Permanent — trust contract. | 2026-05-14 |
| derived-confidence-pipeline-reachable | `tests/architecture/derived-confidence-pipeline-reachable.test.ts` | Trust Sprint T6.5 (2026-05-06) | The T4.4 derived-confidence pipeline is reachable from `.data/tenants/<slug>/recommended-edits.json` consumers. | active | Permanent — trust contract. | 2026-05-14 |
| learning-score-v0-causal | `tests/architecture/learning-score-v0-causal.test.ts` | Trust Sprint T7.5 (2026-05-07) | Learning-score v0 causal join contract. | active | Refines with Section 6 (Primary Recommendation as first-class metric). | 2026-05-14 |
| local-aeo-intelligence-v2 | `tests/architecture/local-aeo-intelligence-v2.test.ts` | Trust Sprint T7.4 (2026-05-07) | Local AEO intelligence v2 pipeline contract. | active | Refines with Section 7 (Off-Site Authority). | 2026-05-14 |

## Cross-cutting — Causal / attribution

| name | source test file | section | purpose (one sentence) | status | retirement-condition | last-verified |
|---|---|---|---|---|---|---|
| causal-analyzer-source-rec-id | `tests/architecture/causal-analyzer-source-rec-id.test.ts` | Trust Sprint T7.1 (2026-05-07) | rec → changelog → outcome causal chain in the analyzer script. | active | Refines with Section 6 (Primary Recommendation) — the scope-discipline rules carry over. | 2026-05-14 |

## Cross-cutting — Pure compute + pipeline integrity

| name | source test file | section | purpose (one sentence) | status | retirement-condition | last-verified |
|---|---|---|---|---|---|---|
| match-engine-purity | `tests/architecture/match-engine-purity.test.ts` | Phase 3 wiring (pre-plan) | Match engine is pure (no I/O); wired into the scan dual-write block separately. | active | Permanent — match-engine purity floor. | 2026-05-14 |
| measurement-quality-boundary-pin | `tests/architecture/measurement-quality-boundary-pin.test.ts` | S1 (operator audit, 2026-05-05) | Pins `NATIVE_REGIME_START = "2026-04-22"` as the single source of truth for the regime boundary. | active | Permanent — regime-boundary single source of truth. | 2026-05-14 |
| url-normalize-canonical-location | `tests/architecture/url-normalize-canonical-location.test.ts` | Trust Sprint T6.6 (2026-05-06) | URL normalize canonical location (single source of truth for URL canonicalization). | active | Permanent — URL canonicalization safety. | 2026-05-14 |
| diagnostics-no-module-level-state | `tests/architecture/diagnostics-no-module-level-state.test.ts` | Sprint 7 Phase 7.5c/4 (2026-04-25) | Diagnostics pages have no module-level state (per-render fresh reads). | active | Permanent — diagnostics correctness. | 2026-05-14 |

---

## Phase A.2 — Section 3 (Cross-Tenant Brain Activation)

| name | source test file | section | purpose (one sentence) | status | retirement-condition | last-verified |
|---|---|---|---|---|---|---|
| brain-config-and-thresholds-provenance | `tests/architecture/brain-config-and-thresholds-provenance.test.ts` | Section 3.1 / 3.3 / 3.5 (2026-05-14) | Pins canonical paths + exact env-var names + locked numeric/literal-string values + module purity + read-at-call-time contract for the cross-tenant brain's config + thresholds modules. | active | Refines when threshold values are revisited post-validation. No scheduled retirement; gates stay, numbers tunable per operator decision. | 2026-05-14 |
| brain-compute-tenant-thresholds-provenance | `tests/architecture/brain-compute-tenant-thresholds-provenance.test.ts` | Section 3.7 / E7 (2026-05-14) | Pins the per-tenant benchmark compute: canonical path, public entry point, single-source gate constant + locked percentiles (0.5/0.75/0.9), Profound fallback sourced only from `T2C_THRESHOLDS`, module purity, inclusion-rule rationale, locked exclusion of `live_not_yet_cited` + `stuck`. | active | Refines when per-action-type compute lands post-MVP or when the percentile choice is revisited. | 2026-05-14 |
| threshold-decision-loader-wiring | `tests/architecture/threshold-decision-loader-wiring.test.ts` | Section 3.7 / Step 3c (2026-05-14) | Pins the loader's threshold-decision wiring AFTER the A.2.3c atomic customer-visible flip: cross-tenant-brain imports, `buildTileStrings` import from `./render-copy`, `resolveTenantThresholdsCached` export + cache-key prefix, `threshold_decision: ThresholdDecision` field on `LifecycleForEdit`, BOTH `threshold_decision: ThresholdDecision` AND `tile_strings: LifecycleTileStrings` fields on `LifecycleSummary`, every `deriveLifecycleStage` call in the loader passes a 2nd thresholds argument referencing `thresholdDecision`, every `renderLifecycleCopy` call passes a `threshold_decision` field, AND the `edit-lifecycle-tile.tsx` import-boundary contract that the component does NOT import `T2C_THRESHOLDS` or `BORROWED_BENCHMARK_TOOLTIP`. | active | Refines when Phase A.3 (indexability) replaces the lifecycle-stage scaffold or a future phase unifies the tile read model. | 2026-05-14 |
| indexability-loader-tenant-isolation | `tests/architecture/indexability-loader-tenant-isolation.test.ts` | Section 4 / Step 3b + post-A.3.5 second-stage (2026-05-15) | Pins the read-only indexability loader's tenant-isolation contract: imports + calls `currentTenantId`, references `opts.tenantId`, throws fail-loud on context mismatch with the literal phrase "tenant context mismatch", filters `reconciliation.canonical_pages` by `tenantDomain` before the URL-membership check, and validates `state.siteDomain` against `tenantDomain` before consuming robots data via `normalizeHost(state.siteDomain) !== tenantDomain`. At least 4 `nullRobotsFlags()` exits cover the robots-defense ladder. After the post-A.3.5 second-stage Supabase mirrors landed, the per-tenant Supabase PKs (`public.robots_state.tenant_id`, `public.sitemap_reconciliation.tenant_id`) became the PRIMARY storage-layer boundary; the loader's two filters are retained as DEFENSE-IN-DEPTH. | active | Permanent at the architectural-pattern level. The defenses become redundant only when ALL upstream writers carry tenant_id correctness guarantees and no global-fallback paths remain. | 2026-05-15 |
| indexability-no-fresh-fetch | `tests/architecture/indexability-no-fresh-fetch.test.ts` | Section 4 / Step 3b (2026-05-14, updated A.3.b1.beta 2026-05-17) | Pins the read-only indexability loader's no-fetch contract: source contains none of `fetch(`, `fetchAndParseRobots`, `refreshRobotsState`, `axios`, `superagent`, `node-fetch`, `got(`; loader does NOT import LLM / cost-ledger / cross-tenant-brain / raw `@/lib/connectors/gsc/*` / raw-dotdata-json modules. A.3.b1.beta (2026-05-17): GSC integration is opt-in only — loader declares `enableGsc?: boolean` (default false; customer-facing callers preserve byte-equal pre-beta behavior) and imports `loadGscSignal` from the bounded sibling adapter `./load-gsc-signal` (NOT the raw `gscUrlInspect` client). The fetch-token list + non-GSC forbidden paths are permanent. | active | Permanent at the no-fetch core. GSC-specific guards refine when A.3.b3 lands additional GSC APIs. | 2026-05-17 |
| indexability-loader-import-allowlist | `tests/architecture/indexability-loader-import-allowlist.test.ts` | Section 4 / Step 3b + post-A.3.5 second-stage (2026-05-15) | Pins the read-only indexability loader's import surface to an explicit allowlist (`server-only`, `next/cache`, `./types`, `./compute-indexability`, `@/domains/pages/types`, `@/domains/pages/robots-parser`, `@/domains/citation-lifecycle/canonicalize-url`, `@/lib/business-config`, `@/lib/tenant-context`, `@/lib/persistence/repositories`). Page-snapshots, sitemap-reconciliation, AND robots-state all flow through the tenant-scoped repository now (post-A.3.5 second-stage). The standalone `@/domains/pages/sitemap-reconciliation-store` import was RETIRED — sitemap is read via `repo.getSitemapReconciliation()` against the new `public.sitemap_reconciliation` Supabase mirror after the classification flip from GLOBAL → TENANT_SCOPED. New imports must be added here AND in the loader together. | active | Refines when A.3.b1 lands the GSC connector (the allowlist gains the GSC client path). Permanent otherwise — the indexability domain's read-only loader contract is structural. | 2026-05-15 |
| robots-state-supabase-mirror | `tests/architecture/robots-state-supabase-mirror.test.ts` | Section 4 / post-A.3.5 second-stage (2026-05-15) | Pins the Phase A.3 robots-state Supabase persistence layer: `readRobotsState` and `writeRobotsState` in `src/domains/pages/robots-parser.ts` are async + take `{ tenantId }`; route through `getRepository().forTenant(...).getRobotsState()` / `.setRobotsState()`; `TenantRepository` declares both methods; supabase-backend's read soft-fails on PostgreSQL `42P01` (undefined_table) for sequencing model A; supabase-backend's write UPSERTs on `tenant_id` (fail-loud); `refreshRobotsState` signature retired the single-string param for `{ siteDomain, tenantId }`; the pre-A.3 flat-path `readFileSync(statePath(), ...)` is gone from the readRobotsState body. | active | Refines when a future schema_version > 1 lands; permanent otherwise. | 2026-05-15 |
| sitemap-reconciliation-supabase-mirror | `tests/architecture/sitemap-reconciliation-supabase-mirror.test.ts` | Section 4 / post-A.3.5 second-stage (2026-05-15) | Pins the Phase A.3 sitemap-reconciliation Supabase persistence layer: `store-classification.ts` lists `sitemap-reconciliation` in `TENANT_SCOPED_STORES` (NOT in `GLOBAL_STORES`); `TenantRepository` declares both `getSitemapReconciliation` + `setSitemapReconciliation`; indexability loader + operator diagnostics page read via `repo.getSitemapReconciliation()` (NOT the standalone `@/domains/pages/sitemap-reconciliation-store` import); `scripts/scan-owned-pages.ts` `saveReconciliation` is async + writes via `repo.setSitemapReconciliation()`; supabase-backend tenant-scoped read soft-fails on `42P01`; UPSERT on `tenant_id`. | active | Refines when a future schema_version > 1 lands; permanent otherwise. | 2026-05-15 |
| indexability-diagnostics-operator-only | `tests/architecture/indexability-diagnostics-operator-only.test.ts` | Section 4 / Step 5 (2026-05-14) | Pins the `/diagnostics/indexability` operator-gate contract: page exists at the canonical diagnostics path; imports `isOperatorModeServer` from `@/lib/operator-mode` and `notFound` from `next/navigation`; calls `notFound()` when `!isOperatorMode()`; allows `NODE_ENV === "test"` bypass for render coverage; Command Center operator-link carries `data-command-center-operator-link="true"` AND `data-command-center-operator-link-target="indexability"` AND lives inside the `isOperator ?` ternary; NO customer-facing nav/layout/component file (outside `command-center.tsx` and the page itself) references `/diagnostics/indexability`. | active | Permanent — operator-only diagnostic surfaces stay operator-only. | 2026-05-14 |
| indexability-diagnostics-no-fresh-fetch | `tests/architecture/indexability-diagnostics-no-fresh-fetch.test.ts` | Section 4 / Step 5 (2026-05-14, updated A.3.b1.beta 2026-05-17) | Pins the `/diagnostics/indexability` page as read-only over stored signals: source contains none of `fetch(`, `fetchAndParseRobots`, `refreshRobotsState`, `axios`, `superagent`, `node-fetch`, `got(`; NO imports from `@/lib/llm/`, `@/lib/cost/`, `@/domains/recommendations/cross-tenant-brain/`, any `*-client` HTTP-client convention path, raw `@/lib/persistence/dotdata-json`, `@/domains/scanning/` orchestrator, or the raw GSC client at `@/lib/connectors/gsc/*`. A.3.b1.beta (2026-05-17): page imports the bounded GSC adapter constant `GSC_INSPECT_PER_RENDER_LIMIT` from `@/domains/indexability/load-gsc-signal` and passes `enableGsc: true` + a budget-capped `gscBudget` to `loadIndexabilityForUrl`. GSC reads route through the loader → adapter → cache + bounded client; the page itself never invokes `gscUrlInspect` or any HTTP fetch directly. Every import path matches an explicit 13-entry allowlist. | active | Permanent at the no-fetch core. GSC-specific guards refine when A.3.b3 lands additional GSC APIs. | 2026-05-17 |
| lifecycle-eligibility-operator-only | `tests/architecture/lifecycle-eligibility-operator-only.test.ts` | Phase A.2 Step 3d (2026-05-18) | Pins the `/diagnostics/lifecycle-eligibility` operator-gate contract: page exists at the canonical diagnostics path; imports `isOperatorModeServer` from `@/lib/operator-mode` and `notFound` from `next/navigation`; calls `notFound()` when `!isAccessAllowed()` / `!isOperatorModeServer()`; allows `NODE_ENV === "test"` bypass for render coverage; the diagnostics hub page (`/diagnostics`) links to it with the `data-diagnostics-hub-link="lifecycle-eligibility"` attribute; NO customer-facing nav/layout file (outside the hub page and the page itself) references `/diagnostics/lifecycle-eligibility`. | active | Permanent — operator-only diagnostic surfaces stay operator-only. | 2026-05-18 |
| lifecycle-eligibility-no-mutation | `tests/architecture/lifecycle-eligibility-no-mutation.test.ts` | Phase A.2 Step 3d (2026-05-18) | Pins the read-only contract for `src/app/(shell)/diagnostics/lifecycle-eligibility/page.tsx` AND every module under `src/domains/lifecycle-eligibility/**`: forbidden patterns in comment-stripped source include Supabase mutation calls (`.insert(` / `.update(` / `.upsert(` / `.delete(`), match-runner triggers (`reconcileMatchRun`, `markRecommendedEditsAsShipped`, `markRecommendedEditAsLive`, `runMatchEngine`), LLM/paid-API surfaces (`@/lib/connectors/gsc/*`, `runProviderAndPersist`, `regenerateProposedText`, `new OpenAI(`, `new Anthropic(`), and `"use server"` action declarations. Every domain module is required to declare `import "server-only"` so a future refactor can't accidentally pull the operator-vocabulary reason taxonomy into a customer-facing client component. | active | Permanent — the diagnostic stays read-only. | 2026-05-18 |
| lifecycle-eligibility-no-customer-surface-import | `tests/architecture/lifecycle-eligibility-no-customer-surface-import.test.ts` | Phase A.2 Step 3d (2026-05-18) | Pins that `@/domains/lifecycle-eligibility/**` modules expose operator vocabulary (snake_case `LifecycleEligibilityReason` enum values, operator-readable `detail` strings, `FunnelCounters` field names) that would leak as customer copy if imported into a customer-facing surface. Customer-surface scan directories (`src/app/(shell)/today`, `recommendations`, `changes`, `prompts`, `local`, `competitors`, `settings`, plus the parallel `src/components/*` trees) must NOT contain the import pattern `from "@/domains/lifecycle-eligibility/**"`. Customer surfaces consume user-facing lifecycle copy from `@/domains/citation-lifecycle/render-copy` instead. | active | Permanent — operator vocabulary stays substrate-only. | 2026-05-18 |

## Section 6 — Primary Recommendation as First-Class Metric

| name | source test file | section | purpose (one sentence) | status | retirement-condition | last-verified |
|---|---|---|---|---|---|---|
| snapshot-builder-topic-null-primary-rec | `tests/architecture/snapshot-builder-topic-null-primary-rec.test.ts` | Section 6 C2 / H8 lock (2026-05-15) | Pins that BOTH daily-metric-snapshot builders (native polling `src/domains/daily-metric-snapshots/build-from-observations.ts` AND legacy Profound import `src/derivations/snapshot-builder.ts`) route topic-scope rows to `primary_recommendation_count: null` via an EXPLICIT source-text literal: the native builder's topic emit block contains the literal `primary_recommendation_count: null` pair (no computed value, no accumulator read, no ternary); the Profound builder's emit block carries a ternary `acc.scope_type === "topic" ? null : …` so topic rows force null regardless of accumulator state. Topic-scope primary share has no v1 consumer; locked NULL until a future phase introduces one. Companion runtime tests `tests/domains/daily-metric-snapshots/build-from-observations-primary-rec.test.ts` + `tests/derivations/snapshot-builder-primary-rec.test.ts` cover the runtime behavior. | active | Refines if Section 6 introduces topic-scope primary share OR retires the legacy Profound builder. | 2026-05-15 |
| section6-c3-no-account-emission | `tests/architecture/section6-c3-no-account-emission.test.ts` | Section 6 C3 / C1.1 contract (2026-05-15) | Pins that `scripts/backfill-section6-primary-recommendation.ts` never emits, inserts, updates, or upserts a `daily_metric_snapshots` row with `scope_type = "account"`: exports the `C3AccountEmissionError` runtime kill-switch class, invokes it when `row.scope_type === "account"`, has no `scope_type: "account"` row-literal anywhere, restricts the `ExistingRowUpdate` type to `"entity" \| "topic" \| "platform"`, and routes all writes through exactly one `.upsert(` call site inside the prompt-scope helper (zero `.insert(` call sites). Enforces the C1.1 column-comment correction at the backfill layer; combined with `snapshot-builder-topic-null-primary-rec`, the entire Section 6 emission set is locked. | active | Refines if a future post-cron account aggregator phase introduces an explicit account-emission code path. | 2026-05-15 |
| section6-c3-dry-run-default | `tests/architecture/section6-c3-dry-run-default.test.ts` | Section 6 C3 / operator-safety convention (2026-05-15) | Pins that `scripts/backfill-section6-primary-recommendation.ts` defaults to dry-run mode: `parseArgs` initializes `commit: false`, the only path that flips commit-true is the literal `--commit` flag, every `deps.applyExistingUpdate` / `deps.applyPromptUpserts` call site appears AFTER the `if (!args.commit)` early-return guard (no unconditional writes), the mode label includes both `"DRY-RUN"` and `"COMMIT"` literals, and `--tenant=<id>` is REQUIRED at parse time (no silent BEACON_TENANT_ID fallback for cross-tenant safety). Lifts the Phase 2A operator-script convention (`scripts/backfill-snapshot-extensions.ts`) to an architecture invariant; protects future drive-by edits from flipping default-write semantics. | active | Permanent — operator-script dry-run default is a floor. | 2026-05-15 |
| today-primary-share-source | `tests/architecture/today-primary-share-source.test.ts` | Section 6 C4b / customer-visible source-swap (2026-05-15) | Pins the Today v2 hero's primary-recommendation pct source contract: `src/app/(shell)/today-v2-data.ts` imports `computeTodayPrimaryShare` from the C4a helper module + invokes it exactly once; the active production client `src/app/(shell)/today-v2-visibility-group-client.tsx` does NOT contain the legacy `platformPrimaryPct` closure body, does NOT read `sparkline.points[i].primaryRate` for hero pct values, and consumes `primaryShare.chatgptPrimaryPct` + `primaryShare.perplexityPrimaryPct` as props. Separately confirms `sampleStatus` reads remain (separate per-platform signal not coupled to the swap). Legacy `today-client.tsx` + `today-v2-client.tsx` intentionally NOT pinned per J2 (not on production hot path). Companion runtime test `tests/components/today/today-v2-visibility-group-client-primary-share-source.test.tsx` proves the rendered HTML reflects the prop, not the sparkline. | active | Refines when legacy today-client.tsx + today-v2-client.tsx are retired and the swap is extended to those paths. | 2026-05-15 |
| today-primary-share-tenant-isolation | `tests/architecture/today-primary-share-tenant-isolation.test.ts` | Section 6 C4b / tenant scope discipline (2026-05-15) | Pins two source-text contracts: (1) every `computeTodayPrimaryShare(` invocation in `src/app/(shell)/today-v2-data.ts` is preceded within the same function body by a `getRepository().forTenant(` call supplying the `repo` argument; (2) the helper module `src/domains/daily-metric-snapshots/today-primary-share.ts` does NOT import `getRepository` from `@/lib/persistence/repositories` and does NOT reference the identifier in its active (comment-stripped) source. Enforces the C4a caller-bound tenant-scope design — the helper consumes a pre-bound `TodayPrimaryShareRepo` and never constructs its own repository instance. | active | Permanent — caller-bound tenant scope is a structural contract; refines only if the helper ever needs to construct its own repo (which would itself require an operator-locked decision). | 2026-05-15 |
| prompt-primary-share-source | `tests/architecture/prompt-primary-share-source.test.ts` | Section 6 C5 / Prompts detail v2 source + tenant isolation (2026-05-15) | Pins three contracts for the Prompts detail v2 per-prompt primary-share sub-line: (1) `src/app/(shell)/prompts/[id]/page.tsx` imports `computePromptPrimaryShare` from the C5 helper module and invokes it exactly once; (2) the helper invocation is preceded within the same function body by a `getRepository().forTenant(` call supplying the `repo` argument; (3) the helper module `src/domains/daily-metric-snapshots/prompt-primary-share.ts` does NOT import `getRepository` and does NOT reference the identifier in its active (comment-stripped) source. Enforces the C5 caller-bound tenant-scope design — the helper consumes a pre-bound `PromptPrimaryShareRepo` and never constructs its own repository instance. Mirrors the parallel `today-primary-share-*` invariants for the prompt-scope helper. | active | Permanent — caller-bound tenant scope is a structural contract. | 2026-05-15 |
| prompts-detail-force-dynamic | `tests/architecture/prompts-detail-force-dynamic.test.ts` | Operator surface prerender safety / Section 6 C5 (2026-05-15) | Pins that `src/app/(shell)/prompts/[id]/page.tsx` declares `export const dynamic = "force-dynamic"` so Vercel does NOT statically prerender the prompt detail (which now performs a tenant-scoped `daily_metric_snapshots` read via `computePromptPrimaryShare`). The directive opts the page into per-request rendering; the Supabase read only executes when an operator actually visits — same fix shape as the `/diagnostics` prerender flake (commit 2bc08aa). Defense-in-depth: pins that the existing invalid-id guard (`decodePromptRouteId(id) === null` → `<PromptDetailV2NotFound />`) remains intact, so a future drive-by edit can't drop both directive AND guard simultaneously. | active | Permanent — customer surfaces that read Supabase should never prerender. | 2026-05-15 |
| change-primary-evidence-loader-tenant-scope | `tests/architecture/change-primary-evidence-loader-tenant-scope.test.ts` | Section 6 C6a / Changes detail primary-recommendation evidence loader tenant-scope (2026-05-15) | Pins six source-text contracts on `src/domains/citation-lifecycle/load-change-primary-evidence.ts`: (1) imports `getRepository` from `@/lib/persistence/repositories`; (2) active source calls `getRepository().forTenant(tenantId)` inside the cache body; (3) wraps the read in `unstable_cache(...)`; (4) cache key array literal includes the four required slots — `"change-primary-evidence:v1"`, `tenantId`, `recommendedEdit.id`, and a `recommendedEdit.live_at` reference; (5) declares `revalidate: 60 * 60 * 6` (or `21600`) — H7 6h TTL; (6) tags writes with `recommended_edits:${tenantId}` so existing invalidation flows through. Plus negative guards: no `createServiceClient(` direct call, no module-level repo cache. C6a holds the entire tenant-binding boundary at the loader; pure helpers stay free of `getRepository`. | active | Permanent — caller-bound tenant scope is a structural contract; refines only if the loader's caching shape itself changes. | 2026-05-15 |
| change-primary-pure-modules-no-getRepository | `tests/architecture/change-primary-pure-modules-no-getRepository.test.ts` | Section 6 C6a / pure-module purity (2026-05-15) | Pins that the three pure C6a modules — `src/domains/citation-lifecycle/change-primary-mode-a.ts`, `change-primary-mode-b.ts`, `change-primary-evidence-copy.ts` — do NOT import `getRepository` from `@/lib/persistence/repositories` and do NOT reference the bare identifier in their active (comment-stripped) source. Mirrors the parallel C5 `prompt-primary-share` invariant: tenant isolation lives at the loader boundary; pure helpers consume already-fetched, already-tenant-scoped data. | active | Permanent — caller-bound tenant scope is a structural contract. | 2026-05-15 |
| change-primary-evidence-forbidden-vocab | `tests/architecture/change-primary-evidence-forbidden-vocab.test.ts` | Section 6 C6a / Section 12 N4 carry-over (2026-05-15) | Pins that `src/domains/citation-lifecycle/change-primary-evidence-copy.ts` (the only C6a module that emits customer-visible text) contains NONE of the following forbidden substrings in its active (comment-stripped, template-substitution-stripped) source: causal verbs (`drove`, `caused`, `generated`, ` made `, `led to`); revenue framing (`$`, `revenue`, `dollars`, `sales`, `leads`); internal Mode labels (`Mode A`, `Mode B`, `Mode C`); customer-schema enum names (`primary_recommendation_count`, `total_possible`, `scope_type`, `claimable`). Status enum identifiers (`pass` / `still_learning` / `silent`) are intentionally allowlisted as TS discriminator strings (never rendered to customers). Pre-emptively enforces Section 12 N4 on the first customer-facing surface that introduces causal-attribution risk. | active | Refines when Section 12 N4's global vocabulary catalog ships — at which point this localized scan becomes a defense-in-depth subset. | 2026-05-15 |
| change-primary-evidence-page-source | `tests/architecture/change-primary-evidence-page-source.test.ts` | Section 6 C6b / Changes detail page source + v2-branch invocation contract (2026-05-15) | Pins seven source-text contracts on `src/app/(shell)/changes/[id]/page.tsx`: (1) imports `loadChangePrimaryEvidence` from the C6a loader module; (2) imports `getBusinessConfig` from `@/lib/business-config`; (3) contains EXACTLY ONE active-source invocation `loadChangePrimaryEvidence(`; (4) the invocation site appears AFTER `if (useV2)` and BEFORE the v2-branch `<ChangeDetailV2Client` JSX return — v1 (legacy) branch NEVER invokes the loader; (5) the invocation is wrapped in a `try { … } catch (error) { … }` block (additive-safety carry from C5); (6) the catch block emits `console.warn("[section6-c6] change primary evidence failed", …)` with `tenantId`, `changeId`, `recommendedEditId`, and a narrowed `error: error instanceof Error ? error.message : String(error)`; (7) the catch block does NOT leak the raw error binding, `error.stack`, or the `SUPABASE_SERVICE_ROLE_KEY` to logs. Companion render contracts pinned by `change-detail-v2-primary-evidence.test.tsx`; client-boundary contracts by `change-primary-evidence-client-boundary.test.ts`. | active | Permanent — page-level invocation safety + v2-only gate are structural contracts. | 2026-05-15 |
| change-primary-evidence-client-boundary | `tests/architecture/change-primary-evidence-client-boundary.test.ts` | Section 6 C6b / client prop boundary (2026-05-15) | Pins five source-text contracts on `src/app/(shell)/changes/[id]/change-detail-v2-client.tsx`: (1) `ChangeDetailV2Props` includes an OPTIONAL `primaryEvidenceLines?:` field (optional so existing fixtures instantiating the client without the new prop still compile); (2) active source does NOT reference C6a loader-result type names `ChangePrimaryModeAResult` / `ChangePrimaryModeBResult` / `LoadChangePrimaryEvidenceResult`; (3) active source does NOT reference per-mode identifier names `modeA` / `modeB`; (4) active source does NOT call the C6a loader directly (identifier `loadChangePrimaryEvidence` absent); (5) client does NOT import `getRepository` (presentation-only boundary; tenant scope lives in the loader). The `raw` identifier is NOT forbidden by this invariant because it's a common variable name elsewhere in the codebase and the other guards are sufficient. | active | Permanent — narrow client prop boundary keeps raw Mode A/B internals server-side. | 2026-05-15 |
| change-primary-evidence-customer-copy | `tests/architecture/change-primary-evidence-customer-copy.test.ts` | Section 6 C6b / Section 12 N4 carry-over on the v2 Changes detail client (2026-05-15) | Pins that `src/app/(shell)/changes/[id]/change-detail-v2-client.tsx` (active source, comment-stripped + template-substitution-stripped) contains NONE of the forbidden customer-vocab set: causal verbs (`drove`, `caused`, `generated`, ` made `, `led to`); revenue framing (`revenue`, `dollars`, `sales`, `leads`); internal Mode labels (`Mode A`, `Mode B`, `Mode C`); customer-schema enum names (`primary_recommendation_count`, `total_possible`, `scope_type`, `claimable`, `still_learning`). Defense-in-depth — the C6a forbidden-vocab invariant covers the copy module (source of customer-visible lines); this companion guards against a future drive-by hard-coding override copy directly in the v2 client. `ChatGPT` / `Perplexity` platform labels are intentionally allowlisted (no forbidden overlap). The literal `$` is NOT forbidden in the client because the codebase uses `${...}` template substitution syntax extensively in unrelated code; the substitution-strip preprocessing keeps the scan honest. | active | Refines when Section 12 N4's global vocabulary catalog ships — at which point this localized scan becomes a defense-in-depth subset. | 2026-05-15 |

## Operator surface safety

| name | source test file | section | purpose (one sentence) | status | retirement-condition | last-verified |
|---|---|---|---|---|---|---|
| diagnostics-pages-force-dynamic | `tests/architecture/diagnostics-page-dynamic.test.ts` | Operator surface prerender safety (2026-05-15) | Pins that the operator-only `/diagnostics` index AND every sibling diagnostics/* page (`brain`, `spikes`, `indexability`) declares `export const dynamic = "force-dynamic"` so Vercel does NOT attempt static prerender of operator-mode pages. Operator pages execute tenant-scoped Supabase reads (e.g., `repo.getPageSnapshots()` at `diagnostics/page.tsx:261`) that exceed the build-time statement_timeout on slow builds, producing nondeterministic deployment failures (e.g., commit 730a6de's Vercel build on 2026-05-15). The directive opts each page into dynamic per-request rendering so the Supabase calls only run when an operator actually visits. Defense-in-depth: also pins that the index page's `isOperatorMode()` + `notFound()` gate remains intact so a future drive-by edit can't drop both directive AND gate simultaneously. | active | Permanent — operator surfaces should never prerender. | 2026-05-15 |

## Section 7 — Off-Site Authority + Local-Service AEO Wedge

| name | source test file | section | purpose (one sentence) | status | retirement-condition | last-verified |
|---|---|---|---|---|---|---|
| off-site-authority-pure-module-purity | `tests/architecture/off-site-authority-pure-module-purity.test.ts` | Section 7 C7a / pure-module purity (2026-05-16) | Pins that the two pure off-site-authority modules `src/domains/off-site-authority/types.ts` and `compute-snapshot.ts` do NOT import any global / hybrid-scope helper: `getBusinessConfig`, `isPlaceholderConfig`, `readLocalReviews`, `getConnectorToken`, `getGoogleConnectorToken`, `getYelpConnectorToken`, `currentTenantId`, `currentTenantSlug`, `getRepository`. All inputs flow in as arguments from the loader; a future drive-by adding any of these imports would blur the tenant-scope boundary the loader carries. | active | Permanent — pure helpers consume already-resolved inputs only. | 2026-05-16 |
| off-site-authority-loader-allowed-imports | `tests/architecture/off-site-authority-loader-allowed-imports.test.ts` | Section 7 C7a / loader import allowlist (2026-05-16) | Pins that `src/domains/off-site-authority/load-snapshot.ts` imports ONLY from this explicit allowlist: `server-only`, `@/lib/business-config`, `@/lib/local-reviews-store`, `@/lib/connector-store`, `@/lib/tenant-context`, `./types`, `./compute-snapshot`. Specifically forbids any import from `@/lib/persistence/repositories` (no `getRepository`), `@/lib/connectors/*` (no new connector wiring in C7a), and any HTTP-client identifier (`fetch(`, `axios`, `httpsAgent`). The catalog row `off-site-authority-multi-tenant-prerequisite` documents the underlying limitation that motivates this strict import boundary. | active | Permanent — single named-import boundary for tenant-scope discipline. | 2026-05-16 |
| off-site-authority-no-paid-or-scan-or-llm | `tests/architecture/off-site-authority-no-paid-or-scan-or-llm.test.ts` | Section 7 C7a / locked invariant #4 carry-over (2026-05-16) | Scans active source across every C7a file (`types.ts`, `compute-snapshot.ts`, `load-snapshot.ts`, the operator diagnostic page) and forbids HTTP-client identifiers (`fetch(`, `axios`, `httpsAgent`), paid-API URL hosts (`mybusiness.googleapis.com`, `api.yelp.com`, `searchanalytics`, `urlInspection`, `searchconsole`), and LLM-provider identifiers (`OpenAI`, `Anthropic`, lowercase variants, `BEACON_LLM_PROVIDER`). Carries Section 7 invariant #4 ("no paid APIs in v1 without explicit approval") + the LLM-free posture into a structural check that survives drive-by edits. | active | Permanent — C7a's read-only-from-existing-data posture is non-negotiable. | 2026-05-16 |
| off-site-authority-customer-surface-isolation | `tests/architecture/off-site-authority-customer-surface-isolation.test.ts` | Section 7 C7a / operator-only scope (2026-05-16) | Pins that no C7a file imports from any customer-facing app shell path (`@/app/(shell)/today`, `recommendations`, `changes`, `prompts`, `local`) including relative-path variants (`../../today`, etc.). C7a is OPERATOR-ONLY in this commit; customer-facing tile work is C7d/C7e and is gated behind the multi-tenant prerequisite. Defense-in-depth — catches a future drive-by that tries to surface off-site authority on Today before the prerequisite ships. | active | Refines when C7d/C7e ship after the multi-tenant prerequisite — at which point the customer-facing paths will be added back to the allowlist with their own scope-discipline invariants. | 2026-05-16 |
| off-site-authority-operator-page-discipline | `tests/architecture/off-site-authority-operator-page-discipline.test.ts` | Section 7 C7a / operator-page structural contract (2026-05-16; refined for C7c 2026-05-16) | Pins six contracts on `src/app/(shell)/diagnostics/off-site-authority/page.tsx`: (1) declares `export const dynamic = "force-dynamic"`; (2) imports `isOperatorModeServer` from `@/lib/operator-mode`; (3) imports `notFound` from `next/navigation`; (4) active source contains both `isOperatorModeServer(` and `notFound(` invocations; (5) the operator gate fires BEFORE the **first off-site data loader invocation** — the check detects the earliest allowed loader among `loadOffSitePresenceSnapshot(` and `loadOffSiteRecommendationPreview(` (the C7c thin wrapper that internally calls the C7a snapshot loader), asserts at least one is present, and pins `gateIdx < firstLoaderIdx`; (6) the page does NOT import `@/lib/business-config` directly — brand name + placeholder state flow through the loader's snapshot only. The C7c refinement preserves the original safety contract (operator gate before any off-site data load) while accepting the new wrapper-loader API. | active | Permanent — operator-gate-before-loader is a structural safety contract. Refines further when additional off-site loaders ship (e.g., C7d Today tile loader) — extend the `ALLOWED_LOADER_INVOCATIONS` list. | 2026-05-16 |
| off-site-authority-customer-copy | `tests/architecture/off-site-authority-customer-copy.test.ts` | Section 7 C7a / locked invariant #3 + Section 12 N4 carry-over (2026-05-16) | Extracts string-literal contents + template-literal static text segments from the operator diagnostic page (skipping `${...}` substitution expressions via a character-level walker) and forbids: causal verbs (`drove`, `caused`, `generated`, ` made `, `led to`), revenue framing (`$`, `revenue`, `dollars`, `sales`, `leads`), operator-only Mode labels (`Mode A`, `Mode B`, `Mode C`), internal schema names (`primary_recommendation_count`, `total_possible`, `scope_type`, `claimable`, `still_learning`), and Section 7 additions: ` GBP ` with surrounding spaces (full-form "Google Business Profile" required in rendered text) + `missing` (scary form — "did not find a confirmed" required per locked invariant #3). Operator surfaces inherit the same vocabulary floor as customer surfaces so a future drive-by promoting an operator page can't ship hostile copy. | active | Refines when Section 12 N4's global vocabulary catalog ships — at which point this localized scan becomes a defense-in-depth subset. | 2026-05-16 |
| off-site-authority-multi-tenant-prerequisite | `docs/ARCHITECTURE_INVARIANTS_CATALOG.md` (this row) + the `data_sources_note` field on `OffSitePresenceSnapshot` | Section 7 C7a / multi-tenant prerequisite (2026-05-16) | Documentation-only row. Names the underlying limitation that motivates C7a's strict operator-only scope: `@/lib/business-config` and `@/lib/connector-store` are PROCESS-GLOBAL today (no `currentTenantSlug` routing) — only `local-reviews` is request-scoped per-tenant. The `data_sources_note` array on every `OffSitePresenceSnapshot` surfaces this verbatim in the operator diagnostic footer so the limitation is visible at the point of use. **Hard prerequisite**: Section 7 customer surfaces (C7d Today tile, C7e Recommendations integration) MUST NOT ship for Customer 2 until a multi-tenant-store workstream lands that gives both helpers `currentTenantSlug`-aware routing. Until then, Section 7 is single-tenant (Ritz-only) in production. | active | Retires when the multi-tenant workstream lands AND business-config + connector-store both route per `currentTenantSlug`. At that point this row converts to a source-pinned invariant or is replaced by a stricter `forTenant` contract on the loader. | 2026-05-16 |
| off-site-action-types-not-llm-allowed | `tests/architecture/off-site-action-types-not-llm-allowed.test.ts` | Section 7 C7b / LLM exclusion contract (2026-05-16) | Pins that the 7 off-site/manual action types added in C7b (`claim_gbp`, `optimize_gbp_profile`, `request_gbp_reviews`, `claim_or_optimize_houzz`, `claim_or_optimize_yelp`, `submit_to_industry_directory`, `pursue_local_pr`) NEVER reach the LLM specific-edit provider via four independent proofs: (1) runtime — every off-site type's `ACTION_TYPE_REGISTRY[t].generatorActive` is `false`; (2) runtime — exported `listActiveActionTypes()` excludes all 7 (parallel logic to non-exported `defaultAllowedActionTypes()` in `specific-edit-evidence.ts`); (3) source-text — `specific-edit-evidence.ts:defaultAllowedActionTypes` body matches the canonical `ACTION_TYPES.filter((t) => ACTION_TYPE_REGISTRY[t].generatorActive)` shape (no parallel allowlist); (4) source-text — no off-site type string literal appears inside any `allowedActionTypes: […]` construction anywhere under `src/domains/recommendations/`. The pre-flight rule "do not widen exports for tests" is honored: `defaultAllowedActionTypes` is intentionally not imported — its shape is pinned by regex on the function body. | active | Retires when C7c lands a deliberate, operator-approved generator path for off-site recs (with its own LLM-safety contract). Until then, this invariant blocks accidental LLM generation. | 2026-05-16 |
| off-site-action-types-no-write-paths | `tests/architecture/off-site-action-types-no-write-paths.test.ts` | Section 7 C7b / locked invariants #1 + #4 + #5 carry-over (2026-05-16) | Scans active (comment-stripped) source across the two C7b production files — `src/domains/recommendations/action-types.ts` and `recommendation-action-rows.ts` — and forbids: external write-API hosts (`mybusiness.googleapis.com`, `api.yelp.com`, `houzz.com/api`, `angi.com/api`, `bbb.org/api`); automation language (`auto-claim`, `auto-post`, `auto-review-request`, `automated outreach`); HTTP-client identifiers (`fetch(`, `axios`, `httpsAgent`); LLM-provider identifiers (`OpenAI`, `Anthropic`, `BEACON_LLM_PROVIDER`). Defense-in-depth — the 7 locked off-site types are recommendations Beacon GIVES, never actions Beacon PERFORMS; this scan catches any future drive-by that tries to wire automation into the registry or row builder. | active | Permanent — read-only/manual posture is structural for the off-site family. | 2026-05-16 |
| off-site-action-types-customer-vocab | `tests/architecture/off-site-action-types-customer-vocab.test.ts` | Section 7 C7b / locked invariant #3 + Section 12 N4 carry-over (2026-05-16) | Pins that each of the 7 off-site action types' `operatorLabel` string contains NONE of the forbidden customer-vocab set: causal verbs (`drove`, `caused`, `generated`, ` made `, `led to`), revenue framing (`$`, `revenue`, `dollars`, `sales`, `leads`), operator-only Mode labels (`Mode A`, `Mode B`, `Mode C`), internal schema enum names (`primary_recommendation_count`, `total_possible`, `scope_type`, `claimable`, `still_learning`), Section 7 additions (` GBP ` standalone — full-form "Google Business Profile" required, `missing` — soft "did not find a confirmed" required). The `operatorLabel` surfaces customer-visible via `src/app/(shell)/recommendations/[id]/recommendation-detail-not-found.tsx:35` (lowercased into a 404 message), so even though off-site recs are not generated in C7b, a stale deep-link to an off-site rec id would render the label. | active | Refines when Section 12 N4's global vocabulary catalog ships — at which point this localized scan becomes a defense-in-depth subset. | 2026-05-16 |
| off-site-action-types-suggested-copy-suppressed | `tests/architecture/off-site-action-types-suggested-copy-suppressed.test.ts` | Section 7 C7b / locked invariant #7 carry-over (2026-05-16) | Pins that off-site action rows never render an Act 4 Suggested Copy tile via three proofs: (1) the exported `SUGGESTED_COPY_ACTION_ROW_TYPES` allowlist does NOT include `"review_decision"`; (2) the exported `actionRowTypeForEdit(<each off-site type>)` returns `"review_decision"` for all 7 — i.e. into the suppressed branch — and `supportsSuggestedCopy(actionRowTypeForEdit(t))` is `false` for each; (3) source-text on `recommendation-action-rows.ts` shows exactly 7 `case "<off-site type>":` arms inside `actionRowTypeForEdit`, each routing to a single `return "review_decision";` statement (no spillover into the existing page-lifecycle arms). The 7 off-site types reuse the existing `"review_decision"` row type rather than introducing a new `ActionRowType` value, which keeps the customer-facing `recommendations-client.tsx` `COMPACT_LABEL` exhaustive map untouched in C7b. When C7e introduces a dedicated off-site row type for the customer queue, this mapping flips there. | active | Refines at C7e when a dedicated off-site row type lands; the suppression contract carries forward but the assertion shape evolves. | 2026-05-16 |
| off-site-recommendation-rules-pure-purity | `tests/architecture/off-site-recommendation-rules-pure-purity.test.ts` | Section 7 C7c / pure-rules module purity (2026-05-16) | Pins that `src/domains/off-site-authority/recommendation-rules.ts` stays as a strict pure-compute module: no imports of `getRepository`, `getBusinessConfig`, `isPlaceholderConfig`, `readLocalReviews`, `getConnectorToken` / `getGoogleConnectorToken` / `getYelpConnectorToken`, `currentTenantId`, `currentTenantSlug`; no imports from `@/lib/persistence/repositories`, `@/lib/persistence/cold-store`, `@/lib/connectors/*`, or `next/cache`; no LLM-provider identifiers in active source; imports restricted to exactly `./types` and `@/domains/recommendations/action-types`, both TYPE-ONLY. Type-only enforcement uses a per-line check that accepts `import type { … }` and `import { type X, type Y }` forms. Together these contracts keep the C7c decision tree safely consumable by the server wrapper without any module-load side-effect. | active | Permanent — pure helpers consume already-resolved inputs only. | 2026-05-16 |
| off-site-recommendation-rules-no-persistence | `tests/architecture/off-site-recommendation-rules-no-persistence.test.ts` | Section 7 C7c / locked invariants #1 + #4 + #5 + #7 carry-over (2026-05-16) | Scans active (comment-stripped) source across the three C7c production files — `src/domains/off-site-authority/recommendation-rules.ts`, `load-recommendation-candidates.ts`, and `src/app/(shell)/diagnostics/off-site-authority/page.tsx` — and forbids: persistence write patterns (`runProviderAndPersist`, `saveRecommendedEdit`, `getRecommendedEdits`, `.insert(`, `.upsert(`, `.update(`); external write-API hosts (`mybusiness.googleapis.com`, `api.yelp.com`, `houzz.com/api`, `angi.com/api`, `bbb.org/api`); automation language (`auto-claim`, `auto-post`, `auto-review-request`, `automated outreach`); HTTP-client identifiers (`fetch(`, `axios`, `httpsAgent`); LLM-provider identifiers (`OpenAI`, `Anthropic`, `BEACON_LLM_PROVIDER`). C7c is preview-only — it writes zero `recommended_edits` rows, calls no external API, and runs no LLM. | active | Permanent — Section 7's read-only / manual / preview-only posture is structural. | 2026-05-16 |
| off-site-recommendation-rules-customer-copy | `tests/architecture/off-site-recommendation-rules-customer-copy.test.ts` | Section 7 C7c / locked invariant #3 + Section 12 N4 carry-over (2026-05-16) | Extracts visible-text candidates (string-literal contents + template-literal static segments, skipping `${...}` substitution expressions via a character-level walker) from `recommendation-rules.ts` and the operator diagnostic `page.tsx`, and forbids: causal verbs (`drove`, `caused`, `generated`, ` made `, `led to`); revenue framing (`$`, `revenue`, `dollars`, `sales`, `leads`); operator-only Mode labels (`Mode A`, `Mode B`, `Mode C`); internal schema enum names (`primary_recommendation_count`, `total_possible`, `scope_type`, `claimable`, `still_learning`); automation language (`auto-claim`, `auto-post`, `auto-review-request`, `automated outreach`); Section 7 additions (` GBP ` standalone — full-form "Google Business Profile" required, `missing` — soft "did not find a confirmed" required); AI-citation / ranking outcome promises (`will improve rankings`, `will make AI cite you`, `will drive`, `you must`, `you need to`). Pinned both the rule-string source AND the operator-page rendered copy so a future drive-by adding hostile rationale text on either side is caught. Allowlist: "Google Business Profile" full form, "Better Business Bureau", "Houzz", "Yelp", "Angi". | active | Refines when Section 12 N4's global vocabulary catalog ships — at which point this localized scan becomes a defense-in-depth subset. | 2026-05-16 |
| off-site-profile-urls-source-discipline | `tests/architecture/off-site-profile-urls-source-discipline.test.ts` | Section 7 C7g v1 / operator-vouched profile-URL source discipline (2026-05-16) | Pins the C7g v1 contract that operator-entered off-site profile URLs (`houzzProfileUrl`, `angiProfileUrl`, `bbbProfileUrl`, `industryDirectoryProfileUrl`) flow through a single validation gate — the `operatorVouchedProfileUrl` helper in `src/domains/off-site-authority/compute-snapshot.ts` — that accepts ONLY `https://`- or `http://`-prefixed strings. The resulting `OffSiteChannelState` carries `source: "business_config"` and `confidence: "medium"`; Beacon does NOT HTTP-verify the URL in v1. Pinned via six checks: (1) `operatorVouchedProfileUrl` function exists in compute; (2) both `"https://"` and `"http://"` literals present inside the validator; (3) `buildConfiguredRow` hard-codes `source: "business_config"` AND `confidence: "medium"`; (4) the four `BusinessConfig` fields are declared with `string` type in `src/lib/business-config.ts`; (5) the loader threads all four fields by name; (6) the operator diagnostic page branches on `row.source === "business_config"` and renders `"Configured"` vs `"Confirmed"` accordingly. A drive-by that widens the gate (e.g., accepts `mailto:`/`javascript:`/bare-domain), promotes an operator-entered URL to `"connector_api"`/`"high"`, or merges the Configured/Confirmed labels will fail this test. | active | Refines at C7g v2 when HTTP HEAD verification ships — at which point an additional `source: "head_verified"` path appears alongside `business_config`, and the validator may accept additional URL shapes (e.g., relative redirects from the verifier). | 2026-05-16 |
| off-site-profile-urls-no-fetch | `tests/architecture/off-site-profile-urls-no-fetch.test.ts` | Section 7 C7g v1 / no-HTTP-verification posture (2026-05-16) | Pins the C7g v1 contract that operator-entered profile URLs are NEVER fetched, HEAD-checked, or rendered as live links. Scans comment-stripped active source across four C7g files (`src/lib/business-config.ts`, `src/domains/off-site-authority/load-snapshot.ts`, `compute-snapshot.ts`, the operator diagnostic page) and forbids: HTTP-client identifiers (`fetch(`, `axios`, `httpsAgent`) AND anchor-tag rendering (`<a href=`). Profile URLs render as plain-text only — the operator must visually verify the URL before trusting it. C7g v2 (HTTP HEAD verification) will revisit the fetch ban via an additive invariant; this row pins the v1 read-only-from-config posture. | active | Retires at C7g v2 when HTTP HEAD verification ships — at which point the `fetch(` ban moves to a stricter site-scoped check (e.g., must be a polite Beacon HEAD wrapper, not raw `fetch`). The `<a href=` ban remains until C7d/C7e ship customer-facing link surfaces. | 2026-05-16 |
| off-site-profile-urls-no-customer-surface | `tests/architecture/off-site-profile-urls-no-customer-surface.test.ts` | Section 7 C7g v1 / operator-only scope carry-over (2026-05-16) | Pins that the four operator-only `BusinessConfig` profile URL fields (`houzzProfileUrl`, `angiProfileUrl`, `bbbProfileUrl`, `industryDirectoryProfileUrl`) appear nowhere under the locked customer-facing surface tree: `src/app/(shell)/{today,recommendations,changes,prompts,local}/**` and `src/components/{today,recommendations,changes,prompts,local}/**`. Walks each tree, scans every `.ts` / `.tsx` file's comment-stripped active source, and asserts zero occurrences of each field name. Customer-facing off-site surfaces (C7d/C7e) remain BLOCKED by the catalog row `off-site-authority-multi-tenant-prerequisite`; this invariant keeps the C7g v1 read-side scope strictly operator-only so a drive-by adding a Today tile or Recommendations card that consumes these URLs fails loudly. | active | Refines when C7d/C7e ship after the multi-tenant prerequisite — at which point a customer-facing off-site surface will need its OWN scope-discipline invariant (e.g., URLs flow only through a server-rendered cell that visually disambiguates "operator-vouched" vs "Beacon-verified"). | 2026-05-16 |
| off-site-customer-tile-safe-copy | `tests/architecture/off-site-customer-tile-safe-copy.test.ts` | Section 7 C7d / off-site customer tile safe copy (2026-05-22) | Pins the customer-visible vocabulary floor of the FIRST customer-facing off-site surface (`src/components/today/off-site-authority-tile.tsx`). The existing `off-site-authority-customer-copy` invariant scans ONLY the operator diagnostic page, so the tile would otherwise be unguarded. Extracts the tile's customer-visible text (string + template-literal static segments, skipping `${...}` so the `$` check is robust) and forbids: scary framing (`missing`, `we checked`), automation framing (`automatically`, `request reviews now`), causal/ranking/revenue (`improve rankings`, `drove`, `caused`, `generated`, `led to`, ` made `, `revenue`, `dollars`, `sales`, `leads`, `guaranteed`, `$`), operator-only Mode labels, and the ` GBP ` abbreviation (full "Google Business Profile" required). Also scans the tile SOURCE and forbids the two policy-risk action-type identifiers (`request_gbp_reviews`, `pursue_local_pr`) so the tile can never name/render them. Positive sanity: tile uses `Connected`/`Configured`/`Worth a manual review` + the 3 allowlisted non-policy-risk actions (`claim_gbp`, `claim_or_optimize_yelp`, `optimize_gbp_profile`). All visible heading copy lives in string constants (not raw JSX text) so the literal extractor reliably captures it. | active | Extends if C7e (Recommendations off-site section) ships a second customer surface — at which point it gets its own paired safe-copy scan or this invariant's scan set widens. | 2026-05-22 |
| off-site-customer-tile-no-queue | `tests/architecture/off-site-customer-tile-no-queue.test.ts` | Section 7 C7d / off-site customer tile no-queue contract (2026-05-22) | Pins that the C7d tile (`src/components/today/off-site-authority-tile.tsx`) + its Today section (`src/app/(shell)/today-v2-sections.tsx`) are READ-ONLY and never promote off-site rows into the customer recommendation queue (off-site stays `diagnostic_only` per the locked `recommendation-intelligence-offsite-contract`). Both files' comment-stripped source must NOT reference: `recommendation-intelligence/emitter`, `recommendation-intelligence/promotion-writer`, `promotion-result-to-edit-row`, `off-site-authority/to-candidate-row`, `recommended-edits-persistence`, `runProviderAndPersist`, `recommendation-actions`, `respondToRecommendation`. The tile exposes no Accept/Defer/Dismiss affordance (`"use server"`, `<form`, `action={`, `<button` all forbidden). Positive: the section reads ONLY the cached snapshot path (`loadOffSitePresenceSnapshot` + pure `computeOffSiteRecommendationCandidates`). Also asserts the offsite-contract invariant file still exists (diagnostic_only routing intact). | active | Extends to cover C7e's Recommendations off-site section when it ships; tightens further if/when an explicit, separately-scoped off-site queue-promotion path is ever designed. | 2026-05-22 |
| off-site-recommendations-section-safe-copy | `tests/architecture/off-site-recommendations-section-safe-copy.test.ts` | Section 7 C7e / off-site recommendations section safe copy (2026-05-22) | Pins the customer-visible vocabulary floor of the NEW copy introduced by the C7e read-only "Off-site opportunities" section (`src/app/(shell)/recommendations/off-site-opportunities-section.tsx`) — the manual-follow-up context label. (The reused C7d `OffSiteAuthorityTile`'s own copy stays pinned by `off-site-customer-tile-safe-copy`.) Extracts the section's visible text (string + template static segments, skipping `${...}`) and forbids: missing / we checked / automatically / request reviews now / improve rankings / drove / caused / generated / led to / ` made ` / revenue / dollars / sales / leads / guaranteed / `$` / Mode A-C / ` GBP `. Also scans the section SOURCE and forbids the policy-risk action identifiers (`request_gbp_reviews`, `pursue_local_pr`). Positive sanity: the section frames itself as "Off-site opportunities" + "manual follow-up". | active | Extends if the section's copy grows or a third customer off-site surface ships. | 2026-05-22 |
| off-site-recommendations-section-no-queue | `tests/architecture/off-site-recommendations-section-no-queue.test.ts` | Section 7 C7e / off-site recommendations section no-queue contract (2026-05-22) | Pins that the C7e section (`src/app/(shell)/recommendations/off-site-opportunities-section.tsx`) is READ-ONLY and never promotes off-site rows into the website-edit queue (off-site stays `diagnostic_only`). The section's comment-stripped source must NOT reference: `recommendation-intelligence/emitter`, `recommendation-intelligence/promotion-writer`, `promotion-result-to-edit-row`, `off-site-authority/to-candidate-row`, `recommended-edits-persistence`, `runProviderAndPersist`, `recommendation-actions`, `respondToRecommendation`, `load-queue` (the website-edit queue loader). No Accept/Defer/Dismiss affordance (`"use server"`, `<form`, `action={`, `<button` forbidden). Positive: reads ONLY the cached snapshot path + reuses the C7d `OffSiteAuthorityTile`. Scans ONLY the section file — `recommendations/page.tsx` legitimately imports the website-edit queue loaders for the primary queue, so it is intentionally NOT scanned. | active | Extends if a separately-scoped off-site queue-promotion path is ever designed. | 2026-05-22 |

| repeat-citation-operator-page-discipline | `tests/architecture/repeat-citation-operator-page-discipline.test.ts` | Section 5.A.2 / operator-page structural contract (2026-05-16) | Pins eight contracts on `src/app/(shell)/diagnostics/repeat-citation/page.tsx`: (1) declares `export const dynamic = "force-dynamic"`; (2) imports `isOperatorModeServer` from `@/lib/operator-mode`; (3) imports `notFound` from `next/navigation`; (4) active source contains `isOperatorModeServer(` AND `notFound(` invocations; (5) operator gate fires BEFORE every data read — gate offset precedes `currentTenantId(`, `getBusinessConfig(`, `getRepository(`, `getRecommendedEdits(`, AND the first `loadRepeatCitationForEdit(` call; (6) imports `loadRepeatCitationForEdit` from `@/domains/citation-lifecycle/load-repeat-citation`; (7) does NOT import from `@/storage/canonical-store`; (8) does NOT call `repo.getObservationRuns()` (negative-lookbehind regex allows the legitimate `.getProfoundImportRuns(` token). | active | Permanent — operator-gate-before-reads is a structural safety contract. Refines when additional data readers land on the page; extend the "gate precedes" assertion list together with the loader signature. | 2026-05-16 |
| repeat-citation-operator-page-vocab | `tests/architecture/repeat-citation-operator-page-vocab.test.ts` | Section 5.A.2 / Section 12 N4 carry-over (2026-05-16) | Extracts visible-text candidates (string-literal contents + template-literal static segments, skipping `${...}` substitutions via a depth-counted walker) from `src/app/(shell)/diagnostics/repeat-citation/page.tsx` and forbids: causal verbs (`drove`, `caused`, `generated`, ` made `, `led to`); revenue framing (`$`, `revenue`, `dollars`, `sales`, `leads`); operator-only Mode labels (`Mode A`, `Mode B`, `Mode C` — Section 6 H8 carry-over); promise verbs (`will improve rankings`, `will drive`, `will make AI cite`); prescriptive (`you must`, `you need to`); cross-section vocabulary (`primary recommendation`, `primary_recommendation` — Section 6 scope); operator-page additions (`scored` — implies a ranking that doesn't exist; `AI cites you` — too informal; standalone `missing` — too judgmental, use "not yet observed" or "not repeated in this window"). Pre-empts hostile copy on the first operator surface to consume the Section 5.A classifier. | active | Refines when Section 12 N4's global vocabulary catalog ships — at which point this localized scan becomes a defense-in-depth subset. | 2026-05-16 |
| repeat-citation-pure-purity | `tests/architecture/repeat-citation-pure-purity.test.ts` | Section 5.A / compute-repeat-citation pure-module purity (2026-05-16) | Pins `src/domains/citation-lifecycle/compute-repeat-citation.ts` as a strict pure module: argument-fed, no I/O, no cache, no global helpers. Negative-import scan forbids `@/lib/persistence/repositories` (+ `getRepository`), `@/lib/business-config`, `@/lib/connector-store`, `@/lib/persistence/cold-store`, `@/lib/tenant-context` (+ `currentTenantId` / `currentTenantSlug`), `next/cache`, `@/storage/canonical-store`, `@/lib/url/normalize` + `normalizeUrl` (path-only canonicalization would corrupt host discrimination), `@/domains/pages/classify` + `normalizePageUrl` (strips only `utm_*`), and LLM-provider identifiers (`OpenAI`, `Anthropic`, lowercase variants, `BEACON_LLM_PROVIDER`). Positive-import scan REQUIRES `canonicalizeCitationUrl` from `./canonicalize-url` AND `getTimeToCitationEligibility` from `./eligibility` — drift on either is the structural signal that Section 5 has diverged from Section 2's locked canonicalization / eligibility contract. | active | Permanent — pure helpers consume already-resolved inputs only. | 2026-05-16 |
| repeat-citation-no-customer-surface | `tests/architecture/repeat-citation-no-customer-surface.test.ts` | Section 5.A guard + Section 5.B Slice 1 allowlist transition (2026-05-16); EXTENDED in Section 5.B Slice 2 — Today tile band counter (2026-05-21) | Walks the customer-facing surface tree (`src/app/(shell)/{today,recommendations,changes,prompts,local,competitors}/**` + `src/components/{today,recommendations,changes,prompts,local}/**`) PLUS the additional single-file scan `src/app/(shell)/today-v2-sections.tsx` (sits at sibling depth to `today/`; added via the new `ADDITIONAL_SCAN_FILES` list). Forbids any occurrence of the Section 5 symbol family (`repeat-citation`, `repeatCitation`, `RepeatCitation`, `loadRepeatCitationForEdit`, `computeRepeatCitation`) EXCEPT in the explicit Section 5.B allowlist: **Slice 1 (3 files)** — `src/components/changes/repeat-citation-act3.tsx` (customer-safe render component), `src/app/(shell)/changes/[id]/page.tsx` (server loader call site), `src/app/(shell)/changes/[id]/change-detail-v2-client.tsx` (v2 client consumer); **Slice 2 (2 files, 2026-05-21)** — `src/components/today/edit-lifecycle-tile.tsx` (extended tile rendering the band counter section), `src/app/(shell)/today-v2-sections.tsx` (server section threading `summary.repeat_citation_30d` to the tile). Invariant also verifies every allowlist entry resolves to a real file in the scan tree (renames force an explicit allowlist update). | active | Permanent at the allowlist-discipline level. Allowlist extends when future Section 5.B customer surfaces ship; each addition requires the same architecture-invariant + catalog co-update. | 2026-05-21 |
| repeat-citation-today-tile-band-counter | `tests/architecture/repeat-citation-today-tile-band-counter.test.ts` | Section 5.B Slice 2 — Today edit-lifecycle tile band counter contract (2026-05-21) | Pins THREE things about `src/components/today/edit-lifecycle-tile.tsx`: (1) **Locked customer-label map** — source carries the constant `REPEAT_CITATION_BAND_LABELS: Record<RepeatCitationBand, string>` with EXACTLY the 5.B.2 customer labels verbatim: `stable: "consistent"`, `intermittent: "recurring"`, `one_off: "early signal"`, `not_repeated: "not repeated"`, `still_learning: "still learning"`. Lower-case forms (vs Section 5.B.1 Changes detail's title-case forms) because the Today tile renders the labels INLINE in a counter line where sentence case reads better. Source also carries `REPEAT_CITATION_BAND_ORDER: ReadonlyArray<RepeatCitationBand>` driving render order. (2) **Module purity + import contract** — type-only import of `RepeatCitationBand` from `@/domains/citation-lifecycle/compute-repeat-citation` (no value imports of compute logic); no `fetch(`, no Supabase / persistence imports, no `openaiProvider`, no `load-repeat-citation` direct import (tile reads pre-aggregated `repeatCitation30d` prop only), no `computeRepeatCitation` call, no Supabase `recommended_edits` write shape, no half-life math (G4 lock — no half-life in v1). (3) **Forbidden customer-copy vocab** — no raw internal band names as JSX-visible text (`>stable<` / `>intermittent<` / `>one_off<` / `>not_repeated<` / `>still_learning<`), no percentage rendering, no Section-6 vocabulary (`Mode A` / `Mode B` / `Mode C` / `primary recommendation`), no internal taxonomy (`aiSearchSignal` / `actualSearchQueries`), no causal/revenue/leads language (`drove` / `caused` / `generated` / `revenue` / `leads` / `$`). **31 cases total — 1 file-exists + 7 customer-label pins + 8 module-purity pins + 10 forbidden-vocab pins + 5 render-surface contract pins.** Defense-in-depth alongside `repeat-citation-customer-copy-vocab` (which pins the 5.B.1 Changes detail surface on a parallel axis) and `repeat-citation-no-customer-surface` (which gates which files may even reference the symbols — the tile + today-v2-sections are explicit allowlist entries). | active | Permanent at the Today-tile band-counter boundary. Refines if a future slice (e.g., 5.B.3 introducing a `live_at` filter on the counter or a per-platform breakdown) lands new render shape — the new shape pins extend; existing pins do not change. Retires only if the band counter is removed from the Today tile entirely (no plan to do so). | 2026-05-21 |
| repeat-citation-customer-copy-vocab | `tests/architecture/repeat-citation-customer-copy-vocab.test.ts` | Section 5.B Slice 1 / customer-copy guard (2026-05-16) | Visible-text extractor on `src/components/changes/repeat-citation-act3.tsx` (string-literal contents + template-literal static segments, skipping `${...}` substitutions). Forbids the locked Section 5.A + Section 6 vocabulary set plus Section 5.B customer-page additions: causal verbs (`drove`, `caused`, `generated`, ` made `, `led to`); revenue framing (`$`, `revenue`, `dollars`, `sales`, `leads`); operator-only Mode labels (`Mode A`, `Mode B`, `Mode C`); promise verbs (`will improve rankings`, `will drive`, `will make AI cite`); prescriptive (`you must`, `you need to`); Section 6 cross-section vocab (`primary recommendation`, `primary_recommendation`); standalone `missing` (word-bounded regex); Section 5.B additions (`One-off` and `Intermittent` — must map through the customer label table to `Early signal` and `Recurring`; `%` — no percentages on customer repeat-citation surface; verdict words `failing`, `underperforming`, `weak`, `bad`). The new sub-line is the first customer-facing surface for Section 5.A's classifier output — this invariant pre-empts hostile copy at the source. | active | Refines when Section 12 N4's global vocabulary catalog ships — at which point this localized scan becomes a defense-in-depth subset. | 2026-05-16 |
| repeat-citation-changes-detail-band-mapping | `tests/architecture/repeat-citation-changes-detail-band-mapping.test.ts` | Section 5.B Slice 1 / band→label mapping (2026-05-16) | Source-text scan on `src/components/changes/repeat-citation-act3.tsx` pinning the exact band→customer-label mapping table: `stable → "Consistent"`, `intermittent → "Recurring"`, `one_off → "Early signal"`, `not_repeated → "Not repeated in this window"`, `still_learning → "Still learning"`. Positive assertions require each customer label string literal to appear. Negative assertions require the active source NOT to contain visible `One-off` or `Intermittent` (internal band names must map through the customer table). Also pins required copy fragments: `Citation stability:`, `successful AI readings`, `First cited`. A future drive-by that renames a band label or drops a required fragment trips this invariant. | active | Permanent — the customer-label mapping IS the customer-facing contract. Refines only when operator approves a re-label (e.g., "Early signal" → "Building citation pattern"). | 2026-05-16 |
| repeat-citation-no-section-6-bridge | `tests/architecture/repeat-citation-no-section-6-bridge.test.ts` | Section 5.B Slice 1 / Section 6 H8 silence carry-over (2026-05-16) | Pins three structural contracts on `src/components/changes/repeat-citation-act3.tsx` to prevent cross-scope mixing with Section 6 primary-recommendation evidence (which renders right above the new sub-line on Changes detail Act 3): (1) NO imports from Section 6 modules (`change-primary-mode-a`, `change-primary-mode-b`, `change-primary-evidence-copy`, `load-change-primary-evidence`); (2) NO visible Section 6 vocabulary in active source (`primary recommendation`, `primary_recommendation`, `Mode A`, `Mode B`, `Mode C`); (3) NO bridge phrases in visible text (`because`, `which means`, `as a result`, `therefore` — word-bounded regex). The two render blocks must sit independently in Act 3 with no causal link between them. Section 6 H8 silence rule extends from the operator-only diagnostic surface into the customer-facing surface. | active | Permanent — cross-scope claim prevention is structural. | 2026-05-16 |
| repeat-citation-forbidden-vocab | `tests/architecture/repeat-citation-forbidden-vocab.test.ts` | Section 5.A / Section 12 N4 carry-over (2026-05-16) | Comment-stripped source-text scan of `compute-repeat-citation.ts` + `load-repeat-citation.ts` forbids: causal verbs (`drove`, `caused`, `generated`, ` made `, `led to`); revenue framing (`$`, `revenue`, `dollars`, `sales`, `leads`); operator-only Mode labels (`Mode A`, `Mode B`, `Mode C` — Section 6 H8 carry-over); promise verbs (`will improve rankings`, `will drive`, `will make AI cite`); prescriptive (`you must`, `you need to`); Section 6 cross-section vocabulary (`primary recommendation`, `primary_recommendation` — Section 5 surfaces must stay non-overlapping with Section 6 to preserve Mode A/B/C silence). Pre-emptive: even though these files emit no customer copy directly, their identifier choices form the authoritative source for any Section 5.B customer surface. | active | Refines when Section 12 N4's global vocabulary catalog ships — at which point this localized scan becomes a defense-in-depth subset. | 2026-05-16 |
| repeat-citation-loader-tenant-scope | `tests/architecture/repeat-citation-loader-tenant-scope.test.ts` | Section 5.A / loader tenant-scope discipline (2026-05-16) | Pins six source-text contracts on `src/domains/citation-lifecycle/load-repeat-citation.ts`: (1) imports `getRepository` from `@/lib/persistence/repositories`; (2) active source calls `getRepository().forTenant(tenantId)` (explicit-tenant gate); (3) calls `repo.getProfoundImportRuns()` — the Section 5 precursor method (commit 4ddb908) — NOT the website-crawl `repo.getObservationRuns()`; (4) wraps the read in `unstable_cache` with a cache-key array containing `"repeat-citation:v1"`, `tenantId`, `recommendedEdit.id`, `recommendedEdit.live_at`, and `windowDays`; (5) declares the `recommended_edits:${tenantId}` cache tag (mirrors `load-lifecycle.ts` + `load-change-primary-evidence.ts` so edit-mutation invalidation flows through); (6) does NOT import from `@/storage/canonical-store` (carry-over from `section5-no-canonical-store-observation-runs-bridge` at the source-text level). | active | Permanent — caller-bound tenant scope is a structural contract; refines only if the loader's caching shape itself changes. | 2026-05-16 |
| profound-import-runs-explicit-tenant-scope | `tests/architecture/profound-import-runs-explicit-tenant-scope.test.ts` | Section 5 precursor + durable denominator / explicit-tenant repository scoping (2026-05-16) | Pins that `getProfoundImportRuns()` on both backend tenant facades scopes by the EXPLICIT `tenantId` argument captured at `forTenant(tenantId)`, NOT by ambient `currentTenantSlug()` routing. **File backend** (`tenant-repo.ts`): declares the explicit-tenant disk-read helper `readProfoundImportRunsForTenant` that (1) accepts `tenantId: string`, (2) resolves slug via `getTenant(tenantId)` with the operator-bootstrap `BEACON_TENANT_ID`/`BEACON_TENANT_SLUG` env fallback only when the id matches, (3) reads `.data/tenants/{slug}/observation-runs.json` directly via `getDataDir(slug)` + `readFileSync`, and (4) does NOT call `readStore("observation-runs")` or `readDotDataJson("observation-runs")` (ambient-routed). **Supabase backend** (`supabase-backend.ts`): queries the existing `observation_runs` table inline with `.from("observation_runs").select(...).eq("tenant_id", tenantId).eq("run_type", "citation_sample_import")` and maps each row through `mapObservationRunRowToProfoundImportRun` (an exported pure helper in `supabase-backend.ts`) into the `ProfoundImportRun` shape Section 5 compute expects. The mapper handles source→platform translation (`"perplexity-native-poll"` → `"perplexity"`, `"openai-native-poll"` → `"chatgpt"`, unknown sources pass through). Both backends pinned to NOT import from `@/storage/canonical-store` and NOT call the website-crawl `repo.getObservationRuns()`. Section 5's durable denominator now reads from production Supabase records that have been collecting since 2026-04-22 — NO new table, NO migration, NO new write path. | active | Permanent — explicit-tenant scoping is the structurally correct contract for any `forTenant(tenantId).getX()` method. Mapper-shape assertions refine when a future native platform's poll script adds a new `source` literal (the mapper's verbatim passthrough already accommodates it). | 2026-05-16 |
| section5-no-canonical-store-observation-runs-bridge | `tests/architecture/section5-no-canonical-store-observation-runs-bridge.test.ts` | Section 5 precursor / tenant-scoped poll-run repository read (2026-05-16) | Pins that no file under `src/domains/citation-lifecycle/**` imports from `@/storage/canonical-store`. Background: Section 5 (repeat-citation classifier) needs `ProfoundImportRun[]` for its locked G2 denominator ("successful poll days since `live_at`"). Two readers return that type today — `getObservationRuns` exported from `@/storage/canonical-store` (process-cached `_state` mutable object; cross-tenant leak risk in multi-tenant runtime per the parked Section 7 prerequisite) and the new tenant-scoped repository method `getRepository().forTenant(tenantId).getProfoundImportRuns()` added in this precursor slice. The new method is the structurally correct path (path-based tenant isolation via `currentTenantSlug()` + positive shape filter on `run_date` + `source_type`). The repository's existing `getObservationRuns()` returns the WRONG type (`ObservationRun` from `@/domains/observations/types` — website crawl runs with `run_id` + `run_type`, NOT poll runs) and is unrelated. This invariant pre-ratchets so the next slice that lands Section 5's `compute-repeat-citation.ts` + `load-repeat-citation.ts` cannot adopt the canonical-store bridge. Currently GREEN by absence — zero matches expected. | active | Permanent — tenant-scoped repo method beats process-cached global reader on structural grounds; ban does not retire when Section 7 multi-tenant prereq ships. May broaden to additional domains when their own compute layers land. | 2026-05-16 |

## Phase A.3.b1.alpha — Section 8 (GSC URL Inspection client + tenant cache)

| name | source test file | section | purpose (one sentence) | status | retirement-condition | last-verified |
|---|---|---|---|---|---|---|
| gsc-client-tenant-isolation | `tests/architecture/gsc-client-tenant-isolation.test.ts` | Section 8 / A.3.b1.alpha + A.3.b2 — GSC URL Inspection client posture (2026-05-16, updated 2026-05-17) | Pins `src/lib/connectors/gsc/client.ts` as operator-substrate only with explicit-tenant scoping AND Supabase-backed cache (post-A.3.b2): (1) `gscUrlInspect` signature declares `tenantId: string`; (2) NO ambient `currentTenantSlug()` / `currentTenantId()` reads; (3) cache lives in Supabase — imports `getSupabaseAdmin` from `@/lib/persistence/supabase`, references the `gsc_url_inspections` table, filters by `.eq("tenant_id", ...)`, upserts with `onConflict: "tenant_id,inspection_url"` (composite-PK isolation at the storage layer); (4) disk path retired — does NOT call `getDataDir`, does NOT import `@/lib/tenant`, does NOT import `getTenant` from `@/domains/tenants/store` (slug resolution no longer needed for cache routing); (5) does NOT import any customer-facing surface (`@/app/**`, `@/components/**`, `@/domains/today/**`, `@/domains/recommendations/**`, `@/domains/indexability/**`) — A.3.b1.beta wires indexability as a separate trust-boundary slice; (6) `import "server-only"` declared on both `client.ts` and `types.ts`; (7) `webmasters.readonly` scope check referenced in source; (8) file header declares A.3.b2 + operator-substrate posture explicitly. Mirrors `profound-import-runs-explicit-tenant-scope` (explicit tenantId) AND `connector-store-no-disk-write` (durable storage in Supabase). | active | Refines when A.3.b1.beta lands the indexability integration (allowlist may need extension); permanent at the explicit-tenant + operator-substrate + Supabase-cache level. | 2026-05-17 |
| gsc-cache-no-disk-write | `tests/architecture/gsc-cache-no-disk-write.test.ts` | Section 8 / A.3.b2 — GSC URL Inspection cache migrated to Supabase (2026-05-17) | Pins `src/lib/connectors/gsc/client.ts` as a Supabase-backed cache with ZERO disk I/O. Negative invariants: no `writeFileSync(`, `renameSync(`, `mkdirSync(`, `unlinkSync(`, `readFileSync(`; no `node:fs` / `node:path` imports; no `getDataDir(` call; no `.data/tenants` substring; no `gsc-url-inspections.json` filename (legacy hyphenated form). Positive invariants: imports `getSupabaseAdmin` from `@/lib/persistence/supabase`; references the `gsc_url_inspections` table; invokes both `.select(` and `.upsert(` against the cache table. Replaces the A.3.b1.alpha disk cache which was inert on Vercel (lambda FS read-only post-init AND `.data/` gitignored — every page render would have re-hit the GSC API, burning the 2,000-call/day quota in minutes). Mirrors `connector-store-no-disk-write`. | active | Permanent — disk-bound cache persistence is the bug this slice fixes; never re-introduce it. | 2026-05-17 |
| load-gsc-signal-tenant-scope | `tests/architecture/load-gsc-signal-tenant-scope.test.ts` | Section 8 / A.3.b1.beta — GSC indexability adapter operator-substrate posture (2026-05-17) | Pins `src/domains/indexability/load-gsc-signal.ts` as the operator-substrate seam between `gscUrlInspect` and the indexability verdict pipeline: (1) `loadGscSignal` signature requires `tenantId: string`; (2) NO ambient `currentTenantSlug()` / `currentTenantId()` reads; (3) imports `gscUrlInspect` from `@/lib/connectors/gsc/client`; (4) imports `getSupabaseAdmin` from `@/lib/persistence/supabase`; (5) references the `gsc_url_inspections` cache table; (6) filters cache reads by `.eq("tenant_id", ...)`; (7) reads `BEACON_GSC_SITE_URL` env var (the SOLE site-URL source); (8) exports `GSC_INSPECT_PER_RENDER_LIMIT = 5` as the locked per-render cap; (9) does NOT import any customer-facing surface (`@/app/**`, `@/components/**`, `@/domains/today/**`, `@/domains/recommendations/**`, `@/domains/changes/**`, `@/domains/prompts/**`, `@/domains/local/**`); (10) `import "server-only"` declared; (11) file header declares A.3.b1.beta + operator-substrate posture. | active | Permanent at the operator-substrate + explicit-tenant level. Refines if A.3.b3 introduces additional GSC APIs (Search Analytics, etc.). | 2026-05-17 |
| gsc-no-customer-surface-import | `tests/architecture/gsc-no-customer-surface-import.test.ts` | Section 8 / A.3.b1.beta — customer surfaces never touch GSC (2026-05-17) | Walks every TypeScript/TSX file under `src/app/(shell)/{today,recommendations,changes,prompts,local,competitors}/**` AND `src/components/{today,recommendations,changes,prompts,local}/**` and forbids: (a) imports from `@/lib/connectors/gsc/*`; (b) imports from `@/domains/indexability/load-gsc-signal`; (c) bare `gscUrlInspect` / `loadGscSignal` identifier references. The indexability loader's `enableGsc?: boolean` opt-in (default off) is the structural gate that prevents customer-facing callers from ever loading GSC data; this invariant is the source-text backstop that catches a regression at PR review. Permitted operator surfaces: `src/app/(shell)/diagnostics/**`, `src/domains/indexability/**`, `src/lib/connectors/gsc/**`. | active | Permanent — GSC is operator-substrate only until a separate trust-boundary slice promotes it (citation-cross verdict, customer-facing diagnostic, etc.). | 2026-05-17 |
| gsc-no-hardcoded-site-url | `tests/architecture/gsc-no-hardcoded-site-url.test.ts` | Section 8 / A.3.b1.beta — GSC site URL config (2026-05-17) | Forbids any `sc-domain:<host>` literal in active `src/` source (comments stripped). The GSC siteUrl MUST come from the `BEACON_GSC_SITE_URL` env var — hardcoding a customer identifier in the repo would break operator-substrate posture AND block multi-tenant onboarding (per-tenant siteUrl resolution is deferred to a future slice). Positive invariant: `src/domains/indexability/load-gsc-signal.ts` references `BEACON_GSC_SITE_URL`. Test fixtures under `tests/` are intentionally NOT scanned — they legitimately reference `sc-domain:` literals as test data. | active | Permanent at the env-var-resolution contract level. Refines when multi-tenant siteUrl resolution lands (per-tenant column, per-tenant env var, or tenant-table lookup). | 2026-05-17 |
| connector-store-no-disk-write | `tests/architecture/connector-store-no-disk-write.test.ts` | connector-tokens-supabase-and-gsc-scope-split (2026-05-16) — connector-token persistence on Vercel | Pins `src/lib/connector-store.ts` as a Supabase-backed store with ZERO disk writes. Negative invariants: no `writeFileSync(`, `renameSync(`, `mkdirSync(`, `unlinkSync(`, and no `.data/connector-tokens` / `connector-tokens.json` substring in the active source. Positive invariants: imports `getSupabaseAdmin` from `@/lib/persistence/supabase`; imports `currentTenantId` from `@/lib/tenant-context`; references the `connector_tokens` table; invokes `.upsert(` + `.delete(` helpers. Replaces the prior file-backed `.data/connector-tokens.json` store that crashed on Vercel with `ENOENT: no such file or directory, open '/var/task/.data/connector-tokens.json.tmp'` — lambda FS is read-only post-init and `.data/` is gitignored. | active | Permanent — disk-bound token persistence is the bug this slice fixes; never re-introduce it. | 2026-05-16 |
| google-oauth-scope-split | `tests/architecture/google-oauth-scope-split.test.ts` | connector-tokens-supabase-and-gsc-scope-split (2026-05-16) — Google OAuth per-kind scope grant | Pins `src/lib/connectors/google-auth.ts` as a per-kind OAuth builder: (1) `buildGoogleAuthUrl(kind: GoogleConnectorKind, state: string)` signature; (2) per-kind `SCOPES` map keyed by `GoogleConnectorKind`; (3) `GSC_SCOPE` = `webmasters.readonly`; (4) `GBP_SCOPE` = `business.manage`; (5) combined `GOOGLE_OAUTH_SCOPES` constant from A.3.b1.alpha is REMOVED (drift backstop — re-adding it conflates both scopes); (6) `encodeOAuthState` + `decodeOAuthState` exported; (7) `BEACON_OAUTH_STATE_SECRET` referenced. Callback contract: imports `decodeOAuthState`; routes valid `kind="gsc"` → provider `google_gsc`; routes `kind="gbp"` → `google_gbp`; redirects with `?error=invalid_state` on bad state; legacy single `provider: "google"` literal absent. | active | Permanent at the per-kind contract level. Refines if future Google APIs require additional scope kinds (extend the `SCOPES` map). | 2026-05-16 |
| connector-token-tenant-scope | `tests/architecture/connector-token-tenant-scope.test.ts` | connector-tokens-supabase-and-gsc-scope-split (2026-05-16) — tenant isolation on connector-token storage | Pins tenant scope on every connector-store read/write/delete: (1) all 6 exported functions (`getConnectorToken`, `getGoogleConnectorToken`, `getYelpConnectorToken`, `saveConnectorToken`, `deleteConnectorToken`, `getConnectorInfo`) accept `tenantId?: string` — explicit-tenant override; (2) every `.from("connector_tokens")` chain in the file pairs `.eq("tenant_id", ...)` OR `upsert({ tenant_id, ... })`; (3) ambient resolution routes through `currentTenantId()` from `@/lib/tenant-context`; (4) `ConnectorProvider` union contains `google_gsc` + `google_gbp` + `yelp`; (5) legacy `provider: "google"` literal is REMOVED. Mirrors the locked `profound-import-runs-explicit-tenant-scope` discipline: explicit tenantId beats ambient when both are available. | active | Permanent — tenant scope on credential storage is structurally non-negotiable. | 2026-05-16 |

## Section 8 J-block remaining (J2 + J3 + J4 + J5)

| name | source test file | section | purpose (one sentence) | status | retirement-condition | last-verified |
|---|---|---|---|---|---|---|
| gsc-quota-stagger-tenant-isolation | `tests/architecture/gsc-quota-stagger-tenant-isolation.test.ts` | Section 8 / J3 — GSC quota stagger purity + tenant isolation (2026-05-18) | Pins `src/lib/connectors/gsc/quota-stagger.ts` as a PURE module with no I/O: (1) no Supabase, fetch, or tenant-context imports; (2) no connector-store / google-auth / LLM / paid-API imports; (3) `staggerSlotHour(tenantId: string)` is the public signature — tenant_id is the SOLE input, no ambient reads; (4) `isStaggerSlotActive({ tenantId, now })` accepts explicit clock injection; (5) locked constants pinned at the source level: `STAGGER_BUCKETS = 6`, `STAGGER_WINDOW_HOURS = 24 / STAGGER_BUCKETS`, `MAX_RETRIES_ON_429 = 3`; (6) determinism — no `Math.random()`, no inline `Date.now()` (clock must be threaded by caller). Backs the locked Section 8 J3 contract: multi-tenant deployments distribute their GSC refresh slots across six 4-hour windows by sha1(tenant_id) so no two tenants share a quota window by accident. | active | Permanent at the structural level — tenant-scope quota safety is non-negotiable. Refines if the bucket count changes via a future operator-locked decision. | 2026-05-18 |

## Section 9 / Slice 9.A1α — GA4 connector substrate

| name | source test file | section | purpose (one sentence) | status | retirement-condition | last-verified |
|---|---|---|---|---|---|---|
| ga4-connector-server-only | `tests/architecture/ga4-connector-server-only.test.ts` | Slice 9.A1 — GA4 connector server-only posture (2026-05-18) | Pins every `.ts`/`.tsx` file under `src/lib/connectors/ga4/` as server-only: (1) at least one file exists (sanity floor); (2) every file imports `"server-only"` so Next.js refuses to bundle them into client components; (3) no file imports a customer-facing surface (`@/app/**`, `@/components/**`, `@/domains/today`, `@/domains/recommendations`, `@/domains/changes`). Operator-substrate only — GA4 access tokens never leak to the browser bundle. | active | Permanent at the server-only + operator-substrate level. Refines when Slice 9.A2 lands the Data API consumers (the allowlist may need a documented diagnostic surface). | 2026-05-18 |
| ga4-connector-tenant-isolation | `tests/architecture/ga4-connector-tenant-isolation.test.ts` | Slice 9.A1 — GA4 connector explicit tenant scope (2026-05-18) | Pins `src/lib/connectors/ga4/{client,property-selection,types}.ts` as explicit-tenant-scoped operator-substrate: (1) `ga4ApiFetch` args declare `tenantId: string`; (2) `listGa4PropertiesForTenant(tenantId: string)` is exported with the explicit parameter; (3) NO file calls ambient `currentTenantSlug()` / `currentTenantId()`; (4) `client.ts` references the `analytics.readonly` scope literal (positive check that the scope gate is wired); (5) `property-selection.ts` targets the Analytics Admin `accountSummaries` endpoint. Mirrors the locked `gsc-client-tenant-isolation` pattern. | active | Permanent at the explicit-tenant + operator-substrate level. Refines when Slice 9.A2 adds Data API helpers (additional functions must keep the same explicit-tenant signature). | 2026-05-18 |
| ga4-no-page-load-call | `tests/architecture/ga4-no-page-load-call.test.ts` | Slice 9.A1β — GA4 calls never on customer page load (2026-05-18, tightened from 9.A1α substrate posture) | Walks every `.ts`/`.tsx` (excluding `.test.*`) under `src/app/(shell)/{today,recommendations,changes,prompts,local,competitors}/**` AND `src/components/{today,recommendations,changes,prompts,local}/**`, forbidding any import from `@/lib/connectors/ga4/*` AND any bare `ga4ApiFetch` / `listGa4PropertiesForTenant` identifier. 9.A1β tightens 9.A1α's substrate-only posture by adding a POSITIVE sanity check that `src/app/(shell)/settings/connectors/actions.ts` DOES import `@/lib/connectors/ga4/` — server actions execute on POST, not on render, so the actions seam is the only allowed entry point; the positive check prevents a future refactor from accidentally dead-coding the connector. Mirrors `gsc-no-customer-surface-import`. | active | Permanent — GA4 customer-surface integration is operator-substrate-only via the server-actions seam. Refines when a future trust-boundary slice promotes a customer-facing GA4 surface (Mode A outcome attribution, etc.). | 2026-05-18 |
| ga4-scope-split | `tests/architecture/ga4-scope-split.test.ts` | Slice 9.A1 — GA4 OAuth scope split (2026-05-18) | Pins the per-kind scope contract in `src/lib/connectors/google-auth.ts`: (1) `GA4_SCOPE` constant declared with exact `analytics.readonly` URL; (2) `SCOPES.ga4 = GA4_SCOPE` mapping present in the `Record<GoogleConnectorKind, string>`; (3) `GoogleConnectorKind` union includes `"ga4"`; (4) `analytics.readonly` literal appears EXACTLY ONCE in the file (drift guard against scope bleed into GSC/GBP constants); (5) `GSC_SCOPE` remains `webmasters.readonly`, `GBP_SCOPE` remains `business.manage` (no scope bleed in the inverse direction); (6) no legacy `GOOGLE_OAUTH_SCOPES` combined-scopes constant. Extends the locked `google-oauth-scope-split` contract by adding the GA4 kind to the per-grant model. | active | Permanent at the per-kind contract level. Refines if a future Google API requires an additional scope kind (extend the `SCOPES` map + bump this invariant accordingly). | 2026-05-18 |

## Section 9 / Slice 9.A2α.1 — GA4 Data API substrate + first Section-9 migration

| name | source test file | section | purpose (one sentence) | status | retirement-condition | last-verified |
|---|---|---|---|---|---|---|
| ga4-data-api-tenant-isolation | `tests/architecture/ga4-data-api-tenant-isolation.test.ts` | Slice 9.A2α.1 — GA4 Data API tenant scope + operator-substrate posture (2026-05-19) | Pins `src/lib/connectors/ga4/data-api.ts` as explicit-tenant-scoped operator-substrate: (1) `runGa4UrlTrafficReport` exported as async; (2) signature takes `args: Ga4RunReportArgs` and destructures `tenantId` (no ambient reads); (3) NO `currentTenantSlug()` / `currentTenantId()` calls; (4) `import "server-only"` at top; (5) references the `analytics.readonly` scope literal (positive scope-gate check); (6) references the `analyticsdata.googleapis.com` Data API host AND the `:runReport` endpoint suffix (positive endpoint check); (7) no customer-facing surface imports (`@/app/**`, `@/components/**`, `@/domains/today`, `@/domains/recommendations`, `@/domains/changes`). Mirrors the locked `ga4-connector-tenant-isolation` pattern from 9.A1α but pins the Data API client specifically. | active | Permanent at the explicit-tenant + operator-substrate level. Refines when 9.A2α.2 (Mode A read model) and 9.A2α.3 (operator diagnostic) consume `runGa4UrlTrafficReport` via the operator-substrate boundary. | 2026-05-19 |
| ga4-data-api-non-2xx-logging | `tests/architecture/ga4-data-api-non-2xx-logging.test.ts` | Slice 9.A2α.1 — GA4 non-2xx logging discipline (2026-05-19) | Pins symmetric non-2xx-response log discipline across BOTH the Admin API client (`src/lib/connectors/ga4/client.ts`) AND the Data API client (`src/lib/connectors/ga4/data-api.ts`): (1) `data-api.ts` calls `log.warn` with `[ga4-data-api]` prefix at the non-2xx branch; (2) payload includes `tenantId` + `status` + `body` fields; (3) body capture bounded to ≤ 500 chars via `.slice(0, 500)`; (4) `client.ts` emits the symmetric `[ga4-client]` log at the same code path (folds in the 9.A1β-deferred fix that hid the Admin API enablement failure during 9.A1β manual smoke); (5) body capture wrapped in try/catch (defensive against broken `response.text()`). Backs the post-9.A1β-triage lesson that silent fail-soft on non-2xx non-401 cost ~30 min of operator triage; same shape MUST NOT recur for the Data API. | active | Permanent at the symmetric-logging level. Any future GA4 endpoint added to either client must mirror this log pattern. | 2026-05-19 |

## Section 9 / Slice 9.A2α.2 — Mode A pure-compute read model

| name | source test file | section | purpose (one sentence) | status | retirement-condition | last-verified |
|---|---|---|---|---|---|---|
| outcome-attribution-mode-a-sample-size-guard | `tests/architecture/outcome-attribution-mode-a-sample-size-guard.test.ts` | Slice 9.A2α.2 — Mode A K4 sample-size guard + purity (2026-05-19) | Pins the Section 9 K4-locked sample-size guard + purity contract in `src/domains/outcome-attribution/mode-a-cited-here-traffic-here.ts`: (1) `MODE_A_MIN_DAYS_POST_LIVE = 7` exported as `const`; (2) `MODE_A_MIN_SESSIONS = 5` exported; (3) `MODE_A_MIN_QUALIFIED_CALLS = 1` exported (CallRail-deferred per K2 but the branch stays present for forward-compatibility); (4) the K4 OR-condition (`!sessionsThresholdMet && !callsThresholdMet`) is enforced at the source level; (5) `computeModeATrafficAttribution` exported as a NON-async pure function; (6) `import "server-only"`; (7) does NOT runtime-import the Supabase admin client, `getRepository`, the GA4 Data API client, ANY `@/lib/connectors/` module, OR any customer-facing surface (`@/app/**`, `@/components/**`, `@/domains/today`, `@/domains/recommendations`, `@/domains/changes`, `@/domains/prompts`) — type-only imports of `Ga4UrlTrafficRow` + `RecommendedEditRow` are allowed because TypeScript erases them at compile time; (8) does NOT call `fetch` (no network access); (9) no ambient `currentTenantSlug()` / `currentTenantId()` reads; (10) defense-in-depth: K5 forbidden customer-vocab tokens (`drove` / `caused` / `generated` / `revenue` / `dollars`) absent from source — protects against future copy leaks when 9.A2β customer surfaces consume the `ModeAResult` union. | active | Permanent at the K4 threshold level. Refines if a future operator-locked decision tunes the sample-size thresholds (would require updating this invariant in lockstep). | 2026-05-19 |

## Section 9 / Slice 9.A2γ — Operator-only GA4 traffic refresh action + persist helper

| name | source test file | section | purpose (one sentence) | status | retirement-condition | last-verified |
|---|---|---|---|---|---|---|
| outcome-attribution-refresh-operator-only | `tests/architecture/outcome-attribution-refresh-operator-only.test.ts` | Slice 9.A2γ — operator-gate the GA4 traffic refresh server action (2026-05-19) | Pins the operator-only contract in `src/app/(shell)/diagnostics/outcome-attribution/actions.ts`: (1) file starts with `'use server';` directive; (2) `refreshTenantGa4Traffic` exported as async function; (3) imports `isOperatorModeServer` from `@/lib/operator-mode`; (4) `isOperatorModeServer()` is called inside the function body BEFORE any reference to `getGoogleConnectorToken` or `persistGa4UrlTraffic` (gate-first; no downstream work, no HTTP, no DB read until the gate passes); (5) returns `{ reason: 'not_operator' }` on gate failure; (6) imports `revalidatePath` from `next/cache` (positive sanity that re-render is wired); (7) imports `persistGa4UrlTraffic` from the persist helper (positive sanity that the Data API seam is wired). Backs the Section 9 architecture invariant "NO outcome connector calls on page load" — page render reads cached rows only; the action is the ONLY entry point that fires a GA4 Data API HTTP call. | active | Permanent at the operator-gate + 'use server' level. Refines if a future slice promotes the refresh path to a cron / scheduled job (which would add an additional non-page entry point and require a parallel invariant). | 2026-05-19 |
| outcome-attribution-refresh-no-customer-surface | `tests/architecture/outcome-attribution-refresh-no-customer-surface.test.ts` | Slice 9.A2γ — operator-only refresh action MUST NOT leak to customer surfaces (2026-05-19) | Walks every non-test `.ts`/`.tsx` under `src/app/(shell)/{today,recommendations,changes,prompts,local,competitors}/**` AND `src/components/{today,recommendations,changes,prompts,local}/**`, forbidding: (a) any import of `@/app/(shell)/diagnostics/outcome-attribution/actions` (the action module); (b) any import of `@/lib/connectors/ga4/persist-url-traffic` (the persist helper module); (c) bare identifiers `refreshTenantGa4Traffic`, `persistGa4UrlTraffic`, `computeRefreshDateRange` (defense in depth against dynamic imports). Positive sanity: (d) `src/app/(shell)/diagnostics/outcome-attribution/page.tsx` DOES reference `refreshTenantGa4Traffic` (the operator diagnostic is the only allowed consumer); (e) `src/lib/connectors/ga4/persist-url-traffic.ts` declares `import "server-only";`. Mirrors the locked `ga4-no-page-load-call` scan pattern but adds a NAMED check on the persist + action exports so a future re-export anywhere cannot bypass the directory scan. | active | Permanent — refresh action + persist helper are operator-substrate only. Refines if a future trust-boundary slice promotes a customer-facing outcome-refresh path (would require a documented exception). | 2026-05-19 |

## Section 9 / Slice 9.A2γ.1 — GA4 pagePath → full URL persist-time normalization

| name | source test file | section | purpose (one sentence) | status | retirement-condition | last-verified |
|---|---|---|---|---|---|---|
| ga4-url-traffic-stored-as-full-url | `tests/architecture/ga4-url-traffic-stored-as-full-url.test.ts` | Slice 9.A2γ.1 — `ga4_url_traffic.url` MUST store full URLs, NOT raw GA4 pagePath (2026-05-19) | Pins the persist-time normalization contract that fixes the 9.A2γ-manual-verification "1000 rows in ga4_url_traffic but Mode A still reports no_traffic_data" bug: (1) `persist-url-traffic.ts` imports `normalizeGa4PagePathToFullUrl` from `./normalize-page-path`; (2) `persist-url-traffic.ts` imports `getBusinessConfig` from `@/lib/business-config` (the domain source); (3) the upsert-rows construction passes `row.url` through `normalizeGa4PagePathToFullUrl({ pagePath: row.url, ... })` — raw `row.url` is NOT upserted directly; (4) `normalize-page-path.ts` declares `import "server-only";`; (5) no customer surface (under `src/app/(shell)/{today,recommendations,changes,prompts,local,competitors}/**` or `src/components/{today,recommendations,changes,prompts,local}/**`) imports the normalizer or references `normalizeGa4PagePathToFullUrl`; (6) Mode A's compute module (`mode-a-cited-here-traffic-here.ts`) does NOT import the normalizer — Mode A continues to use `canonicalizeCitationUrl` only (separation of concerns: normalize at write, canonicalize at read); (7) `canonicalize-url.ts` retains its locked "full URLs only" Phase A.1 contract marker (this slice does NOT touch the citation-lifecycle canonicalizer). Backs the operator-locked posture that GA4's path-only `pagePath` shape is reconciled at persist time so the Mode A read model's canonicalizer continues to operate on its locked contract input. | active | Permanent at the persist-time normalization level. Refines if a future trust-boundary slice changes the canonicalizer's contract to accept path-only inputs natively (Phase-A.1 amendment) — at which point the persist-time normalization becomes belt-and-suspenders and can be reconsidered. | 2026-05-19 |

## Section 9 / Slice 9.A2β — Changes detail Mode A customer copy

| name | source test file | section | purpose (one sentence) | status | retirement-condition | last-verified |
|---|---|---|---|---|---|---|
| outcome-attribution-changes-detail-vocab | `tests/architecture/outcome-attribution-changes-detail-vocab.test.ts` | Slice 9.A2β — K5 forbidden vocabulary on the customer-facing Changes detail Act 3 Mode A sub-line (2026-05-19) | Pins K5 + Section 6 cross-talk discipline on `src/components/changes/outcome-attribution-act3.tsx`: (1) string-literal scan forbids the case-insensitive tokens `drove` / `caused` / `generated` / `revenue` / `dollars` / `ROI` / `sales` / `leads` / `Mode A` / `Mode B` / `Mode C` / `primary recommendation` / `CallRail`; (2) literal `$` character forbidden outside `${...}` template interpolations; (3) component is a pure presentational module — no runtime import of `@/lib/connectors/ga4/*`, no import of `@/lib/persistence/supabase`, no import of `next/cache` / `next/navigation`, no `import "server-only";`, no `fetch(` call; (4) positive sanity: type-only `ModeAResult` + `ReactElement` imports present; (5) positive sanity: `OutcomeAttributionAct3` exported as a named function. The component carries the Section 9 K5 locked customer copy ("received" framing, absolute counts only, no causal language) and is the only Section-9 surface that crosses into customer view. | active | Permanent at the K5 customer-copy boundary. Refines when CallRail K2 lands AND the operator locks new copy variants for "N sessions and M calls" with M ≥ 1 — at which point this invariant gets paired test updates without weakening the forbidden-vocab list. |  2026-05-19 |
| outcome-attribution-changes-detail-suppression | `tests/architecture/outcome-attribution-changes-detail-suppression.test.ts` | Slice 9.A2β — Mode A customer sub-line MUST suppress on every ineligible discriminator + null (2026-05-19) | Pins suppression discipline on `src/components/changes/outcome-attribution-act3.tsx` via BOTH behavioral runs and source-text guards: behavioral — `renderToStaticMarkup` returns `""` for `result == null` AND for every documented `kind: "ineligible"` reason (`no_live_at` / `no_target_url` / `no_traffic_data`), while `still_learning_outcome` (insufficient_days + insufficient_volume) AND `eligible` render non-empty markup carrying the `data-change-detail-outcome-attribution="true"` marker; source-text — the component source contains explicit `result == null` and `result.kind === "ineligible"` guards, AND the ineligible guard immediately `return null;` without intervening JSX. Backs the Section 6 + Section 9 hard contract that substrate gaps and missing data are NEVER customer messages — silence is the correct response. | active | Permanent at the suppression-discriminator boundary. Refines only if Section 9 K-block lock changes to surface a calm placeholder on ineligible (which would require a separate operator decision plus a paired update here). | 2026-05-19 |
| outcome-attribution-changes-detail-no-ga4-api | `tests/architecture/outcome-attribution-changes-detail-no-ga4-api.test.ts` | Slice 9.A2β — Changes detail Mode A path MUST NOT trigger a live GA4 Data API call on render (2026-05-19) | Pins per-file no-GA4-API discipline across four files: (1) `outcome-attribution-act3.tsx` — no runtime import of `@/lib/connectors/ga4/*`, no reference to `runGa4UrlTrafficReport` / `persistGa4UrlTraffic` / `refreshTenantGa4Traffic` / `normalizeGa4PagePathToFullUrl`, no `fetch(` call; (2) `load-mode-a-for-changes-detail.ts` — no runtime import of `data-api` / `persist-url-traffic` / `normalize-page-path` / `client` / `property-selection` / the operator-only refresh action, no bare GA4-action identifiers, no `fetch(`; type-only `Ga4UrlTrafficRow` allowed (TypeScript erases); positive sanity that it imports `getSupabaseAdmin` AND `computeModeATrafficAttribution`; (3) Changes detail server page (`src/app/(shell)/changes/[id]/page.tsx`) — no import of `@/lib/connectors/ga4/*` (runtime OR type), no bare GA4-action identifiers, positive sanity that it imports `loadModeAForChangesDetail`; (4) Changes detail client (`src/app/(shell)/changes/[id]/change-detail-v2-client.tsx`) — no runtime import of `@/lib/connectors/ga4/*`, no bare GA4-action identifiers, type-only `ModeAResult` allowed, positive sanity that it imports `OutcomeAttributionAct3`. Mirrors the locked `ga4-no-page-load-call` directory scan but adds NAMED identifier checks so a future re-export anywhere can't bypass the directory-only scan. Customer surface reads cached `ga4_url_traffic` ONLY; fresh GA4 data lands exclusively via operator-clicked Refresh on the operator-only diagnostic. | active | Permanent at the customer-surface no-Data-API boundary. Refines if a future trust-boundary slice promotes a customer-facing Refresh action (which would require a documented exception + a parallel invariant). | 2026-05-19 |

## Section 9 / Today tile — "Edit outcomes (past 30 days)"

| name | source test file | section | purpose (one sentence) | status | retirement-condition | last-verified |
|---|---|---|---|---|---|---|
| edit-outcomes-tile-vocab | `tests/architecture/edit-outcomes-tile-vocab.test.ts` | Section 9 Today tile — K5 forbidden vocabulary on the customer-facing "Edit outcomes (past 30 days)" Today tile (2026-05-19) | Pins K5 + Section 6 cross-talk + connector-internal-name discipline on `src/components/today/edit-outcomes-tile.tsx`: (1) string-literal scan forbids the case-insensitive tokens `drove` / `caused` / `generated` / `revenue` / `dollars` / `ROI` / `sales` / `leads` / `Mode A` / `Mode B` / `Mode C` / `primary recommendation` / `Google Analytics` / `GA4` / `CallRail`; (2) literal `$` outside `${...}` interpolations forbidden; (3) component is a pure presentational module — no runtime `@/lib/connectors/ga4/*` import, no `@/lib/persistence/supabase` import, no `next/cache` / `next/navigation` import, no `import "server-only";` declaration (isomorphic), no `fetch(` call; (4) positive sanity: type-only `OutcomesSummary` + `ReactElement` imports present; (5) positive sanity: `EditOutcomesTile` exported as a named function. Adds three new connector-internal names (`Google Analytics`, `GA4`, `CallRail`) to the Section 9 K5 forbidden list compared to the Changes detail vocab invariant — Today is a denser surface where connector-name leaks would compound across tiles. | active | Permanent at the K5 + connector-name customer-copy boundary. Refines when CallRail K2 lands AND the operator locks copy that explicitly names the call source — at which point the `CallRail` token may be allowlisted with a paired test update. | 2026-05-19 |
| edit-outcomes-tile-suppression | `tests/architecture/edit-outcomes-tile-suppression.test.ts` | Section 9 Today tile — render state machine MUST collapse loader soft-fails to the still-gathering copy (2026-05-19) | Pins state-machine discipline on `src/components/today/edit-outcomes-tile.tsx` via behavioral runs: (1) `null` summary → still-gathering copy; (2) `status: "data_unavailable"` → still-gathering copy (Today is a denser surface than Changes detail — silence would read as broken, so the soft-fail collapses to the calm still-gathering message); (3) `total_recent_live_edits === 0` AND status ok → empty copy; (4) `eligible_edits === 0` AND total > 0 → still-gathering copy; (5) defensive: `eligible_edits >= 1` AND `sum_post_live_sessions === 0` → still-gathering (NEVER renders "0 sessions"); (6) `eligible_edits >= 1` AND `sum_post_live_sessions > 0` → healthy copy. Also pins that every render output carries `data-today-edit-outcomes-tile="true"` + `data-edit-outcomes-state` with one of `empty` / `still_gathering` / `healthy` (E2E hook stability). | active | Permanent at the state-machine boundary. Refines if Section 9.5 surface design changes to introduce a new state (e.g., "low_signal" between still-gathering and healthy). | 2026-05-19 |
| edit-outcomes-loader-no-ga4-api | `tests/architecture/edit-outcomes-loader-no-ga4-api.test.ts` | Section 9 Today tile — outcomes-summary loader + Today section MUST NOT trigger a live GA4 Data API call on render (2026-05-19) | Pins per-file no-GA4-API discipline across two files: (1) `load-outcomes-summary-for-tenant.ts` — no runtime import of `data-api` / `persist-url-traffic` / `normalize-page-path` / `client` / `property-selection` / the operator-only refresh action, no bare GA4-action identifiers (`runGa4UrlTrafficReport` / `persistGa4UrlTraffic` / `refreshTenantGa4Traffic` / `normalizeGa4PagePathToFullUrl`), no `fetch(` call, no top-level `next/cache` import (must be dynamically imported inside the function body); positive sanity that it imports `getSupabaseAdmin` AND `computeModeATrafficAttribution` AND `canonicalizeCitationUrl` (URL-scoped SELECT pattern mirrors 9.A2β.1) AND `getRepository`; type-only `Ga4UrlTrafficRow` allowed; (2) `today-v2-sections.tsx` — positive sanity that it imports `loadOutcomesSummaryForTenant` AND `EditOutcomesTile`; no runtime `@/lib/connectors/ga4/*` import, no bare GA4-action identifier. Today tile reads cached `ga4_url_traffic` ONLY; fresh GA4 data lands exclusively via the operator-clicked Refresh on the operator-only diagnostic. | active | Permanent at the customer-surface no-Data-API boundary. Refines if a future trust-boundary slice promotes a customer-facing Refresh action on Today (which would require a documented exception). | 2026-05-19 |

## Section 4.5 / Slice 4.5.A — Recommendation Intelligence Expansion: registry audit + activation plan

| name | source test file | section | purpose (one sentence) | status | retirement-condition | last-verified |
|---|---|---|---|---|---|---|
| recommendation-registry-active-set | `tests/architecture/recommendation-registry-active-set.test.ts` | Slice 4.5.A — Recommendation Intelligence Expansion: registry audit + activation plan (2026-05-19); UPDATED in 4.5.B.α₀ (2026-05-19) to count 32 + active set 4 entries; UPDATED in 4.5.B.α₁ (2026-05-19) to active set 5 entries; UPDATED in 4.5.C.α₀ (2026-05-19) to count 37 + active set UNCHANGED; UPDATED in 4.5.C.α₁ (2026-05-20) to active set 9 entries; UPDATED in 4.5.C.α₂ (2026-05-20) to active set 10 entries (added fix_noindex); UPDATED in 4.5.C.α₃a (2026-05-20) to active set 11 entries (added add_internal_link); UPDATED in 4.5.C.α₃b (2026-05-20) to active set {... 11 prior ..., add_schema} (12 entries); UPDATED in 4.5.E.α₁a (2026-05-21) to active set 13 entries (added rewrite_h2) | Pins three structural facts so the Section 4.5 slice sequence (4.5.A → 4.5.G) cannot quietly drift: (1) `ACTION_TYPES` has exactly 37 entries — UNCHANGED in 4.5.E.α₁a; (2) the `generatorActive: true` set in `ACTION_TYPE_REGISTRY` is exactly `{edit_title, edit_meta, change_h1, add_h2_section, add_faq, fix_sitemap, fix_robots, fix_status_code, fix_canonical, fix_noindex, add_internal_link, add_schema, rewrite_h2}` post-4.5.E.α₁a — α₁a flips `rewrite_h2` paired with the new `weak-h2` predicate emitting at `confidence: "low"` (routes to `diagnostic_only` via `applyQueueRules`; first LLM-assisted activation though the LLM gateway is NOT invoked by α₁a — gateway wire-up lands in α₁b via operator-only env-gated server action). Every other registered type carries `generatorActive: false` (binary flag); (3) `docs/RECOMMENDATION_INTELLIGENCE_AUDIT.md` exists and references the locked active set + registry count + the 4.5.A → 4.5.G slice sequence + the 7 Section-7 off-site types. Slices 4.5.D+ that flip `generatorActive` flags or add new registry entries MUST update the audit doc in the same PR, otherwise this invariant fails. Defense-in-depth alongside `off-site-action-types-not-llm-allowed` (Section 7 C7b) which separately pins the off-site types as locked-inactive. **Section 4.5.E.α₁a is the first LLM-assisted flip; gateway invocation deferred to α₁b.** | active | Refines on every Section 4.5 slice that touches the registry (4.5.E.α₁b: 0 new + 0 flips, wires gateway invocation; 4.5.E.α₂: 1 flip `rewrite_faq`; 4.5.E.α₃: +1 new `refresh_stale_page` + 1 flip; 4.5.F: 0 new + 0 flips; 4.5.G: +1 new + 1 flag flip). Each slice updates LOCKED_REGISTRY_COUNT + LOCKED_ACTIVE_SET + the audit doc activation table in lockstep. Retires permanently only if the registry is replaced by a different taxonomy (no plan to do so). | 2026-05-21 |

## Section 4.5 / Slice 4.5.B.α₀ — first activation: metadata trigger diagnostics foundation

| name | source test file | section | purpose (one sentence) | status | retirement-condition | last-verified |
|---|---|---|---|---|---|---|
| recommendation-trigger-predicates-purity | `tests/architecture/recommendation-trigger-predicates-purity.test.ts` | Slice 4.5.B.α₀ — trigger predicate purity (2026-05-19) | Source-text scan over every `.ts` file under `src/domains/recommendation-intelligence/triggers/`. Forbids in active (comment-stripped) source: connector imports (`@/lib/connectors/*` including `google-reviews-sync`, `google-auth`), Supabase admin/repository imports (`@/lib/persistence/supabase`, `@/lib/persistence/repositories`), Next.js cache imports (`next/cache`), LLM provider invocations (`BEACON_LLM_PROVIDER`, `openaiProvider`, `OpenAI(`, `Anthropic(`), HTTP-client calls (`fetch(`), file-system reads (`readFileSync`, ` fs.`). Also forbids module-scope mutable state (top-level `let`/`var` declarations). Every trigger predicate must be a pure function over its passed-in inputs. Defense-in-depth alongside `recommendation-intelligence-no-queue-write` (which separately enforces no `recommended_edits` writes from the same module tree). | active | Permanent at the predicate-purity boundary. Refines if a future slice intentionally introduces a connector-backed predicate (e.g., a GSC-driven predicate in Slice 4.5.C+) — the new file would be hosted OUTSIDE `triggers/` and the invariant scope narrows accordingly. | 2026-05-19 |
| recommendation-intelligence-no-queue-write | `tests/architecture/recommendation-intelligence-no-queue-write.test.ts` | Slice 4.5.B.α₀ — recommendation-intelligence no-queue-write (2026-05-19); EVOLVED in Slice 4.5.D.α₁b — promotion writer + idempotent persistence (2026-05-20); FILE-SET EXTENDED in Slice 4.5.D.α₁c — operator-only live-write action added to scan set (2026-05-20) | Source-text scan over every `.ts` / `.tsx` file under `src/domains/recommendation-intelligence/**` PLUS BOTH operator-only diagnostic surfaces at `src/app/(shell)/diagnostics/recommendation-triggers/page.tsx` AND `src/app/(shell)/diagnostics/recommendation-triggers/actions.ts` (the α₁c server action). **α₁b evolution:** the customer-queue flip lands as a SINGLE allowlisted file — `src/domains/recommendation-intelligence/promotion-writer.ts` — which legitimately imports `recommended-edits-persistence` to call `persistRecommendedEditsLocal` + `syncRecommendedEdits`. **α₁c file-set extension:** the operator-only server action (`actions.ts`) joins the scan set; the action is NOT in the persistence-import allowlist — it reaches writes only through `promoteEligibleCandidates`. Every OTHER file in the tree + the diagnostic page + the diagnostic action STAY write-forbidden. **Current contract (5 tests):** (1) file set non-empty AND includes both diagnostic surfaces; (2) diagnostic actions.ts NOT in the persistence-import allowlist (regression guard); (3) parametric scan: no file imports `@/domains/recommendations/recommended-edits-persistence` or the bare `recommended-edits-persistence` substring EXCEPT the single allowlisted `promotion-writer.ts`; (4) **positive importer pin:** `promotion-writer.ts` MUST exist AND MUST import `@/domains/recommendations/recommended-edits-persistence` AND call both `persistRecommendedEditsLocal` + `syncRecommendedEdits` (defines the customer-queue writer contract); (5) `runProviderAndPersist` STAYS GLOBALLY FORBIDDEN — even the α₁b writer uses persistence helpers directly, NOT the LLM orchestrator path; plus the global Supabase `.from("recommended_edits").(insert\|upsert\|update\|delete)(` write-shape scan remains forbidden across ALL files (the writer uses `syncRecommendedEdits`, which routes through `dualWriteUpsertScoped`). Defense-in-depth alongside `recommendation-trigger-predicates-purity`, `-no-llm-decides`, the α₁a `mapper-eligibility` + `source-pin` invariants, and the α₁c `-promotion-live-write-guards` invariant (which separately pins the action's three-gate ladder + confirmation phrase + positive source-pin). | active | Permanent at the no-queue-write boundary. Allowlist stays single-file; adding another importer fails CI. Refines further only if a future slice intentionally introduces a SECOND legitimate writer (none currently planned) — the allowlist set extends and the positive pin extends to the new file. | 2026-05-20 |
| recommendation-intelligence-promotion-live-write-guards | `tests/architecture/recommendation-intelligence-promotion-live-write-guards.test.ts` | Slice 4.5.D.α₁c — operator-only live-write guard (2026-05-20) | Source-text scan over the single file `src/app/(shell)/diagnostics/recommendation-triggers/actions.ts` — the ONLY server action allowed to invoke `promoteEligibleCandidates({ dryRun: false })`. **Contract (8 source-text pins on the action file + 1 global negative scan)**: (1) file exists at expected path; (2) imports + references `isOperatorModeServer` from `@/lib/operator-mode` (operator gate); (3) imports + references `isPromotionLiveWriteEnabled` from `@/lib/promotion-live-write` (env-flag gate); (4) contains the literal string `"PROMOTE"` (uppercase confirmation phrase, strict case); (5) imports `promoteEligibleCandidates` from `@/domains/recommendation-intelligence/promotion-writer` (positive source-pin to the α₁b allowlisted writer); (6) does NOT import `recommended-edits-persistence` directly (α₁b boundary carried forward); (7) does NOT reference `runProviderAndPersist` (LLM-orchestrator path stays forbidden); (8) does NOT contain a direct Supabase `.from("recommended_edits").{insert/upsert/update/delete}(` write shape; PLUS a global negative scan asserting this file is the ONLY file under `src/app/**` that pairs the source-text `promoteEligibleCandidates` + `dryRun: false` (whitespace-tolerant regex). Forces any future live-write entry point to either land in this allowlisted file or trip the invariant. Defense-in-depth alongside `recommendation-intelligence-no-queue-write` (which separately forbids persistence imports + Supabase writes ANYWHERE under the scan set including this same action file). | active | Permanent at the live-write entry-point boundary. Refines only if Beacon ever introduces a SECOND legitimate live-write entry point (e.g., a future cron task) — the scope narrows to include the new file and the global negative scan updates its allowlist set accordingly. | 2026-05-20 |
| recommendation-intelligence-offsite-contract | `tests/architecture/recommendation-intelligence-offsite-contract.test.ts` | Slice 4.5.F — off-site shared queue contract (2026-05-21) | Pins the adapter `offSiteCandidateToCandidateRow` (Section 7's `OffSiteCandidateAction` → Section 4.5's `RecommendationCandidateRow` carrier) + the `applyQueueRules` carve-out that routes off-site rows to `diagnostic_only` (never to `candidates`). **Contract (10 source-text pins on adapter + 3 behavioral assertions + per-action-type behavioral pin):** (1) adapter file exists at `src/domains/off-site-authority/to-candidate-row.ts`; (2) source contains LITERAL `"human_task"`; (3) source sets `target_url: null` LITERAL; (4) imports `OffSiteCandidateAction` from Section 7's `recommendation-rules`; (5) imports `RecommendationCandidateRow` from the emitter contract; (6) does NOT import `recommended-edits-persistence`; (7) does NOT reference `runProviderAndPersist`; (8) no `fetch(` call; (9) no connector / Supabase / persistence imports; (10) no LLM provider imports; (11) no Supabase `recommended_edits` write shape. **Behavioral pins:** (a) for every 1 of 7 off-site action types, the adapter's emitted `(trigger_signal, action_type)` MUST return `"blocked"` from `eligibilityForTrigger(...)` — defense-in-depth against future regression that might accidentally re-classify off-site as customer-queue-ready; (b) every off-site action_type in `ACTION_TYPE_REGISTRY` carries `signalType: "off_page_seo"` (the detection pivot used by `applyQueueRules`); (c) adapter output is `human_task` + `target_url: null`. **applyQueueRules routing pin:** for all 7 off-site action types, an adapter-produced row (high confidence + no safety flags) is routed by `applyQueueRules` to `diagnostic_only`, NEVER to `candidates`. Defense-in-depth alongside `recommendation-intelligence-no-queue-write` (writer boundary), `recommendation-intelligence-promotion-eligibility-pin` (off-site permanently "blocked" tier), and `recommendation-intelligence-promotion-live-write-guards` (operator-only live-write entry point). | active | Permanent at the off-site shared-contract boundary. Refines only if a future slice intentionally promotes off-site rows to `candidates` (would require revisiting the I-block lock + α₀a.1 eligibility table); none currently planned. | 2026-05-21 |
| recommendation-intelligence-llm-draft-gateway-contract | `tests/architecture/recommendation-intelligence-llm-draft-gateway-contract.test.ts` | Slice 4.5.E.α₀ — LLM-assisted drafting gateway (2026-05-21) | Pins the source-text surface + result-discriminator behavior of `src/domains/recommendation-intelligence/llm-draft-gateway.ts` — the pure module that wires `openaiProvider.generate()` through the existing budget + validator gates and returns a discriminated-union draft result. The gateway is the FIRST piece of α₀ infrastructure — it has NO production caller yet (callers wire in via α₁+ trigger predicates + env-flag-gated diagnostic preview). **Contract (10 source-text pins + 1 behavioral pin)**: (1) gateway file exists at expected path; (2) imports `openaiProvider` from `@/domains/recommendations/providers/openai` (positive source-pin to the live provider); (3) imports `checkBudget` + `recordSpend` from `@/domains/recommendations/adjudicator-budget` (positive — reuses the existing `$10/mo `llm-budget.json` cap; no new ledger); (4) imports `validateSpecificEdit` from the validator (positive — reuses the 19-rule LLM-hardening chain unchanged); (5) does NOT import `recommended-edits-persistence` (carries the α₁b boundary forward); (6) does NOT reference `persistRecommendedEditsLocal` / `syncRecommendedEdits`; (7) does NOT reference `runProviderAndPersist` (the LLM-orchestrator persistence path STAYS globally forbidden in the recommendation-intelligence tree — even the gateway uses provider helpers directly); (8) does NOT contain a direct Supabase `recommended_edits` write shape; (9) does NOT call `fetch(` (provider delegates network I/O); (10) contains literal strings `"blocked_budget"` / `"validation_failed"` / `"drafted"` / `"abstained"` (regression guard on the result-discriminator surface). **Behavioral pin**: `LlmDraftResult.status` union is exactly the 4-value set { `"drafted"`, `"abstained"`, `"validation_failed"`, `"blocked_budget"` } — drift trips the test (regex-scan over `status: "..."` literals in the source). Defense-in-depth alongside `recommendation-intelligence-no-queue-write` (which auto-covers the gateway via its `walk(INTEL_DIR)` scan and separately forbids persistence imports + Supabase writes anywhere under the tree). | active | Permanent at the LLM-drafting infrastructure boundary. Refines when α₁+ slices wire in trigger predicates and env-gated diagnostic preview (the gateway itself does not change; new invariants pin the caller's behavior). | 2026-05-21 |
| recommendation-intelligence-llm-draft-gateway-render-isolation | `tests/architecture/recommendation-intelligence-llm-draft-gateway-render-isolation.test.ts` | Slice 4.5.E.α₁b₁ — env flag + thin packet builder + render isolation (2026-05-21); EVOLVED in Slice 4.5.E.α₁b₂-A — single-allowlist caller + action-contract pins folded in (2026-05-21); UNCHANGED-but-VERIFIED in Slice 4.5.E.α₁b₂-B — page UI consumer added (2026-05-21) | **α₁b₂-A evolution + α₁b₂-B verification:** evolved from the α₁b₁ "no caller yet" boundary to the α₁b₂-A "exactly one allowlisted caller" boundary. The single allowlisted caller is `src/app/(shell)/diagnostics/recommendation-triggers/actions.ts` (the operator-only `generateLlmDraftAction`). Pattern mirrors `recommendation-intelligence-promotion-live-write-guards`. **α₁b₂-B confirmed unchanged:** the page UI consumer (LLM-Draft Preview section + result banner) imports `generateLlmDraftAction` from `./actions` as a Server Action function reference — NOT a gateway import. page.tsx still does NOT import the gateway module or reference `draftProposedTextForCandidate` / `openaiProvider`; the global allowlist scan still finds EXACTLY ONE importer + caller = actions.ts. Page render NEVER invokes the LLM (Next.js Server Actions semantics: SSR embeds an encrypted action ID into the form; the action runs only on explicit POST). **Current contract (6 source-text pins on `page.tsx` + 9 source-text pins on `actions.ts` + 2 global allowlist scans across `src/app/**` + 2 file-exists pins, 19 cases total)**: PAGE: does NOT import `@/domains/recommendation-intelligence/llm-draft-gateway`, does NOT reference `draftProposedTextForCandidate`, does NOT import `@/domains/recommendations/providers/openai`, does NOT reference `openaiProvider`, does NOT reference `openaiProvider.generate`, does NOT call `fetch(`. ACTION: imports + references `isOperatorModeServer`, `isLlmDraftGatewayEnabled`, `buildThinPacketForCandidate`, `draftProposedTextForCandidate` (positive pins); does NOT import `recommended-edits-persistence`, does NOT reference `runProviderAndPersist`, does NOT import `openaiProvider`, does NOT reference `openaiProvider` symbol, does NOT contain Supabase `recommended_edits` write shape (negative pins — defense-in-depth alongside `recommendation-intelligence-no-queue-write`). GLOBAL: EXACTLY ONE file under `src/app/**` imports the gateway module + EXACTLY ONE file under `src/app/**` calls `draftProposedTextForCandidate(` — both must be the action file. Defense-in-depth alongside `recommendation-intelligence-llm-draft-gateway-contract` (pins the gateway module's internal contract) and `recommendation-intelligence-no-queue-write` (separately pins persistence-import + Supabase-write boundaries across the whole intel tree + this same action file). | active | Permanent at the LLM-draft caller boundary. Refines only if a SECOND legitimate caller is introduced (e.g., a future cron or background job) — the allowlist set extends and the global allowlist scan updates accordingly. | 2026-05-21 |
| recommendation-safety-audit-coverage | `tests/architecture/recommendation-safety-audit-coverage.test.ts` | Slice 4.5.G-A.1 — recommendation safety audit scanner + coverage invariant (2026-05-21); EXTENDED in Slice 4.5.G-B.4a — generic claim-risk classification coverage (2026-05-22) | **B.4a extension (+15 cases, 30 total):** in addition to the A.1 locked-set pins, now also pins the generic claim-risk classification: (a) scanner source declares `CLAIM_RISK_BY_TOKEN` + `CLAIM_RISK_UNLOCK` + `classifyClaim` + all 6 locked claim-risk categories (`professional_credential` / `process` / `ranking` / `award` / `guarantee_outcome` / `superiority`); (b) behavioral — for EVERY `ARCHITECT_OVERCLAIM_TOKEN`, `auditRecommendedEditRow` emits a violation carrying a non-empty `claim_risk_category` AND `brand_supported === false` with no assertions (safe default), and `brand_supported` flips to `true` generically when a matching-phrase assertion is supplied (proves registry coverage + generic classification, no tenant-name logic); (c) the scanner test file exercises `brand_supported` true AND false + `claim_risk_category` + `normalized_match`, and uses SYNTHETIC tenants in active code (comment-stripped no-`Ritz Builders`/`ritz-builders`-logic pin). **Original A.1 pins (15 cases):** (1) all 9 locked `SafetyViolationKind` values are exercised in `tests/domains/recommendation-intelligence/safety-audit.test.ts` (one positive case per kind: placeholder / competitor_name / unsupported_claim / architect_overclaim / causal_language / em_dash / leading_superlative / internal_token / uuid_leak); (2) the exported `ARCHITECT_OVERCLAIM_TOKENS` constant contains EXACTLY the 10 operator-locked terms per K3 (architect-led, architect-designed, licensed, licensed architect, accredited, certified, endorsed, master craftsman, award-winning, top-rated) with no extras; (3) the exported `CAUSAL_LANGUAGE_TOKENS` constant contains EXACTLY the 9 operator-locked terms (drove, caused, generated, revenue, roi, leads, sales, converted, produced) with no extras; (4) the scanner source invokes `scanField(...)` on `row.proposed_text` and `row.why` at minimum (field coverage check). Adding a new violation kind / a new locked token / removing a locked token without updating this invariant trips CI before the slice can land. Defense-in-depth alongside `recommendation-safety-audit-read-only` (A.2 file-boundary pin), `recommendation-intelligence-no-queue-write` (auto-covers the scanner via `walk(INTEL_DIR)`), and `recommendation-intelligence-customer-copy-vocab` (pins the customer-template boundary). | active | Permanent at the safety-audit locked-set boundary. Refines whenever the scanner adds a new locked token list — the new list joins the coverage pin in lockstep. | 2026-05-21 |
| recommendation-safety-audit-read-only | `tests/architecture/recommendation-safety-audit-read-only.test.ts` | Slice 4.5.G-A.2 — recommendation safety audit diagnostic page + read-only file-boundary invariant (2026-05-21); EVOLVED in Slice 4.5.G-B.4a — scanner purity tightened + page brand-assertions boundary pin FLIPPED (2026-05-22) | Pins the read-only nature of the A.1 scanner (`src/domains/recommendation-intelligence/safety-audit.ts`) and the A.2 operator-only diagnostic page (`src/app/(shell)/diagnostics/recommendation-safety-audit/page.tsx`). **32 cases total (B.4a: +5 scanner pins; 1 page pin flipped negative→positive).** SCANNER: does NOT import `recommended-edits-persistence`, does NOT reference `persistRecommendedEditsLocal` / `syncRecommendedEdits` / `runProviderAndPersist`, does NOT contain Supabase `recommended_edits` write shape, does NOT import `openaiProvider` or `llm-draft-gateway`, does NOT call `fetch(`; **B.4a NEW scanner pins — does NOT import `brand-assertions` (assertions arrive via the `context.brandAssertions` structural shape), does NOT reference `getBrandAssertions`, does NOT import `@/lib/business-config` / `getBusinessConfig`, does NOT reference `getRepository`, does NOT contain a tenant-name-specific literal (`Ritz Builders` / `ritz-builders`) — proving the scanner stays generic + import-free.** PAGE: same persistence/provider/gateway/fetch negatives PLUS does NOT import validator modules, does NOT import the `action-types` registry, does NOT define a `"use server"` pragma, does NOT call `revalidatePath`, does NOT render `<form>` / `<button>`; positive pins on `isOperatorModeServer`, `notFound`, `auditRecommendedEditRow`; **B.4a FLIPPED pin — the page now DOES import + reference `getBrandAssertions` from `@/domains/recommendations/brand-assertions` (the read-only tenant-resolution boundary that maps assertions onto the scanner's structural shape; this was a negative pin in A.2, intentionally inverted in B.4a).** The audit surface stays end-to-end read-only — no mutation, no LLM, no network, no Supabase writes. Defense-in-depth alongside `recommendation-safety-audit-coverage` (locked-set + claim-risk pins), `recommendation-intelligence-no-queue-write` (auto-covers the scanner via `walk(INTEL_DIR)`), and `recommendation-intelligence-llm-draft-gateway-render-isolation` (LLM-caller boundary). | active | Permanent at the read-only audit boundary. Refines only if a future slice intentionally moves the audit from read-only to remediation (would require new write-side gates + a separate allowlist; none currently planned). | 2026-05-22 |
| recommendation-copy-sanitize-purity | `tests/architecture/recommendation-copy-sanitize-purity.test.ts` | Slice 4.5.G-B.2 — write-time copy-sanitizer purity + coverage contract (2026-05-21) | Pins TWO things about `src/domains/recommendations/copy-sanitize.ts`: (1) module purity — no `fetch(`, no Supabase, no LLM provider, no `recommended-edits-persistence` import, no brand-assertions, no validator, no `why-display-guard` (B.1 render path stays on its own), no `suggested-copy-display-guard`, no action-types registry, no Supabase `recommended_edits` write shape; pure string transformation only. Exports `sanitizeOperatorEvidenceText` (write-time entry called from `recommended-edits-persistence.ts` for `why` / `expectedImpact` / `measurementPlan` / `displayLabel`) AND `scrubInternalLeakagePatterns` (B.2 helper). (2) Coverage contract — source carries the long-hex pattern (`\b[0-9a-f]{32,}\b/gi`), uses `"prompt evidence"` as the hex/UUID fallback wording, contains literal substrings for the 9 word-boundary-friendly locked tokens (`aiSearchSignal`, `actualSearchQueries`, `action_type`, `trigger_signal`, `evidence_tier`, `rec_id`, `source_rec_id`, `diagnostic_only`, `customer-queue-ready`) AND a single shared `Mode\s+[ABC]` regex covering the three Mode labels — 12 locked internal tokens total, matching B.1's render-guard blocklist exactly; carries the operator-locked replacement-style strings (`"search-intent signals"`, `"observed search-intent signals"`, `"Beacon's evaluation mode"`). PLUS deferral pin: source does NOT match `architect-led` / `architect-designed` / `\bbest\b` literal replacement keys (`architect_overclaim` + `unsupported_claim` deferred to slice 4.5.G-B.4); source does NOT contain `validateCompetitorPublicCopy` / `competitorNames` (competitor scrubbing lives on the separate validator path, NOT in this sanitizer). **30 cases total — 1 file-exists + 12 module-purity pins + 4 coverage pins + 9 per-token literal pins + 1 shared-regex pin + 1 replacement-style spot-check + 4 deferral pins.** Defense-in-depth alongside `recommendation-why-render-guard` (B.1; guards the SAME 4 reason kinds at render time on a separate code path) and `recommendation-safety-audit-coverage` (A.1; detects all 9 historical violation kinds at audit time). B.2 closes the forward-prevention half of the loop: leaks scrubbed at row persistence so they never reach the database OR the customer surface. | active | Permanent at the write-time sanitizer boundary. Refines in slice 4.5.G-B.4 when `architect_overclaim` / `unsupported_claim` patterns join the sanitizer alongside the brand-assertion carve-out. Refines whenever the B.1 render-guard's blocklist gains/loses a locked token — the two lists MUST stay synchronized. | 2026-05-21 |
| recommendation-why-render-guard | `tests/architecture/recommendation-why-render-guard.test.ts` | Slice 4.5.G-B.1 — recommendation `why` render-time guard contract (2026-05-21) | Pins TWO things: (1) the guard module `src/domains/recommendations/why-display-guard.ts` is pure — no `fetch(`, no `@/lib/persistence/supabase`, no `@supabase/supabase-js`, no `openaiProvider` / `@/domains/recommendations/providers/openai`, no `recommended-edits-persistence`, no `@/domains/recommendations/brand-assertions`, no `specific-edit-validator` / `validateSpecificEdit`, no `suggested-copy-display-guard`, no `@/domains/recommendations/action-types` / `ACTION_TYPE_REGISTRY`, no Supabase `recommended_edits` write shape; exports the locked fallback string verbatim (`"Beacon has additional context for this recommendation, but it needs review before showing here."`); exports `checkWhyDisplaySafe` function. (2) Every customer-facing recommendation render path that surfaces `why` imports + references `checkWhyDisplaySafe` — the 4 LOCKED render sites are `src/app/(shell)/recommendations/[id]/recommendation-detail-client.tsx`, `src/app/(shell)/recommendations/[id]/suggested-copy-act.tsx`, `src/app/(shell)/recommendations/recommendations-client.tsx` (legacy drawer), and `src/components/recommendations/v2/recommendation-v2-card.tsx` (v2 card). Each site MUST exist, import from `@/domains/recommendations/why-display-guard`, AND have ≥2 occurrences of `checkWhyDisplaySafe` in active code (import + at least one call site). **24 cases total — 1 file-exists pin on the guard + 11 source-text pins on the guard module (10 negatives + 1 positive on `WHY_DISPLAY_GUARD_FALLBACK` literal + 1 positive on `checkWhyDisplaySafe` export) + 12 site-level pins (4 render sites × 3 pins each: file-exists + import + ≥2 references).** Guards the customer-visible `why` field against 4 violation kinds (uuid_leak, long_hex_hash, internal_token, competitor_name) AT RENDER TIME — defense-in-depth alongside the existing `recommendation-safety-audit-coverage` (which scans `recommended_edits` rows for the same kinds at audit time) and `suggested-copy-display-guard.ts` (which guards `proposed_text` at render time). Per B.1 scope discipline, the guard intentionally does NOT block unsupported_claim or architect_overclaim — those defer to slice 4.5.G-B.4 to preserve legitimate Ritz brand-asserted phrasing ("architect-led design-build") during the production audit follow-up. | active | Permanent at the customer-facing `why` render boundary. Refines in slice 4.5.G-B.4 when `unsupported_claim` and `architect_overclaim` are added to the guard's reason union after the Ritz brand-assertion carve-out lands. Adding a new render site that surfaces `why` requires extending `RENDER_SITES` in lockstep with the production code. | 2026-05-21 |
| recommendation-intelligence-no-llm-decides | `tests/architecture/recommendation-intelligence-no-llm-decides.test.ts` | Slice 4.5.B.α₀ — no-LLM-decides-existence contract (2026-05-19) | For every action type currently flagged `generatorActive: true` in `ACTION_TYPE_REGISTRY`: either (a) it appears in the historical-deterministic set (`edit_title`, `add_h2_section`, `add_faq`) and `src/domains/recommendations/providers/deterministic.ts` references it by name (snake_case OR camelCased generator name), OR (b) it is emitted as an `action_type: "<value>"` literal (object-shape or `actionType = "<value>" as const` variable-shape) by at least one trigger predicate file under `src/domains/recommendation-intelligence/triggers/`. Blocks future slices from flipping a `generatorActive` flag without shipping a paired deterministic generator or trigger predicate emitting that action type. Pure code scan; no fixtures. | active | Permanent at the LLM-decides boundary. Refines each slice that flips a new `generatorActive` flag — the test catches the new active type and demands a paired predicate or generator on the spot. | 2026-05-19 |
| recommendation-intelligence-customer-copy-vocab | `tests/architecture/recommendation-intelligence-customer-copy-vocab.test.ts` | Slice 4.5.B.α₀ — customer-copy vocab safety (2026-05-19); EXTENDED in α₁ (2026-05-19) with 3 no-arg H1 templates; EXTENDED in α₂ (2026-05-19) with 2 count-aware duplicate templates probed at integer values [2, 3, 5, 10]; EXTENDED in 4.5.C.α₀ (2026-05-19) with 5 no-arg indexability-remediation templates; EXTENDED in 4.5.C.α₃a (2026-05-20) with 1 no-arg internal-linking template (`addInternalLinkCopy`); EXTENDED in 4.5.C.α₃b (2026-05-20) with 1 no-arg structured-data template (`addSchemaCopy`); EXTENDED in 4.5.E.α₁a (2026-05-21) with 1 no-arg H2-rewrite template (`rewriteH2Copy`) | Behavioral test that invokes every exported function in `src/domains/recommendation-intelligence/customer-copy-templates.ts` with a registered probe set (α₀ 2 no-arg + α₁ 3 no-arg + α₂ 2 count-aware + 4.5.C.α₀ 5 no-arg + 4.5.C.α₃a 1 no-arg + 4.5.C.α₃b 1 no-arg + 4.5.E.α₁a 1 no-arg = 15 templates total). Each output MUST pass the customer-copy safety scan: NO internal taxonomy tokens (`trigger_signal` / `action_type` / `actionType` / `evidence_tier` / `derivedConfidence` / `Mode A` / `Mode B` / `Mode C` / `aiSearchSignal` / `actualSearchQueries` / `source_rec_id` / `rec_stable_key`), NO revenue/causal overclaim (`drove` / `caused` / `generated` / `revenue` / `dollars` / `roi` / `primary recommendation`, case-insensitive), NO connector-internal names (`Google Analytics` / `GA4` / `CallRail`), NO UUID-shape strings, and NO literal `$` character. Also fails if a new exported template is added without a paired probe-set entry — forces the slice that adds the template to land the test update in lockstep. | active | Permanent at the customer-copy boundary. Each later slice that adds a new template under this module MUST register a probe set for it; otherwise the invariant fails with a "missing probe set" error before the slice can land. | 2026-05-20 |

## Section 4.5 / Slice 4.5.B.α₂.1 — diagnostic source + copy guards

| name | source test file | section | purpose (one sentence) | status | retirement-condition | last-verified |
|---|---|---|---|---|---|---|
| recommendation-triggers-diagnostic-source-and-copy | `tests/architecture/recommendation-triggers-diagnostic-source-and-copy.test.ts` | Slice 4.5.B.α₂.1 — diagnostic source + copy guards (2026-05-19); EXTENDED in 4.5.C.α₂ (2026-05-20) with positive assertions for the diagnostic-only bucket render contract | Pins TWO production-validation regressions surfaced by operator manual smoke after α₂ shipped. **(1) Loader source boundary**: `src/domains/recommendation-intelligence/load-trigger-candidates-for-tenant.ts` MUST import `getRepository` from `@/lib/persistence/repositories` AND MUST NOT import `getPageSnapshots` from `@/domains/pages/snapshot-store`. The bare `getPageSnapshots()` function call (file-only boundary that reads `.data/page-snapshots.json` — never deployed to Vercel) is forbidden via negative-lookbehind regex; only `.getPageSnapshots(` (repository method) is allowed. **(2) Page copy slice-version drift**: `src/app/(shell)/diagnostics/recommendation-triggers/page.tsx` MUST NOT contain hardcoded slice-version strings matching `/Slice\s+4\.5\.B\.α/` NOR the legacy "2 metadata deterministic trigger predicates" / "2 metadata predicates" literals. The page MUST interpolate `result.meta.predicates_run` from the loader so future predicate additions surface automatically without paired text edits. **(3) Diagnostic-only bucket render (Slice 4.5.C.α₂)**: page source MUST contain `data-diagnostic-section="diagnostic-only"`, MUST reference `result.diagnostic_only`, MUST expose `data-counter="diagnostic_only_count"`, AND MUST include the operator-locked header copy "Diagnostic-only signals (low-confidence)". Without these, low-confidence Tier-2 predicates that route to the `diagnostic_only` bucket would be invisible to operator validation. Defense-in-depth alongside the customer-copy-vocab invariant (which scans templates) and the no-queue-write invariant (which scans the same diagnostic page for write paths). | active | Refines if a future slice intentionally introduces a new diagnostic-page source (e.g., adds a per-snapshot inspection route) — the new file would extend the scope. Retires permanently only if the diagnostic page is removed (no plan to do so; this is the operator-validation surface for the entire 4.5 trigger family). | 2026-05-20 |

## Section 4.5 / Slice 4.5.B.α₂.2 — page-intelligence + trigger applicability

| name | source test file | section | purpose (one sentence) | status | retirement-condition | last-verified |
|---|---|---|---|---|---|---|
| recommendation-triggers-page-classifier-applied | `tests/architecture/recommendation-triggers-page-classifier-applied.test.ts` | Slice 4.5.B.α₂.2 — page-intelligence/applicability layer (2026-05-19) | Pins the philosophy that EVERY trigger predicate must consult the universal `page-classifier` (8 page types: `homepage` / `city` / `service` / `project` / `hub` / `utility` / `technical_asset` / `other`) before emitting a candidate. Operator-locked principle: every website is different (billions of variations); the system stays pattern-based and config-driven, never tenant-specific slug-hardcoded. **Rules (5)**: (1) every predicate file imports at least one export from `../page-classifier` (`isNonHtmlAsset` or `classifyPageType`); future opt-outs require an explicit `@no-classifier-required` JSDoc tag; (2) `title-h1-mismatch.ts` references `classifyPageType` AND restricts emission to homepage/city/service (pinned via per-allowed-type literal scan); (3) `weak-h1.ts` references `classifyPageType` AND no longer contains the pre-α₂.2 inline `classifyForGate` helper (regression guard) AND no longer contains bare `.includes("/locations` / `/services` / `/projects` substring checks (forces delegation to the classifier's segment-bounded prefix matching); (4) predicate sources do NOT contain hardcoded tenant-specific URL slugs (`ritz`, `atherton`, `palo-alto`, `whole-home-remodel`, etc.); (5) predicate sources do NOT declare their own utility/suppress/blacklist literals — those live in the central `page-classifier.ts` only. Defense-in-depth alongside `recommendation-trigger-predicates-purity` (which forbids connector/LLM/Supabase imports) and `recommendation-intelligence-no-queue-write` (which forbids `recommended_edits` writes). | active | Refines on every Section 4.5 slice that adds a new predicate — the predicate-imports-classifier rule auto-extends. Retires only if the page-classifier itself is replaced or removed (no plan to do so; it's the universal applicability layer for the entire trigger family). | 2026-05-19 |

## Section 4.5 / Slice 4.5.D.α₀a.1 — promotion eligibility + priority scoring foundation

| name | source test file | section | purpose (one sentence) | status | retirement-condition | last-verified |
|---|---|---|---|---|---|---|
| recommendation-intelligence-promotion-eligibility-pin | `tests/architecture/recommendation-intelligence-promotion-eligibility-pin.test.ts` | Slice 4.5.D.α₀a.1 — promotion-eligibility table pin (2026-05-20); EXTENDED in 4.5.E.α₁a (2026-05-21) with the first LLM-assisted pair `weak_h2::rewrite_h2` = "diagnostic-only" | Pins the locked (trigger_signal, action_type) → eligibility tier table (17 entries post-α₁a: 6 customer-queue-ready + 7 operator-review-only + 4 diagnostic-only). Includes the critical α₃b operator correction: `robots_blocks_ai_bots → fix_robots` (NOT `fix_noindex`). 4.5.E.α₁a adds `weak_h2::rewrite_h2` at the operator-locked diagnostic-only tier — first LLM-assisted pair. Drift requires explicit operator decision in a paired test update; no entry can quietly migrate to a different tier. The actual ENFORCEMENT path (safety gates that consume this tier) ships in Slice 4.5.D.α₀a.3; this slice pins the table alone so downstream slices can build against a frozen contract. | active | Refines on every Section 4.5 slice that activates a new predicate (4.5.E.α₂/α₃ will add `rewrite_faq` + `refresh_stale_page` pairs; 4.5.D.α₂ may graduate operator-review-only pairs to customer-queue-ready). Each slice updates LOCKED_TABLE in lockstep. | 2026-05-21 |
| recommendation-intelligence-priority-score-contract | `tests/architecture/recommendation-intelligence-priority-score-contract.test.ts` | Slice 4.5.D.α₀a.1 — priority-score formula contract pin (2026-05-20) | Behavioral pin of the three locked formula rules in `src/domains/recommendation-intelligence/priority-score.ts`: (1) the +25 indexability-blocker bonus applies iff `action_type.startsWith("fix_")` AND confidence ≠ "low"; (2) at the same page-importance, medium/high indexability blockers outrank content polish (verified across homepage/city/service/project/hub); (3) confidence: "low" rows do NOT receive the indexability bonus (defense-in-depth against a future low-confidence fix_* emitter). Sample scores fixed at concrete numerics so any algebraic drift surfaces immediately. | active | Permanent at the priority-formula boundary. Refines if the operator re-tunes SEVERITY / PAGE_IMPORTANCE / EFFORT tables in a later slice (a paired test update locks the new values). | 2026-05-20 |

## Section 4.5 / Slice 4.5.D.α₀a.2 — dedupe + cooldown foundation

| name | source test file | section | purpose (one sentence) | status | retirement-condition | last-verified |
|---|---|---|---|---|---|---|
| recommendation-intelligence-dedupe-key-formula | `tests/architecture/recommendation-intelligence-dedupe-key-formula.test.ts` | Slice 4.5.D.α₀a.2 — dedupe + cooldown key formula pin (2026-05-20) | Locally recomputes both sha1 formulas and asserts byte-for-byte equality against the module exports. Dedupe is 5-part `sha1(tenant_id::action_type::target_url_or_no_url::target_element_key_or_no_key::topic_cluster_label_or_no_topic)`; cooldown is 3-part `sha1(tenant_id::action_type::target_url_or_no_url)`. Sentinels for nulls (`no_url`, `no_key`, `no_topic`) pinned. Distinct from the emitter dedupe key (4-part) — promotion uses element-key precision so two recs that touch different elements on the same page survive as distinct queue rows. Drift breaks the cooldown lookup and is a hard failure. | active | Permanent at the promotion-key boundary. | 2026-05-20 |
| recommendation-intelligence-cooldown-windows | `tests/architecture/recommendation-intelligence-cooldown-windows.test.ts` | Slice 4.5.D.α₀a.2 — cooldown-window table pin (Section 4.5.O6 + α₀a.2 operator decisions Q1/Q2, 2026-05-20) | Pins the locked window (days) per `ImplementationStatus` + `no_prior` sentinel: `dismissed=90` · `accepted=30` · `verified_live=180` · `verified_live_modified=180` · `wrong_page=14` · `partially_implemented=60` · `needs_review=14` · `recommended=0` · `not_found_after_7d=0` (Q1) · `no_prior=0`. Asserts the table key set is exactly those 10 entries (no extras, no missing). Includes a negative assertion that `verified_live_decay_refire` is NOT present (Q2: not a real status; future stale/decay pathway is a separate slice). | active | Permanent unless operator re-locks windows or introduces a real decay status in a future slice with a paired test update. | 2026-05-20 |

## Section 4.5 / Slice 4.5.D.α₀a.3a — promotion safety-gates module

| name | source test file | section | purpose (one sentence) | status | retirement-condition | last-verified |
|---|---|---|---|---|---|---|
| recommendation-intelligence-no-diagnostic-only-promotion | `tests/architecture/recommendation-intelligence-no-diagnostic-only-promotion.test.ts` | Slice 4.5.D.α₀a.3a — diagnostic-only pairs MUST NEVER promote (2026-05-20) | Parametric over every locked diagnostic-only pair in the live `PROMOTION_ELIGIBILITY_TABLE` (3 pairs today: `missing_schema → add_schema`, `noindex_on_indexable_page → fix_noindex`, `robots_blocks_ai_bots → fix_robots`). Each fed through `applyPromotionSafetyGates` returns `eligible: false` with `suppression_reason: "diagnostic_only_tier"`. Auto-extends if the eligibility table grows. | active | Permanent at the diagnostic-only boundary. | 2026-05-20 |
| recommendation-intelligence-confidence-low-stays-diagnostic | `tests/architecture/recommendation-intelligence-confidence-low-stays-diagnostic.test.ts` | Slice 4.5.D.α₀a.3a — confidence: "low" never reaches the customer queue (2026-05-20) | Defense-in-depth: every locked customer-queue-ready (trigger, action) pair at `confidence: "low"` is suppressed by Gate 4 with `suppression_reason: "low_confidence"`. Today no customer-queue-ready emitter produces "low", but the gate stays the canonical enforcement point so a future predicate flip to low without explicit re-tiering is caught. | active | Permanent at the low-confidence boundary. | 2026-05-20 |
| recommendation-intelligence-promotion-respects-already-accepted | `tests/architecture/recommendation-intelligence-promotion-respects-already-accepted.test.ts` | Slice 4.5.D.α₀a.3a — promotion respects already-accepted ancestor (2026-05-20) | When a prior `recommendedEdits` row at the same promotion dedupe key has reached `accepted` / `verified_live` / `verified_live_modified` / `partially_implemented`, `applyPromotionSafetyGates` suppresses new candidates with `suppression_reason: "accepted_ancestor_exists"`. An ancestor in `recommended` status does NOT suppress (operator hasn't acted yet). Prevents Beacon from re-emitting a recommendation the operator already actioned. | active | Permanent at the ancestor-suppression boundary. | 2026-05-20 |
| recommendation-intelligence-page-classifier-applied-at-promotion | `tests/architecture/recommendation-intelligence-page-classifier-applied-at-promotion.test.ts` | Slice 4.5.D.α₀a.3a — page-classifier applied at promotion (2026-05-20) | Carries the upstream `recommendation-triggers-page-classifier-applied` invariant to the promotion edge. Content-edit families (`edit_title`, `edit_meta`, `change_h1`, `add_h2_section`, `add_faq`, `add_schema`, `add_internal_link`) on utility / technical_asset / other page types → `suppression_reason: "skip_page_type"`. Indexability fixes (`fix_*`) BYPASS this skip because operators may need to repair sitemap/robots/status/canonical on any URL kind. Defense-in-depth against a trigger-engine bug leaking a utility-page rec to the customer queue. | active | Permanent at the promotion-time page-type boundary. Refines if a future `PageType` is added to the universal classifier. | 2026-05-20 |
| recommendation-intelligence-no-promotion-without-evidence | `tests/architecture/recommendation-intelligence-no-promotion-without-evidence.test.ts` | Slice 4.5.D.α₀a.3a — promotion requires evidence (2026-05-20) | Empty evidence on a candidate row yields `suppression_reason: "no_evidence"` at Gate 6. Non-empty evidence proceeds past the evidence gate. Promotion-time guarantee: the customer-facing recommendation surface can always show "why" we suggested this edit. | active | Permanent at the evidence boundary. | 2026-05-20 |

## Section 4.5 / Slice 4.5.D.α₀a.3b — promotion orchestrator + caps

| name | source test file | section | purpose (one sentence) | status | retirement-condition | last-verified |
|---|---|---|---|---|---|---|
| recommendation-intelligence-max-rows-per-page | `tests/architecture/recommendation-intelligence-max-rows-per-page.test.ts` | Slice 4.5.D.α₀a.3b — MAX_ROWS_PER_PAGE cap locked at 5 (Section 4.5.O7) (2026-05-20) | Behavioral: 6 distinct customer-queue-ready signals on the same URL produce exactly 5 eligible rows; the surplus row flips to `eligible: false` with `suppression_reason: "max_rows_per_page"`. Pins `MAX_ROWS_PER_PAGE = 5` constant. Surplus rows are NOT deleted — they remain in the output for operator-only Promotion Preview (α₀b) visibility. | active | Permanent unless operator re-locks the cap. | 2026-05-20 |
| recommendation-intelligence-max-rows-per-family | `tests/architecture/recommendation-intelligence-max-rows-per-family.test.ts` | Slice 4.5.D.α₀a.3b — MAX_ROWS_PER_FAMILY cap locked at 10 (Section 4.5.O8) (2026-05-20) | Behavioral: 11 candidates across 11 distinct URLs, all same `action_type` (`edit_title`), produce exactly 10 eligible rows; the surplus row flips to `eligible: false` with `suppression_reason: "max_rows_per_family"`. Pins `MAX_ROWS_PER_FAMILY = 10` constant. Surplus rows are NOT deleted. | active | Permanent unless operator re-locks the cap. | 2026-05-20 |

## Section 4.5 / Slice 4.5.D.α₀b — operator-only Promotion Preview UI

| name | source test file | section | purpose (one sentence) | status | retirement-condition | last-verified |
|---|---|---|---|---|---|---|
| recommendation-triggers-promotion-preview-no-writes | `tests/architecture/recommendation-triggers-promotion-preview-no-writes.test.ts` | Slice 4.5.D.α₀b — Promotion Preview no-writes contract (2026-05-20) | Defense-in-depth source-text scan over `/diagnostics/recommendation-triggers/page.tsx`. Negative: no `.from("recommended_edits").{insert/upsert/update/delete}(`, no `runProviderAndPersist`, no `recommended-edits-persistence` substring. Positive: page imports `selectPromotableCandidates`; page source contains `data-diagnostic-section="promotion-preview"`, the locked title `Promotion Preview (DRY-RUN — no writes)`, `data-counter="promotion-preview-counters"`, and `Eligible for promotion:` counter literal. Pins the section's existence so future maintenance can't silently remove the operator-only render contract. | active | Permanent at the promotion-preview render boundary. Refines if the customer-queue writer (α₁) lands and the diagnostic page renders BOTH a preview + a writer-status surface — at which point this invariant narrows to "no DIRECT write inside the diagnostic page; writes go through the writer helper." | 2026-05-20 |

## Section 4.5 / Slice 4.5.D.α₁a — promotion writer (pure mapper)

| name | source test file | section | purpose (one sentence) | status | retirement-condition | last-verified |
|---|---|---|---|---|---|---|
| recommendation-intelligence-promotion-writer-source-pin | `tests/architecture/recommendation-intelligence-promotion-writer-source-pin.test.ts` | Slice 4.5.D.α₁a — promotion writer source pin (2026-05-20) | Pins three contracts. (1) `SpecificEditSource` union in `specific-edit-provider.ts` is EXACTLY the 5-value set `{deterministic, deterministic_promotion, openai, anthropic, operator_edited}` — drift fails the test. (2) The mapper file `promotion-result-to-edit-row.ts` references the literal `"deterministic_promotion"` in source. (3) Behaviorally: `promotionResultToRecommendedEditRow` returns `source: "deterministic_promotion"` on every produced row (parametric over high + medium confidence). Pins provenance so α₀a-trigger-engine-promoted rows are distinguishable from legacy `"deterministic"` (the deterministic specific-edit provider) and LLM-generated rows. | active | Permanent at the source-provenance boundary. Refines if a future slice introduces an additional source value with paired operator approval. | 2026-05-20 |
| recommendation-intelligence-promotion-writer-eligibility-pin | `tests/architecture/recommendation-intelligence-promotion-writer-eligibility-pin.test.ts` | Slice 4.5.D.α₁a — promotion writer eligibility pin (2026-05-20) | Behavioral pin of the 5 defensive rejection conditions in `promotionResultToRecommendedEditRow`. Mapper returns `null` for: (1) `result.eligible === false`; (2) `result.tier === "diagnostic-only"`; (3) `result.tier === "operator-review-only"`; (4) `result.tier === "blocked"`; (5) `result.candidate.target_url === null`; (6) `result.candidate.confidence === "low"`; (7) `result.candidate.safety_flags.length > 0`. Plus 1 positive sanity case (happy path returns non-null). Defense-in-depth: the α₀a.3a safety gates already enforce these upstream, but the mapper double-checks at the customer-queue boundary so a future regression elsewhere in the engine cannot leak an unsafe row. | active | Permanent at the customer-queue boundary. The mapper IS the canonical shape for everything that ever crosses the boundary; α₁b's writer + α₁c's UI all consume mapper output, so this invariant gates all three. | 2026-05-20 |

## Pending entries (Section 12.1 self-reference)

| name | source test file | section | purpose (one sentence) | status | retirement-condition | last-verified |
|---|---|---|---|---|---|---|
| catalog-sync | `tests/architecture/catalog-sync.test.ts` | Section 12.1 (2026-05-14) | Catalog row count equals architecture-test file count; bidirectional referential integrity. | active | Permanent — catalog discipline floor. | 2026-05-14 |
