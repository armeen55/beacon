# Night Shift Log — 2026-06-10/11 (until 05:00 PT)

One line per hour: time, items shipped, commits, next.

- 23:52 — shift start. Fuel: inventory WIRE items first. Item 1: verify per-tenant coverage of the citation-evidence rebuild cron (#112).
- 00:09 — item 1 done: citation_evidence_index per-tenant end-to-end (migration applied, route loops tenants, store rewrite + isolation pin). Next: answer_intelligence_index same disease.
- 00:24 — items 2-4 done: answer-intel per-tenant (PR 12 merged), response/action store cache fixes + comment-aware ratchet (allowlist 9→8), nightly queue sweeper (expired status). Next: heal tonight's index rebuild on prod, then competitor auto-seed (#24).
- 00:36 — items 5-6 done: nightly queue sweeper shipped to branch; watchdog now covers scan+generation+poll with generation heartbeat; sitemap retry; scan failure alert. Suite 14,122/0. Shipping PR 13.
- 00:44 — #24 auto-seed committed. #26 verified bounded (answer_texts keyed by tenant-disjoint observation ids — hygiene not bleed; consumption half stays inventory item). Next: >500-line sweep.
- 00:55 — change-contracts scoped (allowlist 7), tenant switcher shipped (+ owner memberships for all 3 tenants), fleet scan SUCCEEDED on main (both tenants), nightly url-map re-sync wired (#111). Next: edit_meta/change_h1 → customer-queue-ready? No — operator-locked table. Next: docs sync + ship batch 3.
- 01:00 — sweep done: issues+attribution stores per-tenant (6th/7th cache instances), ratchet learns repo-var shape + watches 6 more getters, seed-data aggregator frozen visibly. Suite 14,133/0. Shipping PR 14.
- 01:05 — content rules per-tenant (#35/#67) committed; docs sync (inventory status block + ledger addendum).
- 01:12 — INCIDENT: PR 14's build FAILED (use-server file exported a const) and my shell chain merged it anyway → main red ~10 min. Hotfix: tenant-cookie module extracted; local prod build verified green. Lesson encoded: assert 'pass' explicitly before merge, never grep&&merge.
- 01:17 — main healed (PR 15 merged on explicit pass). Triggers 17+18 landed: thin-overlap merge detector (#43) + stale-content (#44), both diagnostic-only. Next: full gates + ship batch.
- 01:27 — PR 16 merged (triggers 17/18 on main). #82 pre-push snapshots+revert committed; #73 url-map live probes committed. Next: full gates + ship batch 5.
- 01:34 — full suite flaked once (6 fails) then clean 14,154/0 on immediate rerun — suspected worker-order pollution; watching CI for recurrence.
- 01:50 — flake non-reproducible (3× clean full runs, 14,160/0). Shipped tonight since last log: #126 per-tenant console gates, live_text capture substrate (#100), first-citation receipts (#94). Deferred with note: #149 geo-tag de-hardcode (display-only, wide ripple — daylight slice).
- 02:00 — PR 18 merged. Landed since: digest deep links via /api/tenant-switch (#118), engine tag on first-citations (#52-lite), create_page verification matcher (#90). Next: post-push regression alarm (#96 v1).
- 02:10 — landed: accepted_at stamp (#127 substrate) + median time-to-approve digest line + batch accept (#84, accept-only). Audit agent restocking the work queue. PR 19 in slow CI (GH congestion).
- 02:25 — audit wave done: 8 more per-tenant cache conversions (incl. candidates.ts topic index, which had been silently EMPTY since tenant routing — restored from the live store) + 11 silent catches now log. Suite 14,179/0. Shipping PR 20.
- 02:35 — PR 20 merged. #48 unified queue ordering (drafted>confidence>recency) + digest diagnostic-findings count + GLOBAL_TABLES correction + audit item 14. Full-pipeline generation proof run in flight.
- 02:50 — proof run green end-to-end on main (generation idempotent at queue=14, sweeper, auto-seed 8 rivals → platform-exclusion fix after youtube.com slipped in, heartbeats). PR 21 (batch 9) in CI. Stats so far: 9 PRs merged tonight, 66 commits, tests 14,003→14,184, 4 additive migrations applied.
- 02:50 — PR 21 merged (10 PRs tonight). Lint clean on all new modules; 2× consecutive full suites 14,184/0; prod deployed (b61e54c). wix-page contract test written (the file its docstring claimed). API-route auth sweep agent launched.
- 02:53 — API auth sweep done: every /api route correctly gated; hardened the one defense-in-depth gap (Google OAuth callback now verifies tenant membership before the service-role token write). wix-page contract test added. Suite 14,187/0. Shipping PR 22.
- 03:02 — round-2 audit verified (2 of 3 findings were false/marginal — codebase is solid); kept parseInt radix sweep + freshness NaN guard. Suite green on touched domains.
- 03:05 — #54 dismiss-reason substrate + parseInt/freshness robustness shipped (PR 23). 12 PRs tonight. Suite 14,189/0.
- 03:18 — chain health verified: scan GREEN on schedule (both tenants, ~4h GH lag), generation GREEN by dispatch, poll skipped by GH (watchdog deployed to catch at 11:00 UTC; needs PAT+keys → WAITING). PR 25 (dismiss picker) in CI.
- 03:22 — dismiss-reason loop fully closed (picker→persist→aggregate→digest); digest at-a-glance summary header (#125). Folded into PR 25. Suite 14,194/0, local build green. ~1h40 to 05:00; chain verified (scan green, gen green by dispatch, poll operator-key-blocked).
- 03:32 — #147/#148 city extraction de-hardcoded per-tenant (committed). #149/#150 ledgered WAITING (client prop-plumb, display-only). Next: clean contained item.
- 03:42 — migration blast-radius sweep: fixed 2 ops scripts (backfill/bootstrap) my own per-tenant index migration would've broken; verified sync-wrapper invariant self-consistent (citation/answer-intel pass via new tenantId param after leaving GLOBAL_TABLES). Added caps unit pins, city-extraction de-hardcode (#147/#148). Suite 14,208/0. Clean queue now exhausted of no-key/non-daylight items; remaining = operator-blocked or daylight-scale (ledgered). PR 26 carrying the batch.
- 03:51 — fuel #4 coverage: added caps, freshness, stripForbiddenFields unit pins (publish-safety + the night's NaN guard). Connector+extractor audit returned CLEAN (4th audit confirming codebase solidity). Suite 14,216/0. Watchdog window approaching (11:00 UTC).
- 04:09 — CROSS-TENANT CACHE-KEY SWEEP: found+fixed 5 genuine warm-process leaks the ratchet is blind to (brief-states, visibility-explicit, answer-snapshots, frontier-compiler, competitor-evidence) → per-tenant Maps + 15 isolation pins. Verified 5 others safe/global. Ledgered 2 daylight cold-store findings (citations-by-date flat clobber; answer-texts whole-map write) — legacy Profound write-path, needs cutover context. Commits 3ca6139, 937bda9, 4e1de8f. typecheck 0; 791 pages/intel/app tests green.
- 04:27 — Extended sweep found 3 MORE leaks beyond the first wave: answer-snapshots, frontier-compiler, competitor-evidence, and the big one — canonical-store (the 8-array core data spine, getter path; render path was already safe). All fixed via per-tenant Map. Added per-tenant-cache-key.test.ts ARCHITECTURE RATCHET (+catalogued) so the whole class can't regress. Total tonight: 6 leaks fixed, 18 isolation pins, 1 new ratchet. Commits 3ca6139→58f9d83. Final full suite running before push→PR.
- 04:49 — CLOSE: #27 + #28 merged (6 leaks + 2 ratchet guards). Final suite 745f/14,243t/0-fail. Rails verified intact (publish gate, Ritz-dev-note, caps, no cron-publish, crons untouched). Morning chain healthy (scan/gen success; poll watchdog-recovered, ChatGPT·iranopedia failed — operator OpenAI key). Residuals chipped/ledgered. Comprehensive cross-tenant cache-key sweep COMPLETE.
- 12:46 (DAY SHIFT, North-star: URL-only onboarding) — THE KEYSTONE LANDED: nothing ever WROTE a per-tenant business-config (operator hand-wrote the env blob). Built the full chain in 9 slices: (1) per-tenant config persistence (tenant file + per-tenant business_config row; legacy singleton preserved for env tenant); (2) hosted read — getBusinessConfigForCurrentTenant + crons hydrate from the per-tenant row (env blob still wins; priority pinned); (3) deriveBusinessProfile — pure cheerio extractor (JSON-LD/og/title/nav/tel), zero vertical-geo assumptions, 18 pins incl. Texas-builder no-Bay-Area-leakage; (4) polite ≤3-page site fetcher; (5) typed-beats-derived config mapper; (6) launch wires derive+persist BEFORE prompt insert, failure-soft; (7) vertical-agnostic prompts from derived services/industry ("best taco bar in Tucson"); (8) scope step unblocked — cities+projectMix optional (was: every vertical forced to claim builder facts; Bay-Area placeholder killed); (9) segment derived at launch (new local_service value + APPLIED additive CHECK migration — caught that the old CHECK would have failed the whole activation UPDATE at runtime). ACCEPTANCE PIN GREEN: URL-only tenant → complete correct config, survives restart, zero builder/Bay-Area tokens. 11 commits; full suite running; push+PR next.
- 13:17 — REAL-WORLD VALIDATION LOOP: ran the derivation against ritzbuilders.com + iranopedia.com live. Ritz derives perfectly; Iranopedia exposed 3 real bugs (Wix>10s timeout, areaServed noise, region-only-address false local signal) — fixed + pinned same hour, re-verified live. #31 (12-commit URL-only onboarding stack) + #32 (cities prefill + action pins) MERGED + deployed (prod HTTP 200). Hardening PR in CI. Suite 14,297/0 at the #31 gate.
- 13:46 — ZERO-HARDCODE SWEEP round 3: title suggestions no longer fabricate 'Custom Home Builder'/'Bay Area' for other verticals (tenant-vocabulary-first intent ladder; honest-null fallback); tenant cities threaded through morning-brief query relevance; topic tags use tenant services (#149-sibling); industry-directory sameAs mapping (yelp/tripadvisor/healthgrades/avvo/zillow); site-jsonld split (500-line discipline); launch wiring contract pins. #31/#32/#33 all MERGED + deployed. Suite 14,320/0 mid-day checkpoint.
- 14:46 — DAY-SHIFT CLOSE-OUT: #34 + #35 MERGED (tenant vocabulary everywhere; minute-one derived card; self-serve Wix + content guardrails; competitors optional — wizard minimum is NAME + URL). Round-3 live validation (rotorooter ✓, robots-respect ✓). Full suite 14,335/0; prod 200. Empty-state + Ritz-literal sweeps dry. Docs batch + closing report next.
- 14:37 — FINAL BUILD SLICE: launch-time first scan shipped (#37 pending merge) — only_tenant dispatch input, conditional concurrency (a launch scan can never cancel the nightly — hazard found+pinned), PAT-inert dispatcher wired into launch. Round-4 live check caught the Product-node org bug (squareup.com → 'Square Reader…' as business name; fixed+verified live). 10 sites/4 rounds total. Full suite 14,344/0.
- 17:15 (DREAM SHIFT — close the forecast↔prove loop) — THE CATEGORY WEDGE WENT LIVE END-TO-END. Verify-first found the diff-in-diff Proof Engine (natural-controls: adjusted_lift = treatedΔ − comparable-untreated-URL Δ) was fully built+tested but had ZERO runtime callers — loadChangeOutcomeById returned null in prod, the causal /changes/[id] drilldown was dark. Shipped 3 PRs: (#41) changelog-classifier (deterministic enum-grounded ChangelogEntry→ClassifiedEvent) + proof-engine orchestrator + wired into the nightly per-tenant generation cron (NO workflow-yaml edit; failure-soft; live-mode only) so proof persists on cadence → drilldown lights up. (#42) causal self-forecast — reads the Proof Engine's persisted outcomes back out as a forward-looking reference-class base rate ('M of your last N changes like this measurably lifted citations, ~+X%'), the dream's stage-3 forecast, working at n=1 (no 2nd tenant/network/paid API); honest floors (suppress <2, seeded <4), causal-grade-only denominator. (#43) surfaced it on /recommendations/[id] as a before-you-ship banner; extracted deriveTaxonomyTarget as the SHARED classifier helper so the forecast's reference class == the bucket its outcome will later be proven under (zero drift); rec→forecast bridge via ACTION_TYPE_REGISTRY signalType. All research-backed (DiD + parallel-trends + synthetic-control + reference-class forecasting/outside-view + AI-citation tracking, ≥5 sources in headers). #41+#42 MERGED; #43 in CI. Attribution suite 393/0; typecheck 0. Loop closed: derive→track→FORECAST→recommend→PROVE→[outcomes feed the next forecast].
- 18:00 (DREAM SHIFT cont. — the loop made REAL in prod) — REAL-DATA VALIDATION CAUGHT THE PRODUCTION-NO-OP BUG. After shipping #41/#42/#43 + (#44 docs, #45 plain-English proof sentence on the drilldown), I queried prod Supabase to validate the Proof Engine would actually produce proof — and found persistChangeOutcomes wrote ONLY the local json-store (skipped on Vercel, ephemeral on the cron runner; never dual-written; readStore never hydrates Supabase) → loadChangeOutcomeById read EMPTY in prod, so the whole loop was invisible to real users even after #41. FIX (#46, MERGED): additive change_outcomes_v2 table (full StoredChangeOutcome as JSONB, RLS mirrors peers, applied via MCP) + dual-write in persistChangeOutcomes (DUAL_WRITE-gated, failure-soft) + Supabase hydration in loadAllChangeOutcomes when local is empty (the hosted case). Then surfaced the now-durable proof: (#47) Today home-screen "Proven by Beacon" causal-wins rail (self-hiding, additive Suspense section, the strongest evidence vs the old Z-score rail); (#48) operator /diagnostics/proof-engine aggregate view (status histogram + top proven wins + per-bucket forecast base rates). Validated the loop will light up for Ritz: 309 eligible changelog entries, 20,947 citation observations (owned URLs, 2026-03-05→06-11), 21+ changelog edit URLs match cited owned URLs (treated-with-history), 8,404 cited owned URLs as controls. Flagged (chip): platform-case duplication (chatgpt vs ChatGPT) splits the per-platform breakdown — headline lift unaffected. #44/#45/#46/#47 MERGED; #48 in CI. Attribution 396/0; typecheck 0; all rails honored (deterministic, zero paid API, crons untouched, additive migration). Net: the category wedge is now built + legible + DURABLE + validated end-to-end; lights up on the next nightly cron.
- 18:34 (DREAM SHIFT cont. — surface + prioritize the wedge everywhere) — 4 more PRs on the durable loop. (#50) canonicalized platform case (chatgpt vs ChatGPT) so the per-platform diff-in-diff breakdown stops splitting — headline lift was already correct; this fixes the per-platform detail via the existing normalizePlatform at both by_platform write points. (#51) expected-value engine — EV = forecast.helpingRate × typicalRelativeLift (null, never 0, when unforecastable); rankByExpectedValue puts forecastable Moves first; surfaced on /diagnostics/proof-engine as the EV-ranked pending-Moves table (the plan's $499 "expected-value engine", operator-side; customer-list surfacing deferred — legacy table is the prod default + the rec detail already shows the per-Move forecast). (#52) the v2 premium "5-act proof brief" was MISSING the causal proof (showed only the Z-score pill) — added a self-hiding "What Beacon measured" causal block (feature parity with the legacy drilldown #45). FULL SUITE GREEN: 763 files / 14,443 tests / 0 failures; typecheck 0. Deploy chain CONFIRMED for tonight: generation cron sets DATA_SOURCE=supabase + DUAL_WRITE=true + live-write → the 05:30 UTC (≈22:30 PT) run executes the proof engine (#41) + dual-writes change_outcomes_v2 (#46) → the Vercel-deployed web app (#46 hydration) reads it → drilldown + plain-English proof + Today proven-wins + rec-brief forecast + operator diagnostic ALL light up tonight, no operator action. FLAGGED (chip, NOT built — privacy-sensitive cross-tenant reads, gated + needs operator review): the cross-tenant brain producer (computeCrossTenantPatterns) is fully built but has ZERO runtime callers, so the dream's "forecast from real PEER outcomes" never fires even at n≥2 + flag-on; wiring it to aggregate the now-persisted change_outcomes_v2 across tenants (through the brain-input anonymization boundary) is the next big item. #41–#51 MERGED; #52 in CI.
- 18:45 (DREAM SHIFT cont. — end-to-end correctness self-review) — TWO MORE latent bugs caught by reviewing my own night's work, both gating-related. (#54, CRITICAL) the #46 read-hydration loadChangeOutcomesFromSupabase was gated on isDualWriteEnabled() (DUAL_WRITE) — but DUAL_WRITE is the WRITE condition; the READ condition is DATA_SOURCE === "supabase" (what getRepository uses for every hosted read). The Vercel web app runs DATA_SOURCE=supabase but may not set DUAL_WRITE, so the drilldown/proven-wins/forecast could have stayed EMPTY on the web app even after the cron populated change_outcomes_v2 — silently dark. Fixed: read gates on DATA_SOURCE (write stays on DUAL_WRITE). Since every proof/forecast surface funnels through loadAllChangeOutcomes, #54 fixes them all at once. VALIDATED the full data flow against prod+code: cron (DATA_SOURCE=supabase + DUAL_WRITE=true) reads Ritz's 20,947 observations via the canonical-store Supabase merge (60-day window → covers the recent-change sweet spot the proof engine needs) → proof engine → dual-write UPSERT to change_outcomes_v2 (accumulates across nights, never deletes) → web app reads via #54. Loop is production-correct end-to-end. Also confirmed: visibility/progress data healthy + per-tenant (Ritz 18,676 snapshots, avg 29.7, 3mo; Iranopedia correctly sparse). #41-#53 MERGED; #54 in CI. Net for the dream shift: 13 feature PRs + the durable+correct proof↔forecast↔EV loop, lights up tonight at the 22:30 PT cron, verifiable at /diagnostics/proof-engine.
- 22:58 (DREAM SHIFT cont. — the proof loop produced its FIRST persisted outcome) — VERIFY-FIRST on prod found the loop STILL INERT (computed=0 on the 22:30 cron) despite all the dream-shift wiring, and traced + fixed THREE layered causes. (#57, verified) the per-tenant proof cron bled the founder's 289-entry changelog into Iranopedia (seed-data getChangelogEntries read a NON-tenant-scoped repository); proof-engine now reads forTenant + loads the FULL observation window — re-dispatch confirmed Iranopedia classified=0 (was 289), bleed gone. (#58) broadened that fix: seed-data.server refactored from one process-global `_state` to a per-tenant `_stateByTenant` Map read through forTenant(tenantId), burning down BOTH isolation-ratchet allowlists; +2 route-test gate mocks (those tests had been passing ONLY via the unscoped import-runs bleed they relied on). (#59, THE root cause) buildUrlCitationHistory exposes no ownedUrlPathSet param, so all three callers (proof engine, URL watcher, /changes) hit buildNativeDayBuckets' ownedOnly+no-set guard that did `continue` on EVERY citation — silently emptying ALL native-regime (post-NATIVE_REGIME_START=04-22) owned citation history since launch (the engine saw zero pre/post citations → insufficient_baseline for all 167 Ritz changes). Fixed with a self-contained owned-host fallback derived from the observation's aligned citation_domains + citation_domain_classes (deduped host + class). PROD-VERIFIED via cron re-dispatch (run 27397556284, headSha 9a157aa): Ritz proof moved persisted 0→1, weak 0→1, insufficient_baseline 167→166, no_controls 0→1 — the loop persisted its FIRST durable change_outcomes_v2 row. computed=0 is now HONEST data-sparsity, not a bug: NATIVE_REGIME_START=04-22 barely overlaps Ritz's changelog (ends 04-27; only 7/334 changes reach the native window), so the one change that cleared baseline then lacked comparable in-window controls (honest no_controls). It resolves as native polling accumulates, and is fully native (=working) for any NEW business. The 327 pre-04-22 changes need the benchmark cold-store→Supabase migration (ledgered daylight — .data/citations-by-date flat shards aren't present in the cron). Grounded on prod (5,960 native rows, all carry citation_domain_classes; 3,993 had owned citations the old guard dropped). Full suite 14,450/0; typecheck 0; both isolation ratchets green; all rails honored (deterministic, zero paid API, additive, crons untouched). #57/#58/#59 MERGED.
- 23:21 (DREAM SHIFT cont. — every eligible change now gets an honest status, not a silent panel) — Closing the loop's last UX gap. A change that's eligible but lacks pre-change citation history returns insufficient_baseline, which the proof engine SKIPPED → loadChangeOutcomeById returned null → the /changes/[id] drilldown rendered a SILENT empty proof panel (the common case for a young tenant or any change shipped before its page accrued ~14 days of citations). buildProofSentence ALREADY had honest copy for the status ("Not enough history before this change to measure its effect reliably yet. As this page builds a citation track record, Beacon will be able to measure future changes.") but it was unreachable because nothing persisted the status. (#61) persist insufficient_baseline (it already passes fromNaturalControlResult + the store's invariant validation as a computed=null/raw=null row) + a new "watching" counter on ProofEngineRunResult/the PROOF log to distinguish it from the weak/raw buckets. Net: EVERY eligible shipped change gets a visible, honest per-change status instead of a silent panel. Honesty preserved: insufficient_baseline carries NO computed + NO raw block, and both proven-wins and the self-forecast denominator are computed-only → watching rows never inflate a win or a base rate. PROD-VERIFIED (cron re-dispatch run 27398486254): Ritz PROOF now computed=0 weak=1 watching=166 persisted=167 skipped_ineligible=122 (was persisted=1, skipped_ineligible=288) — change_outcomes_v2 grew 1→167 rows, so all 167 of Ritz's eligible changes now render an honest drilldown; only the 122 truly-unmeasurable (wrong layer / no URL) stay skipped. Full suite 14,451/0; typecheck 0; rails honored (deterministic, zero paid API, additive, crons untouched). #61 MERGED.
- 23:54 (DREAM SHIFT cont. — harden the wedge's credibility + map the next big builds) — With the Proof Engine firing + every eligible change surfaced, attacked the wedge's STATISTICAL CREDIBILITY + de-risked the next two big builds (all rails-safe, no rushed risky prod ops at the deadline). (#63) rigorous CODE-GROUNDED plan to wire the cross-tenant PEER forecast (the dream's stage-3 "forecast from real peer outcomes for ANY business") — its pieces are built but unwired (zero runtime callers); verified the silent-failure crux: the brain's matchKey vocab (closed-9 SimulationActionType) vs the proof outcome's primary_bucket (SIGNAL_MAP-derived) are DISTINCT, so the daylight build needs an explicit tested primaryBucket→actionType map + shared composeMatchKey or it surfaces zero patterns undetectably. (methodology) researched the engine's diff-in-diff thresholds against the literature (Bertrand-Duflo-Mullainathan, Conley-Taber, Donald-Lang, World-Bank parallel-trends, Mixtape, Roth-SantAnna) → VALIDATED: few-treated/many-controls + parallel-trends pre-window + conservative-confidence-with-few-controls (minControlsForHighConfidence=3 + honest no_controls) all match best practice → the "computed" causal claim is defensible. (#64) BUILT the sourced hardening: placebo-inference.ts — a pure, deterministic leave-one-out placebo p-value (treat each control as pseudo-treated → share of placebo lifts ≥ |observed|), reusing the engine's own control deltas (can't drift), zero RNG/LLM/paid-API, 8 tests; UNWIRED (isolated, zero blast radius) — daylight gates high confidence on (≥3 controls AND placebo_p<0.1). typecheck 0; targeted vitest green. #63/#64 in CI.
- 23:56 (DREAM SHIFT cont. — sourced ACCURACY finding for the next build) — Researched AI citation-update latency per engine (2026): Perplexity reflects content changes in hours–days (real-time retrieval); Google AI Overviews tracks Google indexing (days–weeks); ChatGPT lags **6–12 weeks** (training+retrieval cycle). The Proof Engine uses a UNIFORM postWindowDays=14 across all platforms → it structurally **under-measures ChatGPT (and partly Google) lift**: the post-window closes weeks before those engines reflect a shipped edit, so a real ChatGPT citation gain reads as "no effect". Since the engine ALREADY computes a per_platform diff-in-diff breakdown, the sourced next refinement is **platform-aware windows** (e.g. Perplexity ~14d, Google ~30d, ChatGPT ~60–90d) — config-only, deterministic, per-tenant-safe, zero paid API. Flagged for the daylight build (do NOT widen blindly: longer windows raise contamination risk from other concurrent changes, so pair with the placebo p-value from #64). Sources: Leapd, Pravin Kumar, Discovered Labs, SiteUp.ai, GrowthOS (AI-citation sourcing/latency writeups, 2026). NOT a deadline change — touches the working engine, needs tests.
- 11:55 (DAY RUN → 17:00 — Content Schema Engine) — VERIFY-FIRST corrected two stale beliefs: iranopedia DOES promote (11 cards live: 4 change_h1 / 3 edit_title / 10 edit_meta) and the real gap is SCHEMA — its dominant scan signal had NO path to the queue: missing-schema explicitly skipped content pages ("revisit when Article expectations land"), emitted low→diagnostic_only elsewhere, and composeSchema produced a generic WebPage skeleton telling the OWNER to "extend the @type" themselves. Shipped the Content Schema Engine: (1) vertical-neutral CONTENT_PAGE_EXPECTED_SCHEMA (Article required — Google: Article has NO required properties; BreadcrumbList recommended; FAQPage deliberately excluded post-2023 restriction) + diffSchemaCoverageForSpec extraction; (2) content branch in missing-schema → NEW trigger signal missing_schema_content at MEDIUM confidence with a PRODUCT GUARD (store pages with Product schema never get Article proposals); (3) eligibility missing_schema_content::add_schema = customer-queue-ready (builder-tuned missing_schema stays diagnostic-only) + both pin tests updated deliberately; (4) composeContentArticleSchema — COMPLETE paste-ready Article (+BreadcrumbList from URL path, item omitted on last element per Google) derived omit-when-missing from page facts (h1/title/meta/body/brand-suffix), </ escaped in script blocks, Persian/Finglish text passes through verbatim (no Latin casing). CLOSED LOOP: drafts pinned against the scanner's own validateSchema (zero criticals) in tests. 40 files/729 tests green + typecheck. Sources: Google Article + Breadcrumb + intro-structured-data docs, schema.org/Article, Google 2023 HowTo/FAQ changes. Next: full suite → PR → merge; then fix_schema (invalid-schema trigger) for the 277 schema_invalid findings.
- 12:26 — TWO MORE SLICES MERGED + PROD-VERIFIED, THIRD IN CI. (#65 Content Schema Engine, MERGED + verified: cron re-dispatch moved iranopedia candidates 56→240, promoted 11→24; 10 add_schema cards persisted, ALL with complete Article+BreadcrumbList JSON-LD — sampled draft eyeballed: well-formed, brand-correct author/publisher "Iranopedia". Caught a precision gap from the sample: store CATEGORY pages (e.g. /category/best-sellers) get Article proposals — Product guard only covers pages with their own Product schema; ledgered as next refinement, derivable guard = word-count floor / CollectionPage type.) (#66 fix_schema, MERGED + verified: candidates 240→273, promoted 24→34 — the scanner's own validator output now drives repair cards; schema_validation_warnings restored to the egress projection deliberately, both CI-caught ratchets updated with rationale.) (#67 GSC Search Analytics, IN CI — the FIRST fused connector signal: searchanalytics.query on the existing webmasters.readonly grant, day-sliced nightly sync into new gsc_daily_rows/totals tables [migration APPLIED], 28d per-page aggregates, gsc_low_ctr trigger at sourced thresholds [Semrush positional CTR benchmarks, positions 1–5 only], failure-soft cron step. Classifier ratchet caught a missing isNonHtmlAsset guard — fixed.) Research fleet (4 agents, all landed): GSC spec (11 sources), SEMrush spec (16 sources — domain_organic w/ Ur column = the whole per-page join at 10 units/line; ToS 30-day cache TTL), Clarity verdict = DEFER (72h window/no backfill/10 req-day; hedge: install tag at onboarding), Wix per-page SEO/schema API (in flight — would make schema cards one-click pushable). All rails honored; zero paid API (GSC/Wix/Supabase free quotas).
- 12:52 — THE ACCEPT→LIVE WIRING FOR SCHEMA LANDED (commit 43aa2ff, suite in flight). #67 (GSC fused signal) + #68 (SEMrush striking-distance + priority severities — caught that unknown signals scored 0 and the day's new cards would have sunk to the queue bottom) both MERGED. New slice: Wix SEO push — add_schema cards now APPLY on Accept for Wix Stores products: draft JSON-LD blocks are machine-extracted, the product resolves by slug via the Stores/Products system collection, its current seoData is snapshotted FAIL-CLOSED (one-click revert), tags merge idempotently (same-@type custom scripts replaced), then PATCH stores/v1/products seoData — which renders SERVER-SIDE in the page head per Wix docs. Same rails as field pushes (Accept-only, daily caps, ledger, Ritz dev-note). New missing_schema_store trigger: Product pages without BreadcrumbList get the one duplication-safe, pushable block (Wix auto-generates the Product preset; Article mislabel guard stays absolute). CMS dynamic pages have NO per-item SEO field (platform design, staff-confirmed) → their cards stay honestly paste-ready with the dashboard-template path documented. Research fleet today: 5 agents landed (GSC 11 sources, SEMrush 16, Clarity DEFER verdict, Wix write path 8, fusion-math 12 — banked in docs/research/CONNECTOR_RESEARCH_2026-06-12.md). Iranopedia queue today: 17 → 34 promoted cards across 6 signal families, schema cards with complete paste-ready JSON-LD, store cards one-click pushable once this merges.
- 13:36 — LLM FLIP-ON: built + CLOSED-BY-POLICY (PR #73, not merged). The structured-output upgrade path (cron → loadLiveRecommendationQueue → buildPacketForRec → runProviderAndPersist, limit 3/tenant/night, budget-gated, zero spend until env flips) hit THREE operator-locked CI walls that deliberately forbid auto-LLM-in-cron: promotion-writer is the SINGLE allowlisted importer of recommended-edits-persistence; runProviderAndPersist is globally forbidden under recommendation-intelligence; the scheduled-generation script must never import an LLM provider/gateway. These are policy locks from the LLM-gating decision (§10: operator-only, env-gated-OFF), not incidental pins — overriding them autonomously would violate the operator-lock discipline that exists for exactly this class of change. DECISION FOR OPERATOR: to honor "re-upping flips LLM on instantly," bless a BEACON_LLM_NIGHTLY=true opt-in + the three invariant updates (the branch claude/llm-flip-on holds the working, tested implementation — 3 gate pins green, deterministic no-op proven). Until then, LLM drafting stays operator-initiated via the existing CLI/diagnostic paths. Meanwhile #72 (GA4 value weight) MERGED — 8 feature PRs + 2 docs PRs today, all on main, suite green.
- 13:45 — DAY-RUN CLOSE-OUT: #72 (GA4 value weight) + #74 (docs) + #75 (one-tap Accept on v2 cards) MERGED — 10 feature PRs + 3 docs PRs today, zero open, suite green at every gate. The v2 cards now carry inline Accept (same accept action + fan-out; Review stays one click away; card pinned presentation-only without the handler). Final verification cron dispatched (severity/EV-ranked queue). The engine's day: schema (add+fix, accept-ready JSON-LD), fused first+third-party signals (GSC Rules A+B, SEMrush striking distance), Accept→live push for store products, fusion scoring (EV bonus + GA4 weight, blocker-invariant preserved), one-tap UX, and the LLM-nightly policy decision teed up for the operator with a working implementation parked on its branch.
- 16:03 (MIDNIGHT SHIFT — audit verdict + the connector wall is DOWN) — Ran the operator's 35-check spec as an 11-agent adversarially-verified Workflow: **28/35 PASS including ALL TEN P0s** (review-only safety, Accept-gated push, snapshots/rollback, caps, tenant isolation, honest statuses); 7 confirmed findings ledgered in docs/research/AUDIT_35_CHECKS_2026-06-12.md, 0 refuted-but-shipped. Fixes shipped since: (#80 MERGED) self-serve connect cards — SEMrush key+database, Profound key, Clarity token are now paste-and-go on /settings/connectors (was: only GSC/GA4/Wix had UI; keys would have needed an engineer) + the full Clarity nightly harvester (1 req/night of the 10/day cap, accumulating per-URL rage/dead-click history Clarity itself discards after 72h; migration applied). (#81 MERGED, audit #26 — a real measurement bug) proof-engine treatment dates anchored to the linked edit's live_at instead of changelog accept-time: accept→live gaps were counting pre-live days as treated, structurally DILUTING measured lift; conservative alignment (trusted-live states only, exact element match, pass-through otherwise), pure + 8 tests. (#82 IN CI) Profound connector built from the OFFICIAL docs digested by a research agent (never guessed): X-API-Key, category discovery, citations(date/model/root_domain/url) + visibility/SoV(date/model/asset_name) reports, positional-envelope decoder that drops arity-broken rows rather than misalign, 3-day EST re-pull window, ~2+2·cat of the documented 600/hr budget; prod migration applied; cron step dormant until the operator pastes a key. END-STATE posture: all six connect→sync paths (GSC, GA4, SEMrush, Profound, Clarity, Wix) now exist UI→fetch→tables→cron with zero code changes needed at key-paste time. Remaining audit ledger: #33 Wix routing/caps doc, #35 connector retry/dry-run hardening, #13 originality checks, #16 AEO generators (LLM-gated by the parked policy decision). Suite 14,566 green at every gate.
- 17:00 (MIDNIGHT SHIFT cont. — audit-fix sweep COMPLETE, six PRs merged in 90 minutes) — #81 (treatment-date live_at alignment), #82 (Profound connector, official-docs spec, dormant-until-key), #83 (push hardening: merged-page JSON-LD cap, 1MB ceiling, dry-run mode, GSC 429 backoff), #84 (originality guard: duplicate-topic gap briefs expand the existing page instead — eligibility 26→27), #85 in CI (internal-link brain: contextual topic-cluster link cards from the snapshot link graph, 9 sources, eligibility 27→28). Every actionable 35-check finding is now FIXED on main (#13/#23/#26/#30/#33/#35); #16 (AEO generators) stays LLM-gated by the parked policy decision. END-STATE walk PROD-VERIFIED via cron dispatch 27449042865: all five connector steps fire with honest dormant reasons for both tenants (PROFOUND step live in prod), iranopedia 305 candidates → 34 promoted, Ritz proof persists 166 watching outcomes. One ops incident, recovered cleanly: a stale rebase-merge dir from a PREVIOUS session hijacked a `git rebase --abort` and moved the branch onto an old lineage — recovered via cherry-pick onto true main; lesson: check `git status` for inherited rebase state before rebasing in this worktree. Docs sync (HANDOFF banner + VERIFICATION_LOG entry) in flight as the docs PR. Suite 14,596/0 at the last gate. Next: docs PR → then the next derivable superpower (AEO answer-block readiness audit / source-ledger groundwork) until 00:00.
- 18:00 (MIDNIGHT SHIFT cont. — measurement hardening + the link-graph hunt) — Five more PRs merged (#87-#91, twelve on the day shift's heels): (#87) source-ledger groundwork — uncited_content trigger (contentSiteMode-gated add-sources directive; verified honest-but-under-firing on iranopedia: every long page carries exactly 2 or 5 TEMPLATE external links, so zero-threshold never trips — content-area link extraction at scan time chipped as the refinement, never guessed a threshold). (#88) THE WEDGE-CREDIBILITY WIRING: placebo p-value (#64's primitive) now gates HIGH confidence on every computed lift — chance-level lifts cap at an honest medium with the p named; point estimates untouched. (#89) platform-aware post windows: per-platform breakdowns measure over sourced windows (perplexity 14d HIGH / google_aio 30d / chatgpt 60d — the "6-12 week" folklore HONESTLY REFUTED by the research agent; gemini/claude deliberately uncalibrated, no guessing) with post_window_days reporting what was measured; aggregate/headline untouched. (#90) Profound Agent Analytics v2 — bots + referrals raw-domain reports (which AI crawlers hit which paths + AI-referred visits), migration applied, +2 req/night. (#91) THE HUNT: END-STATE re-walk showed candidates flat at 305 after the internal-link brain merged → root cause: the EGRESS-P0 lean projection omits internal_links, so BOTH cross-page link triggers were silently emission-less on hosted/cron behind their emptiness guards (orphan_page since the egress incident!) — fixed with a scoped getPageSnapshotLinkGraphs() read (egress pin untouched, web surfaces never call it). PROD-PROVEN by re-dispatch 27451554663: iranopedia candidates 305→310, ritz 33→38 — exactly the +5/tenant cap; the internal-link brain FIRES in prod. Hosted smoke: 5/5 core routes 200 incl. /settings/connectors + /diagnostics/proof-engine on post-#90 deploy. Suite 14,609/0 at the last gate; both prod migrations of the hour applied via MCP. Iranopedia config verified (contentSiteMode=true in the cron env var; 862/868 snapshots carry link graphs).
- 00:30 (POST-MIDNIGHT — operator left until 7 AM; 7:30 PM resume armed + new AEO superpower) — Operator is away until 7 AM and asked for relentless continuation; usage limits reset 7:30 PM PT. ARMED the resume: a disk-durable one-time scheduled task `beacon-night-resume-730pm` (7:30 PM PT, carries the full self-contained autonomous goal + rails) + noted the operator's PRE-EXISTING recurring `overnight-finglish-shift` task (every 30 min 7PM-6AM). HONEST CONSTRAINT surfaced: every scheduler fires through the Claude app — the app must stay OPEN (idle/limit-paused fine; quit → fires on next launch); nothing runs server-side. Shipped since: (#93) placebo p-value surfaced on the proof-engine diagnostic (operator sees WHY a lift is high vs capped). (#94, in CI) AEO answer-block readiness trigger `missing_answer_block` — the Iranopedia superpower: question-shaped pages (GSC question query ≥100 impr, or question-shaped title crawl-only) lacking any early answer scaffold (no FAQ/no FAQ-schema/no question-H2) earn an add_answer_block DIRECTIVE card (names the question + sourced answer pattern: ~40-60 words, entity-named, pronoun-free, INLINE not FAQ-schema since Google retired FAQ rich results May 2026; NEVER auto-writes the answer — owner's factual authority). Sourced ≥4 (GEO study KDD 2024, Google snippet convention, AirOps/Frase answer-first, Anthropic contextual-retrieval pronoun-penalty via Lumar); honesty: placement magnitude is directional/vendor not an RCT → severity 17 below the deterministic schema signals, no multiplier claims surfaced. VERIFY-FIRST prod proof: 23/217 iranopedia content pages are question-titled with zero FAQ scaffold → the trigger has real targets (not a no-op). Process catch: CI typecheck flagged a bad FaqItem cast in the test that my local sequence missed (typecheck ran before the test existed; vitest uses esbuild, no type-check) → fixed; lesson banked: run typecheck AFTER writing tests. Eligibility table now 30 entries (8 new trigger→action pairs added tonight). Suite green at every gate.
- 01:10 (POST-MIDNIGHT — the headline finding + the parked unlock) — VERIFY-FIRST on the prod cron (iranopedia candidates 273→332 after the answer-block merge) surfaced THE finding: every operator-review-only trigger I shipped tonight (answer-block, source-ledger, internal-link brain, keyword-gap, cannibalization) FIRES + scores + is visible on /diagnostics/recommendation-triggers, but NONE reach the customer /recommendations queue — they're auto-suppressed by operator-lock U4 (safety-gates.ts:135: "operator-review-only auto-suppressed until α₂'s approve-to-promote affordance" — never built). NOT a bug; the proven layer works today (iranopedia queue = 37 Accept-ready cards, all customer-queue-ready). Handled per the rails (operator-locked + operator away = QUEUE the decision, don't bulldoze): wrote docs/DECISION_approve_to_promote.md (option A build-the-lane [recommended] vs B keep-diagnostic-only, with exact mechanism + the safety argument that A adds a review gate and weakens no rail), surfaced it as the TOP item in HANDOFF (#100), spawned a task chip. THEN built option A fully as a PARKED DRAFT (PR #101, the LLM-flip-on precedent of build-and-park): SafetyGateContext.operatorApprovedDedupeKeys (an approved operator-review candidate falls through to the SAME remaining gates — lifts the tier gate only, never auto-pushes; empty default = byte-identical prod) + per-tenant approved-keys store (TENANT_SCOPED registered) + operator-mode approve/unapprove actions + a "Promote to queue" button on the triggers diagnostic. Inert in prod (nothing approved + not merged). Caught a sharp one: the render-isolation ratchet's naive comment stripper swallowed the LLM-gateway call because a JSDoc */ I added paired with the stray "/*" inside the file's existing "src/app/**" header comment → switched my comments to line comments. Also #93 (placebo surfaced) + #98 (AEO cards measure against the Proof Engine) merged. Full suite 14,632 at the gate. DECISION FOR OPERATOR queued; nothing merged that touches the lock.
- 01:28 (MIDNIGHT-GOAL CLOSE-OUT — past 00:00 PT, final task executed) — The midnight deadline passed; executing the goal's explicit last task: "merge everything onto main and push." DONE BY CONSTRUCTION — every slice this shift was squash-merged to origin/main on CI check==pass (13 feature/docs PRs: #93–#105), main never red, Vercel auto-deployed each (prod now serving 98cb0fc = #105), hosted smoke 200 on /, /recommendations, /settings/connectors. The ONLY unmerged work is draft PR #101 (α₂ approve-to-promote) — DELIBERATELY parked per the operator-lock rail (U4): merging it unsupervised would bulldoze the very invariant the goal forbids touching, so it stays a draft + the DECISION is queued (docs/DECISION_approve_to_promote.md, top of HANDOFF). NIGHT TOTALS (since the 15:28 goal install): connectors complete (all 6 sources UI→sync→tables→cron, dormant-until-key); trigger suite expanded (answer-block AEO readiness, source-ledger, internal-link brain, keyword-gap originality guard) — all fire in prod (iranopedia candidates 273→332, verified); measurement credibility hardened (placebo p-value gate + sourced platform-aware windows, ChatGPT "6-12wk" folklore refuted); 35-check audit ALL actionable items closed (#13/#23/#26/#30/#33/#35), only #16 open (LLM-gated, operator policy); prod link-graph starvation found+fixed+proven; live-card QA pass fixed the meta zero-width garbage (Persian-ZWNJ-safe). OPERATOR'S REMAINING STEPS (everything outside your control is finished): (1) connect keys on /settings/connectors — GSC+GA4 reconnect, SEMrush key, Profound key (Enterprise API), Clarity token, Wix key+site; (2) tap Accept on iranopedia's 37 clean cards; (3) decide U4 — merge draft #101 to surface the AEO/content review layer (recommended) or keep it diagnostic-only; (4) decide the LLM-nightly policy (PR #74 / branch claude/llm-flip-on); (5) top up OpenAI for full multi-engine polling. The 7:30 PM disk-durable resume task is armed for the next autonomous window.
- 02:15 (POST-MIDNIGHT cont. — corrected course: NEVER stop, found + closed the real END-STATE gap) — Operator's Stop-hook caught me wrongly "holding" after midnight — resumed. Audited the END-STATE chain and found the weakest piece: the two NEWEST connectors (Profound, Clarity) synced into tables that NOTHING consumed — "evidence fuses" was violated (keys would land but produce zero recommendations / zero measurement). Closed BOTH: (#107) Clarity fuse — clarity-page-signals loader + clarity_friction trigger (script_errors HIGH ≥5%/session — JS errors are a confirmed defect AND most AI crawlers don't run JS so a render-block hides content from answer engines; rage_clicks MEDIUM ≥7%; ≥50-session floor; dead/quickback/scroll excluded, no sourced threshold) → new fix_page_experience action type (registry 37→38, active set unchanged), eligibility 30→31, directive draft, sourced (Microsoft Learn semantic-metrics + Data Export API + Google JS-render + AI-crawler-no-JS + CRO bands). (#108 in CI) Profound→proof bridge — the wedge ("prove what each edit drove") measures from native polling which is OpenAI-quota-BLOCKED → starved; fused the operator's PAID Profound owned-domain citations into buildUrlCitationHistory as a GAP-FILL third source (benchmark>native>profound precedence, zero double-count, empty=byte-identical), so watch windows measure despite the quota block; honesty gates (≥controls, placebo, parallel-trends) unchanged. Also fixed a pre-existing local-presence test-isolation leak (unused _resetTokenStore never wired) that reddened the LOCAL full-suite (CI was clean). Process: VERIFY-FIRST on iranopedia's LIVE cards earlier found + fixed the meta zero-width garbage (Persian-ZWNJ-safe, #105) and confirmed schema/title drafts clean. Full suite 14,633 at the gate; every change additive/fail-soft/dormant-until-key. Only parked item remains #101 (approve-to-promote, operator-locked U4). Now: prod-verify both bridges are safe (no regression) via cron dispatch.
- 02:38 (POST-MIDNIGHT cont. — cron "failure" RUN DOWN to root cause: GitHub bad-runner batch, my code CLEARED) — A verification cron dispatch came back "completed failure" and "never break the crons" is a hard rail, so I ran it to ground before touching anything. Root cause is ENVIRONMENTAL, not my merges: every failing run (4 nightly-generation dispatches + #108's CI "check" at attempt 3) dies with `steps: 0` — the raw job log (`system.txt`) shows "Job is about to start running on the hosted runner: GitHub Actions 1000001614/15/16" then ENDS, i.e. the runner is assigned and dies BEFORE "Set up job" / before `actions/checkout`, before any of our code runs. PROOF my code is clean: (a) the exact compute-matrix command `npx tsx --require ./scripts/mock-server-only.cjs scripts/list-active-tenants.ts` runs GREEN locally on HEAD 509043e → 2 valid active tenants incl. required tenant-ritz-founder; (b) the workflow YAML + matrix script + mock-server are byte-identical to the 08:37 UTC scheduled run that SUCCEEDED (the only merges in the window — #107 Clarity fuse, #109 docs — touch recommendation-intelligence, which compute-matrix never imports); (c) CI runs at 09:20 + 09:22 UTC SUCCEEDED on the same ubuntu-latest pool. So healthy runners pass and a concurrent bad-runner batch (sequential IDs 1614-1616) kills jobs at init — probabilistic, below GitHub's incident threshold (githubstatus.com = Actions "operational"). Course-correction on my own process: I initially muddied the signal by re-dispatching 3x within 10 min — with `concurrency: cancel-in-progress: true` the rapid re-dispatches cancel each other; a single CLEAN undisturbed dispatch (27463055407) still hit the bad batch, which is what isolated it to runner-init. Disposition per rails (conserve Actions minutes; don't bulldoze): STOP hammering, pivot to CI-free local building, retry sparingly with spacing so the batch rotates; the next scheduled cron (05:30 UTC) recovers on its own and alert-on-failure notifies the operator once runners are healthy. NOT a regression; main is not red in code.
- 03:05 (POST-MIDNIGHT cont. — END-STATE empirically PROVEN in prod + GA4 producer-gap closed + Profound SOV-gap sourced/parked) — Spent the block proving the END-STATE loop with REAL prod data, not just code-reading, then closing the one clean gap it surfaced. (1) EMPIRICAL SPINE PROOF (Supabase, both tenants): scan ALIVE (page_snapshots 1085 iran / 1907 ritz), poll ALIVE (prompt_answer_observations 150 / 21,047), recommend ALIVE (recommended_edits: iran 50 'recommended', ritz 24 'recommended' + 1 verified_live), measure ALIVE (change_outcomes_v2 = 167 ritz rows WRITTEN TONIGHT 08:38 UTC by the cron: 166 watching + 1 no_controls + 0 computed). A scare resolved by verify-first: the LEGACY `change_outcomes` table is frozen at 2026-04-15 (Profound-CSV era) — but the engine writes `change_outcomes_v2` (JSONB), which is fresh; nearly chased a false gap. iran proof=0 is HONEST (0 changelog = nothing shipped). 0-computed for ritz is honest data-maturity (most of 289 changes predate sufficient citation history — exactly what the parked #108 Profound bridge attacks). (2) FAN-OUT AUDIT (subagent, read-only) of every connect→sync→consume path: GSC/SEMrush/Clarity/Wix fully wired; found GA4 = live CONSUMER (page-value ranking weight) but NO nightly PRODUCER (only the operator diagnostics button wrote ga4_url_traffic) → a connected GA4 key did nothing nightly. (3) CLOSED IT: PR #111 — syncGa4UrlTrafficForTenant mirrors the GSC/SEMrush/Clarity/Profound nightly steps (fail-soft, dormant-until-key no_token/no_property, reuses the operator-refresh helpers minus the operator gate, computeRefreshDateRange-bounded, UPSERT-idempotent), one try/catch wired into run-scheduled-generation.ts. Gates: 5 new tests + typecheck + GA4 arch ratchets (no-page-load/scope-split/outcomes-loader) + nightly-generation-contract + script tests + FULL SUITE 14,643/0. (4) Profound's other dead-end table (visibility) → designed the "AI cites competitors not you" SOV-gap trigger with a 5-source research digest (own SOV<10% AND competitor≥30%-or-3× AND executions≥100/cell AND ≥2 periods; executions-gate is the rigorous leg, %s are tunable convention; own-brand is NAME-MATCH ONLY — Profound has no ownership flag), but PARKED build (build-blocked: sync persists only opaque category_id not the NAME needed for topic→page matching, ≥2-period guard needs accumulated data, Profound dormant) — full ready-to-build spec in IDEAS_PARKING_LOT rather than half-build blind. RAIL-HONEST: all three pending PRs (#108 Profound bridge, #110 this docs/park, #111 GA4) are BLOCKED from merge by a sustained GitHub hosted-runner-init outage (every run dies pre-checkout with steps:0; CI at 09:20-09:22 passed then the batch went bad; githubstatus = 'operational' so it's below their incident threshold; NOT our code — proven). Merge-gating rail (CI==pass) holds → they merge when runners recover or the operator merges in the morning. Nothing merged that touches the U4 lock (#101 still parked).
- 03:20 (POST-MIDNIGHT cont. — PROVED the wedge's missing data: cold-store migration unlocks 0→6 COMPUTED for Ritz) — Highest-value END-STATE item in the MEASURE leg is Ritz's 0 computed / 166 watching proof outcomes. Root cause confirmed in code: the proof engine's benchmark regime (buildUrlCitationHistory, dates < NATIVE_REGIME_START 04-22) reads pre-native citation history from the LOCAL cold-store (.data/citations-by-date, 48 shards) which doesn't exist on the ephemeral cron runner → every pre-04-22 change = "watching." VERIFY-FIRST (zero prod write): copied the 48 shards into the worktree .data, ran the REAL buildAndPersistTenantProof('tenant-ritz-founder') with a no-op persist + DATA_SOURCE=supabase (Ritz's real 289 changelog + 21,047 observations from prod). Result: **computed 0→6** (weak→84, watching→57). So shipping the shards to prod unlocks 6 real causal "this edit helped" claims — the $499 wedge firing for the demo instead of 166 "still watching." This UPGRADES the long-standing "daylight cold-store→Supabase migration" note from vague-need to PROVEN-worth-it with an exact 4-step build (benchmark_citations_by_date table + cold-store Supabase read backend + one-time shard loader + cron-read verify), specced in IDEAS_PARKING_LOT. NOT built/executed tonight — it's the attribution/persistence CORE WEDGE (Max-tier), needs the prod data load + cron-flip to validate end-to-end, and can't be CI-gated during the runner outage → correctly queued for daylight (operator-present), now with hard proof + a task chip. Cleaned up the throwaway dry-run script + copied shards (worktree clean).
- 03:35 (POST-MIDNIGHT cont. — HONESTY CORRECTION on the 03:20 cold-store finding) — Verified the 6 "computed" outcomes individually before letting the claim stand (the product's whole pitch is "the only ROI claim that survives scrutiny" — can't have the night-shift docs overclaim). REALITY: all 6 are the SAME change — /locations/menlo-park, treatment 2026-04-07, identical lift 6.214 cit/day (54%), 4 controls — counted 6× because the changelog has 6 DUPLICATE rows (cl-real-200..205) for one edit; and 0/6 are placebo-significant (all placebo_p=0.5 → the engine CORRECTLY caps them at MEDIUM confidence with the honest "untreated pages moved this much by chance" warning — the placebo gate I wired in #88 working as designed). So the cold-store migration unlocks ONE distinct, medium-confidence, placebo-INSIGNIFICANT measured outcome (populates the /changes/[id] drilldown with a real number + honest caveat instead of a blank "watching"), NOT "6 causal wins." It's still worth shipping (the prerequisite for the wedge EVER computing as native polling accumulates) but the immediate demo payoff is small, and it surfaced a real DUP-CHANGELOG data-quality issue (cl-real-200..205 → dedupe to one). Corrected IDEAS_PARKING_LOT + re-spawned the operator chip with honest framing + the dedup task (dismissed the overclaiming one). Lesson reaffirmed: verify-first applies to MY OWN findings, not just the code — a counts-only result (computed=6) hid that it was 1 change × 6 dup rows, placebo-insignificant.
- 12:5x PDT (Iranopedia→Wix happy path — first live push staged, BLOCKED on operator) — Operator connected the scoped Wix Admin key (Wix Data only, Iranopedia site) to tenant-iranopedia (connector_tokens, prod). Drove the whole content-push path locally-against-prod (the token + recs live in Supabase; the wix-url-map/collection-config stores are FILE-ONLY with no Supabase dual-write → empty on hosted cold lambdas, so the HOSTED Approve→Push can't resolve fields durably yet — DECISION queued, not built, per "no broad architecture"). VERIFY-FIRST findings (all proven against the live site + CMS, no guessing): (1) The edit_title/change_h1 "missing" cards are CRAWLER FALSE-NEGATIVES — PersianRugs.title="Mashhad Rug", live <title>="Mashhad Rug: Motifs, History, and Patterns", live meta present; raw HTML lacks <h1> only because Wix renders headings client-side. Beacon's scanner reads raw HTML (no JS) so it mis-flags present title/meta as missing. Operator instruction honored: do NOT push these. (2) Wix dynamic-page title/meta are NOT writable via the Data API — they come from the SEO-panel variables/templates. The real path (operator-confirmed + sourced): add a dedicated CMS field + one-time SEO-Variable binding in the Wix dashboard, THEN Beacon writes the field. (3) ONE genuine gap found + verified: koobideh-kabob serves NO <meta name="description"> at all (empty), despite a complete recipe page. STAGED (dry-run green, full safe path: status guard→not-Ritz→publish_target=wix_cms→daily cap→non-destructive→derive field:seoDescription→snapshot): added a dedicated `seoDescription` TEXT field to PersianKabobs via the Wix Data API (41→42 fields, exactly +1, verified — reversible), mapped /persian-kabobs/{slug}, dry-ran executePush for the koobideh edit_meta card → "would update seoDescription" (OLD="" NEW=154-char meta derived from the page's OWN content, no fabrication/LLM). BLOCKED on operator: (a) bind the Kabobs dynamic-page Meta Description → {seoDescription} in the Wix dashboard (~30s, only they can — SEO panel isn't API-writable), (b) tap Accept. On "go": `_wix-koobideh-meta.ts --execute` writes the field live + re-fetches the page to confirm the meta, `--rollback` restores. Shipped earlier this session on main: edit_meta→description field role (8e8bc9f, 14,681 tests green) + the one-founder/registry/de-brand blockers (6c5afad). Extend-to-titles + SEMrush-keyword validation of proposed values = future (operator-noted). Sources: Wix "Working with SEO Settings for Dynamic Pages", "Using Variables in SEO Settings", "Update Data Collection" REST docs.
- 12:5x PDT (BLOCKED: koobideh meta live-write DONE + verified in CMS; live <meta> awaits operator SITE publish — operator-only, logged, pivoting) — Also found: appendPushSnapshot/recordLedger scope tenant via ambient currentTenantId() not the explicit executePush tenantId, so a script-driven (no request ctx) push filed iranopedia's snapshot under the founder tenant's local file (hosted request-ctx is correct; latent for cron/script callers). QUEUED fix, not bulldozed. Next: root-cause the false-positive title/H1/meta cards (operator: do not push these).
- 13:0x PDT (SEV: my koobideh push BLANKED slug+link → /persian-kabobs/koobideh-kabob now 404) — ROOT CAUSE: wixUpdateDataItem stripForbiddenFields()+PUT(full replace) → omitted slug/link CLEARED, not protected. Verified via API: koobideh slug=undefined link=undefined (Joojeh/Barg intact), no item owns slug koobideh-kabob, live URL=404. seoDescription intact (operator: do not rollback it). Restore staged (raw PUT incl. slug, preserves all current fields) — PAUSED for operator Accept (slug write, SEO-sensitive). Then FIX wixUpdateDataItem (stop stripping on PUT; preserve URL fields unchanged) + missing_dynamic_url_mapping diagnostic.
- 13:36 PDT (DEMO SHIPPED — Iranopedia first live Wix push, end-to-end, + safety fix + UI truth-up) — FULL LOOP PROVEN LIVE: Beacon wrote koobideh-kabob's meta description to Wix CMS (seoDescription field, dedicated/decoupled) via the safe push path (cap→snapshot→single-field write); operator bound the SEO Variable + published; live page serves the Beacon-written <meta name=description>, URL 200. SEV during the push (full-item PUT stripped slug/link → 404) was caught, root-caused, and the URL restored via urlRepair; then the bug was FIXED: wixUpdateDataItem now fetches-current + changes only the approved field + preserves slug/link (never strips on PUT), refuses URL/slug targets unless urlRepair; stripForbiddenFields removed; koobideh-preservation regression test added (commit 44c3f62). Also: empty-shell trigger guard kills false-positive missing-title/h1/meta cards from JS-shell scrapes (08e0a58); UI truth-up — demo banner suppressed for Wix/GSC-connected tenants, sidebar 'Review-gated SEO & AEO operator', connectors lead with Wix+GSC not Yelp (e0f2dec). All on origin/main (e0f2dec), Vercel deploying. GSC connected (token present) but property not selected → sync no_property_derivable; honest: enriches future cards once property picked (2-3d lag). Did NOT fake Semrush/Profound. Prod verified: koobideh URL 200, meta present, CMS slug/link/seoDescription intact. Evidence Receipt rendered for the operator. Gate: typecheck clean; full vitest green (2 prior commits 14,685; UI commit confirming).
- 13:43 PDT (GSC sync rerun — 75,992 real rows synced for Iranopedia) — Operator saved domain in Settings->Config + asked to rerun GSC SA sync. Ran syncGscSearchAnalyticsForTenant(tenant-iranopedia). First attempt no_property_derivable (local business-config lacks the hosted-saved domain). sites.list showed the account has ONE property: https://www.iranopedia.com/ (URL-prefix, siteOwner) — NOT a domain property. resolveProperty derives 'sc-domain:'+domain → 403. Reran with BEACON_GSC_SITE_URL=https://www.iranopedia.com/ → synced 45 days / 75,992 rows. VERIFIED in prod gsc_daily_rows: 75,992 rows, 187 pages, 11,331 queries, 2026-04-27..2026-06-10. NUANCE/FOLLOW-UP (queued): resolveProperty's sc-domain guess breaks the HOSTED nightly sync for URL-prefix properties → it'll 403 nightly until the property is stored per-tenant (connect-time sites.list selection) or auto-discovered. main green at 0dcb9a3 (arch ratchet @no-classifier-required tag fix shipped after the empty-shell guard).
- 13:55 PDT (GSC nightly sync now self-resolves end-to-end — proven) — Closed the 2nd gap behind 'GSC syncs nightly': getBusinessConfig reads only env/.data files (no Supabase), so the operator's Settings->Config domain never reaches the hosted nightly sync (read-only lambda FS) → resolveProperty got no domain → no_property_derivable forever. Fix: resolveProperty falls back to the tenant REGISTRY domain (getTenant, Supabase-backed via the store fix) when business-config has none, THEN auto-discovers the property shape via sites.list (commit prior 85624ae). PROVEN end-to-end LOCALLY with NO env override: sync returned synced:true property=https://www.iranopedia.com/ days=5 rows=7661 (re-pull window; idempotent on top of the 75,992 backfill). typecheck clean; gsc suite 93 green. NOTE (pre-existing, broader): business-config is env/file-only on hosted, so non-GSC consumers (segment/prompts/etc.) still get the neutral placeholder there unless BEACON_BUSINESS_CONFIG_JSON* is set — the registry is now the durable domain source for GSC; broader business-config persistence is a separate follow-up.
- 13:58 PDT (PRODUCT PIVOT goal set: review-gated SEO/AEO OS, GSC-first cards not AEO-first) — Starting with the mandated verify-first step: map why recommendation cards lead with '0 AI answers' instead of consuming the 75,992 GSC rows now in gsc_daily_rows. Then reframe card projection: safety→GSC demand→CMS gap→AEO(secondary)→outcomes; source-specific confidence (GSC-backed/CMS-backed/Wix-push-ready/AEO-only); page reframes (Today's site moves, Prompts=optional AEO, Changes=website changes+impact, Connectors=evidence layers).
- 14:13 PDT (PIVOT slice 1 — cards now LEAD with real GSC demand) — Root-caused why cards showed '0 AI answers' despite 213K synced GSC impressions: (a) loadGscPageSignalsForTenant was never called by loadLiveRecommendationQueue (GSC only fed triggers, not the card), (b) the loader's single .limit() hit PostgREST's ~1k-row response cap with NO order → truncated to 11 arbitrary pages (also silently starved the GSC triggers). Fixes: paginate the loader (.range loops, 90-day window per operator) → 187 pages / real impressions (persian-male-names 21,506 impr·6.4- 14:23 PDT (GSC backfill + reconciliation vs operator's GSC UI) — Operator cross-checked Beacon vs their GSC (3mo: persian-male-names 6,340 clicks/136,940 impr; 28d: 1,061/23,866). Confirmed REAL data (top pages persian-male-names/persian-female-first-names/funny-farsi-phrases match exactly). Added an operator backfill override (startDate) to syncGscSearchAnalyticsForTenant (the normal path is incremental-FORWARD via watermark+day-cap, can't reach back). Backfilled Mar12->May11 (61 days/167,092 rows) before the GSC access token expired mid-run (401 at day 61 — ~1h token life; KNOWN LIMITATION: long backfills need mid-run token refresh); combined with the prior Apr27-Jun10 sync the DB now spans Mar12-Jun10. RECONCILIATION (honest): Beacon persian-male-names now 4,439 clicks/69,589 impr vs GSC page total 6,340/136,940 — impressions land ~51%, clicks ~70% because Beacon syncs the page+QUERY grain and GSC HIDES anonymized low-volume queries from that breakdown, so the page+query CTR runs INFLATED (6.4% vs real ~4.6%). FIX NEXT (queued): sync per-page totals via dimension=[page] (includes anonymized) so card impressions/clicks/CTR/position match the GSC UI exactly; keep page+query for topQueries.
- 14:31 PDT (GSC per-page TOTALS — fix the inflated CTR) — Added gsc_daily_page_totals table (migration applied to prod) + the sync now pulls dimensions=[page] per day (mirrors the existing property-totals block) → true page-level clicks/impr/CTR/position that INCLUDE GSC's anonymized queries. Loader (loadGscPageSignalsForTenant) reads the page-totals + OVERRIDES the page+query totals (keeps topQueries), soft-falling-back to page+query when the table is empty (pre-backfill) → zero regression. typecheck clean; 872 gsc+trigger+rec tests green. COULD NOT backfill locally: the GSC access token is ~1h stale and there's no GOOGLE_CLIENT_ID locally to refresh (hosted-only) → page-totals populate on the next HOSTED nightly sync (which refreshes the token), at which point card CTR/impressions auto-upgrade to match the GSC UI. Until then cards show page+query numbers (real demand, ~50% impr, CTR runs high — known + self-correcting). KNOWN follow-up: long backfills need mid-run token refresh.
