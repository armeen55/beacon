# BEACON 500 - THE MASTER PLAN

> The single ranked backlog that takes Beacon from a strong product to the best SEO/AEO team money
> can buy, and takes Iranopedia (and any future site) as far as a site can go. Built 2026-07-01 from a
> 26-agent discovery pass over the real codebase plus the 37 unfinished items from the Final Premium Plan.
> This file SUPERSEDES docs/FINAL_PREMIUM_PLAN.md for sequencing; the old ledger stays as history.

## How to execute this list
- Work strictly top to bottom. The order IS the priority: highest impact first, and within the same
  impact, medium effort first, then small, then large (operator directive).
- Same loop as the Final Premium Plan: group 2-5 items into a slice, implement, gate (typecheck +
  targeted tests), ground-truth on real Iranopedia data, tick the box, commit naming the item numbers,
  push main, deploy smoke. Full suite + build every 10-15 items.
- Hard rules still bind: no em or en dashes anywhere; plain business language on operator surfaces;
  never mutate measurement history; paid APIs only through the existing capped gauntlets; additive
  Supabase migrations only; Ritz stays publish-blocked.
- Items tagged [CARRY-OVER n] are the unfinished Final Premium Plan items; finishing one here also
  ticks item n in docs/FINAL_PREMIUM_PLAN_PROGRESS.md.

**610 items.** By impact: 9: 4, 8: 66, 7: 147, 6: 189, 5: 138, 4: 52, 3: 13, 2: 1.

Areas: frontier (55), Carry-over (37), Code health (34), Design (33), CMS push depth (32), Onboarding and cold start (32), Worklist UX (31), Autonomy (30), Measurement science (30), AI visibility (AEO) (29), LLM engine (29), Today dashboard (29), Search Console depth (28), Team intelligence (26), Completeness critic (25), Trust and receipts (25), DataForSEO depth (24), Performance (23), Learning loop (21), Content engine (20), Technical SEO (17).


## Impact 9

- [x]   1. **Add a trust-budget autopilot that ships proven lever classes without waiting for approval** (frontier, medium)
       Armed publishing, the atomic accept RPC, and per-lever history in src/domains/attribution/shared-brain.ts already exist. Let the operator arm a budget: lever classes with a proven record (for example 80 percent non-regression across 10+ verdicts) auto-accept and ship nightly, capped at N changes per week, every change reversible and receipted. This is the moment Beacon stops being a recommender and becomes a team that works while you sleep.
- [x]   2. **Enable Wix CMS body-field publishing so section drafts ship in one click** (Content engine, large)
       push-service.ts and caps.ts cover title, meta, and schema fields plus paste; the body itself is never pushable. Map the body rich-text field per CMS collection in src/lib/connectors/wix/mappings-store.ts and url-map.ts and enable full-section pushes with pre-push snapshots and rollback via push-snapshots.ts. This converts every answer-block, FAQ, and missing-section draft from a paste chore into an applied, measurable change, the single biggest step toward the app doing the work end to end.
- [x]   3. **Wire real revenue per page: ad RPM, affiliate, and lead dollar values, not CPC proxies** (Completeness critic, large)
       DONE 2026-07-01: revenue_facts table applied + verified on prod; operator unit-economics card on /settings/config; nightly pass in cron-sync (rate x real GA4 traffic, basis "your rate x real traffic", never presented as measured); honest money line on the Today scoreboard; ad-network slot (AdSense/Mediavine/Raptive) ships dark with fail-closed stubs, credential wiring operator-gated. Verified live on Iranopedia (3,260 rows, then cleaned; awaits the operator's real rate).
       Every planned dollar surface (GA4 revenue series, ad-equivalent CPC value, GA4 key events) is a proxy because Iranopedia has no monetization events wired; the money headline is currently fiction. Connect the actual money source: the ad network reporting API (AdSense/Mediavine) for per-page RPM on content tenants, operator-set dollar-per-lead using the existing callrail connector in src/lib/connectors/callrail for service tenants, and persist a revenue_facts table keyed by page and day that the scoreboard and diff-in-diff verdicts read. A stranger pays $250/mo for proven dollars, and no item in the 548 actually connects a dollar pipe.
- [x]   4. **Run the tenant prompt library across 4 AI engines nightly and diff per-engine citation gaps** (frontier, large)
       Execute src/domains/prompts/prompt-library.ts prompts against OpenAI, Claude, Gemini, and Perplexity nightly, parse citations with the existing extraction.ts, and write to the same prompt-answer-observations tables; use the engine-swap support already in src/domains/serp/dataforseo-llm-mentions.ts where it is cheaper. Diff engines per question so 'Perplexity cites you, ChatGPT does not' becomes a specific fixable gap fed into src/domains/experiments/build-daily-candidates.ts. First-party AI visibility at roughly $100/mo replaces the borrowed Profound account that does not even track Iranopedia.

## Impact 8

- [ ]   5. **Add a citation-outcome verdict lane to the nightly measurement loop** (AI visibility (AEO), medium)
       The 7/14/28-day diff-in-diff judges moves on GSC metrics only. Join each shipped move's page to profound_citation_rows and native-poll citations pre and post ship, with comparison pages as controls, so an answer-block experiment gets a second verdict: won or lost the AI citation. Reuse compute-time-to-citation.ts and write into the same proof bands so Wins can read AI now cites this page on 3 prompts, 11 days after the change.
- [ ]   6. **Attribute AI-referral sessions and dollars to cited pages in GA4** (AI visibility (AEO), medium)
       src/lib/connectors/ga4/sync-url-traffic.ts pulls page traffic but never splits sessionSource (verified: no usage), so chatgpt.com, perplexity.ai, gemini.google.com, and copilot referrals are invisible. Add a sessionSource-dimensioned pull, join AI-referred sessions to pages winning citations, and write the dollar line into the proof ledger: this citation win brought 214 visitors from ChatGPT. This is the number that sells the AEO wedge to a $250/mo stranger.
- [ ]   7. **Build the crawl-to-citation-to-revenue funnel per page from existing Profound and GA4 rows** (AI visibility (AEO), medium)
       The pieces exist as islands: profound_bot_rows (bot-coverage.ts), profound_citation_rows, profound_referral_rows, and GA4 sessions. Join them into one per-page funnel: shipped, crawled by GPTBot/ClaudeBot, cited, clicked from an AI surface, session value. Every stalled move then names its exact bottleneck (never crawled vs crawled-not-cited vs cited-no-clicks), which routes to completely different fixes; pages no AI bot ever fetched get internal-link and llms.txt priority moves with the coverage number as evidence.
- [ ]   8. **Poll real tracked prompts and fanouts on Gemini, Claude, and Copilot via llm_responses** (AI visibility (AEO), medium)
       poll-health.ts shows native polling only runs Perplexity and OpenAI, and questionForTopic() in dataforseo-llm-mentions.ts hardcodes one canned best websites template on gpt-4o-mini. Feed the tenant's actual Profound prompts (pullProfoundPrompts) and ranked fanout sub-queries through DataForSEO's per-engine llm_responses endpoints ($0.03/call, path already overridable), writing results into observation_runs/prompt_answer_observations as new sources so SoV, prompt-primary-share, and the Today visibility read model cover all major engines. Roughly 100 prompts x 3 engines x $0.03 fits the existing cap discipline, and per-engine divergence (cited by ChatGPT, invisible on Gemini) is itself a move signal.
- [ ]   9. **Ship the topical coverage map: question coverage percent per hub joined to citation share** (AI visibility (AEO), medium)
       Cluster the demand universe (demand nodes, keyword-universe cache, fanout seeds) into hubs, compute owned coverage percent per hub (Persian Food 34 percent, Nowruz 71 percent) with the ranked queue of unanswered questions from fanouts, PAA, and tracked prompts (prompt-opportunity.ts already knows which the domain answers), and join answer-intelligence citation rates per topic. One line per hub: cited in X percent of prompts, own Y percent of the needed pages, these 3 missing pages lift citation share most, feeding the nightly candidate builder. This is the map that shows where the war is actually fought.
- [ ]  10. **Assert pipeline volume invariants after every nightly sync** (Autonomy, medium)
       The known failure class is silent-empty: /today once rendered blank for weeks off a statement timeout. After sync, assert stage invariants: GSC rows written > 0 for a connected tenant, buildDailyCandidates yields > 0 when demand rows exist, demand-graph moves > 0 when the graph has nodes. A violated invariant files an Ops card naming the exact broken stage instead of letting downstream surfaces quietly show nothing.
- [ ]  11. **Propose rollback on negative verdicts and auto-revert where pre-approved** (Autonomy, medium)
       buildRevertEdit in push-snapshots.ts exists but is only reachable from a manual /diagnostics/wix click, and a losing change sits live until a human acts. Wire 7/14-day negative verdicts from the auto-measure loop to surface a one-click restore action on the proof row and Today, executing through executePush so caps, snapshot, and ledger apply; when the change settles strongly negative at 14 days with clean comparison pages and the lever is under an auto-accept policy, apply the revert automatically and record it as its own shipped change with a lesson line. Turns the safety story from we keep an undo into the team catches its own misses.
- [ ]  12. **LLM adjudicator on the nightly batch** (Carry-over, medium) [CARRY-OVER 85]
       (the reasoning-gap fix, standing): after the deterministic picks, one budget-gated LLM pass sanity-checks each pick against its evidence ("does this answer match the search intent?") and can flag, never silently drop.
- [ ]  13. **Nightly precompute at ~5am local** (Carry-over, medium) [CARRY-OVER 84]
       the daily plan preview, LLM drafts, debates, and war-room reads all computed before the operator wakes. Morning open = instant + full. (The on-demand posture stays for refresh; this is a warm cache, not a behavior change.)
- [ ]  14. **Trend radar goes live on real deltas** (Carry-over, medium) [CARRY-OVER 89]
       WoW query-spike detection from gsc_daily_rows feeding the Demand band ("chaharshanbe searches will spike in March; prep in February").
- [ ]  15. **Wix draft-writing where possible** (Carry-over, medium) [CARRY-OVER 90]
       for CMS-pushable fields, "Apply in Wix" becomes "Stage in Wix" (existing armed-publish rails, operator approves; Ritz stays blocked). Cuts the paste friction that makes nightly batches feel manual.
- [ ]  16. **Build the competitor keyword gap engine on Labs ranked_keywords plus domain_intersection** (DataForSEO depth, medium)
       Beacon knows who wins (winningDomains in research-enrichment.ts, teardown targets in serp-teardown-fusion.ts) but not what else they win. One ranked_keywords call per competitor returns their full ranking portfolio with volume and position; monthly domain_intersection against the top 3 winners returns keywords where they rank and iranopedia.com does not. Pipe the gaps into demand-graph create_page candidates through the existing serp-validation gauntlet with named evidence (competitor X ranks 3 for this, 1900 searches). This replaces guessing demand from AI-attention proxies with a literal list of demand the market already proved.
- [ ]  17. **Keep an append-only SERP history and parse Iranopedia's own rank from every snapshot** (DataForSEO depth, medium)
       runSerpQuery deletes the old cache row before writing the new one, destroying the time dimension Beacon already paid for, and parseDataForSeoSerp never asks whether an organic item is the tenant's own domain. Add a dataforseo_serp_history table written on every ok call at $0 marginal cost, extract ownRank per snapshot, and surface a literal observed Google position next to the blended GSC average on before/after charts. This one change unlocks rank movement charts, feature-shift alerts, and honest before/after Google evidence across the proof ledger.
- [ ]  18. **Put winnability arithmetic in every build verdict: difficulty, domain ranks, and backlink gap** (DataForSEO depth, medium)
       serp-validation.ts judges winnability from SERP shape alone and daily-evidence-brief.ts apologizes that competition is not difficulty. Wire three cheap Labs/Backlinks reads into prepare-create-page-verdicts.ts: bulk_keyword_difficulty (1000 keywords per call), bulk_ranks domain-authority scores over every winningDomains list (a rank-80 SERP is honestly a reject even when the shape looks friendly), and a page-level backlinks summary for the top 3 winning URLs vs the tenant's page (their pages average 210 linking domains, yours has 3). Converts wait verdicts from vibes to arithmetic and directly attacks the worst failure mode: confidently recommending unwinnable pages.
- [ ]  19. **Re-check the live SERP at day 7, 14, and 28 for every ship and write rank deltas into verdicts** (DataForSEO depth, medium)
       The measurement loop judges wins purely from GSC. Add a rank re-check step to the auto-measure cron that reruns runSerpQuery for each shipped pick's target query at each window (cache-busted, about $0.003 each, a few cents per cohort) and stores was-rank vs now-rank in the proof ledger. A Win line that says Google moved us 9 to 4 is the strongest trust artifact the product can produce.
- [ ]  20. **Track Google AI Overview and AI Mode citations per money query on the scoreboard** (DataForSEO depth, medium)
       parseDataForSeoSerp reduces ai_overview to a boolean and poll-health notes Google AI surfaces are never polled natively. Parse the advanced endpoint's ai_overview item (plus a serp/google/ai_mode producer) to persist the overview text and which domains it cites per tracked query, flag when Iranopedia is or stops being one of them, and wire the count into the Today AI-citations band next to Profound. Diff owned-vs-competitor AI Overview citations against organic rank so you rank 3 but the overview cites someone else becomes a distinct move class. Google-surface AEO truth Profound does not see, from calls Beacon already makes.
- [ ]  21. **Archive GSC permanently and pre-position content 6 weeks ahead of seasonal spikes** (frontier, medium)
       GSC deletes data past 16 months; Beacon already syncs gsc_daily_page_totals daily, so make retention permanent and fit last year's seasonal curve per page and query cluster. Six weeks before each predicted peak (Nowruz, Yalda, Chaharshanbe Suri) auto-queue refresh Moves with the target date attached and a YoY overlay as evidence ('last year this cluster did 40k impressions'). The chaharshanbe incident, where demand was a date and Beacon shipped a definition, already proved timing intent is a real failure mode worth owning.
- [ ]  22. **Attribute dollars to individual shipped changes with GA4 key events** (frontier, medium)
       src/domains/recommendation-intelligence/ga4-page-values.ts already values pages and src/domains/attribution/proof-engine.ts already isolates per-change lift vs comparison pages. Multiply them: pull GA4 key-event conversions for each changed URL inside the measurement window and print 'this title change is worth about $340 a month' on the Wins band. A dollar figure on the verdict is the strongest possible answer to the $250 question.
- [ ]  23. **Build the beat-Wikipedia finder for thin or stale Wikipedia coverage** (frontier, medium)
       Cross llm-mentions results where AI cited Wikipedia instead of Iranopedia with the free Wikipedia API (article length, last revision, section count) to score beatability per topic. Hand winners to the page factory as high-conviction create or expand Moves with evidence lines like 'Wikipedia's article is 180 words and last touched in 2019'. For an encyclopedia tenant this is the sharpest AEO wedge available and no commercial tool does it.
- [ ]  24. **Build the Farsi and Finglish language gap matrix with transliteration variant capture** (frontier, medium)
       Use the shipped Persian tokenizer plus search-query-parser.ts in src/domains/prompt-answer-observations to classify every GSC query by script and language (Farsi, Finglish transliteration, English), then matrix demand against the language of the page that ranks. Cluster transliteration variants like the many spellings of 'chaharshanbe soori', measure which spellings each page fails to capture, and emit Moves like 'this page gets 900 Farsi-script impressions but has zero Farsi content' plus answer-section variants covering the top spellings. No mainstream tool handles Finglish at all; this is parity plus a moat for a Persian-culture tenant.
- [ ]  25. **Capture SERP feature owners and queue snippet, AI Overview, and PAA steal moves** (frontier, medium)
       parseDataForSeoSerp in src/domains/serp/dataforseo-serp.ts flags ai_overview, featured_snippet, and people_also_ask as booleans and throws away who owns them and what they say. Extend the parser to capture snippet owner domain, AI Overview cited domains, and current winner format from the same response bodies at zero added spend, then detect every query where Iranopedia ranks 2 to 8 below stealable real estate and queue a format-matched answer block via the existing safe-answer-block.ts lever. 'The snippet on your #4 query belongs to a beatable blog' is a concrete Move current picks cannot see, and snippet capture is the cheapest rank-1 equivalent left.
- [ ]  26. **Learn what AI actually quotes and build a citability rewriter lever** (frontier, medium)
       pullProfoundAnswers and analyzeProfoundAnswers exist and 22.8k citation rows sit in Supabase. Mine the actual answer texts for the phrasing patterns AI engines lift (stat-first sentences, named definitions, attributed claims), distill a deterministic rubric, and add a citability rewrite lever to the daily experiment cycle. This optimizes against observed AI behavior instead of folklore, and the measurement loop to prove it already runs.
- [ ]  27. **Ship a promise ledger: forecast vs delivered, reconciled every month** (frontier, medium)
       Every accepted pick already stores an outcome range via pick-expectations.ts in src/domains/experiments. Aggregate portfolio-level promised vs delivered clicks monthly, show calibration ('we promised 400 to 900 extra clicks, delivered 610, our forecasts run 8 percent hot'), and feed the bias correction back into the forecast math. A tool that publicly reconciles its own predictions is more trustworthy than any competitor dashboard and directly justifies renewal.
- [ ]  28. **Persist numeric forecast ranges and score every pick against its 28-day actual** (Learning loop, medium)
       buildPickExpectations in src/domains/experiments/pick-expectations.ts renders the forecast as a prose string on the plan record, so predicted-versus-actual can never be computed. Persist low, high, and metric numerically on DailyPlanPick in daily-plan-types.ts, then at the 28-day measure compare realized monthly click lift against the range and write a calibration record (inside, above, below). This is the foundation for every calibration feature and costs almost nothing now versus a painful backfill later.
- [ ]  29. **Propagate wins across the page family: auto-queue the proven lever on sibling pages** (Learning loop, medium)
       When an answer block wins on iran-animals/persian-cheetah, nothing systematically queues the same lever for the other iran-animals pages. In build-daily-candidates.ts, read won ledger records, find same-pageFamily pages (pageFamilyOf already exists) that are eligible and lack the lever, and emit boosted candidates tagged reusing a proven win on this family. This turns every single win into 5 to 20 near-certain follow-ups, which is how a real SEO team compounds results.
- [ ]  30. **Feed winning before/after pairs and structures back into every drafter prompt** (LLM engine, medium)
       proof-history-voice.ts aggregateSettled already computes settled win/loss per pageFamily and actionFamily but only as a debate line. Retain the winning before-to-after text pairs, inject the top 2 for the same lever into ATOMIC_EDIT_SYSTEM and ANSWER_BLOCK_SYSTEM in structured-drafter.ts as few-shot examples, and deterministically extract structural features from winners (word count, leading direct answer, presence of a number, question form) into a small per-family template store next to move-draft-store.ts that conditions future drafts. The drafter learns the house patterns that measurably lifted CTR instead of rediscovering style nightly, compounding the measurement loop into better future drafts.
- [ ]  31. **A/A calibration harness that measures and auto-tunes Beacon's own false positive rate** (Measurement science, medium)
       Nightly, run the full measureRecord pipeline on synthetic experiments: untreated pages with random pseudo ship dates and the same control selection, so the fraction reading won or lost is the pipeline's empirical false positive rate. Persist it, then replace the one-size MIN_LIFT_CLICKS=3 and MIN_LIFT_CTR=0.003 floors in measure.ts with floors derived from the placebo pseudo-lift distribution at each page's traffic tier, tuned to a 5 percent target. Show the measured false positive rate on the proof explainer. Thresholds then scale correctly for every site Beacon runs, which multi-tenant credibility requires.
- [ ]  32. **Algorithm-weather guard: detect sitewide shocks and quarantine affected verdicts** (Measurement science, medium)
       A Google core update moves the whole site and every window overlapping it is suspect, yet nothing detects this. Run a CUSUM changepoint detector on sitewide daily clicks and impressions (loadDailyTotalsForTenant in gsc-page-queries.ts already reads the series), keep a small seedable table of confirmed Google update rollout ranges, and compute sitewide control-cohort drift per window in measurement-maturity.ts. Overlapping verdicts get a visible caveat line, stand for display, but are excluded from prior training the way maturityGatedVerdict already neutralizes immature reads. This stops one algorithm update from polluting a month of lessons.
- [ ]  33. **Match controls on baseline level and pre-trend, with a parallel-trends veto** (Measurement science, medium)
       loadControlCandidates in src/domains/proof-gsc/auto-record-on-ship.ts picks the top 3 pages by demand, so a 50-click page gets 5,000-click controls whose drift dwarfs its signal. Port baselineSimilarityRatio and maxTrendSlopeDivergence from src/domains/attribution/natural-controls.ts (already built and tested) into GSC control selection, and at verdict time compute pre-period daily slopes for treated versus controls, downgrading to a weak_comparison state in measurement-maturity.ts when they diverged before the ship. Comparable controls that provably moved together pre-ship are the core validity requirement of diff-in-diff.
- [ ]  34. **Pool same-lever multi-page batches into one powered experiment** (Measurement science, medium)
       The live Iranopedia batch ships one lever across many sibling pages (8 selected, 34 controls) yet each page gets an independent underpowered verdict. Add a pooled estimator that stacks per-page adjusted lifts with inverse-variance weights into one batch-level verdict with its own placebo test, keyed by the daily plan id from daily-experiment-plan-store.ts. Eight noisy pages become one confident answer to whether this lever works on this page type, which is what the team actually needs to learn.
- [ ]  35. **Power analysis gate: never ship a test that cannot reach a verdict** (Measurement science, medium)
       The planner in src/domains/experiments/daily-experiment-planner.ts scores candidates by opportunity but never asks whether the page has enough impressions to detect the forecast effect within 28 days. Compute the minimum detectable effect from baseline impressions and control noise, and exclude or visibly warn on candidates whose forecastRange from pick-expectations.ts sits below the MDE. This stops the nightly batch from burning page slots on experiments that are statistically doomed to read inconclusive.
- [ ]  36. **Reject controls that share queries with the treated page (SUTVA guard)** (Measurement science, medium)
       If a control ranks for the same queries as the treated page, a winning title steals its clicks and the diff-in-diff double counts the effect; a cannibalizing sibling reads as a fake win. Use gsc_daily_rows (page plus query daily data already synced by src/lib/connectors/gsc/sync-search-analytics.ts) to compute impression-weighted query overlap and exclude any control above roughly 20 percent overlap in auto-record-on-ship.ts. No real growth scientist accepts same-market controls.
- [ ]  37. **Wire placebo inference into GSC verdicts and expand the null to every untreated page** (Measurement science, medium)
       src/domains/attribution/placebo-inference.ts already implements placebo inference for the citation engine but summarizeVerdict in src/domains/proof-gsc/measure.ts never calls it (verified: zero placebo references in the file). With only 3 controls the leave-one-out null has a 3-point distribution, so also compute pseudo-lifts for every untreated page with enough pre impressions via readCumulativeSince in gsc-window.ts and report the treated lift's empirical percentile. Gate high confidence on the permutation p and show it in plain English on the Results row. This turns every won or lost claim into a real permutation test at zero API cost.
- [ ]  38. **Build the specialist scoreboard: Brier-score every voice against settled verdicts** (Team intelligence, medium)
       Join settled verdicts from src/domains/proof-gsc/shipped-change-store.ts back to the TeamReview voices persisted on daily plan records (teamReview in src/domains/experiments/daily-plan-types.ts) and write per-specialist and per specialist-plus-actionFamily won, flat, lost tallies to a new store. Treat each supporting voice's conviction as a probability the pick wins and compute Brier scores per specialist and lever family, giving one defensible skill number and an honest best-forecaster line for the operator. Deterministic, no LLM, recomputed from the ledger; this is the substrate every teammate-learning feature needs and nothing computes it today.
- [ ]  39. **Buy the missing decisive evidence before the batch finalizes** (Team intelligence, medium)
       When a top pick's gatekeeper abstained (extras.serpVerdict null makes emitDataforseoOpinion return null in specialist-opinions.ts), queue one targeted DataForSEO SERP check through the existing capped fail-closed $50 ledger and re-run the debate before acceptance. The team should never ship a top pick with the live-Google teammate silent when one cheap lookup would let it speak. Bound it to top-N picks per night so cost stays pennies.
- [ ]  40. **Put a money headline on Today: dollar ROI plus a lifetime clicks-earned odometer** (Today dashboard, medium)
       Combine won-change measured lift from the proof ledger (src/domains/proof-gsc/load-ledger.ts, traffic-outcome.ts) with per-page value from loadGa4PageRevenueForTenant in src/domains/recommendation-intelligence/ga4-page-values.ts to render: changes Beacon shipped are worth about $X per month, and Beacon changes have earned about 1,240 extra clicks since day one. Persist the pure computation in src/domains/proof-gsc so it is testable, degrade to clicks-only when GA4 value is thin. The header currently celebrates a streak; a compounding money odometer is the retention hook that makes cancelling feel like unplugging a money machine.
- [ ]  41. **Ship a portfolio counterfactual sentence: pages we touched vs similar pages we did not** (Today dashboard, medium)
       The diff-in-diff machinery in src/domains/proof-gsc/measure.ts already picks comparison pages per change; aggregate across the whole ledger into one line under the scoreboard: pages Beacon changed are up 18 percent, similar pages we left alone are down 3 percent. One new pure aggregator in src/domains/proof-gsc plus a line in scoreboard-section.tsx. This is the single most convincing $250/mo sentence the product can render and no surface says it today.
- [ ]  42. **Grade every forecast against the actual and show a running hit rate** (Trust and receipts, medium)
       pick-expectations.ts already produces forecast outcome ranges for each nightly pick, and mature verdicts land in the shipped-change ledger. Join them on settled rows in src/app/(shell)/proof/page.tsx and render we forecast +8 to +20 clicks, we got +14, inside the range, plus a running forecasts-inside-range 9 of 12 line on ProofSummarySection. Nothing convinces a stranger to pay $250/mo like a team that keeps score on its own predictions.
- [ ]  43. **Publish a per-teammate accountability scorecard: conviction calibration plus objection track record** (Trust and receipts, medium)
       team-review.ts stores each voice's conviction percentage per pick and its objections; join both to mature proof verdicts and compute a realized hit rate per teammate from src/domains/team/identity.ts. Render Search demand argues at 80 percent conviction and is right 74 percent of the time on the Today standup strip, and when a pick ships over an objection, stamp the objector on the plan record and check it against the verdict: Visitor behavior objected 4 times this quarter and was right twice. The roundtable becomes accountable instead of decorative, conviction bars gain meaning, and honest disagreement history is something no human agency will ever show a client.
- [ ]  44. **Build a safe body-append push route so answer blocks and FAQs publish in one click** (CMS push depth, medium)
       push-service.ts explicitly refuses add_answer_block, add_faq, and add_h2_section (v1 stays field-targeted only), yet those are the highest-value AEO levers the daily plan picks. Add a body role to CONTENT_ACTION_FIELD_ROLE in src/lib/connectors/wix/url-map.ts and a new append route in executePush that snapshots the whole body field, appends a clearly bounded marked section, and is idempotent on re-push. Converts the two levers that currently force manual Wix edits into one-click armed publishes.
- [ ]  45. **Deep-link every item straight into the Wix editor field** (Worklist UX, medium)
       Cards link Open page (the public URL) but never the Wix editor, so the operator hunts for the page inside Wix on every single item, easily half the apply time. The Wix url-map and collection config are already durable in Supabase (Phase 1 work); derive the editor or CMS item URL per item and render Open in Wix next to Copy on ExecutionCard and MoveCard. Attacks the largest remaining friction in the nightly apply loop.
- [ ]  46. **Honest degradation everywhere** (Carry-over, small) [CARRY-OVER 115]
       when a teammate's auth is dead (GSC token refresh is 401 in the logs RIGHT NOW while the UI says nothing), the team standup strip shows it and every debate that misses that voice says "Demand data is 3 days old (reconnect)".
- [ ]  47. **Wire learned priors into the nightly experiment planner score** (Learning loop, small)
       scoreCandidate in src/domains/experiments/daily-experiment-planner.ts uses only CTR opportunity, position, ownership, and the team multiplier; it never consumes the learned win-rate multipliers from src/domains/learning/experiment-prior.ts or the page-level cautions in proof-outcome-caution.ts (verified: zero references in the file). Load outcomes via loadExperimentOutcomes in build-daily-candidates.ts and fold the bounded multiplier into the candidate score with the learnedPrior tag shown on the pick card. Right now the worklist learns but the nightly batch, the thing that actually ships changes, does not.
- [ ]  48. **Ground the nightly title/meta LLM pass in the page snapshot it already loaded** (LLM engine, small)
       In src/domains/experiments/build-today-preview.ts the llmDrafter calls draftAtomicEditStructured with outline: [] (verified at line 238) even though factsByPath already holds the page's title, h1, meta, and bodyParagraphs from page_snapshots (line 165 proves the data is in scope). The rewrite LLM never sees what the page actually says, and the numeric firewall has almost no grounded numbers, so good drafts with real page facts get rejected. Pass the snapshot facts as outline plus evidenceHints. The single cheapest quality lift in the whole LLM engine.
- [ ]  49. **Elect the winning action from the weighted vote tally, with one vote per specialist** (Team intelligence, small)
       routeMove in src/domains/demand-graph/move-router.ts builds a confidence-weighted votes Map (verified at line 129) but the winner is seeded from GAP_TO_ACTION and only vetoes change it; the sorted tally only feeds whyNot lines. Let a clear vote winner (margin above a threshold over the seed) take the decision with the margin persisted, and normalize each specialist to one total vote split across its suggestedMoveTypes so a voice listing two actions stops casting its confidence twice. This makes the debate actually decide instead of decorating.
- [ ]  50. **Build a retrieval twin that predicts citation likelihood before publishing** (AI visibility (AEO), large)
       Embed owned page chunks plus competitor teardown chunks (competitor-page-snapshots.ts already stores competitor content) with cheap OpenAI embeddings, then run every fanout query as a retrieval and report who wins top-k per query. This simulates the retrieval step of every answer engine locally at near-zero cost: the nightly picker can say your draft ranks 7th of 9 candidate passages for this question, here is the passage to beat, and regenerated drafts get scored by predicted retrieval rank.
- [ ]  51. **Run a weekly strategy review agent that reallocates the plan, not just reports** (Autonomy, large)
       Every Sunday, an LLM pass reads settled verdicts from shipped_change_proof, lever priors in src/domains/learning/experiment-prior.ts, and trend data, then adjusts the planner's lever mix and page-family targets for the coming week, writing a signed memo of exactly what it changed and why. The built weekly recap describes the past; this one changes the future plan. The closest thing to a human strategist doing Monday planning.
- [ ]  52. **Ship an auto-accept policy engine fed by the dormant triage rules** (Autonomy, large)
       publishing-mode-store.ts already holds a per-site armed switch; extend it to per-lever policies where the operator pre-approves classes like meta descriptions, image alt, or FAQ schema with per-day caps, and the nightly loop auto-accepts and applies those picks through the existing apply path while holding everything else for review. Feed it with src/domains/learning/triage-rules.ts, which already computes auto_accept recommendations at a 90% historical acceptance threshold but is passive and never consumed. This is the jump from team that proposes to team that runs the site, with every auto-action receipted in the proof ledger.
- [ ]  53. **Trigger an automatic forensic investigation when a collapse signal fires** (Autonomy, large)
       src/domains/trend-radar/spike-detector.ts detects collapses and src/domains/visibility-events has the attribution engine, but nothing runs them autonomously. When a high-severity collapse fires on a page family, auto-run an investigation: fetch the page live, check for noindex and robots regressions, pull the cached SERP for its top queries, cluster recent changes from the changelog, and file a ranked-cause diagnosis card. The difference between a dashboard that shows a drop and a team that investigates it overnight.
- [ ]  54. **Per-page dossier route** (Carry-over, large) [CARRY-OVER 87]
       (/page/[path]): everything the team knows about one page in one place: chart, queries, teammates' reads, history of changes + verdicts, current recommendation. Linked from every page name. This becomes the "research" surface.
- [ ]  55. **Build a section-by-section outline-to-draft pipeline with per-section sources** (Content engine, large)
       CreatePageBriefSchema in src/domains/llm/schemas.ts stops at an outline; nothing drafts the actual page. Add a SectionDraftSchema and have structured-drafter.ts walk the brief section by section, grounding each on the EvidencePacket from gap-compiler.ts, the competitor outline from competitor-page-audit.ts, and fanout questions, with every section passing draft-quality.ts and the numeric-fidelity firewall. Assemble a paste-ready full page with a sources appendix so a New Pages Move ships as a finished draft, not a homework assignment.
- [ ]  56. **Turn GSC decay into a refresh production line with a refresh lever in the daily loop** (Content engine, large)
       triggers/stale-content.ts only fires on sitemap lastmod older than 18 months at low confidence. Build a refresh queue from gsc_daily_page_totals and decay RPCs: pages losing clicks or position quarter over quarter get a refresh brief naming new queries with no matching H2 (page-research-pack.ts buckets) plus the winner's newer sections. Add a refresh lever to build-daily-candidates.ts so refreshes flow through accept, apply, verify-live, and diff-in-diff like every other lever. Refreshing decayed winners is the highest-ROI content work that exists and Beacon has no lane for it.
- [ ]  57. **Build an outreach execution pipeline: send, track, and close the pitches Beacon only drafts** (Completeness critic, large)
       The planned items generate ready-to-send get-cited-there pitches and find unlinked mentions, then stop; a human team's core value is actually sending and following up. Use src/lib/email/resend.ts to send pitches from a per-tenant outreach identity, track opens, replies, and won links in an outreach_pipeline table, auto-follow-up on silence, and confirm acquisition via the planned backlink watch. Link acquisition is the highest-leverage ranking input the current system never executes end to end.
- [ ]  58. **Publish full Farsi editions of the top 50 pages with hreflang and ZWNJ-correct Persian text** (Completeness critic, large)
       The plan stops at bilingual terms in metas, a Farsi gap matrix, and a hreflang audit; nobody actually produces Farsi-language pages. Extend src/domains/page-factory plus the Wix push layer to generate fa editions of the top pages, wire the Persian-English hreflang pairs, and QA ZWNJ and RTL rendering using the already-built Persian tokenizer. Persian-script search from Iran and the diaspora is a second SERP where English competitors do not exist, and it compounds every AEO play since Farsi-locale prompts are already tracked.
- [ ]  59. **Build an ask-your-team chat that answers any question about the site from live data** (frontier, large)
       src/domains/answer-intelligence/build-index.ts and query-index.ts already index tenant facts, and src/domains/team/identity.ts defines the 9 named voices. Add a /ask route where the operator types 'why did clicks drop on /cheetah' and the right specialist answers in first person with real GSC, Profound, and DataForSEO numbers pulled through existing loaders, every claim linked to its source surface. This turns Beacon from a dashboard into a team you can talk to, the single most demo-able $250 moment in the product.
- [ ]  60. **Mine competitor rankings via DataForSEO Labs: keyword gap matrix plus clone-and-beat briefs** (frontier, large)
       Pull DataForSEO Labs ranked keywords for the top 3 of the 8 real co-mention competitors monthly under the existing fail-closed ledger. Intersect against Iranopedia's GSC query set for a winnability-filtered content gap feeding create-page candidates in src/domains/demand/canonical-create-page.ts, and traffic-weight competitor URLs from the same pull to rank their top 20 money pages, running each through the existing competitor-page-audit.ts teardown as a 'their best page, our better version' brief. One capped data pull turns the competitor tab from surveillance into a candidate factory; the existing scanner in src/domains/product/keyword-gap-scanner.ts is AI-observation based and cannot do this.
- [ ]  61. **Ship agentic full-page rewrites with side-by-side approve and one-click Wix publish** (frontier, large)
       Chain the Page Surgeon evidence packets (src/domains/recommendation-intelligence/page-surgeon/assemble-packet.ts) with the draft-quality gate in src/domains/drafts/draft-quality.ts: the LLM rewrites the page body section by section, Beacon renders old vs new with per-section accept, and publishes through the existing field-merge Wix client in src/lib/connectors/wix/client.ts. Auto-enroll every rewrite in the existing diff-in-diff measurement so the team learns whether full rebuilds beat single-lever edits.
- [ ]  62. **Turn the entity-attribute page factory into a governed weekly production line** (frontier, large)
       src/domains/page-factory/entity-attribute-factory.ts emits candidates marked needsDemandValidation but nothing runs the chain at scale. Wire it end to end: validate demand via the DataForSEO keyword cache, draft with src/domains/llm/structured-drafter.ts under the draft-quality gate, publish 3 to 5 pages per week through the Wix blog path, and auto-enroll each page in measurement with its own comparison set. An encyclopedia compounds to hundreds of demand-validated pages this way, which no per-card workflow can reach.
- [ ]  63. **Build the seasonality engine: proven peak calendars that schedule work 6-8 weeks ahead** (Search Console depth, large)
       Combine 16-month GSC history aggregated by week per topic cluster (Persian tokenizer already exists) with DataForSEO Labs historical keyword volume back to 2019 for the seasonal clusters (Nowruz, Yalda, Chaharshanbe Suri, Mehregan), and compute recurring peak months with confidence. Feed the nightly planner a seasonal window opens in N weeks candidate source so refreshes ship ahead of the spike instead of after it. An encyclopedia's traffic year is won on this timing, and no current surface knows it.
- [ ]  64. **Build forecast ranges from Beacon's own measured lever priors with winner's-curse shrinkage** (Learning loop, large)
       forecastRange in pick-expectations.ts assumes every change captures a hardcoded 25 to 75 percent of the generic CTR-curve gap, forever, ignoring everything the ledger has learned. Once realized lifts are persisted, compute the empirical capture distribution per lever from mature outcomes (titles might capture 15 to 40 percent, answer blocks 30 to 80), blend with the static band under a minimum sample, and shrink each threshold-selected win toward its action-family mean weighted by precision so winner's curse does not make forecasts chronically overpromise. The pitch changes from industry curve says to across our last 9 title tests on this site the typical lift was X, which no competitor can copy.
- [ ]  65. **Build the shadow portfolio: measure picks made versus eligible picks skipped** (Learning loop, large)
       Eligible high-scoring candidates that were not selected form a natural comparison cohort against selected picks, and each night's top rejected candidates can be persisted with the forecast they would have carried since their untreated GSC drift is a free counterfactual. Track both cohorts' click deltas over matching windows using existing gsc-window.ts reads and report one honest sentence on /proof: pages we changed moved +9 percent, similar pages we skipped moved +2 percent. Also compare rejected-pick drift to forecasts to calibrate the CTR curve and the voices that backed them. This is portfolio-level proof that Beacon's brain adds value, a sales weapon no competitor has, at zero API cost.
- [ ]  66. **Rebuild the cross-tenant pattern brain on the GSC proof ledger with anonymous dims** (Learning loop, large)
       src/domains/global-patterns/contracts.ts is the right skeleton (distinct-tenant gates, per-change dedup, context bins) but is hardcoded to segment local_residential_builder and citation deltas, and nothing feeds it from the GSC ledger. Generalize the PatternKey to siteCategory, canonicalMoveType, intentBucket, positionBand over mature ShippedChangeRecord outcomes, aggregate to a Supabase table, and let resolvePrior in experiment-prior.ts back off to the global prior when tenant cells are thin, with the contributing_tenant_count >= 3 floor from contracts.ts governing any surfaced benchmark line like title changes like this typically added 5 to 30 clicks a month across sites Beacon runs. New tenants start with a warm brain (including seeded specialist reliability weights) instead of a cold one, honoring the shared-brain privacy model already locked in.
- [ ]  67. **Bayesian verdicts with credible intervals instead of fixed floors** (Measurement science, large)
       summarizeVerdict in measure.ts uses hard floors that ignore sample size, so a 3-click lift on 200 impressions and on 20,000 impressions read the same. Model CTR as beta-binomial and clicks as gamma-Poisson with the pre window as prior, and report P(lift greater than 0) plus a 90 percent credible interval: we are 87 percent sure this helped, likely +5 to +40 clicks a month. Pure TypeScript with no dependencies, and it upgrades every headline in Results from a threshold to honest quantified belief.
- [ ]  68. **Measure the target queries directly, not just page aggregates** (Measurement science, large)
       ShippedChangeRecord stores targetQueries but the verdict in run-measurement.ts is computed on whole-page totals, so a title aimed at one striking-distance query is diluted by the page's entire query mix. Add a per-target-query diff-in-diff (CTR and position for the exact queries, from gsc_daily_rows) reported alongside the page verdict. This is the difference between the page moved and the thing we changed worked, and it gives the roundtable concrete ammunition for the next pick.
- [ ]  69. **Seasonality engine: per-family demand profiles, event calendar, and a voice that objects** (Measurement science, large)
       Iranopedia demand swings violently on Nowruz, Yalda, Chaharshanbe Suri, and Ramadan; a page shipped 3 weeks before Nowruz reads a fake win, and diff-in-diff fails when a page family IS the seasonal topic. Learn per-pageFamily weekly and annual demand profiles from the synced GSC daily series (proof-gsc/daily-series.ts) plus a per-tenant event calendar table, stamp verdicts measured across a seasonal inflection so priors de-weight them, and emit a deterministic seasonality voice through the SpecialistOpinion contract that objects to shipping into a demand cliff. Also unlocks proactive picks: Nowruz demand starts climbing in 6 weeks, prep these pages now. The chaharshanbe date-intent miss shows this class of error already bit once.
- [ ]  70. **Learn bounded per-specialist vote weights by lever family, with calibration shrink** (Team intelligence, large)
       Extend the proven experiment-prior.ts pattern (MIN_DECIDED=3, clamp 0.85 to 1.15, explainable counts) from moves to teammates: multiply each opinion's confidence in routeMove by a reliability weight earned from the scoreboard, keyed by specialist plus ExperimentFamily with backoff to the specialist-only cell under MIN_DECIDED. Also bucket persisted confidencePct into bands, compare each band to measured win rate, and deterministically shrink chronically overconfident voices (dataforseo is hardcoded 0.9, clarity 0.6 in specialist-opinions.ts and never moves). The roundtable can then say search demand has called 8 of its last 11 winners, which is exactly the out-think-a-human-team proof a $250 buyer wants.

## Impact 7

- [ ]  71. **Align AI answers to pages sentence by sentence: the passage that beat you, the line AI quoted** (AI visibility (AEO), medium)
       pullProfoundAnswers stores full answer texts plus citation URLs. Align sentences (n-gram overlap) between AI answers and cited competitor pages to isolate the literal passage the engine lifted, shown on the MoveCard teardown as the 43 words that beat you, and run the same alignment against owned page bodies to show AI quoted this line receipts on the proof ledger. Feed the winning shapes (length, structure, opening pattern) into structured-drafter.ts prompts, closing the loop from draft style to actual citation.
- [ ]  72. **Build the second-order citation playbook: get listed on the sources AI already trusts** (AI visibility (AEO), medium)
       Rank third-party domains by citation frequency across tenant topics from profound_citation_rows plus the llm-mentions cache, classify them (listicle, directory, UGC, media), and generate a per-domain playbook: the exact page citing competitors, the prompt it wins, and the pitch or listing action to get Iranopedia onto it. Engines repeat their trusted sources, so being cited BY the cited is often faster than outranking them; the evidence-relevance noise-domain list already separates aggregators from realistic targets.
- [ ]  73. **Ground entity pages in Wikidata and generate Person schema for biography pages** (AI visibility (AEO), medium)
       CONTENT_PAGE_EXPECTED_SCHEMA in expected-schema.ts only expects Article, yet the live experiment batch touches comedians, actors, and singers pages. Add a biography page-class detector, teach composeSchema to emit Person JSON-LD (name, birthDate, occupation, sameAs to Wikipedia/Wikidata/IMDb), and match other entity pages to Wikidata QIDs (label plus alias matching, operator-confirmable) for sameAs output. AI engines resolve people to knowledge-graph entities before citing sources; an encyclopedia should be wired into that graph.
- [ ]  74. **Make the drafter learn from its own wins: pattern tagging plus few-shot from the ledger** (AI visibility (AEO), medium)
       The daily loop ships answer blocks but never records their structure, and Wins link to exact shipped artifacts in move_drafts that the drafter never learns from. Tag each shipped block with a pattern id (definition-first, stat-first, table, Q&A pair, step list), aggregate proof and citation verdicts by pattern per topic, and inject winning examples as few-shot prompts into structured-drafter.ts. After 20-30 ships the team can say stat-first blocks win citations 2x more often on history topics here, and the content engine starts compounding.
- [ ]  75. **Open a Bing lane: IndexNow pings on verify-live and index-status checks for ChatGPT visibility** (AI visibility (AEO), medium)
       ChatGPT browsing rides Bing's index and there is zero Bing anywhere in the repo. Fire an IndexNow ping automatically when verify-live confirms a shipped change, and add a lightweight Bing index-status check for money pages. A page absent from Bing cannot be cited by ChatGPT regardless of quality, and nobody else's tool tells a Wix owner that.
- [ ]  76. **Publish citable dataset pages from owned data with Dataset schema** (AI visibility (AEO), medium)
       Iranopedia owns genuinely unique structured data (Persian name meanings, Iranian holiday dates by year, city facts) and Beacon holds proprietary aggregates (GSC query data, Profound prompt volumes). Package the top 3 as canonical dataset pages: sortable table, dated methodology line, Dataset JSON-LD, and a cite-this-page block, routed as new-page candidates and tracked through the citation-outcome lane. Engines disproportionately cite pages that look like primary data sources; this is the purest be-THE-source play available.
- [ ]  77. **Run answer-drift detection on the live poll stream, not just imports** (AI visibility (AEO), medium)
       buildAnswerIntelligenceIndex computes narrative shifts only at import time on imported data. Run the same diff week over week on nightly native poll answers per prompt: detect when the brand is added or dropped, when descriptors change, and show the sentence-level diff (Perplexity stopped calling you the leading Persian culture resource on Tuesday). Wire the alert into the Today standup so a lost answer is treated like a lost ranking.
- [ ]  78. **Score answerability at the passage level and show quotable coverage per fanout question** (AI visibility (AEO), medium)
       src/domains/pages/extractability.ts grades whole pages on coarse factors. Split page_snapshots content into chunks and score each against its target fanout query: self-contained in 40-60 words, entity-named first sentence, contains a number or date, no pronoun opener (reusing the heuristic already in competitor-page-audit.ts). Show coverage percent per page, surface the passage AI would lift, wire the same rules into draft-quality.ts so unquotable answer blocks are rejected, and draft the missing quotables through llm-answer-block.ts.
- [ ]  79. **Track cross-engine share of voice per topic with drop alerts that open moves** (AI visibility (AEO), medium)
       Owned SoV data lives in three places that never meet: profound_visibility_rows, native poll observations, and the dataforseo-llm-mentions cache. Compute a weekly owned-vs-top-competitor SoV number per engine per topic, trend it, and when an engine drops past a threshold, automatically open a candidate move naming the prompts that flipped. Beacon currently sees citations happen; this makes it react to losing them.
- [ ]  80. **Add a portfolio circuit breaker that pauses autonomy after consecutive losses** (Autonomy, medium)
       If two consecutive settled batches measure negative, or N rollbacks fire within a week, automatically pause auto-accept and nightly shipping for that tenant and file a plain-language card: I paused myself, here is why, tap to resume. Reads settled verdicts from shipped_change_proof via the measure-due path. Auto-accept without a stop-loss is how autonomous systems lose trust permanently.
- [ ]  81. **Auto-retire levers that keep losing on a page family, with scheduled retests** (Autonomy, medium)
       When a lever has 3+ settled losses and zero wins on a page family, scoreCandidate in daily-experiment-planner.ts should auto-suppress it there and say so plainly: I stopped rewriting titles on city pages, it lost 3 times. Auto-unsuppress after 90 days for a single retest so the rule never fossilizes. Turns the proof ledger into behavior instead of history.
- [ ]  82. **Auto-run a SERP displacement check when a money query drops 3+ positions** (Autonomy, medium)
       When GSC daily data shows a top query losing 3 or more positions, spend one capped DataForSEO SERP pull (src/domains/serp/dataforseo-serp.ts, governed by the budget ledger) to identify who displaced us, then file a competitor-response card with the winner's page teardown. SERP reactions currently happen only on nightly picks; losses deserve the same reflex as opportunities.
- [ ]  83. **Diff nightly Profound citations and file cards when AI answers drop us** (Autonomy, medium)
       sync-nightly.ts pulls Profound citation data every night but nothing compares runs. Diff per tracked prompt: when a prompt that used to cite the site stops, auto-file a card naming which domain now gets the citation and what its answer contains that ours lacks. AI answer share is the AEO wedge, and losses there are invisible until someone stares at a chart.
- [ ]  84. **Escalate connector health: failure streaks at 3 nights plus token-expiry forecasts** (Autonomy, medium)
       Sync failures end at log.warn and Google token death is only classified reactively at read time (expiry-handler.ts). Store a consecutive-failure counter per (tenant, provider) next to last_synced_at in connector-store.ts and file a fix card at 3 straight nights naming the provider, error, and one-click reconnect. Also compute days until the 7-day refresh window closes nightly and at T-2 days send a push or Resend email (src/lib/email/resend.ts) with a reconnect deep link, turning the recurring stale-connection incident into a non-event.
- [ ]  85. **Persist a cron_runs ledger and render a cron health panel from it** (Autonomy, medium)
       syncAllConnectedForActiveTenants (src/lib/connectors/cron-sync.ts) and measure-due return rich per-run results that only hit log.info and vanish. Write each run (started, finished, duration, per-source ok/failed, skipped enrichment, backlog) to a cron_runs table, then render it on /settings/connections: last run, per-source results, next scheduled time from vercel.json, failure streaks. This is the substrate every other autonomy watchdog needs, and a $250/mo team must prove it showed up for work every night.
- [ ]  86. **Run a nightly publish-path canary: Wix token probe plus dry-run per tenant** (Autonomy, medium)
       Wix is excluded from READ_SOURCES because it is publish-only, so a dead token or stale url-map is discovered only when an accepted change fails to apply. Nightly, run a cheap authenticated read (list collections via src/lib/connectors/wix/client.ts) plus one executePush dryRun on a representative pushable card (publishing-mode-actions.ts already does this once for arming), persisting a publish-health row. Today then shows publishing is ready or the one precise fix needed before the morning batch.
- [ ]  87. **Watch comparison pages for mid-window edits and flag contaminated verdicts** (Autonomy, medium)
       Diff-in-diff verdicts in src/domains/proof-gsc/measure.ts assume comparison pages stayed constant across the 7/14/28-day windows. Periodically re-fetch control pages during open windows using the fetch machinery in live-verification.ts plus content hashes; if a control changed materially, mark the measurement contaminated and swap in a clean comparison instead of publishing a corrupted verdict. Wrong wins are worse than no wins for a product selling proof.
- [ ]  88. **Cannibalization + internal-link intelligence into the debate** (Carry-over, medium) [CARRY-OVER 88]
       the GSC cannibalization detection exists; surface it as a Demand-voice objection ("two of your pages fight for this query") instead of a separate buried card.
- [ ]  89. **Every claim clickable** (Carry-over, medium) [CARRY-OVER 113]
       query names link to the GSC UI filtered view (or dossier), competitor domains open the actual page, "26% dead clicks" links to the Clarity project.
- [ ]  90. **Every evidence line carries its source + age** (Carry-over, medium) [CARRY-OVER 112]
       "GSC, read 2h ago" / "live Google results, read Jun 27" / "your page, crawled Jun 11 (stale)". Tiny gray text, huge trust.
- [ ]  91. **Kill silent successes** (Carry-over, medium) [CARRY-OVER 116]
       every server action returns a one-line receipt the UI shows ("Verified live on iranopedia.com at 9:42pm. Tracking started.").
- [ ]  92. **Session memory of the operator's taste** (Carry-over, medium) [CARRY-OVER 118]
       when the operator edits a paste text before applying, store the diff and show "You usually soften our wording; here is a softer variant" after 3+ edits (learning/change-patterns.ts is built and idle).
- [ ]  93. **Weekly snapshot rescan** (Carry-over, medium) [CARRY-OVER 81]
       (crawl own site, $0): page_snapshots are 20 days stale, so every draft argues against old content. Add staleness badge to any evidence derived from a snapshot older than 7 days.
- [ ]  94. **Worklist warm under 3s** (Carry-over, medium) [CARRY-OVER 94]
       same treatment for loadChangesView; the demand graph snapshot cache exists, extend it to the full view model.
- [ ]  95. **Auto-interlink first mentions of known entities across the encyclopedia** (Content engine, medium)
       Detect first mentions of known entities (PageEntity topics plus the entity list from entity-attribute-factory.ts) inside stored body excerpts and propose an internal link to the canonical entity page, emitted through the existing InternalLinkDraft schema. Encyclopedias live and die on dense entity interlinking; this builds it systematically instead of via one-off link picks.
- [ ]  96. **Draft the missing H2 sections for the content_depth lever** (Content engine, medium)
       EvidencePacket computes structure gaps and page-research-pack.ts names content_depth as a lever, but only title, meta, answer block, and internal link levers produce a draft artifact. Generate the exact missing sections from the competitor outline plus unanswered fanout questions, each gated by draft-quality.ts and the numeric-fidelity firewall. The deepest gap type finally gets a paste-ready fix instead of a vague instruction.
- [ ]  97. **Model internal-link equity with PageRank and click depth, then fix starved money pages** (Content engine, medium)
       Snapshots already carry internal_links but no one computes equity or BFS distance from the homepage. Run weighted PageRank over the owned link graph, compute click depth, join per-page equity against impressions from gsc-page-signals.ts, and flag pages with high demand but starved equity or depth 4+, emitting InternalLinkDraft batches that name the exact hub to link from. The parked persian-female-first-names crawl-depth experiment is direct evidence this class exists on Iranopedia.
- [ ]  98. **Store a bounded main-content excerpt in PageSnapshot to unlock body-level intelligence** (Content engine, medium)
       PageSnapshot in src/domains/pages/types.ts keeps only word_count, content_hash, and heading lists; the crawler throws the body away. Persist a boilerplate-stripped excerpt (first ~2000 words, reusing the mainEl logic already in competitor-page-audit.ts) in the extractors. One change unlocks decay diffs, near-duplicate shingles, entity interlinking, quotable-line scoring, and quote matching against Profound answers.
- [ ]  99. **Tear down the top 3 SERP winners per move and compute a consensus outline** (Content engine, medium)
       gap-compiler.ts fuses only competitorUrls[0] per move, so one weird winner skews the whole gap analysis. Audit the top 3 winners per demand key (serp_verdict topDomains are already persisted in move_drafts) through auditCompetitorPage and compute the sections at least 2 of 3 winners share. Consensus sections are far stronger section-gap evidence than a single page's quirks.
- [ ] 100. **Wire the Wix Redirects API and ship the merge executor with equity-transfer measurement** (Content engine, medium)
       The thin-content-overlap trigger already emits merge_pages recommendations, but src/lib/connectors/wix/client.ts only covers wix-data items and store products, so merges are advice-only. Add the Wix redirects endpoint plus a push-service artifact type, generate the 301 map and a merged draft that unions the unique sections of both pages, and measure equity transfer to the surviving page over 28 days through the proof loop. Turns a whole recommendation class from talk into executed, receipted moves with rollback.
- [ ] 101. **Add a spam-policy risk governor that throttles the page factories before Google does** (Completeness critic, medium)
       Boilerplate-ratio and de-templating guards check single pages, but nobody scores the sitewide pattern Google's scaled-content-abuse policy actually punishes: factory page share, cross-page template similarity from the planned shingle detector, and publish velocity. Compute a tenant-level policy risk score and wire it as a hard governor on entity-attribute-factory.ts and city-service-factory.ts in src/domains/page-factory, slowing or pausing production as risk rises. One algorithmic penalty erases every win in the ledger, and no planned item models that failure mode.
- [ ] 102. **Build a sitewide stale-fact sweep so the encyclopedia never states superseded facts** (Completeness critic, medium)
       The accuracy watch only checks what AI says about the brand, and refresh levers only fire on traffic decay; nothing proactively finds pages asserting outdated facts (as-of-2023 phrasing, old population figures, dead officeholders, past-tense events written as upcoming). Run an LLM pass over the planned PageSnapshot main-content excerpts to extract time-sensitive claims, verify against cached evidence, and queue corrections as refresh moves with the exact sentence to change. For an encyclopedia, being current is the trust product, and it doubles as a genuine freshness signal.
- [ ] 103. **Launch a diaspora city-guide factory: Persian life in LA, Toronto, London as a content lane** (Completeness critic, medium)
       The diaspora demand map is analysis only; nobody produces the pages that capture it. Extend src/domains/page-factory/city-service-factory.ts into a diaspora lane (Persian restaurants in Los Angeles, Persian grocery Toronto, Norouz events London) fed by the multi-geo DataForSEO volumes, with local ItemList schema and outbound citations to real businesses. These are commercial-adjacent, low-competition queries where an encyclopedia with cultural authority can win fast and where ad RPM is highest.
- [ ] 104. **Own the year rollover: Solar Hijri aware refresh line that flips 1404 to 1405 and 2026 to 2027** (Completeness critic, medium)
       The seasonality engine schedules work ahead and Event schema is refreshed annually, but no item does the classic year-stamp play: flipping year-bearing titles, H1s, and body dates across every date-anchored page before demand shifts, and Iranopedia has two new years (Nowruz flips the Persian year, January flips the Gregorian one). Build a Persian-Gregorian calendar conversion module in src/lib/dates.ts territory, inventory year-stamped pages from stored titles, and auto-queue rollover edits 6 weeks ahead of each flip. The chaharshanbe incident (194 impressions on a 2026 date query answered with a definition) proves date demand is already being missed.
- [ ] 105. **Prove backups exist: scheduled dumps, Supabase PITR checks, and a monthly restore drill** (Completeness critic, medium)
       The only backup-shaped item is a pre-bundle Wix collection export; the proof ledger, priors, and learning brain on vlxwevsdvwxvopkjsewo have no verified recovery path, and .data is gitignored with no snapshot policy. Schedule logical dumps of hot tables plus .data archives to object storage, verify PITR is enabled and alarms if not, and run a scripted monthly restore drill that boots the app read-only against the restored copy and diffs row counts. A team whose entire value is accumulated evidence cannot have an untested backup story.
- [ ] 106. **Run a nightly hack and cloaking sentinel: Googlebot-UA diff plus injected spam scan** (Completeness critic, medium)
       The planned external-edit scan only watches tracked fields; a hacked site (injected pharma links, Japanese keyword pages, UA cloaking) is a classic small-site death that would currently surface days late as a traffic mystery. Add a nightly dual fetch per sitemap URL, normal UA versus Googlebot UA, diff the rendered HTML, and scan for spam-link and off-topic keyword injection inside src/domains/scanning/scan-site-domain.ts, filing a loud attention-band card with the exact injected snippet. Include new-URL detection in the sitemap for pages the tenant never created.
- [ ] 107. **Ship a site migration guardian: mass URL change detection, redirect maps, pre and post traffic guard** (Completeness critic, medium)
       Migration protection is the single most expensive thing small businesses hire SEO teams for, and no item covers it: a Wix template swap or URL structure change would just read as an unexplained collapse. Detect mass URL churn via sitemap diff in src/domains/scanning/sitemap-parse.ts plus content_hash turnover, auto-generate an old-to-new redirect map through the planned Wix Redirects API integration, and run a page-level pre versus post traffic reconciliation for 28 days. Also fire the guard proactively when the operator announces a redesign.
- [ ] 108. **Add AI-assistant search volume as a first-class demand column in the keyword universe** (DataForSEO depth, medium)
       DataForSEO's ai_optimization ai_keyword_data endpoint returns how often keywords appear in AI chat prompts, the prompt-volume signal the borrowed Profound account only gives for OpenAI categories. Add it behind the same gauntlet as a second demand column in dataforseo-keywords.ts and KeywordDemand so the nightly picker and the AEO teammate rank answer-block levers by AI-ask volume. Topics with high AI volume but low Google volume are exactly where an encyclopedia wins citations cheaply before competitors notice.
- [ ] 109. **Capture the featured-snippet holder and text, then build a snippet-steal lever** (DataForSEO depth, medium)
       serp-provider.ts flags featured_snippet presence but never records who holds it or what text Google lifted. From the advanced endpoint, store holder domain plus snippet text in the SERP history, then generate a lever for queries where the tenant ranks 2-8 and someone else holds the snippet: rewrite the opening block to out-format the incumbent using the existing safe-answer-block.ts machinery. Snippet wins are rank-1-equivalent traffic without outranking anyone.
- [ ] 110. **Run a JS-rendered full-site technical crawl through the On-Page API into worklist levers** (DataForSEO depth, medium)
       Beacon's own scanning fetches raw HTML, missing what Wix client-side rendering adds or hides. The On-Page API crawls with browser rendering for roughly 12 cents per 1000 pages and returns duplicate titles, thin pages, redirect chains, broken links, orphan depth, and non-indexable pages. Map its checks into deterministic technical levers on the goal-grouped worklist (fix 12 duplicate titles as one card) via detect-findings.ts and rerun quarterly to prove debt is shrinking. One integration delivers a professional technical crawl instead of hand-building each check.
- [ ] 111. **Verify shipped blocks exist in raw HTML with dual rendered fetches before measuring** (DataForSEO depth, medium)
       On Wix a block can render for humans via JS while staying invisible in served HTML, silently voiding the experiment, and GPTBot, ClaudeBot, and PerplexityBot do not execute JavaScript. After each apply, call On-Page instant_pages twice (JS on and off, about a cent) and record rendered-visible vs source-visible on the verification receipt, flagging source-invisible answer blocks before wasting a 28-day window; alternatively port src/domains/pages/render-check.ts (currently local-Chrome-only, never runs on Vercel) to a serverless renderer and diff full extractable text on top-cited pages. Protects the integrity of the entire proof loop on JS-heavy platforms.
- [ ] 112. **Add a Clearscope-style term-coverage grade to every draft before ship** (frontier, medium)
       Beacon's draft-quality gate (src/domains/drafts/draft-quality.ts) only rejects generic or thin output; compute a coverage score from cached SERP top-10 results plus fetched competitor text in src/domains/pages/competitor-page-snapshots.ts and show 'covers 14 of 19 terms winners use, missing: X, Y' on the prepared pack. Re-run the same grade at push time on operator-edited text so a paste edit cannot silently drop coverage. This is the core of what SurferSEO and Clearscope charge for.
- [ ] 113. **Add a content pruning engine: merge, redirect, or retire dead pages** (frontier, medium)
       Identify pages with zero clicks, zero AI citations, and zero inbound internal links over 6 months using GSC data, the citation index in src/domains/pages/citation-index.ts, and the link graph, then propose merge-into-sibling or retire Moves with the same safety checks as guardrails.ts. Beacon only has a thin-content-overlap trigger today. For a 500-page encyclopedia, consolidating dead pages that dilute topical authority is one of the highest-leverage moves a human strategist would make.
- [ ] 114. **Add capped backlink monitoring plus a link gap radar via the DataForSEO Backlinks API** (frontier, medium)
       Backlinks are Beacon's biggest data hole and GSC does not expose them. Add a DataForSEO Backlinks producer under the same fail-closed monthly ledger as dataforseo-serp.ts: monthly referring-domain snapshots for the tenant plus the top 3 competitors, stored beside src/domains/off-site-authority/compute-snapshot.ts, with new-authority-link and lost-link events feeding the roundtable. Diff for domains linking to 2 or more competitors but not us and feed the top gaps straight into the get-cited-there pitch generator, keeping it a debate signal rather than a backlink-explorer clone.
- [ ] 115. **Audit AI crawler access and publish llms.txt plus markdown mirrors of top pages** (frontier, medium)
       Extend src/domains/pages/robots-parser.ts with the known AI user-agent list (GPTBot, ClaudeBot, PerplexityBot, Google-Extended, Bytespider), verify key pages against robots rules, and confirm extractable text via extractability.ts survives without JavaScript; one wrong Wix robots default silently zeroes AI visibility and Beacon would misdiagnose it as a content problem. Then serve the fix side: generate llms.txt plus per-page markdown mirrors from existing page snapshots, hosted on the Beacon side if Wix cannot serve root files, cross-linked from live pages. Directly measurable with the existing AI-crawler band and Profound citation series.
- [ ] 116. **Auto-queue a defensive counter-move when a competitor ships a page at an owned query** (frontier, medium)
       competitor-intel already diffs sitemaps and page structure (sitemap-changes-store.ts, structural-diff.ts) and detect-moves.ts spots citation shifts. Add the reflex: when a new competitor URL targets a query we own top-5, auto-create a defense Move with the teardown attached and a freshness or expansion draft prepared. Today's pipeline reacts after rankings drop; this reacts the day the threat is published.
- [ ] 117. **Build a defended-queries rank tracker from GSC history with ship markers** (frontier, medium)
       Beacon only has per-page sparklines; build a tracked-query portfolio with daily position series per money query from the GSC query-level data already synced (gsc_daily_page_totals plus src/domains/recommendation-intelligence/gsc-page-queries.ts), rendered with the same ship-marker treatment as the Today scoreboard and framed as queries the team is defending. Trigger a capped DataForSEO verification via runSerpQuery in src/domains/serp/dataforseo-serp.ts only when GSC shows a 3+ position move, so live checks cost cents. This is the single most-used feature of every parity tool and it is nearly free from data already in Supabase.
- [ ] 118. **Build a sitewide entity knowledge graph in JSON-LD with Wikidata sameAs links** (frontier, medium)
       src/domains/entity/entity-extract.ts already extracts entities and composeSchema already emits Article plus BreadcrumbList. Upgrade to an @id-linked graph: every person, place, and tradition gets a stable node with sameAs pointing at Wikidata and Wikipedia, referenced from every page that mentions it. AI engines resolve entities before they cite; making Iranopedia the machine-readable authority on Persian-culture entities is a durable AEO moat.
- [ ] 119. **Build an algorithm weather sensor that shields verdicts and flags same-day SERP shakeups** (frontier, medium)
       Compute portfolio-wide day-over-day position variance from GSC daily data as a free volatility index, cross-check a maintained list of confirmed Google updates, and additionally track a 20-keyword money watchlist daily under the existing $50 fail-closed ledger in src/lib/cost for same-day rank churn. When volatility spikes, paint weather bands on the Today scoreboard, fire a war-room item naming the exact SERPs that moved, and have the proof engine in src/domains/attribution extend or caveat 7/14/28-day verdicts. Without this, diff-in-diff can silently claim a Google update as an experiment win, which is a trust-destroying failure mode.
- [ ] 120. **Cluster keywords by SERP overlap so one page targets one intent** (frontier, medium)
       Beacon's collapse-create-page-siblings.ts clusters by topic-label text similarity; ground truth is SERP overlap (3+ shared URLs means one page can win both). Use accumulated SERP snapshots to compute URL-overlap clustering before create-page Moves are finalized, merging siblings Google treats as one intent and splitting ones it treats as two. Directly reduces the self-cannibalization that gsc-cannibalization.ts later has to detect and repair.
- [ ] 121. **Compute internal PageRank and route authority to money pages** (frontier, medium)
       Beacon has raw internal_links data plus page-local triggers (internal-link-opportunity.ts, orphan-page.ts) but no site-level authority model. Run a simple PageRank iteration over the internal link graph, surface 'authority pools on X but your money page Y starves', and emit specific add-link Moves with anchor text from the CTR title scorer vocabulary. This upgrades internal linking from page-local heuristics to the site-level strategy a human team would charge for.
- [ ] 122. **Factory-produce comparison pages from entity pairs AI engines love to answer** (frontier, medium)
       X vs Y is the intent AI answers most, and DEFAULT_ATTRIBUTES in entity-attribute-factory.ts never pairs entities. Mine co-occurring entities from demand clusters and profound_fanout_rows, validate pair demand via the keyword cache, and produce structured comparison pages with a verdict table answer engines can lift verbatim. For an encyclopedia the comparison matrix is nearly unbounded and almost uncontested.
- [ ] 123. **Rank the sources AI trusts and generate ready-to-send get-cited-there pitches** (frontier, medium)
       Aggregate cited domains from dataforseo-llm-mentions records and Profound citation rows into a trusted-source leaderboard per topic (Wikipedia, TripAdvisor, specific travel listicles). For cited listicles and roundups missing the tenant, generate a ready-to-send inclusion pitch (angle, specific claim, backing data) as a manual-only off-site Move, replacing the detection_not_implemented placeholders in src/domains/off-site-authority/recommendation-rules.ts. Landing in one cited listicle can flip dozens of AI answers at once, and this is the first surface telling the operator what to do OFF the site.
- [ ] 124. **Replace generic keyword difficulty with a proof-history win-pattern difficulty** (frontier, medium)
       Beacon uniquely owns the training data: every shipped change with its 7/14/28-day position outcome in the proof ledger. Fit a simple model from past wins (starting position, SERP domain composition from cached snapshots, topic cluster) to score each candidate 'sites like ours won queries like this N of M times', and expose it as the strategist's conviction input in src/domains/experiments/daily-experiment-planner.ts. This is the personalized difficulty seoClarity and MarketMuse sell, but grounded in our own verdicts.
- [ ] 125. **Ship a competitor AI share-of-voice matrix per topic cluster** (frontier, medium)
       Aggregate src/domains/serp/dataforseo-llm-mentions.ts runs plus the 22.8k Profound citations into a weekly per-topic-cluster matrix: Iranopedia vs the 8 real co-mention competitors, with trend arrows. Surface the worst cluster as a named teammate concern so losing share auto-generates a Move instead of just a chart. This is the hero chart Profound and Peec sell, built from data Beacon already stores.
- [ ] 126. **Add a position-churn sentinel that detects Google algorithm updates and protects verdicts** (Search Console depth, medium)
       Compute a daily mean absolute position delta across top queries from gsc_daily_rows; a churn spike across many unrelated pages means Google moved the field, not that a shipped change failed. Annotate the scoreboard and the diff-in-diff verdicts in proof-gsc/measure.ts with Google shifted rankings broadly that week so wins and losses are never misattributed.
- [ ] 127. **Build a demand-ranked question bank from GSC regex mining, PAA, and fanouts to feed FAQs** (Search Console depth, medium)
       dimensionFilterGroups is used nowhere: an includingRegex pull for question patterns in English and Persian (how, what, chetor, chist, plus Persian script) builds a property-wide question bank with real impression counts. Merge it with People Also Ask questions from the DataForSEO advanced SERP endpoint (the regular endpoint only flags PAA exists) and Profound fanouts, dedupe against snapshot faqs, and rank per page. Every FAQ then answers a question people provably ask, with a number attached.
- [ ] 128. **Build an intraday site-pulse collapse alarm from fresh GSC totals** (Search Console depth, medium)
       Compare today's dataState=all property impressions against the trailing same-weekday median by early afternoon Pacific. A 50 percent or worse collapse means deindexing, a robots.txt accident, or a hack, and Beacon should scream the same day instead of after the 3-day final lag in sync-search-analytics.ts. Pure protection value a hired team provides instinctively.
- [ ] 129. **Build the transliteration demand engine: canonical spellings, variant coverage, and fa volume** (Search Console depth, medium)
       Group the query universe by normalized transliteration with the existing Persian tokenizer (nowruz, norooz, norouz; koresh, cyrus), pick the canonical spelling per entity from GSC plus DataForSEO volume, align title, H1, and slug to it, and ship moves adding high-demand variants plus native-script names once near the top. Add a monthly language_code fa volume batch since every current call uses en, so Persian-script demand reads as null today. This quantifies and captures an entire demand lane the flagship tenant is blind to, and generalizes to any niche with spelling variants.
- [ ] 130. **Classify query intent deterministically plus Labs search_intent, and let the planner veto levers** (Search Console depth, medium)
       The chaharshanbe incident put 194 impressions on a 2026-dated query and Beacon surfaced a definition. Add a deterministic intent label (definition, question, comparison, date, local, navigational) to GscQuerySignal in gsc-page-signals.ts, with a date and year classifier routing to a freshness lever, and run DataForSEO Labs search_intent over each pick's target queries (1000 keywords per call for cents) so the planner vetoes answer-block levers whose type contradicts the classified intent. Cheaper and more auditable than the planned LLM adjudicator, and they compose.
- [ ] 131. **Fit the tenant's own CTR curve from its GSC data instead of the generic table** (Search Console depth, medium)
       src/domains/recommendation-intelligence/ctr-curve.ts is a hardcoded industry approximation (position 1 = 30 percent, verified). Fit position-bucketed median CTR from the tenant's own 90-day gsc_daily_rows with brand queries excluded, falling back to the generic curve on thin buckets. Every forecast range, striking-distance estimate, and outcome range instantly becomes more honest for this site's actual niche.
- [ ] 132. **Gate every measurement window on proof that Google actually recrawled the shipped page** (Search Console depth, medium)
       The loop verifies changes are live in Wix, then starts the 7/14/28-day diff-in-diff, but Google may not recrawl for days, so verdicts can measure the pre-change page. Poll gscUrlInspect until last_crawl_time exceeds shipped_at, stamp Google recrawled on X onto the experiment record, mark chips not recrawled yet, and start the effect window at recrawl. Without this, false no-effect learnings corrupt the priors the team learns from.
- [ ] 133. **Judge title and meta ships on their target queries and detect Google title rewrites** (Search Console depth, medium)
       proof-gsc/gsc-window.ts reads blended page totals, so brand and navigational queries dilute the CTR a title experiment is judged on; compute CTR on the pick's named queries from gsc_daily_rows instead. Also join the pushed title against the displayed title already in the DataForSEO SERP cache: when Google rewrote it, a flat verdict reads Google did not display our title instead of the title failed, keeping false negatives out of the learning ledger.
- [ ] 134. **Make decay verdicts year-over-year aware to separate seasonal dips from true decay** (Search Console depth, medium)
       triggers/gsc-decay.ts compares two consecutive 28-day windows, so every seasonal down-ramp fires a false decay signal. With the 16-month backfill, compare the current window against the same period last year and label outcomes seasonal, not decay versus true decay, preventing wasted refresh work on pages between holidays.
- [ ] 135. **Mine comparison intent across queries and prompts, then ship table-first comparison content** (Search Console depth, medium)
       Regex-pull vs, best, top N, and difference between queries from GSC, detect comparison demand in tracked prompts and fanouts (IntentType in tracked-prompts/types.ts already includes comparison), and check winner tables via competitor facts hasToolOrCalculator vs owned table_count 0. Ship grounded comparison tables as paste-ready blocks on existing pages and table-first templates through the create_page lane, since tables are the format both Google snippets and AI answers extract most readily.
- [ ] 136. **Pull the searchAppearance dimension so schema ships are judged on rich-result impressions** (Search Console depth, medium)
       No code touches searchAppearance. Add a weekly searchAppearance-grouped pull per page in sync-search-analytics.ts and join the delta into src/domains/proof-gsc/measure.ts, so a shipped FAQ or Recipe block gets a direct yes/no on whether it earned an enhanced appearance instead of inferring from blended CTR. This gives the whole schema program its own money metric.
- [ ] 137. **Run a budgeted URL-inspection sweep for indexation rate, recrawl age, and crawl tiers** (Search Console depth, medium)
       gscUrlInspect in src/lib/connectors/gsc/client.ts is on-demand with a 24h cache while the API allows ~2000 inspections per day. Sweep worklist, shipped, and sitemap URLs on a nightly/weekly budget into a time-series table: per-section indexation rate and median recrawl age from coverage_state and last_crawl_time, alarms when a page flips out of the index, and crawl-frequency tiers that route internal links from hot pages to money pages awaiting recrawl. Clusters of Discovered currently not indexed are the crawl-budget signal for a Wix site with no server logs.
- [ ] 138. **Split brand vs non-brand queries and lead the scoreboard with non-brand growth** (Search Console depth, medium)
       No brand classification exists anywhere in the GSC path. A small per-tenant brand regex (iranopedia plus misspellings) splits every aggregate; non-brand clicks become the honest growth KPI on Today and the weekly recap, and brand queries get excluded from gsc-low-ctr.ts triggers where navigational CTR inflates averages and hides real snippet problems.
- [ ] 139. **Add a query-intent dimension to priors so buckets actually accumulate samples** (Learning loop, medium)
       experiment-prior.ts keys its most specific dimension on the exact query string (queryClusterKey), which will almost never reach MIN_DECIDED=3 on a real site, so the ladder degenerates to actionType. Derive an intent bucket (definition, date, list, how-to, comparison, local) from src/domains/demand-graph/query-intent.ts and src/domains/experiments/answer-intent.ts, add it to SettledOutcome dims in load-experiment-outcomes.ts, and map live Moves the same way. This is the page-type times lever times intent depth the prior was designed for but cannot reach today.
- [ ] 140. **Learn a per-site CTR curve from the tenant's own GSC position data** (Learning loop, medium)
       The industry CTR_CURVE constant is duplicated in daily-experiment-planner.ts and pick-expectations.ts and is wrong for a Persian-culture encyclopedia whose SERPs are full of AI overviews and zero-click answers. Fit a monotonic position-to-CTR curve from the tenant's own GSC query rows (thousands of position and CTR points already synced), blend with the industry curve when data is thin, and export one shared expectedCtrAt. Every opportunity score, forecast, and planner decision inherits the correction at once, and the duplication dies.
- [ ] 141. **Learn from effect sizes, not just binary won/lost verdicts** (Learning loop, medium)
       measure.ts already computes adjustedLift, adjustedCtrLift, and adjustedPosLift per window on every ShippedChangeRecord, but load-experiment-outcomes.ts reduces all of it to won or lost. Carry the mature window's magnitude into SettledOutcome and compute a median realized lift per lever-times-pageType cell alongside the win rate. A lever that wins small 4 times should rank below one that wins big 3 times, and the realized-lift table becomes the honest engine behind every forecast.
- [ ] 142. **Replace the MIN_DECIDED cliff with Beta-posterior shrinkage toward the parent dimension** (Learning loop, medium)
       computeDimPriors in src/domains/learning/experiment-prior.ts throws away everything under 3 settled outcomes (verified at line 150) and trusts a 3-sample 100 percent win rate as much as a 20-sample one. Model each cell as Beta(won+1, lost+1), shrink toward the parent dimension (queryIntent to pageType to actionType to global) weighted by sample size, and derive the multiplier from the posterior mean. Two settled wins then nudge ranking a little instead of nothing, 3 lucky wins stop reading as certainty, and the tag can honestly say wide or tight evidence.
- [ ] 143. **Cluster retrieval grounding for create-page briefs to prevent draft-level cannibalization** (LLM engine, medium)
       draftCreatePageStructured sees competitor pages and fanout queries but nothing about the tenant's own existing pages, so a brief can propose a page that collides with one Iranopedia already ranks with. Retrieve the top related owned pages via the match-engine similarity module plus GSC signals and pass them as existing coverage: differentiate or link, do not duplicate, and require internal_links to point at real cluster members. The duplicate-topic guard exists at trigger level only, not at draft level where the words are chosen.
- [ ] 144. **Learn a per-tenant brand-voice style card and inject it into every system prompt** (LLM engine, medium)
       Every system prompt in structured-drafter.ts and llm-answer-block.ts hardcodes the persona for an encyclopedia content site, and draft-quality.ts hardcodes DEFAULT_CONTEXT_TOKENS to Iranopedia terms. Run a one-time then monthly LLM summarization over page_snapshots body samples to derive a style card per tenant: sentence length, person, formality, vocabulary, do-not-say list. Persist it on business config and prepend it to every drafter call so a second tenant gets its own voice on day one instead of Iranopedia's.
- [ ] 145. **Loss-aware redraft loop: a no-lift verdict auto-queues a different-angle rewrite** (LLM engine, medium)
       When the 28-day verdict on an LLM-drafted change lands as no lift, nothing today reuses that knowledge. Add a pass in the auto-measure cron that, for settled losers with draftSource llm, calls draftAtomicEditStructured with the losing text plus its measured result as explicit negative context (previous meta X did not move CTR, take a different angle) and files the redraft as a new candidate. The losing draft becomes negative few-shot and pages get a second scientifically informed shot instead of going dormant.
- [ ] 146. **Route model and reasoning effort by task difficulty and execution context** (LLM engine, medium)
       providers/openai.ts already prices gpt-5-nano (12x cheaper) and gpt-5.4-mini (3x pricier) in COST_PER_MILLION, yet every call site hardcodes MODEL = gpt-5-mini (verified in structured-drafter.ts line 35) and pins reasoning_effort low. Add a routing table in src/lib/llm: team verdicts and one-line rationales to nano, answer blocks and atomic edits stay on mini, create_page briefs and the page-surgeon battle plan to 5.4-mini; interactive paths keep effort low while cron and overnight paths get medium or high, where 60 extra seconds is free. The same cap buys roughly 5x more cheap calls and meaningfully smarter output where a whole new page hangs on one brief.
- [ ] 147. **Snapshot-entailment fact check on answer blocks before they ship** (LLM engine, medium)
       Page Surgeon got a fact-check pass (WL10) but the daily answer blocks from draftAnswerBlockStructured ship with only the numeric firewall, which cannot catch a wrong non-numeric claim like an incorrect dynasty or city. Add a check that each factual sentence has token-overlap support in the page_snapshots body, escalating ambiguous sentences to a gpt-5-nano entailment call. Unsupported sentences downgrade the draft to useful_but_needs_review in draft-quality.ts terms rather than ready.
- [ ] 148. **Version every prompt, stamp drafts, and score win and edit rates per version** (LLM engine, medium)
       About ten SYSTEM prompt constants live inline across structured-drafter.ts, llm-judge.ts, adjudicate.ts and llm-expert-strategist.ts with no version identity, so a prompt tweak silently invalidates comparisons between old and new measured wins. Move them into src/domains/llm/prompts.ts with id, semver, and a one-line changelog, stamp promptVersion and model into serializeStructuredDraft and onto the proof ledger row at ship time, then join move_drafts to verdicts and surface a small table on /settings/spend: answer blocks v3 won 4 of 6, meta rewrites v2 won 1 of 5 with the operator edit rate beside it. Prompt engineering becomes the same measured loop the product sells.
- [ ] 149. **Block concurrent experiments that compete for the same queries** (Measurement science, medium)
       experiment-eligibility.ts guards page-level contamination but two simultaneously treated pages targeting the same query cluster interfere with each other; one's win is the other's loss. Build a query-collision check from each candidate's target queries against the live ledger's targetQueries and add an interference_conflict exclusion reason to assessEligibility. The planner already has the ExcludedReason plumbing to surface it.
- [ ] 150. **Fixed-query-panel position tracking to kill Simpson's paradox rank reads** (Measurement science, medium)
       Position verdicts use the impressions-weighted average across the whole page, so gaining long-tail queries makes rank appear to slip and losing them makes it improve with no real movement. Freeze the panel at record time (the pre-period top 10 queries from gsc_daily_rows) and compute position deltas only on that panel in run-measurement.ts. measure.ts already documents this exact failure mode for coverage plays; the panel fixes it for every position verdict.
- [ ] 151. **Judge coverage plays on distinct-query growth and feed the numbers into forecasts** (Measurement science, medium)
       The wonOnImpressions path in summarizeVerdict can be satisfied by uniform impression inflation, but the true goal of a section-add or new-content play is ranking for new queries. Count distinct queries with impressions pre versus post from gsc_daily_rows (control-adjusted) and make that the primary metric for the content and new_page families in pickProofMetric, storing queries-gained per family. New Pages forecasts can then say similar pages here started ranking for about 14 new searches within a month, and the operator sentence (this page now shows up for 23 more searches) is far better than raw impressions.
- [ ] 152. **Replace pro-rated pre windows with day-of-week matched baselines** (Measurement science, medium)
       computeWindowLift in src/domains/proof-gsc/measure.ts pro-rates a 28d pre-click sum to a 7d post window (line 228), so a post window with 2 weekends is compared to a baseline with 1.14 weekends per week. Read the per-day series from gsc_daily_page_totals (loadDailyClicksByPagesForTenant already exists in src/domains/recommendation-intelligence/gsc-page-queries.ts) and build the baseline as the mean of the same weekdays in the pre period. This kills the largest systematic bias in every 7-day early checkpoint.
- [ ] 153. **Start the measurement clock at reindex, not at publish** (Measurement science, medium)
       Measurement windows anchor on ship date, but the treatment only exists once Google recrawls; for slow-crawled pages days of pure baseline get counted as treated, diluting every lift toward zero. src/domains/attribution/align-treatment-dates.ts already solves this for citations; port the pattern using recrawlRequestedAt plus the GSC URL inspection API to set an effectiveAt on the record and offset proofCheckDates from it. Crawl lag on a small Wix site is exactly where this bites hardest.
- [ ] 154. **Auto-discover competitors from live SERP instead of a blank text form** (Onboarding and cold start, medium)
       The competitors step (src/app/(shell)/onboard/competitors/) asks the user to type 1 to 5 names cold. Run 3 to 5 cached runSerpQuery calls (src/domains/serp/dataforseo-serp.ts, $0.003 each) on derived services crossed with locations, count repeating top-10 domains, filter directories with the existing noise-domain rule, and present survivors as one-click chips. Most owners cannot name their search competitors; Google can.
- [ ] 155. **Capture a day-0 AI-answer baseline: run starter prompts live plus LLM mentions** (Onboarding and cold start, medium)
       generateStarterPrompts (src/domains/onboarding/prompt-generator.ts) writes tracked_prompts at launch but nothing answers them until a nightly run. Execute the top 3 to 5 prompts once through the budget-gated LLM gateway at launch and fire runLlmMentions (src/domains/serp/dataforseo-llm-mentions.ts, $0.03) for the top derived topic, persisting both as the dated AEO before photo. ChatGPT recommends three competitors and you are absent is the emotional hook of minute one, and every later citation win points at this baseline.
- [ ] 156. **Land Launch on a guided first-win page with a pre-prepared ship staged** (Onboarding and cold start, medium)
       executeLaunchTransaction redirects to / and drops the stranger into the full war room cold (src/app/(shell)/onboard/review/launch-flow.ts). Add /onboard/first-win that walks three moves: optionally connect Google, review the single highest-confidence crawl-evidence fix with its Prepare draft already run, ship it, come back tomorrow for measurement. Record the signup-to-first-ship interval so time-to-first-win under 24h becomes an instrumented number.
- [ ] 157. **Make signup URL-first with everything else derived and confirmed** (Onboarding and cold start, medium)
       The wizard asks for business name first and only fetches the homepage after save (src/app/(shell)/onboard/business/actions.ts). Flip the order: collect only the website URL, run fetchSiteProfilePages plus deriveBusinessProfile immediately, and turn the business, scope, and competitors steps into confirm-what-we-found chips. Cuts typed fields from roughly ten to one and delivers the paste-a-URL-get-a-team promise.
- [ ] 158. **Show a live first-audit scorecard on the review step before Launch** (Onboarding and cold start, medium)
       The review page (src/app/(shell)/onboard/review/page.tsx) previews prompts but shows nothing Beacon found on the site. Render a scorecard from extractPageSnapshot fields: missing meta descriptions, duplicate or truncated titles, absent schema, thin pages, with counts and the top 3 concrete fixes. Six named problems on their own site before launch is the strongest conversion moment in the product.
- [ ] 159. **Collapse the /moves double pipeline into one change-pack build per request** (Performance, medium)
       loadUncached in src/app/(shell)/moves/moves-data.ts runs loadActionPackWorklistForTenant AND loadTodayMovesHeroData in parallel; both independently load the demand graph, change packs, drafts, and proof ledger, then results are joined by URL. Make the hero enrichment consume the ActionPack worklist's packets instead of re-loading its own, so the whole surface computes each heavy input exactly once. The biggest single lever on the residual warm-rebuild cost behind the worklist snapshot.
- [ ] 160. **Move graph-build aggregation into SQL: profound citations and GSC page signals RPCs** (Performance, medium)
       src/domains/demand-graph/competitor-citations-loader.ts pages 22.8k profound_citations rows into Node just to group by domain and URL, and loadGscPageSignalsForTenant (src/domains/recommendation-intelligence/gsc-page-signals.ts) loops serial 1000-row pages twice per uncached graph build. Add two Postgres RPCs: GROUP BY url and root_domain with SUM(citation_count) returning a few hundred rows, and a per-page 90-day aggregate following the gsc_page_totals precedent. Together they cut the largest chunks of the ~6s graph build and pure egress waste.
- [ ] 161. **Audit vetoes as paper trades and tune per-kind downgrade multipliers from the results** (Team intelligence, medium)
       When a cant_outrank_serp or already_ranks veto fires in move-router.ts, or the planner excludes a candidate, the page vanishes with no follow-up, so a systematically wrong rule is never caught. Persist excluded candidates on the plan record (the excluded ledger already exists in DailyExperimentPlan), watch their free GSC drift for 28 days with gsc-window.ts, and score each ObjectionKind's precision: vetoed pages that rise anyway mean the rule is too aggressive. Then replace the flat 0.75 confidence and 0.85 score cuts with per-kind multipliers tuned within tight bounds, with a receipt shown when one changes. Objections earn their teeth from evidence at zero API cost.
- [ ] 162. **Cross-check connector numbers and pause debates on contradictory data** (Team intelligence, medium)
       When GSC position and the cached DataForSEO SERP rank for the same query diverge by more than 5 spots, or Clarity records sessions while GA4 shows zero rows for the page, the problem is data quality, not strategy. Emit a data-integrity tension that routes the pick to a recheck-data state instead of letting teammates debate on bad numbers. Different from the built opinion-tension display: this catches broken inputs.
- [ ] 163. **Enforce per-action quorum rules before a pick can ship** (Team intelligence, medium)
       Define minimum voice quorums in the router: create_page requires the dataforseo voice present plus one demand voice, answer_block requires profound, fix_ux requires clarity. A pick failing quorum routes to a needs-evidence state instead of shipping on one voice, with the quorum line shown on the card. Implement in routeMove and team-review.ts where the veto plumbing already exists.
- [ ] 164. **Give vetoes an expiry and auto re-adjudicate when evidence refreshes** (Team intelligence, medium)
       A cant_outrank_serp veto currently kills a candidate with no path back. Store the veto with a staleAt derived from CADENCE_MS (14 days for dataforseo in specialist-opinions.ts) and re-run the debate on expiry; when the verdict flips, resurface the pick with a plain line that the search results changed and this is back on the table. Turns dead ends into a self-refreshing pipeline.
- [ ] 165. **Have the team vet diff-in-diff controls and score control-page reliability** (Team intelligence, medium)
       SuggestedControl picks in src/domains/experiments/build-daily-candidates.ts are chosen by similarity math alone, and a control with wild variance silently degrades every verdict it anchors. Run a light specialist pass on each proposed control (Clarity flags a friction spike, GSC flags a demand trend break, proof-history flags prior treatment residue) and swap contaminated controls before acceptance. Also track each control page's historical delta variance and cross-experiment disagreement into a persisted reliability score the control picker prefers. Verdict quality gets protected at selection time instead of discovered at measurement time.
- [ ] 166. **Reserve an exploration slot per batch, growing into Thompson sampling over lever families** (Team intelligence, medium)
       planDailyExperiments is pure exploitation with diversification caps; a lever-times-pageFamily cell that has never been tried only gets picked if it happens to score high. Phase one: reserve 1 configurable slot for the highest-opportunity candidate from a cell with zero settled outcomes, labeled honestly on the card as a learning pick. Phase two: turn the won, flat, lost tallies aggregateSettled already computes in proof-history-voice.ts into a Thompson-sampled prior that biases the planner toward uncertain-but-promising levers and away from proven-flat ones. Deliberate exploration fills the prior table in months instead of years.
- [ ] 167. **Run a second deliberation round where specialists react to each other** (Team intelligence, medium)
       attachOpinions in specialist-opinions.ts is one-shot; opinions never see each other. Add round two: each emitter receives round-one opinions and may adjust confidence or add conditional objections, for example Clarity's fix_ux_first only bites when GSC shows real demand, and Profound's create_page suggestion self-downgrades when DataForSEO abstained. Deterministic rules per emitter, capped at one extra round so it stays pure and fast.
- [ ] 168. **Add a performance lane: CrUX field data plus PSI lab audits mapped to Wix-actionable fixes** (Technical SEO, medium)
       Beacon has zero performance data: no LCP, INP, or CLS anywhere in src. Add a nightly free CrUX API pull for the origin plus top 20 GSC-demand URLs with daily p75 values and pass/fail bands on Today, and run the free PSI API against worklist pages, translating each Lighthouse diagnostic through a Wix-specific playbook (compress via Wix media settings, remove third-party embeds) into detect-findings.ts. Wix sites routinely fail INP; a failing CWV band is a ranking headwind the roundtable currently cannot see or argue about, and the Wix mapping layer is the product.
- [ ] 169. **Add a Beacon-touched-pages toggle to the scoreboard hero** (Today dashboard, medium)
       The hero chart shows whole-site clicks; add a second series scoped to only the paths in the shipped-change ledger using loadDailyClicksByPagesForTenant from src/domains/recommendation-intelligence/gsc-page-queries.ts. A small client toggle in scoreboard-section.tsx switches between all traffic and the part you are paying us to move. Isolates Beacon's book of work from seasonal noise in one tap.
- [ ] 170. **Add an annotation layer to the scoreboard: Google updates, seasonal events, operator notes** (Today dashboard, medium)
       Three marker sources on one chart infrastructure in scoreboard-section.tsx: a small versioned list of confirmed Google update rollout dates (new src/domains/scoreboard/google-updates.ts), a per-tenant cultural events file (Nowruz, Yalda, Chaharshanbe Suri) feeding the AreaEvent annotation type that already exists in components/viz/area-chart.tsx, and a tiny operator journal store for notes like sent the newsletter. When traffic moves, the chart itself explains whether it was the weather, the season, or the owner's own actions, and the same dates feed measurement as known confounders later.
- [ ] 171. **Build a site-level traffic smoke alarm with page-level blame in the attention band** (Today dashboard, medium)
       Add a pure anomaly check on loadDailyTotalsForTenant output: yesterday below the trailing same-weekday average by a hard threshold, or three straight abnormal down days, fires a red TodayAttentionItem (src/domains/changes/today-view.ts) naming the 2-3 pages that lost the most via loadDailyClicksByPagesForTenant. The detectSpikes collapse mode in src/domains/trend-radar/spike-detector.ts already has the math shape. Today currently only celebrates; a command center must sound the alarm before the owner hears it from a customer.
- [ ] 172. **Flag early negative drift on measuring changes before the formal checkpoint** (Today dashboard, medium)
       For each measuring ledger row, compare post-ship daily clicks (daily-series.ts in src/domains/proof-gsc) against the pre-ship baseline; when a change trends clearly down mid-window, add an early-warning chip to MeasuringSection in src/app/(shell)/page.tsx: early read looks down 12 percent, confirming at day 14, rollback text ready. Waiting silently for day 7 is scientifically right but feels negligent; a great team says we are watching it.
- [ ] 173. **Append verdict revision history and say plainly when an early read was wrong** (Trust and receipts, medium)
       When run-measurement.ts flips a verdict between the 7, 14, and 28 day windows, append {date, from, to, reason} to the ShippedChangeRecord instead of silently overwriting. The card then reads: our 7-day read said likely helped, the 28-day data said otherwise, updated Jun 24. Never rewriting history silently is the cheapest and most durable honesty feature Beacon can ship.
- [ ] 174. **Build a why-is-this-page-not-in-the-plan inspector** (Trust and receipts, medium)
       experiment-eligibility.ts already evaluates gates with reasons and outcome-prior.ts computes the learned prior. Add an operator search box that takes any path and prints each gate pass or fail in plain words plus the prior nudging it up or down. Makes prioritization reproducible and kills the single most corrosive doubt an owner has: why is Beacon ignoring my page.
- [ ] 175. **Disclose when a comparison page was itself changed mid-window and drop it** (Trust and receipts, medium)
       ControlReservationRecord in daily-plan-types.ts already has an invalidated status. Cross-check control paths against the shipped-change ledger and per-page change history at measure time; when a control was touched, drop it and render one comparison page changed on day 9, so we removed it, 2 remain. Contaminated controls are the classic diff-in-diff failure and Beacon should catch it out loud.
- [ ] 176. **Name the comparison pages on every proof card and draw their band under the treated line** (Trust and receipts, medium)
       Cards currently say compared against N similar pages you did not change, without naming them and without showing them on the chart. List the controlPages from ShippedChangeRecord, each linked with its own mini sparkline, plus one line on how they were chosen from the natural-controls.ts selection rule; then extend src/components/data/sparkline.tsx with an optional band series fed by loadDailyClicksByPathsForTenant in src/domains/proof-gsc/daily-series.ts so a win becomes a visible gap between your line and the shaded band of untouched pages. Hidden controls read like a magic trick; named, plotted controls read like science.
- [ ] 177. **Persist and show a nightly run receipt: candidates, per-gate rejections, near misses, cost** (Trust and receipts, medium)
       build-daily-candidates.ts already enumerates candidates and gates; persist a small receipt per nightly run with candidate count, a per-gate rejection histogram (draft-quality.ts, relevance-gate.ts, budget caps), the LLM plus DataForSEO cost, and the top 5-10 near-miss candidates with the exact gate that stopped each. Render one line on Today near the standup: last night we considered 214 options, picked 3, rejected 120 for weak evidence, finished at 4:12am, with a plain amber line when the run failed, plus a before-run countdown line on the DailyExperimentsSection header. An autonomous team the owner cannot see working is indistinguishable from one that is not working, and a team that shows what it declined earns more trust than one that only shows winners.
- [ ] 178. **Run a placebo check on every mature win and say it in one sentence** (Trust and receipts, medium)
       src/domains/attribution/placebo-inference.ts exists but the proof-gsc ledger never uses it. On a mature won verdict, run the same window math over a set of untouched pages and render only 2 of 20 pages we did not touch moved this much, so luck is unlikely, on the win card in proof/page.tsx. Upgrades a strong directional read from a disclaimer into evidence.
- [ ] 179. **Apply multi-field change bundles atomically with compensating rollback** (CMS push depth, medium)
       A Page Surgeon Change Pack (change-pack.ts) can want title plus meta plus body on one page, but executePush lands one field at a time; a mid-bundle Wix failure leaves the page half-changed with no automatic cleanup. Build a bundle executor in src/domains/push that pre-captures snapshots for every field (push-snapshots.ts), applies writes in order, and on failure reverts already-applied fields via buildRevertEdit before reporting. One reservation, one receipt, no torn pages.
- [ ] 180. **Detect Google rewriting pushed titles and propose a compliant alternative** (CMS push depth, medium)
       Beacon pushes a title, but Google rewrites roughly a third of titles, so the measured CTR change may not reflect the pushed copy at all. After recrawl, compare the pushed title against the SERP-displayed title from the existing DataForSEO SERP cache; when they diverge, flag Google rewrote this on the proof row and generate a shorter query-aligned alternative through the existing title lab. No competitor tool closes this loop automatically.
- [ ] 181. **Ground-truth field-role mapping by matching live page values to item fields** (CMS push depth, medium)
       suggest-mapping.ts only matches field names against synonyms and leaves roles unset, so live content push stays off until the operator hand-maps on /diagnostics/wix. Add a verifier that fetches 3 sampled live pages per collection, parses title, h1, and meta with the cheerio logic from live-verification.ts, and matches values against item fields to auto-fill contentFieldRoles with per-role confidence. Publishing becomes live minutes after connecting instead of after a manual mapping session.
- [ ] 182. **Rebuild the Wix Blog draft-post adapter and wire create_post into the push queue** (CMS push depth, medium)
       client.ts notes the blog handlers (POST /blog/v3/draft-posts, publish endpoint) were deleted 2026-07-01 as unwired dead code, so Beacon can only create pages inside CMS collections the site already has. Rebuild wixCreateDraftPost and wixPublishDraftPost, add a create_post route to executePush, and let the New Pages board target the blog when no collection fits. Drafts land in the Wix drafts panel the operator already trusts, then publish via the capped path after approval.
- [ ] 183. **Start the measurement clock at Google's recrawl using the GSC URL Inspection API** (CMS push depth, medium)
       The 7/14/28-day diff-in-diff starts at the Wix write, but Google may not recrawl for days, diluting the early window with pre-change impressions. Beacon already holds GSC OAuth; call URL Inspection after verified_live, persist lastCrawlTime, and shift the treatment date the proof engine uses to the first post-change crawl. Every verdict gets cleaner and faster to significance.
- [ ] 184. **Add live guardrails to the paste edit box: SERP preview, length meter, and quality validation** (Worklist UX, medium)
       The ExecutionCard textarea (daily-experiments-section.tsx) lets the operator rewrite a title or meta with zero checks before applying, though banned dashes, length limits, topic fit, and numeric fidelity are all enforced elsewhere. Render a live Google-result mock under it (title in blue, URL, meta in gray) with per-lever character and pixel-width truncation warnings, plus a debounced pass through stripBannedDashes detection and a lightweight reviewRecommendation server call showing this-would-fail inline. Catches the mistake while the cursor is still in the box, not after a failed verification cycle or a Google-truncated title that destroys the CTR upside the pick was chosen for.
- [ ] 185. **Add per-pick approve toggles plus one-click swap from the reserve to the daily plan preview** (Worklist UX, medium)
       In src/app/(shell)/daily-experiments-section.tsx the Approve button accepts every selected preview item; the only alternative is rebuilding the whole plan, and DailyExperimentPlanRecord already persists backups (src/domains/experiments/daily-plan-types.ts line 112) that render as a dead text line. Add an include/exclude checkbox per PreviewCard passed into acceptDailyExperimentPlanAction so projectReservations only reserves controls for kept items, and render each backup as a mini card with a Replace button that swaps it into selected and re-persists the preview. Dropping the one distrusted pick or swapping it becomes a 2-second act instead of a full replan.
- [ ] 186. **Batch verify: one button to confirm every applied change is live** (Worklist UX, medium)
       After pasting five changes in Wix, the operator must click I did it in Wix five times, each a full round trip. Add I did them all, run me through it: a server action that calls verifyExperimentLive for every ready_to_apply item concurrently, activates the passing ones through the existing activateItemViaRpc path, and returns a per-item pass/fail table. daily-experiments-actions.ts already has all the pieces; this is composition plus one results panel.
- [ ] 187. **Handle failed live verification end to end: loud status, auto-recheck loop, stranded-state reset** (Worklist UX, medium)
       Wix publish lag makes verifyExperimentLive fail even when the operator did the edit; today the red box in ExecutionCard just says try again, failures elsewhere are quiet despite live-verification.ts returning typed retryable reasons, and markFailed admits items can strand in verification_pending with only a console.error. Add a Keep checking option that re-runs the verify-only path every few minutes for up to an hour and flips the card green; render honest status on the worklist and Today (we checked 3 times, Wix still shows the old title, next retry tonight); and in buildExecutionChecklist flag any verification_pending item older than 5 minutes with a Reset and try again button via updateItemExecution. Kills the most annoying babysitting loop and a silent stuck state the operator cannot escape without database surgery.
- [ ] 188. **Let Suggest different ones take a reason so the replan actually differs** (Worklist UX, medium)
       The button in DailyExperimentsSection calls planTodayExperimentsAction blind; the same inputs often reproduce nearly the same plan. Add quick reason chips before replanning (not these pages, too many metas, bigger swings) that pass exclusion and distribution hints into buildTodayExperimentPreview (src/domains/experiments/build-today-preview.ts). The rejection becomes a steering wheel, and the hints are exactly the operator-preference signal the learning loop wants.
- [ ] 189. **Make batch-level actions reversible: cancel an accepted plan, reopen a completed one** (Worklist UX, medium)
       abandonPreviewPlanAction explicitly refuses accepted plans (the release RPC is deferred, daily-experiments-actions.ts line 153), so regretting an Accept means skipping items one by one, and Finish for today has no undo. Build the atomic release RPC (release all reserved non-active reservations plus plan back to abandoned) and a reopen action for completed plans, with a Cancel today's batch button behind a confirm. Undo at the batch level is the difference between a safe work OS and a one-way conveyor.
- [ ] 190. **Add a statistic-injection lever backed by the GEO evidence** (AI visibility (AEO), small)
       The answer-block trigger's own sourced digest (triggers/answer-block-readiness.ts) cites the KDD 2024 GEO study where adding statistics was among the strongest generative-visibility levers, yet no lever systematically adds stats. Mine owned data for one unique, sourced number per target page (dates, counts, name popularity) and inject it into the answer block with attribution as a third safe lever in the daily cycle, measured via the citation lane and fact fingerprinting.
- [ ] 191. **Auto-expand the tracked prompt roster from GSC questions and fanout orphans** (AI visibility (AEO), small)
       The 100-prompt roster is manually curated while GSC has thousands of question-shaped queries (search-query-parser.ts can classify them) and fanout rows surface sub-queries no tracked prompt covers. Score candidates by impressions and fanout weight, register winners via the already-built createProfoundPrompts, and add them to the native poll roster with a budget-aware cap. The engine currently measures only what a human remembered to track.
- [ ] 192. **Run a live AI-crawler access preflight: real bot UAs, robots and meta audit, WAF 403s** (AI visibility (AEO), small)
       triggers/robots-blocks-ai-bots.ts audits robots.txt only, and AI_CRAWLERS in robots-parser.ts stops at 7 bots, missing OAI-SearchBot (the crawler behind ChatGPT search citations), Perplexity-User, Meta-ExternalAgent, and DuckAssistBot. Refresh the list, live-fetch top cited-or-target pages with GPTBot, ClaudeBot, and PerplexityBot user agents to catch firewall or Wix bot-management 403s robots.txt never shows, and check meta robots plus accidental noindex on the highest-demand pages. A page that passes robots but 403s GPTBot is invisible to ChatGPT no matter how good the content is.
- [ ] 193. **Ship a dedicated zero-source lane: become the canonical source where AI cites nobody** (AI visibility (AEO), small)
       prompt-opportunity.ts already classifies source_gap prompts where engines answer with zero citations. Give them a dedicated lane: draft a definitive, stat-rich page per zero-source prompt through the existing create_page path, then track time-to-first-citation via compute-time-to-citation.ts as the lane's headline metric. The cheapest citations on the board because there is no incumbent to displace.
- [ ] 194. **Add a dead man's switch banner when no cron has succeeded in 36 hours** (Autonomy, small)
       measure-due silently returns 503 forever if CRON_SECRET is unset, and a vercel.json edit or plan change can stop crons with zero symptoms. Check the cron_runs ledger on shell render and show one banner plus a system fix card when the last successful nightly pass is older than 36 hours. Catches the whole class of automation that dies quietly.
- [ ] 195. **Classify GSC ingestion gaps before any alarm or investigation fires** (Autonomy, small)
       A site-wide impressions crater is usually a GSC data delay, not a real collapse; the sync already tracks is_final per day. Gate the spike detector, war-room decline rows, and the investigation trigger on finalized data, and label partial days as data still arriving instead of alarming. One false your-traffic-collapsed from a data artifact costs more trust than ten real alerts earn.
- [ ] 196. **Add a win-decay watchdog that opens defense moves when shipped wins regress** (frontier, small)
       Nightly, re-check GSC position and clicks for every proof-ledger win against its post-verdict level, and when a win gives back more than half its gain, auto-open a defense card with the diagnosis started (SERP re-fetch, competitor page diff via src/domains/competitor-intel/structural-diff.ts). Extends the spike detector in src/domains/trend-radar/spike-detector.ts but scoped to wins with an automatic response, so victories compound instead of quietly eroding.
- [ ] 197. **Add Bing Webmaster and IndexNow because ChatGPT search reads the Bing index** (frontier, small)
       Every connector in src/lib/connectors is Google-shaped, yet ChatGPT browsing and Copilot pull from Bing. Add a free Bing Webmaster Tools connector for index and query data plus an IndexNow ping in the push-receipt path after every verified ship. For AEO, being indexed in Bing within hours directly affects whether ChatGPT can cite the page at all.
- [ ] 198. **Auto-revert changes the 14-day verdict calls a regression** (frontier, small)
       src/domains/attribution/url-verdict.ts already classifies regressions and the push layer stores prior field values for rollback copy. Close the loop: on a confirmed regression verdict, restore the prior value through the existing Wix field-merge client, log the revert in the proof ledger as a lesson line, and feed the outcome to the shared brain. Safe-to-fail experimentation is only real when the undo is automatic, and it is the strongest trust story a paying stranger can hear.
- [ ] 199. **Persist the full donor pool at ship time and auto-repair lost controls** (Measurement science, small)
       auto-record-on-ship.ts fetches 12 control candidates but stores only 3; when one control later gets treated or vanishes, run-measurement.ts drops it and the verdict can fall below MIN_CONTROLS and void. Store all 12 candidates on ShippedChangeRecord in shipped-change-store.ts and have measureRecord promote the next clean candidate when a control is excluded. Measurements stop dying from control attrition, which will happen constantly as the daily cycle treats more pages.
- [ ] 200. **Use median control delta plus a dispersion band, not a bare mean** (Measurement science, small)
       mean() over 2 or 3 control deltas in computeWindowLift is destroyed by a single outlier control, for example one page catching a snippet drags the whole baseline. Switch to median (or trimmed mean at larger pools) and report the control spread; any treated lift inside the control spread is inconclusive by construction. A two-hour change in measure.ts with outsized honesty payoff.
- [ ] 201. **Mirror the demand-graph snapshot to json_store_blobs so cold lambdas skip the rebuild** (Performance, small)
       SUPABASE_MIRRORED_STORES in src/lib/persistence/json-store.ts lists worklist-surface and today-surface but not demand-graph-snapshot, so on Vercel the graph SWR cache in src/domains/demand-graph/graph-snapshot-store.ts lives only in each lambda's memory. Every cold lambda that needs the raw graph (New Pages board, drafts, page-factory, prepare actions) pays the full 6 to 8 second rebuild. One string added to the mirror set makes the ~1.5MB Iranopedia graph hydrate in one blob read across all instances.
- [ ] 202. **Emit the dormant thin_evidence objection from a deterministic evidence auditor** (Team intelligence, small)
       ObjectionKind thin_evidence is declared in specialist-opinions.ts and labeled in debate-summary.ts but nothing ever emits it. Add an auditor pass over the EvidencePacket that fires it when the packet is internally weak: competitor relevance under 0.6 still shaping gaps, basis ai_attention on a create_page, zero fanoutSeeds, or rows older than the emitting specialist's cadence. The team gains a working skeptic for almost no code.
- [ ] 203. **Persist the full router decision, not just the render-ready summary** (Team intelligence, small)
       TeamReviewResult in src/domains/experiments/team-review.ts throws away the MoveRouterDecision internals: the votes map, margins, applied versus dissenting sets, and staleAt. Persist a compact decision record alongside the TeamReview on the plan so learning loops, calibration, replay audits, and challenger-policy comparisons have raw material. Without this, every outcome-learning idea starves.
- [ ] 204. **Expose the diff-in-diff math behind every verdict in a See-the-math table** (Trust and receipts, small)
       Every field needed already exists on WindowReading in src/domains/proof-gsc/measure.ts: treatedDelta, controlDelta, adjusted lift per metric, controlsUsed. Add a details block on LedgerCard in proof/page.tsx that renders treated pre/post, control mean pre/post, the subtraction, and the exact threshold the verdict crossed. A verdict the operator can hand-check is a verdict they will defend to others.
- [ ] 205. **Show a live before/after preview-diff on the card before any publish click** (CMS push depth, small)
       executePush dry_run already resolves the exact item and field but returns only a summary string. Extend the dry_run result to carry the live field value plus proposed text, and render a word-level diff on the worklist MoveCard and rec detail before Approve and Push. The operator sees exactly what changes on the live site, catching stale current_text before the write instead of at the destructive-guard refusal.
- [ ] 206. **Show a word-level before/after diff on every paste card and changes row** (Worklist UX, small)
       ExecutionCard shows only the Paste this box, and the changes Row detail shows Current and Proposed as two separate lines (changes-list-client.tsx lines 137-138), forcing the operator to eyeball what changed in a 150-char meta. Add a pure diff-words helper (new src/lib/diff-words.ts, about 50 LOC LCS) and render deleted words struck and added words highlighted in both daily-experiments-section.tsx and the Row detail. Diff-reading is the fastest possible review.
- [ ] 207. **Mine which page features win citations per topic and feed learned weights to the team** (AI visibility (AEO), large)
       Join profound_citation_rows winners and losers to competitor page structure facts (competitor-page-audit.ts, competitor-page-snapshots.ts): word count, tables, lists, schema types, freshness dates, answer-first layout. Fit simple per-topic weights for cited vs uncited and publish them into specialist-opinions.ts so the AEO teammate argues from locally learned evidence (cited pages here average 2 tables and a dated stat) instead of generic GEO-study priors. This is the compounding moat: every tenant's data sharpens the playbook.
- [ ] 208. **Create an Ops teammate that files and auto-resolves its own health cards** (Autonomy, large)
       Add a 10th system voice to src/domains/team/identity.ts that owns automated watchdog findings as worklist cards under a keep-my-data-healthy goal group. Cards are machine-filed with evidence and auto-resolve with a receipt when the condition clears, so health never rots as stale todos. Gives every watchdog one consistent delivery surface instead of scattered banners.
- [ ] 209. **Open a video lane: video-intent demand map, scripts from winning pages, YouTube citation tracking** (Completeness critic, large)
       Nothing in the 548 items touches video, yet Google video packs and AI engines cite YouTube constantly for Persian food, holidays, and how-to topics. Start cheap: classify video-intent queries from GSC and the SERP snapshots DataForSEO already stores, generate scripts and chapter markup from proven winning pages, and track youtube.com citations per topic in the Profound pipeline (src/domains/profound-coverage) as a named gap. This is a whole distribution channel a hired team would own that Beacon currently cannot even see.
- [ ] 210. **Auto-propose rollback on lost verdicts and measure the reversal as an A-B-A confirmation** (Learning loop, large)
       The changeOurMind line in pick-expectations.ts promises we roll it back and try X instead, but nothing generates the rollback or learns from it. On a mature lost verdict, auto-draft the rollback from the stored before text, queue it as a one-click pick, and measure the post-rollback window: if metrics recover, the loss is causally confirmed and trains the prior at double weight; if they do not, the loss was probably noise and gets down-weighted. Every loss becomes a second, stronger experiment for free.
- [ ] 211. **Synthetic control weighting so the counterfactual actually tracks the page** (Measurement science, large)
       The counterfactual today is an unweighted mean of up to 3 controls. Fit non-negative weights over a 10 to 15 page donor pool to minimize pre-period daily-series distance to the treated page (simple projected gradient in pure TS, no deps), then use the weighted composite as the counterfactual in computeWindowLift. The proof-engine docs already cite the synthetic control literature; this is the implementation the citations promise.
- [ ] 212. **Add GSC connect inside the wizard with 90-day backfill and a CSV fallback** (Onboarding and cold start, large)
       Connectors live only in /settings/connectors post-launch, so the first plan is always demand-blind. Add an optional Google step before review; on OAuth callback pull 90 days of Search Analytics scoped to the new tenant and re-run promotion so the first worklist is grounded in real clicks. For hesitant users, surface the existing import pipeline (src/lib/import, /settings/import) as a paste-or-upload GSC export fallback on the same step.
- [ ] 213. **Build the public connector-free audit page at /audit/[domain]** (Onboarding and cold start, large)
       The pricing ladder includes a free audit but no such route exists. Add an unauthenticated page that runs the polite 3-page derivation plus the homepage scorecard (fetch-site-profile.ts, derive-business-profile.ts), caches by domain, rate-limits, and renders a shareable instant report with a signup CTA that carries the derived profile into provisioning. Doubles as the marketing wedge and a zero-cost demo of the derivation engine.
- [ ] 214. **Make every teammate chip tappable to that voice's evidence: standup popover plus roundtable drawer** (Trust and receipts, large)
       The teammate chips in src/app/(shell)/team-standup.tsx are static spans and roundtable voices in daily-experiments-section.tsx cannot show their work. Make each chip open a popover or drawer with the data slice that voice argued from: the GSC rows behind Search demand, the Clarity friction metrics behind Visitor behavior, the cached SERP snapshot behind Live Google results, reusing the loaders buildLines and daily-evidence-brief.ts already call, with a link to the full surface. The debate stops being theater the moment every voice can show its notebook.
- [ ] 215. **Merge cannibalizing pages with survivor rewrite plus 301 via the Wix Redirects API** (CMS push depth, large)
       Beacon detects GSC-native cannibalization but can only suggest edits; the real fix is consolidation, which needs a redirect. Add a wixCreateRedirect wrapper behind a new explicit approval flow: rewrite the surviving page, hide the loser item, create the 301, verify both live. The never-change-URLs invariant stays intact because the loser URL keeps resolving via redirect; this is a capability no small-business owner does correctly by hand.
- [ ] 216. **Ship a WordPress adapter MVP so the push layer works beyond Wix** (CMS push depth, large)
       PublishTargetKind in src/domains/tenants/types.ts is wix_cms, git_pr, or dev_note, so any WordPress test subject gets dev notes only. Add a wp_rest target: application-password auth, /wp/v2/posts for title, excerpt, and content, Yoast or RankMath REST meta fields for edit_meta, and draft post creation for new pages, reusing the snapshot, cap, and ledger spine of executePush. Directly widens who Beacon can sell to at $250/mo since most small-business sites are WordPress.
- [ ] 217. **Add to today: push any ready change into the current plan from its row** (Worklist UX, large)
       The planner picks the batch, but the operator cannot say I want THIS one tonight; planTodayExperimentsAction is all-or-nothing and the changes list has no bridge into the plan. Add an Add to today action on ready rows that builds a PlannedExperimentRecord for that page via the existing candidate builder (src/domains/experiments/build-daily-candidates.ts), finds controls, and appends it to the preview plan or reserves atomically into an accepted plan. Closes the biggest agency gap: the human can steer the batch, not just veto it.

## Impact 6

- [ ] 218. **Anchor the brand entity: Wikidata item, Organization and WebSite sameAs, recognition probes** (AI visibility (AEO), medium)
       buildEntitySchema supports sameAs but nothing populates or verifies it, and the homepage expected-schema spec has no content-site story. Guide the operator through a Wikidata item for Iranopedia, emit Organization (logo, sameAs array) and WebSite JSON-LD on the homepage via composeSchema, and measure recognition by periodically asking engines what is iranopedia.com through the llm-mentions runner, diffing descriptors over time. Entity disambiguation is a prerequisite for being recommended by name rather than merely linked.
- [ ] 219. **Build an accuracy watch that catches AI being factually wrong and ships corrections** (AI visibility (AEO), medium)
       Profound analysis_types include accuracy, and stored answer texts allow deterministic checks of dates, spellings, and canonical facts against owned page content (the Chaharshanbe Suri 2026 date class of error). Flag hallucinated claims per prompt and generate a correct-the-record move: a dated, sourced, extractable correction block on the authoritative page. Being the correction source is a citation magnet.
- [ ] 220. **Build resource-hub pages aimed at recommendation-class prompts** (AI visibility (AEO), medium)
       Recommendation prompts (best websites to learn about Persian culture) are answered with listicles, and llm-mentions data shows which listicles win. Create a curated resource-hub page type per major topic: honest best-of list with outbound links to genuinely good sources plus Iranopedia's own sections, ItemList schema, dated updates. Sites that curate credibly get cited as the list, the recommendation-prompt equivalent of ranking first.
- [ ] 221. **Fingerprint owned facts and detect uncredited use in AI answers** (AI visibility (AEO), medium)
       Shipped answer blocks carry distinctive phrasings and unique stats. Scan stored AI answer texts (Profound answers plus native poll observations) for those exact n-grams to detect when an engine used Iranopedia's fact without citing it. Surface AI is repeating your sentence on 4 prompts but crediting nobody as a citation-push move and as proof of influence before the citation lands.
- [ ] 222. **Run persona and Farsi-locale variants of the top tracked prompts** (AI visibility (AEO), medium)
       ProfoundPromptSpec supports personas and regions, and the native poll asks one English phrasing per prompt. Run the top 20 prompts as 2-3 variants (diaspora planning a trip, student researching, Farsi-language phrasing) and compare citation outcomes; the Persian tokenizer already handles Farsi text. Iranopedia's bilingual audience means whole answer surfaces are currently unmeasured.
- [ ] 223. **Trend competitor citation share and alert on risers with a teardown link** (AI visibility (AEO), medium)
       Citation rows carry root_domain by date, but nothing trends competitor share over time. Compute per-topic competitor citation-share deltas month over month and alert when a domain gains meaningfully (thebump.com gained 18 points on Persian-names prompts), linking straight to the existing competitor teardown and quoted-passage view. Competitor intel watches Google today; this makes it watch the AI answer layer where the wedge lives.
- [ ] 224. **Add a quiet-night fallback ladder so the nightly loop never idles** (Autonomy, medium)
       When planDailyExperiments finds zero safe primary picks (protected pages, cooldowns, thin evidence), fall back automatically to a maintenance queue: image alt gaps, FAQ schema on already-winning pages, internal links from safe-internal-link.ts. The Today quiet-day line then reads no big swings today, so I tightened 4 small things, with receipts. A team you pay monthly should never report a night of nothing when safe work exists.
- [ ] 225. **Auto-add tracked prompts when emerging query clusters appear in GSC** (Autonomy, medium)
       spike-detector.ts already flags emerging labels (zero baseline to real volume), but tracked_prompts only grow when the operator curates them. When an emerging GSC query cluster fires, auto-generate matching tracked prompts and entities so Profound and DataForSEO LLM-mention checks start covering the demand within a day of it appearing. AI-answer tracking should follow demand automatically.
- [ ] 226. **Build a retry ladder: in-run backoff plus a midday second-chance cron** (Autonomy, medium)
       syncOneTenant in cron-sync.ts does a single Promise.allSettled attempt per source. Classify failures as transient (429, 5xx, network) versus permanent (auth revoked), retry transient ones with short backoff inside the 300s window, and register a third cron around 15:00 UTC that reads the morning cron_runs row and re-executes only the failed (tenant, provider) pairs. One transient Google 5xx at 9:00 currently means a full day of stale data.
- [ ] 227. **Schedule a verification retry queue for unreliable live reads** (Autonomy, medium)
       live-verification.ts explicitly treats the empty pre-hydration Wix shell as a retryable unreliable read (snapshot_stale), but nothing ever retries; a slow SSR at verify time leaves the change unconfirmed. Re-check pending verifications on a backoff schedule (1h, 4h, 12h, 24h) before flagging failure. False could-not-confirm states erode exactly the trust the verify-live step was built to create.
- [ ] 228. **Self-test rollback payloads weekly so reverts are guaranteed to work** (Autonomy, medium)
       The PS7 rollback readiness audit was one-time; pages drift, fields get renamed, and a rollback payload can silently rot. Add a weekly dry-run that validates each stored rollback still applies: page exists, target field still matches the recorded before-value shape. A failed dry-run files an Ops card before the auto-revert policy ever needs that payload in anger.
- [ ] 229. **Watch the sitemap nightly, auto-enroll new pages, auto-close cards for dead ones** (Autonomy, medium)
       Fetch the live sitemap in the nightly pass and diff it against page_snapshots: new URLs auto-enroll in snapshots, demand-graph mapping, and measurement eligibility; deleted or redirected URLs auto-archive their open worklist cards with a receipt. Beacon currently only learns the site changed when a scan is manually triggered, so cards can point at pages that no longer exist.
- [ ] 230. **"While you were away" block** (Carry-over, medium) [CARRY-OVER 50]
       when the operator has not opened Beacon for 48h+: what shipped, what changed verdicts, what the team found since (deterministic digest).
- [ ] 231. **A "Challenge this" button** (Carry-over, medium) [CARRY-OVER 36]
       per pick: one click asks the LLM to argue AGAINST the pick using the same evidence, rendered inline. Adversarial by request; deeply convincing.
- [ ] 232. **App-wide banned-dash + jargon guard** (Carry-over, medium) [CARRY-OVER 109]
       extend the existing test to cover EVERY file under src/app + src/components (currently a whitelist); fix the ~30 remaining visible instances once, then the guard holds the line forever.
- [ ] 233. **Export/share a win** (Carry-over, medium) [CARRY-OVER 77]
       one click renders a clean image/PDF card of a win (chart + sentence) for sharing. Founders show these off; it markets the product for you.
- [ ] 234. **One LLM entry point** (Carry-over, medium) [CARRY-OVER 107]
       fold llm-draft-gateway's remaining uniqueness into llm/structured-drafter (or make the gateway call it) so there is ONE budgeted, firewalled LLM path; delete the duplicate after tests move.
- [ ] 235. **Optimistic UI on every button** (Carry-over, medium) [CARRY-OVER 95]
       (apply/skip/copy/accept): instant visual state, server reconciles; no dead-feeling clicks.
- [ ] 236. **Roundtable everywhere the product recommends anything** (Carry-over, medium) [CARRY-OVER 38]
       New Pages board cards and Competitor moves get the same TeamRoundtable component (they already have packets).
- [ ] 237. **Standing adversarial regression** (Carry-over, medium) [CARRY-OVER 86]
       encode the operator's cases as tests that run on real fixtures weekly: boy vs girl names contamination, cities hub vs city pages, flags parent vs dynasty children, swear-words proof-blocked lever, Chaharshanbe date intent. (The forensic agent died before finishing these; make them permanent.)
- [ ] 238. **The Strategist voice gets a memory** (Carry-over, medium) [CARRY-OVER 40]
       reference the operator's own history ("You shipped 4 flag metas this week; this is the 5th and last before we wait for reads").
- [ ] 239. **Unify dismiss/skip stores** (Carry-over, medium) [CARRY-OVER 108]
       (opportunity_dismissals vs recommendation_response) behind one API so a dismissal on one surface can never leak an item back on another.
- [ ] 240. **Weekly email digest** (Carry-over, medium) [CARRY-OVER 119]
       (operator-triggered opt-in): the Monday recap band as an email. The product should reach out when there is news worth money.
- [ ] 241. **Build a pruning decision engine with keep, refresh, merge, redirect, delete verdicts** (Content engine, medium)
       Nothing decides that a page should die. Combine 12-month GSC zero-click history, zero internal equity, zero Profound citations from citation-evidence-store.ts, and decay state into a per-page verdict with expected payoff, surfaced on the worklist with the existing rollback copy. Pruning dead weight is a proven quality lever big sites underuse because the decision is tedious; Beacon can make it automatic.
- [ ] 242. **Detect mixed-intent pages and emit a split brief** (Content engine, medium)
       Cluster each page's GSC queries by intent; when one page straddles definitional and transactional intent with weak CTR on both, emit a split brief: keep the head intent on the existing URL and spin the other into a new page with cross-links. Intent splits recover rankings no on-page edit can fix, and no current trigger sees them.
- [ ] 243. **Generate author and reviewer pages with Person schema to close own-page E-E-A-T gaps** (Content engine, medium)
       winner-patterns.ts extracts competitor E-E-A-T signals and page_eeat_findings persists the tenant's own gaps, but nothing generates the fix. Draft author and reviewer bios, Person plus author JSON-LD via composeSchema, and byline text for articles missing authorship, delivered paste-ready or as wix_field pushes. Authorship is the cheapest E-E-A-T signal Iranopedia does not send today.
- [ ] 244. **Generate hub index pages for topic clusters** (Content engine, medium)
       group-moves.ts and to-cluster-items.ts already cluster moves by topic, but no hub page ties a cluster together. Generate hub-page briefs: an intro, links to every child page with one-line blurbs, and an FAQ mined from the cluster's fanout questions, routed through the create-page pipeline. Hubs concentrate link equity and are the missing top of the topical map.
- [ ] 245. **Queue a counter-refresh when a winning competitor updates their page** (Content engine, medium)
       competitor-intel structural-diff.ts detects competitor page changes and the teardown cache re-fetches every 14 days, but nothing reacts. When a winner we lost to adds sections or FAQs, auto-queue a counter-refresh on the matched owned page naming the specific new sections they added. Beacon stops being the team that finds out a quarter late.
- [ ] 246. **Enable the GSC BigQuery bulk export to unlock unsampled long-tail query data** (Completeness critic, medium)
       The planned 16-month backfill and anonymized-query gap estimates still ride the sampled Search Analytics API with its row caps; the bulk export to BigQuery is the professional-grade move that returns every query row daily, including the long tail the API hides. Wire export setup into the GSC connector (src/lib/connectors/gsc), land rows in a gsc_bulk table, and point the transliteration demand engine at it. Persian transliteration variants (koobideh vs kubideh vs kabob koobideh) are exactly the queries sampling erases.
- [ ] 247. **Encrypt connector credentials at rest and add a token rotation runbook** (Completeness critic, medium)
       Google, Wix, and Profound tokens flow through src/lib/connector-store.ts and src/lib/connectors/google-auth.ts as plaintext rows, so one leaked Supabase service key exposes every tenant's Google account. Encrypt token columns with an app-held key, scope OAuth grants to minimum APIs, and write a rotation runbook plus a quarterly rotation reminder card. This is table-stakes security a paying customer's IT person will ask about in the first call.
- [ ] 248. **Generate original diagrams and infographics with ImageObject schema as a citation magnet** (Completeness critic, medium)
       The image items only audit alt text and attach openly licensed images; nothing creates original visuals, which is how encyclopedias win image search, Discover cards, and hotlinked backlinks. Build an SVG diagram factory (dynasty timelines, Persian calendar conversion tables, recipe step graphics) from structured facts the entity pages already hold, publish through the Wix media path with ImageObject plus license schema, and measure via the planned type=image GSC lane. Unique visuals also get described and credited by AI engines where stock photos never do.
- [ ] 249. **Harden every URL fetcher against SSRF and rate-abuse before the public audit funnel ships** (Completeness critic, medium)
       Beacon fetches operator-supplied and stranger-supplied URLs (competitor teardowns, cold-start crawl, the planned public /audit/[domain] funnel) with no SSRF guard visible in src/domains/scanning or the fetch paths. Add a shared safe-fetch wrapper that blocks private IP ranges, cloud metadata endpoints, and redirect-to-internal tricks, plus per-IP rate limits and a hard spend fence so anonymous audit requests cannot burn the DataForSEO or LLM budgets. The public funnel turns these from theoretical to day-one attack surface.
- [ ] 250. **Alert when a SERP feature appears or disappears on a query that drives real clicks** (DataForSEO depth, medium)
       With the append-only SERP history in place, diff consecutive snapshots per query and emit a teammate line when an AI Overview, snippet, or video pack arrives or leaves on a query with real GSC demand (join via load-gsc-signal.ts). An AI Overview landing on a top query is a CTR threat with a concrete counter-move (answer block, schema, snippet steal), surfaced through existing Today bands.
- [ ] 251. **Check SERP volatility and incumbent age from historical SERPs before saying build** (DataForSEO depth, medium)
       Labs historical_serps returns past SERPs going back years. Before a build verdict, one lookback answers what serp-validation.ts cannot: has the top 5 churned recently (churny SERPs are winnable, frozen five-year incumbents are not) and did anyone new break in within 12 months. Add a volatility line to PreparedSerpVerdict for the strategist voice to cite.
- [ ] 252. **Route nightly SERP batches through the standard task queue at roughly a third the live price** (DataForSEO depth, medium)
       enrichPickSerpPatterns and prepare-create-page-verdicts.ts run in the nightly batch where latency is irrelevant yet pay live-endpoint prices. Add a task_post/task_get path (POST serp/google/organic/task_post, poll task_get) used only by nightly jobs; standard-queue organic is about $0.0006 vs $0.002 live, so the same cap buys 3x the SERP coverage.
- [ ] 253. **Collapse the two competing color systems into the one token layer** (Design, medium)
       545 raw palette classes (bg-white, border-gray-200, dark:bg-neutral-900, emerald/amber/sky/violet) live across 32 plus shell files like src/app/(shell)/war-room-sections.tsx (the CARD const), scoreboard-section.tsx, page.tsx, and moves/moves-worklist-client.tsx, while globals.css defines a full oklch token system (bg-card, border-border, surface-raised, status-*). Two cards on the same Today screen render slightly different whites and grays in both themes, which reads as cheap. Migrate the war room, Today, worklist, and proof surfaces to tokens first; they carry most of the drift.
- [ ] 254. **Add named demand segments as first-class tracked series** (frontier, medium)
       Define per-tenant named segments as query regex and token rules (branded, question-form, Farsi-script, English), compute each nightly from the GSC archive via src/lib/performance-timeseries.ts, and let the standup speak per segment: 'non-brand questions grew 12 percent, branded flat'. Segments also sharpen attribution since experiments should move their own segment, not the blend.
- [ ] 255. **Audit how AI describes the brand: accuracy and sentiment on stored answers** (frontier, medium)
       Beacon stores answer excerpts in dataforseo-llm-mentions records and full Profound answers but never reads how Iranopedia is characterized. Run a budget-gated LLM pass over brand-mentioning answers to extract descriptors and factual claims, diff claims against tenant ground truth via src/domains/entity/discrepancy-detect.ts, and open correction Moves when AI repeats something wrong. A wrong fact in ChatGPT is a churn-grade problem a paying customer expects the tool to catch.
- [ ] 256. **Build a zero-click exposure ledger per topic cluster** (frontier, medium)
       Compute per-cluster zero-click exposure from GSC impressions vs clicks plus SERP feature flags in cached snapshots ('this cluster gets 30k impressions but features absorb the clicks'), and route those clusters to AEO Moves that win the citation inside the answer instead of futile CTR Moves. It stops the planner from recommending title tweaks where no click exists to win, which protects forecast credibility.
- [ ] 257. **Generate a monthly client-grade report page from the proof ledger** (frontier, medium)
       Build /report/[month]: wins with dollar framing, losses owned plainly, moves shipped, position distribution shift, AI visibility trend, spend vs the $50 cap, and next month's plan, all print-clean from existing loaders like src/domains/product/report-generator.ts. Distinct from the planned weekly email digest: this is the permanent artifact that convinces a stranger the $250 was worth it, twelve times a year.
- [ ] 258. **Give new tenants a day-one brain transplant from cross-tenant priors** (frontier, medium)
       src/domains/attribution/shared-brain.ts and src/lib/event-priors.ts already learn anonymous lever-level win rates. On onboarding, match the new site's shape (page types, demand mix from the first GSC sync) to learned patterns and emit a first plan within the hour: 'on sites like yours, answer blocks on definitional pages won 8 of 11 times'. Day-one competence instead of a cold start is the difference between a trial convert and a churn.
- [ ] 259. **Keep an append-only SERP history and alert on competitor jumps** (frontier, medium)
       The SERP cache in dataforseo-serp.ts is TTL-based, so every refetch destroys history. Persist every fetched SerpSnapshot into an append-only serp_history table keyed by query and date, then diff consecutive snapshots to detect 'competitor X jumped 8 to 3 on your money query' and feed it to the roundtable as a defense candidate. Also gives proof cards a SERP replay strip showing the top 10 before and after our ship date at zero extra API cost.
- [ ] 260. **Re-grade shipped money pages monthly as the SERP evolves** (frontier, medium)
       Beacon measures traffic decay but never content decay. Monthly, re-run the term-coverage grade on the top 20 GSC-traffic pages against fresh cached SERPs and open a refresh Move when coverage falls below the winners' bar, routed through the existing worklist. Pairs with keyword-refresh-candidates.ts in src/domains/demand which lists refresh targets but has no content-vs-SERP evidence.
- [ ] 261. **Ship original-data studies generated from Beacon's own search and AI data** (frontier, medium)
       Beacon sits on 22.8k Profound citation rows, a 210k keyword universe, and per-platform answer observations. Auto-draft a 'What AI actually recommends about Persian culture' study page with charts, real numbers, and a methodology block, built to earn links and AI citations. Original data is the highest-probability link magnet in content marketing and the marginal cost is a template over data Beacon already owns.
- [ ] 262. **Track UGC citation share in AI answers and SERPs, then counter it** (frontier, medium)
       Classify cited domains in llm-mentions and Profound data as UGC vs editorial and flag owned topics where reddit.com or quora.com ranks above us in cached SERPs. When a thread is the cited source, tear it down like a competitor page: extract the question it answered that our page dodges, emit an answer-block Move quoting the exact gap, and surface the specific threads for an authentic operator reply linking back. A large share of AI answers is sourced from exactly these threads and no current move type touches them.
- [ ] 263. **Turn founder-authority into a full author entity system with Person schema bylines** (frontier, medium)
       src/domains/entity/founder-authority.ts exists but nothing publishes author identity on-page. Generate author bios, Person JSON-LD with credentials and sameAs links, and byline blocks pushed through the existing Wix field mappings. Both Google and AI answer engines weight authorship signals, and encyclopedia content without a named author reads as anonymous UGC.
- [ ] 264. **Add a fresh-data lane: provisional scoreboard points plus 24-72h early reads on in-flight ships** (Search Console depth, medium)
       FINAL_LAG_DAYS=3 means the Today scoreboard always ends three days ago and in-flight chips just count to day 7. Pull dataState=all through the existing pullDayRows path, persist with is_final=false, render the last 3 days as visually provisional points, and give each in-flight experiment an early read like impressions already moving at 48h. Makes Today feel alive and same-week ships visible.
- [ ] 265. **Add device grain to the nightly GSC sync and fire a mobile CTR truncation trigger** (Search Console depth, medium)
       sync-search-analytics.ts pulls only page+query, page, and totals grains. Add a [page, device] pull into a gsc_daily_device_totals table, then flag pages where mobile CTR sits well below desktop at similar position; combined with a long title that means mobile truncation, routed into the existing title lever. Iranopedia is almost certainly majority mobile, so blended CTR hides this today.
- [ ] 266. **Aggregate a sitewide striking-distance portfolio into one capturable-clicks headline** (Search Console depth, medium)
       isStrikingDistance in gsc-page-queries.ts flags per-query opportunities but nothing sums them. Aggregate property-wide, price each query with the CTR-curve delta from current position to position 3, and put one number on Today: 1,900 clicks a month sitting between positions 4 and 15. That number is the $250/mo pitch in one line.
- [ ] 267. **Build a back-of-results demand register for positions 20-50** (Search Console depth, medium)
       Queries with real impressions where the best own URL sits at position 20-50 mean Google already indexes and tests the site for that topic; the right play is expanding an existing section, not a new page. A zero-dollar GSC-native discovery source read straight from gsc_daily_rows that complements the paid keyword universe.
- [ ] 268. **Build the diaspora demand map: GSC country grain plus multi-geo DataForSEO volume** (Search Console depth, medium)
       Pull [query, country] in sync-search-analytics.ts into a country rollup, and run the top-100 terms through DataForSEO volume for UK, Canada, Germany, and Australia geos instead of the hardcoded location_code 2840 in dataforseo-keywords.ts. Expose per-page top countries to the strategist voice; pages already ranking in a secondary geo with zero effort are free wins the US-only view cannot see, and this is the factual base for any future hreflang decision.
- [ ] 269. **Close the sitemap loop: audit pollution, diff against impressions, and resubmit after ships** (Search Console depth, medium)
       No sitemaps.list usage exists. Cross-check every sitemap URL from sitemap-parse.ts against live status, noindex, and canonical target; diff sitemap URLs against gsc_daily_page_totals (zero-impression sitemap pages are zombie inventory, impression-earning pages missing from the sitemap are a coverage gap); verify Wix lastmod against Beacon's content_hash history to catch lastmod lying; and call sitemaps.submit after verify-live succeeds, confirming lastDownloaded advanced. The SitemapReconciliation types in pages/types.ts are the natural home.
- [ ] 270. **Detect wrong-landing-page queries where Google ranks the wrong own URL** (Search Console depth, medium)
       When only one own URL ranks for a query and its classified topic (page-classifier.ts, resolve-page-intent) mismatches the query while an exactly-on-topic page earns zero impressions, propose internal links and anchor changes to flip Google's choice. Complements gsc-cannibalization.ts, which only fires when two or more URLs co-rank; the single-wrong-URL case is invisible today.
- [ ] 271. **Gate picks on canonical health and validate that canonical targets resolve cleanly** (Search Console depth, medium)
       One inspection per nightly pick checks coverage_state; Duplicate, Google chose different canonical means edits land on a page Google ignores and the experiment is dead on arrival, so block the pick and emit a fix-canonical move. Also extend src/domains/indexability/ to fetch each distinct canonical_url and flag targets that 404, redirect, or carry noindex, a canonical chain Google ignores.
- [ ] 272. **Add a do-not-repeat registry: block candidate classes that lost twice under the same conditions** (Learning loop, medium)
       proof-outcome-caution.ts only blocks the exact same page plus family; a lever that lost twice on a page family with the same intent can be re-proposed forever on other pages of that family. Derive negative rules (pageFamily times lever times intentBucket with 2 or more mature losses and no wins) from the ledger and enforce them in assessEligibility in experiment-eligibility.ts with an honest excluded reason: this kind of change lost twice on these pages. Negative memory is half of learning and today it is per-page only.
- [ ] 273. **Retrain the CTR title scorer weights from the site's own settled title experiments** (Learning loop, medium)
       src/domains/demand-graph/ctr-title-scorer.ts scores titles with fixed heuristic weights (numbers, brackets, length). Mature title-lever outcomes in the ledger carry the before and after text plus the realized CTR lift, so extract the same features from both versions and fit which features actually predicted lift on this site. Even a simple logistic reweighting grounded in 20+ settled title tests beats industry folklore, and the scorer's explanation can cite the site's own evidence.
- [ ] 274. **Use operator verdict overrides to audit and tighten the measurement rules themselves** (Learning loop, medium)
       operatorVerdictOverride pins on the GSC ledger (the operator saying your measurement was wrong) currently train nothing, and confidence-calibration.ts sits passive on the old citation engine. Aggregate overrides by condition (controls used, overlap state, baseline impressions band) and when override rates cluster, for example verdicts with 2 controls get pinned 3x more often, tighten the corresponding rule in measure.ts or raise MIN_CONTROLS. This is the system learning about its own referee, which protects every other learning feature.
- [ ] 275. **Auto-verify the facts_to_verify list on AEO briefs against cached evidence** (LLM engine, medium)
       AeoPromptBriefSchema emits facts_to_verify but nothing ever verifies them, so the riskiest part of each brief lands on the operator unassisted. Add a verification pass that checks each fact against page_snapshots bodies and cached DataForSEO SERP snippets, marking each confirmed, contradicted, or needs-human. The brief card then shows 4 of 6 facts confirmed automatically, which is exactly the out-think-a-human-team promise.
- [ ] 276. **Implement the Anthropic provider as an automatic failover** (LLM engine, medium)
       src/domains/recommendations/providers/anthropic.ts still throws not_implemented and src/lib/llm/config.ts rejects the value, so any OpenAI outage or 429 night silently downgrades every draft, verdict, and judge to deterministic. Implement a Claude-backed CompleteFn behind the same injectable seam in structured-drafter.ts and fail over on openai_5xx or 429. A one-provider LLM engine is a single point of failure for the product's smartest layer.
- [ ] 277. **LLM labeling pass over the keyword universe: intent and topic clusters in one call** (LLM engine, medium)
       The DataForSEO keyword universe foundation stores raw terms and volumes but no semantics, so New Pages picks lean on string matching. One nightly batched call labels up to 200 discovered terms with intent, topic cluster, and best-page-type, cached permanently per term in Supabase. This upgrades every downstream consumer (new-page demand, fanout coverage, cannibalization checks) for roughly a cent a night.
- [ ] 278. **Move the nightly drafting to the OpenAI Batch API at half price** (LLM engine, medium)
       The nightly pass fires up to 8 atomic edits, 6 answer blocks, and 8 team verdicts as individual live calls, but nothing needs results before morning. Submit them as one Batch API job (50 percent discount, 24h window) from the auto-measure or plan cron, then hydrate move_drafts when the job completes. The same cap buys twice the drafting, and rate limits stop mattering.
- [ ] 279. **Per-feature budget envelopes with monthly pacing so the cap never kills week four** (LLM engine, medium)
       All LLM features share one $10 cap under the single platform tag adjudicator-openai in adjudicator-budget.ts, so a chatty FAQ loop can starve team verdicts, and checkBudget is first-come-first-served so heavy early-month drafting silently disables every LLM pass for the final week. Tag spend per call site (drafter-answer_block, drafter-verdict, page-surgeon-judge, strategist), enforce soft per-feature envelopes, and add pacing: remaining budget divided by remaining nights yields tonight's ceiling with a floor for the highest-value kinds. The /settings/spend page then shows which feature spent what; also raise DEFAULT_CAP_USD from the Ritz dogfood value since the north star says cost is fine if truly smarter.
- [ ] 280. **Prompt regression harness replaying recorded fixtures through the full gauntlet** (LLM engine, medium)
       CompleteFn in structured-drafter.ts is already injectable, so record 15 to 20 real Iranopedia inputs plus captured gpt-5-mini outputs as fixtures. A vitest suite replays every prompt version through robustJsonExtract, Zod validation, content firewalls, and draft-quality.ts, and fails when schema-failure or rejection rates drift. Any prompt edit then gets a red or green signal before it burns a night of live drafting. Distinct from the planned adversarial regression cases, which target evidence gates.
- [ ] 281. **Reuse the Page Surgeon diagnosis as stage one of the daily rewrite** (LLM engine, medium)
       page-surgeon/llm-judge.ts already runs a two-stage diagnose-then-plan prompt with a named bottleneck and ruled-out levers, while the daily draftAtomicEditStructured pass is single-stage and regularly writes a fine meta for the wrong problem. When a fresh Page Surgeon decision exists for the pick's page, inject its diagnosis line into the drafter's user prompt; when it does not, prepend a one-sentence cheap diagnosis call. The rewrite then targets the diagnosed bottleneck, not a generic improvement.
- [ ] 282. **Self-critique grader pass before an LLM draft ships to the plan** (LLM engine, medium)
       Add a second cheap gpt-5-nano call after a draft validates in callStructuredLLM: grade it 1 to 5 against a rubric (answers the dominant intent first, specific to this page, quotable, no filler) and regenerate once when the grade fails. This catches the technically-valid-but-mediocre drafts the deterministic draft-quality.ts gate cannot see, at roughly a tenth of a cent per check. Store the grade on the draft so the card can show it.
- [ ] 283. **Stop re-paying for identical LLM work: call cache plus persisted answer-gap drafts** (LLM engine, medium)
       adjudicate.ts caches on hashEvidencePacket but callStructuredLLM re-pays for byte-identical kind, system, user, model requests, and the preview pipeline rebuilds several times a day with unchanged inputs. Add a Supabase llm_call_cache keyed on a sha256 of those fields with a 7-day TTL, checked before the budget gate. Also persist the D-2 answer-gap drafts (up to 6 per night in build-today-preview.ts, currently discarded from an in-memory map) via the already-built saveStructuredDraft keyed on url plus question, reusing any draft under 7 days old. Team verdicts with unchanged voices and repeat answer-gap drafts become free.
- [ ] 284. **Strict JSON schema outputs generated from the Zod registry** (LLM engine, medium)
       adjudicate.ts already uses strict structured outputs successfully via buildAdjudicatorJsonSchema, while structured-drafter.ts skips response_format entirely and parses JSON out of prose, paying for a retry whenever the shape drifts. Generate a strict json_schema per kind from SCHEMA_BY_KIND with zod-to-json-schema, keep max_completion_tokens headroom for reasoning, and fall back to robustJsonExtract only when content arrives empty. Fewer validation_failed retries means every failed doubling of cost disappears.
- [ ] 285. **Adaptive per-lever measurement windows learned from time-to-signal** (Measurement science, medium)
       PROOF_WINDOW_DAYS is a fixed 7/14/28 constant in measure.ts, but title CTR changes show in days while internal links and content expansions move rank over 6 to 10 weeks, biasing slow levers toward inconclusive. Mine the windows arrays on mature records for the earliest window whose direction matched the final verdict per lever, add a familyWindowDays map (56d for link and content families) threaded through run-measurement.ts and measurement-maturity.ts, and let the debate commit longer windows for volatile pages with the reason persisted on the record. Better-fitted windows mean fewer inconclusive verdicts and denser learning data, plus honest countdown copy like titles usually show by day 10.
- [ ] 286. **Alpha-spending rule across the 7/14/28 day checkpoints** (Measurement science, medium)
       Reading three sequential windows and acting on the earliest is classic peeking; the 7-day floor should be much stricter than the 28-day one. Implement a simple O'Brien-Fleming style multiplier on the lift floors by window day in summarizeVerdict (roughly 3x at 7d, 1.7x at 14d, 1x at 28d), so early_checkpoint signals in measurement-maturity.ts are calibrated rather than merely labeled directional. Small change, large rigor gain.
- [ ] 287. **Backtest command that restates the whole ledger under improved math** (Measurement science, medium)
       Every measurement upgrade changes historical verdicts, and silently rewriting the record would destroy trust. Add npm run proof:backtest that replays measureRecord over the full ledger with the new estimators, produces a restatement diff (which wins survive, which die), and writes a dated entry the Results page can link as we tightened our math on this date and restated 3 results. Real research teams restate; doing it in the open is a selling point.
- [ ] 288. **Equivalence testing to separate proven no-effect from too noisy** (Measurement science, medium)
       Inconclusive currently conflates two opposite lessons: a precisely estimated zero (the lever genuinely does nothing here, stop trying it) and a noisy nothing (the test was underpowered, try again bigger). Add a TOST-style equivalence check in summarizeVerdict using the control-spread noise band, and split the Learning band copy accordingly. The settled no-lift blocker in experiment-eligibility.ts should only fire on true equivalence, not on noise.
- [ ] 289. **False discovery rate control across the concurrent experiment batch** (Measurement science, medium)
       With roughly 8 live experiments judged at loose thresholds, about one fake win per batch is expected, and the Wins band currently counts it as real. Apply Benjamini-Hochberg across the batch's placebo p-values in src/domains/proof-gsc/load-ledger.ts and show an honesty line on Results: at this evidence level we expect about 0.8 of these wins to be luck. This is the statistical version of the honesty sentence the product already values.
- [ ] 290. **Multi-metric coherence score on every verdict** (Measurement science, medium)
       pickProofMetric judges one metric, but a CTR win while impressions and position both collapsed is a red flag the current code never raises. Add a pure coherence check in measure.ts comparing the direction of all four adjusted lifts (clicks, CTR, position, impressions) and attach a coherent, mixed, or contradictory grade to the verdict. Contradictory results get a caution line and reduced learning weight. Cheap cross-validation that catches metric artifacts.
- [ ] 291. **Salvage overlapping edits with truncated clean sub-windows** (Measurement science, medium)
       detectMeasurementOverlaps in measurement-maturity.ts blanket-flags any same-page overlap as attribution_limited, voiding both measurements even when change A had 12 clean days before change B landed. Truncate A's post window at B's ship date and judge it on the clean segment with the alpha-spending floor for its length, instead of discarding real evidence. As the daily cycle raises ship velocity, same-page overlaps become the norm, so partial credit is the difference between a learning system and a stalled one.
- [ ] 292. **Add an LLM industry classifier fallback when JSON-LD is absent** (Onboarding and cold start, medium)
       deriveBusinessProfile only derives industry from schema.org @type, which most small sites lack, so industry is null and starter prompts degrade to brand-only. Add one budget-gated gpt-5-mini call over the extracted homepage text returning industry, audience, and offer as structured output, used only when the deterministic path yields null. Files: src/domains/onboarding/derive-business-profile.ts, launch-config.ts, the existing LLM gateway.
- [ ] 293. **Buy a day-0 keyword demand pack at launch from DataForSEO** (Onboarding and cold start, medium)
       At launch, run planKeywordsCall and runKeywordVolume (src/domains/serp/dataforseo-keywords.ts, inside the $50 fail-closed ledger) on top derived services crossed with locations, roughly $0.30 per tenant. The first worklist and New Pages board then show real monthly search volumes without waiting for GSC, so every day-0 card carries a demand number instead of a blank.
- [ ] 294. **De-verticalize the scope step: derived-services chips replace builder checkboxes** (Onboarding and cold start, medium)
       scope-form.tsx (src/app/(shell)/onboard/scope/) still shows six builder project_mix checkboxes to every signup and PROJECT_MIX_LABELS leaks builder vocabulary to a dentist or a SaaS. Replace with pre-checked chips built from profile.services plus free-text add, and when contentSiteSignal is true swap the cities question for a topics question. The wizard is the last builder-shaped surface in the product.
- [ ] 295. **Kick a competitor teardown at launch so day one names the gap** (Onboarding and cold start, medium)
       When the wizard saves competitors, dispatch the existing competitor-intel refresh (src/domains/competitor-intel/refresh-intel.ts, structural-diff.ts, winner-patterns.ts) scoped to the new tenant in the background. The first Today can then say a named rival has FAQ schema on 12 pages and you have none, reusing machinery already proven on Iranopedia instead of waiting for the first nightly.
- [ ] 296. **Persist a crawl frontier so cold-start coverage grows past 18 pages** (Onboarding and cold start, medium)
       in-process-scan.ts hard-caps at DEFAULT_MAX_PAGES 18 and 22 seconds, and with crons off a new tenant inventory never grows. Persist remaining sitemap URLs into a crawl_frontier table at cold start, then consume bounded chunks on subsequent /today visits or the nightly tick until coverage is complete, with a pages-read counter. A 300-page site currently gets recommendations for 6 percent of itself forever.
- [ ] 297. **Rank a new tenant's first recs with cross-tenant global-pattern priors** (Onboarding and cold start, medium)
       src/domains/global-patterns (aggregate.ts, query.ts) holds anonymous cross-tenant outcome patterns but the cold-start promotion path never consults it. Weight the first worklist by pattern win rates so day-0 cards can say changes like this improved clicks at N other sites, giving a zero-history tenant borrowed proof instead of unproven forecasts.
- [ ] 298. **Run the cold-start crawl in the background during the wizard, not inside Launch** (Onboarding and cold start, medium)
       runInProcessColdStartScan (src/domains/scanning/in-process-scan.ts) runs inside executeLaunchTransaction under a 60s ceiling while the stranger watches a spinner. Dispatch it right after the business step saves the domain using after() or waitUntil so the inventory and promoted candidates already exist by the review step and Launch is instant with a populated /today.
- [ ] 299. **Consolidate the 8-query per-page enrichment fan-out into one page_insights RPC** (Performance, medium)
       today-moves-data.ts fires 8 parallel Supabase reads for the same ~12 pooled URLs: top queries, declines across two windows, cannibalization, GA4, Clarity, keyword cache, SERP patterns, and daily clicks. A single Postgres function page_insights(tenant_id, urls[]) returning one JSONB per page collapses 6+ round-trips into 1 and moves the recent-versus-prior decline math into SQL. The same shape is reusable by the per-page loaders in gsc-page-queries.ts.
- [ ] 300. **Cut both per-request middleware round-trips: local JWT verify plus membership cookie** (Performance, medium)
       src/lib/auth/supabase-middleware.ts calls supabase.auth.getUser() (a network round-trip to Supabase Auth, roughly 100-300ms) and then queries tenant_members on every matched request for a single known user whose memberships change roughly never. Switch to getClaims() with asymmetric JWT signing keys for local verification, keep a periodic real getUser for revocation, and cache the resolved tenant list in an HttpOnly signed cookie with a short TTL re-queried only on miss or tenant switch. Every click in the app gets faster.
- [ ] 301. **Dedupe in-request reads: cache loadShippedChanges, page signals, and daily totals once** (Performance, medium)
       loadShippedChanges (src/domains/proof-gsc/shipped-change-store.ts) fires 4 identical uncached reads on one Today render, loadGscPageSignalsForTenant is called by both load-graph.ts and gap-compiler.ts with serial 1000-row pagination each, and loadDailyTotalsForTenant runs three times with different day arguments (84, 21, 21). Wrap them in react.cache, normalize to one 84-day totals read plus a pure slice helper, and re-key cached loaders like loadTodayMovesHeroData by primitive (tenantId, limit) args since react.cache keys object literals by identity and always misses. Removes several seconds of duplicate Supabase work per request.
- [ ] 302. **Fold the war-room band reads into SWR snapshots so warm Today does near-zero live queries** (Performance, medium)
       The Today composite (today-surface) covers only the canonical view; FrictionFixesSection, AiCrawlerSection, DemandOpportunitiesSection, and TeamStandup in page.tsx each fire their own loaders (Clarity signals, bot referrals, daily totals, keyword caches) on every render even when today-surface is warm. Include their band data in the composite or give each a tiny snapshot store with the same invalidation. Warm Today drops from ~10 queries to ~2.
- [ ] 303. **Never serve cold after invalidation: mark snapshots stale and rebuild in after()** (Performance, medium)
       invalidateDemandGraph, invalidateTodaySurface, and invalidateWorklistSurface all delete or empty the snapshot, so the next operator visit after a connector sync or plan accept pays the full synchronous rebuild chain, up to ~30s cold. Add an invalidatedAt field so reads keep serving the old data instantly with the honest updated-N-ago stamp, and schedule buildAndPersistOnce plus the surface recomputes via after() at the invalidation boundary using the existing single-flight locks in load-graph.ts. The app degrades to stale-but-instant instead of blocking.
- [ ] 304. **Persist nav badges in a shell-surface snapshot and lazy-load the palette changelog** (Performance, medium)
       src/app/(shell)/layout.tsx awaits 7 reads (pending findings, changelog, connector info x2, url-change outcomes, experiment check) before any route under the shell can paint, and serializes up to 50 changelog entries into CommandPalette client props on every render. Apply the today-surface-store.ts pattern: a tiny shell-surface blob holding badge counts served instantly and refreshed via after(), and fetch the palette Results group from a route handler on first Cmd+K open. The layout then does one millisecond-scale read instead of 7 queries.
- [ ] 305. **Slim the worklist RSC payload: collapsed cards get a projection, detail hydrates on expand** (Performance, medium)
       moves-worklist-client.tsx receives up to 75 full TodayMove objects, each carrying preparedDraftText, faqs, outline, titleVariants, debate, and sparkline arrays, serialized into the client flight payload even though most cards render collapsed. Send a slim list projection (label, action, score, chips, sparkline) and fetch the full card body via a server action on expand. Measure first with trace.measureSize on the moves array; this likely cuts multiple MB off the /worklist response.
- [ ] 306. **Relocate today-moves-data.ts and the TodayMove god-type into src/domains** (Code health, medium)
       src/app/(shell)/today-moves-data.ts is 1045 lines of pure domain logic (evidence joins, scoring, draft quality, title variants) plus a ~35-field TodayMove type living in the route layer, violating the pages-stay-thin contract. Worse, src/domains/demand-graph/precompute-drafts.ts and src/domains/action-pack/load-today-cockpit.ts import from @/app/(shell), inverting the layering. Split it into loader, enrichment, and types modules under src/domains/demand-graph so the nightly precompute and the worklist share one tested core.
- [ ] 307. **Unify 18 URL normalize helpers into one url-identity module with fixture tests** (Code health, medium)
       grep finds 18 implementations: src/lib/url/normalize.ts, src/lib/import/parsers.ts, src/lib/persistence/csv-parser.ts, canonicalizeUrl copies in attribution, recommendations, and citation-lifecycle, plus private normalizePath copies in match-engine. Every evidence join in the product is URL-keyed, so trailing-slash or query-string drift silently drops evidence, the exact class the relevance-gate work fought. One module with Iranopedia and Wix URL fixtures ends the drift.
- [ ] 308. **Zod-validate connector API payloads at the boundary instead of blind interface casts** (Code health, medium)
       zod ships in package.json but only 3 files import it; DataForSEO, Profound, GSC, GA4, and Wix responses in src/lib/connectors are cast to TypeScript interfaces with zero runtime checks, so vendor API drift becomes wrong numbers on operator cards instead of a loud error. Add one schemas.ts per connector validating at the fetch boundary with the failing snippet logged. The difference between I checked and I hoped when the team quotes a number.
- [ ] 309. **Age-decay every opinion's confidence by actual evidence row age** (Team intelligence, medium)
       staleAt is binary, so a 13-day-old SERP verdict still speaks at 0.9 while an hour-old GSC read speaks at 0.5. Thread source-row fetch timestamps through the EvidencePacket and decay each voice's confidence linearly toward a floor across its CADENCE_MS window in specialist-opinions.ts. Fresher evidence then naturally wins debates without any display change; distinct from the planned freshness chips, which are display only.
- [ ] 310. **Chain aligned evidence across teammates into explicit multi-source claims** (Team intelligence, medium)
       Today cross-source agreement is one boolean overlap boost in routeMove (hasGsc and hasProfound and overlapMult over 1). Generalize to named chain templates, for example demand plus gap plus winnable, that emit a single chained claim carrying all contributing EvidenceRefs and a confidence raise sized by chain completeness. Cards then show one strong linked argument instead of three adjacent ones.
- [ ] 311. **Codify escalation rules for when the team must ask the operator** (Team intelligence, medium)
       Deterministic triggers that convert a silent pick into an operator-decision card showing both sides: consensus under 55 on a page with GA4 dollarValue, a veto colliding with top-decile demand, or the two passes of a big-bet review disagreeing. Encoded next to the veto logic in team-review.ts. The team out-thinks a human team partly by knowing when to raise its hand.
- [ ] 312. **Make each voice state a falsifier and auto-retract when it trips** (Team intelligence, medium)
       The header of specialist-opinions.ts promises falsifiable opinions but no emitter states a machine-checkable withdrawal condition. Add one per claim, for example if impressions fall under 200 within 14 days this claim is void; the auto-measure cron checks falsifiers as data lands, the card shows the retraction, and the scoreboard logs an early wrong-call signal before the full window settles.
- [ ] 313. **Require a second independent pass on big bets before acceptance** (Team intelligence, medium)
       Any pick on a page above the planner's highTrafficImpressions (9000 in daily-experiment-planner.ts) or any create_page gets a second debate run with forced-fresh evidence past staleAt; if the second pass disagrees on the action or drops a confidence band, the pick demotes to backups with both passes shown. Cheap insurance exactly where a wrong call costs the most.
- [ ] 314. **Rotate a nightly devil's advocate that must argue against each selected pick** (Team intelligence, medium)
       Assign one teammate per night the red-team seat; it deterministically mines its own evidence slice for the strongest disqualifier (seasonal demand cliff, stale rows, weak ownership) and the pick pays a visible score penalty when the challenge lands with a real number. Distinct from the planned on-demand LLM Challenge button: this runs automatically inside the nightly batch and is persisted on the plan record.
- [ ] 315. **Shadow-run a challenger routing policy and promote it on measured wins** (Team intelligence, medium)
       Run the learned-weights router in shadow beside the live deterministic one for a few weeks: log both hypothetical batches nightly, compare settled outcomes on overlapping picks, and promote the challenger only on a measured margin. This is policy-level science applied to the team itself, using the same proof ledger that judges page changes. Requires the persisted decision record.
- [ ] 316. **Upgrade the commerce strategist from tool-gap stub to a full asset router** (Team intelligence, medium)
       emitCommerceAssetOpinion in specialist-opinions.ts only speaks when a missing_tool gap exists, so the what-should-this-become seat is nearly always empty. Give it deterministic routing across article versus list versus tool versus collection using query-intent.ts plus the competitor page shape already in the EvidencePacket, so it argues asset type on every create-class move as the P8 comment always intended.
- [ ] 317. **Add Event and date-anchored schema to Persian holiday pages, refreshed annually** (Technical SEO, medium)
       The chaharshanbe suri case proved date intent: 194 impressions for a 2026 date landed on a definition page. Detect holiday pages, emit Event JSON-LD with the current-year date plus an answer-block line stating the date, and refresh it annually via the nightly loop. Attacks a recurring seasonal demand class (Nowruz, Yalda, Sizdah Bedar) where Google wants an entity with a date, not prose.
- [ ] 318. **Add Recipe schema to Persian food pages for rich-result thumbnails** (Technical SEO, medium)
       Iranopedia food pages are Recipe-shaped but carry only Article markup. Detect ingredient-list and step patterns in body_paragraph_sample and h2_list, then draft Recipe JSON-LD (ingredients, cookTime, image) through composeSchema with the schema-validator gate. Recipe rich results earn image thumbnails in SERPs, a direct CTR lever no title tweak can match.
- [ ] 319. **Audit broken internal links and ship the corrected href as a one-click Wix fix** (Technical SEO, medium)
       PageSnapshot.internal_links already stores every href plus anchor text, but nothing resolves them against live status. Cross-check each internal href against the known page set and a status probe; flag links pointing at 404s or redirecting URLs with the exact source page and anchor, pushable via the Wix data client. Completes the link-graph trio next to the orphan-page and internal-link-opportunity triggers.
- [ ] 320. **Trace redirect chains hop by hop instead of hiding them with redirect follow** (Technical SEO, medium)
       Every fetch in in-process-scan.ts and polite-fetch.ts uses redirect: follow, so a 3-hop chain looks like a clean 200. Add a chain-audit pass with redirect: manual recording each hop, status (301 vs 302), and final URL for sitemap plus internally linked URLs, flagging chains of 2+ hops and 302s that should be 301s. Chains dilute link equity and waste crawl budget silently.
- [ ] 321. **Turn dead URLs with recent search demand into recover-or-redirect moves** (Technical SEO, medium)
       Join gsc_daily_page_totals impression history against live status probes: any URL that earned impressions in the last 90 days and now 404s or redirects is lost equity with proof of demand, surfaced with the number in the headline (I found 340 monthly impressions pointing at a dead page). The bad-http-status trigger only watches tracked pages; this watches everything GSC ever credited.
- [ ] 322. **Add a crosshair scrubber and rich tooltip to the scoreboard chart, touch and mouse** (Today dashboard, medium)
       The hero chart in src/app/(shell)/scoreboard-section.tsx is server-rendered SVG whose only affordance is native title tooltips, which do nothing on mobile. Add a thin client overlay: hover or drag shows date, daily clicks, 7-day average, and any changes shipped that day, with arrow-key stepping; tapping a ship marker scrolls to the matching Measuring row. The hover pattern already exists in components/viz/area-chart.tsx to borrow from, and the chart is the most-touched pixel on the page.
- [ ] 323. **Add a goal pace line to the scoreboard so every morning answers on pace or behind** (Today dashboard, medium)
       Let the operator set a monthly clicks target (or auto derive one from trailing 28-day growth) and draw the required trajectory on the hero chart in src/app/(shell)/scoreboard-section.tsx. Extend buildScoreboard in src/domains/scoreboard/scoreboard.ts with a pure pace computation and change verdictLine to say on pace or behind by N clicks per day. Turns the chart from a rear-view mirror into a race the owner can win.
- [ ] 324. **Add a lead story module that picks the one most important thing each morning** (Today dashboard, medium)
       Today renders parallel bands with no editorial hierarchy. Add a deterministic picker (new pure module in src/domains/today) that ranks candidate stories: anomaly beats verdict landed beats plan ready beats demand spike, and renders one two-sentence narrative card with exactly one action button above the scoreboard in src/app/(shell)/page.tsx. World-class dailies lead with a headline; Beacon currently leads with a greeting.
- [ ] 325. **Add a market news band: what competitors published this week against your topics** (Today dashboard, medium)
       loadCompetitorMoves in src/domains/competitor-intel/load-moves.ts already computes competitor moves with evidence but Today never mentions rivals. Render a compact band in war-room-sections.tsx: rival X published 2 new pages on your topics this week, one overlaps the page we are improving tonight. A war room without enemy movement is a dashboard; keep it to two rows with the existing junk gating.
- [ ] 326. **Add a seasonal countdown planner with lead-time math** (Today dashboard, medium)
       The demand band already flags seasonal trends (res.trends with the seasonal flag in war-room-sections.tsx) but only as a chip. Compute weeks-to-peak for seasonal keywords in src/domains/demand/load-demand-opportunities.ts and render a countdown row: Yalda searches peak in 22 weeks, the page should be live 6 weeks before, draft it by November 3. Encyclopedic sites live and die on seasonal spikes, and pages need indexing lead time.
- [ ] 327. **Add a what-is-bleeding band naming the top decaying pages and clicks lost per week** (Today dashboard, medium)
       loadTopDecliningPagesForTenant already exists in gsc-page-queries.ts but Today never shows decay. Render up to two rows in war-room-sections.tsx with the weekly click loss number and a link to the matching worklist change if one exists. Defense is half of what an SEO team does; Today currently only shows offense.
- [ ] 328. **Add year-over-year context to the week verdict so seasonal dips stop reading as failure** (Today dashboard, medium)
       buildScoreboard compares last 7 vs prior 7 only; fetch 400 plus days via loadDailyTotalsForTenant and append a YoY clause to verdictLine in src/domains/scoreboard/scoreboard.ts: down 8 percent vs last week but up 40 percent vs this week last year. For a seasonal site like Iranopedia (Nowruz, Yalda) this is the difference between panic and patience, and it is what a hired analyst would say first.
- [ ] 329. **Build a start-my-day guided routine: three steps, ten minutes, done** (Today dashboard, medium)
       Add a Start my day button in page.tsx that walks the operator through an ordered overlay: 1 read any landed results, 2 approve or adjust tonight's picks, 3 apply anything pending in Wix, each step deep-linking to the exact card and checking itself off from existing statuses in today-view.ts and the execution checklist. The worklist has a 30-minute mode; Today, the page built for a ritual, has no ritual. Habit design is what makes a $250/mo tool indispensable.
- [ ] 330. **Draw a 14-day projection cone on the scoreboard from trend plus measuring forecasts** (Today dashboard, medium)
       Extend the chart in scoreboard-section.tsx 14 days past reportedThrough with a shaded band: baseline from the 7-day rolling slope, upper bound adding the forecastRange of measuring changes from src/domains/experiments/pick-expectations.ts. Caption honestly: if the 3 measuring changes land mid range, expect about X clicks per day by July 15. No SEO tool gives a small business owner tomorrow's picture with the team's bets priced in.
- [ ] 331. **Plot GA4 revenue per day as a money series on the scoreboard** (Today dashboard, medium)
       loadGa4PageRevenueForTenant exists in ga4-page-values.ts but no Today surface renders revenue over time. Add an optional toggle series to the chart in scoreboard-section.tsx and a header stat: organic sessions drove about $X this week. Business owners keep score in dollars; clicks are our proxy, not theirs.
- [ ] 332. **Surface rank threshold crossings: pages that entered or fell out of top 3 and top 10** (Today dashboard, medium)
       GSC positions already flow through src/domains/recommendation-intelligence/gsc-page-queries.ts (risesForPage, declinesForPage). Detect week-over-week position crossings of the 3 and 10 boundaries and render a small war-room band in war-room-sections.tsx: the singers page entered top 3 for iranian singers, the flags page slipped off page one. Threshold crossings are step changes in CTR, the most decision-relevant rank events, and they read like team news.
- [ ] 333. **Track AI citation topic coverage over time, not just today's snapshot** (Today dashboard, medium)
       The war room's AiCrawlerSection shows cites-you-or-not per topic from readAllCachedLlmMentions (src/domains/serp/dataforseo-llm-mentions.ts) but keeps no history. Persist a weekly cited-on-X-of-Y snapshot per tenant and render the trend next to the AI band: cited on 5 of 12 topics, up from 3 last month. AEO progress is invisible without a time axis, and this is the number Profound charges for.
- [ ] 334. **Archive what verify-live actually saw and open it from the verified-live chip** (Trust and receipts, medium)
       live-verification.ts fetches the live page HTML and throws it away after matching. Persist the observed title, h1, meta, the matched fragment, HTTP status, and fetched-at on the execution record, and make the green verified-live chip on proof/page.tsx expand to that receipt. The chip stops being an assertion and becomes evidence you can read.
- [ ] 335. **Corroborate position wins with one live Google check stamped on the card** (Trust and receipts, medium)
       When pickProofMetric in measure.ts judges a change on position and the verdict is a mature win, spend one capped DataForSEO SERP call via dataforseo-serp.ts (the $50 fail-closed ledger already governs spend) and stamp live Google check today: number 4, baseline was number 7. GSC average position and a live SERP agreeing is a two-source proof no dashboard competitor shows.
- [ ] 336. **Enforce numeric fidelity on roundtable debate lines** (Trust and receipts, medium)
       Voices render humanized free text via humanizeDebateLine from src/domains/demand-graph/debate-summary. Require every number in a debate line to be interpolated from evidence packet fields, mirroring the numeric-fidelity firewall the drafter already has, and pin it with a source-level guard test in the proof-jargon-guard.test.ts style. A single invented number in a voice quote would poison the whole team metaphor.
- [ ] 337. **Exclude and disclose unreliable data days in both baselines and measurement windows** (Trust and receipts, medium)
       src/domains/truth/data-quality.ts emits DataQualityFlag per broken day but no operator surface renders it, and the 28-day baseline behind metricsLine in proof/page.tsx can silently include GSC gaps. When a window or baseline intersects flagged days, exclude them from the read and disclose it on the card: 3 unreliable data days excluded from this reading, baseline excludes 2 days with missing Search data. The Ritz Apr 7-12 defect class becomes a visible-and-handled event instead of a silent verdict poisoner, and before/after claims are only as strong as the before.
- [ ] 338. **Join LLM and SERP spend receipts to the outcomes they funded** (Trust and receipts, medium)
       settings/spend/page.tsx shows spend but not what it bought. Link each budget-ledger entry (budget-ledger-supabase.ts) to the drafts and picks it funded and their eventual verdicts: this $0.42 wrote 3 drafts, 2 shipped, 1 won. A money-in to results-out audit is exactly the receipt a $250/mo buyer asks for at renewal time.
- [ ] 339. **Suffix small-sample percentages app-wide through format-metric** (Trust and receipts, medium)
       src/lib/format-metric.ts is already the shared formatter with tests. Add an optional denominator argument so any percentage computed from fewer than about 20 events renders (from 6 visits, small sample) everywhere at once, generalizing the one-off lowVolume guard in proof/page.tsx. Add a guard test in the proof-jargon-guard.test.ts style so new surfaces cannot ship bare percentages.
- [ ] 340. **Build a Ricos serializer so rich-content body fields render properly in Wix** (CMS push depth, medium)
       Wix CMS RICH_CONTENT fields store Ricos JSON nodes, but the push path writes plain strings, so a body write into a rich field renders wrong or gets rejected. Build a bounded markdown-to-Ricos converter (paragraphs, headings, bold, links, lists) in src/lib/connectors/wix, detect the field type from wixListDataCollections discovery, and serialize accordingly. Prerequisite for the body-append route on real Iranopedia body fields.
- [ ] 341. **Extract a PublishAdapter interface with wix_cms as the first registered adapter** (CMS push depth, medium)
       executePush is 690 lines of Wix-specific routing inlined with the safety spine (caps, snapshots, ledger, destructive guards). Define a PublishAdapter contract (resolveTarget, readField, writeField, insertItem, verifyHint) in src/domains/push/adapters, keep the spine generic, and register wix_cms as the first implementation. Load-bearing groundwork that makes WordPress, Webflow, and Shopify adapters an M each instead of an L each.
- [ ] 342. **Populate reference fields on cluster creates so new pages join the link graph** (CMS push depth, medium)
       cluster-factory.ts writes only text fields, so a new recipe page lands with no category or related-items references, the dynamic template renders no related links, and the page starts as an orphan. Extend ClusterPlan with reference-field specs, query the target collection for best-matching related items (the Persian tokenizer similarity already exists), and set multi-reference fields on insert. Orphan pages are the classic reason programmatic pages never rank.
- [ ] 343. **Roll back an entire day's applied plan in one action** (CMS push depth, medium)
       Each pushed item has its own snapshot, but if a whole daily plan turns out misguided (wrong page set, template bug), the operator must revert item by item through /diagnostics/wix. Add a plan-level revert that iterates the plan's ItemExecutionRecords, builds revert edits from each snapshot via findLatestSnapshotForEdit, and ships them through executePush serially with one summary receipt. Makes big swings feel safe to approve.
- [ ] 344. **Build a Set aside view: restore snoozed, skipped, and dismissed items** (Worklist UX, medium)
       Three flows make work vanish with no recovery: Not relevant records dismissed via respondToRecommendation, skipItemViaRpc marks plan items skipped, and rankChanges in src/domains/changes/strategy.ts drops status skipped from every view. Add a Set aside section behind a small link under the list in changes-list-client.tsx listing all three with a Restore action. A misclick is currently a permanent loss of a ranked opportunity, a trust hole for a $250/mo tool.
- [ ] 345. **Bundle multiple changes on the same page into one visit** (Worklist UX, medium)
       CanonicalChange ids are per page-family, so one page can appear as three separate rows (title, answer, link) scattered through the ranking, and m.also already hints at co-located work. Collapse same-pagePath rows into one while-you-are-on-this-page bundle with a combined effort estimate and a single expanded view. One Wix visit per page instead of three is a real minutes-saved multiplier.
- [ ] 346. **Carry unfinished items into tomorrow's plan on Finish for today** (Worklist UX, medium)
       completeDailyPlanAction closes the batch even when items sit unapplied; they fall back into the general pool with no memory. When s.left is greater than 0, have the finish flow ask carry these into tomorrow, and persist a carryover hint the planner reads in buildTodayExperimentPreview to re-select those candidates first with fresh controls and validation. Unfinished work should follow the operator, not evaporate.
- [ ] 347. **Focus mode: triage one card at a time, inbox-zero style** (Worklist UX, medium)
       Add a Review one by one toggle to ChangesListClient that presents the ranked queue as a single full-width card (reusing MoveCard) with three big verbs, Ship, Snooze, Not relevant, advancing on action. Long lists invite scanning paralysis; a forced-sequence mode converts 20 minutes of scrolling into a decisive pass. Pure client over the already-ranked visible array, no new data.
- [ ] 348. **Make the quality flag actionable: reasons plus one-click re-draft** (Worklist UX, medium)
       A flagged row in changes-list-client.tsx shows only a triangle whose tooltip is qualityNote, and the MoveCard pill surfaces just reasons[0]. Expand the flag into a small panel listing every failed check from reviewRecommendation and draft-quality, with a Regenerate draft button wired to the existing in-place regeneration path from the prepared-output quality gate. A flag the operator can fix in one click is worth ten flags they can only stare at.
- [ ] 349. **Multi-select rows with a floating bulk-action bar** (Worklist UX, medium)
       changes-list-client.tsx has zero multi-select; every triage verb is one row at a time. Add a checkbox per Row plus shift-click ranges, and a floating bar with Snooze all, Not relevant all, Copy all pastes, and Add all to tonight. The actions already exist per item (respondToRecommendation, clipboard builders); this is pure selection plumbing that multiplies triage speed on a 200-row list.
- [ ] 350. **Offer Use what is live when verification finds different but acceptable text** (Worklist UX, medium)
       The failure box already shows expected vs observed (daily-experiments-section.tsx failure state). When observed text exists and passes the same quality gate (reviewRecommendation in src/app/(shell)/daily-experiments-data.ts), offer one click, Track what is live instead, which re-runs markDailyExperimentAppliedAction with editedText set to the observed text. Right now an operator who improved the wording inside Wix gets punished with a dead end.
- [ ] 351. **Show a plan diff whenever a plan is rebuilt or refreshed** (Worklist UX, medium)
       Stale-plan auto-recovery in acceptDailyExperimentPlanAction silently rebuilds the preview (reason plan_refreshed), and Suggest different ones replaces it wholesale, so the operator re-reads five cards to find what changed. Plans are content-addressed records; diff old vs new selected by page plus lever and render kept, replaced, and why lines at the top of the preview. Re-review time drops from minutes to seconds.
- [ ] 352. **Watch Reddit and Quora citations and propose owned answers on the exact threads** (AI visibility (AEO), small)
       Perplexity leans heavily on UGC. Filter citation rows and llm-mentions results to reddit.com and quora.com, list the specific threads engines cite for tenant topics, and generate a go answer this thread move with suggested factual copy and a link to the owned page, tracking whether the thread's citation later carries the owned domain. Attacks the one surface on-page work cannot reach.
- [ ] 353. **Add a nightly experiment janitor for abandoned reservations and stuck limbo** (Autonomy, small)
       A plan accepted but never applied holds its comparison-page reservations indefinitely (daily-experiment-plan-store.ts), starving the control pool, and an item whose apply or verification never completed counts down on the in-flight band forever. A nightly janitor releases reservations and closes plan rows older than 72 hours with no apply, and closes execution-state.ts records stuck beyond 7 days with an honest I could not confirm this went live plus the next step. Keeps the experiment engine and the in-flight band truthful with zero operator effort.
- [ ] 354. **Auto-heal PostgREST schema cache instead of silently using file fallback** (Autonomy, small)
       Stores treat PGRST205 as table-missing and drop to the json file fallback, which on Vercel means data quietly stops persisting. When PGRST205 lands in prod, fire NOTIFY pgrst, reload schema through the admin client in src/lib/persistence/supabase.ts and retry once; if it persists, file an Ops card that production is running on file fallback. Turns a documented recurring gotcha into a self-healing path.
- [ ] 355. **Preflight required env and cron registration, fail loud on config drift** (Autonomy, small)
       A missing CRON_SECRET makes measure-due 503 forever, and missing OpenAI or DataForSEO creds silently no-op whole subsystems. Add one nightly preflight asserting required prod envs and that vercel.json cron paths respond, filing an Ops card on any gap. Config drift is the most common way autonomy dies in single-operator products, and today it is entirely silent.
- [ ] 356. **A "why should I trust this" page** (Carry-over, small) [CARRY-OVER 114]
       (one static, beautiful explainer of the method: comparisons, windows, verdicts, caps) linked from every How we know footer.
- [ ] 357. **GSC indexing queue polish** (Carry-over, small) [CARRY-OVER 91]
       after verify-live, one screen lists the URLs to request indexing for, ordered, with copy buttons and "mark done" (quota-aware, manual by design).
- [ ] 358. **Name the product's own limits** (Carry-over, small) [CARRY-OVER 117]
       pages with thin evidence say "The team does not have enough evidence here yet" instead of a weak recommendation (the eligibility machinery exists; the COPY must say it proudly).
- [ ] 359. **Never block the shell on data** (Carry-over, small) [CARRY-OVER 97]
       every band already streams; ensure the header + scoreboard skeleton paint in the first flush (audit the Suspense boundaries).
- [ ] 360. **Row-level freshness** (Carry-over, small) [CARRY-OVER 63]
       "evidence read 2h ago" chip; anything stale (>14d SERP, >7d snapshot) gets an amber "re-check before shipping" hint with the one-click refresh.
- [ ] 361. **Weekly keyword-universe refresh** (Carry-over, small) [CARRY-OVER 83]
       (one $0.075 batch per week per tenant, budget-gated) so volumes never silently go stale; freshness shown in the how-we-know brief.
- [ ] 362. **Add a tenant-site deadman: uptime, DNS, SSL expiry, and domain expiry probes with paging** (Completeness critic, small)
       The planned traffic smoke alarm is GSC-lagged by 2 to 3 days; a down site, an expired certificate, or a lapsed domain registration needs minutes-level detection. Run an external probe every 15 minutes per tenant (HTTP 200, DNS resolution, SSL days-remaining, WHOIS expiry monthly) from a tiny cron in the existing cron framework and page through the attention band and the planned dead-man banner. Domain expiry alone has killed more small-business SEO than any algorithm update.
- [ ] 363. **Auto-discover true Google competitors from the top-50 query set with Labs serp_competitors** (DataForSEO depth, small)
       The competitor list came from Profound AI co-mentions; the Google-side rival set was never measured. Run serp_competitors quarterly on the tenant's top-50 GSC queries and reconcile with competitors/universe-read.ts. Domains that rank everywhere on your queries but never appear in AI answers, and vice versa, are strategy-defining facts for the roundtable.
- [ ] 364. **Convert rank talk to visit talk with bulk_traffic_estimation on wins and steal-this cards** (DataForSEO depth, small)
       Labs bulk_traffic_estimation prices estimated monthly organic traffic for up to 1000 domains or pages in one cheap call. Rank MoveCard steal-this competitors by what their page earns, and convert projected rank moves into expected-visits numbers for pick-expectations.ts forecast ranges. Beacon speaks money and customers; this turns rank deltas into visit deltas with a named source.
- [ ] 365. **Record the API's actual cost field and reconcile nightly against the account balance** (DataForSEO depth, small)
       Every DataForSEO response carries tasks[].cost, but runSerpQuery ledgers a conservative flat estimate (SERP_COST_USD 0.003 vs real 0.0006-0.002), so the $50 cap trips on phantom spend and wastes 30-60 percent of real headroom. Record body cost when present and add a nightly reconcile against GET /v3/appendix/user_data into budget-ledger-supabase.ts. Same cap, meaningfully more data.
- [ ] 366. **Run a monthly depth-30 sweep on striking-distance queries for true positions past page one** (DataForSEO depth, small)
       runSerpQuery accepts a depth option but every caller uses the default 10 (verified line 234), so pages ranking 11-30 are invisible exactly where the cheapest wins live. Sweep the GSC striking-distance query set at depth 30 monthly and feed true positions into worklist outcome ranges and pick-expectations.ts forecasts. Positions 11-20 are the classic one-edit-to-page-one inventory.
- [ ] 367. **Wire Google Trends explore rising and breakout queries into the trend radar** (DataForSEO depth, small)
       keywords_data/google_trends/explore is cheap and returns trend curves plus related rising and breakout queries. Run it weekly on top topic clusters and map rising queries through keyword-match.ts to existing pages (refresh lever) or create-page candidates (through the SERP verdict gauntlet). Breakout queries are the only demand signal that arrives before volume data does; trend-radar.ts currently has no source that leads the market.
- [ ] 368. **Find broken competitor pages that still rank or earn AI citations** (frontier, small)
       Status-check competitor URLs already collected by competitor-citations-loader.ts using polite-fetch.ts in src/domains/competitor-intel; when a URL that appears in cached SERPs or citation records now 404s or redirects to junk, open a high-conviction Move to publish the replacement resource, since AI engines and Google must re-source that answer. Dead-competitor demand is the cheapest ranking win available and needs no link data.
- [ ] 369. **Fit a per-site CTR curve from our own GSC data instead of the shared curve** (frontier, small)
       Fit position-to-CTR monthly from Iranopedia's own GSC query data segmented by branded vs non-branded, and swap it into the forecast math behind pick-expectations.ts in src/domains/experiments. Forecasts become honestly ours, and the delta between our curve and the generic curve is itself a diagnosable finding since weak titles depress the whole curve.
- [ ] 370. **Harvest People Also Ask questions into the demand graph as question nodes** (frontier, small)
       Once the SERP parser captures PAA text, ingest questions as demand nodes in src/domains/demand-graph/build-graph.ts alongside the Profound fanouts already loaded by load-fanout-seeds.ts, dedupe against existing answer blocks, and let the router propose FAQ and answer-block Moves with the question verbatim. This is free demand data already paid for in every cached SERP call.
- [ ] 371. **Show ad-equivalent dollar value of rankings using the CPC cache** (frontier, small)
       The DataForSEO keyword cache in src/domains/serp/dataforseo-keywords.ts already returns CPC alongside volume. Multiply per-query clicks from GSC by CPC to get 'your rankings replaced $X/mo of Google Ads', trend it on the scoreboard, and stamp each win card with the ad-equivalent value it added, extending money-model.ts in src/domains/demand-graph. Zero new spend, pure persuasion for the $250 question.
- [ ] 372. **Track Google Knowledge Graph presence for the tenant and its key entities** (frontier, small)
       The KG Search API is free and entity presence correlates strongly with AI citation trust. Nightly, query the API for the brand plus tracked entities (src/domains/tracked-entities), store presence and result score over time, and emit Wikidata and schema Moves when key entities are absent, wired into the existing entity-schema.ts work in src/domains/demand-graph. Kalicube charges consultancies for exactly this loop; Beacon runs it at zero marginal cost.
- [ ] 373. **Learn from dismissals: demote candidate classes the operator consistently skips** (Learning loop, small)
       opportunity-dismissal-store.ts persists skip and done per opportunity key but nothing aggregates it. Compute dismissal rates per lever, pageFamily, and evidence-strength band, apply a small bounded demotion to matching future candidates, and add the honesty line you have skipped 6 of the last 7 picks like this. A zero-LLM taste model from data already being written on every dismiss click.
- [ ] 374. **Bilingual Persian directive: include the native-script term in answers and metas** (LLM engine, small)
       Iranopedia demand includes Farsi and Finglish queries and the Persian tokenizer already shipped, but no drafter prompt ever asks for native script. Detect Persian-origin target queries and add a directive to ANSWER_BLOCK_SYSTEM and ATOMIC_EDIT_SYSTEM to include the Persian script form with transliteration, for example chaharshanbe suri with its Persian spelling. AI assistants answering bilingual prompts strongly prefer sources carrying both forms, which is a direct citation lever.
- [ ] 375. **Bill LLM spend from real usage tokens instead of chars divided by 4** (LLM engine, small)
       estimateCostUsd in structured-drafter.ts, llm-answer-block.ts and siblings estimates output cost from visible text length, but gpt-5-mini bills reasoning tokens that never appear in content, so recordSpend systematically under-counts and the cap in adjudicator-budget.ts is looser than it claims. The chat completions response already includes usage.prompt_tokens and usage.completion_tokens. Parse them in defaultComplete and every sibling caller and pass real cost to recordSpend.
- [ ] 376. **LLM call log with silent-fallback health telemetry** (LLM engine, small)
       The gpt-5-mini timeout incident proved the failure mode: LLM passes silently fall back to deterministic for weeks and nobody notices. Log every call from callStructuredLLM, llm-judge.ts and llm-expert-strategist.ts into an llm_call_log (kind, model, latency, retried, outcome, failure reason) and surface a one-line health readout on /settings/spend: strategist fell back 40 percent of nights this week. This is the smoke detector for the product's smartest layer.
- [ ] 377. **Numeric-repair micro-pass instead of rejecting the whole draft** (LLM engine, small)
       The CREATE_PAGE_SYSTEM comment in structured-drafter.ts admits invented counts are the number one rejection reason for culture-topic briefs. When runContentFirewalls fails only on invented_numbers, send a tiny follow-up (here is your draft, remove or qualify these ungrounded figures) instead of a full regenerate. Cheaper than the current retry and it preserves an otherwise good draft.
- [ ] 378. **Flag novelty decay when a 7-day win fades by day 28** (Measurement science, small)
       summarizeVerdict silently overwrites the 7d read with the 28d one, losing the trajectory. Compare adjustedLift across the 7, 14, and 28 day windows already stored on the record and add a faded flag when the early lift decayed more than half, with a plain line: this started strong and faded, likely a freshness boost, not a durable win. Feeds the learning layer the critical distinction between durable levers and novelty effects.
- [ ] 379. **Window completeness guard against gappy GSC data** (Measurement science, small)
       The cumulative-subtraction reads in gsc-window.ts assume every day in the window exists; a sync gap silently deflates the post window and biases toward lost. Count distinct dates present in gsc_daily_page_totals for each window versus expected days and route incomplete windows to the existing blocked_data maturity state instead of computing a verdict on partial sums. The is_final watermark protects the end of the window but not holes in the middle.
- [ ] 380. **Check robots.txt for AI-crawler blocks in the first audit** (Onboarding and cold start, small)
       The cold-start scan fetches robots.txt via the polite-fetch robotsCache (src/domains/competitor-intel/polite-fetch.ts) but never inspects it for GPTBot, ClaudeBot, PerplexityBot, or Google-Extended disallows, and never checks llms.txt. Add a deterministic day-0 check that flags AI assistants cannot read your site with the exact lines to change. Free, deterministic, and often the single highest-leverage AEO fix on a stranger site.
- [ ] 381. **Detect the CMS platform at onboarding and set publishing expectations** (Onboarding and cold start, small)
       Detect Wix, WordPress, Shopify, and Squarespace from the meta generator tag and markup fingerprints inside deriveBusinessProfile (src/domains/onboarding/derive-business-profile.ts) and store it on BusinessConfig. Wix detection unlocks the honest pitch that Beacon can publish approved changes, while WordPress or Shopify sets copy-paste expectations and tailors the connector checklist. One meta tag read changes the perceived product from advice to hands-on-keyboard team.
- [ ] 382. **Generate a ready-to-paste schema starter pack from wizard data** (Onboarding and cold start, small)
       When the cold-start crawl finds no Organization or LocalBusiness JSON-LD, compose it from fields onboarding already holds: name, phone, address, and the sameAs social profiles deriveBusinessProfile extracted. The composeSchema infrastructure already emits full JSON-LD; this is a wiring job yielding a guaranteed riskless first win with one-click Wix push where connected.
- [ ] 383. **Snapshot a day-0 SERP baseline at launch as the before photo** (Onboarding and cold start, small)
       At launch, run bounded runSerpQuery calls for the top 5 derived service and location terms and persist positions into the existing SERP cache tables. Every later diff-in-diff then has a pre-Beacon baseline even for tenants who never connect GSC, at about $0.015 per tenant inside the existing fail-closed ledger (src/domains/serp/dataforseo-serp.ts).
- [ ] 384. **Merge the two divergent CTR curves and pin the survivor with tests** (Code health, small)
       src/domains/recommendation-intelligence/ctr-curve.ts models position 1 at 30 percent with a 0.5 percent tail; page-surgeon/expected-ctr.ts models position 1 at 28 percent with a 2 percent floor, so the same query can show different clicks-at-stake numbers on the worklist versus Page Surgeon output. Keep ctr-curve.ts, delete expected-ctr.ts, re-point the packet assembly and snippet-deficit gate, and land a property-style test file (monotonic decreasing, clamped, tail beyond 15) since the interpolation math currently has zero coverage.
- [ ] 385. **Add a protect-revenue objection to the GA4 teammate** (Team intelligence, small)
       emitGa4Opinion in specialist-opinions.ts only amplifies and never objects. On pages with measured dollarValue, risky lever families like title and H1 rewrites should draw a protect-revenue downgrade plus a higher consensus requirement, so the team cannot bet a converting page on a cosmetic CTR play without extra agreement. Small change, directly protects money pages.
- [ ] 386. **Derive teamScoreMultiplier from the debate itself, not the adjustedScore ratio** (Team intelligence, small)
       team-review.ts line 144 sets the planner multiplier to adjustedScore divided by baseScore, so a unanimous five-voice yes and a lone lukewarm yes both come out 1.0 whenever no score multiplier fired. Fold vote margin, voice count, and objection count into the bounded 0.5 to 1.5 multiplier so scoreCandidate in daily-experiment-planner.ts actually feels the strength of the room.
- [ ] 387. **Turn consensusPct into a support-minus-dissent agreement score** (Team intelligence, small)
       summarizeSpecialistDebate in debate-summary.ts averages only supporting voices, so one lukewarm supporter reads like consensus and loud dissent costs nothing. Compute weighted support minus weighted dissent normalized to 0 to 100, tag contested picks, and let the planner tie-break away from contested ones. Small honest fix to a number the operator already reads.
- [ ] 388. **Shade the GSC still-arriving days on the chart so the right edge never fakes a crash** (Today dashboard, small)
       GSC reports with about a 3 day lag; the chart in scoreboard-section.tsx draws whatever rows exist, so partial trailing days render as a scary dip. Hatch or fade the non-final days and label them data still arriving. A half-day honesty fix that prevents the most common false alarm a daily user will ever see.
- [ ] 389. **Take over the top of Today with the result on days a verdict lands** (Today dashboard, small)
       When a ledger row's verdict finalized today (measure-lifecycle.ts knows checkpoint maturity), render a hero variant above the scoreboard: the cheetah answer block verdict is in, +34 percent clicks, a win, with before and after numbers inline. Verdict days are the product's payoff moments and currently surface as one row inside Measuring or on /proof. Detect via shippedAt plus window math against today's date.
- [ ] 390. **Add a cross-source agreement chip on every measured outcome** (Trust and receipts, small)
       The proof card already carries both the Search windows (measure.ts) and the GA4 trafficOutcome (traffic-outcome.ts). Compute deterministic agreement and render 2 independent sources agree on aligned rows, or the honest split: Search says up, Analytics flat, we trust Search here because the change targeted the results page snippet. Naming a disagreement between sources is a trust surface competitors hide.
- [ ] 391. **Make measurement overlap a visible contamination chip with cross-links** (Trust and receipts, small)
       detectMeasurementOverlaps in measurement-maturity.ts already computes when two ships share a page and window, but the result only tempers tone. Render an explicit chip on both cards: another change shipped on this page 9 days later, we cannot fully separate them, linking each card to the other. Naming contamination beats quietly softening the color.
- [ ] 392. **Audit every factory-created page against its rendered URL before calling it done** (CMS push depth, small)
       cluster-factory.ts validates the JSON fields but nobody checks the rendered dynamic page: an unbound template field silently renders an empty section on the live URL. After a create push succeeds, fetch the new URL, verify each written field's text appears in the rendered HTML, and flag missing fields on the receipt with the exact field name. Catches template-binding gaps the day the page ships, not when GSC shows zero impressions.
- [ ] 393. **Enforce a per-URL change cooldown inside executePush to protect measurement windows** (CMS push depth, small)
       The worklist shows a compounding-edit warning, but nothing structural stops a second push to the same URL two days into a 7-day measurement, which contaminates the diff-in-diff attribution. Add a cooldown check in executePush that consults the push ledger for a recent pushed row on the same target_url and refuses with the days remaining, plus an explicit operator override flag. Moves measurement hygiene from advice into the same fail-closed tier as the daily cap.
- [ ] 394. **Make the post-push probe element-scoped instead of a whole-page substring match** (CMS push depth, small)
       probeLiveText in push-service.ts normalizes the entire HTML and searches a 120-char prefix, so a title change can false-verify by matching body text and a meta change is nearly unverifiable. live-verification.ts already parses title, meta description, and h1 element-scoped with an empty-shell guard. Route the push probe through that parser keyed by action_type so verified_live means the right element changed.
- [ ] 395. **Ping IndexNow and refresh the GSC sitemap after create and edit pushes** (CMS push depth, small)
       New cluster pages and changed titles wait for Google's organic recrawl, which can take days on a small site. After a successful push, submit the URL to Bing IndexNow (free key file) and re-submit the sitemap via the existing GSC OAuth connection. Small effort that directly shortens the gap between shipping and measurable movement, which also tightens every proof window.
- [ ] 396. **Build GSC deep links: prefilled URL inspection per item plus performance views per claim** (Worklist UX, small)
       SEARCH_CONSOLE_URL in daily-experiments-section.tsx links the bare console, leaving the operator to paste URLs themselves. Build src/lib/url/gsc-deep-link.ts composing both the inspection link (search.google.com/search-console/inspect with resource_id and url) for every applied change and the performance URL with page filter, query filter, and exact date range for proof target-query chips and worklist evidence lines. Every claim becomes one click from Google's own UI, the strongest external verification available.
- [ ] 397. **Explain the rank: a why-number-N breakdown on every row** (Worklist UX, small)
       strategyScore in src/domains/changes/strategy.ts computes base impact plus evidence bonus plus ready bonus minus effort and risk, but the operator only sees the resulting order. Export a scoreBreakdown(c, strategy) returning the labeled terms and render it in a popover on the rank chip, for example impact 320, strong evidence +300, ready now +200, effort -6. When the operator can audit the order they stop re-sorting it in their head.
- [ ] 398. **Give Not now a duration menu that auto-returns the change** (Worklist UX, small)
       MoveCard snooze in src/app/(shell)/today-moves-card.tsx records status deferred with no window, yet the store and display logic already support deferUntil (src/domains/product/recommendation-response-store.ts line 35, src/domains/recommendations/display-state.ts). Add a tiny menu: 3 days, next week, after this page's measurement settles, and write deferUntil so the row reappears automatically. Snooze becomes a scheduling primitive instead of a soft dismiss.
- [ ] 399. **Group the execution checklist by Wix surface, not plan order** (Worklist UX, small)
       checklist.items render in plan order, so the operator ping-pongs between Wix SEO Basics and the CMS body editor. Group ExecutionChecklistView by the lever's Wix surface (meta and title under SEO panel, answer_block and internal_link under Page body) with a header line: you are already in SEO Basics, knock out these 3. wixInstructions in execution-checklist.ts already encodes the surface per lever; this is a sort plus section headers mirroring how the tool is actually used.
- [ ] 400. **Turn Tonight's 30 minutes into a budget slider and pack by value per minute** (Worklist UX, small)
       The tonight packer in changes-list-client.tsx (lines 187-196) sorts only by selectedForToday then greedily fills 30 minutes in ranked order, ignoring value density, and 30 is hardcoded. Add a 15/30/60/120 slider and pack by impactScore divided by estimatedEffortMinutes so the budget buys the most outcome. Also show what did not fit: 2 more if you have 15 extra minutes.
- [ ] 401. **Build one Card primitive with named slots and migrate the 133 ad-hoc wrappers** (Design, large)
       133 files hand-roll rounded-border card shells: components/viz/kpi-card.tsx uses rounded-lg border-border/60 bg-surface-raised/40 while war-room-sections.tsx uses rounded-2xl border-gray-200 bg-white and Today measuring rows use rounded-lg border-gray-100. Create components/ui/card.tsx with Card, CardEyebrow, CardMeta, and CardFooter encoding one radius, one border, one padding scale, then migrate Today, worklist, and proof. Every screen instantly shares the same card anatomy.
- [ ] 402. **Build a tools factory that ships embeddable calculators as link magnets** (frontier, large)
       Tool-intent keywords (converter, calculator, meaning lookup) sit unclaimed in the DataForSEO keyword cache. Ship small client-side tools embeddable in Wix pages: a Persian calendar date converter, a Finglish transliterator on the shipped tokenizer, a name-meaning lookup from existing entity data. Tools earn passive links and rank for utility queries for years, and nothing in the current Move taxonomy can produce one.
- [ ] 403. **Tool-calling drafter that pulls cached evidence mid-call instead of prompt stuffing** (LLM engine, large)
       Today every call site guesses which evidence to pre-stuff into the user prompt, so drafts are grounded in whatever the caller happened to pass, as the outline: [] gap proved. Give the drafter function tools backed by cached reads only: getPageSnapshot(path), getSerpPattern(query), getKeywordDemand(term), getProofHistory(pageFamily, lever), all zero-cost lookups. The model requests exactly the grounding it needs, prompts shrink, and the numeric firewall gets richer grounded text from the tool results.
- [ ] 404. **Adopt a counted swallowError helper and retire the 500+ silent catch blocks** (Code health, large)
       Non-test code has 509 bare catch blocks and 134 .catch(() => ...) expressions, including writeTodaySurface in src/app/(shell)/today-surface-store.ts swallowing cache-write failures entirely; the recurring silent-empty-dashboard incidents come exactly from this pattern. Introduce swallowError(label, err) that logs at debug and increments a per-label counter surfaced on the operator-only diagnostics page, then codemod the mechanical cases so nothing disappears without a trace.
- [ ] 405. **Ship an /activity audit log of everything Beacon did this week** (Trust and receipts, large)
       Aggregate a chronological receipt list from stores that already exist: daily plan records (daily-experiment-plan-store.ts), shipped changes (shipped-change-store.ts), verify-live attempts (execution-state.ts transitions), auto-measure passes (auto-measure-pass.ts), and budget ledger entries (src/lib/cost/budget-ledger-supabase.ts). Each row: timestamp, what Beacon did, and a link to the affected page or pick. An autonomous team that publishes its own timesheet is one you can leave alone.
- [ ] 406. **Build a Wix App Market OAuth app to replace API-key copy-paste onboarding** (CMS push depth, large)
       Auth today is a site-level API key plus wix-site-id pasted into connector-store, the worst onboarding step for a paying stranger, and it blocks webhooks. Package Beacon as an installable Wix app with OAuth (the client.ts token seam already injects auth), granting Data, Blog, and Stores scopes on install and unlocking data-change webhooks for drift detection. Also becomes a distribution channel in the Wix App Market.

## Impact 5

- [ ] 407. **Pace the DataForSEO budget daily instead of letting week 1 exhaust the month** (Autonomy, medium)
       The $50 monthly cap in src/lib/cost/budget-ledger-supabase.ts is fail-closed, so a hot week can spend everything and leave SERP verdicts dark for three weeks. Compute a daily allowance (remaining budget divided by days remaining) and defer non-urgent pulls past it, letting urgent triggers like displacement checks borrow ahead. Predictable data beats bursty data for a nightly team.
- [ ] 408. **Retire the legacy recommended_edits queue as an operator surface** (Carry-over, medium) [CARRY-OVER 104]
       82 rows, 100% never actioned, 6+ days stale, now double-gated. Fold anything still unique into the canonical Changes adapter and stop rendering the legacy queue path on /today hero (single decision path = single truth).
- [ ] 409. **Add outbound authority citations to every content draft** (Content engine, medium)
       Extend the structured draft schemas with a citedSources field: 1 to 3 authoritative outbound links per draft chosen from a per-topic credible-domain list, building on the uncited_content trigger groundwork in recommendation-intelligence. Pages that cite sources read as trustworthy to Google raters and AI engines alike, and today every draft ships citation-free.
- [ ] 410. **Build the image SEO lane: inventory, alt coverage audit, and grounded alt-text generation** (Content engine, medium)
       extractor.ts only reads img alt as anchor-text fallback; there is no image inventory. Capture per-page image count, alt coverage, filename quality, and byte size during the scan, flag pages image-poor versus their winner's imageCount from competitor-page-audit.ts, and batch-generate grounded alt text through the budget-gated LLM gateway using title, H1, and nearest heading as context, pushed via the Wix media/data client where mappable. A visual culture encyclopedia leaves Google Images traffic and accessibility signal on the table with empty alts.
- [ ] 411. **Detect near-duplicate owned pages with shingles beyond exact content_hash** (Content engine, medium)
       content_hash only catches identical pages; template-heavy sites produce near-dupes that split rankings. Run shingle or minhash similarity over stored body excerpts plus headings_hash to find near-duplicate pairs, then propose canonical tags or a merge verdict. Complements the GSC cannibalization detector, which sees symptom overlap but not content overlap.
- [ ] 412. **Guard factory pages with a boilerplate-ratio threshold before publishing** (Content engine, medium)
       city-service-factory.ts and entity-attribute-factory.ts can generate many sibling pages from templates. Compute the cross-page boilerplate ratio over body excerpts for factory output and block publishing when unique content falls below a threshold, with a one-line reason on the card. The safety rail that keeps programmatic SEO from tipping into doorway-page territory.
- [ ] 413. **Bridge to gig humans: auto-brief the tasks Beacon cannot do and verify delivered work** (Completeness critic, medium)
       The best team money can buy includes hands: a native Farsi editor reviewing the Farsi edition, original photography of Persian dishes, a phone call to a webmaster. Have Beacon draft complete gig briefs (scope, assets, acceptance criteria, budget from a capped envelope) for Upwork or Fiverr, track them on a small board, and verify delivered work with the same quality gates it applies to its own drafts. This closes the last honest gap between Beacon and a human agency instead of pretending it does not exist.
- [ ] 414. **Capture the diaspora into an owned channel: on-site email signup plus a cultural calendar digest** (Completeness critic, medium)
       Every planned channel is rented (Google, AI engines); nothing converts one-shot search visitors into a returning audience. Add an email capture block on top pages (Wix form or embedded), store subscribers per tenant, and send a monthly Persian cultural calendar digest through the existing src/lib/email/resend.ts. Repeat direct traffic insulates the site from algorithm shocks and gives every seasonal play (Nowruz, Yalda) a launch audience on day one.
- [ ] 415. **Detect content thieves that outrank us and generate DMCA takedown packets** (Completeness critic, medium)
       The fact-fingerprint item only watches AI answers; scraper sites republishing Iranopedia pages and outranking or absorbing its citations are a distinct, common failure mode with a legal remedy. Use the planned shingle infrastructure to search the web for near-copies of owned content (SERP snapshots plus targeted queries of distinctive sentences), rank offenders by traffic taken, and auto-draft DMCA and host-abuse packets with the evidence attached. A human team files these routinely; Beacon currently would never notice.
- [ ] 416. **Add Google Maps and local-pack reads for local-business tenants** (DataForSEO depth, medium)
       Beacon's paying market includes local businesses like Ritz Builders where the local pack is the money surface, and the current SERP layer parses only organic items. Add serp/google/maps and local-finder reads through the same gauntlet, storing local rank plus review counts for the tenant vs named rivals, with a local rank series on Today for local tenants. Iranopedia gains nothing, but the first local customer demo is impossible without it.
- [ ] 417. **Extract one shared DataForSEO gauntlet runner so every new endpoint costs 30 lines** (DataForSEO depth, medium)
       dataforseo-serp.ts, dataforseo-keywords.ts, and dataforseo-llm-mentions.ts each hand-copy the same configured/cache/dry-run/cap/spend/ledger gauntlet. Pull it into src/domains/serp/dataforseo-runner.ts with an endpoint config object (path, cost estimate, cache store, TTL, parser) so every new endpoint becomes a config plus a pure parser and the fail-closed cap logic lives in exactly one place. Enabler for most DataForSEO ideas below.
- [ ] 418. **Find unlinked Iranopedia mentions with the Content Analysis API and turn them into links** (DataForSEO depth, medium)
       The Content Analysis search endpoint finds pages mentioning a phrase, with link presence. Run iranopedia monthly, diff against known referring domains from the Backlinks API, and emit a short outreach list of sites already citing the brand without linking, stored in off-site-authority. Mentions that become links are the cheapest authority available, and the mention series doubles as an AEO citation-worthiness trend.
- [ ] 419. **Split the monthly DataForSEO cap into per-class budget envelopes** (DataForSEO depth, medium)
       All spend rides one shared ledger platform with a single $50 fail-closed cap, so a big Labs or On-Page batch on the 3rd could block nightly pick verdicts on the 20th. Add class envelopes in budget-ledger-supabase.ts (verdicts and rank checks reserved, universe and audits best-effort) and show per-class spend on the receipts page. Priority spend is what makes an aggressive exploitation program safe.
- [ ] 420. **Watch teardown targets for new and lost backlinks weekly** (DataForSEO depth, medium)
       competitor-intel/detect-moves.ts sees sitemap and structural changes but is blind to the off-page war. A weekly backlinks new/lost summary call per tracked competitor appended to the competitor moves feed tells the team when a rival earned strong links to a page competing with yours, the earliest leading indicator of a coming rank loss, naming the page to reinforce before the drop shows in GSC.
- [ ] 421. **Add a Tooltip primitive and put meaning behind every hover-worthy element** (Design, medium)
       components/ui contains only button, scroll-area, sheet, and table; there is no tooltip, so context hides in native title tags or nowhere. Build a Base UI tooltip styled with the popover tokens and apply it to teammate chips (full name plus today's one-liner), verdict dots, sparkline endpoints, and the sidebar attention badges. Hover depth is a big part of the modern-tool feel the operator wants.
- [ ] 422. **Ship a compact/comfortable density toggle driven by spacing tokens** (Design, medium)
       Power days on /worklist and /proof show dozens of rows at fixed comfortable padding. Add a data-density attribute on body from a settings cookie, define --row-py and --card-p tokens that switch with it, and wire changes-list-client.tsx rows and the proof ledger first. Compact mode fits roughly twice the rows per screen, which is exactly what a daily operator wants.
- [ ] 423. **Stand up a visual regression harness for the three money surfaces** (Design, medium)
       Extend the source-guard philosophy (jargon and em-dash tests) to pixels: Playwright screenshots of /today, /worklist, and /proof in light and dark at 1280 and 375 widths, diffed against committed goldens with a small pixel budget, runnable locally so it respects the CI-minutes constraint. Design coherence currently has zero enforcement and the shipping pace is high.
- [ ] 424. **Unify the four sparkline implementations into one canonical component** (Design, medium)
       There are four sparklines: components/viz/sparkline.tsx (tooltip and trend colors), components/today/sparkline.tsx, components/data/sparkline.tsx (imported by the scoreboard), and src/app/(shell)/sparkline.ts. The same screen can render mismatched line weights and hover behavior. Pick the viz one as canonical, port the others' props, delete the rest, and add an architecture test that only one file may define a Sparkline export.
- [ ] 425. **Build a portfolio command center across all tenants** (frontier, medium)
       src/lib/tenant-context.ts and the per-tenant today-surface-store already isolate each site; nothing aggregates them. One screen: every tenant, its scoreboard delta, in-flight experiments, wins this month, and the single next decision, sorted by who needs attention most. Mostly UI since the per-tenant read models exist, but with only two active tenants today its payoff arrives when the agency tier does.
- [ ] 426. **Launch a public instant-audit funnel that runs Beacon on any URL** (frontier, medium)
       src/domains/scanning/in-process-scan.ts plus the Page Surgeon packet can grade a page with zero connectors. Put a form in the (public) route group: enter a domain, get a shareable report with 3 concrete moves, a forecast range from the shared CTR curve, and a signup CTA, spending at most one cached DataForSEO SERP call. This serves the free-audit tier the locked pricing ladder assumes, though it does nothing for Iranopedia rankings and conflicts with the current tool-first directive, so sequence it after the product proves itself.
- [ ] 427. **Mine reviews and call transcripts into customer-language content moves** (frontier, medium)
       google-reviews-sync.ts, yelp-reviews-sync.ts, and the CallRail connector in src/lib/connectors/callrail already land voice-of-customer text that nothing reads for content. Extract recurring questions and the exact phrasing customers use, diff against existing page coverage, and queue FAQ and answer-block Moves written in the customer's own words. Mostly moves the needle for local tenants like Ritz rather than Iranopedia.
- [ ] 428. **Track query footprint per page: first-seen registry plus distinct-query breadth trend** (Search Console depth, medium)
       Stamp the first date each (page, query) pair appeared in gsc_daily_rows and trend distinct-query counts per page per window. Today can then say Google started testing you on 14 new queries this week, and shrinking breadth warns that a page's relevance footprint is eroding before clicks visibly drop, an early signal no click-based trigger provides.
- [ ] 429. **Add a deliberate compound-pair track that learns lever combinations instead of avoiding them** (Learning loop, medium)
       detectMeasurementOverlaps currently neutralizes learning whenever two changes overlap on a page, so Beacon can never learn that title plus answer block together outperform either alone. Add an explicit paired pick type where the planner ships two compatible levers on one page as a single measured unit with its own ledger record and pair-keyed prior cell. Real SEO teams stack changes; Beacon should learn stacking instead of treating it purely as contamination.
- [ ] 430. **Persist weekly prior-table snapshots and render a brain changelog** (Learning loop, medium)
       Priors are recomputed from the ledger on every run and their history evaporates, so what did Beacon learn this week is unanswerable. Snapshot the computeDimPriors table weekly to a small store, diff consecutive snapshots (cells graduated past MIN_DECIDED, multipliers moved, priors flipped sign), and render the diff as plain sentences: I now trust answer blocks on animal pages; title edits on list pages dropped to mixed. A sign flip between snapshots is also the instability alarm that catches a mis-attributed verdict polluting the brain.
- [ ] 431. **Ship a learning coverage map: which lever and family cells the brain has proven** (Learning loop, medium)
       The prior diagnostic in outcome-prior.ts lists action types with settled counts, but nobody can see the full matrix of what Beacon has tested versus what remains dark. Render a lever-times-pageFamily grid on /proof from the ledger: proven-win cells, proven-loss cells, in-flight, and never-tried, with the never-tried cells feeding the exploration slot scheduler. It makes Beacon gets smarter every week a picture the operator can watch fill in.
- [ ] 432. **Calibrate the drafter's self-reported confidence against settled verdicts** (LLM engine, medium)
       Every schema in src/domains/llm/schemas.ts carries confidence high, medium, or low straight from the model, and the UI treats it at face value. Join historical draft confidence to settled proof verdicts and compute a calibration table (high from the answer-block drafter historically wins X percent). Display the calibrated number on cards and feed it to the planner score so a chronically overconfident kind stops outranking better-grounded picks.
- [ ] 433. **Stream the Prepare draft into the worklist card as it generates** (LLM engine, medium)
       The Prepare flow on /worklist blocks on a 45 to 60 second gpt-5-mini reasoning call and shows nothing until done. Switch the interactive drafting path to streaming (server action returning a ReadableStream) and render tokens into the MoveCard with the validation gauntlet applied at completion before the copy button arms. Perceived latency drops from a minute to two seconds and the app finally feels like a modern AI teammate writing in front of you.
- [ ] 434. **Planner-enforced holdout weeks per page family** (Measurement science, medium)
       Extend src/domains/experiments/daily-experiment-planner.ts so each page family (pageFamilyOf already exists) gets a recurring no-ship week, guaranteeing clean concurrent baselines and periodically letting the control pool breathe. The plan surface shows animals pages are resting this week so our comparisons stay honest. This converts an invisible statistical need into a visible team discipline.
- [ ] 435. **Add an online-store segment detected from Product schema** (Onboarding and cold start, medium)
       suggestSegmentFromProfile (src/domains/onboarding/launch-config.ts) only knows content_publisher and local_service; a Shopify or Wix store with Product and Offer JSON-LD falls to null and gets the safe-off default. Detect commerce markers in deriveBusinessProfile, add an online_store segment to src/domains/tenants/types, and give prompt-generator.ts commerce families like best X to buy and X vs Y price.
- [ ] 436. **Build a fixture corpus test for derivation across 15 real verticals** (Onboarding and cold start, medium)
       derive-business-profile.ts carries live-check comments proving quality was verified by hand only. Commit saved homepage HTML from 15 to 20 diverse sites (dentist, restaurant, SaaS, store, blog, non-English) and pin deriveBusinessProfile plus generateStarterPrompts outputs in tests, so vertical leakage, null industries, and junk services fail CI instead of a stranger's signup. This is the regression net under every other onboarding idea.
- [ ] 437. **Detect JS-rendered shells and retry through a rendering fallback** (Onboarding and cold start, medium)
       A client-rendered homepage makes deriveBusinessProfile return all nulls and the tenant onboards blind. Detect it deterministically (extracted text under a threshold with heavy script weight) and retry once through a headless-render or reader-proxy fetch path before falling back to typed-only. Log the shell rate to learn how many signups the plain fetcher silently loses.
- [ ] 438. **Detect site language and generate starter prompts in it** (Onboarding and cold start, medium)
       Read the html lang attribute and hreflang tags during the crawl, store primaryLanguage on BusinessConfig, and localize starter prompt generation (prompt-generator.ts is English-template-only) via one bounded LLM translation pass. A Persian, Spanish, or German small business currently gets English tracked prompts no real customer types; the Persian tokenizer already in the repo proves non-English is a first-class target.
- [ ] 439. **Run NAP consistency and local-pack checks for local segments at day 0** (Onboarding and cold start, medium)
       extractAddress and phone extraction already exist in derive-business-profile.ts; when the segment is local_service, compare phone and address across crawled pages and JSON-LD and check for LocalBusiness schema and a Google Business Profile link. Each mismatch becomes a concrete first-week card, reusing src/lib/local-presence.ts and the local-operator domain rather than new machinery.
- [ ] 440. **Turn the first-reading waiting card into a live progress stream** (Onboarding and cold start, medium)
       first-reading-state.ts renders a static waiting line while the cold-start scan actually works. Persist in-process scan progress to Supabase (the getScanStatus pattern in src/app/(shell)/scan-status-action.ts reads .data and is dead on Vercel) and poll it from the card: 214 pages found, 18 read, 4 fixes drafted. Watching the team visibly work in the first five minutes beats any copy.
- [ ] 441. **Audit the 22 select(*) reads in supabase-backend.ts down to explicit projections** (Performance, medium)
       src/lib/persistence/repositories/supabase-backend.ts has 22 select("*") calls (recommended_edits, page_snapshots, observations, and more); one spot was already converted to an explicit projection omitting heavy columns, proving the win. Include a facts-only getPageSnapshots method for gap-compiler.ts, which reads all snapshots then reduces each to title, meta, h1, h2 count, schema types, faq presence, and word count. Cuts egress (which already caused a Supabase quota incident) and JSON parse time on every change-pack build.
- [ ] 442. **Move surface snapshot reads onto use cache with revalidateTag for cross-lambda invalidation** (Performance, medium)
       The json-store in-process cache in src/lib/persistence/json-store.ts never expires, so after invalidateWorklistSurface on lambda A, lambda B serves its cached copy until its own staleness TTL fires. Wrapping readWorklistSurface, readTodaySurface, and readGraphSnapshot in Next's cache layer with tags (revalidateTag on mutation) gives cross-instance invalidation through Vercel's Data Cache and skips the Supabase blob read on warm hits, keeping the blob as durable source of truth.
- [ ] 443. **Precompute cannibalization at GSC sync time instead of scanning on render** (Performance, medium)
       loadGscCannibalizationForTenant runs on the today-moves enrichment path behind a 6s withTimeout, scanning per-query data across the tenant on every uncached render and silently degrading to empty when slow. Compute it once when the GSC connector syncs (the only time the answer changes) and persist to a small tenant-scoped store read in milliseconds. Also removes the honesty problem of the timeout dropping self-competition warnings.
- [ ] 444. **Attach optional zod row schemas to the store registry and validate .data reads** (Code health, medium)
       readStore<T> in src/lib/persistence/json-store.ts casts JSON.parse output straight to T[], so a store shape change (the move_drafts schema v4 style migration) silently renders as empty or broken UI. With the store registry in place, attach a row schema per store and validate on read in dev and test, count-and-log in prod. Shape drift then fails the suite the day it is introduced, not weeks later on a live surface.
- [ ] 445. **Break the 14 circular import cycles and gate on zero cycles in npm run check** (Code health, medium)
       npx madge --circular reports 14 cycles, including demand-graph/build-graph.ts with collapse-create-page-siblings.ts and profound-evidence-fusion.ts, and experiments/daily-plan-types.ts importing runtime code from execution-state.ts. Cycles make extraction and tree-shaking harder and are why big files keep growing in place. Fix each by hoisting shared types, then add madge --circular to the check script so the count stays zero.
- [ ] 446. **Create src/lib/env.ts as the single typed reader for all 148 process.env accesses** (Code health, medium)
       There are 148 process.env reads spread over 62 non-test files, and the repo has already shipped incidents from this sprawl (NaN budget caps failing open, stale SUPABASE_ACCESS_TOKEN trusted). A zod-parsed env module validated once at startup gives fail-loud typo detection, one inventory of every knob, and a place to document defaults. Migrate mechanically with a codemod and forbid raw process.env in src via no-restricted-syntax.
- [ ] 447. **Define one canonical ActionType union and delete the | string escape hatch** (Code health, medium)
       TodayMoveAction in src/app/(shell)/today-moves-data.ts is a union that ends in | string, silently defeating every exhaustive switch on it, while src/domains/action-pack/types.ts defines an 8-value ActionType and src/domains/recommendations/action-types.ts defines the queue taxonomy separately. One canonical union plus explicit mapping tables per surface makes an unknown action a compile error instead of an unstyled fallthrough card.
- [ ] 448. **Extract one generic createSurfaceCache helper for the three copy-pasted SWR stores** (Code health, medium)
       src/app/(shell)/worklist-surface-store.ts, today-surface-store.ts, and src/domains/demand-graph/graph-snapshot-store.ts each re-implement the read-instantly, stale-check, background-refresh, invalidate-on-mutation, Supabase-mirror pattern with slightly different TTL and error handling. The next cached surface (proof, competitors) will be copy number four. One createSurfaceCache(storeName, ttlMs) factory with shared staleness tests makes new fast surfaces a three-line addition.
- [ ] 449. **Make the test suite hermetic: always run on the pinned fixture, never live .data** (Code health, medium)
       tests/setup/global-fixture-hydrate.ts hydrates the synthetic fixture only when .data/global/tenants.json is absent, so local runs exercise the operator's live curated data while CI runs the fixture; combined with the documented .env.local contamination flips, local green does not equal CI green. Point tests at a temp data dir with the fixture unconditionally, killing works-on-my-machine drift and protecting the real .data from test writes.
- [ ] 450. **Replace the three hand-maintained store Sets with one STORE_REGISTRY plus a completeness test** (Code health, medium)
       src/lib/persistence/store-classification.ts is 280 lines of three Sets and json-store.ts keeps a fourth parallel Set (SUPABASE_MIRRORED_STORES); an unregistered store name makes readStore throw and once caused the DataForSEO cache to re-spend on every call. Collapse into a single registry record per store (scope, mirrored, description) consumed by both files, and add a test that greps readStore and writeStore string literals across src and fails on any unregistered name.
- [ ] 451. **Split vitest into a parallel pure-unit lane and a serial integration lane** (Code health, medium)
       vitest.config.ts sets fileParallelism false globally with a 30 second timeout because heavy Next route-module tests choke in parallel, but that serializes thousands of pure domain tests that need milliseconds. Define two vitest projects: pure src/domains and src/lib tests run fully parallel, route and integration tests keep serial mode. Directly attacks the full-suite-once-as-final-gate cost the operator flagged and keeps CI minutes down.
- [ ] 452. **Standardize domain loaders on explicit tenantId parameters, resolve context at routes** (Code health, medium)
       Loaders mix implicit context reads (today-moves-data.ts calls currentTenantId() internally) with explicit arguments, and the implicit ones are exactly what breaks in headless paths: precompute-drafts.ts and tsx scripts need the documented set -a env sourcing gotcha to work at all. Make src/domains functions take tenantId explicitly and keep currentTenantId() calls in route and action files only. Crons, scripts, and future multi-tenant loops stop depending on ambient state.
- [ ] 453. **Unify the four budget and ledger implementations into one metered-spend primitive** (Code health, medium)
       src/lib/cost holds budget.ts, monthly.ts, pricing.ts, and budget-ledger-supabase.ts; src/domains/recommendations/adjudicator-budget.ts implements its own cap; and the llm-budget plus cost-ledger stores are registered separately. Every new paid API has grown another cap implementation with its own fail-open history. One spend primitive with per-provider buckets, fail-closed semantics, and a single receipts read model makes the next paid feature a config entry.
- [ ] 454. **Replay the last 30 nights of debates in CI whenever router logic changes** (Team intelligence, medium)
       A decision-diff harness feeds persisted packets and opinions through the changed router and prints which historical picks would flip action or leave the batch; an unexplained mass flip fails the gate. Complements the planned adversarial cases by using real production debates as the regression corpus. Depends on persisting the full decision record.
- [ ] 455. **Score team coverage of the nightly batch and ratchet it toward 100 percent** (Team intelligence, medium)
       reviewCandidateWithTeam only runs when build-today-preview.ts (line 214) finds a packet for the path, so uncovered candidates skip the debate silently and carry multiplier 1. Count reviewed versus total on the plan record, print a plain coverage line, and build EvidencePackets for the uncovered entry paths until coverage is near 100 percent.
- [ ] 456. **Audit html lang and dir markup and build the Persian-English hreflang pair map** (Technical SEO, medium)
       Nothing captures the html lang attribute, and Persian-script content declared lang=en confuses both Google and AI retrieval. Extract lang and dir in extractor.ts, detect Persian script density in body_paragraph_sample, flag mismatches, add inLanguage to pushed Article JSON-LD, and map slug-similar translated pairs as hreflang candidates for when Persian variants exist.
- [ ] 457. **Detect URL-variant impression splits and query-parameter index bloat in GSC data** (Technical SEO, medium)
       Group gsc_daily_page_totals rows by normalized URL (trailing slash, case, www, stripped params) and flag any page whose impressions split across variants, plus parameterized or Wix system URLs (search, tag, filter paths) that earn impressions at all. Each cluster becomes a canonical-or-redirect move with the split quantified. Variant splits silently halve a page's apparent strength and no current trigger looks for them.
- [ ] 458. **Verify freshness signals propagate after every Wix push: dateModified and sitemap lastmod** (Technical SEO, medium)
       When the daily loop ships a change, nothing confirms the page's Article dateModified or sitemap lastmod actually updated, and stale freshness signals tell Google nothing changed. Extend verify-live in live-verification.ts to check both, and if Wix does not bump them, include dateModified in the pushed JSON-LD artifact. Freshness is a ranking input for query-deserves-freshness topics like holiday dates, and it is currently unverified.
- [ ] 459. **Build a workhorse pages leaderboard with deltas and pending-change links** (Today dashboard, medium)
       Render the top 3 pages by 7-day clicks with week-over-week arrows via loadTopPagesWithQueriesForTenant, and badge any that have an open change in the canonical backlog from loadChangesView (src/app/(shell)/changes-data.ts). A daily command center should always show where the money pages stand and whether the team already has a play running on them.
- [ ] 460. **Celebrate first-ever events: first AI citation, first click on a page the team created** (Today dashboard, medium)
       Detect first-crossing moments by joining ledger create-page rows and loadOwnCitationsByDay (src/domains/recommendation-intelligence/citations-daily.ts) with per-page daily series: first click ever on the new comedians page, or ChatGPT cited the snacks page for the first time. Render as a one-day celebration chip near the header in page.tsx with the milestone noise cap applied. Firsts are the strongest emotional proof a new page or AEO push is working.
- [ ] 461. **Track the question-query mix so AEO progress shows up in Google data too** (Today dashboard, medium)
       loadQuestionQueries in gsc-page-queries.ts already isolates question-form queries; compute their share of clicks and impressions over trailing weeks and add one stat line to the AI band in scoreboard-section.tsx: 31 percent of your clicks now come from question searches, up from 24 percent. Question share rising is the leading indicator that answer blocks and FAQ work are compounding, weeks before citations move.
- [ ] 462. **Wire the dormant milestone teaser into Today and add traffic record milestones** (Today dashboard, medium)
       pickTodayMilestoneTeaser in src/domains/milestones/surface.ts has a noise cap and magnitude logic but nothing on Today renders it. Add a celebration band under the header in page.tsx, and extend src/domains/milestones/compute.ts with record kinds like best clicks day in 90 days and best week ever. Celebration is designed everywhere except the one place the owner starts every day.
- [ ] 463. **Move judgment thresholds and metric formulas into one named registry rendered in plain words** (Trust and receipts, medium)
       The minimum-controls count, the 0.5 position lift floor, and CTR floors live as private constants in measure.ts, and no shared source explains how each metric is made. Export a thresholds and methodology registry (src/lib/methodology.ts) keyed by metric id with the plain-words formula and the named bar, so cards render cleared our bar of at least 2 comparison pages from the same constant the verdict used, and stat-card.tsx plus LedgerCard read the same entry as a small expandable. The bar Beacon grades itself against becomes public and un-driftable.
- [ ] 464. **Persist the exact Search Console request behind each window reading** (Trust and receipts, medium)
       When run-measurement.ts reads a window, store the request shape (dimensions, date range, page filter) on the window in shipped-change-store.ts. Render a check-this-yourself line on the LedgerCard showing the exact query in plain words plus the matching GSC deep link. Reproducibility turns trust-our-math into run-our-math.
- [ ] 465. **Add per-tenant publish windows so changes land at a consistent site-local hour** (CMS push depth, medium)
       Pushes currently fire whenever the operator clicks, scattering treatment start times across the day and muddying the daily diff-in-diff buckets. Add a publish_window to the tenant record, queue approved publishes as scheduled rows, and release them via the existing auto-measure cron at window open. Uniform apply times make the 7/14/28-day comparisons cleaner and let bundles land together.
- [ ] 466. **Create factory pages in two phases: hidden draft item, render check, then flip visible** (CMS push depth, medium)
       wixInsertDataItem makes new items live on the dynamic page immediately, so a bad LLM page is public the moment it lands. Support an optional per-collection visibility field in wix-collection-config: insert with visible=false, run the render audit and draft-quality gate against the hidden state, then a second small write flips it live. Needs the site's dynamic page to filter on that field, so it stays opt-in per collection.
- [ ] 467. **Ship the git_pr adapter so headless sites get real pull requests instead of dev notes** (CMS push depth, medium)
       push-service.ts routes git_pr tenants to a formatted dev note with repo config pending, and the Finglish tenant has waited on this since June. Implement per-tenant repo config (owner, repo, branch, content path convention), locate the target file by URL, apply the field change, and open a PR via the GitHub API with the receipt as the PR body. The approval, cap, and snapshot spine stays identical; verify-live watches the deployed URL after merge.
- [ ] 468. **Persist first-seen dates: New chips and an oldest-first aging sort** (Worklist UX, medium)
       Nothing records when a canonical change first appeared, so the operator cannot tell tonight's fresh opportunity from a row that has rotted for three weeks. Add a tiny per-tenant first-seen store keyed by CanonicalChange.id written on load, then render a New chip for under-48h rows and an aging line (3 changes have waited 2 plus weeks) with an oldest-first sort. Backlogs need age to be visible before they can be drained.
- [ ] 469. **Preview an answer block in page context before applying** (Worklist UX, medium)
       For answer_block picks, detail.paragraphIndex and currentText exist on PlannedExperimentRecord and page snapshots exist in page_snapshots, yet the instruction is only text. Render a mini page mock inside the How we know expander: H1, then the proposed paragraph highlighted, then the next real paragraph from the snapshot. The operator sees the landing spot instead of reconstructing it mentally inside Wix.
- [ ] 470. **Add DefinedTermSet, ItemList, and speakable generators to the schema engine** (AI visibility (AEO), small)
       composeSchema covers Article, FAQ, and BreadcrumbList; glossary pages (Persian terms, names, holidays) map naturally to DefinedTerm/DefinedTermSet for pages classified definitional by answer-intent.ts, and list pages map to ItemList with position-ranked items. Extend expected-schema.ts so the missing-schema trigger fires for these shapes and emit through the existing Wix push path. Structured lists and definitions are the shapes engines extract most reliably.
- [ ] 471. **Generate and verify a demand-grounded llms.txt and audit the one Wix serves** (AI visibility (AEO), small)
       change-contract.ts already classifies llms_txt_update changes but nothing generates the file, and Wix auto-generates an /llms.txt nobody audits. Build a deterministic generator from the demand graph (entity description from entity-schema.ts, top topic hubs, canonical page per topic with one-line summaries), verify the served file lists the pages Profound shows as most cited, and re-check freshness weekly. Honest framing: engine adoption is unproven, so pitch it as cheap insurance.
- [ ] 472. **Detect runaway LLM spend velocity and pause the caller overnight** (Autonomy, small)
       Budget caps stop total overspend but not a bug loop that burns the whole month in one night. Nightly, compare each caller's ledger spend against its trailing median; a 3x outlier pauses that caller (for example precompute-drafts in cron-sync.ts phase 2b) and files a card naming it. Complements the existing caps with anomaly detection instead of just ceilings.
- [ ] 473. **Give deadline-bound nightly loops resume cursors so no tenant starves** (Autonomy, small)
       cron-sync.ts phase 2 enrichment stops at a 240s soft deadline in fixed tenant order, so the same late tenants can be skipped every night, and measure-due hard-caps at MAX_PER_RUN=16 regardless of time left. Persist a rotation cursor per loop in cron_runs and process until the deadline instead of a fixed count. Fairness and throughput for the multi-tenant future with a small change.
- [ ] 474. **Guard crons against overlapping runs with an advisory lock** (Autonomy, small)
       Neither /api/cron/sync-connectors nor /api/cron/measure-due takes a lock, so a Vercel retry or manual trigger can overlap a slow run, double-stamping freshness or double-measuring records mid-upsert. Take a pg_advisory_lock keyed per cron at route start and exit cleanly if held. Cheap insurance before auto-accept ever ships changes unattended.
- [ ] 475. **Verify links Beacon shipped are still alive with a nightly integrity check** (Autonomy, small)
       Internal links are a shipping lever and plan records store the exact source paragraph and target (daily-plan-types.ts). Add a nightly HEAD-check over links Beacon itself published; a 404 or redirect files a fix card with the anchor, source page, and suggested new target. The team should catch its own work breaking before the operator or Google does.
- [ ] 476. **Manual mark-done flow polish** (Carry-over, small) [CARRY-OVER 76]
       the record-any-page form gets the same premium card treatment + teammate confirmation ("Got it. Tracking against 4 comparison pages from today.").
- [ ] 477. **Measure and pin** (Carry-over, small) [CARRY-OVER 98]
       a perf budget test that fails CI if / or /worklist server render exceeds budget on the seeded fixture.
- [ ] 478. **Add a Cite this page widget with academic citation formats to farm passive backlinks** (Completeness critic, small)
       Iranopedia's natural link audience is students, bloggers, and journalists citing Persian culture facts, and Wikipedia proves a cite-this-page affordance converts that into links at scale. Ship a per-page widget with APA, MLA, and Chicago formats, stable section anchors, and a visible dateModified, pushed via the existing Wix body path. It is the passive complement to the outreach pipeline: the same backlinks with zero ongoing effort, plus it strengthens the authoritative-source framing AI engines reward.
- [ ] 479. **Add cross-tenant isolation regression tests over every repository read path** (Completeness critic, small)
       A cross-tenant script leak already happened once (noted in the Iranopedia experiment batch), and the planned refactors standardize tenantId parameters without testing isolation. Build a fixture suite that seeds two tenants, exercises every read in src/lib/persistence/repositories and the domain loaders, and asserts zero rows cross the boundary, plus a check that any new table carrying tenant_id has the filter applied. This becomes mandatory the day a second paying customer exists, and it is cheap now.
- [ ] 480. **Make Beacon's own crawler a good citizen: robots.txt compliance, named UA, per-domain rate limits** (Completeness critic, small)
       Competitor teardowns, cold-start crawls, and live-page probes fetch third-party sites with no visible robots.txt check or throttle in src/domains/scanning. Add one shared crawl client that honors robots.txt, identifies a BeaconBot user agent, enforces per-domain rate limits, and caches responses across features. Beyond legal cleanliness, this prevents silent IP bans that would corrupt competitor evidence and teardown verdicts without anyone knowing why.
- [ ] 481. **Stand up a weekly editorial QA sample as the counterweight to auto-accept autonomy** (Completeness critic, small)
       With the auto-accept policy engine and trust-budget autopilot planned, content will ship with no human ever reading it; every quality check in the list is pre-ship and automated by the same system being graded. Sample N shipped changes weekly, grade them against a fixed rubric (factual, on-voice, not templated, helpful) using an independent judge model plus a 2-minute operator spot check, and feed the pass rate back into the trust budget. This is the editor role every real content team keeps even after they trust their writers.
- [ ] 482. **Prioritize teardowns by competitor page traffic using Labs relevant_pages** (DataForSEO depth, small)
       pickTopCompetitorUrls in refresh-intel.ts chooses structural-teardown targets by citation count, which says nothing about which competitor pages earn traffic. One relevant_pages call per competitor returns their pages ranked by estimated organic traffic; use it to reorder TOP_CITED_URL_CAP picks so the polite-fetch budget lands on pages that matter.
- [ ] 483. **Seed keyword demand from any competitor URL with keywords_for_site** (DataForSEO depth, small)
       keywords_data/google_ads/keywords_for_site takes a site or page URL and returns Google's own keyword associations with volume in one call. Add it as a producer next to runKeywordVolume so every teardown target can be expanded into the demand it serves, and every new tenant onboarding gets an instant keyword baseline from their domain plus two rivals for about $0.25.
- [ ] 484. **Add a print stylesheet so Results prints as a clean client-ready report** (Design, small)
       globals.css contains zero @media print rules; printing /proof today emits the sidebar, dark backgrounds, and clipped cards. Hide shell chrome, force light tokens, flatten cards to hairline borders, and add page-break rules per verdict band. The operator can hand a client or partner a paper Results page, which directly supports the $250/mo conviction moment.
- [ ] 485. **Add a source-level guard test that bans raw palette colors on shell surfaces** (Design, small)
       Mirror the pattern in src/app/(shell)/proof/proof-jargon-guard.test.ts and the em-dash guards: a grep-based vitest that fails on bg-white, gray-*, neutral-*, and hardcoded hex in src/app/(shell) and src/components once the token migration lands. Without a ratchet, every fast-shipped feature reintroduces the second color system within a week.
- [ ] 486. **Move teammate identity colors to CSS vars with dark-mode variants** (Design, small)
       src/domains/team/identity.ts hardcodes light pastel hex backgrounds (#eff6ff, #ecfdf5, #fdf2f8) for the 9 teammate chips used in the roundtable, standup strip, and war-room bands; in dark mode those chips render as bright pastel patches on near-black cards. Define --teammate-{key} and --teammate-{key}-bg pairs in globals.css with .dark overrides and have identity.ts reference the vars, so the team's visual identity survives both themes.
- [ ] 487. **Polish the scoreboard with weekend shading, a today edge, and labeled guides** (Design, small)
       In scoreboard-section.tsx the y-guides are unlabeled 6 percent opacity lines and nothing distinguishes weekends or the current day, so weekly traffic rhythm looks like noise. Add faint weekend bands, a dashed today marker at the right edge, and small value labels on the guides. Three touches that move the hero from decorative to analyst-grade.
- [ ] 488. **Wire real chart color tokens and kill the hardcoded hex in the scoreboard hero** (Design, small)
       globals.css defines --chart-1 through --chart-5 as unused grayscale values while src/app/(shell)/scoreboard-section.tsx hardcodes #6366f1, #4f46e5, #10b981, #94a3b8, and #f59e0b, so the flagship chart cannot respond to theme changes. Redefine the chart tokens as the actual indigo/emerald/amber palette with .dark overrides and swap the scoreboard and components/viz charts to var(--chart-*).
- [ ] 489. **Chart the rank distribution shift: page-1, striking distance, invisible** (frontier, small)
       Compute weekly position buckets (1-3, 4-10, 11-20) from the GSC archive, overlay ship markers from the changes ledger, and give the standup the sentence 'we moved 14 queries from page 2 to page 1 this month'. Cheap to build on src/lib/performance-timeseries.ts and it reframes individual experiments as portfolio progress.
- [ ] 490. **Mine on-site search for zero-result content gaps** (frontier, small)
       GA4 records view_search_results events with search_term for Wix site search, and the GA4 connector already paginates runReport. Pull the terms, flag searches with no matching page via token overlap against the pages domain, and route the top ones as create-page candidates carrying first-party demand. Visitors literally typing what they want and not finding it is the cleanest demand signal that exists, and it costs nothing.
- [ ] 491. **Track Google Discover as a separate channel in the GSC sync** (frontier, small)
       The GSC connector never sets searchType, so it only ever reads web search; Discover is one enum value away. Sync Discover impressions and clicks, detect which pages fit the profile (large images, fresh entity content suits Iranopedia), and add a Discover band to Today when nonzero. It is free demand no competitor tool is watching for this tenant class.
- [ ] 492. **Compute the anonymized-query gap per page as a long-tail opportunity signal** (Search Console depth, small)
       gsc_daily_page_totals includes the anonymized low-volume queries Google hides while gsc_daily_rows excludes them; the per-page delta is the hidden long-tail share, already sitting in the database uncomputed. High-share pages get add specific subsections moves, and evidence lines get more honest: 42 percent of this page's clicks come from queries Google hides.
- [ ] 493. **Probe Google Discover and News monthly and enforce max-image-preview large** (Search Console depth, small)
       Two type=discover and type=news API calls a month answer whether a whole free channel exists; cultural evergreen content is a plausible Discover candidate. If nonzero, have the scanner verify max-image-preview:large robots meta plus a large hero image on candidate pages; if zero, the probe costs nothing. Report as one standup line.
- [ ] 494. **Run a one-time 16-month GSC backfill to unlock year-over-year analysis** (Search Console depth, small)
       BACKFILL_DAYS=90 in sync-search-analytics.ts while Google holds 16 months; the operator startDate override already bypasses the watermark. Backfill old months at page grain only (skip page+query older than 6 months) to keep gsc_daily_rows sane. This is the enabler for the seasonality engine and YoY decay verdicts.
- [ ] 495. **Source internal-link anchors from real GSC queries and rewrite weak generic anchors** (Search Console depth, small)
       triggers/internal-link-opportunity.ts derives anchors from titles. Pull the target page's top queries via gsc-page-queries.ts as anchor candidates, and audit existing anchors against target-page owned keywords to flag click here, read more, and bare-name anchors pointing at money pages, shipped as safe internal_link candidates through safe-internal-link.ts.
- [ ] 496. **Add recency weighting with a half-life so old outcomes fade from the priors** (Learning loop, small)
       computeDimPriors weighs a 9-month-old win identically to last week's, so the brain cannot adapt when Google shifts or the site changes character. Weight each settled outcome by exp(-age/half-life) with roughly a 180-day half-life in experiment-prior.ts and outcome-prior.ts, keeping the same clamped multiplier band. Combined with Beta shrinkage the change is small and safe, and it prevents the worst long-run failure mode: a brain confidently wrong about a world that moved.
- [ ] 497. **Demote levers with high inconclusive rates as wasted effort** (Learning loop, small)
       Priors ignore inconclusive and insufficient_data entirely, but a lever that produced 6 inconclusive reads in a row is burning the 45-minute nightly budget on nothing. Compute an inconclusive rate per lever from the ledger, apply a small bounded demotion in the planner when it exceeds a threshold, and raise the candidate impressions floor when insufficient_data dominates. Effort is the scarcest resource in the daily cycle and nothing currently protects it.
- [ ] 498. **Recalibrate effort estimates from real accept-to-verified-live timestamps** (Learning loop, small)
       The planner budgets 45 minutes a night against static effortMinutes guesses per lever, but execution-state.ts and live-verification.ts already record when a pick was accepted, applied, and verified live. Compute median real minutes per lever from those timestamps and feed the learned estimates back into build-daily-candidates.ts. The 30-minute mode and nightly budget then reflect the operator's actual speed instead of a guess, so batches stop being under- or over-packed.
- [ ] 499. **Batch-level de-templating guard on nightly meta and title drafts** (LLM engine, small)
       Eight meta rewrites drafted the same night by the same prompt converge on one template (Discover X: history, meaning and more), and templated metas get rewritten or ignored by Google. After the enrichDailyCandidatesWithLlm pass in daily-llm-enrich.ts, compute pairwise similarity across the batch's proposed texts and regenerate any pair above a threshold with an explicit vary-the-structure instruction. Cheap, and it protects the CTR upside the whole nightly cycle exists to win.
- [ ] 500. **Best-of-2 drafting with deterministic pick for the top nightly moves** (LLM engine, small)
       For the 2 to 3 highest-demand picks in build-today-preview.ts, sample the atomic edit twice and let the existing CTR title scorer plus draft-quality.ts pick the winner. Sampling variance on gpt-5-mini is large, so a second sample plus a deterministic judge is the cheapest quality doubling available. Cost stays bounded because it only applies above a demand threshold.
- [ ] 501. **Escalate the retry to a stronger model or higher reasoning effort** (LLM engine, small)
       callStructuredLLM retries validation failures with the identical model and reasoning_effort low, just with error text appended, so hard cases usually fail twice and the pick falls back deterministic. Make attempt 2 escalate: reasoning_effort medium, or gpt-5.4-mini for create_page_brief kinds. Failures are by definition the hard cases, exactly where extra thinking pays.
- [ ] 502. **Prompt-injection sanitization for crawled and competitor text entering prompts** (LLM engine, small)
       Competitor teardown facts and crawled page bodies flow verbatim into drafter prompts via build-today-preview.ts and the AEO brief inputs, so a competitor page containing adversarial instructions could steer Beacon's published copy. Wrap all third-party text in clearly delimited data blocks, strip instruction-like lines deterministically, and add an explicit instruction-hierarchy sentence to each system prompt. Cheap insurance before any second customer runs the pipeline on arbitrary sites.
- [ ] 503. **Tighten the GA4 traffic outcome with floors and robust aggregation** (Measurement science, small)
       computeTrafficOutcome in src/domains/proof-gsc/traffic-outcome.ts takes a bare mean of control percent changes with no minimum-session floor, so a control going 3 to 9 sessions posts +200 percent and wrecks the adjustment. Require a minimum pre-window session count per control, aggregate with the median, and suppress the adjusted percentage when control dispersion exceeds the measured effect. Keeps the dollars-adjacent surface as honest as the GSC one.
- [ ] 504. **Verdict stability history so flip-flopping results never train learning** (Measurement science, small)
       measureRecord overwrites windows and verdict on every pass, so a verdict that flips won to lost between re-measures leaves no trace. Append a compact measuredAt, verdict, lift history array on ShippedChangeRecord and require N consecutive stable passes before learningEligibility in measurement-maturity.ts turns true, surfacing this result changed its mind twice honestly. Also gives the restatement audit trail GSC's own data revisions require.
- [ ] 505. **Add a re-read-my-site action so derived config never goes stale** (Onboarding and cold start, small)
       Config derivation runs exactly once inside executeLaunchTransaction, so a redesign or rename strands stale services and locations forever. Add a settings action that re-runs fetchSiteProfilePages plus deriveBusinessProfile and merges under the existing typed-beats-derived precedence in launch-config.ts, with a diff preview before saving. Also the natural remedy after a failed first derivation.
- [ ] 506. **Instrument the signup-to-first-win funnel per tenant** (Onboarding and cold start, small)
       Record timestamps for signup, launch, first crawl, first recommendation, first ship, and first verified win in a tenant_funnel table written from provision-tenant.ts, launch-flow.ts, and the apply and measure paths. A diagnostics surface flags any tenant stalled more than 24 hours before first ship. You cannot design time-to-first-win without measuring where each cold start dies.
- [ ] 507. **Run the entity foundation check connector-free at cold start** (Onboarding and cold start, small)
       src/domains/entity/entity-extract.ts and the Today entity-foundation copy exist for connected tenants only. At launch, check derived socialProfiles for sameAs consistency and probe Wikidata or Wikipedia presence for the brand name, then include AI assistants cannot verify who you are in the first audit with exact fixes. Entity clarity is the AEO fix with the longest payoff and needs zero connectors to diagnose.
- [ ] 508. **Surface unreachable-site derivation failures honestly with a fix and a retry** (Onboarding and cold start, small)
       deriveAndPersistTenantConfig is failure-soft (src/domains/onboarding/launch-config.ts): a robots-blocked or down site silently produces a typed-only config and the user never learns derivation failed. Show the exact reason in the wizard plus a re-read my site retry that re-runs fetchSiteProfilePages after the fix. Silent degradation on the very first interaction is where trust dies.
- [ ] 509. **Validate the domain live in the business form with did-you-mean** (Onboarding and cold start, small)
       Every downstream engine keys on the domain string, and a typo produces a silent empty derivation and a dead tenant. On blur, resolve the domain (HEAD request, www and apex fallback, common TLD slip suggestions) and confirm reachability before save in src/app/(shell)/onboard/business/, extending normalizeSiteUrl. One cheap check prevents a team assigned to a site that does not exist.
- [ ] 510. **Run Supabase performance advisors and add covering indexes for the hot IN() paths** (Performance, small)
       The per-page reads in gsc-page-queries.ts filter gsc_page_query_daily by tenant_id plus page IN (...) plus date windows repeatedly; a missing composite index turns each into a scan on the biggest table. Use the management API get_advisors performance lints plus EXPLAIN on the six hottest queries, then add covering indexes like (tenant_id, page, date) INCLUDE (clicks, impressions, position). Cheap, and the wins compound under every loader above.
- [ ] 511. **Set experimental.staleTimes so back-and-forward navigation reuses RSC payloads** (Performance, small)
       With everything force-dynamic, the client router cache keeps dynamic RSC payloads for 0 seconds, so bouncing between Today, Worklist, and Results refetches each page every time. experimental.staleTimes.dynamic = 60 in next.config.ts makes tab-style navigation instant for the single operator while mutations still revalidate via revalidatePath. One config line, dramatic perceived-speed win for the daily ritual.
- [ ] 512. **Add an eslint layering rule so src/domains can never import from @/app again** (Code health, small)
       Two files already invert the architecture: src/domains/demand-graph/precompute-drafts.ts imports from @/app/(shell)/today-moves-data and src/domains/action-pack/load-today-cockpit.ts imports from @/app/(shell)/moves/moves-data. A no-restricted-imports rule in eslint.config.mjs (domains cannot import @/app/*, lib cannot import domains) makes the boundary self-enforcing; pair it with fixing the two current offenders.
- [ ] 513. **Encode the repository read boundary as a lint allowlist; 47 files bypass it** (Code health, small)
       src/lib/persistence/repositories/index.ts documents that readStore and readDotDataJson may only be used by backends, persistence writers, scripts, and two named exceptions, yet 47 files under src/app and src/domains import them directly. Each bypass skips the DATA_SOURCE=supabase routing and is a future prod-versus-file inconsistency. Add no-restricted-imports with the documented allowlist so the boundary fails lint instead of relying on doc memory.
- [ ] 514. **Extract shared ISO date math into src/lib/dates.ts and delete 12 private copies** (Code health, small)
       addDays, daysBetween, and isoDay are privately re-implemented at least 12 times: src/domains/proof-gsc/measure.ts, competitor-intel/detect-moves.ts, attribution/natural-controls.ts, experiments/experiment-eligibility.ts, scanning/findings-store.ts, changelog/dedupe.ts, plus 71 raw toISOString().slice(0,10) call sites. The 7/14/28-day diff-in-diff windows depend on this math agreeing about inclusivity and UTC; one tested module removes the off-by-one class from the measurement path.
- [ ] 515. **Alarm on robots.txt and sitemap structure drift between scans** (Technical SEO, small)
       robots-state caches the parsed robots.txt but nothing diffs it across fetches, and sitemap child lists are re-fetched blind each scan. Hash the robots.txt body and the sitemap-index child list and fire a high-severity finding the moment either changes; Wix platform updates have silently rewritten both. A one-line robots change that blocks an AI bot is the classic silent AEO killer that Beacon would only notice indirectly weeks later.
- [ ] 516. **Capture the X-Robots-Tag response header so header noindex stops being invisible** (Technical SEO, small)
       detectNoindex in compute-indexability.ts only parses the HTML meta robots value; a noindex delivered via the X-Robots-Tag HTTP header (which Wix and CDNs can inject) sails through as indexable. Capture response headers during the scan fetch, add x_robots_tag to PageSnapshot, and fold it into the noindex verdict. A silent de-indexing class the current pipeline structurally cannot catch.
- [ ] 517. **Detect soft 404s: 200-status pages that are actually empty shells** (Technical SEO, small)
       Add a soft-404 heuristic: http 200 with near-zero word_count, title or h1 matching not-found patterns, or GSC coverage_state saying Soft 404 from cached gscUrlInspect rows. Emit a new finding type in scanning/types.ts. Deleted Wix collection items often leave 200-status ghosts that bleed crawl budget and get flagged by Google without anyone noticing.
- [ ] 518. **Extend schema-validator.ts to cover Person, Recipe, Event, and VideoObject rich-result specs** (Technical SEO, small)
       src/domains/pages/schema-validator.ts validates the current JSON-LD families against Google specs but none of the new content types. Add required and recommended property tables for Person, Recipe, Event, and VideoObject so every generated or observed block is validated before push and drift is caught by the schema_invalid trigger. Keeps the never-ship-invalid-markup guarantee as schema breadth grows.
- [ ] 519. **Record TTFB and HTML bytes on every scan fetch as a free regression alarm** (Technical SEO, small)
       fetchPageHtml calls in in-process-scan.ts already run under a Date.now budget but throw the timing away. Persist per-URL fetch duration and byte size on PageSnapshot, trend across nightly scans, and fire a finding when a page or the whole site degrades 2x for 3 consecutive scans. Costs nothing and catches Wix app bloat before rankings react.
- [ ] 520. **Age the attention items and call out when the team is waiting on the operator** (Today dashboard, small)
       Attention rows in page.tsx show no elapsed time. Stamp each TodayAttentionItem in src/domains/changes/today-view.ts with days waiting from the plan's acceptedAt and escalate the tint after 2 plus days; when applies have sat, the header brief adds one owned sentence: the team is waiting on you, 3 approved changes are not live yet. Urgency pointed at the true bottleneck, which on a one-person team is usually the person.
- [ ] 521. **Render impressions and CTR stats that buildScoreboard already computes but the UI drops** (Today dashboard, small)
       Scoreboard.last7Impressions is computed in src/domains/scoreboard/scoreboard.ts and never rendered. Add a small stat row under the chart: Google showed you 41,200 times, you captured 1,180 clicks, 2.9 percent, with week-over-week deltas. Demand vs capture sets up every title and snippet change the team proposes.
- [ ] 522. **Write a weekday-adjusted yesterday brief into the greeting** (Today dashboard, small)
       Compute yesterday's clicks vs the average of the last four same weekdays (traffic is weekday cyclic, so raw day-over-day lies) plus the single best page, and fold one sentence into the header brief in src/app/(shell)/page.tsx: yesterday was your best Tuesday in six weeks, led by the cheetah page with 41 clicks. Pure math on loadDailyTotalsForTenant plus one page-level read.
- [ ] 523. **Export the proof ledger as CSV so a skeptic can recompute the verdicts** (Trust and receipts, small)
       One-click export of ShippedChangeRecord rows with baseline metrics, every window reading, control pages, and verdicts from shipped-change-store.ts. A stranger can rebuild the diff-in-diff in a spreadsheet in ten minutes. A product that volunteers its raw work reads as honest in a way no badge can fake.
- [ ] 524. **Give the weekly recap a mandatory we-got-this-wrong section** (Trust and receipts, small)
       src/domains/proof-gsc/weekly-recap.ts summarizes the week; add a non-omittable misses block listing revised verdicts, failed live verifications, and excluded data days whenever the count is nonzero. The Beacon voice contract already demands owned misses; this makes the recap structurally incapable of only celebrating.
- [ ] 525. **Require and display a one-line reason on learning exclusions** (Trust and receipts, small)
       ExcludeFromLearningButton in proof-ledger-client.tsx flips operatorVerdictOverride with no note. Require a short reason, stamp when it happened, and render it on the excluded card: excluded Jun 20, a site redesign hit this page mid-window. Human overrides of the machine deserve the same audit trail as the machine itself.
- [ ] 526. **Add a CMS read-back verification tier between write and page-render probe** (CMS push depth, small)
       After wixUpdateDataItem succeeds, the only confirmation is probeLiveText against the rendered page, which Wix CDN caching can fail for hours, leaving an honest write stuck at pending. Insert a read-back step: wixGetDataItem immediately after the write, confirm the field equals the proposed text, and stamp the receipt confirmed in your CMS, waiting for the page to refresh. Distinguishes write-lost from cache-lag, which read very differently to a paying operator.
- [ ] 527. **Auto-heal unmapped_url refusals with a targeted collection re-sync and one retry** (CMS push depth, small)
       When resolveWixItemForUrl returns null, executePush refuses with run the url-map sync on /diagnostics/wix first, stranding newly added Wix items until the operator visits a diagnostics page. On that refusal, trigger syncWixUrlMap scoped to the likely collection (match by urlPrefix) and retry the resolution once before giving up. Removes a whole class of publish-day friction for zero risk since the sync is read-only.
- [ ] 528. **Guard against concurrent dashboard edits with a revision check before the PUT** (CMS push depth, small)
       Between the pre-push snapshot in executePush and the write fetch, an operator edit in the Wix dashboard can land, meaning the stored snapshot no longer matches what got overwritten. Capture the item's _updatedDate at snapshot time and refuse the write when the write-time fetch shows a newer revision, with a one-click refresh-and-retry. Keeps the rollback promise honest.
- [ ] 529. **Probe API-key scopes at connect time and show exactly which push routes are live** (CMS push depth, small)
       A key missing the Blog or Stores permission only surfaces as an opaque api_error http_403 at push time. On connect, run a read-only probe matrix (collections list, blog posts list, stores products query) and persist a per-route capability map on the connector row, rendering field edits ready, schema on products needs the Stores permission on /settings/connectors. Prevents an approved change failing for a reason nobody explained.
- [ ] 530. **Retry transient push failures automatically on the next cron pass** (CMS push depth, small)
       A push_failed card from a Wix 5xx or timeout sits until the operator manually re-clicks, even though PUSHABLE_FROM already allows re-push. Classify ledger detail codes into structural (length_limit, protected_field, unmapped_url: never retry) versus transient (api_error http_5xx, timeout), and have the nightly cron re-run transient failures with a max of 2 attempts, finalizing the receipt either way. The operator wakes up to shipped, not stuck.
- [ ] 531. **Scan nightly for external Wix edits and flag cards with stale current_text** (CMS push depth, small)
       If the operator edits a mapped item in the Wix dashboard, Beacon's open cards keep a stale current_text until the destructive-guard refusal at push time. Add a nightly drift pass comparing each mapped item's _updatedDate to the url-map's syncedAt, re-reading changed items and stamping affected queue cards the page changed since this was drafted, with a refresh action. Cheap read-only calls that protect draft quality and the before value in receipts.
- [ ] 532. **Verify the edit_meta SEO-variable binding with a first-push canary check** (CMS push depth, small)
       url-map.ts maps edit_meta to a description field on the assumption an SEO Variable binds it to the live meta tag; if the site never bound it, the field write succeeds while the live meta never changes. After the first edit_meta push per collection, probe meta[name=description] specifically; on mismatch, mark the role binding broken in wix-collection-config and downgrade future edit_meta cards to paste-ready with the exact dashboard fix named.
- [ ] 533. **Copy tonight's batch as a spreadsheet for VA handoff** (Worklist UX, small)
       Next to the existing Copy all pending pastes button in daily-experiments-section.tsx, add Copy as spreadsheet emitting TSV rows: URL, Wix field, current text, new text, instructions, one row per pending item. Armeen works with offshore helpers; a paste-into-Sheets handoff turns the worklist into a delegation tool without building assignment infrastructure. Pure client string building over checklist.items.
- [ ] 534. **Show blocked rows their unlock date and sort by soonest free** (Worklist UX, small)
       Blocked rows say only Wait with a text blockedReason, but reservations carry reservedUntil and proofs carry nextCheckpoint, the exact dates the page frees up. Thread the earliest unlock date through buildCanonicalChanges, render frees up in 9 days on the row, and sort blocked items by soonest-free. The operator can plan around the protection window instead of repeatedly poking frozen pages.
- [ ] 535. **Show the cumulative payoff of the top of the list** (Worklist UX, small)
       Each row shows its own expectedOutcome from the shared CTR curve, but nothing answers what this session buys. Above the list in changes-list-client.tsx, sum the visible actionable rows' ctrOpportunityClicks (already computed in changes-data.ts line 53) into one line: do the top 5, roughly 120 to 300 extra clicks a month, updating with filters and the tonight budget. A single motivating number turns triage into a purchase decision.
- [ ] 536. **Sync worklist filters to the URL and add named saved views** (Worklist UX, small)
       ChangesListClient reads deep-link params but never writes them back; strategy, tab, goal, search, and tonight all live in useState, so refresh, back button, and sharing lose the slice. Mirror state into the query string with router.replace, then add 2-3 named saved views in a tiny per-tenant store, for example Monday triage equals clean strategy plus quick wins. The list starts behaving like a real work OS with durable workspaces.
- [ ] 537. **Consolidate four overlapping brief systems into one canonical brief pipeline** (Content engine, large)
       Briefs exist in four places: src/domains/brief-generation/, src/domains/briefs/ with checklist-templates.ts, CreatePageBriefSchema in llm/schemas.ts, and MissingPagePlan plus PlaybookBrief in frontier-compiler.ts and playbook.ts. Collapse everything onto the structured CreatePageBrief and delete the legacy paths so draft quality no longer depends on which forgotten pipeline fired. This is the cleanup that makes the outline-to-draft pipeline safe to build.
- [ ] 538. **Adopt the Button component across the 171 hand-rolled shell buttons** (Design, large)
       src/app/(shell) contains 171 raw button elements but only 1 import of components/ui/button.tsx, whose cva variants (default/outline/ghost/destructive, xs to lg, icon sizes) already handle focus, disabled, and dark mode correctly. Migrating CTAs route by route makes every action identical in height, radius, and focus treatment and deletes dozens of bespoke class strings.
- [ ] 539. **Define a named type ramp as CSS vars and retire 2651 arbitrary pixel sizes** (Design, large)
       The codebase has 2651 text-[Npx] occurrences (text-[9px] through text-[13px]) with no shared line heights or letter spacing; globals.css has color and radius tokens but zero typography tokens. Add @theme steps (micro 10/14, caption 11/16, label 12/16, body 13/20) with matched tracking, then sweep src/components/viz, src/components/display, and the shell surfaces. Optical consistency across every label and meta line is the biggest premium-feel lever left.
- [ ] 540. **Adopt Next 16 cacheComponents so the shell prerenders static with dynamic holes** (Performance, large)
       next.config.ts is empty and src/app/(shell)/layout.tsx sets dynamic=force-dynamic for the whole subtree, so the sidebar, header, and page chrome re-render server-side on every click. With cacheComponents (PPR), the static shell serves instantly from the edge while data sections stream into Suspense holes. Requires removing the force-dynamic escape hatch and guarding build-time Supabase env reads with connection(), which is exactly why the flag was set.
- [ ] 541. **Audit and delete the legacy src/domains/product engine cluster (5k+ lines)** (Code health, large)
       src/domains/product/recommendation-engine.ts is 2029 lines with a single export, consumed only by siblings morning-brief.ts (1787 lines), priority-engine.ts, keyword-gap-scanner.ts, replication-engine.ts, and recommendation-tracker.ts. Verify which live routes still reach this cluster now that the demand-graph engine and ActionPack worklist are the product, then delete or quarantine it with its 1662-line test file. Distinct from retiring the legacy queue route; this is the older generation engine underneath.
- [ ] 542. **Split the 450-line SeedDataRepository god-interface into per-domain read repos** (Code health, large)
       src/lib/persistence/repositories/types.ts defines one giant SeedDataRepository (named from the CSV-seed era) that both file-backend.ts and the 1310-line supabase-backend.ts must fully implement, so every new read widens three files. Split into narrow per-domain interfaces (GscReads, PagesReads, RecommendationReads) composed by getRepository(), and rename to drop the misleading seed-data vocabulary. Buys back speed on every future data feature.
- [ ] 543. **Build a Webflow adapter on the Data API v2 with native drafts and partial updates** (CMS push depth, large)
       Webflow's collection items API supports PATCH partial updates, isDraft staging, and explicit publish, a cleaner fit for Beacon's model than Wix's full-item PUT. Port the url-map pattern (collection plus slug to URL) and register a webflow_cms adapter under the PublishAdapter interface, mapping edit_title and edit_meta to the built-in SEO fields. Second-CMS proof the architecture generalizes, and Webflow shops are premium-priced customers.
- [ ] 544. **Week planner: drag ready changes onto weekdays** (Worklist UX, large)
       Add a light calendar strip above the changes list where the operator drags ready rows onto Tue or Sat, persisting a scheduledFor date per change id that buildTodayExperimentPreview treats as a strong selection bias on that day, shown as a Scheduled chip. The nightly planner stays the brain, but the operator can shape the week around their own time: big pages on the weekend, quick metas on weeknights. The missing schedule half of snooze.

## Impact 4

- [ ] 545. **Add an accessibility audit lane: WCAG basics folded into scan findings** (Completeness critic, medium)
       No item touches accessibility, yet heading order, link names, contrast, and image text overlap heavily with the extractability AI engines need, and ADA demand letters are a real legal exposure for US small-business tenants. Extend src/domains/scanning/detect-findings.ts with a bounded WCAG check set (heading hierarchy, empty links and buttons, contrast on templates, form labels) and emit Wix-actionable fix cards. Frame findings in the existing plain-language voice, not as a compliance dump.
- [ ] 546. **Build styled Select and Checkbox primitives to replace browser-default controls** (Design, medium)
       13 native select and 11 native checkbox/radio inputs render OS-default chrome inside an otherwise polished app (worklist filters, settings forms, review flows). Build Base UI backed Select and Checkbox in components/ui using the existing --input and --ring tokens; the @base-ui/react dependency is already installed via button.tsx.
- [ ] 547. **Codify ledger-row typography: numeric columns, headers, and hover states** (Design, medium)
       proof-ledger-client.tsx and changes-list-client.tsx hand-roll row divs with inconsistent alignment while components/ui/table.tsx sits mostly unused. Codify a ListRow standard: right-aligned tabular-nums numeric cells, 11px uppercase tracked headers, token borders, and a shared hover treatment, applied to the proof ledger and changes list first. Dense data screens are where premium tools visibly separate from templates.
- [ ] 548. **Consolidate the 10 plus hand-rolled pills into one Pill primitive with semantic tones** (Design, medium)
       components/display carries status-badge, priority-badge, effort-badge, threat-badge, confidence-badge, rec-confidence-pill, lifecycle-status-pill, and early-signal-pill, plus inline pills like the Measuring chip in (shell)/page.tsx, each with its own padding, radius, and font size. Define a Pill component with tone and size mapped to the status tokens and reimplement the badge components as thin wrappers, ending pill drift between screens.
- [ ] 549. **Design a real Beacon mark to replace the letter-B div** (Design, medium)
       The brand today is a 24px div containing the letter B (app-sidebar.tsx line 61) plus a default favicon at src/app/favicon.ico. Draw a simple lighthouse-beam SVG mark, use it in the sidebar, favicon, login page, and the FirstReadingWaiting screen. Theme personality starts with a mark the operator recognizes; a template app never earns $250/mo affection.
- [ ] 550. **Extract a micro-illustration library for empty and quiet states** (Design, medium)
       QuietCheckIllustration is a hand-drawn 40px SVG buried inline in war-room-sections.tsx. Move it to components/display/illustrations.tsx and add five siblings in the same stroke style: waiting-for-first-data, measuring hourglass, no-results-yet, error, and all-shipped. Reuse across FirstReadingWaiting, proof empty bands, and the worklist empty state so every quiet moment feels designed rather than absent.
- [ ] 551. **Add a capped local rank grid for location-based tenants** (frontier, medium)
       Run the existing runSerpQuery in src/domains/serp/dataforseo-serp.ts with a 3x3 grid of location coordinates for the top 3 money queries monthly under the same fail-closed cap (about 27 calls), render a small heat grid on the tenant's Today, and emit GBP or location-page Moves when a grid corner is weak. Keep it dark for non-local tenants like Iranopedia via tenant config; this only pays off for Ritz-class local tenants.
- [ ] 552. **Auto-attach openly licensed images with ImageObject schema through the Wix media path** (frontier, medium)
       The Wix media client exists from the capability-ceiling work but no move type produces imagery. On page creation or full rewrite, fetch an openly licensed image (Wikimedia Commons is ideal for Persian-culture entities), upload via the media API, and emit ImageObject schema plus alt text through the existing image-alt analyzer in src/domains/page-factory/image-alt-analyzer.ts. Images gate Google Discover eligibility and lift trust on entity pages.
- [ ] 553. **Add bundle analysis to next.config and dynamic-import the heavy server deps** (Performance, medium)
       next.config.ts is an empty object: no @next/bundle-analyzer, no optimizePackageImports for the UI libs, no visibility into first-load JS per route. Wire the analyzer behind an env flag, snapshot per-route first-load JS, then trace whether xlsx (~1MB), cheerio, and puppeteer-core reach shell route lambdas via static import chains (src/domains/pages/extractor.ts, render-check.ts) and convert them to await import() with serverExternalPackages for puppeteer-core. You cannot fix bundle weight you have never measured.
- [ ] 554. **Collapse the three logging systems and 162 raw console.error calls into one logger** (Code health, medium)
       src/lib/logger.ts (hand-rolled JSON emitter), src/lib/obs/logger.ts (pino, the actual dependency), and src/lib/debug-log.ts (BEACON_DEBUG_RECS-gated console.log) coexist while 162 raw console.error calls bypass all three. Standardize on the pino logger with namespace child loggers and a level env, migrate debugRecEngine to a debug namespace, and delete the other two. One grep then answers what happened last night.
- [ ] 555. **Consolidate the four sparkline implementations into one components/viz primitive** (Code health, medium)
       src/app/(shell)/sparkline.ts declares itself the one shared path builder, yet src/components/today/sparkline.tsx, src/components/viz/sparkline.tsx, and src/components/data/sparkline.tsx each render their own SVG with different null-gap and ship-marker semantics. Keep the pure path math in one module, one presentational component with marker and tint options, and delete the rest. Charts drifting in tiny ways is how before/after visuals stop matching across surfaces.
- [ ] 556. **Delete dead feature flags in flags.ts and llm-draft-gateway-flag.ts plus else-branches** (Code health, medium)
       The operator directive is no flags, always main, single user, yet src/lib/flags.ts still exports 6 flag helpers and src/lib/llm-draft-gateway-flag.ts gates LLM drafting that is now default-on with a funded account. Each stale flag doubles the paths a change must be verified against and hides which branch production actually runs. Sweep them, hard-code the winning branch, delete the losing code.
- [ ] 557. **Introduce a defineTenantAction wrapper for the 20+ server-action files** (Code health, medium)
       today-moves-actions.ts, daily-experiments-actions.ts (403 lines), finding-actions.ts, opportunity-actions.ts, serp-actions.ts, execution-actions.ts, and a dozen more each hand-roll tenant resolution, try/catch, revalidation, and ad-hoc result shapes. One wrapper providing tenant scoping, a standard ok/error envelope, and consistent revalidate semantics cuts each action file to its business logic and gives the receipt UI a single hook point instead of 20.
- [ ] 558. **Split dual-write.ts into a generic chunk-retry engine plus a per-table registry** (Code health, medium)
       src/lib/persistence/dual-write.ts is 1519 lines where the reusable engine (500-row chunking, 4-attempt backoff, non-retryable classification) is interleaved with per-table serializers and truncate logic. Adding a new mirrored table means editing the monolith and re-reading the Bug-1 throw semantics every time. Extract engine.ts plus a tables.ts registry map so a new table is one declarative entry.
- [ ] 559. **Split recommendation-action-rows.ts (2303 lines) into evidence-lines, row-builder, and suppression** (Code health, medium)
       src/domains/recommendations/recommendation-action-rows.ts is the largest file in the repo and mixes evidence-line builders, rec-to-row flattening, suppression rules, and QA-downgrade wiring; madge also shows it in a cycle with recommendation-qa.ts. Three focused modules with the existing tests split alongside make the worklist row pipeline safe to change quickly, and breaking the cycle falls out for free.
- [ ] 560. **Set a deliberate index policy for Wix category and pagination pages** (Technical SEO, medium)
       Wix category, tag, and paginated list URLs get crawled and sometimes indexed with no deliberate policy. Detect paginated URL patterns during the scan, check self-canonicals and noindex state per template, and decide index vs noindex per template based on whether GSC shows real demand for those URLs. Encyclopedia hubs with dozens of paginated segments can bleed crawl budget or hide a hub that deserves to rank.
- [ ] 561. **Filter by teammate** (Carry-over, small) [CARRY-OVER 74]
       "show changes the Behavior teammate drove" etc.
- [ ] 562. **Keyboard flow** (Carry-over, small) [CARRY-OVER 64]
       j/k to move, enter to expand, c to copy paste text. Power feel.
- [ ] 563. **Prefetch on hover** (Carry-over, small) [CARRY-OVER 96]
       for row expansion data and the dossier route.
- [ ] 564. **Merge duplicate FAQPage JSON-LD blocks into one valid block** (Content engine, small)
       faq_schema_block_count in PageSnapshot already flags pages carrying more than one FAQPage block, which disqualifies rich results, but no fix is generated. Emit a one-click merge draft that unions the questions into a single valid FAQPage block through the existing schema composer. Small, deterministic, converts an existing detection into a shipped fix.
- [ ] 565. **Check privacy basics per tenant: consent banner, privacy policy page, GA4 consent mode** (Completeness critic, small)
       Beacon installs and reads GA4 and Clarity on customer sites but never checks whether the site is legally allowed to collect that data. Add a day-0 and quarterly check for a consent banner, a reachable privacy policy page, and GA4 consent mode, filing a fix card with copy-paste remediation when missing. CCPA applies to the USA-only launch plan, and this is a checkbox a $250/mo customer's lawyer or platform reviewer will eventually demand.
- [ ] 566. **Add per-route page titles with a title template** (Design, small)
       Only 2 metadata exports exist in all of src/app, so every browser tab and history entry reads just Beacon. Add a title template in src/app/layout.tsx and static metadata to the roughly 15 shell pages (Today, Changes, Results, Competitors, Settings). Cheap, and it makes the app feel like real software the moment two tabs are open.
- [ ] 567. **Build a SectionHeader primitive so every section opens the same way** (Design, small)
       Today sections use text-sm font-semibold text-gray-900 headings while war-room bands use an 11px uppercase tracking-wide HEAD const and worklist uses its own pattern. One component with title, optional uppercase eyebrow, and an optional View-all action slot, used across Today, worklist, and proof, gives the whole app a single recognizable rhythm.
- [ ] 568. **Create one date voice: a shared format-date util replacing 51 scattered calls** (Design, small)
       51 toLocaleDateString calls with varying options produce Jun 30, June 30, and 6/30 across surfaces; scoreboard-section.tsx even keeps a private monthDay helper. Add src/lib/format-date.ts with monthDay, relative (3 days ago), and checkpoint styles, and migrate callers. Dates are on nearly every card, so inconsistency here is felt everywhere.
- [ ] 569. **Delete the orphaned command palette and polish the live one with kbd styling and icons** (Design, small)
       Two command palettes exist: components/shell/command-palette.tsx (446 lines, imported by the shell layout) and src/app/(shell)/command-palette.tsx (186 lines, imported by nothing found). Delete the orphan, then upgrade the live palette with grouped results, lucide icons per destination, styled kbd chips matching the sidebar shortcut hints, and the new overlay elevation token.
- [ ] 570. **Encode scoreboard verdicts with shape as well as color** (Design, small)
       Ship markers in scoreboard-section.tsx distinguish won/measuring/flat purely by MARKER_FILL color (green/gray/amber). Add shape encoding (circle for won, diamond for measuring, triangle for flat) and a one-line legend under the chart, matching the color-plus-glyph discipline already applied to delta arrows in kpi-card.tsx.
- [ ] 571. **Give dark mode a designed temperature instead of inverted grayscale** (Design, small)
       The .dark block in globals.css is pure neutral (oklch(0.145 0 0) background, flat gray cards). Shift background, card, and border a few points toward a cool blue hue and slightly brighten the accent, the way Linear and Vercel dark themes do. One file change, verified with side-by-side screenshots of Today in both themes.
- [ ] 572. **Merge KpiCard and StatCard into a single stat primitive** (Design, small)
       components/viz/kpi-card.tsx (uppercase 10px label, surface-raised bg, hand-rolled delta arrows) and components/data/stat-card.tsx (sentence-case xs label, bg-card, DeltaIndicator) are two visual dialects for the same job. Merge into one component with size variants so every number tile in the app has identical label case, delta treatment, and background.
- [ ] 573. **Standardize page container widths with a PageContainer in the shell layout** (Design, small)
       Shell pages currently pick max-w-3xl (Today), max-w-5xl (worklist), max-w-4xl, max-w-2xl, max-w-md, and max-w-prose ad hoc, so navigating between routes makes the whole app visually jump. Decide two canonical widths (reading and working), expose them as tokens, and add a PageContainer wrapper in src/app/(shell)/layout.tsx that pages opt into by kind.
- [ ] 574. **Sweep every skeleton and Suspense fallback for dark mode and extract a Skeleton primitive** (Design, small)
       CockpitSkeleton in src/app/(shell)/page.tsx and the worklist fallback use border-gray-100 bg-gray-50 with no dark: variant, so dark-mode users get bright white blocks flashing on every navigation. Replace all loading placeholders with a shared Skeleton component built on bg-muted tokens; grep for animate-pulse to find the full set.
- [ ] 575. **Publish a living public win-report page per tenant for social proof** (frontier, small)
       Add /w/[tenant] in the (public) route group rendering the verified Wins band read-only with dates and before/after sparklines, opt-in per tenant. Distinct from the planned share-a-win image: this is a permanent URL a prospect can be sent, and every tenant's page doubles as Beacon marketing. Pure GTM value, no ranking effect, so it waits behind product work.
- [ ] 576. **Pull type=image GSC data and add an image alt, filename, and caption lever** (Search Console depth, small)
       search-analytics.ts hardcodes type: web (~line 204). Persian food, art, and culture pages likely earn image impressions Beacon has never seen; a daily type=image pull reveals it and an image-optimization lever ships with real impression evidence.
- [ ] 577. **Rescue stalled signups stuck in pending_onboarding** (Onboarding and cold start, small)
       Tenants that abandon mid-wizard sit invisible forever because every engine filters status active (provision-tenant.ts defaults). Add a diagnostics list of tenants pending more than 24 hours with the step they stalled on, plus a magic-link resume email landing them on that exact step. When real strangers sign up, wizard abandonment becomes the invisible top-of-funnel leak.
- [ ] 578. **Seed a bounded alt-text fix pack from the cold-start crawl** (Onboarding and cold start, small)
       Alt-text actions exist in the shell (src/app/(shell)/image-alt-actions.ts, image-alt-client.tsx) but nothing feeds them at cold start. Have extractPageSnapshot count images missing alt attributes on the crawled pages and seed a small capped fix pack into the first worklist. Low-risk deterministic inventory that gives the first session more than one thing to ship.
- [ ] 579. **Show sitemap size instantly after the domain saves** (Onboarding and cold start, small)
       parseSitemapUrlEntries (src/domains/scanning/sitemap-parse.ts) can run the moment the business step saves: your site has 214 pages, we will read the 18 most important now and the rest overnight. Sets honest crawl expectations, catches missing-sitemap sites early (itself a finding worth a card), and gives the wizard its first concrete number within seconds.
- [ ] 580. **Emit Server-Timing headers from perf-trace so every prod request carries phase timings** (Performance, small)
       src/lib/perf-trace.ts logs middleware, shell-layout, and loader phases to console only, and only when BEACON_PERF_TRACE=true. Emit the same entries as a Server-Timing response header so browser devtools and Vercel logs show which phase ate the time on any slow click, always on. Add per-section marks around each war-room Suspense band with a millisecond budget label so a regressing section names itself.
- [ ] 581. **Parallelize the serial awaits on Today: the gate pair and the scoreboard citations read** (Performance, small)
       loadTodayV2GateData in src/app/(shell)/today-v2-data.ts awaits hasActiveExperiment then hasAnyConnectedDataSource serially before the cockpit can decide to render, and ScoreboardSection awaits a Promise.all then serially awaits loadOwnCitationsByDay because the slug resolves late. Start all reads concurrently and resolve the slug before the Promise.all. Cheap 300-600ms off every Today paint.
- [ ] 582. **Report real-user web vitals from the shell to a tiny beacon endpoint** (Performance, small)
       All timing knowledge comes from server-side perf-trace; nothing measures what the operator actually feels (TTFB, LCP, INP per route, including RSC streaming). Add useReportWebVitals in a small client component under (shell)/layout.tsx posting to /api/vitals, stored in a lightweight store keyed by route. Turns speed work from anecdotes into a ranked list of the worst real routes, and later becomes a selling point: the team watches its own speed.
- [ ] 583. **Trim the persisted worklist-surface blob to what the UI actually renders** (Performance, small)
       writeWorklistSurface persists the full TodayMovesHeroData including 75 moves with complete drafts, debates, outlines, and title variants to json_store_blobs; every cold lambda transfers and parses the whole blob. Strip fields the collapsed list never shows, or persist full detail only for the top 15, cutting blob bytes, Supabase egress, and parse time on every cold read. Add trace.measureSize to the write path first to get the honest number.
- [ ] 584. **Add knip to the quality gate to catch dead exports, files, and dependencies continuously** (Code health, small)
       The repo is 331k LOC with repeated manual dead-code campaigns (7k LOC deleted recently) but package.json has no unused-export tooling. knip with a curated baseline in npm run check makes dead code visible at commit time, much cheaper than periodic audit sessions, and also flags unused dependencies like the double logging stack.
- [ ] 585. **Capture and audit Open Graph and Twitter card tags on top pages** (Technical SEO, small)
       The extractor ignores og:title, og:description, and og:image entirely. Capture them onto PageSnapshot, flag missing or default-Wix values on the top 50 demand pages, and auto-draft them from existing title and meta artifacts. OG data drives share previews and is read by several AI answer surfaces when rendering source cards.
- [ ] 586. **Probe outbound source links for rot on encyclopedia citation pages** (Technical SEO, small)
       external_link_count exists on PageSnapshot but individual external hrefs are never captured or probed. Store outbound links during extraction, probe them monthly on a polite budget, and flag dead sources on pages where the uncited-content trigger already cares about sourcing. An encyclopedia citing dead links loses credibility with quality raters and with AI engines that follow citations to judge source quality.
- [ ] 587. **Close the day with a plan completion moment** (Today dashboard, small)
       When the last item of an accepted plan flips to verified live in the execution checklist (execution-data.ts, completeDailyPlanAction path), render an inline completion state in daily-experiments-section.tsx: all 3 changes are live, first reads land July 8, I will take it from here. One transition state, no new data; ending the loop with a handshake instead of a silent empty card makes the ritual feel finished.
- [ ] 588. **Reorder Today by time of day: results first in the morning, tonight's picks first in the evening** (Today dashboard, small)
       page.tsx already computes the Pacific hour for the greeting; use the same value to swap section order in renderCockpit: before noon lead with scoreboard plus landed results, after 6pm lead with the plan review since the run happens overnight. Zero new data, one conditional layout, the personalization a human chief of staff applies unasked.
- [ ] 589. **Attach Google's rich-results validator link to every schema ship** (Trust and receipts, small)
       morning-brief.ts already links search.google.com/test/rich-results once in a draft. Systematize it: every add_schema or fix_schema row on proof/page.tsx gets the validator link prefilled with the live URL so the operator can confirm structured data on Google's own tool. Cheap, external, and it closes the loop on the least-visible change type Beacon ships.
- [ ] 590. **Cold-store a full collection export before any multi-item bundle lands** (CMS push depth, small)
       Per-field snapshots protect single edits, but a misconfigured bundle or reference-population run touching many items has no collection-level restore point. Before any bundle of 3 or more writes to one collection, export the full collection via wixQueryAllDataItems into a Supabase cold-store table with a retention window. Disaster insurance that makes aggressive multi-page moves defensible to a paying customer.
- [ ] 591. **Use the Wix bulk items endpoint for cluster runs to cut rate-limit exposure** (CMS push depth, small)
       The create route inserts factory pages one POST at a time, so a 10-item cluster run makes 10 sequential writes against the 429-retry budget in client.ts. Add wixBulkInsertDataItems (Wix Data v2 bulk insert) with per-item result parsing so partial failures map back to individual cards, and route factory-run.ts through it. Faster runs, fewer retries, same approval and cap semantics.
- [ ] 592. **Fix the sticky batch bar's minute math to use real effort estimates** (Worklist UX, small)
       The sticky bar in daily-experiments-section.tsx line 495 claims about {s.left} minutes, assuming one minute per item; an answer block plus two metas is not 3 minutes. Sum effortMinutes of the actually-pending items from checklist.items instead. Small, but the bar is the most-seen number during the nightly ritual and the Beacon voice promises concrete honest numbers.
- [ ] 593. **Printable run sheet for tonight's accepted batch** (Worklist UX, small)
       wixInstructions() in src/domains/experiments/execution-checklist.ts already produces copy-exact numbered steps per item. Add a Print run sheet button on ExecutionChecklistView that opens a print-styled view: one numbered block per pending item with URL, exact Wix path, old text, new text, and a checkbox square. The operator or a helper can execute the whole batch away from the app; the command palette already proves window.print works here.
- [ ] 594. **Extract the pure section builders from today-v2-data.ts into src/domains/today with tests** (Code health, large)
       src/app/(shell)/today-v2-data.ts is 1238 app-layer lines feeding the war room while its domain half (visibility-read-model.ts, 1209 lines with equivalence tests) already lives in src/domains/today. Move the pure builders (trend rows, declines, lifecycle summary composition) next to the read model with characterization tests as the first concrete slice of the known dual-surface collapse. Every future Today change then edits tested domain code, not a route file.
- [ ] 595. **Turn on noUncheckedIndexedAccess and fix the fallout in scoring and curve math** (Code health, large)
       tsconfig.json has strict but not noUncheckedIndexedAccess, and core math indexes arrays bare-handed (CTR_BY_POSITION[p - 1]! in ctr-curve.ts, sparkline point access, rows[0] reads in surface stores). Enabling it makes undefined-in-arithmetic a compile error instead of a NaN on an operator card. Land it in one sitting with targeted guards; the pain is front-loaded, the protection is permanent.
- [ ] 596. **Lay Shopify adapter groundwork with productUpdate SEO fields and metafield JSON-LD** (CMS push depth, large)
       The Stores seoData route in push-service.ts proves the product-SEO pattern; Shopify is the same shape at 100x market size. Add a shopify target skeleton under the PublishAdapter interface: Admin GraphQL productUpdate for the seo title and description fields, metafields for structured data, and products-by-handle lookup replacing the slug scan. Ship as adapter plus capability matrix only, gated off until a Shopify test subject exists.

## Impact 3

- [ ] 597. **Stop ignoring tests/ and scripts/ in eslint and settle on one test-location convention** (Code health, medium)
       eslint.config.mjs globalIgnores tests/** and scripts/**, leaving 678 test files and 145 scripts completely unlinted, and tests split between two conventions (265 colocated in src, 678 in the tests/ mirror) so finding the pin for a module is a two-place search. Lint both trees and pick colocation as the standard for new tests. Unlinted test code is where dashes, dead imports, and broken patterns accumulate unseen.
- [ ] 598. **Delete dead nav shortcuts** (Carry-over, small) [CARRY-OVER 110]
       ("G T"/"G C" hints) unless keyboard nav (D64) ships; half -implemented power features read as neglect.
- [ ] 599. **Add motion duration tokens and staggered entrances for card lists** (Design, small)
       beacon-rise-in in globals.css fires all cards simultaneously. Define --motion-duration-fast/base and --motion-ease tokens, then stagger war-room bands and changes rows with capped nth-child delays (30ms steps, max 6). The existing prefers-reduced-motion block already covers disabling. Sequential arrival is the cheapest alive-feeling upgrade available.
- [ ] 600. **Brand pass on the login screen** (Design, small)
       src/app/login (page.tsx plus login-form.tsx) is functional but unbranded, and it is the first screen any $250/mo stranger sees. Add the new mark, one sentence of promise in the Beacon voice, and a faint illustrated scoreboard backdrop drawn from the same micro-illustration style, so the front door matches the interior quality.
- [ ] 601. **Create one number voice: compact, money, and percent formatters plus a num utility class** (Design, small)
       Values mix raw numbers and toLocaleString, and tabular-nums is applied ad hoc in 317 places. Add formatCompact (12.4k), formatMoney, and formatPct in src/lib/format-number.ts plus a .num utility class bundling tabular-nums and tracking, then apply to KpiCard, TodayCounts, and the proof ledger so columns of numbers always align and read identically.
- [ ] 602. **Define an elevation token system for floating surfaces** (Design, small)
       Shadows appear exactly twice in the entire app (shadow-sm), so the sticky batch bar on worklist, sheets, and the command palette sit flat against content. Add --shadow-raised and --shadow-overlay tokens in globals.css tuned separately for dark mode (shadows need more spread and less opacity on dark) and apply them to every floating surface so layering reads instantly.
- [ ] 603. **Standardize one focus-ring utility and delete per-file FOCUS constants** (Design, small)
       war-room-sections.tsx defines a FOCUS const using sky-500, Today attention rows use amber-400 rings, and button.tsx uses ring-ring/50; keyboard focus looks different on every surface. Add a .beacon-focus utility in globals.css built on the --ring token and replace the scattered constants, keeping the existing 61 focus-visible sites consistent.
- [ ] 604. **Enable Turbopack filesystem caching so dev route compiles stop costing 100 seconds** (Performance, small)
       The worklist perf memory notes the ~100s was mostly dev compile. package.json runs plain next dev; verify Turbopack is actually active on Next 16.2.2 and enable the experimental filesystem cache so warm dev restarts reuse compiled route graphs. Dev-only, but it directly speeds every daily iteration loop on this project.
- [ ] 605. **Memoize canonicalizeCitationUrl with a bounded module-level map** (Performance, small)
       canonicalizeCitationUrl runs new URL() parsing in the hottest loops: per-row in the graph build across thousands of citation rows, per-move in gap-compiler.ts, and per-pack in moves-data.ts joins. A bounded Map cache of roughly 20k entries makes repeat canonicalizations O(1) string lookups. Small CPU win multiplied across every heavy compute path.
- [ ] 606. **Archive the 40 one-off underscore probe scripts out of the typecheck path** (Code health, small)
       scripts/ holds 145 files of which about 40 are underscore-prefixed one-off probes (_probe.ts, _moves-ab.ts, _gsc-sync-probe.ts) that tsconfig still typechecks while eslint ignores them entirely. They bit-rot against moving domain APIs and then break tsc at the worst moment. Move keepers to scripts/probes with a README, delete the rest, exclude the archive from tsconfig.
- [ ] 607. **Export one shared ConfidenceLevel type to replace 95 inline re-declarations** (Code health, small)
       The literal union high, medium, low appears inline 95 times in non-test source, from TodayMove.confidence to dollarPotential in demand-graph/money-model.ts. One exported type next to src/lib/confidence-labels.ts (which already owns the display words) removes remapping glue, prevents casing typos, and gives one place to evolve the scale. Mostly mechanical find-and-replace.
- [ ] 608. **Fold the orphan src/adapters, src/derivations, and src/storage layers into src/domains** (Code health, small)
       Three tiny top-level layers predate the domains structure: src/adapters (profound plus types.ts), src/derivations (snapshot-builder.ts), and src/storage (canonical-store.ts). They add a where-does-this-go tax on every new file and a fourth import vocabulary for the same concepts. Move profound adapters into src/domains/profound-deep or lib/connectors/profound, canonical-store next to its consumers, and delete the empty layers.
- [ ] 609. **Rename the twin evidence-packet modules so the Move packet and the LLM packet stop colliding** (Code health, small)
       src/domains/demand-graph/evidence-packet.ts (the source-of-truth EvidencePacket every surface renders) and src/domains/recommendations/evidence-packet.ts (the bounded JSON packet for the gpt-5-mini adjudicator) share a filename and a concept name while serving different systems. Imports, greps, and future agents conflate them constantly. Rename the adjudicator one to adjudicator-packet.ts with re-export shims removed in the same pass.

## Impact 2

- [ ] 610. **Animate KPI values with a reduced-motion-safe count-up on first paint** (Design, small)
       A tiny client hook that counts the value up over 250ms on mount for KpiCard, the TodayCounts strip, and the scoreboard week-over-week number, disabled under prefers-reduced-motion. Small touch, but the morning open of Today is the product's daily first impression and it currently just pops.

