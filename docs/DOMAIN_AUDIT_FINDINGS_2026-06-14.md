# Domain Deep-Audit Findings — 2026-06-14

Source: 24-agent domain-by-domain workflow audit (`wf_6ff582e0-f3f`). **30 confirmed** (adversarially verified, default-refute), 9 refuted. Severity: 3 critical · 15 high · 11 medium · 1 low.

**Fixed this session (commit `16aa432`):** promotion-writer dedup (HIGH), indexability `none` (MED), dual-write tenant-scope (MED). The rest are the prioritized work queue below.

> Each was verified by an independent adversarial agent that read the actual code + cited the guarding/reachability path. Re-verify before fixing; the 3 CRITICALs touch id-format / onboarding-insert / hot render path and need tested, migration-aware fixes (not rushed).

## [CRITICAL] tenant_isolation — `src/app/(shell)/today-data.ts:228-232,1847-1875`
- **Issue:** loadTodayPageData() reads the GLOBAL prompt-library store via getPromptLibrary()/getActivePrompts() and feeds it into customer-facing insights: computeJourneyCoverage(promptLibrary) (line 1865) and computeGeoCoverage(allPages, ..., activePrompts) (line 1847 + 1076). prompt-library is classified GLOBAL in store-classification.ts:194 and lives at a single shared file .data/global/prompt-library.json — verified to contain 100 Ritz-Builders Profound prompts (Bay Area / Atherton / Palo Alto custom-home-builder questions) with NO tenant_id. There is no per-tenant prompt-library file. So every tenant's /today computes journey coverage and geo coverage from Ritz's corpus, not its own tracked_prompts. The correct per-tenant source is tracked_prompts (iranopedia has 0; ritz has 100) which /prompts and the decision-matrix already use — only this loader still reads the global library.
- **Customer impact:** Data condition: a non-Ritz active tenant (tenant-iranopedia / tenant-finglish) loads /today. Because the global library always reports 100 active prompts, the 'Journey stage insight' next-move card fires (gate is total_active>=10) describing journey-stage gaps over Ritz's builder prompts, and the geo-coverage card counts prompt demand for Ritz's Bay Area cities (Atherton, Palo Alto). An Iranian-encyclopedia or Finglish customer sees a recommended next action and geographic-gap analysis derived entirely from a different customer's builder business — a wrong-customer-output + tenant-isolation breach on the primary dashboard.
- **Fix:** Stop reading the global prompt-library on the render path. Replace getPromptLibrary()/getActivePrompts() in loadTodayPageData with the tenant-scoped tracked_prompts already loaded via getRepository().forTenant(tenantId).getTrackedPrompts(), and pass those (filtered to is_active for the geo call) to computeGeoCoverage/computeJourneyCoverage. computeJourneyCoverage takes LibraryPrompt[] but only reads .is_active and .journey_stage; map TrackedPrompt -> {is_active, journey_stage: classifyJourneyStage(text)} or add a TrackedPrompt overload. Same swap is needed at the /today geo call (line 1076-1080).
- **Verified (medium):** CONFIRMED REAL (journey-coverage card). loadTodayPageData reads getPromptLibrary()/getActivePrompts() (today-data.ts:228-232) from the GLOBAL prompt-library store (store-classification.ts:194 lists it global; json-store resolves global stores to a single .data/global/prompt-library.json — no per-tenant path). That file holds 100 active prompts, all Bay Area custom-home-builder questions, zero tenant_id. computeJourneyCoverage(promptLibrary) at line 1865 fires its card under gate absent_stages>0 && total_active>=10 (line 1866); the data is 92 consideration + 8 comparison + 100 active, so CORE_S

## [CRITICAL] tenant_isolation — `src/domains/daily-metric-snapshots/build-from-observations.ts:349`
- **Issue:** The deterministic snapshot `id` contains NO tenant_id segment, but the Supabase upsert keys ONLY on `id` (PK is `id` alone — see migrations/2026-05-08_baseline_schema.sql:1152-1153, and no later migration added a (tenant_id,id) composite). The platform-aggregate row id is `derived-${date}-platform-${platformSlug}` (line 349) with no scope_id and no tenant distinction, and the platform label is a tenant-independent constant (`"Perplexity"`/`"ChatGPT"`, run-poll.ts:244-247). So EVERY tenant polling the same platform on the same UTC date produces the IDENTICAL id, e.g. `derived-2026-06-14-platform-perplexity`. syncDailyMetricSnapshots calls dualWriteUpsert(..., "id") via the RLS-bypassing service-role admin client (dual-write.ts:620-630, 59-61), so ON CONFLICT(id) DO UPDATE updates ALL columns — including `tenant_id` — to the LAST writer's values. Topic rows collide too whenever two tenants share a topic key (e.g. empty topic → `derived-${date}-topic-unknown-${platform}`, line 208), and entity rows collide on any shared scope_id slug (shared directories like yelp/houzz; entityToScopeId TLD-stripping at line 596-603). The poll-watchdog cron dispatches polls for multiple tenants on the same day (src/app/api/cron/poll-watchdog/route.ts), so the collision fires every day there is more than one active tenant.
- **Customer impact:** Two tenants (e.g. tenant-ritz-founder + tenant-iranopedia) both poll Perplexity on 2026-06-14. Ritz's poll completes first, writing the platform row with ritz's numbers. Iranopedia's poll completes second and upserts the SAME id, overwriting that row in place — its tenant_id flips to tenant-iranopedia and its counts become iranopedia's. When Ritz then opens /today, fetchTodayDerivedKpis filters `.eq("tenant_id", "tenant-ritz-founder")` (today-kpis.ts:64) and the row no longer matches → Ritz gets zero platform rows → the headline KPI tiles silently fall back to yesterday or render null/missing (today-data.ts:485-494), and today-primary-share's `{Platform} N% primary` hero pill disappears or shows the wrong day. Whichever tenant polls last is the only one whose platform-scope numbers survive for that day; all earlier tenants lose their materialized headline metrics. Same loss applies to any colliding topic/entity scope rows.
- **Fix:** Embed tenant_id into the snapshot id so it is globally unique, e.g. prefix every emitted id with the tenant (or a stable tenant slug): `const idPrefix = \`derived-${tenantId}-\``; and use it for the entity/topic/prompt/platform ids — OR migrate the table to a composite PK `(tenant_id, id)` and change syncDailyMetricSnapshots to `dualWriteUpsertScoped("daily_metric_snapshots", stamped, "tenant_id,id", tenantId)`, mirroring exactly what task #65 did for tracked_prompts/tracked_entities. The composite-PK route is preferred because it also makes the conflict target enforce tenant scope at the DB layer. Add a cross-tenant collision regression test (two tenants, same date/platform, assert both platform rows survive).
- **Verified (high):** CONFIRMED REAL and reachable in production. (1) The deterministic snapshot id has no tenant segment: build-from-observations.ts:349 emits `derived-${date}-platform-${platformSlug}`; the platform label is the tenant-independent constant "Perplexity"/"ChatGPT" (run-poll.ts:244-247). Topic ids (line 208) and entity ids (line 112) are likewise tenant-free. (2) syncDailyMetricSnapshots (dual-write.ts:620-630) upserts with onConflict:"id" via the RLS-bypassing service-role admin client; PK is `id` alone (baseline_schema.sql:1152-1153) and NO later migration added a composite (tenant_id,id) PK (grep 

## [CRITICAL] edge_case_crash — `src/app/(shell)/onboard/review/launch-flow.ts:59-83 (buildTrackedPromptRow) consumed at :262-274 (bulk INSERT)`
- **Issue:** The launch-time starter-prompt INSERT omits the `tenant_id` column entirely. buildTrackedPromptRow only sets `account_id: tenant.slug` (the bare slug, e.g. "abc12345") and never sets `tenant_id`. The Supabase `tracked_prompts` table declares `tenant_id text DEFAULT '' NOT NULL` plus CHECK constraint `tracked_prompts_tenant_id_nonempty_chk` requiring `tenant_id <> ''` (migrations/2026-05-09_phase1_stage_b_c_tenant_id_repair.sql:215-224). With no tenant_id supplied, the column defaults to '' and the bulk INSERT at line 272 is REJECTED by the CHECK constraint, returning `prompt_insert_failed` (line 280) which aborts executeLaunchTransaction before the status→active flip. This is the identical bug already found and fixed in src/app/(shell)/settings/prompts/actions.ts:113-119 ("tenant_id was NEVER set on insert — it defaults to '' which the ... CHECK rejects, so a UI-added prompt's INSERT FAILED outright"), but the onboarding launch path was not fixed. The launch-flow.test.ts mock (line 261) only asserts account_id and does not enforce the CHECK, so tests pass while prod fails. Secondary: even if the insert landed, the read path (supabase-backend.ts:1205-1214 getTrackedPrompts) and the /today first-reading gate (today-v2-data.ts:912-920) filter strictly on `.eq("tenant_id", tenantId)` with the FULL tenant-xxx id, so account_id=slug rows would be invisible to every tenant including their owner.
- **Customer impact:** Every brand-new customer who completes the wizard and clicks Launch on production (DATA_SOURCE=supabase) hits prompt_insert_failed; the tenant is never flipped active, no prompts are tracked, and the first scan never dispatches — onboarding is fully broken for new tenants (Ritz/Iranopedia/Finglish predate the constraint via the migration backfill, so the break is invisible until the next real signup).
- **Fix:** In buildTrackedPromptRow add `tenant_id: args.tenantId` (full tenant id) and thread the full id through from executeLaunchTransaction (pass tenant.id, not tenant.slug). Keep account_id for compatibility but set tenant_id = tenant.id so it satisfies the nonempty CHECK and matches the tenant_id-based read path. Update the launch-flow test mock to enforce a non-empty tenant_id on insert so the regression is caught.
- **Verified (high):** CONFIRMED REAL. buildTrackedPromptRow (launch-flow.ts:59-83) sets only account_id and never tenant_id; the caller (lines 264-268) passes accountId: tenant.slug. tenant.slug is tenant.id with the "tenant-" prefix stripped (provision-tenant.ts:151), so slug ("abc12345") differs from the canonical id ("tenant-abc12345"). The tracked_prompts.tenant_id column is `text DEFAULT '' NOT NULL` with a live CHECK constraint tracked_prompts_tenant_id_nonempty_chk requiring tenant_id <> '' (migrations/2026-05-09_..._tenant_id_repair.sql:216-224); no later migration drops it (the DROP is only in a commented-

## [HIGH] data_corruption_or_loss — `src/domains/recommendation-intelligence/promotion-writer.ts:257-263` ✅ FIXED
- **Issue:** The promotion id/rec_id is keyed ONLY on (tenant, action_type, target_url) — `rec_id = promotion-${cooldown_key.slice(0,16)}` and `id = ${rec_id}__${action_type}__null`, where cooldown_key = sha1(tenant::action::url) with NO topic_cluster_label (see dedupe-cooldown.ts buildPromotionCooldownKey + promotion-result-to-edit-row.ts:144-145). But THREE distinct customer-queue-ready triggers all emit `edit_title` for the same page: gsc_low_ctr, gsc_striking_distance, and semrush_striking_distance (promotion-eligibility.ts:138,142,149). They survive the loader's dedupeByKey (different topic → different dedupe_key) and the α₁a mapper (no id-dedup), so `mapped_rows`/`writeRows` can contain 2-3 rows with the IDENTICAL `id` and the IDENTICAL Supabase conflict key (tenant_id, rec_id, action_type, target_element_key). `writeRows` (line 257) is passed un-deduped to syncRecommendedEdits → dualWriteUpsert (dual-write.ts:59-61) calls `sb.upsert(chunk, {onConflict})` WITHOUT batch-dedup (unlike syncPageElementInventory:1473 / syncObservationRuns which DO dedup the same conflict class). Postgres then raises the non-retryable 'ON CONFLICT DO UPDATE command cannot affect row a second time' for the whole 500-row chunk.
- **Customer impact:** Realistic, common GSC data condition: any single page that underperforms on more than one query (low-CTR on query A AND striking-distance on query B, and/or a SEMrush striking keyword) produces 2+ edit_title promotion rows with the same conflict key. On production (DATA_SOURCE=supabase, where the local file write is an ephemeral no-op), the duplicate-keyed upsert chunk is REJECTED by Postgres, the writer catches it into `sync_warning`, and NONE of that chunk's promotion rows land in `recommended_edits` — the operator clicks 'Promote', sees a sync warning, and the customer queue silently fails to refresh with the new recommendations. In the non-throwing path (or if dedup were added keeping the last element), the rows collapse to ONE and `persistRecommendedEditsLocal`'s `byId.set` keeps the LAST element of the priority-DESC `mapped_rows` array — i.e. the LOWEST-priority signal wins, so the weaker third-party/striking signal overwrites the stronger first-party bleeding-clicks gsc_low_ctr card, contradicting the GSC-led pivot and showing the customer the weaker reason + a weaker proposed title.
- **Fix:** In promotion-writer.ts, dedup `mapped_rows` by `row.id` BEFORE building `writeRows` (and before persisting), keeping the FIRST occurrence (mapped_rows is already in priority-DESC order, so first = highest priority). E.g. `const byId = new Map(); for (const r of mapped_rows) if (!byId.has(r.id)) byId.set(r.id, r); const deduped = [...byId.values()];` then map/persist `deduped`. Optionally also add a defensive batch-dedup by the onConflict key inside syncRecommendedEdits (mirroring syncPageElementInventory:1473) so any future duplicate-keyed batch can't crash the whole chunk.
- **Verified (high):** VERIFIED REAL. Three customer-queue-ready triggers — gsc_low_ctr, gsc_striking_distance, semrush_striking_distance — all emit action_type "edit_title" on target_url=snapshot.url at confidence "medium" with no safety flags (gsc-low-ctr.ts:103-117/223-225, semrush-striking-distance.ts:63-65), so applyQueueRules routes ALL THREE to `candidates` (apply-queue-rules.ts:75-85). Their dedupe_key includes topic_cluster_label (the query) but cooldown_key = sha1(tenant::action::url) does NOT (cooldown-key.ts:27-31; dedupe-cooldown.ts buildPromotionCooldownKey 123-132). So they survive loader dedupeByKey 

## [HIGH] dead_or_unreachable — `src/domains/recommendations/suggested-copy-display-guard.ts:136-137 (CAMEL_CASE_MULTI_TRANSITION_RE) consumed by src/domains/recommendations/suggested-copy-adapters.ts:221-238 (buildCopyTile add_schema branch)`
- **Issue:** The render-time display-safety guard's multi-transition camelCase detector (CAMEL_CASE_MULTI_TRANSITION_RE = /\b[a-z][a-z0-9]*[A-Z][a-z0-9]+[A-Z][a-zA-Z0-9]*\b/) matches legitimate JSON-LD schema.org property keys such as `itemListElement` and `openingHoursSpecification`. For an `add_schema` action row, buildCopyTile() builds a `section` tile and runs checkCopyDisplaySafe() over the body, which is the deterministic JSON-LD `proposed_text` produced by composeSchema / composeStoreBreadcrumbSchema (src/domains/recommendation-intelligence/draft-enrichment.ts:603-657, 700-752). Every BreadcrumbList block for a non-root page contains `itemListElement`, so the guard returns {safe:false, reason:'camel_case_identifier'} and buildCopyTile returns a `fallback` tile. I verified the regex match against a realistic block: the match is `itemListElement`. This is not LLM-leaked internal taxonomy — it is the exact machine-readable markup the operator is supposed to paste.
- **Customer impact:** Data condition: any accepted/queued `add_schema` recommendation whose JSON-LD draft includes a BreadcrumbList (i.e. every page below the site root — the common case, e.g. Iranopedia/Ritz service & location pages, and Wix store-product breadcrumb pushes). What goes wrong: on /recommendations/[id] the Suggested Copy act renders the generic fallback 'Beacon has a draft for this recommendation, but it needs review before showing here.' instead of the ready-to-paste schema. There is no other surface that renders the schema text (v2 is the default; legacy copy path doesn't cover it), so the customer can never see or copy the JSON-LD. This silently defeats the entire Content Schema Engine deliverable (tasks #33-#38) for the customer.
- **Fix:** Special-case schema markup in buildCopyTile so the camelCase/snake-case identifier detectors don't suppress valid JSON-LD. Minimal: for actionType `add_schema`, skip checkCopyDisplaySafe (or run only the UUID + HARD_BLOCKED_TOKENS checks, not the camel/snake heuristics) and render the proposed_text verbatim as a `section`/`table`-style code block — the text is deterministically generated, not LLM output, so the heuristic leak-detectors are both unnecessary and harmful here. Add a guard-bypass flag/argument to checkCopyDisplaySafe (e.g. {allowMarkupIdentifiers:true}) used only on the schema branch, and pin it with a test asserting an `add_schema` row with a BreadcrumbList renders a non-fallback tile.
- **Verified (high):** CONFIRMED REAL (with one impact caveat). The render-time guard genuinely suppresses valid schema on the canonical detail page, via a live, non-flag-gated customer path. Mechanism verified end-to-end: - CAMEL_CASE_MULTI_TRANSITION_RE (suggested-copy-display-guard.ts:136-137) matches schema.org keys — executed it: `itemListElement`, `mainEntityOfPage`, `openingHoursSpecification` all match; `ListItem`/`BreadcrumbList`/`WebPage` do not. - add_schema rows carry the JSON-LD as proposedText: actionRowTypeForEdit maps action_type add_schema/fix_schema → "add_schema" (recommendation-action-rows.ts:347

## [HIGH] pivot_inconsistency — `src/domains/citation-lifecycle/compute-repeat-citation.ts:278-329 (recordMatch + first_citation_date_iso), exploited via src/domains/citation-lifecycle/load-lifecycle.ts:903-919 (deps injection) colliding with src/domains/citation-lifecycle/load-repeat-citation.ts:163-169 (cache key)`
- **Issue:** computeRepeatCitation derives first_citation_date_iso from EVERY observation passed in (recordMatch unconditionally pushes to allMatchDates with no live_at floor — lines 278-285, 320-329). It relies entirely on the loader scoping observations to `since: live_at`. The Today-tile summary loader breaks that assumption: loadLifecycleSummaryForTenant injects `allObservations` fetched `since: now - 90d` into loadRepeatCitationForEdit via `deps.promptAnswerObservations` (load-lifecycle.ts:903-908), and for any edit whose target page was cited BEFORE it went live, those pre-live citations now reach the compute and set an earlier first_citation_date_iso. The Changes-detail page (changes/[id]/page.tsx:375) and the diagnostics page call the SAME loader WITHOUT deps and with the SAME windowDays=30, so they share the identical unstable_cache key ["repeat-citation:v1", tenantId, edit.id, live_at, "30"] (load-repeat-citation.ts:163-169) — but feed different inputs (since:live_at vs since:now-90d). The key does not encode deps presence, so whichever surface warms the 60s cache first wins. The existing unit test (tests/domains/citation-lifecycle/compute-repeat-citation.test.ts:188-210) even feeds a citation 90 days before live_at and asserts not_repeated with a non-null first_citation_date_iso, confirming the compute has no live_at guard.
- **Customer impact:** Data condition: a recommended edit to an EXISTING page that was already cited by AI before the edit shipped (the common case — most edits target existing URLs), with >=7 native poll days and zero in-window citations in the last 30 days. Symptom: the Today tile's repeat-citation rollup and the Changes-detail 'Citation stability' sub-line (repeat-citation-act3.tsx) flip nondeterministically between two states based on which page the operator opened first within the 60s cache window. If the Today summary warms the cache: band = not_repeated, and Changes detail renders 'Citation stability: Not repeated in this window · First cited <DATE>' where <DATE> is BEFORE the edit went live — a misleading claim on a surface explicitly titled 'what happened after this edit went live.' If Changes detail warms it first: band = still_learning with null first-citation, so the sub-line is suppressed entirely (repeat-citation-act3.tsx:109) and the same edit shows nothing. Same edit, same tenant, contradictory customer-facing answers.
- **Fix:** Add a live_at floor to the first-citation computation in computeRepeatCitation so the metric is robust to how the loader scopes observations. Compute `liveDateIso` is already available (line 222); change recordMatch (or the allMatchDates reduction at 320-329) to ignore any match whose dateIso < liveDateIso, so first_citation_date_iso only ever reflects citations at-or-after the edit went live. This eliminates both the deps-vs-non-deps divergence and the misleading pre-live 'First cited' date, and makes the cache-key collision harmless. Update the test at compute-repeat-citation.test.ts:188-210 (which currently encodes the buggy pre-live-counts-as-first-citation behavior) to expect still_learning / null first-citation for a citation strictly before live_at. (Alternatively/additionally, append a deps-presence discriminator to the loadRepeatCitationForEdit cache key, but the compute-layer floor is the correct root fix since the injected superset is genuinely wrong about pre-live citations regardless of caching.)
- **Verified (high):** VERIFIED REAL. Both conditions hold. (1) Real customer path: Two non-gated customer surfaces collide on the same unstable_cache key. /today's TodayV2EditLifecycleSection (today-v2-sections.tsx:97-110) calls loadLifecycleSummaryForTenant with default windowDays=90 (TODAY_TILE_DEFAULT_WINDOW_DAYS, load-lifecycle.ts:289,769). That loader fetches allObservations `since: now-90d` (load-lifecycle.ts:806-811) and, because 90>=REPEAT_CITATION_TILE_WINDOW_DAYS(30), injects that 90d superset as deps.promptAnswerObservations into each per-edit loadRepeatCitationForEdit at windowDays:30 (load-lifecycle.ts

## [HIGH] data_corruption_or_loss — `src/domains/attribution/change-outcome-store.ts:45`
- **Issue:** The Proof Engine's StoredChangeOutcome records and the legacy ChangeOutcome records share the SAME json-store name `change-outcomes`. change-outcome-store.ts uses `STORE_NAME = "change-outcomes"` for both writeStore (persistChangeOutcomes, line 443) and readStore (loadAllChangeOutcomes, line 498), while the legacy change-outcome.ts writes a structurally different shape to the identical store name via `writeStore("change-outcomes", outcomes)` (lines 167, 193). They are TENANT_SCOPED_STORES (store-classification.ts:61), so both writers route to the same per-tenant file/in-process cache key. The legacy materializer (materializePerChangeOutcomes) is called from the Profound import pipeline (import-orchestrator.ts:389) and refreshAttribution (lib/import/actions.ts:752), both of which run in the web app process. loadAllChangeOutcomes reads local-first and returns the raw array with NO shape validation on the local branch (lines 498-501 return `local` directly when length>0). The legacy ChangeOutcome shape has no `source_id`/`status`/`computed` fields.
- **Customer impact:** After an import/refreshAttribution runs in a warm web lambda, the in-process cache key `change-outcomes::tenant:{slug}` holds legacy-shape rows. The next /changes/[id] request reads those via loadAllChangeOutcomes → loadChangeOutcomeById's `all.find(o => o.source_id === sourceId)` never matches → the causal Proof drilldown silently shows the empty/watching state instead of the real computed diff-in-diff result. loadProvenWins filters `o.status === "computed"` → the home-screen proven-wins rail goes empty. loadCausalSelfForecast's isCausalGrade checks `o.status === "computed"` → the Move forecast is suppressed. In file mode (dev/tests/cron local FS) the two writers clobber each other's .data/tenants/{slug}/change-outcomes.json on disk, and conversely getOutcomesForTenant (today-data.ts:1102) filterByTenant drops v2 rows that have no tenant_id field — so the collision corrupts both directions. Net: Beacon's core causal 'Proof' wedge goes dark or wrong on /changes/[id] and /today.
- **Fix:** Give the Proof Engine store a distinct name. Change `STORE_NAME` in change-outcome-store.ts from "change-outcomes" to e.g. "change-outcomes-v2" (and add it to TENANT_SCOPED_STORES in store-classification.ts). This isolates the StoredChangeOutcome local store/cache from the legacy ChangeOutcome store, matching the already-separate Supabase tables (change_outcomes_v2 vs change_outcomes). As defense-in-depth, also make loadAllChangeOutcomes' local branch filter to records that actually carry a StoredChangeOutcome discriminator (e.g. `'status' in o`).
- **Verified (medium):** CONFIRMED collision. change-outcome-store.ts:45 STORE_NAME="change-outcomes" (write 443 / read 498) and legacy change-outcome.ts writeStore("change-outcomes",...) (lines 167/193) share the identical TENANT_SCOPED store (store-classification.ts:61) → same cache key change-outcomes::tenant:{slug} and same .data file, with incompatible shapes (legacy: id/change_id/tenant_id; v2: source_id/status/computed). Wiring is real and in-process: the v2 writer (persistChangeOutcomes via buildAndPersistTenantProof) runs in the CRON (run-scheduled-generation.ts:448), but the legacy writer materializePerChang

## [HIGH] fail_loud_gap — `src/domains/scanning/orchestrate-scan.ts:221`
- **Issue:** The scan's change-detection baseline is read from local disk, not Supabase, so it is empty on every production (GitHub Actions) scheduled scan. `runWebsiteScan` captures `previousSnapshots = await getPageSnapshots()` (line 221), and `regenerateScanFindings` reads `freshSnapshots = readDotDataJson('page-snapshots')` (line 87). Both `getPageSnapshots()` (src/domains/pages/snapshot-store.ts:7) and `readDotDataJson('page-snapshots')` resolve ONLY to the local file `.data/tenants/{slug}/page-snapshots.json` (dotdata-json.ts has no DATA_SOURCE/Supabase branch). The daily-scan.yml workflow does a fresh `checkout` + `npm ci` with NO step that hydrates `.data` from Supabase, `.data` is gitignored, and the CLI (scripts/scan-owned-pages.ts:280 `loadPreviousSnapshots`) likewise reads only that disk file. So on the wiped GH runner the prior snapshot set is absent → baseline is []. In detect-findings.ts the diff loop does `const prev = prevByUrl.get(key); if (!prev) continue;` (lines 231-232), so with an empty baseline EVERY page is skipped and no diff-based finding is ever produced. Note the persisted Supabase snapshots ARE available via the repository read `getRepository().forTenant(tenantId).getPageSnapshots()` (repositories/supabase-backend.ts, `.eq('tenant_id', tenantId)`), which is what other domains (match-runner, recommendation-intelligence) use — the scan just imports the wrong disk-only helper.
- **Customer impact:** Data condition: any night the nightly GH-Actions scan runs (i.e. always in prod, where the runner FS is wiped between runs). What goes wrong: all diff/change findings — title_changed, meta_changed, h1/h2/h3_changed, canonical_changed, faq_changed, schema_changed, content_changed, links_changed, schema_entity_names_changed, unexpected_change — are NEVER generated. The operator's core /today 'N changes detected' review strip stays empty even when the customer's site actually changed (e.g. someone rewrote a page title or stripped FAQ schema). The product silently fails its change-detection promise; only baseline-free findings (faq_without_schema, schema_invalid, robots_txt_blocked, schema_missing_for_page_type) keep firing, masking the gap.
- **Fix:** In orchestrate-scan.ts read the pre-scan baseline through the tenant repository instead of the disk shim: `const previousSnapshots = [...await getRepository().forTenant(tenantId).getPageSnapshots()]` (and similarly source `freshSnapshots` in regenerateScanFindings from disk only because the CLI just wrote it — that part is correct). Prefer the uncapped variant (getAllPageSnapshotsForGeneration, used by promotion-writer/load-trigger-candidates) so encyclopedia-scale tenants like Iranopedia (~1085 pages) get a full baseline rather than the 500-row capped read. Alternatively, add a `.data` hydrate-from-Supabase step to daily-scan.yml before the scan runs.
- **Verified (high):** CONFIRMED real on the only production scan path. The scheduled scan runs exclusively on GitHub Actions (daily-scan.yml) — the workflow header documents that Vercel cannot run it. The runner does actions/checkout + npm ci with NO .data hydration step; .data is gitignored/ephemeral ("job FS wiped at exit"). run-scheduled-scan.ts hydrates business-config from Supabase (lines 112-120) but NOT page-snapshots, so .data/tenants/{slug}/page-snapshots.json is absent every night. orchestrate-scan.ts:221 reads the baseline via getPageSnapshots() → snapshot-store.ts:7 → readDotDataJson('page-snapshots') →

## [HIGH] fail_loud_gap — `src/domains/changelog/actions.ts:88`
- **Issue:** All five reachable changelog mutators (createChangelogEntry L88-92, softDeleteChangelogEntry L172-176, restoreChangelogEntry L200-204, markDedupeReviewedBulk L242-246, updateChangelogHypothesis L277-281) wrap syncChangelogEntries in a try/catch that only console.error's the failure and then returns { success: true }. This directly defeats the deliberate Bug-1 fail-loud contract in src/lib/persistence/dual-write.ts:103-116, where dualWriteUpsert was made to ALWAYS THROW on persistent write failure precisely so 'false-completed' / silent-data-loss cannot happen. On Vercel prod (DATA_SOURCE=supabase, VERCEL=1) writeStore is a no-op to disk (json-store.ts:149-152), so syncChangelogEntries is the ONLY durable write — when it throws (RLS rejection, constraint violation, transient Supabase outage / 429 after retries), the throw is swallowed and the operator is told the action succeeded. The sibling creator finding-actions.ts:288 correctly does NOT swallow, so this is inconsistent within the codebase.
- **Customer impact:** Operator clicks 'Log a change' (or archives a dedupe pair, or marks a pair not-duplicate, or edits a hypothesis) and gets a success toast, but Supabase rejected/dropped the write. The change exists only in the warm lambda's in-process cache; /changes reads fresh from Supabase (force-dynamic) so the 'logged' change never appears, and it is permanently lost on the next cold start. For dedupe: softDelete/markDedupeReviewed silently no-op against the DB, so the duplicate banner the Phase B fix (dedupe/page.tsx:8-14) claims to have eliminated re-appears on every other warm lambda. No error, no retry, no operator awareness — the exact silent-loss class the dual-write Bug-1 fix was written to prevent.
- **Fix:** Stop swallowing the sync failure in all five mutators. Either let syncChangelogEntries throw out of the action (as finding-actions.ts:288 already does) and return { success: false, error } from the catch, or re-throw after logging. At minimum, propagate the failure to the action's return value so the UI surfaces it instead of showing a false success.
- **Verified (high):** Confirmed end-to-end. All 5 changelog mutators in src/domains/changelog/actions.ts (createChangelogEntry L88-92, softDeleteChangelogEntry L172-176, restoreChangelogEntry L200-204, markDedupeReviewedBulk L242-246, updateChangelogHypothesis L277-281) wrap `await syncChangelogEntries(...)` in try/catch that only console.error's and still returns {success:true}. syncChangelogEntries (dual-write.ts:355-362) calls dualWriteUpsert, which by the deliberate Bug-1 fix (dual-write.ts:103-116) ALWAYS throws on persistent write failure to prevent silent loss. DUAL_WRITE=true is real (committed in .env.loca

## [HIGH] data_corruption_or_loss — `src/lib/seed-data.server.ts:117 (with src/domains/brief-generation/actions.ts:108 and src/domains/briefs/actions.ts:86)`
- **Issue:** Accepted/created Brief objects are never persisted to any durable store and are reloaded as [] for every real customer. acceptProposedBrief() (brief-generation/actions.ts:108) and createBrief() (briefs/actions.ts:86) do `(await getBriefs()).push(brief)` — an in-memory mutation of the React.cache/_stateByTenant array. There is NO writeStore("briefs"), NO repository writer, and NO `briefs`/`brief_states` table in migrations (confirmed: repositories/types.ts has only getBriefStates, no brief read/write; grep of migrations finds no briefs table). Worse, in seed-data.server.ts loadFromRepoOrSeed, the real-customer branch (importRuns.length > 0, i.e. DATA_SOURCE=supabase with imported data) hard-codes `state.briefs = []` and never reads briefs back from the repo. So even within the file backend, a new lambda / cold start starts briefs at [] and the just-accepted plan is gone.
- **Customer impact:** An operator on a live tenant (importRuns>0, the prod condition) clicks Accept on a proposed brief or creates a brief. The UI shows 'Accepted' (brief-controls.tsx flips to the success label) and the action returns {success:true,briefId}. Within the same warm lambda the brief is visible at /briefs. On the next cold start / different lambda instance, /briefs renders the 'No execution briefs yet' empty state — the entire execution plan (checklist, expected outcomes, retrospective, links) is permanently lost. Meanwhile the proposed-brief still renders 'Accepted' and increments summary.accepted on /briefs/proposed because brief-states DID persist (file backend) — so the operator sees a permanent contradiction: brief marked accepted, but no brief exists to execute. Every accept/create on a real tenant is silent data loss.
- **Fix:** Give briefs a durable backing store mirroring the brief-states pattern: add a `briefs` store to store-classification (TENANT_SCOPED), have the repository getBriefs()/saveBriefs() read/write it, and load `state.briefs = await repo.getBriefs()` in the importRuns>0 branch instead of `= []`. Then call a persistBriefs()/writeStore after the `.push(brief)` in acceptProposedBrief and createBrief (and after the in-place mutations in updateBriefStatus/toggleChecklistItem/addChecklistItem/judgeOutcome). Until briefs are durable, the accept flow should not present itself as a saved execution plan.
- **Verified (high):** Confirmed real and genuinely broken. No durable briefs store exists: repositories/types.ts exposes only getBriefStates (no getBriefs/saveBriefs); grep finds zero writeStore("briefs"…) anywhere; "briefs" is not in any Set in store-classification.ts (would resolve to "unknown" and throw if attempted); and no briefs/brief_states table exists in migrations (baseline_schema.sql has none). getBriefs (seed-data.server.ts:183) is purely an in-memory React.cache getter over _stateByTenant. acceptProposedBrief (brief-generation/actions.ts:108) and createBrief (briefs/actions.ts:86) only do (await getBri

## [HIGH] other — `src/domains/push/push-service.ts:454-466`
- **Issue:** The wix_cms field-EDIT route locates the target item by pulling a SINGLE page of the collection (wixQueryDataItems with limit:1000, which the client caps at 1000 with no cursor/offset loop — client.ts:157) and then doing items.value.find(i => i.id === mapEntry.dataItemId). For a collection with more than 1000 items, any mapped item that sorts beyond the first 1000 returned is not found, so the push is REFUSED with the misleading message 'mapped Wix item no longer exists — re-run the url-map sync' and a push_failed ledger row is written — even though the item exists and the actual writer (wixUpdateDataItem) would fetch it directly by id via wixGetDataItem (client.ts:176-194).
- **Customer impact:** For a large CMS-backed tenant (e.g. Iranopedia, an encyclopedia where a single collection can exceed 1000 items), approved title/heading/meta/content edits on every page whose item falls past the first 1000 rows can never publish. The operator sees a false 'item no longer exists, re-run the sync' error; re-running the url-map sync (which also only reads the first 1000 items, url-map.ts:191) does not fix it, so the operator is stuck with no correct diagnosis.
- **Fix:** Replace the paged query+find used only to read the previous value with a direct by-id read: const cur = await wixGetDataItem({ dataCollectionId: mapEntry.dataCollectionId, dataItemId: mapEntry.dataItemId }, { ...deps.wix, tenantId }); treat !cur.ok as the read-fail branch and cur.value.data[field] as the snapshot's previous_text. This matches how wixUpdateDataItem already resolves the item and removes the 1000-item ceiling on edits.
- **Verified (medium):** CONFIRMED at code level and reachable for a real tenant. wixQueryDataItems caps at 1000 with no cursor/offset loop (client.ts:157: paging:{limit:Math.min(args.limit??200,1000)}). The wix_cms EDIT route reads one page then items.value.find(i=>i.id===mapEntry.dataItemId), and on miss refuses with "mapped Wix item no longer exists — re-run the url-map sync" plus a push_failed ledger row (push-service.ts:454-466). The real writer wixUpdateDataItem already fetches the item directly by id via wixGetDataItem (client.ts:233-237), so the find-on-a-1000-row-page is an unnecessary ceiling on the snapshot

## [HIGH] pivot_inconsistency — `/Users/armeen/beacon/.claude/worktrees/objective-davinci-c81e70/src/domains/delivery/morning-digest.ts:170-195`
- **Issue:** selectPushRegressionAlarms compares an always-full 7-day PRE window against a POST window that is only as long as the time actually elapsed since the push, but it accepts ANY push within the last 7 days with no minimum-age requirement (line 175 only checks now-t <= weekMs). For a page pushed N days ago, `pre` accumulates citations over the entire prior 7 days while `post` can only accumulate over N days. So a page that was healthy before the push (pre >= 3) and simply hasn't been re-cited yet because only ~1-2 days have elapsed (post near 0) trips the `post <= pre/2` test and fires a false alarm. Reproduced: pushedAt = NOW-1day, 5 own-citations the prior week, 0 since -> alarm fires. The existing test suite only exercises pushes 3+ days old and never catches the too-young case.
- **Customer impact:** Data condition: an operator approves+pushes a page that was already being cited by AI (>=3 own-domain citations the prior week), and the morning digest runs 1-6 days later before a full post-push week has elapsed. The digest renders a loud red line: '<path> — N citations the week before the push, 0 since. Consider the Revert button on the Wix console.' This tells the operator a GOOD, recently-shipped change hurt their AI visibility and nudges them to revert it — based purely on insufficient elapsed time, not a real regression. It also contradicts the GSC-led / AEO-secondary posture: a secondary (AEO citation) signal is escalated to a revert-now alarm against a freshly approved change.
- **Fix:** Require the post-window to have fully elapsed before alarming: in the recentPushes filter, only keep pushes whose age is BETWEEN one full post-window and the lookback cap (e.g. now - t >= weekMs as a minimum, paired with a wider lookback so 7-14-day-old pushes are eligible), OR compare equal-length elapsed windows by computing post over the actual elapsed days and scaling pre to the same span. Minimal change: add `&& now.getTime() - t >= weekMs` to the line-175 filter (only alarm once a full week of post-push observations exists) and widen the upper bound (e.g. <= 14*weekMs/... or a 14-day cap) so recently-matured pushes are still covered.
- **Verified (high):** Confirmed real and reachable. The line-175 filter (`now - t <= weekMs && >= 0`) accepts pushes of ANY age with no minimum, while `pre` always spans a full 7 days before the push and `post` can only span the days actually elapsed since it. For a push 1-6 days old, post is structurally truncated, so a page cited >=3 times the prior week with 0 since (because the post window is too short and AI citations are only sampled by the daily scan, not continuous) trips `post <= pre/2` and fires. Reproduced exactly: pushedAt=NOW-1day, 5 prior-week citations, 0 since -> alarm "/famous-iranians — 5 citation

## [HIGH] tenant_isolation — `src/app/(shell)/competitors/page.tsx:95`
- **Issue:** The /competitors ("Market") page resolves the owned domain via the process-level, module-cached, NON-tenant-aware getSiteConfig().siteDomain (line 95) and feeds it as ownedDomain into competitor-intel computations: computeCoMentionMatrix(siteDomain,…) (99), computeSourceTrustIndex(siteDomain,…) (105), discoverCompetitorUniverse({ownedDomain: siteDomain}) (162), computeCitationDecay(siteDomain) (167), and buildCompetitorRank(citIndex, siteDomain,…) (209). getSiteConfig() reads a single deployment-wide env var (BEACON_SITE_DOMAIN) or the flat .data/business-config.json and caches it for the whole process; prod is a SINGLE deployment with header-based multi-tenancy (middleware injects x-beacon-tenant per request — see src/lib/auth/supabase-middleware.ts). So siteDomain is fixed to ONE tenant's domain for ALL tenants. The same page already resolves the tenant-correct config via getBusinessConfigForCurrentTenant() (line 157) and uses it for directoryDomains — the MT-3B migration fixed directoryDomains but left the far more important owned-domain on the stale helper. (Note: computeMarketBenchmark and computeBattlecards do not actually read ownedDomain — they use the index's is_owned flag — so the KPI strip/rankings/battlecards stay correct; the wrong-domain blast radius is the discovery + co-mention + decay + competitor-rank paths.)
- **Customer impact:** For tenant-iranopedia and tenant-finglish (whose domain ≠ the deployment's BEACON_SITE_DOMAIN), the "Discovered competitors" section computes citationDelta = competitor.citations − ownedTotalCits where ownedTotalCits resolves to 0 (the wrong owned domain isn't in this tenant's tenant-scoped citation index). Result: every discovered domain shows a misleading "+N more than you" red badge and every suggestedMove/topicThreat says "you have no citations there" / "theyLead" even when the tenant actually leads — the page tells a non-Ritz customer they are losing on topics they own. computeCitationDecay/buildCompetitorRank similarly key off the wrong owned domain. computeCoMentionMatrix, when it has data, also persists a matrix computed against the wrong ownedNorm into THIS tenant's per-tenant co-mention store (cross-tenant adjacency leak).
- **Fix:** Resolve businessConfig = await getBusinessConfigForCurrentTenant() at the top of CompetitorsPage (before line 99) and derive const ownedDomain = businessConfig.domain?.trim() ? businessConfig.domain : getSiteConfig().siteDomain;. Pass ownedDomain (not siteDomain) into computeCoMentionMatrix, computeSourceTrustIndex, discoverCompetitorUniverse.ownedDomain, computeCitationDecay, buildCompetitorRank, and syncMilestonesFromWorkspace.siteDomain. Keep the getSiteConfig fallback only for the empty-placeholder/demo case.
- **Verified (medium):** CONFIRMED real tenant-isolation defect. src/app/(shell)/competitors/page.tsx:95 reads `getSiteConfig().siteDomain`, which src/lib/site-config.ts resolves from the process-wide env `BEACON_SITE_DOMAIN` (or flat `.data/business-config.json`) and caches for the whole process (readConfig/getSiteConfig, lines 48-71) — it is entirely tenant-blind. Production is a single deployment with header-based multi-tenancy: src/lib/auth/supabase-middleware.ts injects `x-beacon-tenant` per request (lines 183-189) and src/lib/tenant-context.ts:currentTenantId reads that header. The tenant registry (.data/global/

## [HIGH] data_corruption_or_loss — `src/domains/opportunity-candidates/actions.ts:111`
- **Issue:** promoteToOpportunity() persists the newly-promoted opportunity ONLY via writeStore("imported-opportunities", ...) and never calls the Supabase dual-write (syncOpportunities / dualWriteUpsert("opportunities")). On prod (DATA_SOURCE=supabase) getOpportunities reads from the Supabase `opportunities` table via the repo (supabase-backend.ts:475 / loadFromRepoOrSeed in seed-data.server.ts), NOT from the imported-opportunities JSON store. On Vercel, atomicWrite skips disk and only updates the json-store in-process cache (json-store.ts:149), which is not on the read path. The in-memory `opportunities.push(opp)` mutation makes the row visible only within the same warm lambda; it is never written to Supabase. The canonical persistence path used by the import flow is writeStore + syncOpportunities (lib/import/actions.ts:708) — this action omits the dual-write.
- **Customer impact:** Operator on /expansion clicks "Stage draft in Opportunities"; the button shows the success state "Saved as draft" / "Staged — validate before use" (promote-candidate.tsx) implying durable save. The promoted opportunity appears in Opportunities while the lambda stays warm, then silently vanishes on the next cold start or any request served by a different lambda, because Supabase was never updated. Confirmed-looking work is lost with a misleading success confirmation.
- **Fix:** After pushing the new opp and calling writeStore, also dual-write to Supabase: import { syncOpportunities } from "@/lib/persistence/dual-write" and call `await syncOpportunities(opportunities.filter(isImported), tenantId)` (mirroring lib/import/actions.ts:708). The tenantId is already resolved at the top of the action.
- **Verified (high):** CONFIRMED — both reachability and wrong-behavior hold on prod (DATA_SOURCE=supabase). REACHABILITY: src/app/(shell)/expansion/page.tsx renders <PromoteCandidateButton> (line ~437) for every fresh candidate. The only gate is `hasActiveExperiment()` (importRuns.length>0) — an operator-reachable data state, NOT an off-by-default code flag. promote-candidate.tsx calls promoteToOpportunity() unconditionally on click and shows the success state "Saved as draft" whenever result.success===true, which the action returns unconditionally at actions.ts:118. WRONG BEHAVIOR: promoteToOpportunity (actions.ts

## [HIGH] data_corruption_or_loss — `src/domains/opportunities/actions.ts:29`
- **Issue:** updateOpportunityStatus, captureOpportunity, deferOpportunity, and closeOpportunity mutate the in-memory Opportunity object (e.g. opp.current_status = newStatus; opp.captured_at = ...; opp.notes += ...) and then only revalidatePath. They never call writeStore AND never call the Supabase dual-write (syncOpportunities). The mutation lands only on the cached state.opportunities array in _stateByTenant (seed-data.server.ts), which is process-local. On prod getOpportunities re-hydrates from the Supabase `opportunities` table on cold start / on any other lambda, so none of these status transitions persist. (Even under DATA_SOURCE=file they are lost on process restart, since no writeStore is ever called.)
- **Customer impact:** On the reachable /topics/opportunity/[id] surface (linked from /competitors/[id], /settings/history/[id], /briefs/[id]) the operator marks an opportunity captured/deferred/closed or changes its status and adds notes. The change sticks for the current session, then reverts to its prior status on the next cold start or a request hitting a different lambda — operator-recorded lifecycle decisions and capture/defer/close notes are silently lost, and the same opportunity keeps re-surfacing as if no action was taken.
- **Fix:** In each of the four mutators, after applying the in-memory mutation persist durably: re-read the full opportunity list, then `await writeStore("imported-opportunities", opportunities.filter(isImported))` AND `await syncOpportunities(opportunities.filter(isImported), await currentTenantId())` (same pattern as the import flow at lib/import/actions.ts:708). Resolve tenantId via currentTenantId() as promoteToOpportunity does.
- **Verified (high):** CONFIRMED real and reachable. All four mutators in src/domains/opportunities/actions.ts (updateOpportunityStatus L29-49, captureOpportunity L73-79, deferOpportunity L104-111, closeOpportunity L136-143) mutate the in-memory `opp` from getOpportunities() and then call ONLY revalidatePath("/","layout"). The file imports only revalidatePath, log, getOpportunities, now — no writeStore, no syncOpportunities; now() (src/lib/actions.ts:5) is a pure ISO timestamp with no side effects, so no persistence happens anywhere. REACHABILITY: src/app/(shell)/topics/opportunity/[id]/page.tsx renders OpportunityS

## [HIGH] tenant_isolation — `src/domains/learning/change-patterns.ts:386-393`
- **Issue:** materializeUrlChangePatterns computes UrlChangePatterns from ONLY the current tenant's outcomes (getUrlChangeOutcomes() is tenant-scoped) then does writeStore("url-change-patterns", patterns), which FULLY OVERWRITES the GLOBAL store (classified global in store-classification.ts:207). The url-watcher throttle state was made per-tenant in audit #19 (url-watcher.ts:75-83) but the OUTPUT store stayed global, so the trigger no longer keeps the global store consistent with the reading tenant. On /changes (page.tsx:307) the store is read back and fed to computeReadyOn -> findBestUrlPattern to produce per-row 'Ready on [date]' predictions whose narrative literally says '...landed by day N' / 'in your workspace' citing pattern.sample_count and helping_count (changes/page.tsx:660,674).
- **Customer impact:** Production has 3 tenants (Ritz/Iranopedia/Finglish). When tenant B opens /changes while B's per-tenant url-watcher throttle is active (the common steady state — last run < refresh interval), maybeRefreshUrlWatcher returns {ran:false, reason:'throttled'} and does NOT rewrite the store, so line 307 reads whatever tenant most recently ran the watcher across ALL tenants. Tenant B then sees 'Ready on' landing-day predictions, sample counts, and confidence tiers computed from ANOTHER tenant's URL outcome history, narrated as 'in your workspace'. A content-site tenant gets a builder tenant's landing speeds presented as its own — a wrong, cross-tenant-derived customer prediction.
- **Fix:** Make url-change-patterns per-tenant: move "url-change-patterns" from GLOBAL_STORES to TENANT_SCOPED_STORES (or SINGLETON_STORES) in store-classification.ts, matching the now per-tenant throttle state. The store name + writeStore/readStore calls stay the same; the resolver will route to .data/tenants/{slug}/url-change-patterns.json so each tenant only ever reads its own landing-history brain. (If a true cross-tenant aggregate is later wanted, it must MERGE existing rows rather than overwrite.)
- **Verified (high):** Confirmed cross-tenant bleed. materializeUrlChangePatterns (src/domains/learning/change-patterns.ts:386-393) computes patterns from getUrlChangeOutcomes() — which IS tenant-scoped (src/domains/attribution/url-change-outcome.ts:253 resolves currentTenantId() and loads per-tenant) — then writeStore("url-change-patterns", patterns) FULLY OVERWRITES (atomicWrite of the whole array, no merge). url-change-patterns is in GLOBAL_STORES (src/lib/persistence/store-classification.ts:207), and the runtime resolver honors classifyStore: readStore/writeStore route global stores to a single cache key `url-ch

## [HIGH] pivot_inconsistency — `src/domains/lifecycle-eligibility/derive-reason.ts:412-424`
- **Issue:** deriveLifecycleReason has no branch for the `pushed` status. A `pushed` row is a SUCCESS: the operator clicked Approve & Push, the CMS write returned 200, and markRecommendedEditPushResult (recommended-edits-persistence.ts:434) stamps `live_at` while setting implementation_status='pushed'. On the diagnostic this row has live_at set (enters branch 9 at line 355) but stage===null, because `pushed` is NOT in the time-to-citation ELIGIBLE_STATUSES (citation-lifecycle/eligibility.ts:87-91), so loadLifecycleForEdit returns available:false/stage:null. With live_at set, threshold_eligible false, stage not 'stuck' and not 'live_not_yet_cited', execution reaches the final fall-through at line 415-424 and emits reason='unknown', blocked_by='system', detail='shipped but lifecycle_stage=null; compute output incomplete'.
- **Customer impact:** Data condition: any edit Beacon pushed via the live 'Approve & Push' action that the next scan hasn't yet confirmed (the normal, healthy window between publish-200 and verified_live). The operator-only diagnostic — whose entire stated purpose is to make the operator-blocked vs system-blocked funnel trustworthy — labels a healthy, live, Beacon-published edit as reason='unknown' with detail 'compute output incomplete', and counts it under 'Blocked by scan/match/data' (edits_blocked_by_system). The operator is told to investigate a phantom match-runner/data bug on a row that is actually fine, and the system-blocked counter is inflated. The taxonomy comment itself says 'unknown' should be empty in production.
- **Fix:** In branch 9 (live_at set), before the final unknown fall-through, add an explicit branch for status==='pushed' (and 'push_failed' if live_at happens to be set): return reason='accepted_not_live' (or a new 'pushed_awaiting_verify' reason) with blocked_by:'system' and detail 'pushed to CMS; awaiting scan verified_live confirmation' — i.e. classify it as a normal in-flight/awaiting-scan row, not 'unknown / compute output incomplete'. At minimum, distinguish 'shipped via push, not yet TTC-eligible' from genuine compute anomalies.
- **Verified (high):** Verified end-to-end. A `pushed` row is a real, common state: markRecommendedEditPushResult (recommended-edits-persistence.ts:434) stamps live_at and sets implementation_status='pushed' whenever approveAndPushRecommendedEdit (actions.ts:864-869) runs with executePush returning kind:'pushed' (a real Wix CMS write for a non-Ritz tenant with publish_target='wix_cms', e.g. Finglish/Iranopedia) AND the immediate probeLiveText returns found:false — which is the NORMAL outcome (Wix edge-cache lag, 15s probe timeout/throw returning found:false, or client-rendered text not in raw HTML). The push path is

## [HIGH] tenant_isolation — `src/lib/auth/supabase-middleware.ts:161-165 (paired with src/lib/tenant-context.ts:42-43)`
- **Issue:** When the per-request `tenant_members` lookup returns an error (line 161) or throws (line 200), the middleware logs and FALLS THROUGH without setting the `x-beacon-tenant` header. `currentTenantId()` then drops to the env fallback `process.env.BEACON_TENANT_ID`, which is `tenant-ritz-founder` in prod/.env (.env.local:6, .env.local.example:9). So an authenticated request whose membership query transiently fails is resolved to the operator's founder tenant — NOT the logged-in user's tenant. The membership lookup is the only thing distinguishing tenants for authenticated requests; on its failure the request is silently bound to Ritz instead of failing closed. The comment frames this as 'so a Supabase outage doesn't 500 every request,' but the chosen fallback is a specific *other* tenant's identity, not the requester's.
- **Customer impact:** Trigger: a logged-in non-operator customer (e.g. the Iranopedia or Finglish operator, or any self-serve signup) makes a request during a Supabase blip / `tenant_members` read error. Result: their entire session renders tenant-ritz-founder's confidential data — Today dashboard KPIs, GSC demand signals, recommendation queue, competitors, push targets — and any tenant-scoped write/approve action they take (e.g. Approve & Push) executes against Ritz's tenant context. Cross-tenant data exposure plus potential cross-tenant writes, all without any error surfaced to the user.
- **Fix:** Fail closed on the membership-error/throw branches instead of falling through to the env tenant. On line 161 (error) and line 200 (throw), redirect authenticated users to `/login?error=tenant_lookup_failed` (mirror the existing `no_tenant` redirect at 166-173) rather than leaving the header unset. Additionally, in tenant-context.ts the `BEACON_TENANT_ID` env fallback should only be honored outside a request context (CLI/cron): when `headers()` succeeds (i.e. we ARE in a request) but no `x-beacon-tenant` was injected, throw rather than silently using the operator env id, so an authenticated request can never resolve to a tenant the requester wasn't proven to own.
- **Verified (medium):** CONFIRMED REAL (medium confidence on the one env assumption noted below). Mechanics verified against the actual code: 1) supabase-middleware.ts:161-165 — on a tenant_members query `error`, the middleware logs ("[mw-tenant] tenant_members query failed") and FALLS THROUGH without setting x-beacon-tenant (trace tag "error_fallthrough"). Same at :200-204 on a thrown exception ("throw_fallthrough"). Neither redirects; the 0-tenant branch (:166-173) DOES fail closed via /login?error=no_tenant, so the error/throw branches are inconsistent with the file's own fail-closed precedent. 2) tenant-context.t

## [MEDIUM] pivot_inconsistency — `src/domains/attribution/scorecard.ts:269`
- **Issue:** In computeScorecard, the negative verdict branch only fires when `positiveEvents.length === 0 && negativeEvents.length > 0`. When a single change is linked to BOTH a positive-primary outcome event and a negative-primary outcome event, the negative branch is skipped (positiveEvents.length > 0) and the verdict falls through to the positive primaries logic (lines 289-299), producing `verdict = "validated"` or `"partial"`. The negative events are computed (negativeEvents at line 236) but never influence the headline verdict/verdictSummary in the mixed case.
- **Customer impact:** A change that measurably HURT visibility for one topic while helping another is surfaced with a positive headline verdict ('Closest match...'/'validated'/'partial') and a verdictSummary that mentions only the positive topics, hiding the decline. The operator sees a green/positive verdict on a change that actually regressed a topic, and may replicate a harmful pattern. The decline only appears in the secondary `impact.direction = mixed` field (change-impact.ts:81), not the prominent verdict label.
- **Fix:** Add a mixed-outcome branch before the positive-primaries branches: when both positiveEvents.length > 0 and negativeEvents.some(a => a.role === 'primary'), set verdict to an honest mixed/inconclusive value and have verdictSummary name both the lifted and the declined topics, rather than letting positive primaries silently win.
- **Verified (high):** Confirmed real and reachable in the core scorecard path (no flag/LLM-flip/connect-source/billing gate). In computeScorecard (src/domains/attribution/scorecard.ts), the per-change loop (line 111) evaluates EVERY event in eventCandidateMap; discoverCandidates runs per-event with no constraint tying a change to a single topic×platform series. So one change can be primary/contributing for a positive event (first_appearance/surge/regained) on topic A AND for a negative event (visibility_lost/mention_decline on topic B, detected in events.ts:164-213). The negative verdict branch at line 269 is gated

## [MEDIUM] pivot_inconsistency — `src/domains/outcome-attribution/load-mode-a-for-changes-detail.ts:127`
- **Issue:** loadModeAForChangesDetail computes Mode A outcome attribution for the linked recommended_edit using ONLY target_url + live_at (compute requires no_live_at/no_target_url to be absent). It never checks implementation_status. The two OTHER Mode A surfaces both gate on isVerifiedLive: the Today tile (load-outcomes-summary-for-tenant.ts:159, isVerifiedLive = verified_live | verified_live_modified | partially_implemented) and the operator diagnostic (src/app/(shell)/diagnostics/outcome-attribution/page.tsx:220 edits.filter(isVerifiedLive)). The changes detail page (src/app/(shell)/changes/[id]/page.tsx:399-404) calls this loader for any join-key-linked edit regardless of status, and change-detail-v2-client.tsx:382 renders <OutcomeAttributionAct3 result={modeAResult}/> with only null/ineligible suppression. markRecommendedEditPushResult (recommended-edits-persistence.ts:434) stamps live_at when result==='pushed' even WITHOUT probe verification (status becomes 'pushed', not 'verified_live'), and the push_failed branch (line 431-437) spreads the row so a previously-set live_at is preserved. So 'pushed' and 'push_failed' edits carry live_at while being excluded from the verified-live set.
- **Customer impact:** Operator clicks Approve & Push; the CMS write returns 200 but the on-page probe does not confirm (verifiedByProbe falsy), so the edit sits in status 'pushed' (or later 'push_failed' on a re-push) with live_at set until the next 07:00 UTC scan flips it — a window of hours to ~1 day, or indefinitely if the scan never confirms and it ages to not_found_after_7d. During that window the customer's Changes detail brief shows 'This page received N sessions in the X days since going live' (or the still-learning copy) for a change the rest of the product (Today tile, operator diagnostic) does NOT count as verified-live. The customer sees outcome attribution for a change that was never confirmed live on-page, contradicting the review-gated/verified-live discipline the other surfaces enforce.
- **Fix:** Gate the changes-detail loader on verified-live status, matching the other two surfaces. In loadModeAForChangesDetail, after destructuring recommendedEdit, return null early if the edit is not verified-live, e.g. import the shared isVerifiedLive predicate (or inline the same check: implementation_status one of verified_live | verified_live_modified | partially_implemented) and `if (!isVerifiedLive(recommendedEdit)) return null;` before the canonicalize/cache work. This makes Act 3 suppress outcome copy for pushed/push_failed/non-verified edits, identical to the Today tile and operator diagnostic.
- **Verified (high):** Confirmed in code. loadModeAForChangesDetail (load-mode-a-for-changes-detail.ts:127-147) gates ONLY on canonicalizable target_url; computeModeATrafficAttribution (mode-a-cited-here-traffic-here.ts:198-225) gates ONLY on live_at + target_url. Neither checks implementation_status. The changes detail page resolves linkedEdit by join key (page.tsx:305 — rec_id__action_type__target_element_key, status-agnostic per lifecycle-classification.ts:73-78) and calls the loader for ANY linked edit (page.tsx:399-404), then OutcomeAttributionAct3 (outcome-attribution-act3.tsx:111-112) suppresses only null/ine

## [MEDIUM] fail_loud_gap — `src/domains/scanning/orchestrate-scan.ts:344`
- **Issue:** The `scan_findings` dual-write is unguarded and can throw out of `runWebsiteScan`, defeating the wave-2 heartbeat-protection fix for this one write. `regenerateScanFindings` (line 344) calls `addFindings(newFindings)` (findings-store.ts:133), which calls `syncScanFindings` → `dualWriteUpsert`. Per the Bug-1 fix, `dualWriteUpsert` DELIBERATELY re-throws on persistent/transient-exhausted PostgREST or network failure (dual-write.ts:103-116). Neither `addFindings`, nor `regenerateScanFindings`, nor the call site at line 344 wraps this in try/catch — unlike the snapshot/guardrail/observation dual-writes just below (lines 400-452), which the wave-2 #2 comment (lines 382-395) explicitly hardened to fail soft 'so one connector blip degrades only that write' and so the website_crawl heartbeat still lands. The findings write happens BEFORE `writeLastScanResultFile(merged)` (line 535) and `writeIdleScanStateFromLastResult` (line 536), so a throw here skips the durable last-scan-result, leaves scan-state stuck in 'running', and skips the observation_run heartbeat the poll-watchdog keys recovery on.
- **Customer impact:** Data condition: a single transient/persistent Supabase blip during the scan_findings upsert on a nightly scan. What goes wrong: the throw propagates to run-scheduled-scan.ts which does process.exit(2) → the whole scan is marked FAILED and fires the GitHub alert issue even though the crawl + snapshot/guardrail dual-writes succeeded; the website_crawl observation_run heartbeat is skipped, so the poll-watchdog sees a missing heartbeat and forces a wasteful full re-dispatch of daily-scan.yml; and scan-state is left 'running' until the 5-minute stale-recovery on the next run. This is the exact false-failure / skipped-heartbeat signature the wave-2 fix was meant to eliminate, still reachable through the findings path.
- **Fix:** Wrap the findings persistence so a connector blip fails soft like the sibling dual-writes. Either wrap the `regenerateScanFindings` call at line 344 in try/catch (logging a warn and setting findingsAdded=0 on failure), or split addFindings so the local `writeStore` always lands and the `syncScanFindings` call is individually try/caught and logged — matching the pattern at orchestrate-scan.ts:400-452 so the last-scan-result write, scan-state reset, and website_crawl heartbeat below always execute.
- **Verified (high):** VERIFIED REAL. The scan_findings dual-write is genuinely unguarded and can hard-fail the nightly scan, defeating the wave-2 fail-soft pattern for this one write. Reachable production path (DUAL_WRITE=true is the operating mode): .github/workflows/daily-scan.yml:218 sets DUAL_WRITE:"true" and line 201 runs scripts/run-scheduled-scan.ts → :130 runWebsiteScan({trigger:"cron"}) → orchestrate-scan.ts:344 regenerateScanFindings(...) → :133 addFindings(newFindings) → findings-store.ts:142 syncScanFindings(existing, tenantId) → dual-write.ts:1065 dualWriteUpsert("scan_findings", rows, "id"). The throw

## [MEDIUM] wrong_customer_output — `src/domains/indexability/compute-indexability.ts:120-137` ✅ FIXED
- **Issue:** detectNoindex() does not recognize the `none` meta-robots directive. The tokenizer only matches tokens that equal `noindex` or start with `noindex` (line 131 + line 135). It never matches `none`. Google explicitly documents `<meta name="robots" content="none">` as equivalent to `noindex, nofollow` (https://developers.google.com/search/docs/crawling-indexing/robots-meta-tag). The page extractor (src/domains/pages/extractor.ts:34) captures the meta-robots `content` value verbatim into `robots_meta`, so the literal string `none` reaches detectNoindex unmodified, and the existing test suite (tests/domains/indexability/compute-indexability.test.ts:554-561) never exercises `none`, so this is an unhandled gap, not a pinned decision.
- **Customer impact:** A real customer page that ships `<meta name="robots" content="none">` (a common WordPress/Wix output that de-indexes the page) is treated by computeIndexability as NOT noindex. The composite_verdict resolves to ok/unknown/not_in_sitemap instead of noindex_meta. Consequence: (1) the noindex-on-indexable-page recommendation (src/domains/recommendation-intelligence/triggers/noindex-on-indexable-page.ts:53) never fires, so the customer is never told their page is de-indexed; (2) the page can be reported as healthy (`ok`/'all signals positive'); (3) the operator diagnostics row shows no `[noindex]` badge (src/app/(shell)/diagnostics/indexability/page.tsx:814). Beacon silently calls a de-indexed page indexable — a wrong, customer-visible verdict that hides a high-severity SEO problem.
- **Fix:** In detectNoindex (compute-indexability.ts), treat the `none` token as noindex. Minimal change: inside the `tokens.some(...)` callback add `if (t === "none") return true;` alongside the existing `t === "noindex"` check. Add a test case `["none", "noindex_meta"]` (and e.g. `"NONE"`, `"none, follow"`) to the noindex-parsing table in tests/domains/indexability/compute-indexability.test.ts. Note `none` collapses both noindex+nofollow; for this verdict only the noindex half matters, so the single-token match is sufficient.
- **Verified (high):** Confirmed against the code. detectNoindex (src/domains/indexability/compute-indexability.ts:120-137) only returns true for tokens equal to "noindex" or starting with "noindex" (lines 131, 135); it never matches "none". Verified empirically: detectNoindex("none") and detectNoindex("NONE") both return false. The extractor (src/domains/pages/extractor.ts:33-34, stored at :345) captures meta[name=robots] content verbatim into robots_meta with no none→noindex normalization, so the literal "none" reaches detectNoindex unchanged. This runs on the standard customer render path (computeIndexability:269

## [MEDIUM] wrong_customer_output — `src/domains/prompt-answer-observations/extraction.ts:489-493`
- **Issue:** extractAnswerStructure's comparison detector matches the bare high-frequency English words `than`, `over`, `while`, and `whereas` (regex `/\b(?:vs\.?|versus|compared to|whereas|while|than|over|better than|worse than)\b/gi`) anywhere in the answer text, with a threshold of only >=3 total matches. These words are common in ordinary narrative prose ("with over 20 years…", "while many builders focus on volume…", "more than just homes…"), so a plain narrative AI answer routinely trips 3+ matches and gets mis-classified as `comparison` (or pushed to `mixed` when a list also appears). Unlike the bullet/ranked detectors, the comparison detector is NOT line-anchored and has no list-structure corroboration requirement.
- **Customer impact:** Data condition: any multi-sentence ChatGPT/Perplexity answer containing 3+ of {than, over, while, whereas} in prose — extremely common for a builder/services bio answer. Result: (1) /today's "What format wins" section renders the misleading sentence e.g. "ChatGPT prefers Side-by-side comparison (60%)" when AI is actually writing narrative, steering the operator to restructure content as comparisons that AI never used; (2) the same skewed answer_structure distribution feeds recommendations/evidence-packet.ts (`dominantAnswerStructure`) and prompts/opportunity-classify.ts (structureDist), so recommendation evidence can claim a comparison-format preference that doesn't exist. This systematically biases the AEO format read toward `comparison`/`mixed`.
- **Fix:** Tighten the comparison heuristic so it requires genuine comparison structure, not stray narrative words. Minimal: drop the bare ambiguous tokens (`while`, `over`, `than`, `whereas`) from the marker regex, keeping only unambiguous comparison phrases (`vs`, `versus`, `compared to`, `better than`, `worse than`), and/or raise `hasComparison` to require comparison markers AND list structure (rankedMarkers>=3 or bulletMarkers>=2) before returning `comparison`. This preserves true comparison/mixed detection while stopping narrative answers from being mislabeled.
- **Verified (high):** CONFIRMED real. The comparison regex at src/domains/prompt-answer-observations/extraction.ts:489-493 matches bare high-frequency English words (than/over/while/whereas) anywhere in the text with no line-anchoring and no list corroboration, threshold only >=3 (line 497). Unlike rankedMarkers/bulletMarkers (lines 481-487) which ARE line-anchored, the comparison detector is unanchored. I ran the actual regex against realistic builder-bio narrative answers: "...with over 20 years... While many builders... more than just homes... over the years" → 4 matches → classified "comparison" (not narrative)

## [MEDIUM] pivot_inconsistency — `src/domains/brief-generation/actions.ts:76-90`
- **Issue:** When a proposed brief is accepted, every expected_outcome is hard-coded to `metric_type: "mention_count"` and `platform: "chatgpt"` (with a dishonest `as "chatgpt"` cast that also launders cluster.platformKey values like "mixed" into an invalid Platform). The outcome descriptions are copied verbatim from the GSC/SEO-flavored template successCriteria (e.g. "Page indexed within 7 days", "First impressions/citations within 14 days", "Zero structured data errors in Search Console").
- **Customer impact:** On the accepted-brief detail page (/briefs/[id] → InteractiveOutcomes), each outcome renders its GSC/SEO description next to the badges 'ChatGPT' and 'Mentions' (PLATFORM_LABELS/METRIC_TYPE_LABELS). A GSC-led brief about page indexing or Search-Console structured-data is mislabeled as an AI-answer 'Mentions' prediction on ChatGPT — directly contradicting the GSC-led / AEO-secondary pivot and confusing the operator about what is actually being measured. (Verdict math in computeBriefVerdict is verdict-driven, so the wrong metric_type does not corrupt the validated/missed result — impact is mislabeling, not bad verdicts.)
- **Fix:** Derive metric_type from the proposed brief type (e.g. measurement_fix/crawlability/internal_linking → average_position or organic_clicks; only AEO-specific briefs → mention_count) and set platform from proposed.targetPlatform mapped through a real Platform guard (default "all", not "chatgpt"). Remove the `as "chatgpt"` cast so a non-Platform platformKey ("mixed") can't be stored as a bogus Platform value.
- **Verified (high):** CONFIRMED on a real, ungated operator path. /briefs/proposed (src/app/(shell)/briefs/proposed/page.tsx) is gated only by hasActiveExperiment() — no LLM flip, connect-source, or off-by-default flag. BriefAcceptReject (src/components/data/brief-controls.tsx:42) calls acceptProposedBrief on operator click. In src/domains/brief-generation/actions.ts:76-90, every successCriteria is mapped to an expected_outcome with hard-coded metric_type:"mention_count" and platform:(targetPlatform==="all"?"chatgpt":targetPlatform) as "chatgpt". The successCriteria strings are demonstrably SEO/GSC-flavored (src/do

## [MEDIUM] other — `src/domains/push/push-service.ts:197-201`
- **Issue:** The non-destructive shrink guard (assertNonDestructivePatch, caps.ts:95-101) refuses any patch where current_text > 80 chars and proposed_text < 20% of current length. The revert flow (buildRevertEdit in push-snapshots.ts → revertPushFromForm in diagnostics/wix/actions.ts:195) ships the undo through the SAME executePush, with current_text = the pushed (new) value and proposed_text = the captured previous value. When the original push GREW a field to more than 5x its prior size (e.g. expanded a 30-char description to 200+ chars), the revert's previous value is <20% of the now-current value and >80 chars, so the guard refuses the restore as 'treat as deletion; needs a separate explicit approval'.
- **Customer impact:** After an operator pushes a content-expansion edit live (a common, encouraged action — thin content grown into a full section), the Revert button can never undo it: revertPushFromForm logs '[wix-revert] revert push REFUSED ... proposed text would shrink existing content by >80%' and returns void, so the operator clicks Revert, the page does not change, and they get no actionable explanation in the UI. A legitimate live change is effectively un-undoable through the product.
- **Fix:** Skip (or invert) the >80%-shrink branch of assertNonDestructivePatch for synthetic revert edits — e.g. tag the revert edit (id ends with '__revert' / a dedicated flag) and let executePush bypass the shrink-as-deletion check for it, since the snapshot's previous value is by definition real prior content, not a deletion. The empty-previous-value refusal in buildRevertEdit (push-snapshots.ts:76) already covers the genuine deletion case.
- **Verified (medium):** CONFIRMED REAL but NARROWER than the audit claims. Guard logic verified: assertNonDestructivePatch (caps.ts:95-101) refuses current>80 && proposed<20%*current. The revert (buildRevertEdit, push-snapshots.ts:88-89) sets current_text=original.proposed_text (the grown value) and proposed_text=snapshot.previous_text (the short prior), and ships through executePush. The line-197 guard runs UNCONDITIONALLY for every wix_cms push before routing — there is NO __revert bypass anywhere (the __revert suffix on the id is never inspected). revertPushFromForm (actions.ts:195-203) only console.warns on a ref

## [MEDIUM] fail_loud_gap — `src/domains/lifecycle-eligibility/derive-reason.ts:427-437`
- **Issue:** The decision tree has no branches for the terminal/machine-hygiene statuses `expired`, `not_found_after_7d`, and `push_failed`. These rows carry live_at==null, so they skip branches 1-9 (not dismissed, not wrong_page, target_url present, not in verified_live family, not 'recommended', not 'accepted'&&null, live_at not set) and hit branch 10 fall-through, emitting reason='unknown', blocked_by:null, detail 'status=<x>, live_at=null — uncategorized'. `expired` is set by the live nightly queue-sweeper (queue-sweeper.ts:120, TTL/overflow hygiene, explicitly NOT operator rejection); `not_found_after_7d` is set by the match-runner reconcile path. Both are real, reachable statuses on recommended_edits rows the diagnostic reads unfiltered (page.tsx:233 calls getRecommendedEdits() with no status filter).
- **Customer impact:** Data condition: any tenant whose nightly sweeper has expired stale cards, or whose match-runner has marked an edit not_found_after_7d, or a failed push. These are normal terminal lifecycle states, but the diagnostic collapses them all into reason='unknown' — the bucket the taxonomy documents as 'Should be empty in production. Investigate.' The operator sees a non-empty 'unknown' tally and a wall of 'uncategorized' rows and is steered to investigate non-existent anomalies; expired (cooldown/hygiene) and not_found (terminal) rows are indistinguishable from genuine compute breakage.
- **Fix:** Add explicit terminal branches in deriveLifecycleReason for status==='expired', 'not_found_after_7d', and 'push_failed' (e.g. a new terminal reason or reuse dismissed-family semantics with blocked_by:null and an honest detail like 'auto-expired by nightly sweeper — machine hygiene, not operator rejection'). Add corresponding LifecycleEligibilityReason enum values so they no longer alias to 'unknown', and add derive-reason tests pinning each (currently none of pushed/expired/push_failed/not_found_after_7d/needs_review are covered).
- **Verified (high):** CONFIRMED reachable + wrong. The operator diagnostic (src/app/(shell)/diagnostics/lifecycle-eligibility/page.tsx:233) reads getRecommendedEdits() unfiltered and runs deriveLifecycleReason on every row. The terminal/hygiene statuses all carry live_at==null and are not in any earlier branch's family, so they fall through branches 1-9 into branch 10 (derive-reason.ts:427-437) → reason='unknown', blocked_by:null, 'status=<x>, live_at=null — uncategorized'. types.ts:86-87 documents 'unknown' as "Should be empty in production. Investigate." — so normal terminal states are miscollapsed into the inves

## [MEDIUM] data_corruption_or_loss — `src/lib/connectors/ga4/data-api.ts:104`
- **Issue:** runGa4UrlTrafficReport builds the GA4 Data API runReport body with a hard `limit: 10_000`, no pagination loop, and no `orderBys`. narrowRunReportRows reads `body.rows` but never inspects `body.rowCount` to detect that GA4 returned a truncated page. The (date × pagePath) grain over the 90–180 day window computeRefreshDateRange produces can easily exceed 10,000 rows for a content-heavy tenant (e.g. Iranopedia: hundreds of pages × 90+ days). GA4 then silently returns only the first 10,000 rows (in an unspecified order, since no orderBys is set) and the surplus is dropped with NO log line — unlike the GSC sync (which paginates by 25k) and the Profound sync (which logs `totalRows > rows.length`).
- **Customer impact:** For any GA4-connected tenant whose date×pagePath rows exceed 10k in the refresh window, the nightly GA4 sync persists an arbitrary, incomplete slice of ga4_url_traffic every night. Pages whose rows fell past the 10k cut get no traffic data, so loadGa4PageValuesForTenant under-weights them in the priority score and the Mode A outcome-attribution surface shows missing/under-counted sessions/conversions for real, trafficked pages — a wrong number the operator sees with no warning that data was dropped.
- **Fix:** Add a pagination loop in runGa4UrlTrafficReport: request with `limit` + `offset`, accumulate rows, and continue while returned rows == limit (or until offset >= body.rowCount). At minimum, read body.rowCount and emit a `log.warn` when rowCount > rows.length so the truncation is visible (mirroring the Profound `totalRows > rows.length` pattern). Adding an explicit `orderBys` (e.g. by date) would also make any residual cap deterministic instead of arbitrary.
- **Verified (high):** CONFIRMED. (1) Reachable, not operator-gated: buildRunReportBody (data-api.ts:104) hard-codes limit: 10_000; runGa4UrlTrafficReport (data-api.ts:178) → persistGa4UrlTraffic (persist-url-traffic.ts:195) → syncGa4UrlTrafficForTenant (sync-url-traffic.ts:73) is invoked per-tenant by the NIGHTLY runner scripts/run-scheduled-generation.ts:250-257, alongside the GSC/SEMrush/Clarity/Profound nightly syncs, firing automatically for any tenant with a connected GA4 token+property (no operator action). (2) Genuinely wrong: no pagination loop, no orderBys, and narrowRunReportRows (data-api.ts:119) reads b

## [MEDIUM] data_corruption_or_loss — `src/lib/connectors/callrail/client.ts:86`
- **Issue:** callrailFetchCalls sets `per_page=250` (CallRail's max single page) but never reads the response's pagination metadata (total_pages/page) and never iterates pages. The nightly/operator caller refreshCallRailCalls invokes it with no date range, so it takes the `date_range=recent` branch and pulls only ONE page — at most 250 calls — then groupCallsByUrlDay rolls up just those 250.
- **Customer impact:** For a tenant whose recent CallRail window contains more than 250 calls, every call beyond the first 250 is silently discarded before per-URL/day attribution. loadQualifiedCallCountForUrl then under-reports qualified calls per page, so the Mode A 'M sessions and K calls' outcome figure understates real phone-lead attribution — a wrong, lower number with no indication anything was dropped.
- **Fix:** Page the CallRail calls.json endpoint: read `total_pages`/`total_records` from the JSON body and loop `page=1..total_pages` (or follow until a short page), accumulating `calls`, bounded by a sane max-page cap. Alternatively pass an explicit bounded start/end date window so a single 250-row page reliably covers the period, and log when the page is full (potential truncation).
- **Verified (medium):** Confirmed in code: src/lib/connectors/callrail/client.ts:86 sets per_page=250 with no pagination loop and never reads CallRail's total_pages/total_records/page fields from the JSON body (lines 109-119 just take body.calls). Its only caller refreshCallRailCalls (persist-url-calls.ts:40) is invoked by refreshCallRailMetrics() with no startDate/endDate, so client.ts:89-91 takes the date_range=recent branch -> at most one 250-row page. groupCallsByUrlDay rolls up only those 250 and loadQualifiedCallCountForUrl sums from them, so the customer-facing "M sessions and K calls" figure under-counts for 

## [MEDIUM] tenant_isolation — `src/lib/persistence/dual-write.ts:982` ✅ FIXED
- **Issue:** `syncGuardrailAlertsForUrl` deletes existing alerts with `sb.from('guardrail_alerts').delete().eq('url', url)` — NOT scoped to `tenant_id`. The audit #1 CRITICAL isolation fix (commit 98f3eb7, 2026-06-14) tenant-scoped the sibling `syncGuardrailAlerts` global delete (`.eq('tenant_id', tenantId)`), `dualWriteTruncate`, and `clearAllImportTables`, but did not touch this URL-variant. The function already receives `tenantId` (used for `tenantizeRows` on the insert side) but never applies it to the delete.
- **Customer impact:** When an operator verifies a page fix (verify-action.ts:272 calls this with one URL), the delete removes guardrail_alerts rows for that exact URL string across EVERY tenant before re-inserting only the verifying tenant's fresh alerts. Any other tenant that has an alert for the identical URL loses it silently — the same all-tenant-wipe class flagged CRITICAL in audit #1, residual on this one path. Low reachability today because the three tenants have distinct domains so URL strings don't collide, but it becomes an active cross-tenant data-loss bug the moment two tenants share a domain (e.g. multi-site-on-one-domain or path-scoped tenants).
- **Fix:** Add `.eq('tenant_id', tenantId)` to the delete (and refuse empty tenantId, matching the audit #1 pattern): `await sb.from('guardrail_alerts').delete().eq('tenant_id', tenantId).eq('url', url)`.
- **Verified (high):** CONFIRMED. src/lib/persistence/dual-write.ts:982-985 — syncGuardrailAlertsForUrl deletes with `.delete().eq("url", url)` only, NOT scoped to tenant_id. The sibling syncGuardrailAlerts (lines 913-917) WAS tenant-scoped by audit #1 commit 98f3eb7 (`.eq("tenant_id", tenantId)`), and `git show 98f3eb7` confirms that commit touches dualWriteTruncate, syncGuardrailAlerts, and clearAllImportTables but has zero references to syncGuardrailAlertsForUrl — the URL-variant was left untouched. The function already receives tenantId (used for tenantizeRows on the insert side) but never applies it to the dele

## [LOW] pivot_inconsistency — `src/lib/cost/verify-budget-ledger.ts:217`
- **Issue:** `verifyBudgetLedger` reads every tenant's ledger rows + observation_runs + prompt_answer_observations for the date (deliberately fleet-wide, per the 'tenant-isolation-exempt' comments), but the per-platform report selects the ledger row with `ledgerRows.find((r) => r.platform === pair.ledgerPlatform)` — the FIRST matching row regardless of tenant. The comment claims results are 'reported per-tenant (tenant_id is selected + grouped downstream)', but no per-tenant grouping happens: a single arbitrary tenant's ledger row (spent_usd, prompt_count, chunk_count, last_run_id) is then compared against the SUM of observation counts across ALL tenants (obsCounts and completedRuns are not tenant-filtered).
- **Customer impact:** When two or more tenants poll the same platform on the same UTC day, the operator running `scripts/verify-budget-ledger.ts` gets a misleading verdict: one tenant's ledger numbers are checked against the whole fleet's observation totals, so prompt_count/chunk_count/cost-drift mismatches are reported (false 'warn'/'failed') or real per-tenant discrepancies are masked. The operator trusts this CLI to confirm the spend ledger reconciles with what was actually polled; in multi-tenant it cannot be trusted. Operator-facing diagnostic only (no production caller), hence low severity.
- **Fix:** Iterate the verification per (tenant_id, platform): group ledgerRows by tenant, and tenant-filter the observation_runs and prompt_answer_observations counts (add tenant_id to the obsCounts query / completedRuns filter) so each ledger row is compared against its own tenant's observations. Alternatively, restrict the verifier to a single `--tenant` argument and filter all three reads by it.
- **Verified (high):** Confirmed in src/lib/cost/verify-budget-ledger.ts. The ledger reads (line 136-139), observation_runs reads (167-172), and prompt_answer_observations counts (199-213) are all deliberately fleet-wide (the "tenant-isolation-exempt" comments). But the per-platform report at line 217-218 picks `ledgerRows.find((r) => r.platform === pair.ledgerPlatform)` — the FIRST matching row, ignoring tenant_id. No per-tenant grouping exists anywhere, contradicting the line-134 comment ("reports per-tenant ... grouped downstream"). obsCounts and completedRuns (lines 220-221, 226) are NOT tenant-filtered, so one 

---

## Refuted (verify-first rejections — do NOT act)

- `src/domains/actions/schema-parity-actions.ts:81` — buildSchemaParityActions() bakes a hardcoded, Ritz-Builders-specific literal int → The hardcoded Ritz-specific literal IS real and tenant-blind: src/domains/actions/schema-parity-actions.ts:81 bakes "Menlo-park is the in-account control..." into every tenant's schema-parity action s
- `src/app/(shell)/competitors/page.tsx:112` — The customer-facing /competitors page (NOT operator-gated) calls computeGeoCover → The cross-tenant source mismatch is real but its claimed customer impact does not occur, so the output/behavior is not genuinely wrong. At src/app/(shell)/competitors/page.tsx:109-113, computeGeoCover
- `src/app/(shell)/today-data.ts:2667-2669` — resolveFirstReadingState is invoked with activePromptCount: activePrompts.length → The code defect is factually real but NOT reachable on a real customer path. CONFIRMED FACTS: today-data.ts:2668 passes activePromptCount: activePrompts.length, where activePrompts=getActivePrompts() 
- `src/app/(shell)/diagnostics/page.tsx:1430,1496,1920,1994` — The /diagnostics surface computes computeJourneyCoverage(ctx.promptLibrary) and  → The cited lines (diagnostics/page.tsx:1430,1496,1920,1994) do feed ctx.promptLibrary from the global getPromptLibrary()/getActivePrompts() (lines 249-250), and prompt-library IS a GLOBAL store (store-
- `src/domains/push/push-service.ts:298-316` — The add_schema (Wix Stores seoData) route resolves the target product by queryin → The technical observation is accurate (wixQueryDataItems in src/lib/connectors/wix/client.ts:147-173 issues one non-paginated query capped at limit 1000; the add_schema route in push-service.ts:298-31
- `src/domains/competitor-monitoring/detect-changes.ts:191` — generateCompetitorAlerts → inferTopicFromPath hardcodes Ritz-vertical service pa → The hardcoded Ritz/Bay-Area patterns (detect-changes.ts:191-200) and the "...competes with your services" fallback (line 130) exist exactly as described, and generateCompetitorAlerts IS called on ever
- `src/domains/today/command-center-data.ts:175-208 (loadBrain), surfaced via resolveCommandCenterData:241-251 + resolveCommandCenterFailSoft today-data.ts:2718-2748` — resolveCommandCenterData({tenantSlug}) passes the tenant slug only to loadManife → The code-level diagnosis is accurate but no real customer/operator render path reaches a genuinely-wrong output. CONFIRMED CODE FACTS: loadBrain() (src/domains/today/command-center-data.ts:175) takes 
- `src/domains/learning/change-patterns.ts:222-230` — materializeChangePatterns calls computeChangePatterns(outcomes, changes) on ONLY → The MECHANISM is accurately described, but the audit's reachability premise ("after per-tenant nightly imports run sequentially, the global store reflects only the last-imported tenant") does not exis
- `src/lib/cost/budget.ts:231` — The GLOBAL daily budget cap is computed only from the file ledger: `const global → Mechanism is real but the impact/severity is wrong, so this fails the "genuinely-wrong behavior" gate. Confirmed: budget.ts:231 `globalSpent = sumGlobalToday(readLedger(), date)` reads only the file l
