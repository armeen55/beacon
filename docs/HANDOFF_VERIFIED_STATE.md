# Beacon Verified State

`PRODUCT_TRUTH.md` defines the target. This page records established behavior and the material gaps that still prevent MVP completion. The external [MVP closure register](/Users/armeen/Beacon-MVP-Audit-2026-09-22.md) holds item-level evidence and exact hosted deployment checks.

## Working foundation

- Beacon uses tenant-scoped Supabase persistence, one approved Supabase-scheduled dispatcher, and the five customer surfaces: Today, Visibility, Changes, Results, and Connections. Publishing is manual.
- The canonical research and Decision path stores page snapshots, query comparisons, assignments, proposal versions, and review receipts. Structured OpenAI responses and server validation protect drafts. Paid work is bounded by account limits and a durable ledger.
- A Shipment records a manual change; verification and 7/14/28-day Results have explicit evidence and maturity states. These mechanisms do not by themselves prove a useful delivery stream or a defensible win.

## Last observed production state (September 23–24, 2026)

- Authenticated Changes on exact deployed `5f8819d8` shows **5 Ready**, 28 unfinished drafts and 138 research obligations, paused; Today agrees on five. The fifth is a Safavid internal-link sentence with an exact insertion block, link target, Copy and Mark done, with only its genuine link-list freshness caveat. The other four are the historical Masoud Kimiaei correction, Persian Cat meta, singer title and `/submitrestaurant` meta. Immediate and long-open-tab freshness, more edit families, and repeatable automatic delivery remain open.
- Authenticated held comedian detail names Beacon's current page-read task and shows its source debt without copy or Mark done. The confirmed misleading operator instruction is repaired; this produced no fresh customer copy.
- Authenticated Changes, Today and Results on `b8a7fa63` agree on **138 waiting** for a live check and **21 blocked**, including the Abbasid flag's third-check blocked receipt. Results adds 25 historical records, accounting for all 184 marked-done changes. Older checks remain labeled as needing recheck with their historical readings visible; a later read-only Abbasid page fetch succeeded but did not confirm the applied edit. No verified 28-day win is shown. Broader AI-terminal/confounded taxonomy and long-open-tab parity remain open.
- Inventory held 224 URLs at the audit. A previous pass read back a current-complete 124-word Umayyad flag page. Free canonical readback also made `/appetizers`, Afsharid flag and Early Safavid flag current-complete; both flag candidates still fail copy preflight, so this has not added Ready work. Broader current-page qualification remains open.
- The $4 task ceiling and $4/day account limit stayed unchanged. The September 22–24 task ledger accounts **$0.847587 usage-estimated**. The latest paid Safavid pass made one OpenAI call ($0.004860 usage-estimated; $0.022402 reserved) and zero immediate Ready changes; the later fifth Ready reused its saved review with zero additional provider calls. OpenAI provider-reported charges are unavailable; reservations are not charges. Research remains paused after that zero-output paid pass.
- Local Supabase access works; local OpenAI and DataForSEO credentials are absent. Hosted OpenAI delivered the newly reviewed Persian Cat meta; broader repeated delivery remains unproved. Authenticated Connections shows 3 of 3 configured; GA4 separately shows its last check and data-through date. The external closure register records exact deployed SHA, hosted smoke and authenticated observations.

## Current repair boundary

- Canonical page readback distinguishes complete structure from partial or legacy text, inventory recovery selects unresolved page identities, and per-URL holds leave unrelated pages eligible. Saved structural assignments bind exact source atoms and full rewrites bind the page hash. Source expansion requires support for the actual proposition; changed material sources invalidate checked facts. Runtime binds a free exact page-source continuation to its writer without buying an unrelated read afterward. The complete September 23 `/traditions` capture has a 488-character Nowruz paragraph and a separate `/nowruz` link; the current source-to-placement contract preserves that link outside a paragraph replacement. Deployed `cc02dc35` also refuses the fabricated broad-topic scattered-answer task when that complete page’s opening names explained sections; the saved v16 row has not yet been readjudicated. This is structural proof, not a useful finished edit. Reviewed factual corrections replay at $0, and later technical findings are no longer cut after twelve.
- Deployed `b300cad7` ranks qualified required-atom excerpts by reader-task relevance in the writer's short brief while retaining every exact binding and the full evidence packet. The saved Farsi-numbers draft illustrates the old first-eight problem, but its current complete page already answers the question, so that draft must be held rather than promoted. No fresh usable body change resulted.
- Deployed `b8a7fa63` requires a fresh complete current-page capture before buying review for existing section/answer copy. Deployed `3bc5dcfc` reads coherent, current table rows before judging a missing answer. Production readback of `/farsi-numbers` now finds `7 | ۷ | هفت | haft` as the exact answer at $0; its saved v4 draft remains visible until a capture-bound retirement is implemented, so this did not deliver a fifth change. Changes overlays live verification counts from the persisted ledger while retaining the saved Ready rank. Authenticated Today, Changes and Results agreed on 138 waiting/21 blocked at `b8a7fa63`; broader count freshness remains unproved.
- A current-complete `/submitrestaurant` form capture was stored and read back with its live, unrelated necklace meta. A hosted writer pass reused it but yielded zero Ready: four saved responses recorded the literal old phrase `Discover Iran`, while the preservation ledger expected its generated quoted form `"Discover Iran"`. The first source-key/quote repair reached one hosted reviewer but still produced zero Ready: its verdict cited current page facts absent from that unit's own banked support. The corrected binding then reused the cached writer at $0; one further hosted reviewer saved a source-bound Ready meta with the exact live `before`. Authenticated Changes and Today show it as the fourth finished change; the ranked release took one background refresh to include it. A saved `/traditions` v16 replay uses its exact `persian holidays` question, makes zero writer calls, and owes one exact results-page read; the unrelated `persian culture` query stays separate. The restaurant meta proves one hosted source-to-deliverable path; `/traditions` and repeatable multi-family delivery remain open.
- Deployed `a56fffe6` preserves and projects the substantive Wix singer roster. One $0 canonical readback found `/famous-iranian-singers` current-complete with 49,498 main-HTML characters, 13 `cardTexts`, unchanged content hash and a live “Top 20” title. Deployed `26112098` preserved numeric-loss identities through one hosted OpenAI review. After a later free replay falsely demoted that reviewed title, `5302c3ab` restored it at $0 by reading older reviewed rows against the exact current page, predecessor and reviewed body hash; authenticated Changes shows the same copy-ready SEO-title replacement again. Natural hosted acquisition and repeatable automatic source-to-title production remain unproved.
- Deployed `dbd599e0` routes page, robots and sitemap reads through DNS-validated, socket-pinned manual redirects. Page redirects recheck destination robots; only robots 404/410 permits an absent-file crawl, while 403/5xx/network failure holds it. The exact SHA and authenticated unchanged 1/20/128 Changes state were observed in production. Live robots/redirect behavior and fresh useful output remain unproved.
- Decision and Runtime share the exact completed-claim review key. A stale basis, mismatched proposal owner, forged partial-review digest or already implemented row cannot buy an evaluator call; review save compares against the loaded version. The Persian Cat v287 artifact proves one hosted focused review and guarded promotion; interrupted and changed-source replay remain open.
- Deployed `ebc75297` preserves the banked Safavid review through exact placement replay and reads the actual coherent current content block for link context. Its first $0 production producer pass saved the approved one-sentence review but refused its own readback because JSONB reordered review object keys. Deployed `5ab725e8` uses canonical-value comparison; an independently reviewed guarded same-version promotion saved the fifth Ready row, and a $0 stored-only release rebuilt Changes/Today. Broader automatic and repeated delivery remain unproved.
- The rendered-page transport permits only an optional `www` host redirect for the same paid page while retaining path/query/task/charge checks. Actor and comedian hosted proofs reused saved paid task IDs, stored/read back complete structured captures (976 words/28 H2 and 345 words/11 H2 respectively), and retired false missing-answer work. The bulk Evidence path permits free renewal only when a fresh raw fetch exactly matches a previously complete canonical capture; natural hosted renewal remains unobserved. Persian Cat has a complete 159-word capture. The old v285 meta repeated an unsupported 25–30-year lifespan and was held. Deployed `3763673b` keeps clean writer copy when an empty preservation response interrupts review; production migration `20260923215409` permits an exact paused-tenant Ready-to-review CAS. The historical clean no-lifespan text was requalified against the same current capture, banked v286, freshly reviewed once, and saved Ready v287. Normal producer continuation after unpause remains unproved.
- A live browser read confirms `/persian-kabobs/joojeh-kabob` currently shows a title and ten empty headings with no article body. The source and redraft repair treats that shell as partial, refuses a paid meta writer on stale or unreadable source, and preflights the exact current owner before source acquisition. No Joojeh paid receipt or publishable copy exists; the unchanged incomplete capture is held, while a repeated hosted pass under that hold remains unobserved.
- Manual implementation intent names the selected version and components; verification requires co-captured matching structure and JSON-LD. External title, meta and H1 edits use canonical Shipments, stable event IDs and timezone-qualified instants. Authenticated external-form and live verification behavior remain unproved; an orphan race still needs atomic recording.
- The create-new bridge and complete-page release contract are deployed. A partial source-bound new-page bank can resume its exact topic after another topic ranks higher. The current code holds source-less legacy banks for reconciliation while preserving their copy and review. Six older production banks lack current material/pattern/source identity and exact-topic facts; their `::bundle` IDs do not match the canonical new-page ID, so they have not been durably reconciled. No real complete new page has reached Changes.
- Decision's live-check projection is reflected in authenticated Today, Changes and Results. Runtime has one deadline and publication reserve. The waiting-priority cut is removed; a bounded sitemap continuation and current causal-source gate are deployed. A failed fact-debt count no longer certifies a healthy idle day; the natural hosted outage/recovery case is unobserved. The rank-first owed-review/source repair is deployed, but the 15:28 UTC visit-recovery turn ended paused with zero new Ready. Its saved Farsi review draft is incomplete with ambiguous preservation. Authenticated Changes derives ranking age from the immutable release ID; the Takbir receipt no longer invents 84 attributable missing clicks. Fair-admission/future-wakeup SQL `20260923035156`, complete-new-page release SQL `20260923050956`, and atomic account-owner provisioning SQL `20260923054310` are applied; real multi-tenant/onboarding behavior remains unobserved. AEO history SQL `20260923091301` is service-role-only; a hosted read returned 40 saved answers for each of three tracked questions, but no new AEO customer artifact. Repeated live delivery, whole-page preservation, full queue parity, historical reconciliation, two-archetype onboarding, and sustained manual-use proof remain open.

## Operating constraints

- Reuse saved evidence before provider calls. Do not raise budgets, reset production, discard historical receipts, add another scheduler, or automate publication.
- Run `npm run gate` before a coherent landing. Report the committed and pushed SHA, Vercel build result, exact-SHA hosted smoke, provider charges, and material infrastructure use. The closure register distinguishes local, hosted, live-provider, and pilot evidence.
