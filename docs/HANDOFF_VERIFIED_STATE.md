# Beacon Verified State

`PRODUCT_TRUTH.md` defines the target. This page records established behavior and the material gaps that still prevent MVP completion. The external [MVP closure register](/Users/armeen/Beacon-MVP-Audit-2026-09-22.md) holds item-level evidence and exact hosted deployment checks.

## Working foundation

- Beacon uses tenant-scoped Supabase persistence, one approved Supabase-scheduled dispatcher, and the five customer surfaces: Today, Visibility, Changes, Results, and Connections. Publishing is manual.
- The canonical research and Decision path stores page snapshots, query comparisons, assignments, proposal versions, and review receipts. Structured OpenAI responses and server validation protect drafts. Paid work is bounded by account limits and a durable ledger.
- A Shipment records a manual change; verification and 7/14/28-day Results have explicit evidence and maturity states. These mechanisms do not by themselves prove a useful delivery stream or a defensible win.

## Last observed production state (September 23, 2026; deployed `87933172`)

- The historical Masoud Kimiaei birth-date correction on `/famous-iranian-directors` remains the only Ready change. Settled authenticated Changes shows 1 Ready, 21 unfinished drafts and 126 research obligations, with exact old/new date, placement and two checked sources. Today briefly showed 0 Ready during release refresh, so cross-surface consistency remains open. No fresh copy-ready change came from the live passes; other edit families remain unproved.
- Authenticated Today and Changes both show 139 recorded changes awaiting a live check and 20 that could not be verified. Results adds 25 historical records, accounting for all 184 marked-done changes. No verified 28-day win is shown. Broader AI-terminal/confounded taxonomy and queue admission/count parity remain open.
- Inventory held 224 URLs at the audit. The fifth pass durably stored and read back a current-complete 124-word Umayyad flag page with full main HTML and a new hash; its legacy snapshot lacked structured capture. Its fresh words already answer the assigned question, so no section was invented. Broader current-page qualification remains open.
- The $4 task ceiling and $4/day account limit stayed unchanged. The current-task ledger records 141 reconciled calls and $0.648451 accounted: $0.248251 OpenAI usage-estimated, $0.096300 DataForSEO advances, and $0.303900 DataForSEO provider-reported. Summed pre-call reservation estimates are $4.728798, not charges. A new $0 turn activated exact source debts; the next bought two Instant Pages for $0.003 provider-reported total and delivered zero new Ready. Research is paused after that zero-output paid pass. Both paid metadata receipts contain the actual named lists, but no current structured page capture was saved; the free Raw HTML handoff rejects their `www` host redirect before reading the saved task.
- Local Supabase access works; local OpenAI and DataForSEO credentials are absent. Hosted provider configuration was observed working, but rendered acquisition and new substantive output remain unproved. The external closure register records exact deployed SHA, hosted smoke and authenticated observations.

## Current repair boundary

- Canonical page readback distinguishes complete structure from partial or legacy text, inventory recovery selects unresolved page identities, and per-URL holds leave unrelated pages eligible. Saved structural assignments bind exact source atoms and full rewrites bind the page hash. Source expansion requires support for the actual proposition; changed material sources invalidate checked facts. Runtime saves typed source debt, accepts the canonical Decision basis and now binds an exact page-source continuation to its writer without spending on unrelated drafts. A static page was read back in production, while the two recent JavaScript page reads failed after paid metadata and before structured capture. Complete source-to-useful-copy delivery and sustained progression remain unproved. Reviewed factual corrections replay at $0, and later technical findings are no longer cut after twelve.
- The rendered-page transport now permits only an optional `www` host redirect for the same paid page while retaining path/query/task/charge checks. Its Raw HTML response shape was already correct. Two September 23 paid tasks have complete 10/27-entry metadata and no saved structured page; their free task-ID replay and representative post-JavaScript readback remain unproved.
- Manual implementation intent names the selected version and components; verification requires co-captured matching structure and JSON-LD. External title, meta and H1 edits use canonical Shipments, stable event IDs and timezone-qualified instants. Authenticated external-form and live verification behavior remain unproved; an orphan race still needs atomic recording.
- Decision's live-check projection is reflected in authenticated Today, Changes and Results. Runtime has one deadline and publication reserve. A completed production run held 143 replenishment jobs but retained waiting priority for only 40; the cut is removed in the current code, with hosted tail progression still unobserved. Fair-admission/future-wakeup SQL `20260923035156`, complete-new-page release SQL `20260923050956`, and atomic account-owner provisioning SQL `20260923054310` are applied; their real multi-tenant/onboarding behavior remains unobserved. AEO history SQL `20260923091301` is applied with service-role-only execution; a real hosted read returned 40 saved answers for each of three tracked questions, but no new AEO customer artifact yet. Live copy delivery, whole-page preservation, full queue parity, historical reconciliation, two-archetype onboarding, and sustained manual-use proof remain open.

## Operating constraints

- Reuse saved evidence before provider calls. Do not raise budgets, reset production, discard historical receipts, add another scheduler, or automate publication.
- Run `npm run gate` before a coherent landing. Report the committed and pushed SHA, Vercel build result, exact-SHA hosted smoke, provider charges, and material infrastructure use. The closure register distinguishes local, hosted, live-provider, and pilot evidence.
